# 任务登记与优先级机制

本文是未完成任务、后续优化、阻塞项、基础版遗留问题和 owner 配合事项的统一登记表。已完成和基础版完成任务归档在 `docs/task_register_completed.md`；`docs/implementation_backlog.md` 继续负责长期模块路线图。

最后更新：2026-06-07

## 使用规则

- 所有非平凡 TODO、review 后续项、基础版遗留项、临时绕行方案和需要 owner 配合的数据源事项，都必须登记在本文。
- 不把任务只留在代码注释、聊天记录、临时 checklist 或个人记忆中。
- 后续讨论出的非平凡需求、bug 修复、评分调整、数据链路调整、报告行为调整，必须先在本文新增或更新任务，明确优先级、状态、下一责任方、阻塞项和验收标准，再进入实现。
- 只有不影响系统行为、投资解释、数据流、数据质量、评分、回测或报告输出的纯 housekeeping，才可以不先登记。
- 如果需求上下文太长，不适合塞进任务表格，应该在 `docs/requirements/` 或其他清晰命名的 `docs/` 子目录创建独立 Markdown 文档。任务表只保留简短摘要和文档链接；详细背景、设计决策、开放问题、验收标准、进展记录和状态迁移必须在该文档中维护。
- 如果任务需要拆成多个开发步骤，必须先创建或更新独立需求文档，记录阶段拆解、依赖关系、实施顺序、各阶段验收标准、开放问题和状态迁移；任务表必须引用该文档，而不是把完整计划塞进单个表格单元。
- 使用独立需求文档时，后续开发进展必须同步更新任务表的摘要/状态，以及被引用文档中的详细进展记录。
- 任务状态变化必须和触发变化的代码或文档改动一起更新。
- 任务改为 `DONE`、`BASELINE_DONE` 或 `DROPPED` 时，同一变更必须把整行移动到 `docs/task_register_completed.md`，并同步更新 completed 文档的 `最后更新` 日期。
- `docs/task_register.md` 的“当前任务”只保留仍需推进、验证、owner 输入或外部条件解除的任务；已归档状态不得继续留在当前任务表。
- 归档前必须检查任务 ID 是否已在 `docs/task_register_completed.md` 中存在；如果发现 ID 复用，先修正为唯一 ID，并在备注中记录原 ID 和修正原因。
- 提交前执行 `rg "^\|[^|]+\|[^|]+\|P[0-3]\|(DONE|BASELINE_DONE|DROPPED)\|" docs/task_register.md`；正常情况下不应有结果。若确实需要例外，必须在同一变更中说明原因。
- `BASELINE_DONE` 不等于完成。它表示已有可审计基础闭环，但仍存在数据源、验证、设计或覆盖缺口。
- 被 owner 输入、数据源、API 权限、样本时间窗口或成本限制阻塞的任务，必须明确 blocker 和解除条件。
- 每个任务必须有可验收的完成标准，避免长期停留在模糊“优化”状态。

## 优先级

|优先级|含义|处理原则|
|---|---|---|
|P0|影响正确性、数据质量门禁、未来函数、仓位硬约束或可能导致错误投资解释|优先于新功能；未解决前不能把相关输出当作完整交易结论|
|P1|显著改善评分、回测、数据源可信度、风险控制或审计解释能力|排在近期开发队列前列|
|P2|提升覆盖率、易用性、报告下钻、长期维护或开发效率|在 P0/P1 稳定后推进|
|P3|低风险清理、文档润色、非关键体验优化|有明确收益时再做|

## 状态

|状态|含义|
|---|---|
|PROPOSED|已记录，但需求、设计或验收标准还未充分明确|
|READY|需求和验收标准明确，可以实施|
|IN_PROGRESS|正在实现或验证|
|BLOCKED_OWNER_INPUT|需要项目 owner 提供数据源、权限、样本、业务判断或人工复核|
|BLOCKED_EXTERNAL|受外部 API、供应商、时间窗口、成本或依赖限制|
|BASELINE_DONE|基础版已完成，但仍有明确后续缺口|
|VALIDATING|实现已完成，等待真实数据、回测、审计报告或运行观察确认|
|DONE|验收标准已满足，且相关文档、测试和报告已更新|
|DEFERRED|明确暂缓，且暂缓原因已记录|
|DROPPED|明确不再需要，且原因已记录|

## 当前任务

本节只保留仍需推进、验证、owner 输入或外部条件解除的任务。已完成、基础版完成或不再需要的任务归档在 `docs/task_register_completed.md`。

|ID|领域|优先级|状态|下一责任方|阻塞或下一步|验收标准|备注|
|---|---|---|---|---|---|---|---|
|TRADING-111_to_113|ETF portfolio research/Dynamic v3 Rescue Real Research Evidence Closure|P0|VALIDATING|项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-111_to_113_Real_Research_Evidence_Closure.md`；实现已完成，下一步是 owner 复核真实 real evaluator / promotion pack evidence summary 后决定是否需要更高覆盖真实研究运行或数据源补充|新增 `window-audit`、`weight-path`、`data-provenance` CLI；real sweep / candidate / attribution / promotion artifacts 显示 requested 与 actual date range、daily weight path、data provenance 状态；缺 `actual_evaluation_start/end`、daily weight path、checksum provenance 或 attribution source 时不得 PASS；`BACKTEST_WINDOW_INCOMPLETE`、`MISSING_DAILY_WEIGHT_PATH`、`DATA_PROVENANCE_INCOMPLETE`、`ATTRIBUTION_INCOMPLETE`、`OVERFIT_REVIEW_REQUIRED` 阻断 `promote_candidate` / `production_candidate`；focused tests、ruff、compileall、git diff check 和全量 pytest PASS|2026-06-06: 从 IN_PROGRESS 进入 VALIDATING，原因：已完成 data provenance、window audit、weight path export、candidate attribution PARTIAL/COMPLETE、promotion pack evidence gate、Reader Brief/report registry/artifact catalog/system flow/runbook/README integration；验证通过 ruff、compileall、git diff check、dynamic-v3 focused tests、Reader Brief focused tests 和全量 pytest。|
|TRADING-102_to_110|ETF portfolio research/Dynamic v3 Rescue Stable Real Parameter Iteration Backtest Loop|P0|VALIDATING|系统验证 + 项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-102_to_110_Stable_Real_Parameter_Iteration_Backtest_Loop.md`；baseline 已完成 data audit、small_real profile、injection audit、candidate attribution、true walk-forward selection、overfit control、parameter governance、research index、shadow monitor、Reader Brief shadow monitor 摘要和 promotion pack 集成；下一步 owner 复核 `small_real` 0.65 workflow-smoke constraint-hit 校准、`prices_download_manifest_checksum_missing` warning、candidate attribution `INCOMPLETE` 和 overfit `REVIEW_REQUIRED` 是否可接受|`small_real` profile 已完成 data-audit -> sweep run-profile -> injection-audit -> leaderboard -> candidate report -> candidate attribution -> walk-forward select-run -> overfit -> shadow register -> shadow monitor -> promotion pack；第 13.2 节 validate 命令 PASS；新增 artifact 路径、Reader Brief section、system flow、artifact catalog、report registry、README、operations runbook 和 requirements 文档同步；focused tests、ruff、compileall、git diff --check PASS；尽量全量 pytest|2026-06-06: 新增并进入 IN_PROGRESS，原因：TRADING-101 已完成 real evaluator integration，但真实参数研究仍缺少参数注入验证、数据审计、运行 profile、true walk-forward、overfit gate、shadow monitor 和 promotion review 的稳定闭环。2026-06-06: 实现完成并进入 VALIDATING；small_real `sweep_20260606T105642Z_ae5ae1d8` 跑通 50 candidates，top candidate `aa02bb947cf29885` 为 `review_required`；data audit `d9e434a07019d286`、injection audit `ede86378d2ac235d`、walk-forward selection `51fb80c576c29d2e`、overfit `a275a9f67316668d`、shadow monitor `df1b1d05827db3f9`、promotion pack `f0b0020a1ae42c1c` 均生成且对应 validation PASS；promotion pack 保持 `incomplete` / manual_review_required，不生成 production candidate。|
|TRADING-101|ETF portfolio research/Dynamic v3 Rescue Real Evaluator Sweep Integration|P0|VALIDATING|Codex|详细拆解见 `docs/requirements/TRADING-101_Dynamic_v3_Rescue_Real_Evaluator_Sweep_Integration.md`；已接入 `real_dynamic_v3_rescue` evaluator，同时保持默认 CI / focused tests 走 `tiny_fixture_proxy`|Evaluator mode 仅允许 `tiny_fixture_proxy` / `real_dynamic_v3_rescue`；config 或 CLI 可指定 real mode；candidate parameters 注入 dynamic-v3-rescue real evaluation；`candidate_results.jsonl` 包含 evaluator mode/version、real artifact path、data_quality 和 metrics_source；tiny reports 标记 not-for-investment 且 promotion pack 不可 `promote_candidate`；real mode metrics 来自真实 evaluation artifact，`validate-sweep` 检查 artifact path；small real sweep smoke、focused tests、ruff、compileall、git diff --check 通过；全量 pytest 已尝试但超时|2026-06-06: 新增并进入 IN_PROGRESS，原因：TRADING-093~100 platform MVP 默认 tiny fixture proxy 只能验证平台闭环，不能作为真实 investment evidence；需要把 sweep 平台接入 TRADING-091 price-driven real evaluation pipeline。2026-06-06: 实现完成并进入 VALIDATING；sample real sweep `sweep_20260606T034508Z_ae5ae1d8` 以 `real_dynamic_v3_rescue` / workers=1 跑通 2 candidates 且 `validate-sweep` PASS；focused tests 16 passed，ruff、compileall、git diff --check PASS；`pytest tests -q` 15 分钟超时未返回失败明细。|
|TRADING-093|ETF portfolio research/Dynamic v3 Rescue Parameter Sweep Config Schema|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-093_to_100_Dynamic_v3_Rescue_Parameter_Research_Platform.md`；baseline 已实现参数 sweep 配置、schema validation、stable candidate_id 和 preview CLI；下一步在真实大规模参数研究前复核 owner policy 是否需要收窄或扩展参数空间|`config/etf_portfolio/dynamic_v3_rescue/parameter_sweep_v1.yaml` 存在；所有业务阈值来自 YAML；`aits etf dynamic-v3-rescue sweep-config validate/preview` 可运行；candidate_id 在相同配置、strategy family、code/data manifest 下稳定；空 parameter_space、缺 hard_constraints、缺 scoring 和超过 max_candidates 均 fail closed；focused tests 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：TRADING-092 已确认不应继续手工迭代单个 rescue 版本，需要先建立可审计参数空间和候选 ID contract。2026-06-06: baseline 实现完成并转入 VALIDATING；sample config validation PASS，candidate preview count 5000；验证为 focused tests、legacy dynamic-v3 tests、Reader Brief/report index、ruff、compileall、diff check 和全量 pytest 通过。|
|TRADING-094|ETF portfolio research/Dynamic v3 Rescue Batch Parameter Backtest Runner|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-093_to_100_Dynamic_v3_Rescue_Parameter_Research_Platform.md`；baseline 已实现 sweep run/status/validate、checkpoint、resume、candidate error isolation 和不可变 artifacts；下一步在真实数据/更大 search space 上运行并观察运行时与 error isolation|`aits etf dynamic-v3-rescue sweep run/status/validate` 可运行；生成 sweep_manifest、normalized config、data_manifest、candidates/result/error jsonl、checkpoint、gate summary、leaderboard、report 和 run.log；data quality FAIL 整体停止；candidate crash 写入 error 且不终止 sweep；resume 不重复 completed candidates；latest pointer 不覆盖历史 artifact；tiny fixture tests 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：参数研究需要可恢复批量 runner 和 artifact contract，不能把单次人工回测作为晋级依据。2026-06-06: baseline 实现完成并转入 VALIDATING；sample sweep `sweep_20260606T024119Z_98fa3c81` 完成 5000 candidates、0 errors、12 observe_only、4988 rejected；验证为 focused tests、legacy dynamic-v3 tests、Reader Brief/report index、ruff、compileall、diff check 和全量 pytest 通过。|
|TRADING-095|ETF portfolio research/Dynamic v3 Rescue Candidate Ranking Leaderboard Reports|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-093_to_100_Dynamic_v3_Rescue_Parameter_Research_Platform.md`；baseline 已实现 hard gate、soft score、leaderboard、sweep report、candidate report 和 Reader Brief sweep section；下一步 owner 复核 leaderboard 指标权重和 next actions 文案|`aits etf dynamic-v3-rescue sweep leaderboard/report` 与 `candidate report` 可运行；hard gate 先于 score；score 只排序不覆盖 hard gate；leaderboard 包含 eligible/rejected 摘要、reject reason aggregation、指标分布和 recommended next actions；candidate report 包含参数、gate、metrics、score breakdown、artifact links 和 recommendation；Reader Brief 只读摘要；focused tests 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：参数搜索必须先风险过滤再排序，禁止收益最高者自动胜出。2026-06-06: baseline 实现完成并转入 VALIDATING；sample top candidate `ce9db518659c8d68` 为 observe_only，score 0.487134；验证为 focused tests、legacy dynamic-v3 tests、Reader Brief/report index、ruff、compileall、diff check 和全量 pytest 通过。|
|TRADING-096|ETF portfolio research/Dynamic v3 Rescue Walk-forward OOS Validation|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-093_to_100_Dynamic_v3_Rescue_Parameter_Research_Platform.md`；baseline 已实现 walk-forward run/report/validate 和 OOS summary；下一步用真实样本窗口复核 OOS gate 是否过宽或过窄|`aits etf dynamic-v3-rescue walk-forward run/report` 和 `validate-walk-forward` 可运行；artifact 包含 manifest、windows、candidate results、leaderboard 和 report；top-N 选择来自 sweep leaderboard；报告披露 holdout window、OOS gate 和 recommendation；缺失 artifact fail closed；focused tests 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：候选不得只凭单一全历史回测进入晋级复核。2026-06-06: baseline 实现完成并转入 VALIDATING；sample walk-forward `1c295ba52ed095df` status PASS；验证为 focused tests、legacy dynamic-v3 tests、Reader Brief/report index、ruff、compileall、diff check 和全量 pytest 通过。|
|TRADING-097|ETF portfolio research/Dynamic v3 Rescue Robustness Sensitivity Overfit Diagnostics|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-093_to_100_Dynamic_v3_Rescue_Parameter_Research_Platform.md`；baseline 已实现 robustness run/report/validate、邻近参数敏感性、stress/regime bucket 和 overfit diagnostics；下一步用真实 price-driven evaluator 替换 tiny fixture proxy 后复核 REVIEW_REQUIRED 触发率|`aits etf dynamic-v3-rescue robustness run/report` 和 `validate-robustness` 可运行；artifact 包含 manifest、sensitivity matrix、stress bucket results、regime bucket results、overfit diagnostics 和 report；placeholder 必须标记 REVIEW_REQUIRED，不得伪造 PASS；focused tests 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：候选如果只在极窄参数点表现好，应被标记为敏感或过拟合风险。2026-06-06: baseline 实现完成并转入 VALIDATING；sample robustness `8125b579107a88a4` status REVIEW_REQUIRED、overfit_status REVIEW_REQUIRED，按 fail-closed 设计不伪造 PASS；验证为 focused tests、legacy dynamic-v3 tests、Reader Brief/report index、ruff、compileall、diff check 和全量 pytest 通过。|
|TRADING-098|ETF portfolio research/Dynamic v3 Rescue Shadow Candidate Registry|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-093_to_100_Dynamic_v3_Rescue_Parameter_Research_Platform.md`；baseline 已实现 observe-only registry、register/list/report/validate CLI；下一步 owner 复核是否允许将更长观察期结果纳入 promotion pack|`registry/etf_portfolio/dynamic_v3_rescue_shadow_candidates.yaml` 存在；`shadow register/list/report` 和 `validate-shadow-registry` 可运行；rejected candidate 不可注册；缺 candidate report 或 source_sweep_id fail closed；缺 walk-forward/robustness 时标记 incomplete_observation_basis；observe_only 不允许 production 使用；focused tests 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：初筛候选需要进入长期影子观察，但不得自动 enroll 或生产使用。2026-06-06: baseline 实现完成并转入 VALIDATING；sample shadow registry 注册 `ce9db518659c8d68` 为 observe_only，registry runtime 文件已加入 `.gitignore`；验证为 focused tests、legacy dynamic-v3 tests、Reader Brief/report index、ruff、compileall、diff check 和全量 pytest 通过。|
|TRADING-099|ETF portfolio research/Dynamic v3 Rescue Scheduled Evaluation Artifact Retention Latest Pointer|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-093_to_100_Dynamic_v3_Rescue_Parameter_Research_Platform.md`；baseline 已实现 artifact latest/validate/stale CLI、latest pointer 和 retention/runbook 文档；下一步把生产调度触发纳入统一 daily scheduler 的日期/条件门控路径|`aits etf dynamic-v3-rescue artifacts latest/validate/stale` 可运行；latest pointer 只保存指针；validate 能发现 broken paths；stale detection 可报告过期 artifact；retention policy 来自 YAML 并文档化；operations runbook 覆盖 overnight sweep、resume、weekly shadow report、promotion pack 和 validation；focused tests 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：长期参数研究需要可审计 latest pointer、stale 检查和保留策略，而不是散落手工 artifacts。2026-06-06: baseline 实现完成并转入 VALIDATING；sample latest/validate/stale PASS，stale_count 0；验证为 focused tests、legacy dynamic-v3 tests、Reader Brief/report index、ruff、compileall、diff check 和全量 pytest 通过。|
|TRADING-100|ETF portfolio research/Dynamic v3 Rescue Promotion Review Pack|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-093_to_100_Dynamic_v3_Rescue_Parameter_Research_Platform.md`；baseline 已实现 promotion review/pack/validate 和 Reader Brief promotion section；下一步 owner 复核 reject 结论和后续真实 evaluator/parameter-space 改进方向|`aits etf dynamic-v3-rescue promotion review/pack` 和 `validate-promotion-pack` 可运行；pack 链接 sweep、candidate、walk-forward、robustness、shadow 和 latest real evaluation evidence；缺证据 incomplete/review_required；hard gate、walk-forward、robustness 或 overfit high risk 失败时 reject；完整候选最多自动到 `promote_candidate + manual_review_required`，不得自动产生 `production_candidate`；focused tests 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：observe_only 候选晋级前必须有完整人工复核包和 fail-closed gate。2026-06-06: baseline 实现完成并转入 VALIDATING；sample promotion pack `622ee067448ae09c` status reject，reason `walk_forward_failed`，`production_candidate_generated=false`；验证为 focused tests、legacy dynamic-v3 tests、Reader Brief/report index、ruff、compileall、diff check 和全量 pytest 通过。|
|TRADING-092|ETF portfolio research/Dynamic Rescue Failure Attribution and v0.4 Promotion Review|P0|VALIDATING|项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-092_Dynamic_Rescue_Failure_Attribution_and_v0_4_Promotion_Review.md`；baseline 已基于 TRADING-091 latest real evaluation report 重建 price-driven v0.3 / v0.4 robustness daily paths，对 v0.3 reject 原因完成结构化归因；真实 latest 结论为 v0.3 reject 主因 `constraint_structure_and_overfit_review`，v0.4 不可直接 promote 但应保持 `observe_v0_4_with_constraint_guard`，v0.5 建议 `recommend_v0_5_constraint_guard`；下一步由 owner 复核是否接受优先设计 v0.4-path constraint guard；本任务只能输出 attribution / promotion-review / design-recommendation evidence，不得 approval、shadow enrollment、production baseline mutation、official target weights mutation 或 broker action|`config/etf_portfolio/dynamic_v3_failure_attribution.yaml` 存在且 attribution thresholds / promotion review thresholds / safety fields mandatory；`aits etf dynamic-v3-rescue failure-attribution`、`failure-attribution-report --latest` 和 `validate-attribution` 可运行；报告必须包含 v0.3 rejection attribution、v0.3 vs v0.4 metric delta table、constraint hit failure bucket breakdown、drawdown degradation attribution、robustness overfit `REVIEW_REQUIRED` 原因解释、v0.4 promotion review 和 v0.5 design recommendation（当 v0.4 不可 promote）；需要解释 constraint smooth 是否只降低切换噪音、constraint hit 是否集中于 regime/ticker/rebalance window、v0.3 是否过度平滑导致风险暴露调整不及时、v0.4 优势来自 lower_turnover 还是 exposure path、v0.4 constraint hit 增加能否通过单独 constraint guard 修复；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`；system flow、artifact catalog、operations runbook、README、task register、requirement docs、report registry、Reader Brief 和 tests 同步；focused pytest、ruff、compileall、diff check、`aits etf dynamic-v3-rescue validate-attribution` 和全量 pytest 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：TRADING-091 real evaluation 已判定 v0.3 reject，best v0.3 只把 constraint hits 568 -> 564 且 full overfit status 为 `REVIEW_REQUIRED`；需要进一步归因失败结构，并判断应修 v0.4 constraint guard 还是设计 v0.5。2026-06-06: baseline 实现完成并转入 VALIDATING；真实 latest attribution report `dynamic-v3-failure-attribution-report_893674979fd07e14.json/md`，v0.3 constraint hits 564 vs v0.4 568，hit reduction 4 低于 material threshold 10，smoothing 主要降低 turnover / 切换噪音但未改变 constraint hit 权重结构；drawdown 未成为 reject 主因，overfit status 为 `REVIEW_REQUIRED`；v0.4 review=`observe_v0_4_with_constraint_guard`，v0.5 recommendation=`recommend_v0_5_constraint_guard`；验证为 focused tests 13 passed，`validate-attribution` PASS，`failure-attribution-report --latest` 正常，report index PASS_WITH_WARNINGS，Reader Brief OK，ruff、compileall、diff check 和全量 pytest 2177 passed / 330 warnings 通过。|
|TRADING-091|ETF portfolio research/Dynamic v0.3 Rescue Real Evaluation and Promotion Gate|P0|VALIDATING|项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-091_Dynamic_v0_3_Rescue_Real_Evaluation_and_Promotion_Gate.md`；baseline 已实现真实历史价格驱动评估、v0.3 vs baseline / v0.2 / v0.4 对比、综合分析、Reader Brief 摘要和 promotion gate；真实评估 best candidate 为 `dynamic_regime_overlay_v0_3a_constraint_smooth`，但 gate 判定 `reject`，下一步由 owner 复核是否接受 reject 结论或新开后续 rescue/research task；promotion gate 只能输出 `promote_candidate` / `observe_only` / `reject` 作为人工复核候选资格，不得自动 approval、shadow enrollment、production baseline mutation、official target weights mutation 或 broker action|`config/etf_portfolio/dynamic_v3_real_evaluation.yaml` 存在且 promotion thresholds / safety fields mandatory；真实评估 workflow 必须先运行 `aits validate-data` 等价 cached market / macro data quality gate 并在输出披露 status/report path；`aits etf dynamic-v3-rescue real-evaluate`、`real-report --latest` 和 `validate-real` 可运行；报告必须包含 v0.3 rescue evaluation report、v0.3 vs baseline / v0.2 / v0.4 对比表、constraint hit / false risk-off / drawdown preservation / turnover / static gap 综合分析、过拟合/区间集中度分析、promotion gate 判定和 Reader Brief 面向读者摘要；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`；system flow、artifact catalog、operations runbook、README、task register、requirement docs、report registry、Reader Brief 和 tests 同步；focused pytest、ruff、compileall、diff check、`aits etf dynamic-v3-rescue validate-real` 和全量 pytest 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：TRADING-090 已完成 deterministic v0.3 rescue baseline，但是否具备进入生产候选复核资格必须基于真实历史评估判断；需要验证 v0.3 是否真实降低 constraint hit、改善 dynamic vs static 差距、保持 false risk-off 可控、不明显牺牲 drawdown protection，且没有过拟合某一段市场区间。2026-06-06: TRADING-091A-G baseline 实现完成并转入 VALIDATING；真实 cached-data evaluation `validate_data_status=PASS_WITH_WARNINGS`，报告 `dynamic-v3-real-evaluation-report_922c4bccd3e4dff1.json/md`，best v0.3 `dynamic_regime_overlay_v0_3a_constraint_smooth`，constraint hits 568 -> 564、false risk-off delta 0、turnover 0.7983、dynamic-vs-static gap 0.2005、drawdown degradation vs v0.4 -0.30pp，但 constraint hit rate 仍超 gate 且 full overfit status `REVIEW_REQUIRED`，promotion gate=`reject`；验证为 focused + Reader Brief tests 10 passed，`validate-real` PASS，`real-report --latest` 正常，ruff、compileall、diff check 和全量 pytest 2174 passed / 330 warnings 通过。|
|TRADING-090|ETF portfolio research/Dynamic v0.3 Constraint-Aware Rescue Policy|P0|VALIDATING|项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-090_Dynamic_v0_3_Constraint_Aware_Rescue_Policy.md`；baseline 已实现 candidate-only constraint-aware v0.3 rescue workflow，保留 v0.4 lower_turnover 的低 turnover / false-risk-off 改善并直接约束 `CONSTRAINT_HIT_WORSENED` 与 `DRAWDOWN_PRESERVATION_FAILED`；下一步由 owner 复核 v0.3 candidate report 是否足以进入 TRADING-091；本阶段不得生成 owner approval、shadow enrollment、production baseline mutation、official target weights mutation 或 broker action|`config/etf_portfolio/dynamic_v3_constraint_aware_rescue.yaml` 存在且 safety fields mandatory；root-cause loader、pre-constraint normalization、soft constraint penalty/smoothing、drawdown guardrail、emergency risk-off、v0.3 template generator、candidate batch runner、evaluation report、Reader Brief section 和 `aits etf dynamic-v3-rescue validate` 可运行；候选必须链接 v0.4 review package、constraint decomposition、drawdown review、dynamic rescue report、dynamic robustness report、source policy config 和 data quality report；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`；system flow、artifact catalog、operations runbook、README、task register、requirement docs、report registry、Reader Brief 和 tests 同步；focused pytest、ruff、compileall、diff check、`aits etf dynamic-v3-rescue validate` 和全量 pytest 通过|2026-06-05: 新增并进入 IN_PROGRESS，原因：TRADING-089 review-only v0.4 lower_turnover 已确认 false risk-off、turnover 和 dynamic-vs-static 指标改善，但 constraint hits 增加 143、drawdown preservation 为 -5.95pp，仍为 `review_candidate / not_shadow_ready`；需要构建 constraint-aware v0.3 rescue candidate workflow，在不 approval、不 enroll、不生产写入的前提下给 owner 提供下一阶段复核包。2026-06-05: TRADING-090A-K baseline 实现完成并转入 VALIDATING，原因：新增 dynamic v0.3 rescue policy、`aits etf dynamic-v3-rescue run/report/validate`、constraint/drawdown root-cause loader、pre-constraint normalization、soft constraint penalty/smoothing、drawdown guardrail、emergency risk-off exception、v0.3a-v0.3d candidate templates、candidate comparison、JSON/Markdown evaluation report、Reader Brief `Dynamic v0.3 Rescue` section、report registry/artifact catalog/system flow/runbook/README integration 和 focused tests；验证为 focused + Reader Brief tests 12 passed，`aits etf dynamic-v3-rescue validate` PASS，ruff、compileall、diff check 和全量 pytest 2171 passed / 330 warnings 通过；剩余条件是 owner 复核。|
|TRADING-089|ETF portfolio research/Dynamic v0.2 Robustness and Shadow Review Package|P0|VALIDATING|项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-089_Dynamic_v0_2_Robustness_and_Shadow_Review_Package.md`；baseline 已实现 review-only v0.4 lower_turnover 复核包，明确 constraint hit worsening 和 drawdown preservation failure 是 shadow blockers；真实 latest rescue / robustness artifacts smoke 已生成 package，下一步由 owner 复核是否继续研究；本阶段不得生成 owner approval、shadow enrollment、production baseline mutation、official target weights mutation 或 broker action|`config/etf_portfolio/dynamic_v2_review.yaml` 存在且 safety fields mandatory；v0.4 evidence loader、rescue improvement attribution、constraint hit decomposition、drawdown preservation failure review、regime robustness review、benchmark comparison、shadow review eligibility gate、review package generator、Reader Brief section 和 `aits etf dynamic-v2-review validate` 可运行；v0.4 improvements 必须被识别，但 `constraint_hit_worsened_materially` 和 `drawdown_preservation_negative` 必须使 status 保持 `review_candidate / not_shadow_ready`；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`；system flow、artifact catalog、operations runbook、README、task register、requirement docs、report registry、Reader Brief 和 tests 同步；focused pytest、ruff、compileall、diff check、`aits etf dynamic-v2-review validate`、真实 latest package smoke 和全量 pytest 通过|2026-06-05: 新增并进入 IN_PROGRESS，原因：TRADING-088 显示 v0.4 lower_turnover false risk-off、turnover 和 dynamic vs static 指标显著改善，但 constraint hits 增加、drawdown preservation 为负，不能进入 shadow enrollment；需要生成 review-only robustness/shadow review package 供 owner 复核。2026-06-05: TRADING-089A-K baseline 实现完成并转入 VALIDATING，原因：新增 dynamic v0.2 review policy、`aits etf dynamic-v2-review package/report/validate`、v0.4 evidence loader、improvement attribution、constraint hit decomposition、drawdown preservation blocker、regime robustness、benchmark comparison、eligibility gate、JSON/Markdown review package、Reader Brief `Dynamic v0.2 Review` section、report registry/artifact catalog/system flow/runbook/README integration 和 focused tests；验证为 dynamic related tests 13 passed，`aits etf dynamic-v2-review validate` PASS，ruff、compileall、diff check 通过；真实 latest package smoke 生成 `dynamic-v2-review-package_8405ad6c02dc.json/md`，`status=not_shadow_ready`、`review_status=review_candidate`、`candidate=v0_4_lower_turnover`、`blockers=CONSTRAINT_HIT_WORSENED,DRAWDOWN_PRESERVATION_FAILED,DRAWDOWN_REVIEW_REQUIRED,SHADOW_ENROLLMENT_NOT_ALLOWED`、`shadow_enrollment_allowed=false`；全量 `python -m pytest tests -q` 通过，结果为 2166 passed、330 warnings、耗时 634.90s；剩余条件是 owner 复核。|
|TRADING-088|ETF portfolio research/Dynamic Strategy Failure Diagnostics and Policy Rescue|P0|VALIDATING|系统验证 + 项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-088_Dynamic_Strategy_Failure_Diagnostics_and_Policy_Rescue.md`；baseline 已实现 dynamic failure diagnostics / bounded rescue workflow；下一步在真实 latest failed dynamic package 上运行 `aits etf dynamic-rescue run --latest-failed-package`，由 owner 复核 rescue candidate 是否足以进入 TRADING-089；本阶段不得写 official ETF target weights、production baseline、broker state、owner approval 或 shadow enrollment|`config/etf_portfolio/dynamic_failure_diagnostics.yaml` 存在且 safety fields mandatory；failure dataset builder、Layer 1 signal attribution、false risk-off/on attribution、Layer 2 allocation attribution、turnover/constraint breakdown、v0.2-v0.5 rescue templates、rescue candidate batch runner、rescue evaluation report、Reader Brief section 和 `aits etf dynamic-rescue validate` 可运行；rescue policies 必须 evidence-linked、candidate-only、bounded templates；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`；system flow、artifact catalog、operations runbook、README、task register、requirement docs 和 tests 同步；focused pytest、ruff、compileall、diff check、`aits etf dynamic-rescue validate` 和全量 pytest 通过|2026-06-05: 新增并进入 IN_PROGRESS，原因：真实 dynamic v0.1 shadow review package 未获批准，暴露 trend signal quality zero、high redundancy、false risk-off、high turnover、constraint hit rate 和 underperformance；需要先诊断失败来源，再生成 bounded v0.2 rescue candidates。2026-06-05: TRADING-088A-K baseline 实现完成并转入 VALIDATING，原因：新增 dynamic failure diagnostics policy、`aits etf dynamic-rescue run/report/validate`、failure dataset、Layer 1/Layer 2 attribution、false signal attribution、turnover/constraint breakdown、v0.2-v0.5 rescue templates、rescue batch comparison、evaluation report、Reader Brief `Dynamic Strategy Rescue` section、report registry integration 和 focused tests；当前已验证 `tests/test_etf_dynamic_rescue.py -q` 4 passed，focused ruff/compileall 通过；剩余条件是真实 latest failed package 上的 rescue run 和 owner 复核。|
|TRADING-087|ETF portfolio research/Dynamic Candidate Shadow Review and Enrollment Workflow|P0|VALIDATING|系统验证 + 项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-087_Dynamic_Candidate_Shadow_Review_and_Enrollment_Workflow.md`；baseline 已实现 owner-approved-only dynamic shadow workflow；下一步 owner 复核真实 dynamic shadow package，并在批准后运行 enroll-approved/update/weekly-review 观察；本阶段不得写 official ETF target weights、production baseline、broker state 或自动 shadow enrollment|`config/etf_portfolio/dynamic_shadow.yaml` 存在且 safety fields mandatory；`aits etf dynamic-shadow package --latest --top N`、`approve --package <path> --candidate <candidate_id>`、`enroll-approved --latest`、`update`、`weekly-review` 和 `validate` 可运行；dynamic candidate enrollment 必须要求 dynamic calibration validation、dynamic robustness validation、data quality gate、ops validation、owner approval、`production_effect=none`、`broker_action=none`；未批准或前置 gate 缺失的 candidate 不得 enroll；forward tracking records 包含 dynamic_candidate_return、static_base_return、current_baseline_return、QQQ_return、SPY_return、SMH_return、excess_vs_static、excess_vs_baseline、drawdown、turnover、regime_switch_count、false_signal_count、constraint_hit_count；weekly review 展示 active_shadow / needs_more_data / watch / reject_pending_review / rejected / archived；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`；system flow、artifact catalog、operations runbook、README、task register、requirement docs 和 tests 同步；focused pytest、ruff、compileall、diff check、`aits etf dynamic-shadow validate`、真实 latest-package smoke 和全量 pytest 通过|2026-06-05: 新增并进入 IN_PROGRESS，原因：TRADING-086 已完成真实价格驱动 dynamic robustness baseline 并确认其报告只提供 owner review 前置证据，不自动 enroll；下一优先级是按 roadmap 建立 owner-approved-only dynamic candidate forward shadow observation workflow，补齐 review package、approval、enrollment、tracking、weekly review 和 Reader Brief / evidence visibility。本阶段不允许 production mutation、broker action、automatic promotion 或 auto-enrollment without owner approval。2026-06-05: TRADING-087A-K baseline 实现完成并转入 VALIDATING，原因：新增 dynamic shadow policy、`aits etf dynamic-shadow package/approve/enroll-approved/update/weekly-review/validate`、owner-approved-only enrollment gate、dynamic shadow registry、forward tracking metrics、weekly review statuses、decision journal reference/proposed entries、Reader Brief `Dynamic Shadow Review` section、Strategy Evidence Dashboard `dynamic_shadow` optional source、report registry/artifact catalog/system flow/runbook/README integration 和 focused tests；验证为 19 个相关测试 passed，`aits etf dynamic-shadow validate` PASS，`aits etf evidence-dashboard validate --as-of 2026-06-05` PASS，ruff、compileall、diff check 通过；真实 latest package smoke 生成 `dynamic-shadow-package_4f8d09abd136.json/md`，`status=OWNER_REVIEW_REQUIRED`、`top_candidate=dynamic-candidate-pack_8904048b4dbd`、`ready_after_owner_approval_count=1`、`blocked_count=0`、`automatic_enrollment_allowed=false`；全量 `python -m pytest tests -q` 通过，结果为 2157 passed、330 warnings、耗时 645.98s；剩余条件是真实 owner approval 后的 enrollment/update/weekly observation 运行复核。|
|TRADING-086|ETF portfolio research/Dynamic Strategy Backtest, Robustness, and Attribution Review|P0|VALIDATING|系统验证 + 项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-086_Dynamic_Strategy_Backtest_Robustness_and_Attribution_Review.md`；baseline 已实现真实价格驱动 dynamic robustness workflow；下一步 owner 复核真实 robustness report，并在 TRADING-087 前确认哪些 dynamic candidates 可进入 owner-approved shadow package；本阶段可输出 dynamic/static robustness evidence，但不得写 official ETF target weights、production baseline、broker state 或 shadow enrollment ledger|`config/etf_portfolio/dynamic_robustness.yaml` 存在且 safety fields mandatory；`aits etf dynamic-robustness report --candidate <candidate_id>`、`report --latest` 和 `validate` 可运行；从 cached price data 生成 report 前必须运行 `aits validate-data` 等价门禁并在输出披露 status；比较指标包含 CAGR、total return、max drawdown、volatility、Sharpe、Sortino、Calmar、turnover、downside/upside capture、risk-off drawdown reduction、false risk-off opportunity cost、false risk-on drawdown cost；walk-forward、regime attribution、false risk-on/off、turnover sensitivity、AI/semiconductor contribution、event risk overlay 和 overfit diagnostics 可见；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`；system flow、artifact catalog、operations runbook、README、task register、requirement docs 和 tests 同步；focused pytest、ruff、compileall、diff check、`aits etf dynamic-robustness validate`、真实 cached-data smoke 和全量 pytest 通过|2026-06-05: 新增并进入 IN_PROGRESS，原因：TRADING-085 已完成 two-layer dynamic candidate batch/cache baseline，但其 `dynamic_backtest` 只是 calibration proxy；下一优先级是用真实 ETF price data 完整评估 dynamic candidate 是否优于 static alternatives，并量化 false signal、turnover、regime robustness 和 overfit risk，为 TRADING-087 owner-approved shadow enrollment 提供前置证据。2026-06-05: TRADING-086A-K baseline 实现完成并转入 VALIDATING，原因：新增 dynamic robustness policy、`aits etf dynamic-robustness report --candidate <candidate_id>` / `report --latest` / `validate`、真实价格驱动 daily dynamic path、dynamic/static/current/QQQ/SPY/SMH/best-static comparison、walk-forward、regime attribution、false risk-on/off diagnostics、turnover sensitivity、AI/semiconductor contribution、event risk overlay attribution、overfit diagnostics、JSON/Markdown report、Reader Brief `Dynamic Robustness Review` section、report registry/artifact catalog/system flow/runbook/README integration 和 focused tests；验证为 20 个相关测试 passed，`aits etf dynamic-robustness validate` PASS，ruff、compileall、diff check 通过；真实 cached-data smoke 使用 `dynamic-candidate-pack_8904048b4dbd`、2022-12-01 至 2026-06-03，`validate_data_status=PASS_WITH_WARNINGS`，生成 `dynamic-robustness-report_98f2aa81bb60.json/md`，`status=REVIEW_REQUIRED`、`dynamic_total_return=1.1048362233046518`、`excess_vs_static_base=-0.42559745604739074`、`false_risk_off_count=217`、`false_risk_on_count=40`、`overfit_status=REVIEW_REQUIRED`、`shadow_enrollment_allowed=false`。2026-06-05: 全量 `python -m pytest tests -q` 长跑复核通过，结果为 2153 passed、330 warnings、耗时 641.51s；剩余条件是真实 robustness report 的 owner 复核和 TRADING-087 前的 dynamic shadow input 审查。|
|TRADING-085|ETF portfolio research/Two-Layer Dynamic Candidate Batch Runner and Cache|P0|VALIDATING|系统验证 + 项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-085_Two_Layer_Dynamic_Candidate_Batch_Runner_and_Cache.md`；baseline 已实现 two-layer dynamic candidate batch/cache/ranking workflow；下一步 owner 复核真实 dynamic calibration batch report，并在 TRADING-086 前确认哪些 candidate packs 进入完整 robustness review；本阶段可输出 candidate packs / allocation path / dynamic calibration proxy / ranking report，但不得写 official ETF target weights artifact、production baseline 或 broker state|`aits etf dynamic-calibration run/report/validate` 可运行；candidate pack schema、two-stage search protocol、coarse-to-fine iteration、trend score cache、allocation path cache、dynamic backtest cache、parallel runner visibility、candidate ranking、batch report 和 validation gate 可用；cache keys 包含 data/config/model/engine/policy hashes；ranking 不只按 return，需披露 drawdown、turnover、regime robustness、false signal penalty、data quality 和 overfit risk components；`dynamic_backtest` cache 当前为 calibration proxy，完整 robustness/false-signal diagnostics 属于 TRADING-086；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`；system flow、artifact catalog、operations runbook、README、task register、requirement docs 和 tests 同步；focused pytest、ruff、compileall、diff check 和 `aits etf dynamic-calibration validate` 通过|2026-06-05: 新增并进入 IN_PROGRESS，原因：TRADING-083 Layer 1 和 TRADING-084 Layer 2 baseline 已验证并推送，下一优先级是把二者组合成可批量测试、缓存和排名的 candidate pack 工作流，为 TRADING-086 robustness/attribution review 和 TRADING-087 dynamic shadow review 提供审计输入。2026-06-05: TRADING-085A-J baseline 实现完成并转入 VALIDATING，原因：新增 dynamic calibration policy、`aits etf dynamic-calibration run/report/validate`、candidate pack schema、two-stage search metadata、coarse/local refinement candidate generation、trend score cache、allocation path cache、dynamic calibration proxy cache、cache manifest wrapper、worker/cache visibility、evidence-based ranking、JSON/Markdown report、Reader Brief `Dynamic Calibration Batch` section、report registry/artifact catalog/system flow/runbook/README integration 和 focused tests；验证为 19 个相关测试 passed，`aits etf dynamic-calibration validate` PASS，ruff、compileall 初步通过；真实 latest-trend-report smoke 生成 24 个 candidate packs，`status=PASS`、`data_quality_status=PASS_WITH_WARNINGS`、`top_ranking_score=51.071078`、`calibration_proxy=true`、`full_robustness_backtest_required=true`、warm rerun `cache_hit_rate=1.0`。2026-06-05: 全量 `python -m pytest tests -q` 长跑复核通过，结果为 2149 passed、330 warnings、耗时 620.63s；剩余条件是真实 batch report 的 owner 复核和 TRADING-086 前的 candidate pack input 审查。|
|TRADING-084|ETF portfolio research/Regime-Aware Dynamic Allocation Policy Engine|P0|VALIDATING|系统验证 + 项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-084_Regime_Aware_Dynamic_Allocation_Policy_Engine.md`；baseline 已实现 two-layer dynamic ETF allocation 的 Layer 2 candidate policy engine；下一步 owner 复核真实 dynamic allocation report，并在 TRADING-085 batch/cache 前确认 candidate policy 是否可作为输入；本阶段可输出 candidate target weights / decision records，但不得写 official ETF target weights artifact 或修改 production baseline|`config/etf_portfolio/dynamic_allocation_policy.yaml` 存在且 safety fields mandatory；`aits etf dynamic-allocation decide/report/validate` 可运行；regime-to-weight mapping、trend overlay、event risk cash overlay、exposure/turnover constraints、rebalance decision、decision records、policy registry、report 和 Reader Brief section 可用；所有 thresholds/caps/confirmation windows/overlay sizes 来自 policy config；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`；不得写 `data/etf_portfolio/target_weights.csv`、baseline config、broker action 或 automatic promotion；system flow、artifact catalog、operations runbook、README、task register、requirement docs 和 tests 同步；focused pytest、ruff、compileall、diff check 和 `aits etf dynamic-allocation validate` 通过|2026-06-05: 新增并进入 IN_PROGRESS，原因：TRADING-083 Layer 1 trend signal calibration baseline 已验证并推送，下一优先级是把 calibrated trend/regime/risk scores 映射为 candidate-only dynamic ETF allocation decision records，为后续 TRADING-085 batch/cache 和 TRADING-086 robustness review 提供审计底座。2026-06-05: TRADING-084A-K baseline 实现完成并转入 VALIDATING，原因：新增 dynamic allocation policy、`aits etf dynamic-allocation decide/report/validate`、regime mapping、trend overlay、event risk cash overlay、exposure/turnover constraints、rebalance decision、decision records、policy registry、JSON/Markdown report、Reader Brief `Dynamic Allocation Candidate` section、report registry/artifact catalog/system flow/runbook/README integration 和 focused tests；验证为 14 个相关测试 passed，`aits etf dynamic-allocation validate` PASS，ruff、compileall、diff check 和 sample real CLI smoke 通过。2026-06-05: 全量 `python -m pytest tests -q` 长跑复核通过，结果为 2144 passed、330 warnings、耗时 645.57s；真实 `--latest-trend-report` smoke 显示 `status=PASS`、`selected_regime=semiconductor_leadership`、`rebalance_decision=rebalance_candidate`、`data_quality_status=PASS_WITH_WARNINGS`、`official_target_weights_mutated=false`；剩余条件是真实 dynamic allocation report 的 owner 复核和 TRADING-085 前的 candidate policy input 审查。|
|TRADING-083|ETF portfolio research/Trend Signal Weight Calibration|P0|VALIDATING|系统验证 + 项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-083_Trend_Signal_Weight_Calibration.md`；baseline 已实现 two-layer dynamic ETF allocation 的 Layer 1 trend-analysis information weight calibration；下一步 owner 复核真实 trend calibration report，并在 TRADING-084 dynamic allocation policy 前确认 Layer 1 signal config 的可解释性|`config/etf_portfolio/trend_calibration.yaml` 存在且 safety fields mandatory；`aits etf trend-calibration run/report/validate` 可运行；signal registry、dataset builder、weight config schema、score engine、bounded search runner、bucket forward attribution、redundancy diagnostics、regime stability review、candidate config registry、report 和 Reader Brief section 可用；所有 forward returns 固定 `evaluation_only=true`；从 cached price data 生成 dataset/report 前必须运行 `aits validate-data` 等价质量门禁并在输出披露 status；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 production weights、baseline config、target weights、broker action 或 automatic promotion；system flow、artifact catalog、operations runbook、README、task register、requirement docs 和 tests 同步；focused pytest、ruff、compileall、diff check 和 `aits etf trend-calibration validate` 通过|2026-06-05: 新增并进入 IN_PROGRESS，原因：TRADING-082 已完成工程验证并推送，下一优先级是按 two-layer dynamic ETF allocation roadmap 建立 Layer 1 trend signal calibration workflow，为后续 TRADING-084 dynamic allocation policy 提供独立、可解释、candidate-only 的 trend/regime/risk scores。2026-06-05: TRADING-083A-L baseline 实现完成并转入 VALIDATING，原因：新增 trend calibration policy、`aits etf trend-calibration run/report/validate`、data quality gate enforcement、evaluation-only dataset、score computation engine、bounded preset search、bucket forward attribution、redundancy diagnostics、regime stability review、candidate config registry、JSON/Markdown report、Reader Brief `Trend Signal Calibration` section、report registry/artifact catalog/system flow/runbook/README integration 和 focused tests；验证为 14 个相关测试 passed，`aits etf trend-calibration validate` PASS，ruff、compileall 和 diff check 通过。2026-06-05: 全量 `python -m pytest tests -q` 长跑复核通过，结果为 2141 passed、330 warnings、耗时 644.64s。2026-06-05: 真实 cached-data smoke 通过，`run --start 2022-12-01 --end 2026-06-03 --top 5` 生成 `validate_data_status=PASS_WITH_WARNINGS`、dataset/report/registry 和 `status=PASS`，`report --latest` 显示 `evidence_status=PASS`、`redundancy_risk=high`、`regime_stability=usable`；剩余条件是真实 report 的 owner 复核和 TRADING-084 前的 signal config 审查。|
|TRADING-082|ETF portfolio research/Shadow-Ready Candidate Review and Enrollment Playbook|P0|VALIDATING|项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-082_Shadow_Ready_Candidate_Review_and_Enrollment_Playbook.md`；baseline 已实现 shadow-ready review policy、diagnostics artifact loader、shape-level aggregator/ranking、near-shadow summary、owner review package、approval capture、approved-only enrollment、forward tracking linkage、Reader Brief section 和 validation gate；下一步 owner 复核真实 review package，并在批准后观察 forward shadow tracking|`config/etf_portfolio/shadow_ready_review.yaml` 存在且 safety fields mandatory；`aits etf shadow-review package/approve/enroll-approved/validate` 可运行；diagnostics artifacts、stable shapes 和 near-shadow inputs 可加载并保留 source links；review priority ranking 不只按历史收益，且解释 supporting/blocking evidence；owner approval 是 enrollment 前置条件，未批准 candidate 不得 enroll；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；Reader Brief 只读 latest review package/enrollment 状态；system flow、artifact catalog、operations runbook、README、task register、requirement docs 和 tests 同步；focused pytest、ruff、compileall、diff check、`aits etf shadow-review validate` 和全量 `python -m pytest tests -q` 长跑通过；真实 owner approval 和 forward observation 仍待运行验证|2026-06-04: 新增并进入 IN_PROGRESS，原因：TRADING-081 production-like profiling evidence 显示 cold runtime 已低于 1200s trigger 且 warm/resume cache 问题已解决，下一优先级是把 TRADING-079/080/081 产出的 `shadow_ready` observations 转成 owner-reviewed forward shadow enrollment workflow。本阶段只做 review/enrollment governance，不允许 production baseline replacement、broker action、automatic promotion 或 auto-enrollment without approval。2026-06-04: 从 IN_PROGRESS 改为 VALIDATING，原因：TRADING-082A~K baseline 实现完成；新增 shadow-ready review policy、`aits etf shadow-review package/approve/enroll-approved/validate`、diagnostics artifact loader、shape aggregation/ranking、near-shadow summary、owner approval capture、approved-only enrollment、forward tracking link、Reader Brief `Shadow Candidate Review` section、report registry/artifact catalog/system flow/runbook/README integration 和 focused tests。验证：相关测试 140 passed，文档契约相关测试 19 passed，ruff、compileall、diff check 和 `aits etf shadow-review validate` 通过。2026-06-05: 全量 `python -m pytest tests -q` 长跑复核通过，结果为 2138 passed、330 warnings、耗时 635.12s；TRADING-082 继续保持 VALIDATING，剩余验证条件是 owner 复核真实 review package，并在批准后观察 forward shadow tracking。|
|TRADING-081|ETF portfolio research/Weight Calibration Profiling and Hotspot Baseline|P0|VALIDATING|项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-081_Weight_Calibration_Profiling_and_Hotspot_Baseline.md`；production-like cold detailed profiling 样本已完成，下一步 owner 复核 hotspot evidence；基于当前证据，TRADING-082 优先建议做 Shadow-Ready Candidate Review and Enrollment Playbook，除非 owner 要求更严格 cold-run SLA 或函数级热点 cProfile|`config/etf_portfolio/profiling_policy.yaml` 存在且 safety fields mandatory；`aits etf weight-calibration diagnostics` 支持 `--profile off/summary/detailed/cprofile`、`--profile-output` 和 `--profile-top-n`；profiling report 输出 step timing、candidate hotspot、cache timing、worker timing、optional cProfile top functions、vectorization audit、regime mask assessment、optimization recommendations 和 safety banner；Reader Brief 只读 latest profiling report；`aits etf weight-calibration profiling-validate` fail closed 校验 A-K workflow 和 safety boundary；normal runs 不强制 heavy cProfile；runtime profiling artifacts 位于 ignored reports/data paths；不得做 broker action、production weight mutation、automatic promotion、native rewrite、Numba rewrite 或 optimizer rewrite；pytest、ruff、compileall、diff check、profiling validation gate 和 production-like detailed profiling evidence 通过|2026-06-04: 新增并进入 IN_PROGRESS，原因：TRADING-080 post-fix production evidence 显示 cold run wall runtime 1378.734s、warm 2.570s、resume 2.610s，warm/resume cache hit rate 已达 1.0；重复计算问题已解决，但 cold startup 仍处于 20-30 minute profiling trigger zone。TRADING-081 只做 measurement-first profiling baseline，固定 observe_only/candidate_only/manual_review_required，不允许 broker action、production weight mutation、automatic candidate promotion 或 native kernel rewrite。2026-06-04: TRADING-081A~K baseline 实现完成并转入 VALIDATING，原因：新增 profiling policy、diagnostics profile modes、step/candidate/cache/worker timing、optional cProfile artifacts、vectorization audit、regime mask assessment、profiling report、Reader Brief section、report registry entry 和 `profiling-validate` gate；相关测试 129 passed，ruff、compileall、diff check 和 profiling validation gate 已通过。2026-06-04: production-like detailed cold profiling 样本完成，`validate-data --as-of 2026-06-03` 为 `PASS_WITH_WARNINGS`，full diagnostics detailed runtime 970.143s、`slowest_step=run_searches` 968.956s、24 个 preset/search 组合、240 个 candidate observations、39 个 shadow-ready candidates、`cache_miss_count=6464`、`cache_write_count=12926`；主要耗时为 per-candidate regime robustness / backtest computation，cache write 不是主瓶颈；vectorization audit 建议 Python/NumPy/precompute/batching，不建议 native extension；由于 runtime 低于 1200s trigger 且 warm/resume cache 已解决重复计算，推荐先做 shadow-ready review playbook。|
|TRADING-080|ETF portfolio research/Weight Calibration Cache and Parallel Diagnostics Runner|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-080_Weight_Calibration_Cache_and_Parallel_Diagnostics_Runner.md`；baseline 已实现 cache policy、cache key / manifest schema、price/returns matrix diagnostics hot path、candidate backtest cache、regime robustness cache、diagnostics aggregation cache、miss-path parallel runner、run manifest、performance report 和 validation gate；真实 production cold/warm/resume 已验证，下一步由 owner 判断 cold startup 是否需要进入 TRADING-081 profiling|`config/etf_portfolio/cache_policy.yaml` 存在且 safety fields mandatory；cache key builder deterministic 且强制 `source_config_hash`、`data_hash`、`engine_version` 和 layer-specific inputs；cache manifest 校验 schema/safety/config/data/engine；`aits etf weight-calibration diagnostics` 支持 cache/workers/resume/performance flags 且 cache hit/miss/write 可见；price/returns matrix、candidate backtest、regime robustness、diagnostics aggregation cache hit 不改变 ranking、metrics 或 robustness output；同一 production raw price file 连续加载必须生成稳定 `data_hash`；`aits etf weight-calibration performance-validate` fail closed 校验 cache policy、key/manifest、price/returns cache、candidate/regime/aggregation cache behavior、parallel runner、resume manifest、performance report 和 safety boundary；runtime cache artifacts 位于 ignored `data/cache/weight_calibration/` 或验证用 ignored `data/cache/trading080_validation/`；README、artifact catalog、system flow、operations runbook、requirement docs 和 tests 同步；pytest、ruff、compileall、diff check、默认 policy `performance-validate` 和 production cold/warm/resume evidence 通过|2026-06-04: 新增并实现 cache / parallel / resume / performance baseline，原因：TRADING-079 真实 multi-preset diagnostics 在 24 个 preset/search 组合和 240 个 candidate observations 上外层 30 分钟超时，需要 cache-aware、parallel-capable、resumable、auditable research workflow；本阶段固定 observe_only/candidate_only/manual_review_required，不允许 broker action、production weight mutation 或 automatic promotion。2026-06-04: fixture full cold/warm/resume/partial-config evidence 通过。2026-06-04: production route A 首轮发现 raw price metadata `created_at` 使用当前时间导致 cache `data_hash` 漂移；已改为 deterministic metadata placeholder 并补充 regression test。Post-fix production cold run wall runtime 1378.734s、performance runtime 1375.764s、`worker_count=8`、`shadow_ready_count=39`、`slowest_step=run_searches`；warm run wall runtime 2.570s、performance runtime 0.382s、`cache_hit_rate=1.0`、`cache_hit_count=2`、`cache_miss_count=0`；resume run wall runtime 2.610s、performance runtime 0.390s、`resume_status=resumed`、`cache_hit_rate=1.0`。TRADING-080 重复计算问题已解决；cold startup 超过 20 分钟，可作为是否启动 TRADING-081 profiling 的 owner 复核输入。|
|TRADING-079|ETF portfolio research/Historical Weight Search Diagnostics and Robust Candidate Discovery|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-079_Historical_Weight_Search_Diagnostics_and_Robust_Candidate_Discovery.md`；baseline 实现已完成，下一步运行完整 multi-preset diagnostics（默认六个 presets，可加 `--include-robust-packs`）并由 owner 复核 stable shapes / near-shadow rescue suggestions|`aits etf weight-calibration diagnostics` 或登记的等价 CLI 可运行；报告比较 `last_2y`、`last_3y`、`last_5y`、`post_2022_bear`、`ai_cycle_recent`、`full_available` 的 Top-N、overfit risk distribution、shadow-ready count 和稳定权重结构；输出 `cross_preset_stability_score`、`rank_consistency`、`weight_shape_similarity`、`regime_failure_count`、`near_shadow_candidates` 和 rescue suggestions；新增 bounded defensive / balanced / moderate AI search pack config；报告说明为什么没有 shadow-ready、哪些条件最常失败，以及是否建议扩大窗口、降低 semiconductor cap、提高 cash floor 或调整 objective；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`，不得写 production weights、baseline config、broker action 或 automatic promotion；README、artifact catalog、system flow、task register、requirement docs 和 tests 同步；pytest、ruff、compileall、diff check 通过|2026-06-03: 新增并进入 IN_PROGRESS，原因：真实 TRADING-078 `ai_cycle_recent` run 的 Top 10 全部被 `blocked_by_overfit_risk` 阻断，`enroll-top` 正确 fail closed；owner 要求下一阶段诊断为什么没有 shadow-ready，并通过多 preset 稳定性、near-shadow rescue 和更保守搜索包寻找更可靠的 candidate-only 初始 ETF 权重。2026-06-03: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `weight-calibration diagnostics` CLI、multi-preset diagnostics builder/writer、cross-preset stability score、near-shadow rescue diagnostics、shadow minimum criteria report、三组 lower-semiconductor robust search packs、report registry/artifact catalog/system flow/README integration 和专项测试；全量 pytest（2110 passed）、ruff、compileall、diff check、TRADING-078 usability gate 均通过；小规模真实 smoke `--preset ai_cycle_recent --include-robust-packs --top 3 --max-candidates 4` 成功生成 diagnostics artifacts，完整六 preset 运行和 owner manual review 仍是验证条件。|
|TRADING-078|ETF portfolio research/Historical Weight Calibration Usability Upgrade|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-078_Historical_Weight_Calibration_Usability_Upgrade.md`；TRADING-078A/B/C/D/E/F/G/H/I 已完成；下一步观察真实 recommendation artifacts、shadow enrollment records 和 forward shadow samples|Historical range presets 存在且安全字段 mandatory；`aits etf weight-calibration export-top/comparison/regime-robustness/overfit-explain/enroll-top/recommendation/usability-validate` 或登记的等价 CLI 可运行；candidate comparison 必须包含 current ETF baseline、Buy & Hold QQQ/SPY/SMH、static references 和 top-N candidates；所有 top candidates 包含 benchmark comparison、overfit risk explanation、forward readiness status、blockers/warnings 和 safety fields；shadow enrollment 只允许 safe candidates 且不写 production weights、baseline config、broker action 或 automatic promotion；Reader Brief 只读 latest recommendation report；system flow、artifact catalog、operations runbook、README、task register 和 tests 同步；final usability validation gate、pytest、ruff、compileall、diff check 通过|2026-06-03: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-078 计划，要求把 TRADING-071 dual-track historical calibration 能力升级为 practical candidate selection workflow；本阶段只输出 observe-only / candidate-only historical candidate artifacts、comparison/recommendation/report 和 shadow enrollment suggestion，不允许 production mutation、broker action 或 automatic promotion。2026-06-03: TRADING-078A 完成，原因：新增 historical data range presets config、preset schema/loader/resolver、`search --preset` CLI 接入、search payload `historical_range_preset` metadata、README/artifact catalog/system flow/operations runbook/requirement docs 和专项测试；focused `tests/test_etf_weight_calibration.py` 通过。2026-06-03: TRADING-078B 完成，原因：新增 Top-N export schema/builder/writer/Markdown renderer、`export-top --latest/--run-id --top N` CLI、JSON/CSV/Markdown runtime artifacts、overfit/readiness/blocker/warning fields 和专项测试；focused `tests/test_etf_weight_calibration.py` 通过。2026-06-03: TRADING-078C 完成，原因：新增 candidate comparison schema/builder/writer/Markdown renderer、`comparison --latest/--run-id --top N` CLI、baseline/QQQ/SPY/SMH/static reference/Top-N rows、metric null reasons 和专项测试；focused `tests/test_etf_weight_calibration.py` 通过。2026-06-03: TRADING-078D 完成，原因：新增 regime robustness heatmap schema/builder/writer/Markdown renderer、`regime-robustness --latest/--run-id --top N` CLI、candidate/regime matrix、missing regime warnings 和专项测试；focused `tests/test_etf_weight_calibration.py` 通过。2026-06-03: TRADING-078E 进入 IN_PROGRESS，原因：基于 TRADING-071G diagnostics 增加 human-readable overfit explanation artifact，用于解释 performance concentration、regime fragility、turnover/constraint instability、extreme weights、benchmark dependency 和 forward divergence 风险。2026-06-03: TRADING-078E 完成，原因：新增 overfit explanation schema/builder/writer/Markdown renderer、`overfit-explain --latest/--run-id --top N` CLI、top reasons/supporting metrics/blocking metrics/manual review note 和专项测试；focused `tests/test_etf_weight_calibration.py`、ruff、compileall、diff check 通过。2026-06-03: TRADING-078F 进入 IN_PROGRESS，原因：在现有 forward enrollment ledger 上增加 Top-N/weight_set_id 驱动的 shadow enrollment workflow，并要求只允许 `shadow_ready` candidates、保留 source links、继续禁止 production mutation。2026-06-03: TRADING-078F 完成，原因：新增 `enroll_top_weight_candidates_forward`、`enroll-top --latest/--run-id --top N`、`enroll --latest/--run-id --weight-set <id>`、source-linked enrollment results 和专项测试；blocked/non-shadow-ready candidates fail closed，forward enrollment ledger 固定 production_effect=none / broker_action=none。2026-06-03: TRADING-078G 进入 IN_PROGRESS，原因：汇总 A-F 产物生成 candidate-only initial weight recommendation report，供人工 review 选择 forward shadow candidates。2026-06-03: TRADING-078G 完成，原因：新增 recommendation report schema/builder/writer/Markdown renderer、`recommendation --latest/--run-id --top N` CLI、A-F artifact summary sections、shadow enrollment recommendation 和专项测试；focused `tests/test_etf_weight_calibration.py`、ruff、compileall、diff check 通过。2026-06-03: TRADING-078H 进入 IN_PROGRESS，原因：Reader Brief 需要只读 latest initial recommendation report，展示 Top candidate、shadow action、overfit risk、blocked count、safety 和 detail link。2026-06-03: TRADING-078H 完成，原因：新增 Reader Brief `ETF Initial Weight Candidates` payload/HTML section、`etf_initial_weight_recommendation_report` registry entry、fixture 和专项断言；区块只读 latest recommendation report，缺失时显示 `MISSING`，不运行上游。2026-06-03: TRADING-078I 进入 IN_PROGRESS，原因：需要新增 historical calibration usability validation gate，统一校验 A-H workflow availability、bounded search 和 safety boundary。2026-06-03: TRADING-078I 完成，原因：新增 historical calibration usability validation builder/writer/Markdown renderer、`usability-validate` CLI、registry/docs 和专项测试；实际 CLI status=PASS、failed_check_count=0；父任务转为 VALIDATING，等待真实 selected candidates 的 forward shadow samples 和 owner manual review。|
|TRADING-077|ETF portfolio governance/ETF Allocation Baseline Candidate Review Playbook|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-077_ETF_Allocation_Baseline_Candidate_Review_Playbook.md`；TRADING-077A~J baseline 已实现；下一步观察真实 candidate artifacts、owner manual review cycle、source freshness blockers 和 proposal-only draft 审计链路|`config/etf_portfolio/baseline_review.yaml` 存在且安全字段 mandatory；`aits etf baseline-review eligibility/package/validate` 可运行；evidence dashboard blocked、critical data quality、ops validation failed、forward sample too small、missing journal link、unsafe production_effect 和 broker_action 必须 fail closed；review package 保留 source evidence links；owner decision 是 proposal draft 前置条件；proposal draft 不修改 baseline config / target weights；Reader Brief 只读展示 baseline review status；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；system flow、artifact catalog、operations runbook、README、task register 和 tests 同步；final validation gate、pytest、ruff、compileall、diff check 通过|2026-06-03: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-077 计划，要求建立 ETF allocation candidate 进入 human baseline review 的 evidence-linked playbook；本阶段仅生成 manual review package、decision capture、journal linkage、proposal-only draft 和 outcome tracking，不自动 promotion、不写 production state、不触发 broker。2026-06-03: 从 IN_PROGRESS 改为 VALIDATING，原因：TRADING-077A~J baseline 已实现；新增 baseline review policy、eligibility gate、evidence matrix、review package generator、owner decision capture、decision journal integration、proposal draft generator、outcome tracker、Reader Brief section、report registry/catalog/system-flow/runbook/README integration 和专项测试；`python -m pytest tests -q`（2064 passed）、ruff、compileall、diff check 和 `aits etf baseline-review validate`（PASS）均通过；等待真实 candidate evidence 和 owner manual review cycle 观察。|
|TRADING-076|ETF portfolio research/Strategy Evidence Dashboard|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-076_Strategy_Evidence_Dashboard.md`；TRADING-076A~J baseline 已完成；下一步观察真实 dashboard artifacts、source freshness blockers 和 owner manual review priorities|`config/etf_portfolio/evidence_dashboard.yaml`、strategy evidence dashboard schema/aggregator/cards/ranking/conflicts/manual queue/report/validation gate 均可运行；`aits etf evidence-dashboard report --as-of YYYY-MM-DD` 和 `aits etf evidence-dashboard validate` 输出 JSON/Markdown；每个 evidence card 必须包含 source_module、source_report_path、source_metric、as_of_date、freshness_status、data_quality_status、sample_count_if_applicable 和 validation status；data-quality/freshness/validation context 必须随 evidence 展示；Reader Brief 只读展示 dashboard status 并链接详细报告；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 production weights、不得自动 promotion、不得触发 broker action；system flow、artifact catalog、operations runbook、README、task register 和 tests 同步；final validation gate、pytest、ruff、compileall、diff check 通过|2026-06-03: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-076 计划，要求把 ETF baseline、weight calibration、forward simulation、AI confirmation/attribution、satellite replacement/attribution、parameter review、weekly review、decision journal、data quality、operations health 和 validation gates 汇总为单一研究证据视图；本阶段仅聚合证据和人工复核优先级，不新增信号、不改 baseline、不触发 broker。2026-06-03: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：TRADING-076A~J baseline 已完成；新增 strategy evidence source registry、schema/aggregator/cards/ranking/conflicts/manual queue/report/validation gate、Reader Brief strategy evidence section、report registry/catalog/system-flow/runbook/README integration 和专项测试；验证通过全量 pytest（2057 passed）、ruff、compileall、diff check 和 `aits etf evidence-dashboard validate`（PASS）；实际 2026-06-03 dashboard report 生成成功且 current local overall_status=blocked，原因由缺失/陈旧 source evidence 显式暴露，供 owner review / artifact refresh，不自动绕过。2026-06-05: 从 BASELINE_DONE 重新归类为 VALIDATING，原因：当前任务表只保留仍需推进、验证、owner 输入或外部条件解除的任务；TRADING-076 仍在等待真实 dashboard artifacts、source freshness blockers 和 owner manual review priorities 观察，因此不归档为 completed。|
|TRADING-075|ETF portfolio data quality/Data Quality and Staleness Governance Upgrade|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-075_Data_Quality_and_Staleness_Governance_Upgrade.md`；TRADING-075A~K baseline 已完成；下一步观察真实 daily runs、artifact freshness、Reader Brief links 和 owner manual review，确认治理信号是否稳定支持后续 data-source/policy calibration|`config/etf_portfolio/data_quality.yaml` 存在且可校验；`aits etf data-quality report --as-of YYYY-MM-DD` 和 `aits etf data-quality validate` 可运行；critical required data/artifact/gate failures 阻断 dependent research interpretation，optional missing artifacts 只 warning；data quality report JSON/Markdown 披露 safety banner、run metadata、source links、blocking failures、warnings 和 manual review items；Reader Brief 只读展示 latest data quality status；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不写 production weights、不触发 broker action；system flow、artifact catalog、operations runbook、README 和 tests 同步；final validation gate、pytest、ruff、compileall、diff check 通过|2026-06-03: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-075 计划，要求在 TRADING-074 operations workflow 后新增显式 data quality / staleness governance layer，防止 stale price data、missing bars、config/model drift、incomplete evidence、stale gates/reports 和 Reader Brief 旧链接误导 daily/weekly/attribution/parameter review outputs；本阶段保持 observe-only/candidate-only/manual-review-only，不应用权重、不修改 baseline config、不触发 broker action。2026-06-03: 从 IN_PROGRESS 改为 VALIDATING，原因：TRADING-075A~K baseline 已完成；新增 data quality policy、freshness/coverage/outlier/drift/evidence/gate/report/link checkers、governance report、validation gate、Reader Brief data-quality section、operations daily graph step、report registry/catalog/system-flow/runbook/README integration 和专项测试；验证通过全量 pytest（2046 passed）、ruff、compileall、diff check、`aits etf data-quality validate` 和 `aits etf ops validate`；下一步等待真实运行观察和 owner manual review。|
|TRADING-074|ETF portfolio operations/Portfolio Operations Runbook and Scheduler Review|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-074_Portfolio_Operations_Runbook_and_Scheduler_Review.md`；TRADING-074A operations schedule spec、TRADING-074B daily operations command graph、TRADING-074C weekly operations command graph、TRADING-074D monthly / biweekly operations pipelines、TRADING-074E artifact freshness / dependency checks、TRADING-074F failure severity / blocking policy、TRADING-074G owner review checklist、TRADING-074H scheduler dry-run / simulation、TRADING-074I operations report generator、TRADING-074J Reader Brief operations health section 和 TRADING-074K operations validation gate 已完成；真实 cadence dispatch / owner review 进入 VALIDATING|`config/etf_portfolio/operations_schedule.yaml`、daily/weekly/monthly/biweekly command graph、freshness checker、failure policy、owner checklist、dry-run、operations report、Reader Brief ops health section 和 `aits etf ops validate` 均可运行或按阶段验收；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；dry-run 不执行命令、不写 production state；required stale/missing artifacts 阻断 dependent reports，optional missing artifacts 只 warning；Reader Brief 只读链接 latest operations report；system flow、artifact catalog、operations runbook、README、task register 和 tests 同步；最终 validation gate、pytest、ruff、compileall、diff check 通过|2026-06-03: 新增并进入 IN_PROGRESS，原因：owner 要求继续 TRADING-074 开发计划，把 TRADING-062 至 TRADING-073 的 ETF portfolio research modules 组织成可审计的 daily/weekly/biweekly/monthly operations workflow；本阶段保持 observe-only/candidate-only/manual-review-only，不应用权重、不修改 baseline config、不触发 broker action。2026-06-03: TRADING-074A 完成，原因：新增 operations schedule source config、loader/validator 和专项测试，覆盖 cadence sections、daily required nodes、step ID uniqueness、non-empty commands、dependency references、required expected outputs、safety fields 和 unsafe production effect fail-closed。2026-06-03: TRADING-074B 完成，原因：新增 daily operations command graph schema/builder、required daily node validation、topological ordering、optional attribution node skipping、cycle detection、dry-run-only flags 和 safety propagation。2026-06-03: TRADING-074C 完成，原因：新增 weekly operations command graph、cadence-aware graph builder、external daily dependency disclosure、manual review checkpoint propagation、optional parameter review skip handling、missing weekly review blocking 和 cycle detection。2026-06-03: TRADING-074D 完成，原因：新增 biweekly/monthly operations command graph、slow cadence required node validation、cross-cadence external dependency disclosure、monthly bounded historical weight search slow runtime classification、manual-review propagation、missing required node fail-closed 和 cycle detection。2026-06-03: TRADING-074E 完成，原因：新增 artifact freshness/dependency checker、artifact metadata schema、JSON/text/filename date parsing、dynamic run-id glob resolution、required stale/missing blocking、optional missing warning 和 dependency chain blocking propagation。2026-06-03: TRADING-074F 完成，原因：新增 failure policy evaluator、`etf_operations_failure_policy_v1` report schema、severity / policy summary、critical validation failure blocking、optional missing warning、required stale dependent-step blocking、manual review required event 和 stable serialization。2026-06-03: TRADING-074G 完成，原因：新增 owner checklist builder、`etf_operations_owner_review_checklist_v1` report schema、daily/weekly/monthly/incident owner review template、blocking/warning/manual-review event checklist item、safety/cadence/signoff items、cadence mismatch fail-closed 和 stable incident serialization。2026-06-03: TRADING-074H 完成，原因：新增 scheduler dry-run builder、`etf_operations_scheduler_dry_run_v1` report schema、`aits etf ops dry-run --cadence ... --as-of ...` CLI、direct dispatcher 路由、dry-run JSON writer、planned steps / execution order / blockers / warnings / expected outputs / safety summary，且固定不执行 planned commands、不写 production state。2026-06-03: TRADING-074I 完成，原因：新增 operations health report generator、`etf_operations_health_report_v1` schema、JSON / Markdown writer、`aits etf ops report --cadence ... --as-of ...` CLI、direct dispatcher 路由、safety banner / run metadata / pipeline schedule / graph summary / freshness summary / dependency status / failure-warning / owner checklist / expected next run / source artifacts sections。2026-06-03: TRADING-074J 完成，原因：Reader Brief 新增 `Operations Health` 区块和 `etf_operations_health_report` registry entry，只读 latest operations health report，展示 status、blocking failures、warnings、stale/missing artifacts、next owner review、safety posture 和 detailed report link；缺失时 section-level `MISSING`，不运行上游、不补造状态。2026-06-03: TRADING-074K 完成并进入 VALIDATING，原因：新增 `aits etf ops validate` final gate、`etf_operations_validation_v1` JSON/Markdown artifact、direct dispatcher、report registry/catalog visibility 和专项测试，校验 A-J workflow availability、invalid schedule、required step missing、dependency cycle、freshness blocker/warning behavior、Reader Brief integration 和固定 safety boundary；后续观察真实 cadence run / owner review 证据。|
|TRADING-073|ETF satellite replacement forward attribution/Satellite Replacement Forward Attribution Review|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-073_Satellite_Replacement_Forward_Attribution_Review.md`；TRADING-073A-L baseline workflow 已完成，当前等待真实 forward samples 和 owner manual review 验证 satellite replacement attribution evidence 是否足以支持后续 policy review|`aits etf satellite-attribution build/report/validate` 或登记的等价 CLI 可运行；forward returns 必须固定 `evaluation_only=true` 且只用于 attribution/evaluation；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 production weights、不得自动 promotion、不得触发 broker action；report 必须声明 `ai_after_chatgpt` market regime、actual requested date range、data coverage、sample size、source links、fallback-to-ETF evidence 和 manual review recommendation；Reader Brief、report registry、artifact catalog、system flow、operations runbook、README 和 tests 同步；final validation gate 与全量 pytest、ruff、compileall、diff check 通过|2026-06-03: 新增并进入 IN_PROGRESS，原因：owner 要求继续 TRADING-073 计划，验证 satellite replacement decisions 是否相对 ETF-first exposure 改善 forward performance，并检查 eligible stock outperformance、fallback protection、SatelliteCandidateScore ranking power、risk/drawdown/volatility、role/group 与 AI confirmation interaction；本阶段保持 observe-only/candidate-only/manual-review-only，不应用权重、不修改 baseline config、不触发 broker action。2026-06-03: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 satellite attribution dataset、eligibility bucket/stock-vs-ETF/fallback/score/risk/role/group/AI interaction analysis、evidence scorecard、JSON/Markdown report、Reader Brief `Satellite Attribution Review`、report registry/artifact catalog/system flow/operations runbook/README integration 和 fail-closed validation gate；验证通过全量 pytest（1947 passed）、ruff、compileall、diff check 和 `aits etf satellite-attribution validate`（PASS）；下一步等待真实 forward samples 与 owner manual review。|
|TRADING-072|ETF AI confirmation forward attribution/AI Confirmation Forward Attribution Review|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-072_AI_Confirmation_Forward_Attribution_Review.md`；TRADING-072A-J baseline workflow 已完成，当前等待真实 forward samples 和 owner manual review 验证 AI confirmation attribution evidence 是否足以支持下一阶段 policy review|`aits etf ai-attribution build/report/validate` 或登记的等价 CLI 可运行；forward returns 必须固定 `evaluation_only=true` 且只用于 attribution/evaluation；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 production weights、不得自动 promotion、不得触发 broker action；report 必须声明 `ai_after_chatgpt` market regime、actual requested date range、data coverage、sample size、source links 和 manual review recommendation；Reader Brief、report registry、artifact catalog、system flow、operations runbook、README 和 tests 同步；final validation gate 与全量 pytest、ruff、compileall、diff check 通过|2026-06-02: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-072 计划，要求验证 `AIConfirmationScore` 和 component scores 是否对 QQQ/SPY/SMH/SOXX、candidate overlay 和 satellite candidate forward outcomes 具备 attribution / explanatory value；本阶段保持 observe-only/candidate-only/manual-review-only，不应用权重、不修改 baseline config、不触发 broker action。2026-06-02: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 AI attribution dataset、bucket/component/regime/event/redundancy analysis、evidence scorecard、JSON/Markdown report、Reader Brief `AI Attribution Review`、report registry/artifact catalog/system flow/operations runbook/README integration 和 fail-closed validation gate；验证通过全量 pytest（1938 passed）、ruff、compileall、diff check 和 `aits etf ai-attribution validate`（PASS）；下一步等待真实 forward samples 与 owner manual review。|
|TRADING-071|ETF allocation dual-track weight calibration/ETF Allocation Dual-Track Weight Calibration|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-071_ETF_Allocation_Dual_Track_Weight_Calibration.md`；TRADING-071A/B/C/D/E/F/G/H/I/J/K 已完成；当前等待真实 forward evidence 滚动观察和 owner manual review|`aits etf weight-calibration search/register-candidates/enroll-forward/aggregate-evidence/overfit-diagnostics/generate-proposals/report/validate` 或登记的等价 CLI 可运行；historical search config 必须 bounded 且 safety fields mandatory；candidate initial weights 只能进入 candidate/shadow observation，不得写 production weights、不得触发 broker action、不得自动 promotion 或 baseline replacement；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；report 必须声明 market regime 和实际 requested date range；JSON/Markdown report、validation gate、Reader Brief、report registry、artifact catalog、system flow、operations runbook、README 和 tests 同步；final validation gate 与全量 pytest、ruff、compileall、diff check 通过；真实 forward evidence 和 owner manual review 决定后续是否创建新的 baseline review/promotion 任务|2026-06-02: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-071 计划，要求用 bounded historical backtest search 生成 ETF candidate initial weights，同时用 real forward evidence 防止 overfitting；本阶段保持 observe-only/candidate-only/manual-review-only，不应用 weights、不修改 baseline config、不触发 broker action。2026-06-02: TRADING-071A 完成，原因：新增 bounded historical weight search config、schema/loader、cross-config validation、`aits etf weight-calibration validate-config` 和 tests；验证通过 `python -m pytest tests -q`（1878 passed）、ruff、compileall、diff check 和 config CLI。2026-06-02: TRADING-071B 完成，原因：新增 bounded grid candidate generator、static historical candidate backtest/scoring engine、HistoricalWeightScore components、hard blockers、deterministic ranking、search run JSON/Markdown/CSV outputs、`aits etf weight-calibration search` 和 tests；outputs 明示 candidate cap、market regime、requested date range、baseline/benchmark comparison、`production_weights_mutated=false` 和 `applied_weight_set=null`，不写 production weights、不修改 baseline config。2026-06-02: TRADING-071C 完成，原因：search payload 新增 full-period、walk-forward 和 regime/proxy slice robustness metrics、`robustness.json`、insufficient data handling、stability score 和 ranking robustness component；候选不再只按 full-period backtest 判断。2026-06-02: TRADING-071D 完成，原因：新增 candidate initial weight registry、idempotent registration、safe status validation、`aits etf weight-calibration register-candidates` 和 tests；blocked/rejected candidate 不可转为 `shadow_ready`，registry 不写 production weights。2026-06-02: TRADING-071E 完成，原因：新增 `data/etf_portfolio/weight_calibration/forward_enrollments.json`、shadow-style enrollment record、forward tracking link、safe/idempotent `aits etf weight-calibration enroll-forward` 和 tests；blocked/needs_more_data/rejected candidates 不可 enroll，ledger 不写 shared experiment shadow registry、target weights、baseline config 或 broker state。2026-06-02: TRADING-071F 完成，原因：新增 backtest-vs-forward evidence aggregation、return/drawdown/turnover/stability gap metrics、evidence statuses、source links、JSON/Markdown writer、`aits etf weight-calibration aggregate-evidence` 和 tests；缺少 forward row 或 forward days 不足时输出 `needs_more_forward_data`，不补造 conclusion。2026-06-02: TRADING-071G 完成，原因：新增 overfit diagnostics、8 个 component risk scores、low/medium/high/critical risk band、JSON/Markdown writer、`aits etf weight-calibration overfit-diagnostics` 和 tests；diagnostics 只标记 risk，不 promotion、不应用 weights。2026-06-02: TRADING-071H 完成，原因：新增 candidate weight proposal schema、allowed/disallowed proposal type validation、evidence-linked proposal routing、JSON/Markdown writer、`aits etf weight-calibration generate-proposals` 和 tests；strong historical + forward evidence creates `propose_manual_baseline_review`，insufficient forward evidence defers，high overfit risk 或 bad forward evidence rejects，unsafe proposal types fail closed。2026-06-02: TRADING-071I 完成，原因：新增 dual-track calibration report schema、summary/status classifier、manual review package、source links、JSON/Markdown writer、`aits etf weight-calibration report --latest` 和 explicit-path CLI/tests；report 只汇总 evidence/proposals，不应用权重、不修改 baseline。2026-06-02: TRADING-071J 完成，原因：report registry 新增 `etf_weight_dual_track_calibration_report`，Reader Brief 新增 `ETF Weight Calibration` 区块，展示 search pack、top candidate、forward status、overfit risk、candidate status、manual review proposal count、safety posture 和 detail link；缺失时显示 MISSING，不运行上游。2026-06-02: TRADING-071K 完成并进入 VALIDATING，原因：新增 `aits etf weight-calibration validate`、validation schema、deterministic in-memory sample pipeline、JSON/Markdown validation artifact、report registry entry 和 tests；gate 校验 bounded config、workflow modules、Reader Brief visibility、unsafe proposal type blocking、evidence-linked proposals、`production_effect=none`、`broker_action=none`、`manual_review_required=true`，失败时 fail closed。父任务保持 VALIDATING，直到真实 forward evidence 和 owner manual review 支持下一阶段 baseline review。|
|TRADING-070|ETF allocation parameter review/ETF Allocation Parameter Review from Forward Evidence|P0|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/TRADING-070_ETF_Allocation_Parameter_Review_From_Forward_Evidence.md`；TRADING-070A~I baseline workflow 已完成，当前等待真实 forward evidence、weekly review 和 owner manual review 继续验证 proposal quality|`aits etf parameter-review aggregate/report/run/validate` 可运行；所有 proposal 必须链接 forward dashboard、weekly review、decision journal、experiment report、candidate gate 和 validation gate evidence；缺少 forward evidence 输出 `needs_more_data` / `INSUFFICIENT_FORWARD_EVIDENCE`；输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 production weights、不得触发 broker action、不得自动 promotion 或 baseline replacement；JSON/Markdown report、Reader Brief、report registry、artifact catalog、system flow、operations runbook 和 tests 同步|2026-06-02: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-070 计划，要求把 TRADING-064 historical experiment evidence、TRADING-065 forward dashboard、TRADING-068 weekly review 和 TRADING-069 human decision journal 连接成 proposal-only ETF allocation parameter review workflow；本阶段保持 observe-only/candidate-only/manual-review-only。2026-06-02: TRADING-070A 完成，原因：新增 parameter review evidence schema、required fields、source link validation、nullable metric reason validation、date range validation 和 safety field enforcement；验证通过目标 pytest、ruff 和 diff check。2026-06-02: TRADING-070B 完成，原因：新增 `aits etf parameter-review aggregate --as-of YYYY-MM-DD`、parameter review evidence aggregation JSON/Markdown、source path preservation、missing required forward evidence `needs_more_data` 处理、optional source warning、system flow/artifact catalog/runbook/README 同步和 tests；输出固定 observe-only/candidate-only/manual-review-only，不写 production weights、不触发 broker action、不修改 baseline config。2026-06-02: TRADING-070C 完成，原因：新增 candidate evidence comparison payload、baseline/benchmark/backtest/weekly/journal comparison metrics、status classifier、pilot comparison policy 和 tests；comparison 只作为后续 proposal/report/manual review 输入，不写 production weights、不触发 broker action。2026-06-02: TRADING-070D 完成，原因：新增 decision journal evidence linker、support/conflict status、confidence/rationale/follow-up aggregation 和 tests；linker 只读 journal report，不修改 journal state、shadow registry、production weights 或 broker state。2026-06-02: TRADING-070E 完成，原因：新增 proposal-only parameter change proposal generator、allowed proposal type enforcement、supporting/blocking evidence、risk summary 和 unsafe proposal validation；proposal 不应用参数变更、不写 production weights、不触发 broker action。2026-06-02: TRADING-070F 完成，原因：新增 deterministic weighted proposal scorecard、hard blockers、missing journal link block/warn policy 和 tests；scorecard 只决定 manual-review eligibility/needs_more_data/blocked/rejected/continue_shadow，不应用参数变更。2026-06-02: TRADING-070G 完成，原因：新增 `aits etf parameter-review report/run`、parameter review JSON/Markdown report、manual review requirements、next steps、report registry/artifact catalog/system flow/runbook/README 同步和 tests；报告只展示 evidence/proposal package，不应用参数变更。2026-06-02: TRADING-070H 完成，原因：Reader Brief 新增 `ETF Parameter Review` 区块，只读 latest parameter review report，展示 status、candidate/proposal counts、main reason、safety posture 和 detail link；缺失 report 显示 MISSING，不运行上游、不写 production weights 或 broker state。2026-06-02: TRADING-070I 完成，原因：新增 `aits etf parameter-review validate`、parameter review validation JSON/Markdown、report registry/artifact catalog/system flow/runbook/README 同步和 tests；gate 校验 proposal-only workflow、Reader Brief visibility、source links、unsafe proposal blocking 和固定 safety fields，失败时 fail closed。父任务转入 VALIDATING，等待真实 forward evidence 和 owner review。|
|TRADING-069|ETF 组合人工决策日志/Portfolio Decision Journal and Human Review Notes|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-069_Portfolio_Decision_Journal.md`；TRADING-069A-H baseline implementation 已完成：schema、manual review CLI、weekly review action linking、candidate state proposal、journal report、Reader Brief summary、analytics 和 validation gate；下一步观察真实 weekly decision entries 和 owner review|`aits etf decision-journal add/update/list/remove/report/analytics/propose-state-updates/validate` 可运行；journal entries 必须链接 TRADING-068 weekly review 和 source action item；输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；阻断 `place_order`、`enable_broker_action`、`promote_to_production_without_governance`；不写 production weights、不触发 broker action、不自动 promotion/rejection；JSON/Markdown/HTML report、Reader Brief、report registry、artifact catalog、system flow、operations runbook 和 tests 同步|2026-06-02: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-069 计划，要求把 TRADING-068 weekly review 的人工 review notes、accepted/rejected recommendations、rationale 和 follow-up actions 持久化为结构化 Portfolio Decision Journal，用于长期 traceability、未来 calibration 和 decision analytics；本阶段保持 observe-only/candidate-only/manual-review-only。2026-06-02: TRADING-069A 完成，原因：新增 decision journal schema、required fields、decision_status enum、confidence bounds、安全字段和 disallowed action validation；验证通过目标 pytest、ruff 和 diff check。2026-06-02: TRADING-069B/C 完成，原因：新增 `aits etf decision-journal add/update/list/remove`、weekly review action item linking、source action/evidence snapshot 和 removal audit trail；验证通过目标 pytest、ruff 和 diff check。2026-06-02: TRADING-069D-H 完成并转入 VALIDATING，原因：新增 candidate state proposal、journal JSON/Markdown/HTML report、analytics、Reader Brief `Portfolio Decision Journal`、report registry/artifact catalog/system flow/operations runbook 和 validation gate；验证通过目标 pytest、report/docs tests、ruff、diff check 和 `python -m ai_trading_system.cli etf decision-journal validate`（PASS）；所有输出固定 observe-only/candidate-only/manual-review-only，不写 production weights、不触发 broker action、不自动修改 candidate state。|
|TRADING-068|ETF 组合周度复核/ETF Portfolio Weekly Review Workflow|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-068_ETF_Portfolio_Weekly_Review_Workflow.md`；TRADING-068A~J baseline implementation 已完成：aggregator、portfolio summary、shadow/AI/satellite/risk sections、manual action safety、weekly report、Reader Brief navigation 和 final validation gate；下一步观察真实 weekly artifacts 和 owner review|`aits etf weekly-review generate --as-of YYYY-MM-DD` 生成 JSON/Markdown；`aits etf weekly-review validate` PASS；输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；只读 existing artifacts，不写 production weights、不触发 broker action、不自动 promotion/rejection；source report traceability、missing data、manual review action items、Reader Brief/report registry/system flow/artifact catalog/docs/tests 同步；全量 pytest、ruff、compileall、diff check 通过|2026-06-02: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-068 ETF Portfolio Weekly Review Workflow 计划，要求把 ETF baseline、forward shadow candidates、AI confirmation、satellite replacement、risk/watchlist 和 validation status 汇总为单一 weekly human-review package；本阶段不新增 live trading、broker action、automatic production promotion 或 baseline replacement。2026-06-02: TRADING-068A~J 完成并转入 VALIDATING，原因：新增 `aits etf weekly-review aggregate/generate/run/validate`、weekly review JSON/Markdown、validation gate、Reader Brief `Weekly Portfolio Review`、report registry/artifact catalog/system flow/operations runbook 和测试；验证通过 `python -m pytest tests -q`（1811 passed）、ruff、compileall、diff check、`python -m ai_trading_system.cli etf weekly-review validate`（PASS）和 `python -m ai_trading_system.cli etf weekly-review generate --as-of 2026-05-29`；所有输出固定 observe-only/candidate-only/manual-review-only，不写 production weights、不触发 broker action、不允许 automatic promotion/rejection。|
|TRADING-065|ETF forward observation/ETF Forward Simulation Review Dashboard|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-065_ETF_Forward_Simulation_Review_Dashboard.md`；TRADING-065A~I baseline implementation 已完成：shadow state hardening、daily forward update、dashboard、rolling metrics、lifecycle、weekly review、Reader Brief、watchlist 和 final validation gate|`aits etf forward validate` PASS；forward outputs 固定 `observe_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；forward returns 仅存在于 `evaluation_only=true` 评估产物；不写 production weights、不触发 broker action、不自动 promotion；JSON/Markdown reports、Reader Brief、report registry、scheduled daily ops、artifact catalog、system flow 和 tests 同步；全量 pytest、ruff、compileall、diff check 通过|2026-06-01: 新增并进入 IN_PROGRESS，原因：TRADING-064 已完成历史实验候选 selection/enrollment，下一步需要真实 forward shadow observation dashboard；本阶段只观察候选相对 baseline/QQQ/SPY/SMH 的表现，不改变 production ETF allocation。2026-06-01: TRADING-065A~I 完成并转入 VALIDATING，原因：新增 `config/etf_portfolio/forward_simulation.yaml`、hardened shadow registry schema、`aits etf forward update/dashboard/weekly-review/watchlist/validate`、Reader Brief forward simulation section、report registry/scheduled daily ops integration 和 docs；验证通过 `python -m pytest tests -q`（1747 passed）、ruff、compileall、diff check 和 `python -m ai_trading_system.cli etf forward validate`（PASS）。|
|TRADING-063|ETF 组合可信度/Portfolio Credibility Validation & Calibration|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/etf_portfolio_credibility_validation_2026-06-01.md`；TRADING-063A~K 已完成，final credibility gate PASS；下一步观察真实 ETF shadow-evaluation artifacts 和后续 owner review|TRADING-063A~K 均实现并逐项验证；最终 `python -m pytest -q`、`python -m ruff check config src tests scripts docs`、`python -m compileall -q src tests scripts`、`git diff --check` 通过；credibility gate PASS；P2/live 模块保持 candidate-only / observe-only；`production_effect=none`；无 broker action；runtime artifacts 保持不纳入源码；docs、task register 和 system flow 同步|2026-06-01: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-063+ ETF Portfolio Allocation System Credibility Validation Development Plan，要求继续开发、验证并提交改动；本阶段不新增 live trading 能力，只建立可信度验证与校准闭环。2026-06-01: TRADING-063A 完成并归档，原因：runtime artifact ignore policy、ETF fixture 目录和 artifact/system-flow 文档已完成，验证通过 `git diff --check`、`python -m pytest tests -q`（1630 passed）和 `python -m ruff check config src tests scripts docs`。2026-06-01: TRADING-063B 完成并归档，原因：B001-B008 benchmark registry、static portfolios、SOXX benchmark、risk-off cash switch、benchmark common comparison schema、报告/文档和测试已完成；验证通过 `aits etf backtest run --config config/etf_portfolio/backtest.yaml --fast`、`python -m pytest tests -q`（1633 passed）、ruff、compileall 和 diff check。2026-06-01: TRADING-063C 进入实现，原因：需要把 ETF 信号日、特征可见日、执行日、simulation delayed evaluation 和 report decision section 的 no-lookahead 契约变成可测试的 validation helper。2026-06-01: TRADING-063C 完成，原因：no-lookahead validation helper 已接入 backtest、simulation ledger 和 daily brief；测试覆盖 timing/future-field/report leakage；真实 ETF backtest smoke 通过；全量验证通过 `python -m pytest tests -q`（1639 passed）、ruff、compileall 和 diff check。2026-06-01: TRADING-063D 进入实现，原因：需要用 deterministic toy prices 证明 NAV、交易成本、asset contribution、drawdown 和 next-close execution timing；当前 backtest timing 将同步修正为 `signal_date < execution_date < return_date`。2026-06-01: TRADING-063D 完成，原因：toy fixture/accounting tests 已覆盖 NAV、cash、cost、contribution、drawdown、bad weights、rebalance delta threshold 和 next-close execution；backtest/benchmark timing 已改为 `signal_date < execution_date < return_date`；真实 ETF backtest smoke 通过；全量验证通过 `python -m pytest tests -q`（1645 passed）、ruff、compileall 和 diff check。2026-06-01: TRADING-063E 进入实现，原因：需要执行 `max_rebalance_trade_weight` / `max_daily_turnover` 配置约束，并把 constraint diagnostics 扩展为可审计 before/after/reason/severity 结构。2026-06-01: TRADING-063E 完成，原因：allocation 已执行 max rebalance trade / daily turnover caps，target weights 输出结构化 constraint diagnostics，新增风险约束测试覆盖 asset/sleeve/equity/cash/rebalance/turnover normalization 与 signal 风险惩罚；真实 ETF backtest smoke 通过；全量验证通过 `python -m pytest tests -q`（1652 passed）。2026-06-01: TRADING-063F 进入实现，原因：需要把 backtest weights/trades 原始表汇总为 allocation stability JSON/Markdown/CLI diagnostics，覆盖 turnover、weight delta、regime transition、constraint hit rate 和 exposure distribution。2026-06-01: TRADING-063F 完成，原因：新增 allocation stability diagnostics 模块、backtest summary/metrics/Markdown 集成、`stability_diagnostics.json/md` artifact 和 `aits etf backtest diagnostics --latest`；测试覆盖 stability schema、turnover、weight delta、regime transition、constraint hit rate、exposure distribution 和 CLI artifact 写出；真实 ETF backtest/diagnostics smoke 通过；全量验证通过 `python -m pytest tests -q`（1656 passed）。2026-06-01: TRADING-063G 进入实现，原因：需要把 simulation ledger 改为 decision/evaluation record 分层，防止 delayed forward evaluation 覆盖原 decision-time record。2026-06-01: TRADING-063G 完成，原因：simulation ledger 已分离 decision/evaluation rows，decision rows 保留 snapshot/config hash 与 observe-only/production-effect 边界且无 future values，evaluation rows 按 as-of deterministic upsert 并固定 evaluation_only=true；simulation report/daily brief summary 区分两类样本；全量验证通过 `python -m pytest tests -q`（1657 passed）。2026-06-01: TRADING-063H 进入实现，原因：需要统一 ETF backtest 跨策略比较 metrics schema、月度收益表、null reason 和 Reader Brief/report 可见性。2026-06-01: TRADING-063H 完成，原因：backtest summary/metrics/Markdown 已输出 standardized metrics、monthly returns、primary benchmark excess 和 null reasons，Reader Brief 只读展示 ETF Backtest Summary；真实 ETF backtest/report index smoke 通过；全量验证通过 `python -m pytest tests -q`（1664 passed）、ruff、compileall 和 diff check。2026-06-01: TRADING-063I 进入实现，原因：需要把 ETF daily brief 从数字摘要升级为可审计的权重变动解释报告，补齐 safety banner、drivers、benchmark context、P2/live note、actionability note 和 future-field 防泄漏测试。2026-06-01: TRADING-063I 完成，原因：ETF daily brief 已输出 safety banner、AI regime/range、target/previous/delta、结构化正负 driver、constraint diagnostics、benchmark context、simulation status、P2/live candidate-only note 和 actionability note；旧 target weights artifact 缺结构化 diagnostics 时显式标记 unavailable；测试覆盖附加 future/evaluation 字段不进入 decision sections；真实 `aits etf report daily --date latest` smoke 通过；全量验证通过 `python -m pytest tests -q`（1664 passed）、ruff、compileall 和 diff check。2026-06-01: TRADING-063J 进入实现，原因：需要新增 ETF parameter governance policy、candidate promotion gates、stable governance summary schema，并 fail-closed 阻断缺 benchmark、样本不足、turnover 过高、no-lookahead 失败和 P2/live self-promotion。2026-06-01: TRADING-063J 完成，原因：新增 ETF parameter governance policy/config、candidate promotion gate、`aits etf governance summary` JSON/Markdown、report registry/artifact catalog/system flow 接入；测试覆盖缺 benchmark、样本不足、turnover 过高、no-lookahead 未通过、P2/live self-promotion、全部 gate 通过和 schema stability；真实 `aits etf governance summary --date 2026-06-01` smoke 输出 `NO_CANDIDATE` 且 `production_effect=none`。2026-06-01: TRADING-063K 进入实现，原因：需要单一 credibility gate 聚合 A~J 验证项、P2/live safety 和 JSON/Markdown 输出，作为 TRADING-063 完成前的最终 fail-closed 门禁。2026-06-01: TRADING-063K 完成，原因：新增 `aits etf credibility validate`，聚合 runtime artifact hygiene、benchmark suite、no-lookahead、toy accounting、risk constraints、allocation stability、simulation schema、backtest metrics、brief explainability、parameter governance 和 P2/live safety；真实 `aits etf credibility validate --date 2026-06-01` PASS，`production_effect=none`、`broker_action=none`；目标测试覆盖 all-pass 与关键 failure；TRADING-063A~K 实现阶段完成。|
|TRADING-064|ETF 组合校准/Calibration Experiment Pack|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/TRADING-064_ETF_Portfolio_Calibration_Experiment_Pack.md`；TRADING-064A/B/C/D/E/F/G/H/I/J 已完成，final experiment validation gate PASS；下一步观察真实 experiment/shadow artifacts 和 owner review|TRADING-064A~J 逐项完成并验证；`etf_calibration_v1` pack 存在；batch experiment runner、comparison report、ranking policy、candidate gate、shadow enrollment、weekly review、Reader Brief/report index 集成和 final validation gate 均可运行；全量 pytest、ruff、compileall、diff check 和实际 experiments validation gate 通过；所有输出固定 `observe_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；runtime experiment artifacts 不提交|2026-06-01: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-064 ETF Portfolio Calibration Experiment Pack 计划并要求继续依次实现、验证和推送；本阶段只建立受控 ETF 校准实验与 shadow observation 基础，不新增 live trading、broker action 或 automatic production promotion。2026-06-01: TRADING-064A 完成，原因：新增 controlled experiment registry config、loader/validator、测试和文档；registry 覆盖 first-matrix 16 个实验，强制安全字段、唯一 ID、base weights 合计、base config ref 和 override surface。2026-06-01: TRADING-064B 完成，原因：新增 `etf_calibration_v1` controlled pack config、pack loader/validator、测试和文档；pack 引用 16 个安全 experiment，声明 ranking/promotion policy，避免 uncontrolled combinatorial search。2026-06-01: TRADING-064C 完成，原因：新增 batch experiment runner、CLI `--pack/--experiment --start --end`、manifest/results/benchmark/metrics/diagnostics runtime outputs、失败隔离和 unsafe fail-closed 测试；旧 `--config` P1 registry 行为保持兼容。2026-06-01: TRADING-064D 完成，原因：新增 batch run comparison JSON/Markdown report、CLI `compare --run-id/--latest`、baseline/benchmark context、metrics/risk/turnover/stability tables、constraint/warning summary 和 missing-metrics null reason；ranking 明确等待 TRADING-064E，不按 return-only 排名。2026-06-01: TRADING-064E 完成，原因：新增 `risk_adjusted_v1` ranking policy config、candidate score/component score/hard rejection/ranking reason 计算和 tests；comparison report 对 pack run 应用 ranking，high-return 但 turnover、drawdown、benchmark 或 safety 不合格的 candidate 会被 hard reject。2026-06-01: TRADING-064F 完成，原因：新增 `shadow_only_manual_review` promotion policy threshold/classification、candidate selection report builder/renderer、CLI `aits etf experiments select-candidates --run-id/--latest` 和 tests；gate 输出 `eligible_for_shadow`、`needs_more_data`、`rejected` 或 `blocked`，固定 `production_promotion_allowed=false`，缺 ranking、missing benchmark、credibility failure、unsafe safety 或 missing manual review 均 fail closed，不写 production weights 或 broker action。2026-06-01: TRADING-064G 完成，原因：新增 `aits etf experiments enroll-shadow --run-id/--latest --candidate/--top`、observe-only `data/simulation/etf_shadow_candidates.json` registry 和 tests；只登记 `eligible_for_shadow` candidate，记录 shadow/source/model/config/evaluation schedule，重复登记确定性，非 eligible 或 unsafe candidate fail closed，不写 production weights 或 broker action。2026-06-01: TRADING-064H 完成，原因：新增 `weekly_shadow_review_v1` policy、`aits etf experiments weekly-review --as-of/--latest`、weekly JSON/Markdown renderer 和 tests；报告比较 shadow candidate、baseline、QQQ、drawdown、turnover、weight stability 和 constraint hits，动作仅限 continue/needs_more_data/reject/longer observation，固定 `production_promotion_allowed=false`。2026-06-01: TRADING-064I 完成，原因：report registry 已登记 experiment manifest、comparison、candidate selection、shadow registry 和 weekly review；Reader Brief 新增 ETF Calibration Experiments 摘要，展示 latest pack、top candidate、rejected/blocked count、active shadow candidates、weekly review action、detail report 和 safety status，缺失 artifact 时只读降级，不运行上游 experiment 或 shadow 流程；下一步进入 TRADING-064J final validation gate。2026-06-01: TRADING-064J 进入实现，原因：需要新增 final validation command/report，聚合 registry/pack/runner/comparison/ranking/candidate gate/shadow/weekly/report integration/P2-live safety checks，作为 TRADING-064 baseline completion gate。2026-06-01: TRADING-064J 完成，原因：新增 `aits etf experiments validate --pack etf_calibration_v1` JSON/Markdown final gate，当前配置 PASS，覆盖 registry、pack、runner、comparison/ranking、candidate gate、shadow enrollment、weekly review、report index/Reader Brief integration、safety fields 和 P2/live production-input blocker；TRADING-064A~J baseline 实现完成，状态转入 VALIDATING，后续等待真实 experiment/shadow artifacts 和 owner review。|
|TRADING-051|回测输入/Signal Ablation & Contribution Validation|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/signal_ablation_contribution_validation_2026-05-30.md`；已完成 remove-one-signal ablation、贡献分类、proxy/fallback 风险标记、JSON/Markdown 报告、Dashboard/Reader Brief/promotion decision 只读接入和 report registry 可见性；下一步观察更多真实窗口并决定是否进入 TRADING-052 calibration|`aits signals ablation --latest` 生成 `artifacts/signal_ablation/YYYY-MM-DD/signal_ablation_summary.json/md`；`aits reports signal-ablation --latest` 可读取并写报告 alias；`aits signals validate-ablation --latest` 输出 `status=LIMITED`、`production_effect=none`、`manual_review_required=true`、`auto_promotion=false`；fallback/proxy signal 不得获得 promotion credit；shadow promotion decision 可引用 ablation summary 但不得自动晋升；专项测试、全量 pytest、ruff、compileall 和 diff check 通过；不修改 `config/parameters/production/current.yaml`|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-050 已完成 full-signal-limited shadow backtest，但当前缺少逐信号贡献验证，继续新增信号前必须先确认已有 signal 是否有效。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 TRADING-051 v0.1 实现、文档和真实 CLI 验收；2026-05-28 最新 ablation 为 LIMITED，positive=0、negative=0、fallback=3、promotion_credit=0，candidate promotion 仍 disabled。|
|TRADING-051A|回测输入/Ablation Validation & Signal Contribution Diagnostics|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/signal_ablation_diagnostics_2026-05-30.md`；已增强 signal ablation 的 usage、score impact、portfolio impact、threshold diagnostics、classification reason、debug/explain CLI、Dashboard 和 Reader Brief no-credit reason；下一步用更多真实窗口复核 trend/sector calibration 需求|每个 signal contribution 输出 `diagnostic_status`、`classification_reason`、`signal_usage_diagnostics`、`score_impact`、`portfolio_impact` 和 `threshold_diagnostics`；real signals 未进 score、score 无变化、score 到 portfolio 断开、fallback signal 均有明确状态和 warning；`aits signals ablation --latest --debug` 与 `aits signals explain-ablation --latest` 输出诊断链路；`validate-ablation` 显示 diagnostics present；不修改 `config/parameters/production/current.yaml`；不解除 candidate promotion；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-051 真实结果为 `promotion_credit_signals=0`，需要解释是贡献低于阈值、组合构造不敏感、fallback 限制，还是 ablation 未真正影响 score/portfolio。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：真实 2026-05-28 ablation 已确认 real signals used in score、ablation changed scores 和 portfolio；`promotion_credit_signals=0` 的原因是 real signal contributions below threshold，candidate promotion 仍 disabled。|
|TRADING-052|回测输入/Trend/Sector Signal Calibration|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/trend_sector_signal_calibration_2026-05-30.md`；已完成多 profile trend/sector signal calibration、distribution/correlation diagnostics、profile ranking、报告、Dashboard/Reader Brief 和 shadow promotion 只读引用；下一步观察更多真实窗口并决定是否进入 TRADING-053 portfolio sensitivity|`aits signals calibrate --latest` 生成 `artifacts/signal_calibration/YYYY-MM-DD/signal_calibration_summary.json/md` 和 recommended profile；`aits reports signal-calibration --latest` 可读取 latest 报告；calibration profile 不写 `config/parameters/production/current.yaml`，不自动 candidate promotion；Dashboard/Reader Brief 展示 calibration 摘要；shadow promotion reason 可引用 calibration artifact 但仍保持 `production_effect=none`、`manual_review_required=true`、`auto_promotion=false`；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-051A 已确认 trend/sector 接入和 ablation 链路有效，但 real signal contribution below threshold，需要校准价格派生信号公式和诊断是否进入 TRADING-053 portfolio sensitivity。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 TRADING-052 v0.1 实现、文档和真实 CLI 验收；2026-05-28 最新 calibration 为 LIMITED，best_profile=trend_long_bias，positive real signal diagnostics 改善但 signal quality 仍 LIMITED，candidate promotion 仍 disabled。|
|TRADING-053|回测输入/Portfolio Sensitivity Diagnostics|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/portfolio_sensitivity_diagnostics_2026-05-30.md`；已完成 score -> target allocation -> actual weight -> performance 传导诊断框架、报告、Dashboard/Reader Brief 和 shadow promotion 只读引用；TRADING-053B 已解除 latest data gate blocker，当前真实 sensitivity 结论为 `LIMITED`，primary bottleneck 为 `rebalance_threshold`|`aits portfolio sensitivity --latest` 生成 `artifacts/portfolio_sensitivity/YYYY-MM-DD/portfolio_sensitivity_summary.json/md` 和 recommended profile；`aits reports portfolio-sensitivity --latest` 可读取 latest 报告；`aits portfolio validate-sensitivity --latest` 输出安全字段；Dashboard/Reader Brief 展示 sensitivity 摘要；shadow promotion reason 可引用 sensitivity artifact 但仍保持 rejected/observe-only；不修改 `config/parameters/production/current.yaml`；专项测试、全量 pytest、ruff、compileall 和 diff check 通过；真实 latest data_gate 为 OK，status 为 `LIMITED`，candidate promotion 仍 disabled|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-052 真实结果显示 best_profile=trend_long_bias 但仍 LIMITED，需要先判断 score 改善是否被 rebalance threshold、position cap、sector cap、cash floor 或 score-to-weight mapping 压制，再决定后续优化 portfolio construction 还是继续补 signal quality。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：TRADING-053 框架、测试和文档已完成；全量 pytest/ruff/compileall/diff check 通过；真实 `aits portfolio sensitivity --latest` 写出 2026-05-29 artifact 但因 `aits validate-data` 缺 `GOOGL`、`BRK.B`、`SGOV` 停在 FAILED/blocked，未做临时补数。2026-05-30: TRADING-053A 后复验，`aits portfolio sensitivity --latest` 改为写出 latest valid manifest date `2026-05-28` artifact，并明确 `data_gate.error_code=MANIFEST_PRICE_CACHE_MISMATCH`、`latest_resolution=MISMATCH`；candidate promotion 仍 disabled。2026-05-30: TRADING-053B 后复验，`aits portfolio sensitivity --latest` 通过 data_gate，输出 `status=LIMITED`、`primary_bottleneck=rebalance_threshold`、`portfolio_is_too_insensitive=True`；signal quality 仍 LIMITED，promotion disabled。|
|TRADING-053A|数据质量/Data Repair Persistence & Latest Artifact Consistency|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/data_repair_persistence_latest_artifact_consistency_2026-05-30.md`；已新增统一 symbol resolver、data registry consistency report、manifest context `validate-data` error code、portfolio sensitivity `data_gate`、CLI、Dashboard/Reader Brief 展示；真实 blocker 已被 TRADING-053B reconcile 收敛为一致 registry / manifest view|`aits data inspect-registry --latest` 输出 latest resolution、price cache paths、symbol mapping 和 registry status；`aits data validate-backtest-manifest --latest` 明确显示 `GOOGL`、`BRK.B via BRK-B`、`SGOV` 的 manifest/cache 一致性；`aits data reconcile-price-cache --latest --dry-run` 输出修复计划；portfolio sensitivity 使用 latest valid backtest input manifest context，不再把已修复标的误判为简单 missing；Dashboard/Reader Brief 展示 data registry / price cache reconcile 状态；不修改 `config/parameters/production/current.yaml`，不降低 data quality gate；专项测试、全量 pytest、ruff、compileall 和 diff check 通过；真实 latest resolution 现为 OK|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-053 真实 latest 被 `validate-data` 报告 `GOOGL`、`BRK.B`、`SGOV` missing 阻断，但 TRADING-049 repair/diagnostics 曾显示这些标的已修复；需要先修正 repair persistence、symbol mapping、latest manifest 和下游 data gate 一致性，再解释 portfolio sensitivity 结果。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实现和测试完成；真实 `aits data inspect-registry --latest` 显示 market latest `2026-05-29`、latest valid manifest `2026-05-28` 的 `LATEST_DATE_MISMATCH`，并把 `GOOGL`、`BRK.B`、`SGOV` 诊断为 `MANIFEST_PRICE_CACHE_MISMATCH`；`aits portfolio sensitivity --latest` 现在输出明确 `data_gate.error_code=MANIFEST_PRICE_CACHE_MISMATCH`，candidate promotion 仍 disabled。2026-05-30: TRADING-053B 后复验，`aits data inspect-registry --latest` 与 `aits data validate-backtest-manifest --latest` 均通过，`GOOGL`、`BRK.B via BRK-B`、`SGOV` 不再报 `MANIFEST_PRICE_CACHE_MISMATCH`。|
|TRADING-053B|数据质量/Price Cache Reconcile & Backtest Manifest Refresh|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/price_cache_reconcile_manifest_refresh_2026-05-30.md`；已把 TRADING-053A 的 dry-run reconcile 推进为可执行，并刷新 latest backtest manifest / registry lifecycle|`aits data reconcile-price-cache --latest` 生成 `price_cache_reconcile_summary.json/md`；支持 `--refresh-manifest-only`、`--register-repaired-only`、`--symbols` 和 dry-run；新增 `aits data refresh-backtest-manifest --latest`；可审计 repaired rows 已注册 `GOOGL`、`BRK.B via BRK-B`、`SGOV`；若 repaired rows 未覆盖全局最新交易日，latest resolution 回到 required-asset common date 而不是补造价格；不修改 `config/parameters/production/current.yaml`，不降低 data quality gate，不生成伪价格；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-053A 已确认真实 blocker 是 primary cache 与 backtest manifest/latest resolution 不一致；下一步需要正式实现 reconcile/manifest refresh，才能让 portfolio sensitivity 在真实数据门禁下运行。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实际 reconcile 已从 audited FMP raw cache 恢复并注册 `GOOGL`、`BRK.B via BRK-B`、`SGOV` 各 1008 行，生成 `price_cache_registry.json` 和 reconcile summary；因可审计 rows 只覆盖到 2026-05-28，未伪造 2026-05-29 行，latest resolution 改为 required-asset common date `2026-05-28` OK；portfolio sensitivity data_gate 已通过。|
|TRADING-054|回测输入/Portfolio Construction Candidate Profiles|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/portfolio_construction_candidate_profiles_2026-05-30.md`；已完成只读 portfolio construction candidate profile evaluation、候选 profile 配置、shadow simulation、remove-one-signal contribution summary、ranking、JSON/Markdown/recommended candidate artifact、Dashboard/Reader Brief 和 shadow promotion supporting artifact；真实 latest 推荐 `lower_rebalance_threshold_2pct`，但 signal quality 仍 `LIMITED`，candidate promotion 仍 disabled|`aits portfolio candidates --latest` 生成 `artifacts/portfolio_candidates/YYYY-MM-DD/portfolio_candidates_summary.json/md` 和 `recommended_portfolio_candidate.yaml`；`aits reports portfolio-candidates --latest` 可读取 latest 报告；`aits portfolio validate-candidates --latest` 输出 `status=LIMITED`、`production_effect=none`、`manual_review_required=true`、`auto_promotion=false`；每个 candidate 输出 signal transmission、performance、risk guardrails、turnover/cost、signal contribution 和 delta vs baseline；ranking 不以收益最高作为唯一标准且一票否决 data gate、drawdown、turnover、production effect 和 manual review 缺失；Dashboard/Reader Brief 展示 candidate 摘要；shadow backtest dry-run 可引用 candidate artifact 但仍 rejected；不修改 `config/parameters/production/current.yaml`，不降低 data quality gate，不使用 mock 数据，不自动 promotion；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-053B 已解除数据层 blocker，真实 sensitivity 指向 `rebalance_threshold`，需要在不修改 production 参数的前提下评估若干 portfolio construction 候选方案。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、文档和真实 latest 验收完成；`aits portfolio candidates --latest` 生成 2026-05-28 artifact，`best_profile=lower_rebalance_threshold_2pct`、`data_gate=OK`、`status=LIMITED`、`production_effect=none`；`aits parameters shadow-backtest --latest --dry-run` 已引用 `portfolio_candidates` supporting artifact 且 promotion 仍 rejected。|
|TRADING-055|回测输入/Portfolio Candidate Manual Review Workflow|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/portfolio_candidate_manual_review_workflow_2026-05-30.md`；已在 TRADING-054 recommended candidate 基础上新增正式人工 review package、decision artifact、状态流转、production hash、rollback reference、Dashboard/Reader Brief 和 shadow promotion 只读引用；真实 latest decision 为 `watch`，`approved_for_shadow_candidate` 只允许 shadow tracking，不代表 production approval|`aits portfolio review-candidate --latest` 生成 `artifacts/portfolio_candidate_reviews/YYYY-MM-DD/portfolio_candidate_review_package.json/md` 并首次写出 pending decision；`aits portfolio decide-candidate --latest --decision watch --reason ...` 生成 decision JSON/Markdown；`aits portfolio validate-review --latest` 输出 `status=watch`、`production_effect=none`、`manual_review_required=true`、`auto_promotion=false`、`production_config_modified=false`；`aits reports portfolio-candidate-review --latest` 可读取 latest review；shadow backtest dry-run 可引用 `portfolio_candidate_review_decision.json` 但仍 rejected；Dashboard/Reader Brief 展示 review 状态；不修改 `config/parameters/production/current.yaml`，不降低 data quality gate，不因人工 review 绕过 signal quality LIMITED，不自动启用 recommended profile；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-054 已生成 advisory recommended candidate，但缺少人工 review/watch/shadow tracking 审计流程；本任务只实现 recommended candidate -> manual review package -> explicit review decision artifact。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、文档和真实 latest 验收完成；review package/decision 生成 2026-05-28 artifact，`candidate_profile=lower_rebalance_threshold_2pct`、`status=watch`、`allowed_next_step=continue_shadow_tracking`、`production_effect=none`；shadow backtest dry-run 已引用 `portfolio_candidate_review` supporting artifact 且 promotion 仍 rejected，production config hash 前后一致。|
|TRADING-056|回测输入/Shadow Candidate Tracking & Daily Roll-forward|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/portfolio_candidate_shadow_tracking_daily_rollforward_2026-05-30.md`；已新增 shadow candidate tracking、daily roll-forward、tracking summary/state、Dashboard/Reader Brief 和 shadow backtest 只读引用；真实 latest 进入 `degraded_tracking`，因为 tracking date 为 2026-05-29、effective data date 为 2026-05-28|`aits portfolio track-candidate --latest` 生成 `artifacts/portfolio_candidate_tracking/YYYY-MM-DD/portfolio_candidate_tracking_summary.json/md` 与 `state/active_shadow_candidates.json`；`aits portfolio validate-tracking --latest` 输出 candidate profile、tracking status、安全字段和 data gate 状态；`aits reports portfolio-candidate-tracking --latest` 可读取 latest tracking report；`aits parameters shadow-backtest --latest --dry-run` 仍保持 rejected 且引用 tracking summary；Dashboard/Reader Brief 展示 tracking 摘要；不修改 `config/parameters/production/current.yaml`，不降低 data gate，不自动 promotion，不使用 mock 数据；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-055 已产出 `watch` review decision，但 latest run date 与 review decision date 不一致且数据可能 roll-forward，需要正式 shadow tracking 机制承接 candidate watched -> candidate tracked。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、真实 CLI smoke 和全量验证完成；`track-candidate --latest` 输出 `candidate_profile=lower_rebalance_threshold_2pct`、`tracking_status=degraded_tracking`、`data_gate=OK`、`effective_data_date=2026-05-28`，shadow backtest dry-run 仍 `promotion_status=rejected` 且引用 tracking summary；production/current.yaml SHA256 前后一致。|
|TRADING-057|数据质量/Market Data Freshness & Tracking Readiness Gate|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/market_data_freshness_tracking_readiness_gate_2026-05-30.md`；已实现 market data freshness config、CLI、report、candidate tracking readiness gate、Dashboard/Reader Brief 和 shadow backtest 只读引用；真实 latest 为 `STALE`，tracking 被 readiness gate 阻断，下一步观察 cache refresh 后是否回到 OK 或 ACCEPTABLE_LAG|`aits data freshness --latest` 生成 `artifacts/data_freshness/YYYY-MM-DD/market_data_freshness_summary.json/md`；`aits data validate-freshness --latest` 输出 freshness status、tracking readiness 和安全字段；freshness status 区分 OK、ACCEPTABLE_LAG、NON_TRADING_DAY、STALE、MISSING、SOURCE_DELAYED、MARKET_CALENDAR_UNKNOWN、FAILED；`aits portfolio track-candidate --latest` 写入 `market_data_freshness_status` 并按 readiness 映射 tracking 状态；`aits reports data-freshness --latest` 可读取 latest report；Dashboard/Reader Brief 展示 freshness 摘要；shadow backtest dry-run 可引用 freshness artifact 但仍 rejected；不修改 `config/parameters/production/current.yaml`，不降低 data gate，不下载或伪造数据，不自动 promotion；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-056 已把 candidate 放入 shadow tracking，但 latest `tracking_date=2026-05-29` 与 `effective_data_date=2026-05-28` 的 degraded 状态缺少正式 freshness/readiness 判断，无法区分正常延迟、非交易日、stale cache、source delayed 或真实缺失。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、文档和真实 latest 验收完成；`aits data freshness --latest` 输出 `freshness_status=STALE`、`tracking_readiness=cannot_track`，`aits portfolio track-candidate --latest` 输出 `tracking_status=tracking_blocked`，shadow backtest dry-run 仍 `promotion_status=rejected` 且引用 freshness/tracking artifacts；全量验证通过，production/current.yaml 无 diff。|
|TRADING-057A|数据质量/Market Data Refresh & Freshness Recovery Workflow|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/market_data_refresh_freshness_recovery_workflow_2026-05-30.md`；已实现 stale freshness -> refresh plan -> required price refresh -> registry/manifest refresh -> rerun freshness/tracking 的可审计恢复链路；真实 latest 已恢复到 `freshness_status=OK`、`tracking_status=active_tracking`，promotion 仍 disabled|`aits data refresh-market --latest --dry-run` 生成 refresh plan；`aits data refresh-market --latest` 生成 `artifacts/data_refresh/YYYY-MM-DD/market_data_refresh_summary.json/md`；`aits data recover-freshness --latest` 执行 freshness -> refresh -> reconcile/manifest -> freshness -> tracking；可用时 freshness 恢复 `OK` 且 tracking 为 `active_tracking`；source delayed 时 refresh 输出 `SOURCE_DELAYED`、freshness 保持 `STALE`、tracking 保持 `tracking_blocked` 并说明原因；Dashboard/Reader Brief/shadow backtest 引用 refresh artifact；不修改 `config/parameters/production/current.yaml`，不降低 data gate，不使用 mock 数据，不生成 synthetic latest bar，不解除 promotion；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-057 已把 stale market data 正确阻断为 `tracking_blocked`，但缺少正式 refresh/recovery workflow 来安全刷新 required assets、更新 registry/manifest 并区分可恢复 stale cache 与 `SOURCE_DELAYED`。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：refresh workflow、CLI、报告、Dashboard/Reader Brief、candidate tracking 和 shadow backtest 集成已完成；真实 refresh 从数据源恢复 `GOOGL`、`BRK.B via BRK-B`、`SGOV` 的 2026-05-29 日线后，freshness 为 `OK`，candidate tracking 为 `active_tracking`，shadow backtest 仍 `promotion_status=rejected`；全量 pytest、ruff、compileall 和 diff check 通过，`production/current.yaml` 无 diff。|
|TRADING-058|回测输入/Shadow Candidate Performance Review|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/portfolio_tracking_review_2026-05-30.md`；已完成 active shadow candidate rolling performance review、recommendation、CLI/report/Dashboard/Reader Brief 和 shadow backtest 只读引用；TRADING-058A 已补齐 tracking window 进度，真实 latest 仍为 `LIMITED` / `needs_more_data`，因为当前只有 1 个 tracking day，下一步等待至少 5 个有效 tracking days|`aits portfolio review-tracking --latest` 生成 `artifacts/portfolio_tracking_reviews/YYYY-MM-DD/portfolio_tracking_review_summary.json/md`；`aits portfolio validate-tracking-review --latest` 输出 `status=LIMITED` 或 `OK`、recommendation、安全字段和 data readiness；`aits reports portfolio-tracking-review --latest` 可读取 latest review；`aits parameters shadow-backtest --latest --dry-run` 仍保持 rejected 且引用 review summary；Dashboard/Reader Brief 展示 tracking review；不修改 `config/parameters/production/current.yaml`，不降低 data gate，不自动 promotion，不因单日表现好输出积极 production 结论；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-057A 已恢复 freshness 和 active shadow tracking，下一阶段需要 rolling performance review 框架，当前重点 candidate 为 `lower_rebalance_threshold_2pct`，前几天大概率只能输出 `needs_more_data` 而非 promotion 结论。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、文档和真实 CLI 验收完成；`review-tracking --latest` 生成 2026-05-29 artifact，`candidate_profile=lower_rebalance_threshold_2pct`、`tracking_days=1`、`recommendation=needs_more_data`、`production_effect=none`；shadow backtest dry-run 已引用 `portfolio_tracking_review` supporting artifact 且 promotion 仍 rejected；全量 pytest/ruff/compileall/diff check 通过，production/current.yaml 未修改。2026-05-30: TRADING-058A 后复验，tracking window 显示 `stage=initial_observation`、`days_until_short_review=4`、`days_until_extended_review=19`，TRADING-058 继续保持 VALIDATING。|
|TRADING-058A|回测输入/Tracking Window Accumulation & Scheduled Review|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/portfolio_tracking_window_accumulation_2026-05-30.md`；已实现 tracking window 阶段、剩余天数、每日 review 可见性、Dashboard/Reader Brief 展示和 shadow backtest 最小窗口解释；TRADING-058 在少于 5 个真实 tracking days 时继续保持 VALIDATING|新增 `config/portfolio/portfolio_tracking_review_windows.yaml`；`portfolio_tracking_review_summary.json/md` 输出 `tracking_window.stage`、`days_until_short_review`、`days_until_extended_review`、结论许可和 done condition；`tracking_days<5` 强制 `needs_more_data`；`aits portfolio review-tracking --latest --show-window-progress` 与 `aits portfolio tracking-window-status --latest` 显示窗口进度；Dashboard/Reader Brief/portfolio tracking report/shadow backtest dry-run 引用同一窗口状态；daily scheduled review 链路登记 tracking review；不修改 `config/parameters/production/current.yaml`，不降低 data/freshness gate，不伪造 tracking days，不自动 promotion；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-058 latest 真实状态为 `tracking_days=1` / `needs_more_data`，需要把最小 review window 和为什么暂不能 DONE 转为系统可见状态。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、文档和真实 CLI 验收完成；latest 输出 `tracking_days=1`、`stage=initial_observation`、`recommendation=needs_more_data`、`days_until_short_review=4`、`production_effect=none`；Reader Brief 明确 only 1 tracking day / at least 5 valid tracking days；shadow backtest dry-run 仍 rejected；全量 pytest/ruff/compileall/diff check 通过，production/current.yaml 未修改。|
|TRADING-059|回测输入/Restricted Backtest Weight Tuning|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/restricted_backtest_weight_tuning_2026-05-30.md`；已完成受限 signal weight grid search、walk-forward comparison、shadow-only recommended weights、CLI/report/Dashboard/Reader Brief 和 shadow backtest supporting artifact 接入；真实 latest 结果为 `NO_CANDIDATE`，下一步观察更多真实窗口并等待 signal quality 改善后复核是否需要新一轮 shadow candidate|`aits parameters tune-weights --latest` 生成 `artifacts/weight_tuning/YYYY-MM-DD/weight_tuning_summary.json/md`、`recommended_shadow_weights.yaml` 和 `weight_tuning_candidates.json`；`aits parameters validate-weight-tuning --latest` 输出 `status=LIMITED/OK/NO_CANDIDATE`、`production_effect=none`、`manual_review_required=true`、`auto_promotion=false`、`fallback_signals_free_tuned=false`、`production_config_modified=false`；`aits reports weight-tuning --latest` 可读取 latest 报告；Reader Brief/Dashboard 展示 weight tuning 摘要；`aits parameters shadow-backtest --latest --dry-run` 引用 weight tuning artifact 且 promotion 仍 rejected；不修改 `config/parameters/production/current.yaml`，不降低 data gate，不使用 mock 数据，不让 fallback signals 自由调参，不自动 promotion；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-058A 已完成 tracking window visibility，但 baseline signal weights 仍主要为人工设定，需要新增受限 backtest-based tuning 闭环来判断是否存在可审计的 shadow weight candidate。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：v0.1 实现、文档和真实 CLI 验收完成；`aits parameters tune-weights --latest` 生成 2026-05-28 artifact，`candidate_status=rejected`、`candidates_evaluated=240`、`guardrail_status=FAIL`，因此 baseline 仍是参考配置，production 参数和 promotion gate 均未改变。|
|TRADING-059A|回测输入/Weight Tuning Failure Attribution & Guardrail Diagnostics|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/weight_tuning_failure_attribution_2026-05-30.md`；已完成 latest weight tuning failure attribution、candidate rejection ranking、near-miss diagnostics、Dashboard/Reader Brief 和 shadow backtest 只读引用；真实 latest root cause 为 `portfolio_turnover_too_high`，下一步复核是否进入 TRADING-060 turnover attribution|`aits parameters explain-weight-tuning-failure --latest` 生成 `artifacts/weight_tuning_failure/YYYY-MM-DD/weight_tuning_failure_summary.json/md`；`aits parameters validate-weight-tuning-failure --latest` 输出 `status=NO_CANDIDATE_EXPLAINED/BLOCKED`、`production_effect=none`、`manual_review_required=true`、`auto_promotion=false`、`production_config_modified=false`；`aits reports weight-tuning-failure --latest` 可读取 latest 报告；Dashboard/Reader Brief 展示 root cause、top failure reason 和 recommended next action；shadow backtest dry-run 引用 failure attribution artifact 且 promotion 仍 rejected；不修改 `config/parameters/production/current.yaml`，不降低 guardrail，不放开 fallback signal，不生成伪 candidate；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-059 最新真实结果为 `NO_CANDIDATE` / `candidate_status=rejected` / `guardrail_status=FAIL`，需要先解释候选失败主因，再决定后续是改信号、改组合构建、改搜索空间还是改鲁棒性框架。2026-05-30: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、文档和真实 CLI 验收完成；`aits parameters explain-weight-tuning-failure --latest --debug` 生成 2026-05-28 artifact，`status=NO_CANDIDATE_EXPLAINED`、`root_cause_category=portfolio_turnover_too_high`、`top_failure_reason=cost_drag_too_high`、`most_common_guardrail_failure=turnover_guardrail_failed`，shadow backtest dry-run 仍 `promotion_status=rejected` 且引用 failure attribution artifact，production config hash 不变。|
|TRADING-060|回测输入/Portfolio Turnover & Cost Drag Attribution for Weight Candidates|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/portfolio_turnover_attribution_2026-05-31.md`；已完成只读 turnover / cost drag attribution、candidate/rebalance/asset/window 归因、CLI/report alias、Dashboard/Reader Brief 和 shadow backtest supporting artifact 接入；真实 latest root cause 为 `weight_search_too_aggressive`，已触发 TRADING-061 进入 search stability constraints|`aits portfolio explain-turnover --latest` 生成 `artifacts/portfolio_turnover_attribution/YYYY-MM-DD/portfolio_turnover_attribution_summary.json/md`；`aits portfolio validate-turnover-attribution --latest` 输出 `status=TURNOVER_FAILURE_EXPLAINED/LIMITED/BLOCKED`、`production_effect=none`、`manual_review_required=true`、`auto_promotion=false`、`production_config_modified=false`；`aits reports portfolio-turnover-attribution --latest` 可读取 latest 报告；Dashboard/Reader Brief 展示 root cause、top turnover assets、cost drag 和 recommended next action；shadow backtest dry-run 引用 turnover attribution artifact 且 promotion 仍 rejected；不修改 `config/parameters/production/current.yaml`，不降低 guardrail，不修改 cost model，不启用 rejected candidate，不使用 mock 数据；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-31: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-060 需求文本；TRADING-059A 已确认 weight tuning failure 主因为 turnover / cost drag，需要先做结构化归因，再决定 TRADING-061 是否进入 turnover overlay、score smoothing、search stability、asset rotation penalty 或 cost assumption review。2026-05-31: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、文档和真实 CLI 验收完成；`aits portfolio explain-turnover --latest` 生成 2026-05-28 artifact，`status=TURNOVER_FAILURE_EXPLAINED`、`root_cause_category=weight_search_too_aggressive`、`top_failure_reason=cost_drag_too_high`、`most_common_guardrail_failure=turnover_guardrail_failed`、`failed_by_turnover=240`；Reader Brief 已显示 turnover 摘要，shadow backtest dry-run 仍 `promotion_status=rejected` 且引用 turnover attribution artifact，production config 未修改。2026-05-31: TRADING-061 进入 IN_PROGRESS，原因：owner 确认下一步先收敛搜索空间，而不是降低 guardrail 或修改 cost model。|
|TRADING-061|回测输入/Weight Search Stability Constraints|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/weight_search_stability_constraints_2026-05-31.md`；已新增 stable weight tuning profile、候选稳定性约束、turnover-aware prefilter、cost drag objective penalty、报告/Dashboard/Reader Brief/shadow backtest 只读引用；真实 latest artifact 为 `INSUFFICIENT_DATA`，原因是 upstream freshness / signal snapshot readiness 未满足，未进入 candidate backtest|`aits parameters tune-weights-stable --latest` 生成 `artifacts/weight_stability/YYYY-MM-DD/weight_stability_summary.json/md` 和 `stable_weight_candidates.json`；如有候选则生成 `recommended_stable_shadow_weights.yaml`；`aits parameters validate-weight-stability --latest` 输出 `production_effect=none`、`manual_review_required=true`、`auto_promotion=false`、`production_config_modified=false`；`aits reports weight-stability --latest` 可读取 latest 报告；Dashboard/Reader Brief 展示 Weight Search Stability；shadow backtest dry-run 引用 stability artifact 且 promotion 仍 rejected；不修改 `config/parameters/production/current.yaml`，不降低 turnover guardrail，不修改 cost model，不放开 fallback signals，不自动 promotion；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-31: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-061 规格；TRADING-060 已确认失败主因为 aggressive weight search，需要先用 baseline proximity、单 signal delta、trend/sector cap 和 turnover prefilter 收敛搜索。2026-05-31: 从 IN_PROGRESS 改为 VALIDATING，原因：实现、文档和验证完成；`aits parameters tune-weights-stable --latest` 生成 2026-05-28 stability artifact，`status=INSUFFICIENT_DATA`、`candidates_backtested=0`，阻断来自 upstream freshness / signal snapshot readiness，不是候选质量结论；`validate-weight-stability` 与 `reports weight-stability` 通过，shadow backtest dry-run 引用 `weight_stability_summary.json` 且 promotion 仍 rejected。|
|REPORT-058|报告/Reader Brief Decision View|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_brief_decision_view_2026-05-29.md`；已实现 Reader Brief 三层状态、Action Checklist、artifact 三链路影响、score change 叙事、funnel 人读版、Top Review Items Today、影响类型分组和显示层 cadence override|Reader Brief 顶部必须拆分 Build Status / Decision Usability / Research Promotion Status；manual review 或 promotion blocker 存在时不能用单一 OK 暗示结论可直接执行；缺失 artifact 必须区分 daily score、reader context 和 promotion chain 影响；首页包含 Action Checklist、Top Review Items Today、自然语言 score change、降噪 funnel 和明确 cadence；输出仍 `production_effect=none`，不重算 score、不改 weights/gates、不触发交易；专项测试、真实 `reader-brief --latest` 与 `validate-reader-brief --latest` 通过|2026-05-29: 新增并进入 IN_PROGRESS；同日进入 VALIDATING；专项测试、reader-brief latest、validate-reader-brief latest、documentation contract 均已通过，当前 daily run bundle 已更新；浏览器自动化被本地 file URL 安全策略拦截，已用 HTML 结构校验替代自动视觉截图。|
|DATA-021|数据工程/估值与配置 YAML 读取边界韧性|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/valuation_yaml_loader_robustness_2026-05-29.md`；本轮修复 `score_daily` 在 valuation snapshot store 和核心配置 loader 通过 PyYAML 读取 YAML 文件流时触发 Windows access violation 并阻断 daily-run 的问题|daily-run 会触达的估值和配置 YAML loader 必须先读取稳定 UTF-8 文本快照，再用项目统一安全 YAML loader 解析；文件读取、解码和 YAML 解析失败都进入既有 fail-closed 路径；不得跳过估值门禁、补写快照或伪造数据；目标测试和真实 `aits ops daily-run` 通过或报告新的真实门禁原因|2026-05-29: 新增并进入 IN_PROGRESS，原因：`DATA-020` 修复后完整 daily-run 已通过 download-data/PIT/SEC/valuation fetch/validation/SEC PIT shadow monitor，随后 `score_daily` 在 `valuation.load_valuation_snapshot_store` 的 `yaml.safe_load(file)` 路径触发 Windows native access violation，未能进入现有估值校验审计报告。2026-05-29: 范围扩展到核心配置 loader，原因：valuation loader 改为文本快照后，目标测试在 `config.load_watchlist()` 的同类 `yaml.safe_load(file)` 路径复现 access violation；完整 daily-run 又在 `pit_snapshots_validate` 读取 `config/data_sources.yaml` 时命中 PyYAML 纯 Python scanner 内部 `TypeError`，PIT manifest 报告本身为 PASS。2026-05-29: 从 IN_PROGRESS 改为 VALIDATING，原因：项目统一 YAML loader 已改为稳定 UTF-8 文本快照 + `CSafeLoader`，目标测试/ruff/black/diff check 通过，真实 `aits ops daily-run` as-of 2026-05-28 已 23/23 PASS。|
|DATA-020|数据工程/外部请求缓存映射快照韧性|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/external_request_cache_mapping_snapshot_2026-05-29.md`；本轮修复 `download-data` 在生成外部请求 cache identity / response metadata 时遇到可变 header/params mapping 抛出 `dictionary keys changed during iteration` 并阻断 daily-run 的问题|外部请求缓存对 params、headers、json payload 和 response headers 生成脱敏 identity/metadata 前必须稳定快照 mapping；遇到一次 `dictionary keys changed during iteration` 应重试并保留脱敏边界；不得写入 API key/token/Cookie/User-Agent 原文；目标测试和真实 `aits ops daily-run` 通过或报告新的真实供应商门禁原因|2026-05-29: 新增并进入 IN_PROGRESS，原因：RISK-016/TRADING-047/TRADING-048 修复后完整 daily-run 已越过 SEC PIT shadow monitor，随后在第 1 步 `download_data` 因本地 RuntimeError `dictionary keys changed during iteration` fail closed；凭据存在，诊断未捕获结构化 provider 错误。2026-05-29: 从 IN_PROGRESS 改为 VALIDATING，原因：request cache mapping snapshot 修复通过目标测试/ruff/black/diff check，真实 `aits ops daily-run` as-of 2026-05-28 已 23/23 PASS。|
|TRADING-048|基本面/SEC PIT Shadow Monitor Dtype Robustness|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/sec_pit_shadow_monitor_dtype_robustness_2026-05-29.md`；本轮修复 `sec-pit shadow-monitor --latest` 在 rolling top-rank label delta 计算中因 Pandas/Arrow string dtype 与 `.loc[idxmin()]` 交互抛出 `NotImplementedError` 的 daily-run 阻断|Shadow monitor rolling metrics 必须只用数值化 rank/label 序列计算 top-rank label delta，不依赖混合 dtype DataFrame row lookup；Arrow string / pandas string dtype 输入下不应裸 traceback；monitor 仍 fail closed 于真实 coverage/rolling metrics gate，不吞掉因子监控失败；目标测试和真实 `aits ops daily-run` 通过或报告新的真实门禁原因|2026-05-29: 新增并进入 IN_PROGRESS，原因：TRADING-047 修复后重新执行 daily-run 已确认 shadow observe 使用 research baseline 且 baseline coverage OK，但 monitor 在 `_top_rank_label_delta` 的 `.loc[idxmin()]` 路径触发 Pandas `NotImplementedError: eq not implemented for pandas.StringDtype`，导致日报后续报告链再次阻断。2026-05-29: 从 IN_PROGRESS 改为 VALIDATING，原因：monitor dtype 修复通过目标测试/ruff/black/diff check，真实 `aits ops daily-run` as-of 2026-05-28 中 `sec_pit_shadow_monitor` PASS。2026-06-01: CI-like `.[dev]` 环境复现 GitHub Actions Test 失败，原因是 Arrow string fixture 构造需要 `pyarrow` 但 dev extra 未声明；本轮补齐 dev 依赖以保留 Arrow dtype 覆盖，不修改 monitor 逻辑或生产配置。|
|TRADING-047|基本面/SEC PIT Daily Shadow Baseline Contract|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/sec_pit_shadow_daily_run_baseline_contract_2026-05-29.md`；本轮修复 `daily-run` 中 `sec-pit shadow-observe --latest` 默认误用 production score fallback，导致 `sec-pit shadow-monitor --latest` 因 baseline coverage drift fail closed 的问题|`shadow-observe --latest` 在默认 baseline dir 下优先使用 research-only `data/processed/research/scores_daily_backfill_sec_pit_2023_2026.csv`，显式 `--baseline-score-path` 仍优先；production `data/processed/scores_daily.csv` 仅作为兼容 fallback；observe/monitor/coverage audit 的 baseline contract 一致；目标测试和真实 `aits ops daily-run` 通过或报告新的真实门禁原因|2026-05-29: 新增并进入 IN_PROGRESS，原因：`RISK-016` 修复后重跑 daily-run 已通过 `score_daily`，但 `sec_pit_shadow_monitor` 失败；诊断显示同日 baseline coverage audit 为 OK，而 shadow observe summary 因 fallback 到 production scores 仅约 2.1% 覆盖。2026-05-29: 从 IN_PROGRESS 改为 VALIDATING，原因：默认 baseline contract 修复通过目标测试/ruff/black/diff check，真实 `aits ops daily-run` as-of 2026-05-28 中 `sec_pit_shadow_observe` 和 `sec_pit_shadow_monitor` 均 PASS。|
|RISK-016|风险事件/官方政策来源解析韧性|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/official_policy_source_parser_hardening_2026-05-29.md`；本轮修复 `score-daily` 官方来源抓取在 OFAC XML row_count 阶段裸 traceback 阻断 daily-run 的问题，并重跑失败的每日任务|官方政策来源每个 source 的 row_count 和候选抽取必须按 source fail closed：raw payload 写入后若解析异常，生成 `official_policy_sources_YYYY-MM-DD.md` 和结构化错误，不丢失 checksum/source_id/诊断上下文；OFAC XML row_count 不使用易产生不可审计 traceback 的 generator 路径；`score-daily` 可在正常 OFAC payload 下完成官方来源抓取，异常 parser fixture 返回 FAIL 报告而不是裸崩溃；目标测试和真实 `aits ops daily-run` 通过或报告新的真实门禁原因|2026-05-29: 新增并进入 IN_PROGRESS，原因：自动化 daily-run as-of 2026-05-28 在 `score_daily` 的 `official_policy_sources._row_count(..., parser_kind='ofac_xml')` 阶段裸 traceback，导致日报、alerts、evidence dashboard、decision snapshot、ops health 和 secret scan 均未生成。2026-05-29: 从 IN_PROGRESS 改为 VALIDATING，原因：official policy parser 修复通过目标测试/ruff/black/diff check，真实 `aits ops daily-run` as-of 2026-05-28 中 `score_daily` PASS。|
|REPORT-057|报告/Reader Brief Visual Layout & Priority Styling|P2|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_brief_visual_layout_priority_styling_2026-05-29.md`；已在现有 Reader Brief HTML 上增强首屏 summary cards、status badges、market proxy cards、funnel/gate 高亮、manual review severity styling 和 missing artifact impact 分区；不新增数据源、不改变 scoring/backtest/weights/trading 行为；下一步在真实每日 HTML 阅读中观察段落长度、重复信息和图表需求|`reader_brief_YYYY-MM-DD.html` 首屏包含 final action、final AI position、binding gate、market movement、manual review count、`production_effect` cards；主要状态显示 badge；Market Situation 以 SPY/QQQ/SMH/SOXX/VIX/DGS10 compact cards 展示可用 1D/5D/20D 并保留明细表；Score & Decision Funnel 展示可扫描流程并高亮 binding gate；Manual Review Queue 和 Missing / Limited Artifact Impact 按 severity/impact 分区；audit details 仍在 `<details>`；测试覆盖 HTML 关键结构、deterministic output 和只读边界；真实 `--latest` 生成和 validate-reader-brief 保持 OK|2026-05-29: 新增并进入 IN_PROGRESS，原因：REPORT-056 已补齐统一治理 artifact，Reader Brief 内容闭环后需要降低表格感、提升每日投资简报扫描效率。2026-05-29: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 Reader Brief HTML summary cards、status badges、market proxy cards、funnel flow、binding gate highlight、severity/impact grouping、audit details 保留和专项测试；真实 `reader-brief --latest` 与 `validate-reader-brief --latest` 均为 OK。|
|REPORT-056|报告/Research Governance Summary Aggregator|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/research_governance_summary_aggregator_2026-05-29.md`；已在既有 `REPORT-051` summary cards 基础上增强为 Reader Brief 可消费的统一治理聚合 artifact，并修复 Report Navigation 同组重复 artifact 展示；下一步在后续真实 daily-run artifacts 上观察 promotion gate、weight candidate 和 documentation freshness 复核项是否足够清晰|`aits reports research-governance-summary --date/--latest` 生成 `research_governance_summary_YYYY-MM-DD.md/json`；JSON 覆盖 backtest、weight iteration、shadow observe、SEC PIT、documentation、manual review queue、limitations 和 source artifacts；promotion 默认 `NOT_PROMOTABLE`，缺 promotion gate 阻断 promotion；Reader Brief 消费 summary 后不再把该 artifact 计为 IMPORTANT，Executive Summary 展示 governance/promotion 状态，Manual Review Queue 合并治理复核项；Report Navigation 同组 artifact_id 去重且 runtime index 优先；测试覆盖 schema、Markdown、缺失降级、SEC PIT observe-only、documentation、去重、只读和 deterministic output|2026-05-29: 新增并进入 IN_PROGRESS，原因：REPORT-055 后 Reader Brief 剩余重要缺口是 research governance summary，且 Report Navigation 出现 daily_decision_summary 重复行。2026-05-29: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 aggregator schema/CLI、Reader Brief 消费、manual review 合并、navigation 去重、registry/catalog/system flow 和专项测试；验证通过 `tests/trading_engine/test_research_governance_summary.py`、`tests/trading_engine/test_reader_brief.py`、目标 report tests、全量 pytest、ruff、black、diff check 和真实 `--latest` 生成，`reader_brief_2026-05-27` 状态为 OK 且 missing important_count=0。|
|REPORT-055|报告/Score Change Attribution & Market Panel for Reader Brief|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/score_change_market_panel_reader_brief_2026-05-29.md`；已补齐 score change attribution 读者字段、`--date` / `--latest` CLI，新增只读 market panel builder/CLI、registry/catalog/system flow 和 Reader Brief 消费；下一步在真实 daily-run artifacts 上观察 market proxy 覆盖、data quality gate 和 Reader Brief 叙事是否足够清晰|`aits reports score-change-attribution --date/--latest` 与 `aits reports market-panel --date/--latest` 可输出 `score_change_attribution_YYYY-MM-DD.md/json` 和 `market_panel_YYYY-MM-DD.md/json`；Reader Brief 消费两类 artifact，Executive Summary 增加市场变化一句话，缺失影响层不再把已生成 artifact 列为 IMPORTANT；缺市场数据时输出 `MISSING_MARKET_PRICE_DATA` 且不编造涨跌；所有输出固定 `production_effect=none`；测试覆盖 schema、Markdown、降级、Reader Brief 只读消费和 production_effect|2026-05-29: 新增并进入 IN_PROGRESS，原因：REPORT-054 之后 Reader Brief 仍缺“为什么今天变了”和“市场实际涨跌”两类核心读者上下文。2026-05-29: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 score change attribution 字段/Markdown/CLI 增强、market panel artifact、Reader Brief 集成、registry/catalog/system flow 和专项测试；验证通过 report/Reader Brief/documentation contract 目标 pytest、触达文件 ruff、black 和 diff check。|
|REPORT-047|报告/Unified Reader Brief|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_reporting_improvement_plan_2026-05-28.md`；已新增 `aits reports reader-brief --as-of YYYY-MM-DD`、`reader_brief_YYYY-MM-DD.html/json` 和专项测试；首版只读消费 `daily_decision_summary`、`daily_score`、`evidence_dashboard`、`daily_task_dashboard`、calculation explainers、trace、decision snapshot 以及 daily task dashboard 已汇总的 backtest/shadow/SEC PIT/weight 状态；不迁移输出目录、不重算 score、不改变 production；下一步在真实 daily-run 产物上观察可选 artifact 缺失/过期时的降级表现，并评估是否纳入 daily-run 自动步骤|生成 `outputs/reports/reader_brief_YYYY-MM-DD.html/json`；首屏展示 run context、市场状态、核心结论、仓位语言、置信度、binding gate、Data Gate 和人工复核状态；正文展示 score-to-position funnel、component 贡献、gate ladder、data/PIT safety、backtest/shadow/weight/SEC PIT 摘要、manual review queue 和 trace links；所有输出固定 `production_effect=none`，缺少可选 artifact 只能标记 `MISSING` / `LIMITED` / `INSUFFICIENT_DATA`；实现时同步更新 `docs/system_flow.md`、`docs/artifact_catalog.md` 和专项测试|2026-05-28: 新增，原因：外部读者视角改进方案指出当前 Markdown 日报、evidence dashboard 和 daily task dashboard 职责都正确但入口不统一；本任务作为只读阅读首页，不替代现有审计 artifact。2026-05-28: 从 READY 改为 VALIDATING，原因：已完成 Reader Brief builder、HTML/JSON writer、CLI/direct dispatcher、trace links、artifact catalog、system flow 和专项测试；验证通过 `tests/test_reader_brief.py`、触达文件 ruff/black，后续观察真实 daily-run artifacts。|
|REPORT-048|报告/Calculation Explainer Registry|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_reporting_improvement_plan_2026-05-28.md`；已新增 `config/metric_explainers.yaml`、只读 explainer builder、`aits reports calculation-explainers` 和 `score-daily` 自动写出 calculation explainers JSON；下一步在真实 daily-run 中观察该 artifact 是否稳定进入 legacy reports 和 canonical run bundle，并供 `REPORT-047` Reader Brief 读取|新增 `outputs/reports/calculation_explainers_YYYY-MM-DD.json`；覆盖 overall score、component score、effective weight、confidence adjustment、model band、macro budget cap、position gate、final position、RankIC、drawdown、baseline coverage 等关键指标；每个 explainer 包含 formula、input fields / values、source artifacts、PIT policy、production_effect、limitations 和 common misread；HTML 报告可用 `<details>` 展示计算解释；缺少必需 explainer 时测试失败或显式 warning；实现时同步更新系统流图、产物目录和报告 contract 测试|2026-05-28: 新增，原因：现有 `docs/calculation_logic.md` 已能解释通用逻辑，但每日结果页缺少“这个数字今天具体怎么算出来”的结构化解释层。2026-05-28: 从 READY 改为 VALIDATING，原因：已完成 registry、builder、CLI、score-daily 自动产物、系统流图、产物目录和专项测试；验证通过 `tests/test_calculation_explainers.py`、`tests/test_daily_scoring.py`、`tests/test_evidence_dashboard.py`、`tests/test_daily_task_dashboard.py` 共 53 项，以及触达文件 ruff、black 和 diff check。|
|REPORT-049|报告/Report Registry & Cadence Calendar|P2|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_reporting_improvement_plan_2026-05-28.md`；已新增 `config/report_registry.yaml`、`aits reports index --as-of YYYY-MM-DD`、`report_index_YYYY-MM-DD.html/json`、direct dispatcher 和 Reader Brief freshness 摘要接入；下一步在真实 daily-run / ad hoc research 产物上观察 freshness SLA、missing/stale 和 owner action 是否足够清晰|生成 `outputs/reports/report_index_YYYY-MM-DD.html/json`；registry 记录 report_id、title、cadence、owner、audience、command、input/output artifacts、freshness SLA、production_effect、是否进入 Reader Brief / daily task dashboard、fallback status；报告 index 展示最新 artifact、freshness、status、是否影响 production、是否需要人工复核和链接；Reader Brief 只展示摘要和过期/缺失状态；实现时补系统流图、产物目录、schema 和 contract 测试|2026-05-28: 新增，原因：当前报告和周期任务增长很快，靠 dashboard cards、task register 和 artifact catalog 拼上下文的维护成本上升；但该项不应阻塞 Reader Brief 首版。2026-05-28: 从 READY 改为 IN_PROGRESS，原因：开始实现只读 report registry / cadence index 首版。2026-05-28: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 registry、report index builder、HTML/JSON artifact、CLI/direct dispatcher、Reader Brief freshness 摘要、系统流图、产物目录和专项测试；验证通过 `tests/test_report_index.py`、`tests/test_reader_brief.py` 和 `tests/test_cli_direct.py`。|
|REPORT-050|报告/Score Change Attribution|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_reporting_improvement_plan_2026-05-28.md`；已新增 `aits reports score-change-attribution --as-of YYYY-MM-DD`、只读 builder、Markdown/JSON writer、direct dispatcher 和 Reader Brief top changes 接入；首版只读比较今日与上一条 signal-time decision snapshot 的 score、component、effective weight、coverage、gate、confidence、position 和 data quality，不解析日报自然语言、不重算 score；下一步在真实连续 daily-run snapshots 上观察归因稳定性|生成 `outputs/reports/score_change_attribution_YYYY-MM-DD.md/json`；输出 `ai_after_chatgpt` regime、实际比较日期、overall score delta、component score delta effect、weight delta effect、coverage delta effect、gate delta effect、confidence delta effect、data quality / PIT 状态变化、top positive/negative/missing/neutralized/changed drivers；Reader Brief 展示 top changes；缺少上一交易日或关键 artifact 时输出 `INSUFFICIENT_DATA` 而不是补造变化原因；实现时补系统流图、产物目录和专项测试|2026-05-28: 新增，原因：读者最关心“为什么今天和昨天不一样”，当前日报有变化原因树基础，但还缺少机器可读、可复用的变化归因 artifact。2026-05-28: 从 READY 改为 IN_PROGRESS，原因：开始实现只读 score change attribution builder、CLI 和 Reader Brief 摘要接入。2026-05-28: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 score change attribution builder、CLI/direct dispatcher、Markdown/JSON artifact、Reader Brief 接入、系统流图、产物目录和专项测试；验证通过 `tests/test_score_change_attribution.py` 与 `tests/test_reader_brief.py`。|
|REPORT-051|报告/Research Governance Summary Cards|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_reporting_improvement_plan_2026-05-28.md`；已新增统一只读 research governance summary、`aits reports research-governance-summary --as-of YYYY-MM-DD`、Markdown/JSON artifact、direct dispatcher、Reader Brief 接入和 daily task dashboard 只读卡片；下一步在真实 backtest / SEC PIT / shadow / weight governance 产物上观察缺失、过期和 warning 分组是否足够清晰|生成 `outputs/reports/research_governance_summary_YYYY-MM-DD.md/json`；按 `Production-active`、`Approved but inactive`、`Shadow observe-only`、`Candidate / research-only`、`Blocked / insufficient data`、`Rollback / warning` 分组；每个候选或研究结果展示 candidate_id、source task、status、evidence summary、production_effect、manual_review_required、next review date 和 next action；Reader Brief 与 daily task dashboard 可读取同一 summary，避免重复解析多个 artifact；所有 research / shadow 项不得改变 production scoring、weights、position gates 或 trading 行为；实现时补系统流图、产物目录和专项测试|2026-05-28: 新增，原因：SEC PIT 与 shadow/weight 链路近期快速扩展，dashboard 卡片越来越多；需要统一治理摘要来防止 observe-only / research-only 结果被误读为当日 production 依据。2026-05-28: 从 READY 改为 IN_PROGRESS，原因：开始实现只读 research governance summary builder、CLI 和 Reader Brief 接入。2026-05-28: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 summary builder、CLI/direct dispatcher、Markdown/JSON artifact、Reader Brief 接入、daily task dashboard 只读卡片、系统流图、产物目录和专项测试；验证通过 `tests/test_research_governance_summary.py`、`tests/test_reader_brief.py`、`tests/test_daily_task_dashboard.py` 以及 report 相关回归。|
|REPORT-052|报告/Documentation Hygiene & Generated Catalog|P2|VALIDATING|系统验证|详细拆解见 `docs/requirements/documentation_hygiene_generated_catalog_2026-05-28.md`；已新增 documentation contract 生成器、`aits docs report-contract`、默认 registry 覆盖测试和产物目录/系统流图同步；当前默认 `config/report_registry.yaml` 与 `docs/artifact_catalog.md` 契约检查为 PASS；下一步观察 report registry 后续新增项是否能被该契约及时拦截，并评估是否进入 `docs/artifacts/*` 生成索引阶段|生成 `outputs/reports/documentation_contract_YYYY-MM-DD.md/json`；每个 registry report 必须能映射到 artifact catalog 行，并保留 command、source artifact、schema/status、production_effect 和 common misread 摘要；缺少 catalog 覆盖、production_effect 或 common misread 时 fail closed；命令或 schema/status 术语不完整先 warning；输出固定 `production_effect=none`，不运行上游报告、不修改 production；实现时同步更新系统流图、产物目录和专项测试|2026-05-28: 新增为 PROPOSED，原因：长文档维护压力真实存在，但范围较大且依赖 report registry 设计，适合后置规划而不是与 Reader Brief 首版捆绑。2026-05-28: 从 PROPOSED 改为 IN_PROGRESS，原因：REPORT-049 registry 首版已进入 VALIDATING，开始实现只读 documentation contract 第一阶段，先做生成/校验链路而不是拆分大文档。2026-05-28: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 documentation contract builder、CLI、Markdown/JSON writer、report registry 登记、artifact catalog 缺口修复、系统流图和专项测试；验证通过 report/docs 相关目标 pytest 75 passed、ruff、black。|
|REPORT-053|报告/Unified Reader Brief HTML v1|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/unified_reader_brief_html_v1_2026-05-28.md`；已在现有 `REPORT-047` Reader Brief 上完成 HTML/JSON 产品化增强，新增 `--date` / `--latest`、Task Cadence Calendar、Report Navigation、documentation contract/manual-review 汇总和关键数字 `<details>` 展开解释；下一步在真实 daily-run artifacts 上观察页面是否仍过度表格化，以及 market situation 需要补哪些真实 benchmark/sector/ticker 输入|`aits reports reader-brief --date YYYY-MM-DD`、`--as-of YYYY-MM-DD` 和 `--latest` 均可生成 `reader_brief_YYYY-MM-DD.html/json`；页面按 Executive Summary、Market Situation、Score & Decision Funnel、Backtest/Outcome、Shadow/Weight、Data Quality/PIT、Task Cadence Calendar、Manual Review Queue、Report Navigation 顺序组织；只读读取既有 artifacts，不运行 scoring/backtest/shadow/SEC PIT/weight/docs 上游；缺可选 artifact 降级显示；每个 production-sensitive 区块显示 `production_effect`；测试覆盖 JSON schema、HTML、missing degraded mode、source links、manual review 和 cadence calendar|2026-05-28: 新增并进入 IN_PROGRESS，原因：REPORT-047~052 已完成治理底座，但每日读者入口仍需要从“可用汇总”升级为“先结论、再证据、再计算逻辑、最后详细链接”的 HTML v1。2026-05-28: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 Reader Brief HTML v1 增强、CLI alias/latest、direct dispatcher、文档和测试；验证通过目标 pytest、ruff、black。|
|REPORT-054|报告/Reader Brief UX Quality & Missing Artifact Impact Layer|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_brief_ux_quality_2026-05-28.md`；已在现有 Reader Brief 上增强叙事首屏、缺失 artifact 影响层、manual review 分级行动、component contribution summary、market minimum panel、短路径显示、分组导航、registry fallback cadence 和 quality report；保持只读，不运行 scoring/backtest/shadow/SEC PIT/weight/docs 上游；下一步在真实 daily-run 自动产物中观察 `LIMITED_READER_CONTEXT` 是否足够清晰，以及 market price panel 是否需要单独任务补齐|`reader_brief_YYYY-MM-DD.html/json` 首屏有 narrative summary；缺失/受限 artifact 输出 `BLOCKING` / `IMPORTANT` / `OPTIONAL` / `INFO` 影响解释；manual review queue 分 Critical / Warning / Info 且每项有 recommended action；Task Cadence Calendar 缺 report_index 时能从 registry fallback；新增 `aits reports validate-reader-brief --date/--latest` 和 `reader_brief_quality_YYYY-MM-DD.json/md`；系统流图、artifact catalog、registry、runbook 和专项测试同步更新|2026-05-28: 新增并进入 IN_PROGRESS，原因：真实 `2026-05-27` Reader Brief 已证明统一入口方向成立，但页面仍偏表格化，缺失 artifact 影响和复核动作不够清晰。2026-05-28: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 Reader Brief UX quality layer、quality CLI/output、registry fallback、文档和测试；验证通过目标 pytest、`tests/test_daily_task_dashboard.py`、全量 pytest、ruff、black 和真实 `--latest` 生成。|
|TRADING-040|基本面/SEC PIT Cognitive Evaluation Loop|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/sec_pit_cognitive_evaluation_loop_2026-05-26.md`；已按最新规格重构 `aits sec-pit evaluate`：输出 summary JSON/Markdown、feature effectiveness CSV、signal attribution CSV、shadow candidate weights CSV、run.log；PIT gate 改为逐行排除 `available_time` 缺失或晚于 `decision_time` 的行并计数；dashboard 新增只读 SEC PIT Evaluation Summary 卡片；下一步用真实 TRADING-039 SEC PIT panel 跑 `outputs/sec_pit_evaluation` 并复核 shadow candidate 是否需要进入独立 shadow weight iteration|CLI 支持 `--start`、`--end`、`--feature-panel`、`--universe`、`--tickers`、`--benchmark`、`--output-dir`；报告展示 ai_after_chatgpt regime、实际日期范围、数据质量状态、PIT 安全性、覆盖率、IC/RankIC、top-bottom spread、signal attribution、shadow/research/downweight/insufficient-data 分层和限制；解释性阈值来自 `config/sec_pit_evaluation.yaml`；shadow weights 固定 `manual_review_required=true`、`production_effect=none`；专项测试覆盖 PIT gate、missing availability、provenance 降级、schema、dashboard 只读、样本不足和 deterministic CSV 输出；验证通过目标 pytest、dashboard pytest、全量 pytest 和全量 ruff；触达 Python 文件 black check 通过，全仓 black 仍被既有 `tests/test_market_data.py` baseline 阻断|2026-05-26: 新增并进入 IN_PROGRESS，原因：TRADING-039 已完成 SEC EDGAR reconstructed PIT 数据底座，但还缺少可审计的认知评估闭环来判断 SEC PIT feature 是否有稳定解释力、是否进入 shadow weight iteration，以及报告能否披露 PIT 安全性、覆盖、贡献和限制。2026-05-26: 基础版从 IN_PROGRESS 改为 VALIDATING，原因：已完成旧口径 evaluation loop 实现、CLI、policy config、报告/CSV artifact、文档和专项测试。2026-05-26: 维持 VALIDATING，原因：按 owner 最新规格重构 artifact 命名和 schema，新增 `config/sec_pit_evaluation.yaml`、signal attribution、shadow candidate weights、dashboard 只读卡片和专项测试；验证通过 `tests/trading_engine/test_sec_pit_evaluation.py`、`tests/test_daily_task_dashboard.py`、全量 `python -m pytest -q`（1269 passed, 1 warning）和全量 `python -m ruff check config src tests scripts docs`；触达 Python 文件 black check 通过，全仓 black 仍仅被既有 `tests/test_market_data.py` baseline 阻断。|
|TRADING-038|交易执行/Manual Approval Record / Retry Execution Dry Run|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/retry_execution_dry_run_2026-05-26.md`；只读 manual approval record 读取和 retry execution dry-run 核心模块、CLI、JSON/Markdown/log、dashboard 只读卡片、runbook、requirements index、system flow、artifact catalog 和专项测试已完成；本任务只读取 latest 或显式 TRADING-037 queue JSON 与可选人工 approval JSON，不执行 retry、不发送 email/webhook/mobile notification、不修改 delivery state、不修改 approval record/state、不修改 production 参数、不修改 TRADING-035/036/037 历史 artifact|新增 `scripts/run_retry_execution_dry_run.py` 和 `src/ai_trading_system/trading_engine/retry_execution_dry_run.py`；输出 `outputs/retry_execution_dry_run/retry_execution_dry_run_YYYY-MM-DD.json/md/log`；区分 `SOURCE_QUEUE_UNAVAILABLE` / `NOTHING_TO_DRY_RUN` / `WAITING_FOR_MANUAL_APPROVAL` / `READY_FOR_DRY_RUN` / `APPROVAL_MISMATCH` / `SAFETY_BLOCKED`；仅 `APPROVED_FOR_DRY_RUN` 且 safety constraints 为 dry-run-only 的 `PENDING_APPROVAL` candidate 可生成 simulated retry action，且 `actual_retry_executed=false`、`external_delivery_executed=false`、`state_mutation_executed=false`；dashboard 只读读取 TRADING-038 JSON，不运行 dry-run generator、不执行 retry、不发送 notification、不修改 approval record；专项测试覆盖 source missing/malformed、empty、waiting approval、ready、mismatch、rejected、safety blocked、deterministic dry-run action id、Markdown、CLI、dashboard 只读和 fail flags；验证通过 `tests/trading_engine/test_retry_execution_dry_run.py`、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest 和 ruff；TRADING-038 触达文件 Black check 通过，全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断|2026-05-26: 新增并进入 IN_PROGRESS，原因：TRADING-037 已能生成 `PENDING_APPROVAL` retry candidates，但还缺少可审计人工 approval record 读取和 retry execution dry-run 层；本任务严格 dry-run/report-only，不触达真实通知通道或状态变更。2026-05-26: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 dry-run builder、CLI、JSON/Markdown/log、dashboard 只读卡片、runbook、requirements index、system flow、artifact catalog 和专项测试；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，TRADING-038 触达文件 Black 通过。|
|TRADING-037|交易执行/Retry Candidate Queue / Manual Approval Gate|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/retry_candidate_queue_2026-05-26.md`；只读 retry candidate queue / manual approval gate 核心模块、CLI、JSON/Markdown/log、dashboard 只读卡片、runbook、requirements index、system flow、artifact catalog 和专项测试已完成；本任务只读取 latest 或显式 TRADING-036 classification JSON，不执行 retry、不发送 email/webhook/mobile notification、不修改 delivery state、不修改 approval state、不修改 production 参数、不修改 TRADING-035/036 历史 artifact|新增 `scripts/run_retry_candidate_queue.py` 和 `src/ai_trading_system/trading_engine/retry_candidate_queue.py`；输出 `outputs/retry_candidate_queue/retry_candidate_queue_YYYY-MM-DD.json/md/log`；区分 `EMPTY` / `PENDING_APPROVAL` / `BLOCKED` / `SAFETY_BLOCKED` / `SOURCE_UNAVAILABLE`；retryable failure 转为 `PENDING_APPROVAL` candidate，non-retryable failure 转为 blocked item；输出 approval gate、安全不变量和 recommended actions；dashboard 只读读取 TRADING-037 JSON，不运行 generator、不执行 retry、不发送 notification、不修改审批状态；专项测试覆盖 queue 规则、missing/malformed source、deterministic candidate id、Markdown、approval gate、CLI、dashboard 只读和 `--fail-on-safety-blocked`；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和本次改动文件 black check；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入 TRADING-037 格式问题|2026-05-26: 新增并进入 IN_PROGRESS，原因：TRADING-036 已能分类通知投递失败和 retry readiness，但还缺少稳定、可审查、可追踪的 retry candidate queue 与 manual approval gate；本任务严格 read-only queue/report-only，不触达真实通知通道或状态变更。2026-05-26: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 queue builder、CLI、JSON/Markdown/log、dashboard 只读卡片、runbook、requirements index、system flow、artifact catalog 和专项测试；验证通过 `tests/trading_engine/test_retry_candidate_queue.py`、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-031|交易执行/Operator Brief Notification Delivery Preflight|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/operator_brief_notification_delivery_preflight_2026-05-24.md`；只读 delivery preflight 核心模块、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试已完成；本任务不发送 email、不创建或修改 Gmail draft、不调用 Slack/Discord webhook、不推送 mobile notification、不运行 operator brief、notification draft generator、market/backtest/scoring/data download/apply/rollback/broker/replay/trading；下一步观察真实 TRADING-030 artifacts 下的 preflight 结果|新增 `scripts/run_operator_brief_notification_delivery_preflight.py` 和 `src/ai_trading_system/trading_engine/operator_brief_notification_delivery_preflight.py`；读取 latest TRADING-030 notification draft metadata 与 email/chat/mobile draft 文件，校验 TRADING-030 task_id 和安全字段，检查 draft 存在性、非空、checksum、redaction 和敏感字段，读取可选 recipient/channel/approval config，输出 `preflight_status`、`delivery_readiness`、`channel_readiness`、approval validation、safety validation、alerts、recommended next steps 和 run log；dashboard 只读读取 TRADING-031 artifact，不触发 018B-030、031 script、operator brief、notification draft generator、email/Gmail/Slack/Discord/webhook/mobile/broker/replay/trading；专项测试覆盖输入缺失/非法、安全阻断、draft 缺失/空文件、redaction、severity/approval、channel config、不发送通知、dashboard 只读和 output safety invariants；repo 外 smoke 覆盖 PASS、NEEDS_APPROVAL、SAFETY_BLOCKED、BLOCKED；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-030 已能生成 review-only 通知草稿，但缺少独立 delivery preflight 检查草稿是否具备人工发送/未来 delivery dry-run 条件；本任务严格 delivery-preflight-only/read-only，不发送通知、不创建 Gmail draft、不调用 webhook、不运行任何上游或交易流水线。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 delivery preflight builder、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；临时目录 smoke 确认 PASS、NEEDS_APPROVAL、SAFETY_BLOCKED、BLOCKED 四路径和所有 sent/webhook/execution flags=false；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-032|交易执行/Operator Brief Notification Dry-run Dispatch Preview|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/operator_brief_notification_dispatch_preview_2026-05-24.md`；只读 dry-run dispatch preview 核心模块、CLI、JSON/Markdown/latest/run.log、dashboard 只读卡片、runbook、system flow、artifact catalog、requirements 索引和专项测试已完成；本任务不发送 email、不创建 Gmail draft、不调用 Slack/Telegram/Discord/webhook、不读取私密邮箱、不访问外部网络、不读取 `.env` secrets、不修改 scheduler 或 production 配置；下一步观察真实 TRADING-031 artifacts 下的 dispatch preview 结果|新增 `scripts/run_operator_brief_notification_dispatch_preview.py` 和 `src/ai_trading_system/trading_engine/operator_brief_notification_dispatch_preview.py`；读取 latest TRADING-031 preflight、latest TRADING-022 operator brief JSON/Markdown 和 TRADING-030 draft metadata/草稿摘要，生成 `WOULD_SEND` / `NEEDS_APPROVAL` / `SAFETY_BLOCKED` / `BLOCKED` / `NOOP` dispatch preview、channel plan、masked target、subject/title/body excerpt、safety flags、decision、JSON/Markdown/latest/run.log；dashboard 只读读取 TRADING-032 artifact，不重跑 preview pipeline 或发送逻辑；测试覆盖所有状态、缺失 brief、notification disabled、recipient 脱敏、body excerpt 限制、schema 字段、Markdown、无外部发送、path traversal、dashboard 只读和 run.log；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 diff check；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-031 只能判断通知是否具备发送条件，仍缺少发送前可人工审阅的 dry-run dispatch plan；本任务严格 preview-only/read-only，不执行真实发送、不访问网络、不读取 secrets、不自动审批。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 dispatch preview builder、CLI、JSON/Markdown/latest/run.log、dashboard 只读卡片、runbook、system flow、artifact catalog、requirements 索引和专项测试；验证通过 `tests/trading_engine/test_operator_brief_notification_dispatch_preview.py`、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest、ruff 和 diff check；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-033|交易执行/Operator Brief Notification Approval Gate|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/operator_brief_notification_approval_gate_2026-05-24.md`；只读 approval gate 核心模块、CLI、JSON/Markdown/latest/run.log、稳定 preview hash、approval marker 校验、dashboard 只读卡片、runbook、system flow、artifact catalog、requirements 索引和专项测试已完成；本任务不发送 email、不创建 Gmail draft、不调用 SMTP / Slack / Telegram / Discord / webhook、不读取 `.env` secrets、不访问网络、不自动审批、不修改 TRADING-032 artifact 或 production 配置；下一步观察真实 TRADING-032 artifacts 下的 approval gate 结果|新增 `scripts/run_operator_brief_notification_approval_gate.py` 和 `src/ai_trading_system/trading_engine/operator_brief_notification_approval_gate.py`；输出 `data/derived/operator_briefs/notifications/approval_gate/operator_brief_notification_approval_gate_YYYY-MM-DD.json/md`、`latest.json`、`latest.md`、`run.log`；支持 `APPROVED` / `APPROVAL_REQUIRED` / `APPROVAL_EXPIRED` / `APPROVAL_MISMATCH` / `SAFETY_BLOCKED` / `BLOCKED` / `NOOP`；dashboard 只读读取 TRADING-033 artifact，不重跑 approval gate 或 dispatch；测试覆盖状态映射、hash 稳定性、marker 过期/不匹配/非法、safety block 不可由 marker 覆盖、Markdown、CLI 和 dashboard 只读；文档同步 runbook、system flow、artifact catalog 和 requirements 索引；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 diff check；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-032 已能生成 dry-run dispatch preview，但真实 dispatch 前缺少唯一可信人工审批门控 artifact；本任务严格 approval-gate-only/read-only，不执行真实发送、不访问网络、不读取 secrets、不自动审批。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 approval gate builder、CLI、JSON/Markdown/latest/run.log、稳定 preview hash、approval marker 校验、dashboard 只读卡片、runbook、system flow、artifact catalog、requirements 索引和专项测试；验证通过 `tests/trading_engine/test_operator_brief_notification_approval_gate.py`、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest、ruff 和 diff check；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-035|交易执行/Notification Delivery Audit Summary|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/notification_delivery_audit_summary_2026-05-25.md`；只读 audit summary 核心模块、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试已完成；本任务只读取 TRADING-030/031/034 artifacts，不发送 email、不创建或修改 Gmail draft、不调用 Slack/Discord/webhook/mobile、不运行 operator brief、notification draft generator、delivery preflight、draft dispatch、market/backtest/scoring/data download/apply/rollback/broker/replay/trading|新增 `scripts/run_notification_delivery_audit_summary.py` 和 `src/ai_trading_system/trading_engine/notification_delivery_audit_summary.py`；读取 latest TRADING-030 notification draft metadata、latest TRADING-031 delivery preflight、TRADING-034 `draft_dispatch/latest.json` 和 dispatch artifact，校验 task_id/status/safety boundaries、draft/preflight/dispatch path/hash/date/latest 一致性、external side effects 和 lifecycle；输出 `audit_status`、`notification_lifecycle_status`、artifact chain、draft/preflight/dispatch summary、external side effect audit、safety validation、alerts、recommended next steps、manual review instructions 和 run log；dashboard 只读读取 TRADING-035 audit artifact，不触发 018B-034、035 script、operator brief、notification generator、preflight、dispatch 或任何发送/交易路径；测试覆盖 PASS、INCOMPLETE、MISMATCH、SAFETY_BLOCKED、Markdown banners、dashboard 只读和 output safety invariants；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-25: 新增并进入 IN_PROGRESS，原因：TRADING-030/031/034 已能形成通知草稿、delivery preflight 和 draft dispatch，但缺少统一只读审计摘要来记录生命周期完整性、hash/latest 一致性和外部副作用为 false；本任务严格 audit-only/read-only，不执行任何上游或发送动作。2026-05-25: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 audit summary builder、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过 `tests/trading_engine/test_notification_delivery_audit_summary.py`、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-036|交易执行/Notification Delivery Failure Classification / Retry Readiness|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/notification_delivery_failure_classification_2026-05-26.md`；只读 failure classification / retry readiness 核心模块、CLI、JSON/Markdown/log、dashboard 只读卡片、runbook、requirements index、system flow、artifact catalog 和专项测试已完成；本任务只读取既有 TRADING-035 artifact，不发送 email、不创建或修改 Gmail draft、不调用 Slack/Discord/webhook/mobile、不运行 TRADING-035 或上游 notification pipeline、不自动 retry、不修改 production 参数或历史投递状态|新增 `scripts/run_notification_delivery_failure_classification.py` 和 `src/ai_trading_system/trading_engine/notification_delivery_failure_classification.py`；默认读取 latest TRADING-035 audit summary，也支持 `--audit-summary` 指定 JSON；输出 failure classification JSON、Markdown 和 run log，覆盖 `PASS`、`INCOMPLETE`、`MISMATCH`、`SAFETY_BLOCKED`、missing/malformed source 和 unknown status；输出 categories、manual review、safe_to_retry、retry blockers、recommended actions 和 safety invariants；dashboard 只读读取 latest TRADING-036 JSON，不运行 classifier、不读取外部通知状态、不发送 notification；专项测试覆盖分类、CLI、Markdown、safety invariants、dashboard 只读和 missing report graceful handling；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-26: 新增并进入 IN_PROGRESS，原因：TRADING-035 已能回答通知链路审计状态，但还缺少失败类型、重试准备度、人工复核和通知链路阻断判断；本任务严格 read-only classify + report，不执行通知、重试、状态变更或 production 参数变更。2026-05-26: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 classifier builder、CLI、JSON/Markdown/log、dashboard 只读卡片、runbook、requirements index、system flow、artifact catalog 和专项测试；验证通过 `tests/trading_engine/test_notification_delivery_failure_classification.py`、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|DATA-019|数据工程/Cboe VIX 请求缓存新鲜度|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/cboe_vix_cache_freshness_2026-05-21.md`；实现已让 Cboe VIX 静态 CSV 请求级 cache key 纳入 ticker/start/end/interval，并在同一窗口缓存体未覆盖 as-of 终点时强制重新请求；真实 2026-05-19 daily-run 已 PASS，下一步观察后续默认 daily-run 是否持续不复用过期 `VIX_History.csv` 响应|`CboeVixPriceProvider` 对可变静态 `VIX_History.csv` 的请求级缓存必须包含评估窗口身份或等价 freshness 约束；既有旧 cache 不得继续让 `^VIX` 停在 2026-05-11；`download-data` 后 `prices_daily.csv` 中 `^VIX` 至少覆盖到 as-of 2026-05-19；`aits validate-data --as-of 2026-05-19` 与 `aits ops daily-run` 不再因 `^VIX` stale 阻断；测试、系统流图和需求文档同步更新|2026-05-21: 新增并进入 IN_PROGRESS，原因：真实 daily-run 连续停在 `score_daily`，只读诊断确认 Cboe 官方端点已更新到 2026-05-19，但项目外部请求级 cache 命中 2026-05-13 的旧静态 URL 响应，导致 manifest downloaded_at 刷新而 `^VIX` 最新观测仍为 2026-05-11。2026-05-21: 从 IN_PROGRESS 改为 VALIDATING，原因：Cboe VIX provider cache identity 已纳入 ticker/start/end/interval，且旧缓存体若未覆盖请求 `end` 会重新抓取；聚焦测试、请求缓存相关测试、ruff、git diff --check 和全量 pytest 824 passed；真实 `aits ops daily-run` 默认 as-of=2026-05-19 已 15/15 PASS，Run ID=`daily_ops_run:2026-05-19:20260520T165804Z`，`^VIX` 覆盖到 2026-05-19，data_quality PASS。|
|TRADING-014A|交易执行/Successful Fill Local Validation|P0|BLOCKED_EXTERNAL|系统验证 + 项目 owner|详细拆解见 `docs/requirements/ibkr_paper_regular_session_fill_followup_2026-05-18.md`；下一步等待美股 `REGULAR` session，人工显式运行 controlled fill；只有 `market_session_status=REGULAR` 且 `fill_seen=true` 才进入本任务|固化真实 IBKR Paper controlled fill 成功样本的 sanitized review / fixture，记录 `production_effect=none`、paper-only、manual-only、`market_session_status=REGULAR`、`fill_seen=true`、fill quantity / price、commission / execution event 是否可见、同 `OrderIntent` 的本地 `PaperBroker` 诊断对比；不得提交完整 `DUP` account、真实 broker order id、perm id、execution id、password、token 或 API key；本任务不修改 `PaperBroker` fill 行为|2026-05-18: 新增并进入 BLOCKED_EXTERNAL，原因：owner 明确 TRADING-014 后最有价值的是获取真实 regular session fill 样本；无 `fill_seen=true` 样本前不得进入真正 fill model 改造。|
|TRADING-014B|交易执行/Fill Failure Diagnostics|P0|BLOCKED_EXTERNAL|系统验证 + 项目 owner|详细拆解见 `docs/requirements/ibkr_paper_regular_session_fill_followup_2026-05-18.md`；仅当美股 `REGULAR` session controlled fill 仍输出 `fill_seen=false` 时启动；当前等待下一次真实 regular session 样本|诊断 regular session no-fill 的 broker rejection、market data / quote stale、limit price 不可成交、order status path 异常、TWS / IB Gateway 连接事件、contract details / liquid hours 解析问题；输出 issue classification、下一步检查建议和脱敏 review；不得把 no-fill 直接归因于 `PaperBroker` fill model；不修改 `PaperBroker` fill 行为|2026-05-18: 新增并进入 BLOCKED_EXTERNAL，原因：如果 regular session 仍 no-fill，需要先做 failure diagnostics，而不是用失败样本改造 `PaperBroker`。|
|TRADING-015|交易执行/Weight Adjustment Candidate Generator|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/weight_adjustment_candidate_generator_2026-05-18.md`；observe-only 候选生成层、policy、standalone script、dashboard 只读卡片、系统流图、产物目录和测试已完成；不接 daily-run 自动调度，不修改 production 权重；下一步观察真实 daily/paper/shadow 输入样本下 candidate gate 是否保持保守语义|新增 `outputs/reports/weight_adjustment_candidates_YYYY-MM-DD.json/md`；输入只读读取 daily decision summary、paper signal quality、shadow parameter impact、可选 continuous replay、parameter governance、current production profile 和 existing shadow profile；每个 candidate 包含 source/target profile、parameter_changes、reason_codes、expected_effect、risk_notes、blocked_by、required_validations 和 `production_effect=none`；第一版只允许小幅权重调整、单日最大变化受 policy 限制、总权重不失衡、不删除 core risk gate、不绕过 data quality gate、不根据单日 PnL 调高权重；candidate gate 覆盖 insufficient sample、low data quality、synthetic snapshot ratio too high、continuous replay missing、shadow impact insufficient、paper signal quality unreliable 和 manual approval required；daily dashboard 只读展示 candidate_count、top_candidate_id、gate_status、main blocked_by、report link 和 `production_effect=none`，不触发候选生成、调参应用或交易；目标 pytest、全量 pytest、ruff 和 black check 通过|2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求新增每日权重调节候选生成层，当前目标不是自动修改 production 权重、启动定时任务、改变 dashboard 主结论或触发交易。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `config/weight_adjustment_candidate_policy.yaml`、`scripts/run_weight_adjustment_candidates.py`、`weight_adjustment_candidates_YYYY-MM-DD.json/md` 生成器、保守 candidate gate、单日权重变化上限、dashboard Weight Adjustment Candidate 只读卡片、系统流图/产物目录和测试；验证通过目标 pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。|
|TRADING-016|交易执行/Weight Candidate Evaluation|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/weight_candidate_evaluation_2026-05-18.md`；只读候选权重评估层、policy、standalone script、dashboard 轻量卡片、系统流图、产物目录和测试已完成；本地验收和 GitHub Actions 已通过；下一步观察真实 TRADING-015 / paper / shadow / replay 输入下 gate 是否保持保守语义|新增 `outputs/reports/weight_candidate_evaluation_YYYY-MM-DD.json/md`；固定 `production_effect=none`、`evaluation_mode=observe_only`；7d/14d/30d 窗口输出 candidate_count、evaluable/blocked/insufficient/low-quality 统计、continuous replay availability、synthetic ratio、OHLC coverage、reconciliation PASS、paper/shadow status、replay mode 和可用的 final equity / max drawdown / exposure / concentration delta；candidate 级 `evaluation_status` 只允许保守枚举，gate 覆盖 candidate blocked、manual approval、insufficient sample、low data quality、synthetic ratio、continuous replay missing、shadow impact insufficient、paper signal quality unreliable、reconciliation unreliable、drawdown/exposure/concentration worse；dashboard 只读卡片展示 evaluation_status、candidate_count、evaluable_candidate_count、top_candidate_id、main blocked_by、report link 和 `production_effect=none`；目标 pytest、全量 pytest、ruff、black、push 后 CI 通过|2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-015 后继续推进 Weight Candidate Evaluation；当前阶段只允许 observe-only manual review 入口，不允许自动调参、production promotion、交易触发或 dashboard 主结论变更。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `config/weight_candidate_evaluation_policy.yaml`、`scripts/run_weight_candidate_evaluation.py`、`weight_candidate_evaluation_YYYY-MM-DD.json/md` 生成器、7/14/30 日窗口 gate、candidate scorecard、dashboard Weight Candidate Evaluation 只读卡片、系统流图/产物目录和测试；验证通过目标 pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。2026-05-18: GitHub Actions CI run `25996830767` 对 commit `aa84423e756428c2531a58fc77bd2252b379a31e` 完成且通过。|
|TRADING-017|交易执行/Weight Promotion Gate|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/weight_promotion_gate_2026-05-18.md`；manual-review-only promotion gate、policy、standalone script、dashboard 轻量卡片、系统流图、产物目录和测试已完成；本地验收和 GitHub Actions 已通过；下一步观察真实 TRADING-015/016、paper、shadow、replay 输入下 gate 是否保持保守语义|新增 `outputs/reports/weight_promotion_gate_YYYY-MM-DD.json/md`；读取同日 weight adjustment candidates、weight candidate evaluation、shadow parameter impact、paper signal quality、可选 replay、daily summary 和 governance/profile metadata；固定 `production_effect=none`、`gate_mode=manual_review_only`；candidate 级 status 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`BLOCKED`、`NO_CLEAR_IMPROVEMENT`、`CANDIDATE_PROMISING_BUT_LIMITED`、`READY_FOR_MANUAL_REVIEW`；必须阻断缺 candidate evaluation、candidate blocked、manual approval 缺失、样本/filled 不足、数据质量差、synthetic ratio 高、OHLC coverage 低、reconciliation 低、continuous replay 缺失、drawdown/exposure/concentration 恶化、shadow/paper unreliable、data gate BLOCK 和重大 risk warning；dashboard 只读展示 gate_status、candidate_count、ready_for_manual_review_count、blocked_count、top_candidate_id、main blocked_by、report link 和 `production_effect=none`，不触发 gate 重跑|2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-015/016 后新增候选权重 promotion gate；当前阶段只允许进入人工复核判断，不允许自动晋级、production profile 修改、approved profile 写入、IBKR / PaperBroker / replay runner 或交易触发。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `config/weight_promotion_gate_policy.yaml`、`scripts/run_weight_promotion_gate.py`、`weight_promotion_gate_YYYY-MM-DD.json/md` 生成器、manual-review-only gate、dashboard Weight Promotion Gate 只读卡片、系统流图/产物目录和测试；验证通过目标 pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。2026-05-18: GitHub Actions CI run `25997552867` 对 commit `a86fde5a207a6fffa3471dde46dcc664def184ce` 完成且通过。|
|TRADING-018|交易执行/Daily Scheduled Weight Adjustment Job|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/daily_weight_adjustment_job_2026-05-18.md`；observe-only daily summary、standalone script、dashboard 轻量卡片、runbook、系统流图、产物目录和测试已完成；当前不自动启用定时任务，不修改 production profile，不写 approved profile，不触发 IBKR / PaperBroker / replay runner / controlled fill / lifecycle / comparison 脚本；下一步 push 后确认 GitHub Actions CI|新增 `scripts/run_daily_weight_adjustment.py` 和 `outputs/reports/daily_weight_adjustment_summary_YYYY-MM-DD.json/md`；默认 `mode=observe_only`、`production_effect=none`、`manual_review_only=true`；只读取同日 `weight_adjustment_candidates_YYYY-MM-DD.json/md`、`weight_candidate_evaluation_YYYY-MM-DD.json/md`、`weight_promotion_gate_YYYY-MM-DD.json/md`；summary 至少包含 as_of、production_effect、mode、candidate_status、evaluation_status、promotion_gate_status、candidate_count、evaluable_candidate_count、ready_for_manual_review_count、blocked_count、top_candidate_id、main_blocked_by、warnings、source_artifacts、missing_artifacts、required_manual_review_items 和 recommendation；缺任一上游 artifact 时只标记 `LIMITED` / `INSUFFICIENT_DATA`，不补造 improvement，且不得把 promotion gate 摘要输出为 `READY_FOR_MANUAL_REVIEW`；daily dashboard 只读读取 summary JSON 展示 candidate_count、evaluation_status、promotion_gate_status、ready_for_manual_review_count、main blocked_by、report link、`production_effect=none` 和 manual_review_only；新增 runbook，说明手动运行和未来调度接入；本地验收通过目标 pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check；push 后 CI 通过|2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-015/016/017 完成后串入每日只读权重调节评估流程；本阶段只做 observe-only summary 和 dashboard 可见性，不允许任何 production 权重修改、approved profile 写入、broker / paper runner / replay runner 调用或 dashboard 主投资结论变更。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `scripts/run_daily_weight_adjustment.py`、`daily_weight_adjustment_summary_YYYY-MM-DD.json/md` 生成器、缺失输入降级、安全边界、dashboard Daily Weight Adjustment Summary 只读卡片、runbook、系统流图/产物目录和测试；验证通过目标 pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。|
|TRADING-018A|交易执行/Daily Weight Adjustment Scheduler Dry Run|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/daily_weight_adjustment_scheduler_dry_run_2026-05-18.md`；scheduler dry-run wrapper、Windows Task Scheduler runbook、系统流图、产物目录和测试已完成；下一步 push 后确认 GitHub Actions CI，并按 runbook 连续观察 3～7 个美股交易日|新增 `scripts/run_daily_weight_adjustment_scheduler_dry_run.py` 和 `outputs/reports/daily_weight_adjustment_scheduler_dry_run_YYYY-MM-DD.json/md`；dry-run 固定 `mode=dry_run`、`production_effect=none`、`manual_review_only=true`；封装同日 `run_daily_weight_adjustment.py` summary pipeline；输出 as_of、started_at、completed_at、duration_seconds、dry_run_status、invoked_command、generated_artifacts、missing_artifacts、pipeline_status、candidate_count、promotion_gate_status、ready_for_manual_review_count、blocked_count、warnings 和 safety_checks；安全检查证明未写 production profile、未写 approved profile、未调用 IBKR、PaperBroker、replay、controlled fill、lifecycle 或 comparison；Windows Task Scheduler runbook 说明本地手动运行、每日调度、日志、artifact 检查、GitHub repo 不自动 commit、失败处理和连续 3～7 天观察；验证通过目标 pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check|2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-015/016/017/018 完成后推进每日权重调节 scheduler dry-run；本阶段只封装和验证调度安全性，不允许自动应用权重、写 approved profile、触发 broker、paper order、replay 或后续 lifecycle/comparison。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 scheduler dry-run 脚本、JSON/Markdown report schema、显式 safety checks、Windows Task Scheduler runbook、GitHub Actions schedule 暂不启用说明、系统流图/产物目录和测试；验证通过目标 pytest、`tests/trading_engine`、`tests/test_daily_task_dashboard.py`、全量 pytest、ruff 和 black check。|
|TRADING-018B|交易执行/Daily Shadow Weight Iteration State|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/daily_shadow_weight_iteration_2026-05-19.md`；shadow-only current state、daily candidate/report/log、policy、standalone script、dashboard 只读卡片、runbook、系统流图、产物目录和测试已完成；下一步观察真实 TRADING-015/016/017/018/018A artifact 下 UPDATE/NO_UPDATE/SAFETY_BLOCKED 语义是否保持保守|新增 `scripts/run_daily_shadow_weight_iteration.py`、`data/derived/weight_iterations/shadow/current_shadow_weights.json`、history/candidates/logs JSON/Markdown；首次缺 current state 时从 production profile snapshot 复制，缺 production source 时使用 policy conservative default 并披露 `initialization_source`；每日 candidate decision 只允许 `UPDATE`、`NO_UPDATE`、`INSUFFICIENT_DATA`、`SAFETY_BLOCKED`、`ERROR`；缺 policy、缺 artifact、scheduler `report_type` 不匹配、scheduler safety 失败或信号不足不得更新 current shadow state；成功 UPDATE 必须应用 max abs/relative delta clamp、归一化到 1.0、写 history snapshot 并原子替换 current；所有输出固定 `production_effect=none`、`manual_review_only=true`；dashboard 只读展示 current shadow weights、最近 decision、delta、candidate report link，不重跑 pipeline；目标 pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check 通过|2026-05-19: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018/018A 后进入 shadow learn 阶段；本阶段只维护独立 shadow 参数状态，禁止 production promotion、production 权重修改、真实交易执行或 dashboard 主结论变更。2026-05-19: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 shadow iteration policy、builder、CLI、candidate/current/history/log 输出、原子 current 更新、dashboard 只读卡片、runbook、系统流图/产物目录和测试；验证通过目标 pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。2026-05-20: 提交前 hardening，原因：policy 缺失和 TRADING-018A scheduler `report_type` 不匹配均阻断 shadow update，confidence 默认值和 conservative 初始化权重纳入 policy。|
|TRADING-014C|交易执行/Fill Model Calibration Proposal|P0|BLOCKED_EXTERNAL|系统设计 + 项目 owner|详细拆解见 `docs/requirements/ibkr_paper_regular_session_fill_followup_2026-05-18.md`；阻塞于 `TRADING-014A` 真实 `fill_seen=true` 样本和本地验证完成；本任务只做 proposal，不直接实现 fill 行为改造|基于真实 regular session fill evidence 提出可审计 calibration 方案，说明 `PaperBroker` 与 IBKR Paper 行为差异、数据需求、候选调整项、预期影响、风险、回归测试范围、报告语义变化和退出条件；任何阈值或启发式必须进入 reviewed policy / manifest 或有注释的不可调常量；实际修改 `PaperBroker` fill 行为前需要单独任务和 owner 决策|2026-05-18: 从原未提交 `TRADING-016` 改为 `TRADING-014C`，原因：owner 本轮明确 `TRADING-016` 用于 Weight Candidate Evaluation；fill model proposal 仍保持 BLOCKED_EXTERNAL，当前缺真实 fill 样本，不应提前进入 fill model 改造。|
|TRADING-008|交易执行/Shadow Parameter Impact Evaluation|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/shadow_parameter_impact_evaluation_2026-05-17.md`；observe-only impact report、policy manifest、standalone script、dashboard 轻量入口、测试和文档已完成；下一步观察真实 production / shadow paper 样本是否积累到可比较窗口|新增 `outputs/reports/shadow_parameter_impact_YYYY-MM-DD.json/md`，只读读取最近 7/14/30 日 `order_intent_candidates`、`paper_trading_summary`、`paper_signal_quality` 和可选已有 `paper_trading_replay`；按 `source_profile` / `mode` / `strategy_version` 区分 production / shadow / unknown；比较样本、订单状态、paper PnL、continuous replay final_equity/max_drawdown、snapshot 质量、reconciliation、blocked_by/reason_code 和 confidence bucket performance；impact gate 至少覆盖样本不足、低数据质量、synthetic snapshot 过高、daily-independent-only 和 reconciliation 不可靠；`impact_status` 只能使用保守状态集合；dashboard 只读展示 Shadow Impact 摘要，不触发 replay / broker / 参数晋级；目标 pytest、全量 pytest、ruff 和 continuous replay smoke 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-007A CI 通过后推进 shadow 参数 impact 观察层，目标是判断 shadow 是否真实改善 paper / replay / signal quality 表现，而不是上线参数或扩展交易执行。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `config/shadow_parameter_impact_policy.yaml`、`scripts/run_shadow_parameter_impact.py`、`shadow_parameter_impact_YYYY-MM-DD.json/md` 生成器、impact gate、7/14/30 日 production vs shadow/unknown profile 对比、dashboard Shadow Impact 只读卡片、系统流图/产物目录和测试；验证通过目标 pytest、全量 pytest、ruff、continuous replay smoke、shadow impact smoke 和 docs freshness。|
|TRADING-008A|交易执行/Shadow Impact Hardening|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/shadow_parameter_impact_evaluation_2026-05-17.md#trading-008a-shadow-impact-hardening`；前置 GitHub Actions `CI` run `25986926245` 已确认对 commit `a720b95436f8481011145553399c6ec0b01c2e4c` 完成且通过；hardening 已完成，下一步随真实 production / shadow paper 样本观察 impact 输出是否保持保守语义|`impact_status` 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`SHADOW_PROMISING_BUT_LIMITED`、`NO_CLEAR_IMPROVEMENT`、`SHADOW_UNRELIABLE`、`LOW_DATA_QUALITY`；JSON/Markdown/dashboard 输出不得出现 production promotion/live trade/approval 危险语义；JSON 必须包含 policy id/version/threshold snapshot 和 `production_effect=none`，Markdown 展示 policy version 和关键 thresholds；impact gate 每个 blocker/warning 必须有 human-readable explanation；缺 continuous replay 时输出 `continuous_replay_missing` warning，存在 continuous replay 时记录 artifact path/mode/date range；dashboard 只读摘要，不触发 replay、broker、参数晋级或读取 broker API key；目标 pytest、全量 pytest、ruff 和 black check 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-008 基础版后做 hardening，防止短样本 shadow PnL、daily-independent replay 或 dashboard 文案被误读为上线、实盘、交易批准或参数晋级证据。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现保守枚举含 `LOW_DATA_QUALITY`、危险语义扫描、policy snapshot 断言、impact_gate blocker/warning explanation、`continuous_replay_missing` warning、continuous replay source path/mode/date range、daily-independent 非 continuous 防护和 dashboard 只读摘要测试；验证通过目标 pytest、全量 pytest、ruff 和 full black check。|
|TRADING-009|交易执行/IBKR Paper read-only sync|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/ibkr_paper_readonly_sync_2026-05-17.md`；只读 adapter、默认关闭配置、snapshot JSON/Markdown、PaperPortfolio 对账 scaffold、系统流图、mock 测试和真实 TWS Paper snapshot 验证已完成；下一步继续观察后续 IBKR Paper snapshot 样本；不提交订单、不接 production account、不读取 broker API key|新增 `config/ibkr_paper_readonly.yaml` 默认 `enabled=false`、`trading_mode=paper`、`readonly=true`、`production_effect=none`；新增只读 adapter 仅允许 connect/disconnect/account summary/positions/open orders/executions/contract details，`submit_order` 抛明确错误；非 paper 或 readonly=false fail closed；account id 仅 masked 输出，非 paper/DUP account 至少 LIMITED/BLOCK；snapshot 输出 `outputs/reports/ibkr_paper_account_snapshot_YYYY-MM-DD.json/md`，含 connection_status、masked account、summary、positions、open orders、recent executions、contract sample、reconciliation_status、`production_effect=none`、`readonly=true`；mock client 测试覆盖默认关闭、安全边界、脱敏、无真实网络和无密钥读取；真实 TWS Paper snapshot 已确认 connected / PASS 且无 submit/cancel/placeOrder 痕迹；目标 pytest、全量 pytest、ruff、black、push 后 CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-008A post-merge sanity review 和 CI 通过后推进 IBKR Paper 只读同步，用于验证 IB Gateway / TWS Paper 连接、账户状态读取和本地 PaperPortfolio 对账能力；本轮不提交订单，保持 read-only、paper-only、`production_effect=none`。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `IBKRPaperReadOnlyAdapter`、`config/ibkr_paper_readonly.yaml`、`scripts/run_ibkr_paper_readonly_snapshot.py`、`IBKRPaperReadOnlyReconciliation` scaffold、mock client 测试和系统流图更新；本地验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。2026-05-17: 继续保持 VALIDATING，原因：真实 TWS Paper API socket `127.0.0.1:7497` 已连接成功，修复 Python 3.14 `ib_insync` event loop 初始化和 AccountValue / Position 序列行解析后，`ibkr_paper_account_snapshot_2026-05-17.json/md` 生成且 `snapshot_status=PASS`、`reconciliation_status=PASS`、account id masked、`production_effect=none`、`readonly=true`，未出现 submit/cancel/placeOrder。|
|TRADING-009A|交易执行/IBKR Paper read-only local validation report|P0|VALIDATING|系统验证|本机验证 runbook、local config / snapshot ignore 规则、sanitized fixture、`--config` CLI alias 和测试已完成；下一步随后续本机 IBKR Paper snapshot 样本观察 runbook 是否需要补充|`docs/runbooks/ibkr_paper_readonly_local_validation.md` 必须说明 TWS / IB Gateway Paper 启动、API socket 开启、推荐 port/client_id、本地 `config/ibkr_paper_readonly.local.yaml` 准备、local config 不提交、snapshot 命令和常见错误；`.gitignore` 必须覆盖 `config/*.local.yaml` 和 IBKR snapshot 输出；sanitized fixture 不含完整 account id、真实姓名、邮箱、现金细节或敏感标识；本机 checklist 覆盖 masked account、`readonly=true`、`production_effect=none`、`submit_order` 不可用、未读取 broker API key、未写入完整账号；测试覆盖 `.local.yaml` 不被测试依赖、fixture 不含完整 DUP 账号格式、snapshot writer 默认 mask account id；目标 pytest、全量 pytest、ruff、black、push 后 CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-009 完成且 CI 通过后，固化本机 IBKR Paper 只读连接验证流程和安全边界；本阶段只记录/验证 read-only local workflow，不提交敏感配置。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增本机验证 runbook、`.gitignore` local/snapshot 规则、sanitized fixture、`--config` snapshot CLI alias 和安全边界测试；验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。|
|TRADING-010|交易执行/IBKR Paper order lifecycle test|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/ibkr_paper_order_lifecycle_2026-05-17.md`；Paper-only lifecycle adapter、默认关闭配置、人工显式脚本、JSON/Markdown 报告、runbook、系统流图和 mock 测试已完成；TRADING-010A 已固化 2026-05-17 本机 TWS Paper lifecycle PASS 样本；下一步观察后续真实 Paper lifecycle 样本并继续确认 CI 结果|新增 `config/ibkr_paper_order.yaml` 默认关闭且不保存凭证；新增 `src/ai_trading_system/trading_engine/brokers/ibkr_paper_order.py` 和 `scripts/run_ibkr_paper_order_lifecycle.py`；仅允许 `trading_mode=paper`、`paper_order_lifecycle_enabled=true`、`production_effect=none`、`DUP` account；订单仅允许白名单股票、`LIMIT`、`DAY`、`BUY/SELL`、极小数量，拒绝 market/option/short/margin/GTC/bracket/stop/trailing/algo；默认使用远离市场价限价，无法取得参考价时不提交并输出 `LIMITED`；输出 `outputs/reports/ibkr_paper_order_lifecycle_YYYY-MM-DD.json/md`，含 lifecycle_status、connection_status、account_id_masked、submitted_order、broker_order_id、order_status_events、open_order_seen、cancel_requested、final_order_status、cancelled_confirmed、fills_seen、safety_checks、issues；mock 测试覆盖 fail-closed、安全边界、正常 submit/open/cancel/cancelled、异常仍写 ERROR/BLOCK 报告、脱敏、不读 API key、不调用 live broker；本地验收、push 和 GitHub Actions CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 明确 TRADING-009/009A 只读 snapshot 已通过，要求推进 IBKR Paper 专用订单生命周期测试能力；本阶段只验证 Paper 账号 submit/status/cancel/report 链路，不允许 live trading、production 自动交易或任何生产仓位解释影响。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `IBKRPaperOrderLifecycleAdapter`、`config/ibkr_paper_order.yaml`、`scripts/run_ibkr_paper_order_lifecycle.py`、lifecycle JSON/Markdown writer、Paper-only safety checks、runbook、系统流图和 mock client 测试；本地验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。2026-05-17: 继续保持 VALIDATING，原因：本机 TWS Paper order lifecycle 已生成 PASS 样本并通过 TRADING-010A 脱敏 review / fixture 固化；样本为 `LIMIT DAY` stock、NVDA BUY 1 @ 10.0、final `ApiCancelled`、无 fill、`production_effect=none`，未触发 daily-run / replay / dashboard / production conclusion。|
|TRADING-010A|交易执行/IBKR Paper order lifecycle local validation report|P0|VALIDATING|系统验证|本机 TWS Paper order lifecycle 验证结果、sanitized fixture、runbook 成功后/意外成交/live boundary 检查和安全语义测试已完成；下一步随未来本机 Paper lifecycle 样本观察 runbook 是否需要补充|新增 `docs/reviews/ibkr_paper_order_lifecycle_local_validation_2026-05-17.md` 记录本机验证状态、TWS Paper / IB Gateway Paper 连接说明、`LIMIT DAY` stock 订单字段、submitted/openOrder/orderStatus/cancel/final cancelled 状态、是否 fill、`production_effect=none` 和无 production surface 影响；新增 `tests/fixtures/ibkr_paper_order_lifecycle_sanitized.json`，account id masked、broker order id redacted、不含凭证值；runbook 增加验证成功后检查清单、意外成交处理和确认无 live account / production effect 的步骤；测试覆盖 fixture 不含完整 `DUP` account、review 固定 `production_effect=none`、不含凭证字段、明确 paper-only，且不含 live/trade/promotion 危险语义；目标 pytest、全量 pytest、ruff、black、push 后 CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-010 Paper lifecycle 本机运行后，固化本机验证结果和安全边界，确保 committed artifact 只保留脱敏、paper-only、无生产效果的审计证据。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增本机验证 review、sanitized fixture、runbook 检查清单和报告/fixture 安全边界测试；本地验证通过目标 pytest、`tests/trading_engine`、全量 pytest、dashboard 目标测试、ruff 和 black check。|
|TRADING-011|交易执行/Local PaperBroker vs IBKR Paper lifecycle/fill comparison|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paperbroker_vs_ibkr_paper_comparison_2026-05-17.md`；diagnostic comparison CLI、JSON/Markdown 报告、IBKR Paper comparison 开关、系统流图和 mock 测试已完成；下一步提交、push 并确认 GitHub Actions CI；本任务不扩展下单能力、不接 live、不接 production 自动交易|新增 `scripts/run_paperbroker_vs_ibkr_paper_comparison.py`，输出 `outputs/reports/paperbroker_vs_ibkr_paper_comparison_YYYY-MM-DD.json/md`；报告固定 `production_effect=none`、`comparison_mode=diagnostic_only`，并声明不影响 daily-run、dashboard production conclusion、paper signal quality、shadow impact 或参数晋级；只允许显式 `OrderIntent` fixture 或 CLI 参数构造的 `stock` / `LIMIT` / `DAY` / `BUY` / `quantity=1` / `NVDA|AAPL|TSM`；拒绝 market、option、short、margin、GTC、bracket/stop/algo 和 live account；IBKR 侧必须 `paper_order_lifecycle_enabled=true` 且 `ibkr_paper_comparison_enabled=true`，只允许 `DUP` account；本地记录 PaperBroker status/open/fill/avg price/cancel/final/reconciliation，IBKR 记录 masked/redacted broker order id、openOrder、orderStatus、cancel、final、fills、reconciliation；diff 输出 status/fill/cancel match、local filled but IBKR not filled、broker rejected、price source、reference price 和 lifecycle gaps；差异只标记 `EXPECTED_DIFFERENCE` / `LOCAL_SIM_TOO_OPTIMISTIC` / `BROKER_REJECTED` / `INSUFFICIENT_MARKET_DATA` / `CANCEL_TIMING_DIFFERENCE`，recommendations 只提出后续 quality feedback，不直接修改 PaperBroker；目标 pytest、trading_engine、全量 pytest、ruff、black、push 后 CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 明确 TRADING-010/010A 已完成本机验证，要求推进本地 `PaperBroker` 与 IBKR Paper lifecycle/fill 的诊断比较；本阶段只输出 comparison artifact，不改变生产结论、参数晋级、paper signal quality 或 `PaperBroker` 行为。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `paperbroker_ibkr_comparison` diagnostic-only 核心逻辑、人工显式 CLI、`ibkr_paper_comparison_enabled` 默认关闭开关、comparison JSON/Markdown writer、broker order id redaction、系统流图和 mock client 测试；本地验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。|
|TRADING-011A|交易执行/Comparison local validation & findings|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paperbroker_vs_ibkr_paper_comparison_local_validation_2026-05-17.md`；本机真实 IBKR Paper comparison PASS 样本、sanitized fixture、runbook、安全语义测试和本地验收已完成；下一步 push `main` 并确认 GitHub Actions CI；本任务不修改 PaperBroker 行为，不进入 fill model calibration|新增本机验证 review，记录 TRADING-011 comparison 真实 Paper 样本：NVDA BUY LIMIT DAY 1 @ 10、本地 PaperBroker no fill/cancelled、IBKR orderStatus `PendingSubmit`/`PreSubmitted`/`PendingCancel`/`Cancelled`、fills_seen=false、status/fill/cancel match；新增 sanitized fixture，不含完整 `DUP` account、真实 broker order id、password/token/API key/session cookie；runbook 记录前置 TWS Paper / IB Gateway Paper、API socket、local config comparison 开关、fail-closed 检查和验证后处理；测试覆盖 review/fixture/runbook 的 paper-only、`comparison_mode=diagnostic_only`、`production_effect=none`、无敏感信息、无 live/promotion 危险语义；目标 pytest、trading_engine、全量 pytest、ruff、black、push 后 CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：TRADING-011 已完成并通过 CI，本机真实 comparison 已跑通，需要把 local validation evidence 固化为脱敏、可审计、无生产效果的 committed artifact，并明确 TRADING-012 之前不修改 fill model。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 TRADING-011A requirement、local validation review、sanitized fixture、comparison local validation runbook 和安全语义测试；真实本机样本为 `comparison_status=PASS`、`comparison_mode=diagnostic_only`、`production_effect=none`、本地和 IBKR 均 cancel/no fill/status match；本地验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。|
|TRADING-007A|交易执行/continuous replay hardening & visibility|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/continuous_replay_hardening_visibility_2026-05-17.md`；continuous replay summary、Markdown 解释、daily task dashboard latest replay 可见性和 paper signal quality warning 语义已完成；GitHub Actions run `25986442870` 对 commit `75cba8fe37621c73e2c0e32334e3a0f78c3ad475` 已完成且通过，下一步观察后续真实 daily flow 样本|`paper_trading_replay_START_END.json` 必须明确输出 replay/order policy/continuous metrics/final portfolio/max drawdown/concentration 字段；Markdown 必须区分 `daily-independent` 与 `continuous-portfolio`，并声明 continuous replay 不是真实账户收益、真实 broker 成交、完整税费/滑点模拟或实盘上线依据；dashboard Paper Trading Trend 只读识别最近已有 replay，continuous 时展示 final_equity、max_drawdown、exposure_peak、final_positions count 和 carry-forward；paper_signal_quality 使用 continuous replay 时不得误报 `DAILY_INDEPENDENT_ONLY`，但必须保留 `PAPER_ONLY_SIMULATION` warning，且不得出现 live/trade/promotion 语义；目标 pytest、全量 pytest、ruff、两种 replay smoke、提交 push 和 GitHub Actions CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求推进 TRADING-007A，在已实现 continuous replay 后补齐语义、可见性和 paper signal quality 边界；真实 broker 和订单类型扩展仍不在范围内。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 replay JSON/Markdown summary hardening、DAY/GTC policy 可见性、final portfolio metrics、dashboard latest replay summary、paper signal quality `PAPER_ONLY_SIMULATION` warning 和 continuous replay 下移除 `DAILY_INDEPENDENT_ONLY`；本地验证通过 `python -m pytest tests/trading_engine`、`python -m pytest tests/test_daily_task_dashboard.py`、`python -m pytest`、`python -m ruff check scripts src tests`、两种 replay smoke 和 `git diff --check`。2026-05-17: CI 确认完成，GitHub Actions `CI` run `25986442870` 对 commit `75cba8fe37621c73e2c0e32334e3a0f78c3ad475` 状态为 `completed/success`。|
|TRADING-012|交易执行/PaperBroker Fill Model Calibration Diagnostics|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paperbroker_fill_model_calibration_2026-05-17.md`；diagnostic-only calibration report、policy manifest、standalone script、系统流图、artifact catalog 和测试已完成；本机 2026-05-17 report 读取 1 个 comparison 和最新 replay quality flags，输出 `LIFECYCLE_ALIGNED_FILL_UNTESTED`、`comparison_count=1`、`fill_tested=false`；下一步提交、push 并确认 GitHub Actions CI|新增 `outputs/reports/paperbroker_fill_model_calibration_YYYY-MM-DD.json/md` 和 `scripts/run_paperbroker_fill_model_calibration.py`；只读读取最近 N 个 `paperbroker_vs_ibkr_paper_comparison_*.json`、可选 replay `quality_flags` 和可选 `paper_signal_quality`；统计 comparison/lifecycle/status/fill/cancel/local optimistic/broker rejection/market data/synthetic/no-fill lifecycle 指标；`calibration_status` 只允许 `INSUFFICIENT_SAMPLE`、`LIFECYCLE_ALIGNED_FILL_UNTESTED`、`LOCAL_SIM_TOO_OPTIMISTIC`、`BROKER_REJECTION_GAP`、`OBSERVE_ONLY`，禁止 live/trade/promotion/approval 语义；报告固定 `production_effect=none`、`calibration_mode=diagnostic_only`，并声明不影响 replay、paper signal quality、shadow impact、production conclusion 或交易建议；目标 pytest、trading_engine、全量 pytest、ruff、black、commit、push 后 CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-011/011A 完成本机 no-fill lifecycle comparison 后，先建立 `PaperBroker` fill model calibration diagnostics，用于解释本地模拟与 IBKR Paper 差异；本任务只收集和解释证据，不调整 fill model。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 calibration policy、report builder、CLI、JSON/Markdown 输出、系统流图/产物目录和安全边界测试；本机报告为 `LIFECYCLE_ALIGNED_FILL_UNTESTED`、`comparison_count=1`、`lifecycle_match_ratio=100%`、`fill_tested=false`，recommendations 明确不修改 fill model；验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。|
|TRADING-013|交易执行/IBKR Paper Controlled Small Fill Test|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/ibkr_paper_controlled_fill_test_2026-05-17.md`；受控 IBKR Paper 小额成交测试脚本、默认关闭配置、PaperBroker 诊断对比、JSON/Markdown 报告、runbook、系统流图和 mock 测试已完成；下一步提交、push 并确认 GitHub Actions CI；本阶段仍禁止 live trading、production 自动交易和 PaperBroker fill 行为修改|新增 `scripts/run_ibkr_paper_controlled_fill_test.py` 和 `config/ibkr_paper_controlled_fill.yaml`，默认 `controlled_fill_enabled=false`、`trading_mode=paper`、`production_effect=none`、`max_quantity=1`；人工显式运行，只允许 `stock` / `BUY` / `LIMIT` / `DAY` / `quantity=1` / 白名单 symbol / 手工 limit price；必须连接 IBKR Paper、校验 `DUP` account、等待 `orderStatus` / `execDetails` / `commissionReport`，成交则记录 fill，timeout 未成交则 cancel 并输出 `LIMITED`；报告输出 `outputs/reports/ibkr_paper_controlled_fill_YYYY-MM-DD.json/md`，含 masked account、redacted broker order id、order status events、fill/cancel/commission、issues、safety checks；fill_seen=true 且存在可靠 `MarketSnapshot` 时才用相同 `OrderIntent` 运行本地 `PaperBroker` 对比，缺 snapshot 标记 `INSUFFICIENT_MARKET_DATA`；不记录完整 account id、broker order id、password/token/API key；不触发 daily-run/replay/dashboard/paper signal quality/shadow impact；目标 pytest、trading_engine、全量 pytest、ruff、black、commit、push 后 CI 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：TRADING-012 当前 `calibration_status=LIFECYCLE_ALIGNED_FILL_UNTESTED`，说明 open/cancel lifecycle 已对齐但 fill model 尚未被验证；owner 要求新增受控 IBKR Paper 小额成交样本采集能力，并保持 paper-only、manual-only、`production_effect=none` 和不修改 `PaperBroker` 行为。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 controlled fill 配置、核心 report builder、人工显式 CLI、JSON/Markdown 输出、PaperBroker 同 OrderIntent 诊断对比、runbook、系统流图/产物目录和 mock IBKR client 测试；验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff、black 和 diff check。2026-05-17: 继续保持 VALIDATING，原因：本机 controlled fill 运行在写报告前触发 broker order id 脱敏 fail-closed 检查；已修复短数字 IBKR order id 与 `quantity=1`、日期或 redaction token 长度等非敏感输出冲突的问题，新增短 `order_id=1` 回归测试；重跑本机命令正常生成报告，结果 `test_status=LIMITED`、`fill_seen=false`、`cancel_requested=true`、`final_order_status=Cancelled`，broker order id 已 redacted。|
|TRADING-013A|交易执行/IBKR Paper Controlled Fill Local Validation Report|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/ibkr_paper_controlled_fill_local_validation_2026-05-17.md`；本机 controlled fill no-fill review、sanitized fixture、runbook、calibration 语义、系统流图/产物目录和测试已完成；下一步 commit、push 并确认 GitHub Actions CI；该样本只证明 IBKR Paper submit/open/cancel 链路，不证明 fill model；本任务不新增交易能力，不修改 `PaperBroker` fill 行为|新增本机验证 review 和 sanitized fixture，记录 `test_status=LIMITED`、`connection_status=CONNECTED`、NVDA BUY LIMIT DAY 1 @ 224.50、order status path `PendingSubmit -> PreSubmitted -> PendingCancel -> Cancelled`、`fill_seen=false`、`fill_quantity=0`、`commission_report_seen=false`、`cancel_requested=true`、`final_order_status=Cancelled`、`issue=fill_not_seen_timeout`、likely non-regular trading session / remained `PreSubmitted`；runbook 补充非交易时段 no-fill 判断和重新运行建议；calibration 读取 controlled fill no-fill artifact 时归类 `NO_FILL_LIFECYCLE_VALIDATED`，但继续输出 `fill_tested=false` 和 `LIFECYCLE_ALIGNED_FILL_UNTESTED`；review/fixture 不含完整 `DUP` account、真实 broker order id、perm id、execution id、password、token 或 API key；目标 pytest、trading_engine、全量 pytest、ruff、black、commit、push 后 CI 通过|2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-013 已完成且 CI 通过后，固化本机真实 controlled fill no-fill evidence，并防止该样本被误解释为 `PaperBroker` fill model 已验证。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 local validation review、sanitized no-fill fixture、runbook no-fill 判断、calibration `NO_FILL_LIFECYCLE_VALIDATED` 分类、系统流图/产物目录和安全语义测试；本地验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff、black 和 diff check。|
|TRADING-014|交易执行/Market Session Guard for Controlled Fill|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/ibkr_paper_controlled_fill_market_session_guard_2026-05-18.md`；market session guard、controlled fill submit 前检查、CLI override、JSON/Markdown 报告字段、runbook、系统流图、artifact catalog 和测试已完成；下一步随下一次真实 `REGULAR` session controlled fill 样本观察 guard 与 fill 报告语义；本阶段不接 live、不修改 `PaperBroker` fill 行为、不接 daily-run 自动执行|新增 `market_session_guard.py`，根据 symbol、as_of、contract details `tradingHours` / `liquidHours` 和 exchange timezone 输出 `REGULAR` / `OUTSIDE_RTH` / `CLOSED` / `UNKNOWN`、`can_submit_controlled_fill`、reason 和 source；controlled fill 默认仅 `REGULAR` 可提交，非 regular session 输出 `BLOCK` 或 `LIMITED`、issue code `market_session_closed` / `outside_regular_trading_hours` / `market_session_unknown`，并标记 `controlled_fill_submission=blocked_by_session_guard`；CLI 增加 `--allow-outside-rth-diagnostic`，显式覆盖时报告必须标记 `outside_rth_override=true` 且 fill interpretation limited；JSON/Markdown/runbook/system flow/artifact catalog 同步 session guard 字段；测试覆盖 closed、regular、outside-RTH、unknown、override、blocked 不调用 submit_order、`production_effect=none`、不读取 API key、不触发 daily-run/dashboard；目标 pytest、trading_engine、全量 pytest、ruff、black、commit、push 后 CI 通过|2026-05-18: 新增并进入 IN_PROGRESS，原因：`TRADING-013A` 本机 no-fill 样本 likely 来自 non-regular trading session，需要在提交 controlled fill 前 fail closed，避免把 `PreSubmitted` no-fill 误归因于 `PaperBroker` fill model。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 IBKR contract details session guard、controlled fill submit 前阻断、outside-RTH 显式诊断 override、报告字段、runbook/系统流图/产物目录和 mock tests；本地验证通过目标 pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。|
|TRADING-001|交易执行/paper trading engine MVP|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paper_trading_engine_mvp_2026-05-17.md`；独立 `trading_engine` 子系统、paper-only 配置、风控链路、审计日志、交易日报、demo、基础测试、安全边界测试和 audit replay helper 已完成；下一步观察后续是否接入真实趋势输出的 `OrderIntent` adapter，真实券商仍不在本阶段范围|趋势系统不得直接调用 broker；交易执行只能通过 `OrderIntent -> PreTradeRiskChecker -> ExecutionService -> PaperBroker`；真实交易默认关闭且真实券商 adapter 只能是 stub；订单、风控、成交、组合快照和日报均可审计；demo 可生成模拟订单、模拟成交和交易日报；安全边界测试必须覆盖 paper-only、`real_trading_enabled=false` 默认、IBKR/Alpaca stub 禁止下单、外部趋势/日报/dashboard 模块不得直接 import broker adapter、trading daily report `production_effect=none`、测试不得读取真实券商环境变量；audit 完整性测试必须覆盖 approved / rejected / filled / open，并能用 intent_id 串起 order_intent、risk_result、order、execution_report、fill 和 portfolio_snapshot；目标测试、全量 pytest、ruff、目标 mypy、diff check 和 demo smoke 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 提供第二套实时交易引擎开发文档，要求在现有 repo 中实现初版系统设计，并保持与既有趋势预测系统合理隔离。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：paper trading MVP 已实现，默认 demo 跑通并生成 JSONL 审计日志和交易日报；验证通过目标测试、全量 pytest、ruff、目标 mypy、diff check 和真实 demo smoke；真实券商仍为 stub。2026-05-17: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求为 trading_engine 补充安全边界测试，覆盖 paper-only、real_trading_enabled=false 默认、IBKR/Alpaca stub 禁止下单、外部模块不得直接 import broker adapter、trading daily report production_effect=none、测试不得读取真实券商环境变量。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `tests/trading_engine/test_safety_boundaries.py`，ExecutionService 运行时拒绝非 PaperBroker，IBKR/Alpaca stub submit_order 抛出明确错误，交易日报固定输出 `production_effect=none`；验证通过 trading_engine 测试、全量 pytest、ruff、目标 mypy、diff check 和 demo smoke。2026-05-17: 继续保持 VALIDATING，原因：已新增 `replay_intent_audit_trace` 和 `tests/trading_engine/test_audit_integrity.py`，audit JSONL 顶层写入 intent lineage 字段并新增 execution_report_log；验证通过 audit integrity 测试、trading_engine 测试、全量 pytest、ruff、目标 mypy、diff check 和 demo smoke。|
|TRADING-002|交易执行/paper trading closed loop|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paper_trading_closed_loop_2026-05-17.md`；本轮已把 candidate paper runner 接入每日主流程产物链：runner 默认可补齐 `daily_decision_summary`、正式 `order_intent_candidates`、`paper_trading_summary` 和 trading daily report，并把 dashboard Paper Trading Summary 扩展到 candidate 维度；真实 broker 仍不在本阶段范围|新增 `ReconciliationResult` schema；可从 fill logs 或 ExecutionReport 重建 expected portfolio 并与 PaperPortfolio / PortfolioState 对比，cash、position quantity、avg cost 不一致必须输出 BLOCK；TradingDailyReport 显示 reconciliation status；新增 `OrderIntentCandidate` schema 和 candidate -> OrderIntent adapter，candidate 默认 `production_effect=none`，只有 `blocked=false` 且 `mode=paper` 才允许转换；新增 `scripts/run_paper_trading_from_candidates.py`，保留 demo；daily task dashboard 展示 order intent candidates 和 paper trading execution summary，包括 candidate_count、blocked_candidates、generated_intents、approved/rejected、submitted、filled/open/cancelled、realized/unrealized PnL、reconciliation_status、audit_log_path、`production_effect=none`；测试覆盖正常成交、拒单、取消、部分未成交、上游缺失 limited/missing 和 dashboard 接线；daily run 中 paper trading 失败只能标记 LIMITED/ERROR，不得阻断主报告或改变生产仓位建议；验证通过 trading_engine 测试、全量 pytest、ruff、diff check 和 2026-05-17 default runner smoke|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求把趋势判断到模拟执行复盘闭环接入主 dashboard，同时保持 paper-only 和 audit/reconciliation 边界。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：闭环基础实现完成；验证通过 reconciliation/adapter/runner/dashboard 目标测试、全量 pytest、ruff、目标 mypy、diff check、demo smoke 和 candidate runner smoke。2026-05-17: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求本轮不扩展真实券商能力，而是把现有 paper trading engine 接入每日主流程，补齐默认上游产物生成、runner 默认路径、dashboard candidate 维度和失败降级边界。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 default runner 上游 limited summary/candidates 生成、paper summary candidate 计数、dashboard 展示、`data_gate_blocked` blocker 和系统流图/产物文档同步；验证通过 `python -m pytest tests/trading_engine`、`python -m pytest`、`python -m ruff check src tests scripts`、`git diff --check` 和 `python scripts/run_paper_trading_from_candidates.py --date 2026-05-17`。|
|TRADING-003|交易执行/paper trading flow validation & replay|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paper_trading_flow_validation_replay_2026-05-17.md`；多日 paper replay、replay 汇总产物、dashboard Paper Trading Trend、测试和文档已完成；下一步观察后续真实 daily flow 样本|新增 `scripts/run_paper_trading_replay.py --start YYYY-MM-DD --end YYYY-MM-DD`，逐日复用 candidate runner；候选文件缺失时生成 limited upstream artifacts；输出 `paper_trading_replay_START_END.json/md`，汇总 candidate_count、blocked_candidates、generated_intents、approved/rejected、submitted、filled/open/cancelled、realized/unrealized PnL、reconciliation_status 分布，并按 symbol / strategy_id / reason_code / blocked_by 聚合；daily task dashboard 可读取最近 7/14/30 日 paper summary trend，缺数据显示 `LIMITED`；replay 和 dashboard trend 均 `production_effect=none`，不得读取真实 broker API key、调用真实 broker 或改变 production 仓位建议；目标测试、全量 pytest 和 runner smoke 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求继续推进 paper trading daily flow validation 和多日 replay；当前目标是验证 paper-only 连续运行、可复盘和 dashboard 历史趋势，不扩展真实券商能力。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增多日 replay 脚本、replay JSON/Markdown 汇总、按 symbol/strategy_id/reason_code/blocked_by 聚合、dashboard Paper Trading Trend 和目标测试；验证通过 `python -m pytest tests/trading_engine`、`python -m pytest`、`python -m ruff check scripts src tests`、`git diff --check`、`python scripts/run_paper_trading_from_candidates.py --date 2026-05-17` 和 `python scripts/run_paper_trading_replay.py --start 2026-05-01 --end 2026-05-17`。|
|TRADING-004|交易执行/paper replay quality & continuous portfolio|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paper_replay_quality_continuous_portfolio_2026-05-17.md`；replay mode、market snapshot provider、quality flags、continuous portfolio 结构预留、Black 配置、测试和文档已完成；后续真实 continuous portfolio replay 已拆到 `TRADING-007` 实现|`paper_trading_replay` JSON 必须包含 `replay_mode=daily_independent`、`portfolio_carry_forward=false` 和 `quality_flags`；CLI 支持 `--mode daily-independent / continuous-portfolio`，TRADING-004 阶段只要求 continuous portfolio 有明确结构预留且不生成虚假的连续组合收益；新增 `MarketSnapshotProvider`，优先历史 OHLC，其次 candidate metadata，最后 synthetic limit price，并在 daily / replay summary 记录 `market_snapshot_source`；Markdown 展示逐日独立语义、synthetic snapshot 使用次数和 quality flags；synthetic snapshot 使用占主导时 status 至少为 `LIMITED`；目标测试、全量 pytest、Black/Ruff 和 runner smoke 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求提高 paper replay 的可解释性、真实性和可维护性，同时只为 continuous portfolio 做结构预留，不接真实券商、不扩展订单类型。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `replay_mode`、`portfolio_carry_forward`、`quality_flags`、`--mode daily-independent / continuous-portfolio`、`MarketSnapshotProvider`、historical OHLC / metadata / synthetic snapshot source 记录、continuous portfolio 结构预留边界、Black 配置和脚本可维护性测试；验证通过 `python -m pytest tests/trading_engine`、`python -m pytest`、`python -m black --check scripts/run_paper_trading_from_candidates.py scripts/run_paper_trading_replay.py`、`python -m ruff check scripts src tests`、`git diff --check`、`python scripts/run_paper_trading_from_candidates.py --date 2026-05-17` 和 `python scripts/run_paper_trading_replay.py --start 2026-05-01 --end 2026-05-17 --mode daily-independent`。|
|TRADING-005|交易执行/dashboard paper replay visibility|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/dashboard_paper_replay_visibility_2026-05-17.md`；7/14/30 日 Paper Trading Trend、snapshot 质量和 candidate 解释维度已接回 daily task dashboard；下一步观察真实 daily flow 样本|daily task dashboard 必须只读读取已有 `paper_trading_summary_YYYY-MM-DD.json` 和 `order_intent_candidates_YYYY-MM-DD.json`，同时展示最近 7/14/30 日 Paper Trading Trend；趋势字段包括 `replay_mode=daily_independent`、`portfolio_carry_forward=false`、candidate / intent / fill / PnL / reconciliation 分布、synthetic snapshot 使用比例、top `blocked_by` 和 top `reason_code`；缺历史 summary 或 candidate 文件时显示 `LIMITED` 且不补造结论；dashboard 不运行 paper runner/replay、不读取 broker API key、不调用真实 broker、不改变 production 仓位建议、不影响参数晋级|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-004 后把 paper replay 历史可见性接回 daily dashboard，同时保持 paper-only、只读和 `production_effect=none` 边界。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 7/14/30 日趋势窗口、`replay_mode=daily_independent`、`portfolio_carry_forward=false`、只读 execution boundary、synthetic snapshot 比例、top `blocked_by` / `reason_code` 和缺失历史 LIMITED 行为；验证通过 `python -m pytest tests/test_daily_task_dashboard.py -q`、`python -m pytest tests/trading_engine -q`、`python -m pytest`、`python -m ruff check src tests scripts`、`python -m black --check src/ai_trading_system/daily_task_dashboard.py tests/test_daily_task_dashboard.py` 和 `git diff --check`。|
|TRADING-006|交易执行/paper signal quality evaluation|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paper_signal_quality_evaluation_2026-05-17.md`；只读 paper signal quality evaluation 产物、policy manifest、evaluation gate、聚合分析、dashboard 轻量入口、测试和文档已完成；本轮已用项目 `.venv` Python 3.11 / Black 26.5.0 完成目标 dashboard 文件格式化并重验；下一步观察 daily dashboard 持续展示和 repo-wide formatter baseline 后续治理|新增 `outputs/reports/paper_signal_quality_YYYY-MM-DD.json/md`，只读读取最近 7/14/30 日 `paper_trading_summary_YYYY-MM-DD.json`、`order_intent_candidates_YYYY-MM-DD.json` 和可选 `paper_trading_replay_START_END.json`；`production_effect` 必须为 `none`；evaluation gate 至少覆盖 `INSUFFICIENT_SAMPLE`、`INSUFFICIENT_FILLED_SAMPLE`、`LOW_DATA_QUALITY`、`LIMITED_MARKET_DATA`、`UNRELIABLE_EXECUTION_STATE`；按 strategy_id / symbol / reason_code / blocked_by / confidence bucket / market_snapshot_source 聚合 sample_count、candidate_count、generated_intents、filled_count、avg_paper_pnl、synthetic_snapshot_ratio、quality_status；daily task dashboard 只展示 evaluation_status、主要 blocked_by、synthetic_snapshot_ratio、sample_count 和 report link，明确 observe-only / production_effect=none；不得读取 broker API key、调用真实 broker、触发 paper runner/replay、改变 production 仓位建议、影响参数晋级或把 paper PnL 当成上线依据；目标 pytest、ruff 和触达文件 Black check 通过；全仓 Black 在当前 `python -m black` 环境下仍被既有无关 repo-wide formatting baseline 阻断|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求继续推进 TRADING-006，在既有 paper trading summary、order intent candidates、多日 replay 和 dashboard trend 之上增加只读 signal quality evaluation 层。2026-05-17: 实现完成但保持 IN_PROGRESS，原因：已新增 `config/paper_signal_quality_policy.yaml`、`scripts/run_paper_signal_quality.py`、`paper_signal_quality_YYYY-MM-DD.json/md` 生成器、evaluation gate、六类聚合、dashboard Paper Signal Quality 轻量卡片和文档/测试；验证通过 `python -m pytest tests/trading_engine`、`python -m pytest tests/test_daily_task_dashboard.py`、`python -m pytest`、`python -m ruff check src tests scripts` 和本次改动文件 Black check；`python -m black --check src tests scripts` 在当前 Black 26.5.0 / Python 3.14.4 环境下仍报告 125 个既有文件 would reformat，未为通过该命令而重排无关文件。2026-06-07: 从 IN_PROGRESS 改为 VALIDATING，原因：已用项目 `.venv` Python 3.11 / Black 26.5.0 对 TRADING-006 触达的 `daily_task_dashboard.py` 做机械格式化，目标 paper signal quality/dashboard tests、ruff 和触达文件 Black check 通过；全仓 Black 仍受既有无关 baseline 阻断，不在本任务中重排无关模块。|
|TRADING-006A|交易执行/paper signal quality hardening|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/paper_signal_quality_hardening_2026-05-17.md`；本轮 hardening 已实现并进入验证；下一步需要单独处理当前环境下 repo-wide Black baseline，或固定项目认可的 formatter 版本重验|Paper signal quality 目标文件必须 Black 格式化且不是超长单行；`evaluation_status` / `quality_status` 只能使用 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`PROMISING_BUT_LIMITED`、`LOW_DATA_QUALITY`、`UNRELIABLE`，不得出现 `READY_FOR_LIVE`、`SHOULD_TRADE`、`PROMOTE_TO_PRODUCTION`、`APPROVED_FOR_TRADING`；JSON 必须包含 `policy_id`、`policy_version`、`production_effect=none` 和 thresholds snapshot；Markdown 必须显示 policy version 和关键 thresholds；evaluation gate 必须包含 human-readable explanation，并为每个 blocked_by 给出解释；若 `replay_mode=daily_independent` 或 `portfolio_carry_forward=false`，报告必须包含 `DAILY_INDEPENDENT_ONLY` warning，并说明不能解释连续组合收益、现金占用、持仓结转、open order 跨日处理或最大回撤；dashboard 只展示摘要，不展示完整长表；不得触发 paper runner/replay/broker；本轮目标 pytest、全量 pytest、ruff 和目标文件 Black check 通过；全仓 `python -m black --check src tests scripts` 仍被 125 个既有无关文件 would reformat 阻断|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求加固刚提交的 TRADING-006，只增强只读 evaluation 解释层，不接真实券商、不扩展订单类型、不改变业务逻辑之外的交易边界。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成状态语义收紧、policy snapshot、gate explanation、daily-independent warning、dashboard 只读摘要和安全边界测试；验证通过 `python -m pytest tests/trading_engine`、`python -m pytest tests/test_daily_task_dashboard.py`、`python -m pytest`、`python -m ruff check src tests scripts` 和本次目标/改动文件 Black check；全仓 Black check 在当前环境下仍报告 125 个既有无关文件 would reformat。|
|TRADING-007|交易执行/continuous portfolio replay|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/continuous_portfolio_replay_2026-05-17.md`；`--mode continuous-portfolio`、连续复用同一个 `PaperPortfolio`、每日 reconciliation、DAY order 到期、连续收益指标、报告语义、测试和系统流图已完成；下一步观察真实 daily flow 样本|`scripts/run_paper_trading_replay.py --mode continuous-portfolio` 必须输出 `replay_mode=continuous_portfolio`、`portfolio_carry_forward=true`、`production_effect=none`；start date 初始化一个 `PaperPortfolio` 并跨日结转持仓和 cash；每日读取 `order_intent_candidates_YYYY-MM-DD.json`、转换可执行 candidate、经过 `PreTradeRiskChecker`、用同一个 portfolio 执行并生成带 `date`、`cash`、`positions`、`equity`、`exposure` 的 snapshot；DAY 未成交订单当日过期，不支持 GTC，GTC candidate 直接 rejected / limited；输出 equity_curve、daily_equity、daily_cash、daily_exposure、daily_realized_pnl、daily_unrealized_pnl、cumulative_realized_pnl、cumulative_unrealized_pnl、max_drawdown、exposure_peak、position_concentration_peak；任一天 reconciliation 非 PASS 时整体 status 至少 LIMITED；Markdown 明确 paper-only、不是实盘、不是真实 broker 成交、不读取 broker API key，并写清与 daily-independent 的区别；目标测试、全量 pytest 和两种 replay smoke 通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 明确要求推进 TRADING-007，在现有 paper trading engine 上实现真正的 continuous-portfolio replay，同时保持 paper-only、`production_effect=none` 和真实 broker 隔离边界。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 continuous replay orchestration、同一 `PaperPortfolio` 跨日结转、每日 `PortfolioState` snapshot、DAY open order 过期、GTC candidate rejected / limited、每日 reconciliation、连续收益指标和 Markdown 边界说明；验证通过 `python -m pytest tests/trading_engine`、`python -m pytest`、`python -m ruff check scripts src tests`、`python scripts/run_paper_trading_replay.py --start 2026-05-01 --end 2026-05-17 --mode daily-independent` 和 `python scripts/run_paper_trading_replay.py --start 2026-05-01 --end 2026-05-17 --mode continuous-portfolio`。|
|ARCH-003|架构/结构化降复杂度后续切片|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/structured_refactor_followup_2026-05-17.md`；五个基础切片已完成：daily-run workflow 适配、daily task dashboard view model 拆分、`aits docs` 命令组拆分、shadow lineage helper 抽取、ProductionEffect 低风险替换；下一步观察后续重构是否继续沿用这些边界|五个切片均保持行为兼容；不改变 production scoring、position gate、approved overlay、正式 prediction ledger、backtest 语义或 shadow promotion 结论；每个切片有目标测试，最终通过 ruff、mypy 目标范围、diff check 和全量 pytest；任务登记、支持文档、系统流图/架构文档按实际边界更新|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求继续依次开发、测试、修复并验收五个低风险重构任务。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：五个切片均已实现并通过目标测试、全量 ruff、全量 pytest 和 diff check；`prediction_ledger.py` 目标 mypy 也已修复并通过。|
|ARCH-002|架构/workflow 与 artifact 契约|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/architecture_contract_refactor_2026-05-17.md`；`core` 层的 `ArtifactRef`、`ProductionEffect` 和 workflow step/result 契约已抽出，daily-run manifest artifact 记录已改为复用统一契约，`ops_daily` 已新增到通用 workflow contract 的适配层；下一步观察 dashboard / shadow / replay 是否继续接入该 contract|新增核心契约类型后，daily-run run manifest 的路径、checksum、artifact type、size/file count 字段保持行为兼容；`production_effect` 可用统一枚举表达核心标签，但既有报告文本不被批量改写；文档明确模块边界、workflow contract 和阶段顺序；目标测试、ruff、diff check 通过；不改变生产评分、仓位 gate、approved overlay、正式 ledger 或 shadow promotion 结论|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 提供结构化降复杂度重构讨论，当前最安全切片是先抽出 daily-run/artifact/production-effect 共同契约，作为后续 CLI 拆分、dashboard 分层和 shadow 子系统拆分的底座。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `core` 契约模块，`run_artifacts` 复用 `ArtifactRef` 生成 manifest artifact 记录，架构文档、产物目录和系统流图已同步；验证通过目标测试、ruff、mypy、docs freshness、diff check 和全量 pytest。2026-05-17: 继续保持 VALIDATING，原因：`ops_daily` 已新增 `DailyOpsStep` / `DailyOpsStepResult` 到 `WorkflowStep` / `WorkflowStepResult` 的适配函数并通过目标测试。|
|OPS-015|运行展示/每日任务 Dashboard|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/daily_task_dashboard_2026-05-16.md`；daily-run 总控展示页、`daily_decision_summary` 和只读 `order_intent_candidates` 基础版已实现；下一步观察后续 OrderIntentBuilder 是否以单独任务接入，当前所有候选仍 blocked 且不接入 trading engine|新增 `outputs/reports/daily_task_dashboard_YYYY-MM-DD.html/json`，自动由 `aits ops daily-run` 生成并进入 run bundle；页面首屏先汇总重要任务的关键业务结论，包括投资动作/仓位/置信度/Data Gate、数据可信度、参数治理、反馈复盘、shadow parameter search、pipeline health 和 secret hygiene；新增 `outputs/reports/daily_decision_summary_YYYY-MM-DD.json`，字段覆盖 as_of、run_id、`production_effect=none`、data_gate、investment_conclusion、parameter_governance、feedback_review、system_health、source_artifacts / href / checksum；新增 `outputs/reports/order_intent_candidates_YYYY-MM-DD.json`，从 daily decision summary、decision snapshot 和 position/gate 结论生成候选方向；所有 candidate 必须 `blocked=true`，`blocked_by` 至少包含 `trading_engine_not_enabled` 和 `manual_approval_required`；不得读取 broker API、paper broker、真实下单接口或账户状态；这些 JSON 只能作为后续 order intent candidate 上游，不得生成订单或改变 scoring/gate/ledger；缺子报告时必须标记 limited/missing，不得编造结论；同步系统流图、产物目录和测试|2026-05-16: 新增并进入 IN_PROGRESS，原因：owner 确认需要独立每日任务展示页，汇总各子任务最重要结论和重要风险，详细内容后续在各子任务网页继续整理。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：`aits reports daily-tasks`、daily-run 自动生成、legacy mirror、测试和文档已完成；目标 pytest、ruff、窄范围 mypy 和 2026-05-15 smoke 生成通过。2026-05-16: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 复核后要求页面弱化任务执行状态，把重要任务的关键结论信息组织到最前面。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增结构化 `key_conclusions`，HTML 首屏改为“关键结论总览”，任务执行状态后置，2026-05-15 页面已重生成。2026-05-16: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 指出反馈复盘应展示 shadow parameter search 的诊断领先参数和相对主线收益差距。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：反馈复盘已接入最近可用 shadow parameter search bundle，2026-05-15 页面显示诊断领先 trial、shadow vs production return、excess、主因 cap、promotion 状态和阻断条件。2026-05-16: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 复核指出 shadow parameter 信息仍无法看出具体哪些参数改变，需要加入 production vs candidate 对比表。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：反馈复盘卡片已新增 Shadow 参数对比表，逐项展示 production/current 与 candidate 的 weight 和 gate cap；2026-05-15 页面已重生成。2026-05-16: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 复核指出当前参数对比表排版横向挤压、换行过多，无法快速看出具体参数改动。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：参数对比表已拆成 Gate cap override 与权重参数两个分区，反馈复盘卡片全宽展示，2026-05-15 页面已重生成并通过 targeted checks。2026-05-16: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 提出其他子任务也需要统一考虑页面设计，主 dashboard 应附带子任务页面/报告链接。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：子任务详情入口已改为卡片式可点击下钻区，既有 Markdown/HTML/JSON 子报告使用相对链接，2026-05-15 页面已重生成。2026-05-16: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 指出 Gate cap override 的 production/current 不应只写无静态 override，还应展示 production 默认/实际 cap 数值。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：Gate cap override 表已读取 search window 内 production decision snapshots 的实际 `position_gates`，2026-05-15 页面显示 actual cap 数值/区间。2026-05-16: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 复核发现 production/current 长文本与 shadow override 列重叠。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：production/current 单元格已拆成实际 cap 数值和 override/快照说明两行，Gate cap override 表列宽已调整，2026-05-15 页面已重生成。2026-05-16: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 指出 shadow 参数对比缺少结果对比，且 return 计算口径需要在页面披露。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：反馈复盘 Shadow 区块已新增结果对比表，展示 Total return、Max drawdown、Turnover、Beat rate 和样本覆盖，并披露 return 计算口径；2026-05-15 页面已重生成。2026-05-17: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求继续推进“每日决策总线”，新增 machine-readable daily decision summary JSON，作为后续 OrderIntentBuilder 的只读上游输入且 `production_effect=none`。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 `daily_decision_summary_YYYY-MM-DD.json`、daily-run/CLI 生成、legacy mirror、文档和测试；验证通过目标 pytest、全量 pytest、ruff、窄范围 mypy 和 diff check。2026-05-17: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求新增 `order_intent_candidates_YYYY-MM-DD.json`，但不实现 trading engine；候选只能表达未来可能动作，默认 blocked。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 `order_intent_candidates_YYYY-MM-DD.json`、daily-tasks/daily-run 自动生成、canonical/legacy mirror、blocked-only schema、missing 结论防护和文档更新；验证通过目标 pytest、全量 pytest、ruff、窄范围 mypy。|
|DOCS-LEARN-001|报告学习链路/产物目录|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/report_learning_path_and_lineage_2026-05-16.md`；阶段 1-5 基础版已进入 VALIDATING；`docs/system_flow.md` 暂不拆分，原因是当前作为 source-of-truth 仍比多文件双源维护更安全，后续若拆分需先建立生成或校验机制|新增 `docs/learning_path.md`、`docs/artifact_catalog.md` 和 `docs/calculation_logic.md`；日报第一屏附近说明生成命令、关键输入、关键输出、trace 和 `production_effect`；日报用学习型漏斗解释 component score -> effective weights -> overall score -> model position -> confidence -> macro risk budget -> position gates -> final position；明确 binding gate 和复核五问；字段字典覆盖核心 CSV/JSON 字段；参数搜索报告有 Trial Card；计算逻辑文档解释价格、收益率、移动平均、相对强弱、信号归一化、模块分数、权重、仓位映射、confidence、macro budget、position gates 和 shadow 计算边界；`aits explain` 可只读反查字段、gate 和 artifact 的来源、上下游、production_effect 和常见误解；evidence dashboard 已有快速读者、投资复核者、系统审计者三层读者模式；同步系统流图、README 和目标测试；不改变 production scoring、position gate、approved overlay、正式 prediction ledger 或回测仓位|2026-05-16: 新增并进入 IN_PROGRESS，原因：系统已有完整审计链路但学习路径不清晰，使用者需要从每日报告和 artifact 反查来源、处理链路、production 影响和复核入口。2026-05-16: 阶段 1-3 基础实现完成并进入 VALIDATING，原因：学习路径、产物目录、字段字典、日报 lineage/funnel/gate ladder/复核五问和 parameter search Trial Card 已实现；目标测试、ruff 和 diff check 通过；剩余阶段为 `aits explain`、dashboard 三层读者模式和 system_flow 拆分评估。2026-05-16: 阶段 4 基础实现进入 VALIDATING，原因：新增面向无金融背景读者的 `docs/calculation_logic.md`，说明输入数据到输出数据的计算逻辑、原因和设计思路；同步 README、学习路径、产物目录和系统流图；验证通过目标测试 40 passed、ruff 和 diff check；`docs validate-freshness` 仍因既有 11 个无关 requirements metadata 问题失败。2026-06-07: 阶段 5 基础实现进入 VALIDATING，原因：新增 `aits explain` 只读解释入口和测试，补齐 `risk_event_prereview_queue.json` 产物目录登记，确认 dashboard 三层读者模式已存在；本阶段未拆分 system_flow，以避免缺少生成/校验机制时形成双源维护。|
|CALIBRATION-015|市场反馈优化/shadow cap-level attribution|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/shadow_parameter_attribution_promotion_contract_2026-05-16.md`；cap-level attribution 与最终仓位变化解释已实现并通过当前样本 smoke，下一步继续滚动观察 forward shadow|`search-shadow-parameters` 报告和 manifest 必须在 weight/gate factorial attribution 之外，拆出单个 gate cap 的边际 excess、primary gate cap、每个 signal date 的 production/candidate 最终仓位、binding gate 和 return impact；输出仍为 `production_effect=none`，不得修改生产 gate、权重、approved overlay、正式 ledger 或日报结论|2026-05-16: 新增并进入 IN_PROGRESS，原因：最新 shadow 搜索显示 primary driver 为 gate，但只知道 gate 主导不足以支持治理判断。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 cap-only ablation、仓位变化解释表、manifest 输出和目标测试覆盖；当前样本 `current_20260504_20260514_cap_promotion_v3` 显示 diagnostic-leading 为 `source_current__grid_gate_0217`，primary gate cap 为 `valuation`，valuation cap-only excess delta 约 2.42%、thesis 约 0.76%；全量 pytest 551 passed，ruff 通过。|
|CALIBRATION-016|市场反馈优化/shadow 参数 promotion contract|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/shadow_parameter_attribution_promotion_contract_2026-05-16.md`；独立 contract 配置和 CLI 已实现并通过当前 search bundle smoke，下一步等待 forward shadow outcome 样本|新增 `config/weights/shadow_parameter_promotion_contract.yaml` 和 `aits feedback evaluate-shadow-parameter-promotion`；无 eligible best、样本低于 contract floor、非正 excess、回撤/换手恶化、gate primary driver 未完成 cap review、缺 forward shadow outcome 或 approved_hard 未接入时，不得输出 production-ready 结论；报告只能给 `NOT_PROMOTABLE`、`READY_FOR_FORWARD_SHADOW` 或 `READY_FOR_OWNER_REVIEW`，且 `production_effect=none`|2026-05-16: 新增并进入 IN_PROGRESS，原因：需要把 search ranking 与生产晋级 contract 分离，避免 in-sample 最优被误用为上线证据。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 contract loader、promotion report/JSON、CLI 和测试覆盖；当前默认 contract 仍要求 forward shadow 与 owner/rollback。2026-05-16: 当前样本 promotion smoke 输出 `NOT_PROMOTABLE`，原因：无 eligible best、available=8 低于 contract floor 30，且缺 forward shadow outcome。|
|CALIBRATION-017|市场反馈优化/shadow objective regularization 与 lineage|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/shadow_parameter_attribution_promotion_contract_2026-05-16.md`；objective 正则化和 search lineage 已实现并通过当前样本 smoke，下一步观察 top ties 是否继续收敛到 gate-only 候选|默认 objective 增加 gate relaxation、weight distance、changed dimension penalty，以及 max L1 distance / max single factor step 生产邻近性限制；search manifest 必须记录 source weight checksum、price checksum、decision snapshot aggregate checksum、resolver version、git commit sha 和 dirty worktree 标记；这些字段不改变生产评分或仓位 gate|2026-05-16: 新增并进入 IN_PROGRESS，原因：top trials 大量并列时需要优先选择更接近 production、更少放松 gate、更可解释的候选，并强化复现审计。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 objective 字段、trial regularization metrics、eligibility 限制、lineage 字段和目标测试覆盖；当前样本 manifest 已记录 source weight / price / decision snapshot checksum、resolver version、git commit sha 和 dirty worktree 状态。|
|CALIBRATION-018|市场反馈优化/shadow 参数持续迭代 MVP|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/shadow_parameter_iteration_mvp_2026-05-17.md`；P0/P1 已实现，P2 自动 production mutation 项已落实为 guardrails 而不是启用生产修改；当前进入滚动验证|基于已有 shadow parameter search output 生成 `data/processed/shadow_iteration_registry.csv`、`outputs/reports/shadow_iteration_YYYY-MM-DD.md/json` 和 `outputs/shadow_iterations/<run_id>/*`；候选分类区分 `weight_only`、`gate_only`、`weight_gate_bundle`；只有 `weight_only` 可标记为潜在权重迭代候选，`gate_only` 只能进入 gate policy review，bundle 只能 diagnostic；报告披露 production 参数未改变、best candidate、primary driver、promotion contract check、blocked reasons、next action、attribution、position change 降级状态和 data lineage；dashboard 只读展示 shadow iteration status；`register-forward-shadow` 只更新 registry 状态；retirement 规则不写 production；P2 禁止自动改 production weight/profile、approved overlay、approved_hard、gate-only/bundle 晋级和 shrinkage proposal；不得写 production weight/scoring/portfolio、approved overlay 或正式 prediction ledger；目标测试通过|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求尽快启动独立 shadow-only 参数持续迭代流程，并为后续权重迭代闭环预留扩展点。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `aits feedback run-shadow-iteration`、registry/report/JSON/run 目录输出、candidate 分类和 production safety 测试；当前 search bundle smoke 输出 `PASS_WITH_LIMITATIONS`，结论为 gate policy review，不产生权重晋级候选。2026-05-17: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求继续完成 P1/P2 并通过回测验证后提交；P2 中生产修改项按项目安全边界实现为 guardrails。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：P1 dashboard 只读卡片、register-forward-shadow、retirement 规则、字段字典和 P2 guardrails 已完成；目标测试、全量 pytest 569 passed、ruff、mypy、shadow iteration smoke、dashboard smoke 和 ai_after_chatgpt 回测验证通过，production safety 边界保持不变。|
|CALIBRATION-019|市场反馈优化/forward shadow candidate lifecycle|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/forward_shadow_candidate_lifecycle_2026-05-17.md`；已实现 `run-shadow-iteration` 幂等、`register-forward-shadow` 状态登记强化、`evaluate-forward-shadow` 每日评估和 `REVIEW_PENDING` 生命周期；保持 production 权重、gate、approved overlay 和正式 prediction ledger 隔离，当前进入滚动验证|forward shadow 候选可从 registry 登记为 `FORWARD_SHADOW_ACTIVE`；每日评估输出 `forward_shadow_evaluation_YYYY-MM-DD.md/json`，按候选类型、primary driver、contract/retirement evidence 和 forward 表现给出 `CONTINUE`、`RETIRE`、`REVIEW_GATE_POLICY` 或 `REVIEW_WEIGHT_CANDIDATE`；只有 `weight_only` 且 `primary_driver=weight` 才能进入权重候选复核，`gate_only` 只进入 gate policy review，bundle 和非 weight primary driver 不得进入 weight review；重复运行不得产生重复 registry 行；实现同步更新系统流图并补测试|2026-05-17: 新增并进入 IN_PROGRESS，原因：owner 要求 shadow candidate 跨交易日持续观察，而不是每天只输出新的 search result。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `aits feedback evaluate-forward-shadow`、`REVIEW_PENDING` registry 生命周期、forward evaluation Markdown/JSON 输出、run-shadow registry 幂等保护和 production safety 测试；验证通过 shadow iteration 目标测试、daily task dashboard 相关测试、全量 `ruff check src tests`、全量 `pytest -q` 572 passed 和 `git diff --check`。|
|CALIBRATION-014|市场反馈优化/shadow 参数验证收紧|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/shadow_parameter_validation_hardening_2026-05-16.md`；factorial attribution、验证级 objective、`approved_hard` hard effect fail-closed 和生产权重 metadata 清理已实现；下一步继续滚动观察真实样本，生产晋级仍需 promotion floor、forward shadow、owner approval 和 rollback 条件|`search-shadow-parameters` 报告能拆出 weight-only、gate-only、combined 与 production baseline 的 post-gate 表现；默认 objective 不把当前 8 个 available 小样本写成 eligible best trial；带 hard effect 的 `approved_hard` overlay 在执行层未接入前 fail closed；`weight_profile_current.yaml` metadata 与当前 score-daily/backtest resolver 状态一致；系统流图、目标测试和当前样本 CLI smoke 通过，且不改变 production scoring、position gate、approved overlay、正式 prediction ledger 或日报结论|2026-05-16: 新增并进入 IN_PROGRESS，原因：owner 要求把本轮权重/gate 回测评估结论登记，并继续推进实现和测试整条 validation-only 流程。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现并验证目标闭环；完整当前样本搜索 `current_20260504_20260514_validation_v3` 为 `PASS_WITH_LIMITATIONS`，51,612 trials，因 available=8 低于 objective floor 13 而没有 eligible best trial，diagnostic-leading 为 `grid_weight_0118__grid_gate_0217`，factorial primary driver 为 `gate`；全量 pytest 548 passed，ruff 通过，diff check 无 whitespace error。|
|LLM-005|新闻/LLM 请求成本调参|P1|VALIDATING|项目 owner + 系统验证|`risk_event_daily_official_precheck` 已降档为成本 pilot；下一步由 owner 明天对照 OpenAI 账单观察成本变化，并在下一次日报后复核候选命中质量|日报默认 OpenAI profile 从高成本 baseline 降为 `reasoning_effort=medium`、`max_candidates=10`、`official_policy_limit=30`；其他 LLM profile 不变；provider 权限、cache、审计、`store=false` 和 pending_review 边界不变；配置测试、ruff 和 diff check 通过；明日账单/日报预审质量用于决定是否继续降到 6-8 个候选或调整缓存 TTL|2026-05-13: 新增并进入 IN_PROGRESS，原因：owner 反馈每日 OpenAI 请求约 5 美元，希望先降低日报默认请求成本并观察明天账单。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：日报 profile 已改为 `medium`、10 个候选、官方来源 limit 30；README、系统流图、架构说明、数据源说明和配置测试同步；验证通过目标 pytest 55 passed、`ruff check src tests`、`git diff --check` 和 docs freshness。|
|GOV-004|规则治理/启发式硬编码审计|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/heuristic_hardcoding_governance_2026-05-13.md`；P1-A/P1-B/P1-C 和 feature coverage 第一批迁移已完成；下一步补自动审计工具和已配置阈值 rationale|项目规则明确禁止无依据的投资解释启发式硬编码；影响评分、仓位、置信度、反馈校准、回测晋级、readiness 或报告结论的阈值必须配置化或有命名 policy/rationale；初版审计文档列出候选位置、风险级别、迁移目标和例外边界；样本 floor 已迁移到统一 sample policy；score/position band、日报结论边界、confidence/source-type policy、backtest data credibility coverage 和 backtest validation/promotion policy 已配置化；新增规则不改变 production 结论但约束后续开发|2026-05-13: 新增并进入 VALIDATING，原因：owner 指出 `30` 样本门槛属于类似启发式硬编码风险，要求 review 当前项目中类似逻辑，并加入项目规则。初版已更新 `AGENTS.md` 的 heuristic governance 规则，并产出静态审计清单。2026-05-13: 完成 P1-A 第一批迁移，原因：`decision_outcomes.py`、`prediction_ledger.py`、`feedback_loop_review.py`、`periodic_investment_review.py`、`backtest/promotion_gate.py` 和 CLI 状态颜色已读取 `config/feedback_sample_policy.yaml` 的 diagnostic/promotion floor。2026-05-13: 完成 P1-B/P1-C 和 feature coverage 第一批迁移，原因：`config/scoring_rules.yaml` 承载评分仓位、日报结论、confidence 和 source-type policy，`config/backtest_validation_policy.yaml` 承载 backtest data credibility coverage、robustness 默认实验参数、解释阈值和 promotion gate 要求；目标测试、ruff 和 mypy 已通过。|
|CALIBRATION-006|市场反馈优化/current context 与 effective weights 审计产物|P1|VALIDATING|系统验证|`score-daily` 已补齐正式默认输出路径下的 current context / current effective weights 写入；2026-05-14 既有数据闭环验证已跑通，剩余观察点是下一次 production daily-run 是否自动写出两个 current 审计产物|`score-daily` 默认生产输出时写入 `outputs/current_context.json` 和 `outputs/current_effective_weights.json`；自定义测试/临时输出不得污染项目全局 current 文件；`apply-calibration-overlay` 默认路径可直接运行；`optimize-market-feedback` 和 dashboard 读取到 effective weights，仍不修改 production scoring、position gate 或 approved overlay|2026-05-15: 新增并进入 IN_PROGRESS，原因：owner 要求为了验证流程系统逻辑跑完全流程，并允许数据阻塞时用既有数据放低限制；当前阻塞是 current context 审计产物未落盘，不是市场数据缺失。2026-05-15: 从 IN_PROGRESS 改为 VALIDATING，原因：CLI 已新增 current context/effective weights 写入；目标单测、ruff、diff check 通过；真实链路已用 2026-05-14 既有数据跑通 `backtest robustness -> parameter replay -> candidates -> governance -> feedback/dashboard/health/secret scan`，dashboard PASS。|
|CALIBRATION-007|市场反馈优化/flow validation 放宽门禁|P1|VALIDATING|系统验证|flow validation 模式和 parameter shadow runner 已实现；2026-05-14 既有数据验证跑通 candidates -> governance -> parameter shadow -> prediction outcomes -> shadow maturity -> market feedback，剩余观察点是后续是否需要把 strict 候选进入 shadow 的 owner 节奏独立化；production overlay 仍未接入|`build-parameter-candidates` 支持 `strict` 与 `flow_validation` 模式；flow validation 产物必须标记 `evaluation_mode`、保留原始 `veto_reasons`、声明 `production_effect=none`；参数治理在 flow validation 模式下只能输出 validation-only shadow 准备/观察动作，不能 owner approval 或 production；新增 parameter shadow runner 可把 `READY_FOR_FORWARD_SHADOW` 参数候选追加到 prediction ledger 的 `production_effect=none` 行；用 2026-05-14 既有数据跑通 flow validation candidates -> governance -> parameter shadow -> prediction outcomes -> shadow maturity -> market feedback|2026-05-15: 新增并进入 IN_PROGRESS，原因：当前严格门禁 16 个 candidate 全部 blocked，无法测试后续 shadow/治理接线；owner 明确允许先降低限制验证流程逻辑。2026-05-15: 从 IN_PROGRESS 改为 VALIDATING，原因：CLI 已支持 `--candidate-gate-mode flow_validation`，并新增 `run-parameter-shadow`；真实验证使用既有 2026-05-14 数据生成 16 个 ready / 16 个 flow_validation_override candidate，治理动作为 PREPARE_FORWARD_SHADOW=2、COLLECT_MORE_EVIDENCE=3，单独 validation ledger 写入 16 条 parameter shadow prediction，prediction outcomes 160 行（available=13、pending=127、missing=20），shadow maturity 与 market feedback 均为 PASS_WITH_LIMITATIONS / PILOT_DIAGNOSTIC_REVIEW；目标测试 18 passed、全量 pytest 534 passed、ruff、diff check 和 secret scan 通过；生产 scoring、position gate、approved overlay 均未改变。|
|CALIBRATION-008|市场反馈优化/shadow weight profiles 长期观察|P1|VALIDATING|系统验证|隔离测试权重 manifest、只读主线评分对比 CLI 和独立 observation/prediction 观察链路已实现；2026-05-14 既有数据已跑通 shadow profile -> prediction outcomes -> shadow maturity，后续观察真实交易日滚动样本；替换生产权重条件仍待后续 owner/promotion 规则定义|新增 `config/weights/shadow_weight_profiles.yaml`，所有 profile 必须 `production_effect=none` 且权重信号与生产 profile 一致；新增只读 CLI 从 production decision snapshot 计算各 shadow profile 的整体分数、模型仓位和受既有 gate 后的观察仓位，并写入独立 observation ledger / 报告；可选写入隔离 prediction ledger 以进入 outcome/shadow maturity；不得修改 `weight_profile_current.yaml`、approved overlay、production prediction ledger、score-daily 正式输出或仓位 gate|2026-05-15: 新增并进入 IN_PROGRESS，原因：owner 要求把调参后续流程也尝试跑通，并先维护多套隔离测试权重进行长期观察。2026-05-15: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 3 套 shadow weight profiles（alpha tilt、risk/macro tilt、guardrail tilt）和 `feedback run-shadow-weight-profiles`；真实 2026-05-14 验证 PASS，主线评分 67.79，alpha_tilt=71.46（+3.67）、risk_macro_tilt=63.52（-4.27）、guardrail_tilt=66.18（-1.61），三者 gate 后观察仓位均被 valuation gate 压至 40%-40%；隔离 prediction ledger 写入 3 行，outcome 15 行（available=0、pending=15、missing=0），shadow maturity PASS_WITH_LIMITATIONS；目标测试 23 passed、全量 pytest 537 passed、ruff、diff check 和 secret scan 通过；生产权重、approved overlay、正式 prediction ledger、日报结论和仓位 gate 未改变。|
|CALIBRATION-009|市场反馈优化/当前样本 validation 门槛|P1|VALIDATING|系统验证|当前样本 validation 门槛已实现并跑通；后续继续观察真实滚动样本，production promotion floor、approved overlay 和正式仓位 gate 仍未放宽|样本政策必须明确当前 pilot diagnostic floor 的依据、生产晋级仍使用 promotion floor；shadow maturity 必须区分 validation 与 promotion 模式，validation 达标只能进入 `READY_FOR_VALIDATION_REVIEW`，不得误写为 owner production approval；用现有 decision snapshots / outcomes 跑通 shadow weight 后续验证链路|2026-05-16: 新增并进入 IN_PROGRESS，原因：shadow weight 工程链路已跑通，但默认 20D / promotion 口径导致当前样本只能 pending 或停留观察；owner 要求在不改变生产边界的前提下，把当前积累样本用于启动后续验证。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：`feedback_sample_policy_v2` 将 decision/prediction diagnostic floor 下调为 14/13，promotion floor 仍为 60/30；`feedback shadow-maturity --review-mode validation` 使用 pilot floor 并输出 `READY_FOR_VALIDATION_REVIEW`，不再把低样本验证误写成 `READY_FOR_GOV_REVIEW`。真实 2026-05-14 验证使用 2026-05-04 至 2026-05-14 可追溯 decision snapshots 回填隔离 shadow-weight ledger（2026-04-30 旧 snapshot 因 trace 指向 pytest 临时目录未写 prediction ledger），`calibrate-predictions --horizons 1,5` 生成 66 行 shadow weight outcome（available=36、pending=18、missing=12），validation maturity PASS，market feedback readiness 进入 `READY_FOR_WEIGHT_DIAGNOSTIC_REVIEW`；全量 pytest 538 passed、ruff 和 diff check 通过。|
|CALIBRATION-010|市场反馈优化/shadow weight 表现优选|P1|VALIDATING|系统验证|shadow weight performance evaluator 已实现并用当前样本跑通；三套现有 shadow 权重在 gate 后仓位上与主线完全一致，当前未找到正向 excess profile；下一步应先生成能改变 gate 后仓位的候选权重，或确认硬 gate 下权重调参的有效边界|新增 shadow weight performance evaluator，读取独立 observation ledger 和价格缓存，按同一 signal date 比较 production gate 后仓位与 shadow gate 后仓位的仓位加权收益、最大回撤、换手和成本；报告必须声明 validation-only、production_effect=none，不得自动写 approved overlay 或生产权重；用当前样本筛出 return-leading profile 作为继续观察/调参候选|2026-05-16: 新增并进入 IN_PROGRESS，原因：owner 要求继续推进权重调参以找到更好表现配置；下一步先补主线 vs shadow 的 position-weighted evaluator，避免把“分数更高”误判为“真实表现更好”。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `aits feedback evaluate-shadow-weight-performance`、机器可读 performance CSV 和报告；observation ledger 增补 production/shadow 模型目标仓位与 gate 后目标仓位字段。真实 2026-05-14 验证使用 2026-05-04 至 2026-05-14 样本，Horizon=1D、SMH、单边成本 5bps；三套 profile available=8、pending=1、missing=2，production total return=4.74%、shadow total return=4.74%、excess=0.00%、turnover=1.20，未找到正向 excess profile。目标测试通过，ruff 通过；生产权重、approved overlay、正式 prediction ledger、日报结论和仓位 gate 未改变。|
|CALIBRATION-011|市场反馈优化/shadow gate 参数实验|P1|VALIDATING|系统验证|shadow gate profile 已实现并用当前样本跑通；下一步继续滚动观察样本，production gate 是否替换仍需 promotion floor、forward shadow、owner approval 和 rollback 条件|新增隔离 `shadow_position_gate_profiles` manifest，覆盖 valuation、risk_budget、thesis、confidence、data_confidence 等可配置 hard cap 的 validation-only 候选；`run-shadow-weight-profiles` 可组合 shadow weight 与 shadow gate profile，写入独立 observation/prediction ledger，所有 profile 必须 `production_effect=none`；报告必须披露 observed gate cap、shadow override、gate 后仓位、收益/回撤/换手/成本，不得修改生产 `scoring_rules.yaml`、`portfolio.yaml`、approved overlay、日报结论或正式仓位 gate|2026-05-16: 新增并进入 IN_PROGRESS，原因：现有 shadow weight 调参被 valuation/risk/thesis hard gate 压平，owner 明确允许把这些可配置参数也纳入调参观察，但要求做好 shadow 参数隔离。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `config/weights/shadow_position_gate_profiles.yaml`、`run-shadow-weight-profiles --gate-manifest-path` 组合观察和 gate override 审计字段；真实 2026-05-04 至 2026-05-14 样本已回填 9 个 weight/gate 组合/日，performance evaluator 找到 return-leading profile `shadow_alpha_tilt_v1__shadow_gate_relaxed_valuation_v1`，available=8、pending=1、missing=2，shadow total return=8.31%、production total return=4.74%、excess=3.58%、shadow MDD=-1.57%、production MDD=-1.09%、beat rate=75.00%。全量 pytest 542 passed、ruff 和 diff check 通过；生产 scoring、position gate、approved overlay、正式 prediction ledger 和日报结论未改变。|
|CALIBRATION-012|市场反馈优化/shadow 参数搜索器|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/market_feedback_optimization_loop_2026-05-13.md#阶段-9f-shadow-参数搜索器`；搜索器已实现并用 2026-05-04 至 2026-05-14 当前输入跑通；下一步滚动观察真实样本并补更严格 walk-forward/OOS 评价口径|新增 `aits feedback search-shadow-parameters`，按指定回测区间、搜索空间和目标函数枚举或采样 shadow weight/gate 组合，输出 run manifest、trial registry、pareto front、best profile YAML 和中文报告；必须记录配置 checksum、输入区间、成本假设、seed/算法版本和 production_effect=none；不得修改生产权重、approved overlay、正式 prediction ledger、日报结论或仓位 gate|2026-05-16: 新增并进入 IN_PROGRESS，原因：owner 希望后续频繁调用该功能来确定特定回测区间最优解，本轮先做第一版并用刚才输入跑出当前样本最优权重策略。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 search-space/objective 配置和 `feedback search-shadow-parameters`；真实 2026-05-04 至 2026-05-14 搜索 PASS，共 204 个 weight candidates、4 个 gate candidates、816 个 trials、582 个 Pareto front trials，当前最优 trial 为 `source_current__shadow_gate_relaxed_valuation_v1`，shadow total return 8.31%、production total return 4.74%、excess 3.58%、shadow MDD -1.57%、production MDD -1.09%、turnover 0.60、beat rate 75.00%；结果主要来自 relaxed valuation gate，多个权重候选并列，生产权重、approved overlay、正式 prediction ledger、日报结论和仓位 gate 未改变。2026-05-16: 修正实现语义，原因：owner 指出目标不是从几套预设 gate profile 里选优，而是用算法拟合最优参数；`shadow_parameter_search_space.yaml` 新增 `gate_grid`，默认关闭预设 shadow gate profile 参与最优解选择，`search-shadow-parameters` 改为枚举 weight grid + gate cap grid，并在 manifest/report 记录搜索算法和 grid 启用状态。2026-05-16: 修正版真实搜索 PASS，run id `current_20260504_20260514_grid_v2`；共 204 个 weight candidates、253 个 gate candidates、51,612 个 trials，最优 trial `grid_weight_0118__grid_gate_0217`，target weights 为 trend 25%、fundamentals 35%、macro_liquidity 10%、risk_sentiment 20%、valuation 5%、policy_geopolitics 5%，gate caps 为 valuation 0.70、risk_budget 0.70、thesis 0.70、confidence 0.90、data_confidence 0.80；shadow return 9.18%、production return 4.74%、excess 4.45%、shadow MDD -1.70%、turnover 0.75。|
|CALIBRATION-013|市场反馈优化/shadow 与 production 边界复核|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/shadow_production_boundary_review_2026-05-16.md`；已修复 `validation_shadow` source level 漏配和 parameter shadow 默认 ledger 隔离，并用默认 daily-run 验证全流程|`validation_shadow` 必须是合法参数治理 source level 且只能用于 validation-only shadow 参数面；`run-parameter-shadow` 默认写独立 `prediction_ledger_flow_validation.csv`，不写正式 production prediction ledger；正式 `score-daily`、approved overlay、生产权重、正式仓位 gate、rule card 和日报结论不得因 shadow 链路变化而改变；目标测试、ruff、diff check 和真实 daily-run 通过或报告新的真实门禁原因|2026-05-16: 新增并进入 IN_PROGRESS，原因：owner 要求 review shadow 链路和正式生产链路的逻辑正确性，上一轮 daily-run 已暴露 `validation_shadow` 枚举漏配；同时 review 发现 parameter shadow 默认 ledger 与 validation-only 语义隔离不够。2026-05-16: 从 IN_PROGRESS 改为 VALIDATING，原因：`validation_shadow` 已加入参数治理合法枚举，`run-parameter-shadow` 默认 ledger 已隔离到 `data/processed/prediction_ledger_flow_validation.csv`，正式 `score-daily`、approved overlay、生产权重、正式仓位 gate、rule card 和日报结论链路未改；验证通过 ruff、目标 pytest、`git diff --check` 和真实 `aits ops daily-run`，2026-05-15 run 15/15 PASS，parameter_governance 为 PASS_WITH_LIMITATIONS 且 `production_effect=none`。|
|CALIBRATION-003|市场反馈优化/独立校准闭环|P1|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/market_feedback_optimization_loop_2026-05-13.md`；本轮已实现权重 resolver 接入生产评分/回测审计、approved overlay fail-closed、多目标 candidate gate，以及 OOS/random/coverage veto 汇总；下一步用当前真实 robustness 链路观察新 candidate 状态分布，并等待 owner 对 shadow/overlay 推进节奏做治理确认；不做黑箱权重优化或自动 production 晋级|`score-daily` 和 `backtest` 使用同一个 effective weight resolver 注入模块权重，并在 scores/backtest/trace/报告中写入 `weight_profile_version`、matched overlay、effective weights 和审计原因；approved overlay 出现未知 signal 必须 fail closed；parameter replay/candidate ledger 必须消费 robustness 的 OOS、same-turnover random、signal-family baseline、coverage/data credibility 证据，把 baseline-only、OOS 弱化、随机基线未过、coverage 不足等场景降级或阻断，不再只靠 total return 进入 owner review；runbook/系统流图/测试同步；仍保持 candidate-only、`production_effect=none`，不得自动生成 approved overlay 或改变 hard gate|2026-05-13: 新增并进入 IN_PROGRESS，原因：owner 明确要求建立能持续从市场反馈优化判断逻辑的独立系统，避免无依据 if/else。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：需求文档、任务登记、系统流图/runbook 计划、`aits feedback optimize-market-feedback` 只读报告入口和单测已完成；后续阶段仍需真实周/月样本、weight diagnostics、as-if replay 和 owner governance。2026-05-13: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 追问 30 样本门槛缺少项目内依据，并要求在数据少时先放宽启动后续流程，后续再按观察调整间隔。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `config/feedback_sample_policy.yaml`，报告读取 reporting/pilot/diagnostic/promotion floor；当前 2026-05-12 outcome、因果链、学习队列、规则实验、shadow maturity 和优化报告均已跑通，readiness=`PILOT_DIAGNOSTIC_REVIEW`。2026-05-13: 2026-05-11 严格 outcome 刷新因价格缓存包含 2026-05-12 被数据质量门禁阻断；未绕过门禁，后续如需历史 as_of 刷新应补 replay-scoped feedback calibration 输入。2026-05-13: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求推进整个复盘迭代闭环，当前缺少把参数候选复测收益变化纳入 feedback 报告的功能。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `aits feedback build-parameter-replay`、`parameter_replay_YYYY-MM-DD.md/json`、`optimize-market-feedback` 参数 replay 汇总、系统流图/runbook/README 和测试；真实 smoke 已从最新 robustness summary 生成参数 replay 报告，因来源 summary 较旧仍披露 module_weight_perturbation 缺口。2026-05-13: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 明确要求继续推进到当前数据可测试跑通、并能持续迭代的回测调参闭环。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `aits feedback build-parameter-candidates`、`data/processed/parameter_candidates.json` 和 `parameter_candidates_YYYY-MM-DD.md`；当前数据已跑通 `parameter replay -> candidate ledger -> optimize-market-feedback` 烟测，报告汇总 3 个 trial / 3 个 candidate，因 materiality policy 缺失仍保持 `PASS_WITH_LIMITATIONS` 且不进入 production。2026-05-13: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求继续排查 materiality policy 阻塞并保证闭环跑通。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：`build-parameter-replay` 已对旧 robustness summary 缺 policy 的情况读取当前 `config/backtest_validation_policy.yaml` 并披露 limitation；当前闭环 smoke 显示 Needs materiality policy=0、1 个正向 owner review、1 个负向 risk review、1 个 observe-only，仍保持 production_effect=none。2026-05-13: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求处理所有阻塞点，确保从头开始的回测调参闭环完整跑通；当前阻塞集中在最新 `backtest --robustness-report` 超时且不落盘。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：robustness 派生场景已复用基础回测已审计行，默认全窗口 `backtest --robustness-report --to 2026-05-12 --quality-as-of 2026-05-13` 已完成并写出带 as-run policy 的 summary；随后 parameter replay/candidates/overview 默认链路均跑通，replay/candidate 为 PASS，overview 为 PASS_WITH_LIMITATIONS（受 outcome/shadow/overlay 样本限制）。2026-05-14: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 认为长期维护“跑一次后复用基础行派生指标”会影响回测正确性，要求不要维护两套计算逻辑；本轮改为缓存 PIT feature/report 上下文，但权重扰动、成本压力和窗口切分等调参场景必须继续调用同一评分/回测执行路径。2026-05-14: 从 IN_PROGRESS 改为 VALIDATING，原因：`backtest` 已拆出 prepared PIT context，非 lag 调参场景复用缓存输入但仍重走同一 `run_daily_score_backtest` 评分/回测路径；默认全窗口真实命令 431 秒完成并写出 41 个 robustness 场景，随后 parameter replay/candidates/overview 均读取新产物跑通。验证通过 `ruff check src tests`、目标 pytest 32 passed、全量 pytest 513 passed、`git diff --check`。2026-05-14: 从 VALIDATING 改回 IN_PROGRESS，原因：外部方案复核确认当前短板是 effective weights 未成为评分/回测单一入口，以及 parameter candidate 仍过度依赖 total return；本轮开始实现 resolver 接入和多目标 veto。2026-05-14: 从 IN_PROGRESS 改为 VALIDATING，原因：`score-daily` 与 `backtest` 已接入同一个 effective weight resolver，scores/backtest/trace/report 写入 profile、overlay 和 effective weights；approved overlay 未知 signal fail closed；parameter replay/candidates 汇总并使用 OOS、same-turnover random、signal-family baseline、data quality/data credibility 和 drawdown veto，正向收益不再直接进入 owner review。验证通过 `ruff check src tests`、`git diff --check`、目标 pytest 83 passed 和全量 pytest 516 passed。|
|CALIBRATION-004|市场反馈优化/高级证据门禁与分层迁移|P1|VALIDATING|系统验证 + 项目 owner|真实链路已跑通但所有候选被高级证据阻断；production effect 仍必须等待 owner-approved overlay。下一步观察数据覆盖、随机基线和架构基线阻断是否随样本/数据源改善而解除，再决定是否进入 forward shadow。|扩展当前校准闭环：coverage/placeholder veto、approved overlay 的 `target_weights`/priority/conflict 处理、更多策略 benchmark、统计证据诊断、有效独立样本/purging 口径，以及 alpha/risk/gate 分层迁移的保守落地。验收：需求文档记录阶段拆解和状态；parameter replay/candidates 在 owner review 前暴露阻断证据；robustness 输出新增 benchmark/statistical evidence；全量 pytest 和目标 CLI replay 通过，或记录真实数据/权限阻塞。|2026-05-14: 新增并进入 IN_PROGRESS，原因：owner 要求继续推进外部复核中的其余方向，不只停在 resolver 和 total-return gate 修复；保持 candidate-only，不自动批准 overlay 或改变 hard gate。2026-05-14: 从 IN_PROGRESS 改为 VALIDATING，原因：coverage/source veto、overlay target_weights/priority/conflict、same-exposure/vol-target/no-gate/architecture benchmarks、bootstrap CI、有效独立窗口和 candidate 高级门禁已实现；验证通过 `ruff check src tests`、目标 pytest 26 passed、全量 pytest 524 passed、`git diff --check` 和真实 `backtest robustness -> parameter replay -> parameter candidates -> optimize-market-feedback` 链路。真实 replay 66 场景、material delta=2，candidate ledger 16 个 candidate 全部 `BLOCKED_BY_DATA` 或附带 random/architecture veto，未进入 forward shadow。|
|CALIBRATION-005|参数配置治理/owner 暂缺量化输入|P1|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/parameter_governance_manifest_2026-05-15.md`；参数治理 manifest、CLI 和 market feedback 摘要已完成；OPS-059 后参数治理报告登记为 weekly cadence，不再作为 `daily-run` dashboard 前置步骤；下一步观察真实 candidate ledger 阻断是否随数据覆盖改善解除，owner 暂缺量化输入期间不得改 production 参数|新增 `config/parameter_governance.yaml` 记录可调参数面、来源等级、owner/status/rationale、验证证据、退出条件和 production 边界；新增 CLI 报告读取 parameter candidate ledger、sample/backtest policy 和当前配置，输出 `parameter_governance_YYYY-MM-DD.md/json`；当 owner 缺席时只能给出 keep/collect-evidence/forward-shadow-prep/owner-required/block-by-data 等动作建议，不能写 approved overlay 或修改 scoring/backtest hard gate；报告接入市场反馈优化摘要；OPS-059 后由 `config/scheduled_tasks.yaml` 登记为 weekly governance，不由 daily-run 自动触发；目标测试、ruff、`git diff --check`、全量 pytest 和真实 smoke 通过|2026-05-15: 新增并进入 IN_PROGRESS，原因：owner 明确当前无法提供可量化参数输入，但要求继续迭代参数配置逻辑并修复验证阻塞；本轮选择可审计 governance manifest + evidence-driven action 建议，避免把主观数值硬编码为生产参数。2026-05-15: 从 IN_PROGRESS 改为 VALIDATING，原因：`evaluate-parameter-governance`、`config/parameter_governance.yaml`、`market_feedback_optimization` 汇总和 `daily-run` 前置步骤已实现；真实 2026-05-13 smoke 为 PASS_WITH_LIMITATIONS，16 个 candidate 映射为 BLOCKED_BY_DATA=2、COLLECT_MORE_EVIDENCE=3，无生产参数改写；ruff、目标测试、全量 pytest 528 passed 和 `git diff --check` 通过。2026-05-29: OPS-059 将该报告从 daily-run 自动链路移入 weekly cadence 登记，Reader Brief daily governance 改读 `research-governance-summary`，生产参数边界不变。|
|DATA-018|数据工程/外部供应商请求缓存|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/external_provider_request_cache_2026-05-12.md`；FRED provider 已支持基于同 series/start 最近 2xx cache 的 tail refresh，避免 as_of 当日尚未发布宏观观测时每天重拉全量窗口并被 FRED 504/timeout 阻断；真实 `daily-run --as-of 2026-06-03` 已 31/31 PASS|所有外部供应商请求先查询本地请求级 cache；相同 provider/endpoint/参数请求第二次不得访问供应商；FRED daily refresh 应优先复用同 series/start 的最近可审计 cache，并只请求最近有效观测日至 as_of 的 tail window；合并输出仍必须来自缓存响应 + live/cache tail 响应并交给 `aits validate-data` 判定新鲜度；cache metadata 和失败诊断脱敏且不保存 API key/token/Cookie/User-Agent 原文或供应商响应正文；响应体 checksum 可审计；业务 raw cache、manifest、PIT/SEC/valuation 报告语义保持不变；相关测试和文档通过，真实 daily-run 通过或报告新的真实门禁原因|2026-05-12: 新增并进入 IN_PROGRESS，原因：owner 明确所有外部供应商接口存在调用次数限制，要求所有向外请求的数据都必须 cache，且相同请求不得重复发送。2026-05-12: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `external_request_cache` 并接入主要外部供应商路径；目标测试 123 passed，`ruff check src tests` 和 `git diff --check` 通过，仍需真实 daily-run 观察。2026-05-13: 从 VALIDATING 改回 IN_PROGRESS，原因：真实 daily-run 两次在 `download_data` 阻断，cache metadata 显示 FMP/Cboe/FRED 已有 200 响应但无 Marketstack metadata；需要增强 Marketstack/download-data 失败诊断，避免只看到 stderr 行数。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：Marketstack provider 已在 HTTP 前、HTTP status、provider error、JSON/schema 和 normalization 阶段抛出结构化诊断；`aits download-data` 失败时写入脱敏 `download_data_diagnostics_YYYY-MM-DD.md`；daily-run 计划列出该报告；ruff、目标 pytest 和 `git diff --check` 通过。2026-05-13: 从 VALIDATING 改回 IN_PROGRESS，原因：增强后真实 daily-run 诊断把阻断定位到 FRED response 前 `ReadTimeout`，但 FRED provider 尚未提供 series_id/cache/stage 结构化诊断，仍需补齐后再重跑确认。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：FRED provider 已在 HTTP 前、HTTP status、CSV parse 和 schema 阶段输出结构化诊断，包含 series id、cache key 和预期 metadata path；ruff、目标 pytest 35 passed 和 `git diff --check` 通过。2026-05-13: live daily-run 验证确认当前阻断为 FRED `DGS10` 请求 `ReadTimeout`，stage=`http_request`，cache_status=`MISS_NO_RESPONSE`，DGS2 同范围已有 cache 200；下游按 fail-closed 未执行。2026-05-13: 实现 FRED 无响应超时韧性：timeout_seconds=60、max_attempts=2、retry_backoff_seconds=3，仅对未获得 HTTP response 的网络异常上重试；诊断报告新增 attempt_count、max_attempts 和 timeout_seconds。2026-05-13: 修复后真实 `daily-run` 先越过 `download_data`、PIT、SEC 和 valuation，最终复跑 `daily_ops_run:2026-05-11:20260512T170255Z` 11/11 步 PASS；外部请求 cache metadata 覆盖 FMP、Marketstack、Cboe、FRED、SEC、TSMC IR 和 official policy。2026-06-04: 从 VALIDATING 改回 IN_PROGRESS，原因：`daily_ops_run:2026-06-03:20260603T223206Z` 和重跑 `20260603T223626Z` 均在 `download_data` fail closed；诊断定位 FRED `DGS2` exact full-window 请求 response 前 `ReadTimeout`，curl exact URL 约 60 秒后返回 HTTP 504，但 `DGS2 2026-06-02..2026-06-03`、`DGS10 2026-06-01..2026-06-03` 和 `DTWEXBGS 2026-05-29..2026-06-03` tail windows 可快速返回真实 FRED CSV；需要实现 FRED tail refresh 合并，避免因 as_of 当日尚未发布观测而每天重拉全量窗口。2026-06-04: 从 IN_PROGRESS 改为 VALIDATING，原因：`FredRateProvider` 已改为先查同 series/start 最近 2xx request cache，解析最近有效观测日，再请求 `latest_observation_date..as_of` tail window 并合并去重；非 2xx cache 不作为 seed；验证通过目标 pytest/ruff/black/diff check、真实 `download-data --start 2018-01-01 --end 2026-06-03`、`validate-data --as-of 2026-06-03` PASS，以及 `daily_ops_run:2026-06-03:20260604T011839Z` 31/31 PASS。|
|UNIV-001|标的池/模型参数化|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/ai_universe_and_model_parameterization_2026-05-11.md`；expanded universe 已通过真实非交易日 `daily-run`、交易日 strict `replay-day` 和包含周末的 `replay-window` 验证；下一步继续观察后续真实运行中的 AMZN R&D 披露口径 warning 和新增 ticker 基本面特征质量|新增代表 ticker 进入 `core_watchlist`、watchlist、lifecycle 和 SEC 映射；默认数据下载、特征、评分、回测和 dashboard 使用扩展后的核心观察池；配置测试、watchlist/lifecycle 校验和相关单测通过；文档列出 valuation gate、评分仓位映射、置信度折扣、宏观预算、风险预算、数据质量阈值和执行政策等后续可调参数候选；真实 daily/replay 验证不因新增 ticker 的价格、估值或基本面覆盖配置错误而阻断|2026-05-11: 新增并进入实现，原因：owner 明确系统尚未稳定进入生产，可以优先提升 AI 产业链代表性覆盖，不要求与早期 6 ticker 输出保持兼容；同时要求整理类似 valuation 40% cap 的硬阈值作为后续模型迭代重点。2026-05-11: 从 IN_PROGRESS 改为 VALIDATING，原因：核心观察池扩展到 17 个 ticker，watchlist/lifecycle/SEC 映射、风险事件映射、官方政策 ticker 匹配、AVGO/LRCX 拆股事件、系统流图和参数化清单均已更新；验证通过 CLI 校验、ruff 和全量 pytest。2026-05-11: 从 VALIDATING 改回 IN_PROGRESS，原因：expanded universe 真实非交易日 daily-run 已跑到 SEC metrics 并阻断于 ASML taxonomy 配置，SEC companyfacts 实际返回 `us-gaap + dei`，需要修正配置后复测。2026-05-11: 从 IN_PROGRESS 改为 VALIDATING，原因：ASML SEC taxonomy 修正为 `us-gaap + dei` 并补 `us-gaap/EUR` 指标映射；非交易日 `daily-run --as-of 2026-05-10` 为 PASS_WITH_SKIPS，交易日 `replay-day --as-of 2026-05-08 --full-universe` 为 PASS，`replay-window 2026-05-08..2026-05-10` 为 PASS_WITH_SKIPS（1 个交易日、2 个周末跳过）。|
|RUN-002|运行归档/每日输出路径|P2|VALIDATING|系统验证|详细拆解见 `docs/requirements/daily_run_output_archive_2026-05-11.md`；`daily-run` canonical bundle 已改为执行时间戳优先路径，manifest/docs/tests 已同步；下一步观察真实 daily-run 产物是否符合新目录规范|`daily-run` 默认写入 `outputs/runs/daily/<executed_at_utc>/as_of_<YYYY-MM-DD>__<safe_run_id>/`；manifest 记录执行时间戳、as_of、run root、输入/输出 artifact checksum；legacy `outputs/reports` mirror 保持过渡兼容；`data/raw`/`data/processed` 明确作为状态缓存而非每次运行归档副本；休市日 skipped dashboard 不得从 legacy 路径带入本轮 run bundle|2026-05-11: 新增并进入实现，原因：owner 建议每日输出产物根据执行时间戳整理到统一路径，并进一步明确需要归档记录的数据管理方式。2026-05-11: 从 IN_PROGRESS 改为 VALIDATING，原因：路径生成、manifest 字段、runbook、系统流图和单测已更新，仍需后续真实 daily-run 观察新目录落盘。|
|ARCH-001|架构/CLI 模块化拆分|P2|IN_PROGRESS|系统实现|详细拆解见 `docs/requirements/system_design_review_2026-05-10.md`；`aits docs` 已迁移到 `cli_commands/docs.py`，`aits security scan-secrets` 已迁移到 `cli_commands/security.py`，`aits scenarios validate/lookup` 已迁移到 `cli_commands/scenarios.py`，`aits catalysts validate/upcoming/lookup` 已迁移到 `cli_commands/catalysts.py`，`aits execution validate/lookup` 已迁移到 `cli_commands/execution.py`，`aits watchlist list/validate/validate-lifecycle` 已迁移到 `cli_commands/watchlist.py`，`aits industry-chain list/validate` 已迁移到 `cli_commands/industry_chain.py`，`aits trace lookup` 已迁移到 `cli_commands/trace.py`，`aits evidence validate/import-csv` 已迁移到 `cli_commands/evidence.py`，`aits data-sources list/validate/health/yahoo-price-diagnostic` 已迁移到 `cli_commands/data_sources.py`，`aits thesis list/validate/review` 已迁移到 `cli_commands/thesis.py`，`aits valuation fetch-fmp/fetch-fmp-valuation-history/fetch-eodhd-trends/validate-fmp-history/import-csv/list/validate/review` 已迁移到 `cli_commands/valuation.py`，`aits pit-snapshots validate/build-manifest/fetch-fmp-forward` 已迁移到 `cli_commands/pit_snapshots.py`，`aits llm precheck-claims` 已迁移到 `cli_commands/llm.py`，ETF 根级 compatibility alias 注册已迁移到 `cli_commands/etf_compat.py`，`aits signals build-snapshot/validate-snapshot/ablation/calibrate/explain-ablation/validate-ablation` 已迁移到 `cli_commands/signals.py`，`aits fundamentals list-sec-companies/download-sec-companyfacts/download-sec-submissions/download-sec-filing-archive/sec-accession-coverage/validate-sec-companyfacts/extract-sec-metrics/validate-sec-metrics/build-sec-features/extract-tsm-ir-pdf-text/import-tsm-ir-quarterly/import-tsm-ir-quarterly-batch/fetch-tsm-ir-quarterly/merge-tsm-ir-sec-metrics` 已迁移到 `cli_commands/fundamentals.py`，`aits portfolio exposure/sensitivity/validate-sensitivity/candidates/validate-candidates/explain-turnover/validate-turnover-attribution/review-candidate/decide-candidate/validate-review/track-candidate/validate-tracking/tracking-status/review-tracking/tracking-window-status/validate-tracking-review` 已迁移到 `cli_commands/portfolio.py`，`aits parameters shadow-backtest/validate-shadow-backtest/tune-weights/validate-weight-tuning/tune-weights-stable/diagnose-weight-stability-inputs/recover-weight-stability-inputs/validate-weight-stability-readiness/validate-weight-stability/explain-weight-stability/explain-weight-tuning/explain-weight-tuning-failure/validate-weight-tuning-failure` 已迁移到 `cli_commands/parameters.py`，主系统 `aits data diagnose-backtest-inputs/inspect-registry/validate-backtest-manifest/reconcile-price-cache/refresh-backtest-manifest/freshness/validate-freshness/refresh-market/validate-refresh/recover-freshness/repair-backtest-inputs` 已迁移到 `cli_commands/data.py`，`aits risk-events list/validate/list-occurrences/record-review-attestation/import-occurrences-csv/fetch-official-sources/triage-official-candidates/precheck-triaged-official-candidates/apply-llm-formal-assessment/precheck-openai/import-prereview-csv/validate-occurrences` 已迁移到 `cli_commands/risk_events.py`，共享 profile/path helper 已抽入 `cli_commands/risk_event_artifacts.py` 并供命令模块和 `score-daily` 共用，`aits ops health/daily-plan/daily-run/replay-day/replay-window` 已迁移到 `cli_commands/ops.py`，`aits reports` 命令组已迁移到 `cli_commands/reports.py`，direct dispatcher 报告 alias 已调用 `reports_cli`；保留 `ai_trading_system.cli:app` 入口；下一批继续评估其他低耦合命令组|形成可分阶段实施的 CLI 分包方案；每阶段有明确命令组、导入边界、测试范围和回滚方式；拆分后 `aits` 命令名、参数和退出码保持兼容；不得在未验证前改动评分、回测或日报语义|2026-05-10: 新增，原因：`cli.py` 已超过 400KB，review 认为长期维护风险上升，但本轮应先做运行闭环和报告收口，避免大规模结构迁移。2026-05-10: 路线已在系统设计 review 文档登记，本任务继续保留为下一批实际分包实施入口。2026-05-17: 从 READY 改为 IN_PROGRESS，原因：已完成第一批低耦合命令组迁移，`aits docs validate-freshness` 行为和测试保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第二批低耦合命令组迁移完成，`aits security scan-secrets` 迁入 `src/ai_trading_system/cli_commands/security.py`，主 CLI 只注册 `security_app`，direct dispatcher 改为调用 `security_cli`；命令路径、参数、退出码和报告语义保持兼容，下一步继续评估其他低耦合命令组。2026-06-07: 继续保持 IN_PROGRESS，原因：第三批低耦合命令组迁移完成，`aits scenarios validate/lookup` 迁入 `src/ai_trading_system/cli_commands/scenarios.py`，主 CLI 只注册 `scenarios_app`；命令路径、参数、退出码和情景库报告/查询语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第四批低耦合命令组迁移完成，`aits catalysts validate/upcoming/lookup` 迁入 `src/ai_trading_system/cli_commands/catalysts.py`，主 CLI 只注册 `catalysts_app`；评分路径仍复用底层 catalyst calendar loader/validator，命令路径、参数、退出码和报告语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第五批低耦合命令组迁移完成，`aits execution validate/lookup` 迁入 `src/ai_trading_system/cli_commands/execution.py`，主 CLI 只注册 `execution_app`；评分路径仍复用底层 execution policy loader/validator/report writer，命令路径、参数、退出码和报告语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第六批低耦合命令组迁移完成，`aits watchlist list/validate/validate-lifecycle` 迁入 `src/ai_trading_system/cli_commands/watchlist.py`，`aits industry-chain list/validate` 迁入 `src/ai_trading_system/cli_commands/industry_chain.py`，主 CLI 只注册对应 Typer app；评分、thesis、risk、valuation 和回测路径仍复用底层 config loader/validator，命令路径、参数、退出码和报告语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第七批低耦合命令组迁移完成，`aits trace lookup` 迁入 `src/ai_trading_system/cli_commands/trace.py`，`aits evidence validate/import-csv` 迁入 `src/ai_trading_system/cli_commands/evidence.py`，主 CLI 只注册对应 Typer app；日报/回测 trace bundle 生成和 dashboard 仍复用底层 trace/evidence builder，命令路径、参数、退出码和审计语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第八批低耦合命令组迁移完成，`aits data-sources list/validate/health/yahoo-price-diagnostic` 迁入 `src/ai_trading_system/cli_commands/data_sources.py`，主 CLI 只注册 `data_sources_app`；其他评分/报告命令仍复用底层 data source loader、quality gate 和价格诊断 helper，命令路径、参数、退出码和报告语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第九批低耦合命令组迁移完成，`aits thesis list/validate/review` 迁入 `src/ai_trading_system/cli_commands/thesis.py`，主 CLI 只注册 `thesis_app`；日报人工复核摘要仍复用底层 thesis loader/validator/review builder，命令路径、参数、退出码和 thesis gate / 报告语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第十批低耦合命令组迁移完成，`aits valuation fetch-fmp/fetch-fmp-valuation-history/fetch-eodhd-trends/validate-fmp-history/import-csv/list/validate/review` 迁入 `src/ai_trading_system/cli_commands/valuation.py`，主 CLI 只注册 `valuation_app`；score-daily、回测和 PIT manifest 仍复用底层估值 loader/validator/review builder 与 raw cache 默认目录，命令路径、参数、退出码和估值报告语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第十一批低耦合命令组迁移完成，`aits pit-snapshots validate/build-manifest/fetch-fmp-forward` 迁入 `src/ai_trading_system/cli_commands/pit_snapshots.py`，并修复 daily-run direct dispatcher 对 PIT 与 valuation 模块化命令的引用漂移；验证通过 focused tests、Ruff、black check、PIT validate/build smoke、文档新鲜度和 documentation contract；命令路径、参数、退出码、PIT manifest/raw cache、`--continue-on-failure` 和报告语义保持兼容，不改变评分、回测或日报投资结论。2026-06-07: 继续保持 IN_PROGRESS，原因：第十二批低耦合命令组迁移完成，`aits llm precheck-claims` 迁入 `src/ai_trading_system/cli_commands/llm.py`；验证通过 LLM precheck focused tests、Ruff、black check、CLI help smoke、compileall 和文档新鲜度；命令路径、参数、退出码、provider LLM permission fail-closed、OpenAI request cache、中文报告和待复核队列语义保持兼容，不改变风险事件预审、LLM formal assessment、评分、回测或日报投资结论。2026-06-07: 继续保持 IN_PROGRESS，原因：第十三批低耦合命令组迁移完成，ETF 根级 compatibility alias 注册迁入 `src/ai_trading_system/cli_commands/etf_compat.py`；验证通过 ETF alias focused tests、Ruff、black check、CLI help smoke、compileall 和文档新鲜度；`data ingest/validate`、`features build`、`signals generate`、`regime generate`、`portfolio allocate`、`simulation record/evaluate/report`、`report daily`、`run daily` 和 `experiments register/run/compare` 的 help/命令路径保持兼容，不改变 ETF 子命令实现、根级主系统 `backtest` 或投资解释语义。2026-06-07: 继续保持 IN_PROGRESS，原因：第十四批低耦合命令组迁移完成，`aits signals build-snapshot/validate-snapshot/ablation/calibrate/explain-ablation/validate-ablation` 迁入 `src/ai_trading_system/cli_commands/signals.py`，direct dispatcher 改为调用 `signals_cli`；验证通过 signals focused tests、ETF alias tests、Ruff、black check、CLI help smoke、compileall、文档新鲜度和 documentation contract；命令路径、参数、退出码、artifact 写入/校验、ETF compatibility alias `signals generate` 和报告语义保持兼容，不改变 signal snapshot/ablation/calibration 计算、质量门禁、评分、回测或日报投资结论。2026-06-07: 继续保持 IN_PROGRESS，原因：第十五批低耦合命令组迁移完成，`aits fundamentals list-sec-companies/download-sec-companyfacts/download-sec-submissions/download-sec-filing-archive/sec-accession-coverage/validate-sec-companyfacts/extract-sec-metrics/validate-sec-metrics/build-sec-features/extract-tsm-ir-pdf-text/import-tsm-ir-quarterly/import-tsm-ir-quarterly-batch/fetch-tsm-ir-quarterly/merge-tsm-ir-sec-metrics` 迁入 `src/ai_trading_system/cli_commands/fundamentals.py`，direct dispatcher 改为调用 `fundamentals_cli`；验证通过 SEC/TSMC focused tests、direct dispatcher tests、Ruff、black check、全量 fundamentals CLI help smoke、compileall、文档新鲜度和 documentation contract；命令路径、参数、退出码、SEC/TSMC artifact 写入/校验、日常调度链路和报告语义保持兼容，不改变 SEC/TSMC 数据处理、质量门禁、评分、回测或日报投资结论。2026-06-07: 继续保持 IN_PROGRESS，原因：第十六批低耦合命令组迁移完成，`aits portfolio exposure/sensitivity/validate-sensitivity/candidates/validate-candidates/explain-turnover/validate-turnover-attribution/review-candidate/decide-candidate/validate-review/track-candidate/validate-tracking/tracking-status/review-tracking/tracking-window-status/validate-tracking-review` 迁入 `src/ai_trading_system/cli_commands/portfolio.py`，portfolio artifact resolver 抽入 `src/ai_trading_system/cli_commands/portfolio_artifacts.py` 并供命令与 `reports portfolio-*` alias 共用，direct dispatcher 改为调用 `portfolio_cli`；验证通过 portfolio/direct/ETF alias focused tests、Ruff、black check、portfolio CLI help smoke、report alias help smoke、compileall、文档新鲜度和 documentation contract；命令路径、参数、退出码、ETF compatibility alias `portfolio allocate`、daily-run direct dispatcher 和报告 alias 路径解析保持兼容，不改变 portfolio artifact、tracking state、review report、评分、回测或日报投资结论。2026-06-07: 继续保持 IN_PROGRESS，原因：第十七批低耦合命令组迁移完成，`aits parameters shadow-backtest/validate-shadow-backtest/tune-weights/validate-weight-tuning/tune-weights-stable/diagnose-weight-stability-inputs/recover-weight-stability-inputs/validate-weight-stability-readiness/validate-weight-stability/explain-weight-stability/explain-weight-tuning/explain-weight-tuning-failure/validate-weight-tuning-failure` 迁入 `src/ai_trading_system/cli_commands/parameters.py`，shadow/weight artifact resolver 抽入 `src/ai_trading_system/cli_commands/parameter_artifacts.py` 并供参数命令与 `reports shadow-parameter-backtest` / `reports weight-*` alias 共用，direct dispatcher 改为调用 `parameters_cli`；验证通过 direct/docs focused tests、shadow backtest focused tests、weight tuning focused tests、weight stability/readiness focused tests、weight failure/dashboard focused tests、Ruff、black check、参数 CLI help smoke、report alias help smoke、compileall、文档新鲜度和 documentation contract；命令路径、参数、退出码、daily-run direct dispatcher 和报告 alias 路径解析保持兼容，不改变 shadow backtest、weight tuning、stability、readiness、failure attribution、promotion、评分、回测或日报投资结论。2026-06-07: 继续保持 IN_PROGRESS，原因：第十八批低耦合命令组迁移完成，主系统 `aits data diagnose-backtest-inputs/inspect-registry/validate-backtest-manifest/reconcile-price-cache/refresh-backtest-manifest/freshness/validate-freshness/refresh-market/validate-refresh/recover-freshness/repair-backtest-inputs` 迁入 `src/ai_trading_system/cli_commands/data.py`，market data freshness/refresh artifact resolver 抽入 `src/ai_trading_system/cli_commands/data_artifacts.py` 并供 data 命令与 `reports data-*` alias 共用，direct dispatcher 改为调用 `data_cli`，root 级 `download-data` / `validate-data` 和 ETF compatibility alias `data ingest/validate` 保持不动；验证通过 direct/ETF/scheduled focused tests、data quality/download/ops focused tests、backtest input/registry/repair focused tests、freshness/refresh focused tests、Ruff、black check、data CLI help smoke、report alias help smoke、compileall、文档新鲜度和 documentation contract；命令路径、参数、退出码、daily-run direct dispatcher、ETF compatibility alias 和报告 alias 路径解析保持兼容，不改变数据质量门禁、freshness/refresh side effect、评分、回测或日报投资结论。2026-06-07: 继续保持 IN_PROGRESS，原因：第十九批低耦合命令组迁移完成，`aits risk-events` 命令组迁入 `cli_commands/risk_events.py`，共享风险事件 CLI helper 抽入 `cli_commands/risk_event_artifacts.py`；验证通过 risk-events help smoke、风险事件 focused tests 62 passed、score-daily/ops/direct/backtest/alerts focused tests 102 passed、Ruff、变更文件 Black check、compileall、文档新鲜度和 documentation contract；命令路径、参数、退出码、官方来源抓取、OpenAI 预审、LLM formal、评分、回测和日报投资解释语义保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第二十批低耦合命令组迁移完成，已先读取 `docs/operations/operations_runbook.md`；`aits ops` 命令组迁入 `cli_commands/ops.py`，`cli_direct` 的 `ops health` 改为调用新模块；验证通过 ops help smoke、ops/direct/scheduler focused tests 54 passed、historical replay tests 8 passed、daily task dashboard tests 22 passed、Ruff、变更文件 Black check、compileall、文档新鲜度和 documentation contract；每日 scheduler 统一入口、cadence、调度顺序、质量门禁、canonical run bundle、历史 replay 和 production effect 边界保持兼容。2026-06-07: 继续保持 IN_PROGRESS，原因：第二十一批低耦合命令组迁移完成，`aits reports` 命令组迁入 `cli_commands/reports.py`，direct dispatcher 报告 alias 改为调用 `reports_cli`；验证通过 reports help smoke、direct/scheduler/ops focused tests 54 passed、Reader Brief/dashboard/report focused tests 36 passed、single-report builder focused tests 20 passed、alias writer focused tests 21 passed；报告生成逻辑、Reader Brief、dashboard、daily task dashboard、报告 registry、评分、回测和数据质量语义保持兼容。|
|UI-002|仪表盘/每日决策展示|P2|VALIDATING|系统验证|详细拆解见 `docs/requirements/decision_dashboard_v2_2026-05-10.md`；dashboard v2 已接入自动 daily-run 并保持只读展示；OPS-059 后 market feedback、feedback loop review 和 investment review 不再是 daily-run 前置步骤，dashboard 只读消费已有产物并在缺失时降级显示；下一步观察 Reader Brief 链进入 daily-run 后 dashboard 与 Reader Brief 的职责边界是否清晰|Dashboard v2 仍只读取已生成 artifact，不重新计算结论；HTML 首页能展示今日动作、仓位区间、置信度、Data Gate、最大限制、变化摘要、Top evidence/invalidators/next checks；可选读取既有 `market_feedback_optimization_YYYY-MM-DD.md`、`feedback_loop_review_YYYY-MM-DD.md` 和 `investment_weekly_review_YYYY-MM-DD.md`，缺失时必须标记 `NOT_CONNECTED` / `LIMITED`，不得补造复盘结论；OPS-059 后 `daily-run` 在 `score-daily` 后先生成 dashboard，再继续生成 SEC PIT shadow observe/monitor、score change attribution、market panel、report index、documentation contract、research governance summary、Reader Brief 和 Reader Brief quality；页面、JSON 和文档均声明 `production_effect=none`、不替代 Markdown 日报和 trace bundle 审计源；休市日 skipped dashboard 不得从 legacy 路径带入 canonical run bundle|2026-05-10 至 2026-05-14: 已完成静态 dashboard、自动 daily-run 接入、历史趋势、run bundle mirror guard 和旧反馈复盘读取。2026-05-29: OPS-059 将旧 feedback/investment review daily-run 前置链移出每日执行，改由 `config/scheduled_tasks.yaml` 登记为 weekly/biweekly cadence；dashboard 保持只读可选消费，Reader Brief 上游链成为新的每日报告链。|
|VIS-001|数据可见性/PIT 复现契约|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/input_visibility_contract_2026-05-10.md`；market/macro raw cache replay 过滤已实现；2026-05-01 至 2026-05-10 复测中 5/5-5/8 交易日 PASS，周末正确跳过，5/1 和 5/4 剩余阻断为归档输入缺口|`daily-run` 对历史 `as_of` 在执行任何子命令前 fail closed，并在执行报告/metadata 中说明应改用 `ops replay-day`；`replay-day` 不调用 live provider/OpenAI，按可见窗口过滤 PIT、估值、风险事件、OpenAI cache-only、交易 thesis、交易记录、市场价格和宏观利率输入，并生成 replay-scoped dashboard；所有相关文档说明 `event_time/source_published_at/provider_available_at/ingested_at/available_time/visibility_cutoff` 的分层语义；测试覆盖历史 daily-run 前置阻断、OpenAI cache-only 可见性过滤、手工输入过滤、raw cache 过滤和最近窗口 cache-only replay 全链路|2026-05-10: 新增并进入实现，原因：最近交易日 `daily-run --as-of 2026-05-08` 在 OpenAI 预审处被 `risk_event_prereview_request_in_future` 阻断，暴露生产调度、历史复现和研究重建的输入可见性边界仍不够显式；owner 要求继续修复并用原日期复测。2026-05-10: 从 IN_PROGRESS 改为 VALIDATING，原因：daily-run 历史 as_of 预检查、metadata input visibility、replay dashboard、文档和测试已完成；原日期 daily-run 现在前置 `BLOCKED_VISIBILITY`，cache-only replay PASS。2026-05-10: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 要求 replay 尽量复用历史 OpenAI 请求结果，但必须按复现时点可见性过滤缓存记录，避免未来 LLM 请求结果进入严格复现。2026-05-10: 从 IN_PROGRESS 改为 VALIDATING，原因：OpenAI cache-only replay 已按 `request_timestamp/cache_created_at/cache file created_at` 过滤；真实 2026-05-08 replay PASS，源 OpenAI 预审队列 5 条均因 2026-05-10 请求时间晚于 cutoff 被排除，dashboard/health/secret 全链路通过。2026-05-10: 调查 dashboard `final AI position=0` 时确认，直接原因是 `trade_theses` 全部创建/更新时间为 2026-05-10，严格回放 2026-05-08 时 thesis 校验报 24 个 future-input 错误并触发 `thesis` position gate 上限 0%；当前行为 fail closed，但后续应在 replay 输入冻结阶段先生成 thesis 隔离视图。2026-05-10: 从 VALIDATING 改回 IN_PROGRESS，原因：owner 明确要求这些手工数据也要做好隔离；本轮验收包含 replay path override、过滤 manifest 和原日期复测。2026-05-10: 从 IN_PROGRESS 改为 VALIDATING，原因：手工输入隔离视图已实现并通过全量测试；真实 2026-05-08 replay PASS，6 条 future thesis 被排除，dashboard 最终 AI 仓位从旧 run 的 0%-0% 恢复为估值 gate 约束下的 40%-40%。2026-05-11: 从 VALIDATING 改回 IN_PROGRESS，原因：窗口复测发现 2026-05-05 至 2026-05-07 的 `score-daily` 仍读取完整生产 market/macro raw cache，触发 `prices_future_dates` / `rates_future_dates`。2026-05-11: 从 IN_PROGRESS 改为 VALIDATING，原因：replay raw cache 过滤和 replay download manifest 已实现；复跑窗口后 2026-05-05 至 2026-05-08 PASS，2026-05-01/04 剩余失败为归档 PIT/OpenAI/SEC 输入缺口。|
|OPS-011|运行架构/daily-run 生产 as-of 时区|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/daily_run_market_timezone_asof_2026-05-12.md`；默认 as-of 和 visibility check 已改为按美股市场日期/收盘后可见窗口解析；下一步观察真实 daily-run 是否默认选择最近完整美股交易日|未显式传入 `--as-of` 时，`daily-plan` 和 `daily-run` 默认选择 `America/New_York` 美股常规收盘后 30 分钟可见的最新完整交易日；`daily-run` 输入可见性预检查对交易日使用最新已完成交易日，对显式休市日使用当前美东日期；显式未来 as-of 仍 fail closed，显式历史 as-of 仍提示使用 cache-only replay；测试、README、runbook 和系统流图同步更新|2026-05-12: 新增并进入实现，原因：自动化在日本时间早晨默认取本机日期 `2026-05-12`，而 visibility check 使用 UTC 日期，未按 U.S. equity market 交易日/收盘后可见窗口解析，导致生产 daily-run 日期边界不可靠。2026-05-12: 从 IN_PROGRESS 改为 VALIDATING，原因：美东市场日期 resolver、daily-plan/daily-run 默认日期、交易日/休市日 visibility check、文档和测试已完成；`daily-plan` smoke 默认评估日期为 `2026-05-11`。|
|OPS-013|运行架构/daily-run direct dispatcher|P0|VALIDATING|系统验证|`cli_direct` 已按当前 Typer `score-daily` 语义映射 OpenAI precheck flag；下一步观察后续 CLI 签名演进是否继续有 direct dispatcher parity 测试覆盖|`.venv\\Scripts\\python.exe -m ai_trading_system.cli_direct score-daily ...` 必须把 `--risk-event-openai-precheck/--skip-risk-event-openai-precheck` 正确映射到当前 `score_daily(risk_event_openai_precheck=...)`；daily-run 不得因 direct dispatcher 与主 CLI 签名漂移阻断；目标单测、ruff、`git diff --check` 和真实 daily-run 复测通过或报告新的真实门禁原因|2026-05-13: 新增并进入 IN_PROGRESS，原因：修复 FRED 无响应重试后，真实 `daily-run` 的 `score_daily` 子步骤失败于 `TypeError: score_daily() got an unexpected keyword argument 'skip_risk_event_openai_precheck'`，属于 daily-run 入口 wiring bug。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：`cli_direct` 已改为传 `risk_event_openai_precheck=not --skip-risk-event-openai-precheck`；新增 direct dispatcher 单测；真实 `daily-run` 已越过 `score_daily` 并最终 11/11 PASS。|
|OPS-014|运行健康/PIT production visibility cutoff|P0|VALIDATING|系统验证|真实 latest production daily-run 曾在 `ops health` 误把收盘后/JST 次日 PIT `available_time` 判为晚于 `as_of`；已改为仅对当前最新已完成美股交易日使用 health 运行时间作为 production visibility cutoff；下一步观察后续 production/replay 双路径|`aits ops health --as-of <latest_completed_us_trading_day>` 对本轮 forward-only PIT 应要求 `available_time <= visibility_cutoff`，而不是要求 `available_time.date <= as_of`；历史 as-of 未进入 production cutoff 时仍按 as-of 严格阻断未来 `available_time`；pipeline health、alerts、daily-run 和 replay 语义不得混淆；相关测试、文档和真实 daily-run 通过|2026-05-13: 新增并进入 VALIDATING，原因：`daily_run:2026-05-11:20260512T164618Z` 已完成评分和 dashboard，但 `ops health` 因 PIT latest_available_time=2026-05-12 晚于 as_of=2026-05-11 失败；修复后单独 `ops health --as-of 2026-05-11` PASS，真实 `daily-run` 11/11 PASS。|
|OPS-016|运行架构/Reader Brief current-run decision summary|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/reader_brief_current_decision_summary_2026-06-04.md`；daily-run finalization 已在 Reader Brief step 通过后，用 current-run `daily_decision_summary` / `daily_task_dashboard` 只读刷新最终 Reader Brief 和 quality mirror；下一步观察后续真实 daily-run 是否持续不保留旧失败 run Core Decision|`aits ops daily-run` 最终发布的 Reader Brief 必须读取同一 run 的 `daily_decision_summary` 和 `daily_task_dashboard`；canonical run bundle 与 legacy `outputs/reports` mirror 保持一致；修复只允许重建 Reader Brief / Reader Brief quality，只读、`production_effect=none`，不得重跑 scoring/PIT/SEC/valuation/backtest/shadow/weight/docs 上游，不写 production weights、active shadow weights、broker state 或交易指令；目标测试和真实 as_of 复测通过|2026-06-04: 新增并进入 IN_PROGRESS，原因：修复 FRED 后完整 daily-run 31/31 PASS，但当前 Reader Brief HTML Core Decision 仍显示 `missing`，排查确认其读取的是修复前失败 run `daily_ops_run:2026-06-03:20260603T223626Z` 的 stale summary；current-run summary `20260604T011839Z` 在 Reader Brief 后才刷新。2026-06-04: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 daily-run finalization Reader Brief refresh helper、legacy mirror 覆盖 Reader Brief/quality、daily-run CLI 单测断言 current run_id、系统流图/artifact catalog/runbook 文档；验证通过 `tests/test_ops_daily.py tests/test_reader_brief.py`、ruff、diff check、`aits reports reader-brief --as-of 2026-06-03`、`aits reports validate-reader-brief --as-of 2026-06-03`、`aits docs report-contract --latest` 和完整 `aits ops daily-run --as-of 2026-06-03`（`daily_ops_run:2026-06-03:20260604T020951Z`，31/31 PASS）。|
|OPS-012|运行架构/daily-run 子命令解释器|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/daily_run_local_python_executor_2026-05-12.md`；本地 `.venv` 已标准化为 Python 3.11 并补齐项目/IBKR 只读依赖，关键测试和 IBKR snapshot 已用 `.venv` 通过；下一步观察后续真实 daily-run 是否仍有 native crash|`daily-run` 执行 `aits ...` 子命令时若项目 `.venv` Python 存在，必须使用该解释器调用 direct dispatcher；找不到时才回退当前 `sys.executable`；子命令必须强制使用稳定 Python env、每次运行唯一 `PYTHONPYCACHEPREFIX`，并在启动前清理源码 `__pycache__`；PIT 等轻量子命令不得因无关 valuation pydantic class construction 或 Typer 全局 command tree annotation 解析崩溃；报告仍展示逻辑命令，主报告/metadata 不记录 secret 或未脱敏 stdout/stderr 原文；失败步骤必须写入脱敏 stdout/stderr 诊断 artifact 并在执行报告/metadata 暴露路径；真实 daily-run 必须完成最新交易日链路或报告新的真实门禁原因|2026-05-12: 新增并进入实现，原因：本机 PATH `aits` 指向全局 Python `C:\Users\32739\AppData\Local\Programs\Python\Python311\Scripts\aits.exe`，daily-run 子命令继承全局解释器后在 PIT/SEC 大数据步骤出现 Windows access violation；项目 `.venv\Scripts\python.exe -m ...` 直接运行 PIT/SEC 可通过。2026-05-12: 已改用项目 `.venv`、强制 `PYTHONMALLOC=malloc`/`PYTHONDONTWRITEBYTECODE=1`，将 `PYTHONPYCACHEPREFIX` 改为每次运行唯一目录，清理 `src/**/__pycache__`，移除 valuation schema 对 pydantic import-time model construction 的依赖，并新增 `cli_direct` 绕开 Typer 全局解析；PIT 单独 direct dispatcher 可 PASS，但完整 daily-run 仍在 download 后的 PIT 子进程随机 3221225477，已按 fail-closed 停止。2026-05-17: 从 BLOCKED 改为 IN_PROGRESS，原因：owner 明确要求继续推进 Python 版本切换；本轮目标是把本地项目命令、验证和 IBKR Paper read-only snapshot 切到 `.venv` Python 3.11，与 CI 和工具 target version 对齐。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `.python-version=3.11`、`brokers` optional dependency group 和 README 3.11 venv 安装说明，本机 `.venv` 已补齐依赖；验证通过 `.venv` Python 3.11 的 IBKR Paper snapshot、`tests/trading_engine`、全量 pytest、ruff 和 black check。2026-05-28: 继续保持 VALIDATING，原因：真实 daily-run 连续两天在 `valuation_snapshots` 出现短暂 return_code=1，但旧执行器只保留 stderr 行数，无法事后还原 traceback；本轮增加失败步骤脱敏 stdout/stderr 诊断 artifact，以便后续 root cause 排查。|
|DATA-017|数据工程/FMP forward PIT normalized_id|P0|VALIDATING|系统实现|详细拆解见 `docs/requirements/fmp_forward_pit_id_normalization_crash_2026-05-12.md`；PIT 单独 direct dispatcher 已 PASS，完整 daily-run 仍受 OPS-012 Windows native crash 阻断|FMP forward-only PIT 标准化 ID 生成必须对非 ASCII、超长和非标供应商字段保持确定性、可读前缀和冲突保护；不得因逐字符 Unicode 分类或大批量 dataclass 字段复制触发 Windows 原生访问冲突；真实 CLI 应从 raw payload 流式写出 normalized CSV，而不是为全量行创建常驻 dataclass；raw payload、normalized CSV、PIT manifest、抓取报告和 PIT validation 报告必须正常生成；真实 `daily-run` 至少越过 PIT 阶段|2026-05-12: 新增并进入实现，原因：最新交易日 `daily-run` 默认 as-of 已正确落到 `2026-05-11`，但 `pit_snapshots` 在 `_id_token` 标准化真实 FMP payload 时 Windows access violation 退出，未生成当日 FMP PIT 抓取报告、normalized CSV 或 PIT validation 报告。2026-05-12: 修复 raw JSON、scalar extraction、report attach 后，栈又定位到 normalized dataclass 构造/复制 12411 行；CLI 已改为 streaming normalized CSV，单独 PIT direct dispatcher PASS（17 payload、12411 rows、manifest PASS）。|
|FUND-002|基本面/SEC companyfacts 写入稳定性|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/sec_companyfacts_json_write_stability_2026-05-12.md`；实现已完成，下一步观察最新交易日真实 daily-run 是否稳定越过 SEC companyfacts 并暴露后续真实门禁|SEC companyfacts raw JSON 写入必须保留原始字段和值，但不得对大型 payload 先做完整 pretty/sorted `json.dumps` 中间字符串；manifest 继续记录 endpoint、请求参数、fact count 和 checksum；真实 daily-run 至少越过 SEC companyfacts|2026-05-12: 新增并进入实现，原因：扩展核心观察池后，17 个 ticker 的 SEC companyfacts 大型 JSON 在 `_write_json()` 的 `json.dumps(..., sort_keys=True, indent=2)` 路径触发 Windows access violation，阻断每日链路。2026-06-07: 从 IN_PROGRESS 改为 VALIDATING，原因：SEC companyfacts 下载已优先保存 raw response bytes，非 raw provider 改为 `JSONEncoder.iterencode()` chunk 写入 compact JSON，manifest checksum 仍基于实际写入文件；focused tests、ruff、compileall 和 git diff check 通过，真实 daily-run 进入后续观察。|
|RISK-015|风险事件/OpenAI 预审可见时间|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/risk_event_openai_prereview_visibility_cutoff_2026-05-12.md`；显式 production `visibility_cutoff` 已从 `daily-run` 注入 `score-daily` 并传入 OpenAI 预审校验，报告已披露 request/as_of/cutoff；下一步用真实 latest production daily-run 观察|最新已完成美股交易日的生产 `score-daily` / `daily-run` 可以在收盘后 UTC 次日生成 OpenAI 预审记录并通过校验；历史 as-of 未提供生产 cutoff 时仍拒绝晚于评估日的 live OpenAI 请求；报告/文档说明 request timestamp、as_of 和 visibility cutoff 的关系|2026-05-12: 新增并进入实现，原因：最新交易日 `daily-run` 已越过 PIT、SEC 和 valuation，但 `score_daily` 在 `risk_event_prereview_request_in_future` 阻塞；现有校验把收盘后 UTC 次日生成前一美股交易日日报误判为未来请求。2026-06-07: 继续实现，原因：已有校验层参数但生产命令传递和报告披露仍不完整；验收范围限定为风险事件 prereview cutoff、focused tests 和相关文档。2026-06-07: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `score-daily --risk-event-openai-precheck-visibility-cutoff`、daily-run latest production 注入、预审报告 request/as_of/cutoff 披露和 focused tests；验证通过相关 pytest、ruff 和 `git diff --check`，尚未执行真实 latest production daily-run。|
|DATA-016|数据质量/价格 reconciliation|P0|VALIDATING|系统验证|详细拆解见 `docs/requirements/confidence_lift_workplan_2026-05-10.md`；`NOW` 2025-12-17/18 拆股窗口 raw close 日期口径差异已按已知 corporate action 归因，下一步观察后续真实 `download-data` / `validate-data` 是否继续无未解释 raw close 冲突|`aits validate-data` 或独立 reconciliation 报告能列出主源/二源 ticker-date 差异、分类规则、证据字段、主/二源数值、可解释原因和不可解释冲突；能证明是指数 volume、已知拆股窗口日期口径或分红复权口径差异的必须降为有证据限制；无法解释的 raw close/OHLC/非正价格仍不得静默通过|2026-05-10: 新增并进入实现，原因：owner 指出该项理论上可把日报置信度从 61 提升到约 76；实现必须可审计，不允许静默忽略。2026-05-10: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 INFO severity、指数 volume/拆股/Marketstack 自身坏点/adjusted close 口径差异 reconciliation CSV；真实 `validate-data --as-of 2026-05-10` 为 PASS，错误 0、警告 0、信息 11。2026-05-11: 从 VALIDATING 改回 IN_PROGRESS，原因：expanded universe 真实价格刷新后，`NOW` 官方 5-for-1 split 导致 Marketstack 在拆股窗口提前按 split-adjusted raw close 记录 2025-12-17，当前规则仍按未解释 raw close mismatch 阻断，需要补 corporate-action window 归因。2026-05-11: 从 IN_PROGRESS 改为 VALIDATING，原因：已补 `NOW` 官方 5-for-1 split 配置和 `known_split_raw_close_basis_difference` reconciliation 分类；`validate-data --as-of 2026-05-10 --full-universe` 与 replay 内 `data_quality_2026-05-08` 均为 PASS，错误 0、警告 0。|
|FUND-001|基本面/TSM IR 覆盖规则|P1|VALIDATING|系统验证|详细拆解见 `docs/requirements/confidence_lift_workplan_2026-05-10.md`；TSM IR 已合并进 2026-05-10 SEC-style metrics，且日报计划/`score-daily` 已接入合并路径；下一步观察每日流程持续运行结果|TSM 基本面覆盖由 TSMC IR 官方季度 Management Report 或等价可审计来源满足；缺 TSM IR 时报告明确缺官方 IR 覆盖；不再因 TSM 缺 SEC companyfacts 季度指标而误判为美国 SEC 指标缺口|2026-05-10: 新增并进入实现，原因：owner 明确 TSM 不应硬等同美国 SEC companyfacts 覆盖。2026-05-10: 从 IN_PROGRESS 改为 VALIDATING，原因：`merge-tsm-ir-sec-metrics` 后 SEC metrics 72 行且 validation PASS，`build-sec-features` PASS、警告 0。2026-05-10: 补充日报链路，`ops daily-plan` 在 `validate-sec-metrics` 前加入 TSM IR 合并，`score-daily` 在本地 TSM IR 缓存存在时自动合并后再校验。|
|VAL-001|估值/PIT 历史日回跑|P0|VALIDATING|系统验证|估值校验已改为历史 as-of 只使用可见快照，未来快照进入 warning 审计；下一步观察后续 replay/daily-run 是否继续保持无误用未来估值|历史 as-of 日报或 daily-run 只使用评估日可见估值快照；后续日期快照不得进入评分/复核，也不得导致历史日流程误失败；被排除快照必须以 warning/审计记录出现在估值校验报告；真实 schema/load 错误和当前可见快照错误仍 fail closed|2026-05-10: 新增并进入实现，原因：owner 要求测试最后一个交易日流程，真实 `ops daily-run --as-of 2026-05-08` 失败于 `valuation_snapshots`，暴露累计快照目录对历史日回跑的未来数据处理缺陷。2026-05-10: 从 IN_PROGRESS 改为 VALIDATING，原因：`valuation fetch-fmp --as-of 2026-05-08` 已从 FAIL 转为 PASS_WITH_WARNINGS，报告显式列出被排除的 2026-05-09/2026-05-10 快照。|
|PROD-002|趋势判断/风险复核运行纪律|P1|BLOCKED_OWNER_INPUT|项目 owner + 系统验证|`RISK-005/RISK-006/RISK-007/RISK-008` 已有工程底座；下一步需要 owner 明确每日风险复核责任人、合格来源清单、跳过规则和复核声明运行纪律；详细索引见 `docs/requirements/production_readiness_gaps_2026-05-04.md` 与 `docs/requirements/risk_event_review_workflow_2026-05-04.md`|连续运行样本中，日报能区分“已复核且未发现未记录重大事件”“有待复核 backlog”“复核缺失导致 policy/geopolitics 降级”；有效复核声明必须包含 reviewer、source scope、reviewed_at、coverage window 和结论；缺失或过期声明时不得把空 occurrence 目录解释为无风险|2026-05-06: 从 `PROD-001` 拆出，原因：总控任务已完成缺口登记和边界收口，但风险事件真实复核是 owner 运营纪律，不应继续压在总任务里。|
|RISK-013|风险事件/官方复核闭环|P1|VALIDATING|系统验证 + 项目 owner|详细拆解见 `docs/requirements/confidence_lift_workplan_2026-05-10.md` 和 `docs/requirements/risk_event_llm_formal_assessment_2026-05-10.md`；日报内自动 LLM formal occurrence/attestation 写入、65% LLM formal 置信度和文档边界已实现；下一步观察真实 2026-05-12 及后续交易日 daily-run 是否稳定解除 `policy_geopolitics` 低置信，同时不触发 position gate|报告能列出官方来源权限状态、OpenAI 预审队列、官方候选分类、已写入 occurrence/attestation 和缺口；`score-daily` 在 OpenAI 预审通过后可自动写入 LLM formal assessment，full coverage LLM formal 不再触发低置信模块；LLM formal 仍不得伪装成人工复核、不得单独触发 position gate，且报告必须披露人工复核跳过原因、行为影响、风险、验证覆盖和退出条件；真实人工复核声明仍只能由 reviewer 显式写入|2026-05-10: 新增并进入实现，原因：owner 要求补风险事件复核闭环以解除 `policy_geopolitics` 的 `insufficient_data` 和 data confidence 上限；人工复核不得由系统伪造。2026-05-10: 从 IN_PROGRESS 改为 BLOCKED_OWNER_INPUT，原因：官方 API key 可见、官方来源抓取/triage/occurrence 校验已通过，但当前只有 LLM formal attestation，没有真实人工复核结论，不能写 manual_input 声明。2026-05-12: 从 BLOCKED_OWNER_INPUT 改为 IN_PROGRESS，原因：owner 明确允许短期放宽人工复核限制，以 LLM formal assessment 作为可信政策/地缘结论，但必须保留审计边界和退出条件。2026-05-12: 从 IN_PROGRESS 改为 VALIDATING，原因：`score-daily` 自动 LLM formal 写入、配置化 LLM formal 置信度、报告/系统流图/README/数据源说明和测试已完成；`python -m pytest -q` 通过 467 项。|
|RISK-014|风险事件/PIT 历史日回跑|P0|VALIDATING|系统验证|风险事件 occurrence 校验已改为历史 as-of 只使用可见记录/声明，未来记录进入 warning 审计；下一步观察后续 replay/daily-run 是否继续保持无误用未来风险事件|历史 as-of 日报或 daily-run 只使用评估日可见的风险事件 occurrence、evidence 和 attestation；后续日期记录不得进入评分、仓位闸门或复核声明，也不得导致历史日流程误失败；被排除记录必须以 warning/审计记录出现在风险事件校验报告；当前可见记录自身错误仍 fail closed|2026-05-10: 新增并进入实现，原因：owner 要求测试最后一个交易日流程，估值阻断解除后，真实 `score-daily --as-of 2026-05-08` 失败于 2026-05-10 风险事件记录相对历史日的未来日期。2026-05-10: 从 IN_PROGRESS 改为 VALIDATING，原因：`score-daily --as-of 2026-05-08 --skip-risk-event-openai-precheck` 已通过，报告显式列出被排除的未来 occurrence/attestation。|
|THESIS-002|交易 thesis/初始主动 baseline|P1|BLOCKED_OWNER_INPUT|项目 owner + 系统验证|详细拆解见 `docs/requirements/confidence_lift_workplan_2026-05-10.md`；原 6 个 active baseline thesis 已写入并校验通过；扩展后的新增 ticker 仍为 `watch_only`，暂不要求主动交易 thesis；下一步需要 owner/人工复核 pending 业务指标|原 6 个主动 baseline ticker 均具备 active thesis、验证指标、证伪条件、复核频率、来源、适用边界和 owner 复核状态；校验通过；日报不再因缺少主动交易 thesis 将 `watch_only` 观察池误写成 thesis 失败|2026-05-10: 新增并进入实现，原因：owner 要求建立核心 ticker thesis，提高趋势判断到主动交易假设的可解释性。2026-05-10: 从 IN_PROGRESS 改为 BLOCKED_OWNER_INPUT，原因：MSFT/GOOG/TSM/INTC/AMD/NVDA thesis YAML 已写入，`thesis validate` PASS；`thesis review` 仍为 WATCH，因部分业务驱动指标需人工复核，系统未伪装为 confirmed。2026-05-11: owner 扩展核心观察池到 17 个代表性 ticker；本任务语义收窄为原 6 个主动 baseline thesis，新纳入 ticker 暂按 `watch_only` 处理，不新增 thesis gate 负担。|
|PROD-004|趋势判断/PIT 估值样本成熟度|P1|BLOCKED_EXTERNAL|系统验证 + 时间窗口|`DATA-003/BACKTEST-002/BTINPUT-001` 已完成 forward-only PIT 快照、回测可信度标签和覆盖诊断；下一步等待自建 PIT 样本自然积累，并用覆盖报告判断何时可提升估值/预期结论可信度|`aits backtest-pit-coverage` 能给出 core universe 的可用样本起始日期、覆盖率和 A/B readiness；`score-daily` 对 `eps_revision_90d_pct`、valuation percentile 和 vendor/self-archived PIT 来源的限制声明与实际覆盖一致；历史不足时继续降级，不得用采集日视图伪装 signal_date 可见数据|2026-05-06: 从 `PROD-001` 拆出，原因：owner 已决定不购买或伪造历史 PIT；剩余工作是持续验证 forward-only 样本成熟度，而不是继续推进总控任务。2026-05-10: owner 再次要求继续积累 forward-only PIT；本轮只刷新覆盖报告和运行状态，不回填历史 PIT。2026-05-10: 测试最后交易日流程时确认，若在 2026-05-10 对 2026-05-08 执行 live `pit-snapshots fetch-fmp-forward`，`available_time` 会晚于 as_of 并被 `ops health` 正确阻断；历史交易日应使用 `ops replay-day` 的 cache-only/隔离视图，而不是用 live daily-run 伪造 PIT。|
|PROD-005|趋势判断/规则治理 owner approval|P1|BLOCKED_OWNER_INPUT|项目 owner + 系统验证|`GOV-001/GOV-002/GOV-003` 已有 rule card、rule version 注入和受控 promotion/retirement 基础版；下一步需要 owner 对 production rule baseline、promotion 条件和 retirement 条件形成批准记录|核心 production rule card 具备 owner approval、适用范围、验证证据、已知失败模式、rollback condition 和最后复核时间；未批准候选规则不得改变正式评分、仓位 gate、日报结论或回测 production 规则；日报、回测、decision snapshot 和 trace 能追溯本次运行使用的规则版本|2026-05-06: 从 `PROD-001` 拆出，原因：规则治理工程底座已完成，但真实批准流属于 owner 决策和长期治理，不应让 `PROD-001` 继续保持进行中。|
|PROD-006|趋势判断/forward shadow 与 outcome 成熟度|P1|BLOCKED_EXTERNAL|系统验证 + 时间窗口|`SHADOW-002/SHADOW-003/REPORT-005` 已有 challenger shadow runner、样本成熟度报告和 production vs challenger 复盘入口；下一步等待真实 prediction outcome 样本达到可评估窗口|shadow maturity 报告按 candidate、horizon 和 `ai_after_chatgpt` regime 输出样本数、pending/missing、收益、胜率、回撤和 benchmark excess；样本不足时 promotion gate 维持观察或缺失状态；达到门槛前，回测或 shadow 结果不得被写成 production 规则晋级结论|2026-05-06: 从 `PROD-001` 拆出，原因：趋势判断长期可信度需要真实前向样本，但该成熟度受交易日和 label horizon 限制，适合作为独立时间窗口任务。2026-05-10: owner 要求继续积累 outcome 样本；本轮刷新成熟度/校准报告，样本不足仍保持外部时间窗口阻塞。|

## 暂缓任务

本节保留已明确暂缓、但未来可能因数据规模、owner 决策或外部条件变化而重新打开的任务。

|ID|领域|优先级|状态|下一责任方|阻塞或下一步|验收标准|备注|
|---|---|---|---|---|---|---|---|
|PROD-003|趋势判断/第二合格数据源准入|P1|DEFERRED|项目 owner + 系统验证|owner 2026-05-10 决策继续使用现有 FMP + Cboe VIX + Marketstack + FRED，暂无引入额外 macro/price qualified source 计划；系统继续用 provider health、FMP/Marketstack 价格 reconciliation、FRED series freshness 和数据质量门禁披露限制|重新打开前需 owner 明确新增宏观或价格 source、授权/成本、endpoint、缓存审计字段和长期口径；暂缓期间，趋势判断报告不得把 `macro_rates` 单一 FRED 来源写成跨源核验完成，价格仍按 FMP + Marketstack baseline 标识|2026-05-06: 从 `PROD-001` 拆出，原因：市场价格和宏观利率 reconciliation 是趋势判断可信度前置条件，但供应商选择与授权需要独立 owner 决策。2026-05-10: 从 BLOCKED_OWNER_INPUT 改为 DEFERRED，原因：owner 确认继续使用现有 FMP + Marketstack + FRED（含 Cboe VIX 专用源）且暂无引入第二个更可靠 macro/price qualified source 计划；缺口改为明确限制和报告披露，不再等待供应商选择。|
|STORAGE-001|数据工程|P3|DEFERRED|系统实现|已复核当前本地缓存、回测和报告规模，暂不引入 DuckDB / Parquet；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#storage-001`|重新打开前需证明 CSV 在 schema 演进、join 性能、历史快照、类型约束或审计查询上成为实际瓶颈；迁移仍必须保持 CSV 兼容期、checksum、数据版本、来源追溯和回滚验证|2026-05-06: 从 PROPOSED 改为 DEFERRED，原因：本轮全量校验、短区间真实缓存回测和报告生成未暴露存储层瓶颈；当前 CSV 更利于本地审计和可读性，避免无收益迁移。|

## 更新模板

新增任务时使用下面格式补充到“当前任务”表：

```text
ID:
领域:
优先级:
状态:
下一责任方:
阻塞或下一步:
验收标准:
备注:
```

状态更新时至少记录：

```text
YYYY-MM-DD: 从 <旧状态> 改为 <新状态>，原因：<触发条件、实现范围、验证结果或 blocker>。
```
