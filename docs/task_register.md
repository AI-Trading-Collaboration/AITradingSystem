# 任务登记与优先级机制

本文是未完成任务、后续优化、阻塞项、基础版遗留问题和 owner 配合事项的统一登记表。`docs/implementation_backlog.md` 继续负责长期模块路线图；本文负责具体任务的优先级、状态和下一步动作。

最后更新：2026-05-05

## 使用规则

- 所有非平凡 TODO、review 后续项、基础版遗留项、临时绕行方案和需要 owner 配合的数据源事项，都必须登记在本文。
- 不把任务只留在代码注释、聊天记录、临时 checklist 或个人记忆中。
- 后续讨论出的非平凡需求、bug 修复、评分调整、数据链路调整、报告行为调整，必须先在本文新增或更新任务，明确优先级、状态、下一责任方、阻塞项和验收标准，再进入实现。
- 只有不影响系统行为、投资解释、数据流、数据质量、评分、回测或报告输出的纯 housekeeping，才可以不先登记。
- 如果需求上下文太长，不适合塞进任务表格，应该在 `docs/requirements/` 或其他清晰命名的 `docs/` 子目录创建独立 Markdown 文档。任务表只保留简短摘要和文档链接；详细背景、设计决策、开放问题、验收标准、进展记录和状态迁移必须在该文档中维护。
- 如果任务需要拆成多个开发步骤，必须先创建或更新独立需求文档，记录阶段拆解、依赖关系、实施顺序、各阶段验收标准、开放问题和状态迁移；任务表必须引用该文档，而不是把完整计划塞进单个表格单元。
- 使用独立需求文档时，后续开发进展必须同步更新任务表的摘要/状态，以及被引用文档中的详细进展记录。
- 任务状态变化必须和触发变化的代码或文档改动一起更新。
- `BASELINE_DONE` 不等于完成。它表示已有可审计基础闭环，但仍存在数据源、验证、设计或覆盖缺口。
- 被 owner 输入、数据源、API 权限、样本时间窗口或成本限制阻塞的任务，必须明确 blocker 和解除条件。
- 每个任务必须有可验收的完成标准，避免长期停留在模糊“优化”状态。

## 优先级

|优先级|含义|处理原则|
|---|---|---|
|P0|影响正确性、数据质量门禁、未来函数、仓位硬约束或可能导致错误投资解释|优先于新功能；未解决前不能把相关输出当作完整交易结论|
|P1|显著改善评分、回测、数据源可信度、风险控制或审计解释能力|排在近期开发队列前列|
|P2|提升覆盖率、易用性、报告下钻、长期维护或开发效率|在 P0/P1 稳定后推进|
|P3|低风险清理、文档润色、非关键体验优化|有明确收益时再做|

## 状态

|状态|含义|
|---|---|
|PROPOSED|已记录，但需求、设计或验收标准还未充分明确|
|READY|需求和验收标准明确，可以实施|
|IN_PROGRESS|正在实现或验证|
|BLOCKED_OWNER_INPUT|需要项目 owner 提供数据源、权限、样本、业务判断或人工复核|
|BLOCKED_EXTERNAL|受外部 API、供应商、时间窗口、成本或依赖限制|
|BASELINE_DONE|基础版已完成，但仍有明确后续缺口|
|VALIDATING|实现已完成，等待真实数据、回测、审计报告或运行观察确认|
|DONE|验收标准已满足，且相关文档、测试和报告已更新|
|DEFERRED|明确暂缓，且暂缓原因已记录|
|DROPPED|明确不再需要，且原因已记录|

## 当前任务

|ID|领域|优先级|状态|下一责任方|阻塞或下一步|验收标准|备注|
|---|---|---|---|---|---|---|---|
|PROD-001|趋势判断就绪/日报边界|P1|IN_PROGRESS|系统实现 + 项目 owner|根据 owner 2026-05-04 决策，当前日报不追求实际触发交易，生产化缺口改按“趋势判断可靠性”排序；详细索引见 `docs/requirements/production_readiness_gaps_2026-05-04.md` 和 `docs/requirements/trend_judgment_scope_2026-05-04.md`|每个阻止日报成为可靠趋势判断输入的缺口都有对应任务、owner、阻塞条件和验收标准；交易执行、券商税费、账户级买卖闭环不再作为近期阻塞项，只保留为后续可选扩展；后续推进时同步更新本任务和对应子任务|2026-05-04: 新增任务，原因：2026-05-04 日报虽成功生成但结论使用等级为 `data_limited`、执行动作为 `observe_only`，需要把生产化不足登记为有序 TODO，而不是只保留在聊天结论中。2026-05-04: 更新口径，原因：owner 明确当前系统不需要实际触发交易，仅作趋势判断；近期优先级从交易生产化改为趋势判断可靠性和报告边界。|
|TREND-001|趋势判断/产品边界|P0|DONE|系统实现|已落实“当前系统仅作趋势判断，不触发交易”的结论边界；详细拆解见 `docs/requirements/trend_judgment_scope_2026-05-04.md`|日报和回测即使数据质量全绿，也不能把默认成功态写成交易触发、账户目标仓位或“可作为仓位复核依据”；报告必须明确趋势判断范围、`production_effect=none` 和投资姿态标签边界；实现时同步更新 `docs/system_flow.md` 并补充报告边界测试|2026-05-04: 新增并进入实现，原因：owner 明确当前系统只做趋势判断；该边界影响投资解释，属于 P0 误读风险控制。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `trend_only` 结论等级和 `trend_judgment` 适用范围，`score-daily` 与回测均按趋势判断范围渲染，测试覆盖日报、回测和干净通过时不升级为仓位复核。|
|REPORT-001|报告/审计|P0|DONE|系统实现|已按 `docs/requirements/report_traceability.md` 第一版实现日报/回测 evidence bundle、run manifest、dataset/quality refs 和 `aits trace lookup` 反查入口|最终报告的每个核心结论都有稳定 `claim`、`evidence`、`dataset`、`quality` 引用；evidence bundle 记录 signal、配置快照、ticker、日期窗口、`ai_after_chatgpt` regime、数据质量门状态和数据集 provider/row count/checksum；可从报告反查原始或标准化数据快照；实现时同步更新 `docs/system_flow.md` 并补充测试或示例报告|2026-05-04: 从 READY 改为 IN_PROGRESS，原因：创建需求拆解文档并开始实现第一版报告可追溯链路。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：日报和回测已生成 trace bundle 并注入可追溯引用章节，`aits trace lookup` 可反查 claim/evidence/dataset/quality/run id；`python -m pytest -q` 通过 203 项测试。|
|REPORT-002|报告/执行解释|P1|DONE|系统实现|设计并实现日报/最终报告的变化原因树和“什么情况会改变判断”章节；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#report-002`|报告明确展示本期仓位区间变化、分模块分数变化、thesis/风险事件状态、最终动作约束和加/减仓触发条件；能区分市场短期波动、thesis 承压、thesis 证伪、估值过高但基本面未坏、基本面恶化；实现时同步更新 `docs/system_flow.md` 并补充报告快照或文本测试|2026-05-04: 已完成首版实现，`scores_daily.csv` overall 行保存模型/最终/置信度调整仓位区间，日报新增变化原因树、分模块分数变化、最终动作约束和判断改变条件；来自 2026-05-04 报告解释优化讨论；与 `REPORT-001`、`SCORE-002`、`THESIS-001` 协同|
|REPORT-003|报告/呈现结构|P1|BASELINE_DONE|系统实现|已完成第一阶段日报“今日结论卡”和决策链入口：开头输出状态标签、市场吸引力、判断置信度、评分映射仓位、风险闸门后最终仓位、执行动作、主结论、三个核心原因、最大限制和下一步触发条件；剩余缺口是统一所有报告模板、状态标签配置化和核心原因排序策略；详细拆解见 `docs/requirements/report_decision_chain_presentation_2026-05-04.md#report-003`|日报开头输出结论卡，按“状态标签、市场吸引力、判断置信度、评分映射仓位、风险闸门后最终仓位、执行动作、一句话主结论、三个核心原因、一个最大限制、一个下一步触发条件”组织；报告章节顺序从“状态 -> 原因 -> 约束 -> 行动 -> 证据下钻”展开，评分明细后置；每个核心输出先写判断句再给数字、证据和限制；实现时同步更新 `docs/system_flow.md` 并补充日报快照或文本顺序测试|来自 2026-05-04 最终报告呈现讨论。该任务不新增评分信息，而是重排 `REPORT-001/002`、`SCORE-001/002`、`THESIS-001`、`COGNITION-001` 和 `position_gate` 已有输出，避免报告退化为模块结果堆叠。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现日报“今日结论卡”、核心原因/限制/触发条件和文本顺序测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：`score-daily` 日报开头已生成结论卡，执行动作接入 `execution_policy` 输出，测试覆盖结论卡位于变化原因树、数据门禁和模块评分之前；`production scoring`、`position_gate` 和 evidence bundle 语义未改变。|
|REPORT-004|报告/周期复盘|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成周报/月报投资复盘 CLI：读取 `scores_daily.csv`、decision snapshots、belief_state、outcomes、learning queue 和 rule experiments，输出人类投资复核模板，并显式声明 `production_effect=none`；剩余缺口是 catalyst calendar 自动接入、完整产业链节点健康度、真实 outcome/learning/rule experiment 数据积累和更严格的样本充分性校准；详细拆解见 `docs/requirements/report_decision_chain_presentation_2026-05-04.md#report-004`|周报回答本周结论/仓位是否变化、改变判断的前三个证据、产业链节点热度/健康度变化、thesis/risk 变化、系统判断是否被市场验证和下周观察事件；月报回答系统判断表现、高分信号有效性、gate 是否过严或过松、thesis warning 是否有提前量、哪些数据源/模块影响置信度和候选规则是否需要调整；报告声明 `ai_after_chatgpt` regime 和实际日期范围，保留 trace 下钻；实现时同步更新 `docs/system_flow.md` 并补充报告文本测试|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现面向投资复核者的周/月复盘报告入口，区别于 `feedback loop-review` 的系统运营视角。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits reports investment-review`、周/月 Markdown 输出、trace 下钻、outcome/learning/rule experiment 覆盖摘要和文本测试；真实本地运行周报/月报均为 `PASS_WITH_LIMITATIONS`，原因是 decision outcomes、learning queue 和 rule experiments 尚未形成有效样本。|
|SCORE-001|评分/仓位|P0|DONE|系统实现|已实现独立 `position_gate` 层，把评分仓位、风险事件、估值拥挤、thesis 状态、数据置信度和组合限制取最严格上限|`score-daily` 和 `backtest` 使用同一最终仓位约束；日报说明每个 gate 的来源、上限和是否触发；测试覆盖 L2/L3 风险压低仓位但不只依赖总分|2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现共享仓位硬闸门并接入日报、回测、文档和测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：`score-daily` 与 `backtest` 已共用最终仓位闸门；日报和回测报告输出 gate 摘要；`python -m pytest -q` 通过 203 项测试。|
|SCORE-002|评分/报告|P1|DONE|系统实现|已实现 AI 产业链评分与判断置信度拆分；日报、`scores_daily.csv`、decision snapshot、回测每日明细和回测报告均输出 confidence|`score-daily` 日报顶部同时输出 AI 产业链评分、判断置信度、模型评分仓位、置信度调整后建议仓位和限制原因；`scores_daily.csv` 或等价结构化输出可追踪整体置信度和主要扣减原因；回测每日明细和审计报告能按置信度分桶检查低置信度结论；实现时同步更新 `docs/system_flow.md` 并补充评分/报告/回测测试|来自 2026-05-04 评分解释讨论。目标是避免用户把 72/100 之类的市场分数误读为同等可靠的交易结论；低置信度不直接否定机会，但必须限制结论解释和仓位使用。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现评分/置信度拆分、日报/CSV/回测输出和系统不变量测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：已完成组件级和整体置信度、日报/CSV/快照/回测输出、文档和测试；`python -m pytest -q` 通过 220 项测试。|
|SCORE-003|仓位/风险预算|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成可审计风险预算基础版：保留总分生成基础仓位区间，再由市场压力、真实组合单票/节点/相关性簇集中度、ETF beta 覆盖和已有估值/风险/thesis gate 约束最终建议区间；剩余缺口是真实持仓、参数 replay/shadow、流动性/税费/完整相关性预算和 `GOV-001` 批准；详细拆解见 `docs/requirements/risk_budget_model_2026-05-04.md`|形成风险预算设计文档或配置方案，明确哪些指标作为硬上限、哪些作为禁止加仓条件、哪些只进入报告解释；`score-daily` 和 `backtest` 输出模型基础区间、风险预算后建议区间、总资产折算、主要限制原因和不得加仓条件；回测覆盖同分不同风险状态下最终仓位不同；实现时同步更新 `docs/system_flow.md`|来自 2026-05-04 仓位映射优化讨论。该任务应和 `SCORE-001` 的 `position_gate`、`VALUATION-001` 的估值拥挤 gate、`MACRO-001` 的动态风险资产预算协同推进；避免直接引入不可审计黑箱优化器。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：`PORTFOLIO-002` 已完成基础暴露输入，开始实现第一阶段 `risk_budget` position gate、配置 schema、日报/回测输出和测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/portfolio.yaml:risk_budget`、共享 `risk_budget` gate、日报真实持仓集中度约束、回测市场压力约束、执行纪律禁止主动加仓语义、系统流图和测试；完整 `DONE` 仍需真实持仓和长期验证。|
|COGNITION-001|认知模型/belief_state|P1|DONE|系统实现|建立只读 `belief_state` 认知状态层；详细拆解见 `docs/requirements/cognitive_model_2026-05-04.md#cognition-001`|每次 `score-daily` 通过质量门禁后生成机器可读 `belief_state`，结构化表达市场状态、产业链节点状态、估值状态、风险状态、thesis 状态、仓位边界、限制因素和多维置信度；核心判断必须引用数据质量、证据或人工复核来源；日报输出中文认知状态摘要；第一版不得改变正式评分、`position_gate`、回测仓位或交易建议；实现时同步更新 `docs/system_flow.md` 并补充 schema、报告、trace 引用和“belief_state 不直接改仓位”的测试|2026-05-04: 已完成首版实现，`score-daily` 写入 `belief_state` JSON 和历史索引，日报输出认知状态摘要，decision snapshot 写入 `belief_state_ref`，evidence bundle 增加 `belief_state` dataset/claim 引用，并测试只读状态不改变仓位；原因：将系统长期目标从评分器升级为可审计认知模型，需要显式中间状态承接 evidence、thesis、risk、valuation、industry chain、confidence、position boundary 和后续校准；与 `EVIDENCE-001`、`CHAIN-001`、`FEEDBACK-001/002`、`CAUSE-001`、`GOV-001` 协同|
|CHAIN-001|产业链/报告解释|P1|BASELINE_DONE|系统实现|已完成阶段 3 回测历史节点状态追踪：`backtest_daily_*.csv` 写入 top 节点、热度、健康等级和节点覆盖缺口，回测报告输出“产业链节点历史状态摘要”；剩余缺口是阶段 4 仓位约束评估，需先经过真实数据观察、回测和规则治理；详细拆解见 `docs/requirements/industry_chain_node_state.md`|`score-daily` 报告按产业链节点展示热度、健康度、覆盖率、集中度和解释；能区分行情是 GPU/HBM 等局部节点交易，还是完整产业链扩散；健康度只使用已通过门禁的基本面、估值、风险事件和 thesis 输入，数据不足必须显式标记；报告能按“市场价格信号 -> 产业链节点 -> thesis 状态 -> 评分模块 -> 仓位 gate -> 行动影响”解释关键结论；回测或审计能追踪历史节点状态；实现时同步更新 `docs/system_flow.md` 并补充测试|来自 2026-05-04 产业链节点热度/健康度讨论。2026-05-04: 补充最终报告呈现讨论中的产业链因果链模板；第一版定位为解释和诊断层，不直接触发交易；后续可与 `SCORE-001`/`SCORE-003` 协同进入仓位限制。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段节点热度基础版、日报章节和文本/覆盖测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `industry_node_state` 模块，`score-daily` 日报输出产业链节点热度、覆盖率、集中度和主要贡献 ticker；测试覆盖集中/扩散节点和缺失 ticker 覆盖；`production_effect=none` 防止节点热度改变 scoring、`position_gate` 或执行建议。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续实现第二阶段节点健康度基础版，要求保持只读解释层并显式标记基本面、估值、风险和 thesis 覆盖缺口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：日报已接入节点健康度基础版，明确估值拥挤和风险事件不是基本面证伪，并保持 `production_effect=none`；剩余缺口转入历史节点状态追踪和仓位约束评估。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：开始实现阶段 3 回测历史节点状态追踪和报告摘要。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：回测每日明细和报告已可按 `signal_date` 追踪产业链节点热度/健康度，测试覆盖 CSV 字段和报告摘要；`production_effect=none`，未改变评分、仓位闸门或回测仓位。|
|EVIDENCE-001|信息输入/证据账本|P1|DONE|系统实现 + 项目 owner|已实现 `market_evidence` schema、YAML/CSV 导入、校验报告、CLI 和 LLM/public_convenience 隔离|新市场信息进入系统前都有结构化证据记录、来源等级、采集时间、去重键、影响对象和人工复核状态；报告能区分已确认证据、待复核证据和不能进入自动评分的低可信证据；证据可追溯到 risk event、thesis、valuation、catalyst、industry_chain_node 或日报 claim；实现时同步更新 `docs/system_flow.md` 并补充 schema、去重、来源等级和 LLM 隔离测试|该任务补足“新信息进入闭环”的入口；与 `LLM-001` 边界明确：LLM 只能辅助分类和写入待复核证据，不得直接改评分或仓位。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现 market_evidence schema、CSV/YAML 导入、校验、CLI 和 LLM 隔离测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：证据账本、导入、校验、报告、系统流图和 LLM 隔离测试已完成；`python -m pytest -q` 通过 220 项测试。|
|CAUSE-001|决策因果链/审计|P1|DONE|系统实现|已建立 `decision_causal_chain` ledger、构建命令和查询入口，把 evidence、模块变化、gate、decision snapshot、quality、outcome 和预留 rule candidate 串起来；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#cause-001`|每个核心仓位变化或结论降级都能对应至少一个 `chain_id`；因果链能连接 evidence、module score/confidence 变化、gate、decision snapshot、outcome 和后续规则复核；不允许用未来 outcome 改写 signal_date 当时的 causal chain，只能追加后验观察字段；实现时同步更新 `docs/system_flow.md` 并补充因果链和未来函数防护测试|2026-05-04: 开始实现 `decision_causal_chain` ledger、CLI 构建/查询入口和未来 outcome 隔离测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `aits feedback build-causal-chain`、`aits feedback lookup-chain`、`data/processed/decision_causal_chains.json` schema、中文因果链报告、系统流图和测试；后续 `linked_rule_candidate` 由 `LEARNING-001/EXPERIMENT-001` 回填。|
|LEARNING-001|反馈闭环/错误归因|P1|DONE|系统实现 + 项目 owner|已建立结果归因、错误分类与学习队列；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#learning-001`|校准报告和重大偏差样本能生成 review queue，记录 `data_issue`、`rule_issue`、`thesis_issue`、`execution_issue`、`universe_or_proxy_issue`、`exogenous_unforecastable`、`sample_limited` 等归因分类、证据、owner、下一步和是否需要规则候选；成功样本也能归因；样本不足不得强行生成规则修改建议；实现时同步更新 `docs/system_flow.md` 并补充归因分类测试|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：`FEEDBACK-002` 和 `CAUSE-001` 已具备基础产物，开始实现 learning queue、归因分类、CLI 和样本不足防护测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `decision_learning_queue`、`aits feedback build-learning-queue`、`aits feedback lookup-learning`、中文报告、系统流图和测试；样本不足项不生成规则候选，规则候选仍需 `EXPERIMENT-001/GOV-001`。|
|EXPERIMENT-001|规则实验/回放验证|P1|BASELINE_DONE|系统实现|已完成第一阶段 rule experiment ledger：从 learning queue 生成 candidate-only 规则实验、replay/shadow 计划、报告和查询入口，并接入 loop-review；剩余缺口是真实 historical replay runner、前向 shadow 观测记录和 GOV-001 rule card 批准流程|每个候选规则都有触发原因、关联 causal chain、历史 replay、前向 shadow 计划、样本限制说明、风险和回滚条件；报告比较 production vs candidate 在收益、回撤、换手、置信度、触发 gate、结论等级和失败样本上的差异；候选规则未批准前不得影响正式评分、仓位或日报结论；实现时同步更新 `docs/system_flow.md` 并补充 candidate isolation 和 replay 测试|与 `GOV-001` 协同：该任务负责验证候选规则，`GOV-001` 负责规则生命周期、批准和版本治理。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 rule experiment ledger，把 learning queue 中需要规则候选的复核项接入 replay/shadow 计划和生产隔离报告。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits feedback build-rule-experiments`、`aits feedback lookup-rule-experiment`、`data/processed/rule_experiments.json` schema、中文实验台账报告、loop-review 规则候选接入和测试；`production_effect=none` 防止候选规则影响生产评分/仓位/日报；`python -m ruff check src tests`、`python -m pytest -q` 通过。|
|LOOP-001|反馈闭环/周期复核|P1|DONE|系统实现|已建立闭环复核编排与周期报告；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#loop-001`|生成闭环复核报告，覆盖 new evidence、decision snapshots、due outcomes、calibration findings、causal chains、learning queue、rule candidates、blocked owner inputs 和 task register 状态变化；报告声明市场阶段，默认以 `ai_after_chatgpt` 作为主要结论窗口；明确哪些结论可执行、哪些仅研究、哪些因数据或样本不足降级；实现时同步更新 `docs/system_flow.md` 并补充周期报告测试|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：反馈产物已具备，开始实现 `aits feedback loop-review` 周期复核报告，把证据、快照、outcome、因果链、学习队列和 task register 状态串成运营入口。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `feedback_loop_review`、`aits feedback loop-review`、中文周期复核报告、系统流图和测试；rule candidate 暂标 `NOT_CONNECTED` 等待 `EXPERIMENT-001/GOV-001`。|
|FEEDBACK-001|反馈闭环/决策记录|P1|DONE|系统实现|已实现 `decision_snapshot`，`score-daily` 通过数据质量门禁后按 signal_date 覆盖写入确定路径，并保存 trace 关联|每次评分都保存 market regime、实际请求日期、总分、模块分、置信度、模型仓位区间、最终仓位区间、触发 gate、thesis 状态、风险事件、估值状态、主要解释因子、配置版本、数据质量引用和 evidence bundle 引用；重复运行同一 signal_date 具备幂等策略；实现时同步更新 `docs/system_flow.md` 并补充快照测试|来自 2026-05-04 反馈闭环强化讨论；是 `FEEDBACK-002`、规则校准和后续执行复盘的前置条件。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现 score-daily 决策快照写入、trace 关联和幂等测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：score-daily 已写入包含评分、置信度、仓位、gate、质量、估值、风险和 trace 引用的 JSON 快照；`python -m pytest -q` 通过 220 项测试。|
|FEEDBACK-002|反馈闭环/评分校准|P1|DONE|系统实现|基于 `decision_snapshot` 生成 1D/5D/20D/60D/120D 结果观察和评分校准报告；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#feedback-002`|输出 `decision_outcomes` 和校准报告，按总分分桶、置信度分桶、gate 状态、thesis 状态、风险等级和估值状态统计未来收益、回撤、波动、胜率和相对 SPY/QQQ/SMH/SOXX 或 AI proxy 的超额收益；报告声明样本数量、市场阶段、数据质量状态和样本不足限制；实现时同步更新 `docs/system_flow.md` 并补充 outcome 计算测试|2026-05-04: 已完成首版实现，新增 `aits feedback calibrate`，复用数据质量门禁，从本地 decision snapshot 与 prices_daily 生成 outcome CSV 和中文校准报告；支持总分/置信度/gate/thesis/风险/估值分桶，并明确样本不足、窗口重叠和不得自动修改生产规则。|
|UNIVERSE-001|观察池/回测正确性|P0|DONE|系统实现|已建立 point-in-time watchlist lifecycle、校验命令和回测 signal_date 过滤|每个进入评分、回测或报告的 ticker 都有 `added_at`、`removed_at`、`reason`、`active_from`、`active_until`、`competence_status`、`node_mapping_valid_from` 和 thesis 要求可见日期；回测只能使用 signal_date 当时已进入观察池且映射有效的标的；实现时同步更新 `docs/system_flow.md` 并补充防幸存者偏差测试|优先级列为 P0，原因：扩展 universe 后如果没有 point-in-time 生命周期，回测会产生幸存者偏差并误导投资解释。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现 watchlist lifecycle schema、校验、回测按 signal_date 过滤和防幸存者偏差测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：默认 lifecycle、`aits watchlist validate-lifecycle`、回测过滤、系统流图和防幸存者偏差测试已完成；`python -m pytest -q` 通过 220 项测试。|
|DATA-002|数据源/质量|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成低成本初版：基于现有数据源目录和 download manifest 建立 provider health score、cache/manifest 检查和 reconciliation 覆盖报告；趋势判断下一步是补强 `market_prices` 和 `macro_rates` 第二 qualified source；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#data-002`|初版可验收为 `BASELINE_DONE`：对价格、估值、财报和宏观数据输出 provider 新鲜度、字段漂移、row count 异常、checksum 变化、ticker alias 风险和可用来源差异；跨供应商冲突进入调查项而不是自动平滑；无合格第二来源的域必须标记 reconciliation 未覆盖；严重问题能停止下游或降级结论；完整 `DONE` 还要求市场价格和宏观利率明确第二 qualified source、授权/成本/限制、缓存审计字段和 checksum；实现时同步更新 `docs/system_flow.md` 并补充数据源健康测试|2026-05-04: 从 BLOCKED_OWNER_INPUT 改为 READY，原因：owner 确认先搭低成本框架版，后续再按需订阅 Intrinio、Sharadar、Databento 等生产级来源。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现 `aits data-sources health`、provider health score、cache/manifest 检查和 reconciliation 未覆盖声明。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增数据源健康报告、checksum mismatch 失败、cache/manifest/freshness 检查、qualified source 覆盖表和测试。2026-05-04: owner 已提供并验证低成本初版权限：SEC User-Agent、FMP、FRED、Tiingo EOD 和 EODHD Fundamentals 可访问；EODHD EOD 价格未订阅，价格核验使用 Tiingo。完整 `DONE` 仍依赖生产级第二来源、商业授权/再分发限制和长期口径策略。2026-05-04: 更新趋势判断口径，原因：owner 明确当前系统只做趋势判断，`market_prices` 与 `macro_rates` 的 reconciliation 缺口仍直接影响趋势可信度，继续作为 P1 后续推进。|
|DATA-005|数据源/Marketstack 行情第二来源|P1|BASELINE_DONE|系统实现|owner 已订阅 Marketstack Basic 并配置 `MARKETSTACK_API_KEY`；低成本正式接入已完成，详细记录见 `docs/requirements/marketstack_second_price_source_2026-05-05.md`；完整生产级覆盖仍受 Marketstack Basic `^VIX` 缺失和部分 ETF adjusted close 分红复权口径限制|`aits download-data` 写入独立 `data/raw/prices_marketstack_daily.csv` 与 manifest 记录；`aits validate-data` 校验 Marketstack schema/覆盖/新鲜度/重复键/异常值，并比较主缓存与第二来源 raw `close` 价差和 adjusted close 口径差异；`data_sources health` 中 `market_prices` 在 Yahoo + Marketstack 低成本组合下显示 `COVERED_BASELINE`；报告明确 Marketstack 只做第二来源核验，不自动修正主价格；API key 不进入报告、manifest 或错误输出|2026-05-05: 新增并进入实现，原因：owner 已购买 Marketstack Basic，希望正式接入项目；Marketstack Basic 不覆盖 `^VIX` 等所有 Yahoo 宏观代理，实施中必须显式记录覆盖限制，不能伪造第二来源。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：已实现 provider、独立缓存、manifest、质量门禁、数据源目录、系统流图、README 和测试；真实烟测下载成功，`validate-data` 为 `PASS_WITH_WARNINGS`，警告来自 ETF adjusted close 分红复权口径差异，raw close 核验通过。|
|DATA-006|数据源/广义美元指数|P0|DONE|系统实现|已用 FRED `DTWEXBGS` 替换生产路径中的 Yahoo `DX-Y.NYB`；详细记录见 `docs/requirements/broad_usd_index_replacement_2026-05-05.md`|验收完成：`configured_price_tickers` 不再包含 `DX-Y.NYB`；`configured_rate_series` 包含 `DTWEXBGS`；`aits download-data` 已将 `DTWEXBGS` 写入 `rates_daily.csv` 和 manifest；`validate-data` 对 `DTWEXBGS` 使用 series 级数值范围和单日变化阈值，不再因 Yahoo `DX-Y.NYB` OHLC 异常阻断；`score-daily` 宏观流动性评分和 `macro_risk_asset_budget` 使用 `DTWEXBGS.return_20d`；报告和文档明确这是广义美元指数，不是 ICE DXY；已同步更新 `docs/system_flow.md` 和测试|2026-05-05: 新增并进入实现，原因：Yahoo `DX-Y.NYB` 在数据质量门禁中出现 OHLC 异常，ICE DXY 商务接入较慢；owner 批准使用 FRED 官方 `DTWEXBGS` 作为可审计、低成本、daily 的广义美元指数生产输入。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：配置、下载、质量门禁、特征、评分规则、宏观预算、文档和测试已切换到 `DTWEXBGS`；真实 `validate-data` 不再出现 `DX-Y.NYB`，但仍因 SPY/Yahoo OHLC、SOXX 异常和 Yahoo/Marketstack GOOG/SMH 口径差异等价格问题失败。|
|DATA-007|数据源/FMP 主价格源|P0|BASELINE_DONE|系统实现 + 项目 owner|已接入 FMP Starter 作为默认股票/ETF 日线价格主源；详细设计和烟测记录见 `docs/requirements/fmp_primary_price_source_2026-05-05.md`；`^VIX` 已由 DATA-008 Cboe 源补入，价格 alias 和二源归因由 DATA-009 继续收敛，第二源阻断策略由 DATA-011 落地|Baseline 完成：`aits download-data` 默认读取 `FMP_API_KEY` 并使用 FMP 写入股票/ETF 主价格；manifest 主价格 source_id 为 `fmp_eod_daily_prices` 且不泄露 API key；Marketstack 继续作为第二行情源；Yahoo 仅保留显式迁移选项，不再默认生产使用；FMP raw OHLC 使用 `non-split-adjusted` endpoint，`adj_close` 使用 `dividend-adjusted` endpoint；价格路径直接请求 `GOOG`，估值/PIT alias 独立维护；已同步更新 `docs/system_flow.md`、数据源目录、README 和测试。剩余限制：需持续用真实 `download-data`/`validate-data` 确认主价格源清洁度；Marketstack 第二源自检异常按 DATA-011 记录为告警，跨源 raw close 未解决冲突仍会阻断下游结论|2026-05-05: 新增并进入实现，原因：Yahoo 主源质量错误阻断日报，owner 已购买 FMP Starter 作为低成本主价格源；`^VIX` 仍需 Cboe 单独接入，FMP 主源不覆盖全系统宏观代理。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：FMP provider、CLI 默认主源、manifest、数据源目录、系统流图、README 和测试已完成；真实烟测下载成功但质量门禁仍失败，不能继续日报评分。2026-05-05: 更新剩余 blocker，原因：DATA-008 已用 Cboe official historical data 补 `^VIX`；DATA-009 已确认此前部分硬错误主要落在 Marketstack 第二源缓存，后续按来源归因继续验证。2026-05-05: 更新限制，原因：DATA-011 owner 已批准第二源自身异常降级为可审计告警，FMP 主源仍是价格权威输入。|
|DATA-008|数据源/Cboe VIX 官方源|P0|BASELINE_DONE|系统实现|已接入 Cboe VIX official historical data，作为内部 ticker `^VIX` 的专用主源；详细边界见 `docs/requirements/cboe_vix_official_price_source_2026-05-05.md`|Baseline 完成：`aits download-data` 在 FMP 股票/ETF 主价格后，如主源缺少 `^VIX`，从 Cboe CSV 补 `^VIX` 并合并写入 `prices_daily.csv`；manifest 单独记录 `cboe_vix_daily_prices` 的 provider、endpoint、请求参数、row count 和 checksum；Cboe schema/空结果/请求失败 fail closed，不允许用 Yahoo 或本地缓存补写；完整 DONE 仍需真实全量下载后确认 `validate-data` 不再出现 `prices_missing_expected_values: ^VIX`|2026-05-05: 新增并进入实现，原因：DATA-007 已接入 FMP 主价格源，但 FMP 不覆盖 `^VIX`，质量门禁仍因缺少 VIX 阻断；owner 要求提交 FMP 后继续接入 VIX。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 Cboe provider、主缓存合并、manifest 记录、数据源目录、系统流图、README 和测试；Cboe live smoke 返回 2 行 `^VIX`，102 项聚焦测试和 Ruff 通过。|
|DATA-009|数据源/价格源不一致归因|P0|BASELINE_DONE|系统实现 + 项目 owner|已拆分 FMP 价格 alias 与估值/PIT alias，并让数据质量报告明确标注问题来源；详细记录见 `docs/requirements/price_source_reconciliation_2026-05-05.md`；下一步等待真实 `download-data` / `validate-data` 观察|Baseline 完成：`FmpPriceProvider` 默认直接请求 `GOOG`，manifest 不再记录 `GOOG -> GOOGL` 的价格 alias；估值/PIT 路径仍保留独立 alias；数据质量报告问题表包含“来源”列，能区分价格主源、第二行情源 Marketstack、跨源核验、FRED 宏观序列和下载审计清单；Marketstack 自身硬错误不再被误读为主源错误；已同步更新 `docs/system_flow.md` 并补充测试。完整 DONE 需下一次真实下载确认主源和第二源质量状态|2026-05-05: 新增并进入实现，原因：owner 要求继续重点关注股价数据不一致；复核显示 `GOOG` 硬差异来自价格路径错误复用估值 alias，SPY/INTC/MSFT/SOXX 硬异常主要落在 Marketstack 第二源缓存，报告缺少来源列导致误读。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：价格路径已直接请求 `GOOG`，质量报告来源列和归因测试已完成；定向 Ruff、87 项相关 pytest 和 `aits data-sources validate` 通过。|
|DATA-010|数据源/一致性研究窗口|P0|BASELINE_DONE|系统实现|已把价格一致性、宏观变化和跨源 reconciliation 的默认检查窗口绑定到 `ai_after_chatgpt` 研究窗口；详细记录见 `docs/requirements/price_source_reconciliation_2026-05-05.md`|Baseline 完成：默认数据质量门禁的价格波动、复权比例、宏观单日变化和主源/二源 consistency 检查只统计 `2022-12-01` 以来样本；早于研究窗口的复权/跨源/宏观变化差异不阻断或提示默认日报；基础 schema、覆盖、新鲜度、重复键、主源硬错误和研究窗口内 raw close 跨源真实冲突仍 fail closed；第二源自身硬错误的默认处理由 DATA-011 降级为告警；报告声明一致性检查窗口；已同步更新 `docs/system_flow.md`、配置和测试。完整 DONE 需后续按真实日报运行观察确认窗口口径足够稳定|2026-05-05: 新增并进入实现，原因：owner 明确验证数据一致性只考虑研究时间跨度，太久之前的不一致可忽略或降低严重等级；该规则影响质量门禁和日报是否可继续，属于 P0 数据解释口径。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：配置、质量报告、价格/宏观窗口过滤和测试已完成；真实 `validate-data` warning 从 8 降到 7，2020 年 DTWEXBGS 旧窗口 warning 已移出，4 个研究窗口内 Marketstack 错误仍 fail closed；`ruff check .` 和 `pytest -q` 356 项通过。2026-05-05: 更新边界，原因：DATA-011 已将第二源自身异常从阻断项改为可审计告警。|
|DATA-011|数据源/第二源阻断策略|P0|BASELINE_DONE|系统实现|owner 已批准 Marketstack 第二行情源自身异常降级策略；详细记录见 `docs/requirements/price_source_reconciliation_2026-05-05.md`|Baseline 完成：Marketstack 第二源 self-check 的 schema/覆盖/新鲜度/非正价格/OHLC/极端 adjusted close 等异常默认记录为“第二行情源 Marketstack”警告，不覆盖、不平滑、不修正 FMP 主价格缓存，且在 FMP 主源通过时不阻断 `score-daily`；配置 `secondary_source_self_check_fail_closed` 可恢复 fail-closed；第二源文件缺失/不可读、主源错误、raw close 跨源未解决冲突和重叠覆盖不足仍按错误阻断；数据质量报告保留 row count、样例、来源和告警状态|2026-05-05: 新增任务，原因：DATA-010 已排除旧历史一致性噪音，但真实 `validate-data` 仍因 2024/2026 Marketstack 第二源自身错误 fail closed；这不是代码 bug，而是数据源治理策略选择。2026-05-05: 从 BLOCKED_OWNER_INPUT 改为 BASELINE_DONE，原因：owner 明确“记录第二源异常，以主数据源为主，不阻塞后续流程”；实现了配置开关、告警降级、报告归因、文档和测试，主源错误与 raw close 跨源冲突仍保持 fail closed。|
|DATA-012|数据源/Yahoo 诊断复查|P1|READY|系统实现|为 Marketstack 明显自身坏行增加显式 Yahoo 诊断性第三来源复查；详细记录见 `docs/requirements/price_source_reconciliation_2026-05-05.md`|实现一个只读诊断入口或报告：仅针对质量报告中 Marketstack 第二源 self-check 异常的 ticker/date 附近样本拉取 Yahoo raw OHLC/close，记录 provider、endpoint、request parameters、download timestamp、row count、checksum、FMP/Marketstack/Yahoo 对比和 `diagnostic only / production_effect=none`；不得写入 `prices_daily.csv` 或 `prices_marketstack_daily.csv`，不得进入评分、仓位闸门或回测真值；Yahoo 失败、空结果或与 FMP 不一致只进入调查报告，不阻断 `score-daily`，也不降低 FMP 主源权威性|2026-05-05: 新增任务，原因：owner 提出当 Marketstack 第二源明显有问题时，可借助 Yahoo 做简单复查；该来源必须保持 public_convenience 诊断定位，不能重新启用为生产主源或形成三源投票自动改价。|
|DATA-013|数据源/SEC 日报指标缓存|P0|DONE|系统实现|已通过正式 CLI 生成并校验 `data/processed/sec_fundamentals_2026-05-05.csv`，`score-daily` 已越过 SEC 指标门禁|当日 `sec_fundamentals_YYYY-MM-DD.csv` 必须来自正式 `aits fundamentals extract-sec-metrics` 路径，先通过 SEC companyfacts 缓存校验，再通过 `aits fundamentals validate-sec-metrics`；覆盖缺口必须如实进入校验报告，不允许补空行或伪造指标；`score-daily` 应能越过 SEC 指标校验进入后续门禁，若后续门禁失败则单独报告|2026-05-05: 新增并进入实现，原因：当前 `score-daily` 已通过市场数据质量门禁，但因 `data/processed/sec_fundamentals_2026-05-05.csv` 不存在、覆盖 0/72 而停止。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：`validate-sec-companyfacts` PASS，`extract-sec-metrics` 生成 66 行并 `PASS_WITH_WARNINGS`，`validate-sec-metrics` 覆盖率 92% 且 0 错误，`score-daily --as-of 2026-05-05` 已完成。|
|OPS-002|数据源健康/Yahoo inactive manifest|P1|DONE|系统实现|已把 inactive/diagnostic-only manifest checksum 漂移降为调查警告，active source checksum mismatch 仍 fail closed|Inactive 或 diagnostic-only 数据源的历史 manifest checksum 与当前共享缓存不一致时，不应让 provider health 进入 FAIL；应记录为 inactive/diagnostic stale manifest warning 或忽略硬错误，同时 active source 的 checksum mismatch 仍必须 fail closed；补充测试、更新系统流图或数据源健康文档，并验证 `aits data-sources health` 不再因 inactive Yahoo 旧 manifest 阻断|2026-05-05: 新增任务，原因：FMP/Cboe 已成为主价格缓存来源后，inactive `yahoo_finance_daily_prices` 的历史 manifest 仍指向同一个 `prices_daily.csv` 并触发 checksum mismatch，造成 data-sources health FAIL；这是 health 语义问题，不应恢复 Yahoo 为主源或改写缓存。2026-05-05: 从 READY 改为 DONE，原因：新增 `inactive_manifest_checksum_mismatch` warning，`tests/test_data_sources.py` 覆盖 inactive 降级和 active 硬失败，`aits data-sources health --as-of 2026-05-05` 已转为 PASS_WITH_WARNINGS、0 错误。|
|DATA-003|数据源/PIT 快照归档|P1|DONE|系统实现 + 项目 owner|阶段 1-5 已完成：通用 raw manifest/校验、FMP forward-only PIT 抓取归档、raw payload、normalized as-of CSV、valuation/revision as-of 查询、pipeline health / alerts，以及 SEC accession-level archive 评估和后续任务登记；详细拆解见 `docs/requirements/pit_snapshot_archive_2026-05-04.md#data-003`|已完成验收：能校验 raw snapshot manifest 必填字段、payload sha256、row count、`available_time`/`ingested_at` 约束、重复快照、PIT 可信度误标和 provider 授权字段；FMP 四类 endpoint 写入 raw archive 和 normalized as-of 输出；valuation/revision 查询走 PIT as-of 索引，自建历史不足 90 天时输出降级原因；运行健康报告/告警能暴露 PIT 缺跑、断更、row count 和 checksum 异常；SEC accession-level 增强已拆为 `DATA-004`|2026-05-04: 新增任务，原因：owner 确认不应为了补齐历史 PIT estimates archive 硬买昂贵数据，改为从当前日期开始积累可审计 as-of 数据层；历史回测不得倒用采集日之后才可见的供应商视图。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：owner 确认优先开发 DATA-003，并要求先把具体任务加入 TODO 后开始逐项实现。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：阶段 1 已实现并通过本地 `aits pit-snapshots build-manifest --as-of 2026-05-05`、全量 pytest 和 Ruff 校验；后续阶段继续保留。2026-05-05: 从 BASELINE_DONE 改回 IN_PROGRESS，原因：继续推进阶段 2 的 FMP forward-only PIT 抓取归档。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：阶段 2 已实现 `fetch-fmp-forward` raw/normalized 归档、manifest 刷新、文档和测试；阶段 3-5 继续保留。2026-05-05: 从 BASELINE_DONE 改回 IN_PROGRESS，原因：继续推进阶段 3 的 valuation/revision as-of 查询接入。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：阶段 3 已实现 PIT normalized as-of loader 并接入 `eps_revision_90d_pct` 计算；阶段 4-5 继续保留。2026-05-05: 从 BASELINE_DONE 改回 IN_PROGRESS，原因：继续推进阶段 4 的 PIT health/alerts 接入。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：阶段 4 已将 PIT manifest、PIT 质量报告和当日 FMP PIT normalized CSV 接入 `aits ops health`，并把缺失、row count、新鲜度和 checksum 异常输出为 `pipeline_health_alerts_YYYY-MM-DD.md` data/system 告警；阶段 5 继续保留。2026-05-05: 从 BASELINE_DONE 改回 IN_PROGRESS，原因：继续推进阶段 5 的 SEC accession-level archive 设计评估。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：阶段 5 已完成设计结论并新增 `DATA-004`；DATA-003 的 forward-only PIT 快照归档闭环完成。|
|DATA-004|数据源/SEC accession archive|P1|BASELINE_DONE|系统实现 + 项目 owner|已实现 baseline：下载 SEC submissions JSON、从 SEC 指标 CSV 生成 accession coverage，并归档 accession directory `index.json`；剩余决策是是否进一步下载 primary document、complete submission text 或全部 exhibit；详细拆解见 `docs/requirements/sec_accession_archive_2026-05-05.md`|Baseline 验收已完成：能下载 active CIK 的 submissions JSON 并写 manifest；能从 SEC 指标 CSV 已使用的 accession 生成 accession coverage report；至少保存 accession directory `index.json` 的 raw payload 路径、sha256、bytes 和下载时间；报告明确 `accepted_time`/`filing_date` 可见性规则和剩余限制；完整 DONE 仍取决于 owner 是否要求归档 primary document、complete submission text 或全部 exhibit|2026-05-05: 从 DATA-003 阶段 5 评估拆出，原因：当前 SEC companyfacts baseline 已保留 `accession_number` 和 `filed_date`，但还不能追溯到 accession 原始 filing 目录、accepted time、primary document 和 raw payload checksum；该能力影响 SEC 基本面下载、指标抽取、验证、报告和回测审计，应作为独立 P1 实施。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始实现 baseline submissions 下载、accession coverage 和 accession directory index 归档。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：已实现 `download-sec-submissions`、`download-sec-filing-archive`、`sec-accession-coverage`、数据源目录、README、系统流图和测试；剩余 raw document/exhibit 归档范围待 owner 决策。|
|SCENARIO-001|风险/压力测试|P1|BASELINE_DONE|系统实现|已完成第一阶段 scenario library registry：覆盖首批 AI 产业链压力场景、节点/ticker/risk event/position gate 映射、观察条件、证据要求和人工复核要求；剩余缺口是日报/回测当前组合暴露、量化冲击参数和历史压力样本接入|每个场景能映射到产业链节点、ticker、thesis、风险事件和仓位 gate；报告输出当前组合在云 CapEx 下修、GPU 毛利压缩、HBM 供给、先进封装、出口管制、地缘、利率、估值压缩、电力限制和 AI 应用需求不及预期等场景下的脆弱点、观察条件和人工复核要求；实现时同步更新 `docs/system_flow.md` 并补充场景映射测试|与 `BACKTEST-001` 互补：回测回答历史表现，场景压力测试回答当前组合在指定冲击下的暴露。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 scenario library registry、节点/ticker/risk event/gate 映射校验、报告和查询入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/scenario_library.yaml`、`scenario_library` 模块、`aits scenarios validate`、`aits scenarios lookup`、中文情景库报告、系统流图和测试；情景库只做压力测试和人工复核提示，不作为概率预测，也不直接修改 production 评分、仓位闸门或回测仓位。|
|CATALYST-001|事件/报告|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段手工/审计 catalyst calendar schema、校验、未来 5/20/60 天分桶报告、`upcoming` 报告和查询入口；剩余缺口是真实事件数据源或 owner 手工维护流程、日报摘要接入和事件后复核队列联动|日报显示未来 5/20/60 天重要财报、指引、宏观、CapEx、芯片发布、监管、出口管制和行业会议等催化剂；重大事件前可触发 `pre_event_review`、禁止主动加仓或结论降级；事件后要求复核相关 thesis、risk event 或 valuation state；实现时同步更新 `docs/system_flow.md` 并补充日历和报告测试|自动化数据源需 owner 确认；第一版可从人工复核日历开始，但必须记录来源和采集时间。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段手工/审计 catalyst calendar schema、校验、未来 5/20/60 天分桶报告和查询入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/catalyst_calendar.yaml`、`catalyst_calendar` 模块、`aits catalysts validate`、`aits catalysts upcoming`、`aits catalysts lookup`、中文日历报告、系统流图和测试；默认日历不编造真实事件日期，当前只提供审计结构和空基线。|
|PORTFOLIO-002|组合风险/解释|P1|BASELINE_DONE|系统实现 + 项目 owner|已建立真实持仓文件驱动的 AI 仓位 ticker、产业链节点、地区、客户链、因子、ETF beta、相关性簇和集中度暴露分解；剩余缺口是真实账户持仓文件、FX/流动性/税费扩展，以及后续 `SCORE-003` 风险预算接入；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#portfolio-002`|日报显示当前 AI 仓位不是只显示百分比，还显示单票集中度、节点暴露、地区暴露、客户链暴露、估值/成长因子、ETF beta、相关性簇和主要拥挤风险；缺少真实持仓输入时明确 `NOT_CONNECTED`；第一版只做解释层，不直接改变仓位；实现时同步更新 `docs/system_flow.md` 并补充暴露计算测试|与 `SCORE-003` 有重叠但不重复；该任务先提供解释和输入数据，后续由 `SCORE-003` 决定是否进入风险预算约束。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：生产就绪复盘确认日报仓位还不是账户级建议，开始实现真实持仓 CSV、只读暴露分解、CLI、日报章节和测试；没有真实持仓时不得用观察池替代。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `portfolio_exposure` 模块、`aits portfolio exposure`、持仓 CSV 模板、日报组合暴露章节、数据源目录、系统流图和测试；`production_effect=none`，不会改变评分、`position_gate`、执行建议或回测仓位。|
|EXEC-001|执行纪律/报告|P1|BASELINE_DONE|系统实现|已完成第一阶段 advisory execution policy：固定动作词表、阈值/低置信度/gate 配置、校验报告、查询入口和日报执行建议章节；剩余缺口是冷却期、连续信号确认、成本、事件窗口和回测执行模拟的完整联动；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#exec-001`|日报输出执行建议章节；再平衡阈值、最小调仓幅度、冷却期、禁止追涨、事件前不新增仓位、连续信号确认和高波动期降低交易频率等规则可配置、可回测、可审计；动作词汇固定为维持、小幅加仓、禁止主动加仓、减仓到目标区间、等待人工复核、观察不形成交易结论等 advisory language；小幅变化不触发交易建议；实现时同步更新 `docs/system_flow.md` 并补充执行纪律测试|系统不是自动交易，但需要避免用户把每日小幅仓位区间变化误当成机械交易指令。2026-05-04: 补充最终报告呈现讨论中的动作语言规范，默认不输出“买入/卖出”式交易指令。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 advisory execution policy、固定动作词表、校验、日报执行建议和查询入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/execution_policy.yaml`、`execution_policy` 模块、`aits execution validate`、`aits execution lookup`、中文校验报告、日报执行建议章节、系统流图和测试；`production_effect=none` 防止执行建议改变 production scoring、`position_gate` 或回测仓位。|
|COST-001|回测/执行成本|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成交易摩擦模型基础版：回测成本拆成 commission、spread、slippage、market impact、tax、FX、financing 和 ETF delay 等显式假设，并写入明细、报告和 trace manifest；剩余缺口是真实成交样本、券商费率/税费规则、容量约束、最小交易单位和成交质量验证；详细拆解见 `docs/requirements/transaction_cost_model_2026-05-04.md`|回测支持 bid-ask spread、滑点、市场冲击、汇率、税费、融资利率、ETF 申赎或成交延迟、最小交易单位；报告显示成本敏感性，run manifest 和 evidence bundle 记录成本假设|当前回测已有基础单边成本和线性滑点；该任务有价值但不应挤占 P1 的 point-in-time、反馈闭环和数据源健康任务。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：生产就绪缺口推进到交易摩擦层，开始实现不依赖外部数据的成本假设拆分、报告和 trace 记录。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 backtest 成本假设拆分、每日成本列、中文报告摘要、trace `cost_assumptions` 和测试；完整 `DONE` 仍需真实成交和券商/税务/容量验证。|
|PROXY-001|回测/基准|P1|BASELINE_DONE|系统实现|已完成第一阶段 benchmark policy registry：登记 SPY/QQQ/SMH/SOXX 解释角色，提供 `validate-benchmark-policy` 校验、报告和 `lookup-benchmark-policy` 查询，并把回测/校准报告接入基准政策解释；剩余缺口是可复算的自定义 AI basket 历史收益、成员 lifecycle 与权重快照|报告说明为什么使用 SPY、QQQ、SMH、SOXX 或自定义 AI basket；回测和校准可区分半导体 beta、纳指 beta、AI 主题 beta 和系统动态仓位价值；自定义 basket 必须 point-in-time 维护；实现时同步更新 `docs/system_flow.md` 并补充基准治理测试|避免把 SMH 这类半导体 ETF 表现误解释成完整 AI 产业链判断；与 `UNIVERSE-001` 协同。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 benchmark policy registry、校验报告、查询入口，并把回测/校准报告接入基准政策解释。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/benchmark_policy.yaml`、`benchmark_policy` 模块、`aits feedback validate-benchmark-policy`、`aits feedback lookup-benchmark-policy`、回测/校准报告基准政策章节、系统流图和测试；当前 planned custom AI basket 只做治理要求，不生成正式 basket return；`python -m ruff check src tests` 和相关 pytest 通过。|
|GOV-001|治理/规则管理|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段 rule card registry：登记现有 production 规则 baseline，提供 `validate-rule-cards` 校验、治理报告和 `lookup-rule-card` 查询；剩余缺口是正式 owner 批准流、候选规则 promotion/retirement 操作，以及 `score-daily`、回测和 decision snapshot 写入 rule version|scoring、position gate、risk event、valuation gate、thesis 状态机等核心规则有 rule card；每次规则变更可追溯到规则版本、适用范围、上线原因、验证报告、已知失败模式、最后复核时间、批准记录和回滚方式；`score-daily`、回测和 decision snapshot 记录 rule version；实现时同步更新 `docs/system_flow.md` 并补充规则版本测试|反馈闭环可提出改进，但不能无审计地自动改生产规则。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 rule card registry，先把已存在 production 规则登记为 baseline rule cards，并提供校验、报告和查询入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/rule_cards.yaml`、`rule_governance` 模块、`aits feedback validate-rule-cards`、`aits feedback lookup-rule-card`、中文治理报告、系统流图和测试；当前 `baseline_recorded` 只表示已把现有 production 行为纳入审计台账，正式 promotion/retirement 和 rule version 注入仍待后续实现；`python -m ruff check src tests`、`python -m pytest -q` 通过。|
|GOV-002|治理/规则版本|P1|BASELINE_DONE|系统实现|已把当前 production rule card 版本注入 `score-daily`、回测、decision snapshot 和 evidence bundle；剩余缺口是正式 owner approval、promotion、retirement 操作和历史 rule registry replay|`score-daily` 和 `backtest` 先校验 rule card registry，失败时停止；trace run manifest 记录适用于本次命令的 production rule_id/version/type/approval/validation/source paths；decision snapshot 保存同一 rule version manifest；报告/系统流图说明这是本次运行使用的当前规则登记，不是历史时点审批证明；补充规则版本和 trace 测试|2026-05-04: 新增并进入实现，原因：生产就绪缺口 #7 已有 rule card registry，但日报/回测产物尚未写入具体 rule version，审计链仍不完整。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 rule version manifest、日报/回测 rule card 校验、trace run manifest 参数、decision snapshot 字段、系统流图和测试；真实日报/短区间回测已验证 rule_versions 写入。正式 approval/promotion/retirement 仍由 `GOV-001` 后续 owner 流程承接。|
|OPS-001|运维/可靠性|P2|BASELINE_DONE|系统实现|已完成第一阶段只读 pipeline health report：检查关键输入/输出 artifact 存在性、mtime、文件大小和排查入口；剩余缺口是结构化 run log、耗时、异常栈、API 错误和自动化调度状态采集；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#ops-001`|每次运行记录成功/失败、耗时、输入文件版本、质量门禁状态、API 错误、缺失数据和报告生成状态；失败时生成明确排查入口和相关日志引用；运行成功不等于投资结论有效|适合在日常自动化运行稳定后推进；当前优先级低于数据质量和投资解释正确性。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段只读 pipeline health 报告，检查关键输入/输出存在性、mtime、文件大小和排查入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `pipeline_health` 模块、`aits ops health` 和 `outputs/reports/pipeline_health_YYYY-MM-DD.md`；报告明确运行健康不等于投资结论有效。|
|ALERT-001|报告/告警|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成只读 data/system 与 investment/risk 告警基础版：`score-daily` 生成结构化告警记录、单独 Markdown 告警报告，并把告警摘要接入日报；剩余缺口是通知渠道、确认/解除流、持久化去重、调度状态和 owner 告警策略；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#alert-001`|告警有等级、来源、触发条件、解除条件、对应 claim/evidence 引用和去重策略；覆盖数据源断更、估值快照过期、risk event 升级、L2/L3 风险触发、thesis 状态恶化、仓位上限大幅下降和未来重大事件；日报输出告警摘要|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：报告、risk event、thesis 和 catalyst 基础触发条件已稳定，开始实现不接通知渠道的本地只读告警基线。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `alerts` 模块、`outputs/reports/alerts_YYYY-MM-DD.md`、日报“告警摘要”、告警 schema/去重/触发/解除字段和测试；真实 2026-05-04 日报告警状态为 `ACTIVE_WARNINGS`，说明数据质量、政策/地缘输入、估值拥挤和 thesis 复核仍需关注。|
|TEST-001|测试/正确性|P1|DONE|系统实现|已建立跨模块 system invariant test suite；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#test-001`|每条核心投资原则都有测试：`watch` 风险事件不直接重扣仓位、`public_convenience` 不自动评分、LLM 输出不直接影响仓位、`position_gate` 取最严格上限、回测不能使用未来估值快照、SEC 指标 `filed_date <= signal_date`、估值采集日之后才可见、数据质量未通过不能输出完整结论；新增关键规则时同步补测试或说明不适用原因|保护系统设计边界，防止后续改动破坏 source policy、point-in-time、gate 优先级、LLM 隔离和 watch/active 区分。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始补充覆盖 source policy、PIT、gate、LLM 隔离和 watch/active 边界的系统不变量测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 watch/active、public_convenience、LLM evidence、watchlist PIT、decision snapshot、confidence、valuation PIT 和 thesis 状态机测试；`python -m pytest -q` 通过 220 项测试。|
|STORAGE-001|数据工程|P3|PROPOSED|系统实现|评估从 CSV 逐步升级到 DuckDB / Parquet 的内部存储方案；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#storage-001`|不破坏现有 CSV 输入输出；内部可用 typed schema 存储；报告和回测仍可追溯 checksum、数据版本和来源；迁移计划包含兼容期、回滚方式和数据验证|当前 CSV 对本地 MVP 仍足够透明；当 schema 演进、join 性能、历史快照或类型约束成为实际瓶颈时再升级|
|UI-001|仪表盘/解释|P2|BASELINE_DONE|系统实现|已完成第一阶段本地静态 evidence-first dashboard：`aits reports dashboard` 读取日报、trace bundle、decision snapshot 和可选 belief_state，生成简约 HTML，把报告结论、论证链、实际输入数据、quality refs 和 trace lookup 连起来；剩余缺口是更完整交互服务、跨日报对比和可配置 reader mode；详细拆解见 `docs/requirements/ui_evidence_dashboard_2026-05-04.md`|第一阶段从日报 Markdown、evidence bundle、decision snapshot 和可选 belief_state 生成只读 HTML；可按快速读者、投资复核者、系统审计者查看结论卡、动作/限制、论证链、gate、thesis/risk/valuation 状态、数据源、原始证据和质量报告；输出声明 `production_effect=none` 且不替代 Markdown 报告的审计责任|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：owner 明确主要使用者是自己，优先关注报告结论与实际输入数据的联系以及论证逻辑，视觉风格保持简约；第一阶段先做本地静态证据下钻页，不引入交互式服务或新投资计算。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits reports dashboard`、`evidence_dashboard` 模块、HTML 输出、系统流图和测试；页面只读展示结论到 evidence/dataset/quality 的链路，未改变评分、仓位、回测或执行建议。|
|SECURITY-001|安全/密钥|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段 secret hygiene 扫描：本地 CLI 扫描配置、文档、报告、manifest 和 trace bundle 中的疑似 secret，并输出脱敏报告；剩余缺口是供应商权限治理、日志脱敏策略、pre-commit/CI 集成和 owner 审批流程；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#security-001`|API key 不进入报告、日志、manifest、trace bundle 或错误输出；支持 secret scan 或等价检查；供应商权限、缓存限制、再分发限制和 LLM 处理限制记录在 data source catalog；新数据源接入必须通过安全与权限检查|当前已有 FMP key 从环境变量读取且不写入报告的基础约束；扩展供应商后需要系统化治理。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 secret hygiene 扫描报告和 CLI，检查配置、文档、报告、manifest、trace bundle 中的疑似 secret。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `secret_hygiene` 模块、`aits security scan-secrets` 和 `outputs/reports/secret_hygiene_YYYY-MM-DD.md`；报告只输出脱敏片段，不输出完整疑似密钥。|
|DOC-001|产品边界/解释|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段 recommendation status taxonomy：日报和回测报告输出结论使用等级、适用范围、降级原因、解除条件、证据引用，并与日报投资姿态标签分开；剩余缺口是 taxonomy 配置化、未来事件窗口和更完整 owner review policy；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#doc-001`|每份日报和回测报告都有结论使用等级：`trend_only`、`actionable`、`review_required`、`research_only`、`data_limited`、`backtest_limited` 或等价分类；当前 `score-daily` 和回测以 `trend_judgment` 范围运行，干净通过时也只能显示趋势判断、不触发交易；同时引入独立投资姿态标签，如积极进攻、中高配、中高配但受限、中性观察、防守降仓、人工复核；低置信度、数据不足、来源不足、回测覆盖不足和事件前状态自动降级，并说明原因、解除条件和证据引用；实现时同步更新 `docs/system_flow.md` 并补充报告测试|与 `SCORE-002` 置信度拆分、`REPORT-002` 解释输出和 `REPORT-003` 决策链呈现协同；减少用户把市场状态、分数、仓位和结论可靠性混为同一个答案的风险。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现日报和回测报告的结论使用等级、投资姿态标签、降级原因和解除条件。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `conclusion_boundary` taxonomy，日报与回测报告均输出“结论使用等级”，覆盖 `review_required`、`data_limited` 和 `backtest_limited` 降级；报告明确结论等级与投资姿态标签不能互相替代。2026-05-04: 补充 `trend_only` 和 `trend_judgment` 适用范围，原因：owner 明确当前系统只做趋势判断，不需要实际触发交易。|
|RISK-001|风险事件|P1|DONE|系统实现|已扩展 `risk_event_occurrence` schema，增加 `evidence_grade`、`severity`、`probability`、`scope`、`time_sensitivity`、`reversibility`、`action_class`|校验报告显示来源可信度和风险本体分层；低可信高严重事件进入人工复核但不自动大幅扣分；高可信高严重事件可触发 position gate|保持向后兼容或提供迁移模板。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始扩展 occurrence schema、导入模板、校验报告和评分资格规则。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：schema、CSV 导入、模板、校验/复核报告、评分资格规则和测试均已完成；`python -m pytest -q` 通过 220 项测试。|
|RISK-002|风险事件|P1|DONE|系统实现|已区分 `watch` 与 `active` 对评分和仓位的影响|`watch` 默认进入报告和人工复核；只有达到证据等级或确认条件后才进入自动扣分或仓位闸门；回测 point-in-time 切片保持一致|当前 `active_or_watch_l2_count` 和 `active_or_watch_l3_count` 会把两类状态合并评分。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始把 watch 发生记录改为默认不可自动评分，只进入报告和人工复核。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：policy/geopolitics 评分和 risk_events gate 只使用可评分 active occurrence，watch 只进入报告/人工复核；`python -m pytest -q` 通过 220 项测试。|
|SOURCE-001|数据源/风险|P1|DONE|系统实现|已按 owner 批准的保守 source policy 落地；详细拆解见 `docs/requirements/source_policy_2026-05-04.md`|形成 source policy：S/A/B/C/D/X 等级；`S/A` 可自动评分和触发仓位闸门，`B` 仅在确认后进入普通评分，`C/D/X` 只能 report/manual review；`public_convenience` 与 `llm_extracted` 不得直接评分；更新数据源目录、导入校验、风险事件评分资格和系统流图|2026-05-04: 从 BLOCKED_OWNER_INPUT 改为 IN_PROGRESS，原因：owner 批准先按保守方案落地 source policy。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `source_policy` helper，market evidence 和 risk event 校验报告已区分普通评分/仓位闸门资格，风险事件 position gate 只读取 `S/A` active 且 `position_gate_eligible` 的记录，测试覆盖 B 级 score-only、C/D/X 报告复核、public_convenience/LLM 隔离和 CSV 缺省 action class 保守行为；`python -m pytest -q` 通过 239 项测试，`python -m ruff check .` 通过。|
|RISK-003|风险事件数据源|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成正式流程基础版：`SOURCE-001` 固化来源分层，`RISK-004` 提供 OpenAI 结构化预审导入、live API 整理和待人工复核队列，`RISK-005` 提供每日复核声明链路，正式 occurrence 校验要求 reviewer/reviewed_at/review_decision/rationale/next_review_due；剩余缺口是真实授权来源样本、provider 级外部 LLM 授权记录、风险事件生产样本验证和 owner 每日复核运行纪律；详细流程见 `docs/requirements/risk_event_review_workflow_2026-05-04.md`|风险事件发生记录能从更可信来源或结构化人工流程进入；报告记录 provider、endpoint/来源、captured_at、row count/checksum 或人工复核人；复核流程明确 owner、频率、SLA、升级标准、双确认条件和无法确认时的保守降级规则|2026-05-04: 从 BLOCKED_OWNER_INPUT 改为 READY，原因：owner 确认采用“官方/一手来源 + OpenAI API 预审 + 结构化人工复核”的保守流程；实际实施拆解由 `RISK-004` 承接。2026-05-04: 从 READY 改为 BASELINE_DONE，原因：`SOURCE-001/RISK-004/RISK-005` 已完成来源分层、预审隔离、人工确认元数据、复核声明、日报识别、回测 PIT 切片和系统流图；完整生产使用仍依赖真实授权来源样本、API key、provider 级外部 LLM 授权记录和 owner 运行纪律。2026-05-04: 更新剩余缺口，原因：`RISK-004` 已补风险事件专用 live OpenAI 预审入口，生产级完成仍需真实授权样本和 owner 运行纪律。|
|RISK-004|风险事件/OpenAI 预审|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第二阶段 live OpenAI Responses API 风险事件预审入口：`aits risk-events precheck-openai` 复用 provider LLM 权限 fail closed、固定结构化输出和审计字段，并把风险事件候选直接写入 `risk_event_prereview_queue.json`；真实授权来源生产样本验证仍依赖 owner 批准样本|API 输出只能转换成 `llm_extracted` / `pending_review` 风险事件候选，保存 model、reasoning effort、prompt version、source URL、request id/response id、输入/输出 checksum、source permission、候选 risk_id、ticker/节点映射和人工复核问题；不得直接评分、触发仓位闸门或自动写成 L2/L3 active；人工确认后的正式 occurrence 仍必须包含 reviewer、reviewed_at、review_decision、rationale、next_review_due；同步更新 `docs/system_flow.md`、示例输入和隔离测试|来自 2026-05-04 RISK-003 流程讨论；付费新闻或供应商内容只有在授权允许外部 LLM API 处理时才可作为 OpenAI 输入。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现第一阶段结构化预审导入、待人工复核队列、报告和隔离测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits risk-events import-prereview-csv`、`data/processed/risk_event_prereview_queue.json` schema、中文导入报告、预审模板、正式 occurrence 人工复核元数据校验和测试；`python -m ruff check src tests`、`python -m pytest -q` 通过。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：owner 批准顺着 OpenAI API 整理风险事件思路继续推进，开始补风险事件专用 live API 预审入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits risk-events precheck-openai`、live API 到风险事件待复核队列转换、source permission 审计、中文报告、示例输入、系统流图和隔离测试；`.venv\\Scripts\\python.exe -m pytest -q` 通过 311 项测试，ruff 定向检查通过。|
|RISK-005|风险事件/每日复核声明|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段“已复核且未发现未记录重大事件”的结构化声明链路：支持 `review_attestation` YAML、`aits risk-events record-review-attestation`、校验报告、日报政策/地缘评分识别和 point-in-time 回测切片；剩余缺口是真实每日复核 owner、来源清单和运行纪律；详细流程见 `docs/requirements/risk_event_review_workflow_2026-05-04.md#risk-005`|支持人工复核声明 YAML 和 CLI 写入；校验报告显示复核声明数量、当前有效声明数量、覆盖窗口、复核人、来源范围和过期状态；只有覆盖 `as_of`、复核结论明确、未过期且来源范围合格的声明，才允许政策/地缘模块把无可评分 active 风险事件作为经复核输入，而不是 `insufficient_data`；日报和系统流图说明该声明不是自动风险消除证明；补充测试覆盖有效声明、过期声明、日报评分和 gate 隔离|2026-05-04: 新增并进入实现，原因：2026-05-04 日报显示风险事件发生记录目录缺失导致政策/地缘模块降级；该项可独立补工程链路，但真实复核内容仍需 owner 填写，不由系统伪造。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增复核声明 schema/loader/writer、CLI 写入、发生记录校验报告展示、日报政策/地缘模块接入、历史切片、数据源目录、系统流图和测试；系统没有自动生成任何真实无风险声明。|
|LLM-001|新闻/LLM|P1|BASELINE_DONE|系统实现 + 项目 owner|已把 owner 选择的 OpenAI 默认请求策略落地为 `gpt-5.5` + `reasoning.effort=high`，并把 reasoning effort 写入请求、报告和待复核队列；剩余依赖仍是真实 owner 批准样本、更多 provider 授权条款覆盖、人工复核 SLA 运行纪律和批量处理评估；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#llm-001`|LLM 输出必须是结构化证据分类结果，并保留原始来源、采集时间、输入/输出 checksum、model、reasoning effort、prompt version、request id 和 provider 授权标记；报告区分 LLM 辅助抽取、人工确认和自动评分输入；没有人工确认或明确证据等级前，LLM 输出不得直接改变仓位建议；付费新闻/供应商内容只有在 provider 级授权允许外部 LLM 处理时才可发送，授权未知时 fail closed；实现时同步更新 `docs/system_flow.md` 并补充防止 LLM 交易建议直连评分和供应商授权绕过的测试|来自 2026-05-04 新闻/LLM 接入边界讨论；明确禁止 LLM 直接输出看多/看空/加仓/减仓作为交易评分。2026-05-04: 从 PROPOSED 改为 READY，原因：owner 确认使用边界、付费/供应商内容处理原则和预审 schema 方案，剩余工作转为实现 provider 授权字段、结构化输出 schema、OpenAI 请求审计和隔离测试。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：owner 已设置 API key，并批准剩余阻塞项按保守默认处理：真实样本先用官方/一手来源和手工摘要，provider 授权未知时 fail closed，LLM 输出只进入待复核队列。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `llm_precheck` 模块、`aits llm precheck-claims`、`data/processed/llm_claim_prereview_queue.json` schema、中文预审报告、provider `llm_permission` 校验、输入模板、系统流图和隔离测试；`python -m pytest -q` 通过 301 项测试，`python -m ruff check .` 通过；尚未用真实 owner 批准样本发起生产预审。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：owner 指定 OpenAI 默认策略升级为 `gpt-5.5-pro` 和 `reasoning.effort=high`，开始补请求 payload、CLI 选项、审计输出、系统流图和测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：`aits llm precheck-claims` 和 `aits risk-events precheck-openai` 默认请求均使用 `gpt-5.5` + `reasoning.effort=high`，队列 schema、报告、数据源目录、示例、系统流图和测试均记录 reasoning effort；`.venv\Scripts\python.exe -m pytest -q` 通过 311 项测试，`ruff check config src tests` 通过。2026-05-05: owner 决策后续默认从 `gpt-5.5-pro` 切换为 `gpt-5.5`，原因：日报前单候选真实 smoke 中 `gpt-5.5-pro` 返回 HTTP 502，而 `gpt-5.5` 可稳定返回；代码常量、文档和示例已同步；不带 `--openai-model` 的单候选日报前预审 smoke 约 96 秒完成，预审 PASS_WITH_WARNINGS，待复核队列 0，日报 PASS_WITH_LIMITATIONS。|
|LLM-002|OpenAI 请求发送链路诊断|P1|BASELINE_DONE|系统实现|已完成生产默认链路修复：OpenAI Responses API 默认 HTTP client 改为 `requests`，保留 `--openai-http-client urllib` 做本机对照；失败/重试报告输出 sanitized diagnostics；所有 5xx（含 HTTP 520）按瞬时错误重试 2 次；剩余增强是独立 dry-run/debug 命令和 SDK/httpx 后续评估；详细拆解见 `docs/requirements/openai_transport_diagnostics_2026-05-05.md`|失败报告或 debug 输出应记录 sanitized attempt 诊断：attempt count、HTTP client、每次 attempt 的 client request id、candidate/precheck id、endpoint host、payload byte size、input checksum、exception type、errno/reason、HTTP status 和 OpenAI x-request-id（如有）；不得输出 API key、Authorization header 或未授权付费全文；单次请求失败仍按 RISK-007 重试 2 次，第 3 次失败整批 fail closed；默认链路必须能用真实 `score-daily` 跑通日报前 OpenAI 预审|2026-05-05: 新增任务，原因：默认 20 条日报前 OpenAI 预审样本出现间歇性 `URLError`；同一失败候选用相同 payload builder 单独重跑约 20 秒成功，说明候选内容和模型判断不是主要 blocker，需先补请求发送链路可观测性和客户端稳定性验证。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：已在 OpenAI 重试层记录每次 attempt 的 sanitized diagnostics，并在 LLM claim/risk event Markdown 报告输出“请求诊断”；验证 `ruff check src tests` 和目标测试 22 passed。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：默认 OpenAI HTTP client 切换为 `requests` 并补 CLI 参数、文档、数据源目录和测试；真实 USTR 单候选 dry-run 约 20 秒 PASS、record_count 0；完整默认 `score-daily --as-of 2026-05-05` 在补齐 HTTP 520 作为 5xx 重试后通过，OpenAI 预审 PASS_WITH_WARNINGS、官方候选 402、上限 20、LLM claim 24、待复核队列 7、L2/L3 候选 6、active 候选 0，日报 PASS_WITH_LIMITATIONS；验证 `ruff check src tests`、目标测试 45 passed、完整 `pytest -q` 369 passed。|
|VALUATION-001|估值/仓位|P1|DONE|系统实现|把估值拥挤从单纯评分升级为新增仓位限制或禁止加仓 gate|估值过热时日报输出“趋势仍强但新增仓位受限”等结论；回测使用同一规则；不把估值拥挤误写成基本面证伪|2026-05-04: 已完成，valuation `position_gate` 在共享 gate 层按 `EXPENSIVE_OR_CROWDED` / `EXTREME_OVERHEATED` 限制最终 AI 仓位，日报和回测共用同一最终仓位约束；补充测试验证估值拥挤 gate 压低仓位且不被误写成 thesis 证伪。|
|VALUATION-002|估值/数据可信度|P1|DONE|系统实现|已把估值输入显式分为真实 point-in-time、采集日快照和回填历史分布，并在估值复核、日报和回测审计中输出可信度等级和限制原因；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#valuation-002`|日报、估值复核和回测审计显示估值分位可信度：高/中/低及原因；严格回测不能使用采集日之后才可见的估值回填数据得出历史时点结论；`eps_revision_90d_pct` 只允许真实可审计历史 analyst 快照支持；实现时同步更新 `docs/system_flow.md` 并补充估值校验和回测切片测试|来自 2026-05-04 估值历史快照伪 point-in-time 风险讨论；与 `BTINPUT-001`、`DATA-001` 协同。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现估值 PIT 可信度字段、FMP historical 低可信标签、报告输出和切片测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：valuation snapshot、CSV/FMP 来源、校验、复核、日报、decision snapshot、回测输出和测试均已覆盖 PIT 可信度；`python -m pytest -q` 通过 220 项测试。|
|BTINPUT-001|回测输入覆盖|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第二阶段 forward-only PIT 覆盖持续验证：新增 `aits backtest-pit-coverage`，按 PIT manifest 汇总自建快照覆盖、历史 C 级原因和可升级为 B/A 级样本的起始日期；历史补数继续暂缓；详细拆解见 `docs/requirements/pit_snapshot_archive_2026-05-04.md#btinput-001`|在提供合格历史估值/风险事件输入后，`backtest_audit_2022-12-01_2026-05-01.md` 中 `valuation` 和 `policy_geopolitics` 覆盖率不再长期为 0%；审计报告能区分“历史确无事件”和“事件库未建设”；估值历史不能用采集日伪造过去可见性；新增或更新导入模板、校验报告和测试|来自 2026-05-04 TSM IR 回填后的下一步建议；当前回测仍显示估值快照和风险事件发生记录全区间为空，限制投资解释力。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现第一阶段历史输入缺口报告，先把可独立完成的补数诊断和模板入口落地。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `backtest.input_gaps` 模块、`aits backtest-input-gaps`、中文缺口报告、系统流图和测试；真实短区间诊断显示 2026-04-01 至 2026-05-01 的估值和风险事件/复核声明缺口均为 21 个 signal_date。2026-05-04: owner 决策不购买或伪造历史 PIT；本任务保留缺口诊断能力，但不继续推进历史 PIT 补数、严格 PIT 估值或历史 occurrence/attestation 补齐；未来自建快照样本由 `DATA-003` 自然积累，历史回测可信度由 `BACKTEST-002` 标注。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：owner 接受不采购低成本 PIT 的策略，开始补 forward-only 覆盖持续验证和升级日期报告。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `backtest.pit_coverage`、`aits backtest-pit-coverage`、中文覆盖报告、系统流图和测试；`ruff check src tests` 和 `.venv\Scripts\python.exe -m pytest -q` 通过 372 项测试。|
|BACKTEST-001|回测/稳健性|P1|DONE|系统实现|已完成回测稳健性报告：覆盖成本压力、起点后移、固定仓位、再平衡频率、趋势信号族基线、模块权重扰动、同换手率随机策略和时间顺序样本外验证；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#backtest-001`|报告声明市场阶段、实际请求日期范围和数据质量状态；所有实验使用相同 point-in-time 输入和交易成本假设，且不引入未来函数；Markdown 和机器可读摘要都能说明动态系统相对固定仓位/买入持有基准的收益、回撤、换手和纪律差异；实现时同步更新 `docs/system_flow.md` 并补充 baseline 和摘要测试|来自 2026-05-04 回测防过拟合讨论；优先默认使用 `ai_after_chatgpt` 窗口。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：`SCORE-003` 和 `COST-001` 已具备基础输入，开始实现第一阶段 backtest robustness report。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `backtest.robustness` 模块、`--robustness-report`/`--robustness-report-path`、中文稳健性报告、系统流图和 CLI 测试；完整 `DONE` 仍需完整扰动/随机/样本外实验。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：开始补第二阶段固定 60% 基线和机器可读稳健性摘要。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增固定 60% 总资产 AI exposure 基线、`--robustness-summary-path` JSON 摘要、系统流图和测试；真实短区间回测已验证摘要生成，固定仓位在短样本中优于动态策略，结论仍需更多稳健性实验。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续按回测开发优先顺序补充再平衡频率稳健性场景，复用同一 PIT 输入、收益生效规则和显式成本假设。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `rebalance_every_5d` 和 `rebalance_every_21d` 稳健性场景，JSON 剩余缺口移除 `rebalance_frequency`；`ruff check src tests` 和 `.venv\Scripts\python.exe -m pytest -q` 通过 371 项测试。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续补趋势-only 与趋势+风险情绪基线，评估复杂基本面/估值模块相对简化信号族的增量。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `trend_only_baseline` 和 `trend_plus_risk_sentiment_baseline` 稳健性场景，JSON 剩余缺口移除趋势基线；`ruff check src tests` 和 `tests/test_backtest.py` 通过。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续补模块权重扰动，使用扰动后的评分规则重新运行同一 PIT 输入，避免近似复用 gate 上限。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增每个评分模块上下扰动场景、`--robustness-weight-perturbation-pct`、权重敏感性解释和 JSON 字段，剩余缺口移除 `module_weight_perturbation`；`ruff check src tests` 和 `tests/test_backtest.py` 通过。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续补同换手率随机策略，判断信号方向是否优于相同交易频率下的随机方向。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增同换手率随机策略、`--robustness-random-seed-start`、`--robustness-random-seed-count`、随机基线解释和 JSON 字段，剩余缺口移除 `same_turnover_random_strategy`；`ruff check src tests` 和 `tests/test_backtest.py` 通过。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续补时间顺序样本外验证，避免只看全样本和单一起点后移。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：新增 `in_sample_window` 和 `out_of_sample_holdout` 场景、`--robustness-oos-split-ratio`、样本外解释和 JSON 字段，`remaining_gaps` 为空；`ruff check src tests` 和 `.venv\Scripts\python.exe -m pytest -q` 通过 371 项测试。|
|BACKTEST-002|回测/数据可信度|P1|DONE|系统实现|已完成回测 A/B/C 数据可信度、vendor historical estimates / self-archived snapshot 声明、核心输入 PIT 覆盖摘要、输入覆盖 CSV PIT 字段和 feature/universe lag sensitivity Markdown + JSON；详细拆解见 `docs/requirements/pit_snapshot_archive_2026-05-04.md#backtest-002`|回测报告显示 `Backtest Data Quality`、`Uses Vendor Historical Estimates`、`Uses Self-Archived Snapshots`、`Minimum Feature Lag`、`Universe PIT`、`Corporate Actions Handling` 和核心输入 `point_in_time_class/backtest_use` 覆盖；支持 0/1/3/5/10/20 交易日 feature-only、universe-only、combined 滞后敏感性；C 级探索性输入明确不得输出无条件 Sharpe 结论；已同步更新 `docs/system_flow.md` 并补充报告、CLI 和审计测试|2026-05-04: 新增任务，原因：自建 PIT 只能改善未来样本，历史回测仍需要显式标注可信度和滞后敏感性，防止把供应商回填历史误读为可交易结论。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：owner 要求按回测相关任务优先顺序继续开发，先实现 A/B/C 数据可信度、核心 PIT 覆盖摘要和 lag sensitivity 报告。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：`aits backtest` 已可输出数据可信度标签、核心 PIT 覆盖和滞后敏感性报告/摘要，C 级报告明确降级 Sharpe 解释；`ruff check src tests` 和 `.venv\Scripts\python.exe -m pytest -q` 通过 371 项测试。|
|PIT-001|特征/PIT 可见时间|P0|BASELINE_DONE|系统实现|已建立 `config/feature_availability.yaml` 和 PIT feature availability 报告；`build-features`、`score-daily`、`backtest` 写入可见时间报告，并把摘要纳入 report trace bundle；剩余缺口是把单个新特征族的真实 `available_time` 字段校验继续下沉到各数据源 schema；详细拆解见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#pit-001全系统-feature-availability-policy`|所有用于评分、回测、校准和日报核心结论的特征族都有明确 availability rule；缺少 `available_time` 或只有 `event_time` 的新特征默认不得进入 A/B 级回测主结论；回测报告和 trace bundle 能显示特征可见时间规则、实际 `feature_as_of` 和数据可信度等级；实现时同步更新 `docs/system_flow.md` 并补充未来快照、未来公告、未来 universe 和未来宏观修订防护测试|2026-05-05: 新增任务，原因：PIT 讨论确认下一阶段应把 `available_time <= decision_time` 从估值/风险/SEC/观察池扩展为全系统特征规则，避免新增特征族重新引入未来函数。2026-05-05: 从 READY 改为 BASELINE_DONE，原因：新增 feature availability catalog、校验/报告模块、CLI 接入、trace 引用和测试；完整 DONE 仍需各数据源级别的可见时间字段强校验。|
|SHADOW-001|模型评估/在线 PIT prediction ledger|P1|BASELINE_DONE|系统实现|已新增 append-only `prediction_ledger.csv` schema、`score-daily` production 写入和 `aits feedback calibrate-predictions` outcome 报告；剩余缺口是把 `EXPERIMENT-001` challenger/shadow 候选自动接入同一 ledger，并让周/月复盘读取 production vs challenger 表现；详细拆解见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#shadow-001在线-pit-prediction--shadow-ledger`|每个 production/challenger prediction 都能追溯到当时可见的数据快照、特征版本、规则版本和质量状态；challenger 行 `production_effect=none`，不得改变正式评分、仓位闸门或日报动作；outcome 追加后不能改写 signal-time 字段；周报/月报能区分 production 真实判断和 challenger shadow 表现；实现时同步更新 `docs/system_flow.md` 并补充 append-only、future outcome isolation 和 challenger isolation 测试|2026-05-05: 新增任务，原因：历史 PIT 不完整时，在线真实 prediction log 是后续模型迭代和晋级最可靠的 PIT 样本；现有 `decision_snapshot` 只覆盖 production 判断，不足以比较多个 challenger。2026-05-05: 从 READY 改为 BASELINE_DONE，原因：生产日报写入 prediction ledger，prediction outcome 校准命令和报告已实现，测试覆盖 append-only、shadow isolation 和 outcome 分桶；完整 DONE 仍需 challenger 自动接入和周期复盘展示。|
|BACKTEST-003|回测/模型晋级门槛|P1|BASELINE_DONE|系统实现 + 项目 owner|已实现模型晋级门槛基础版：`aits backtest --promotion-report` 汇总数据可信度、robustness、lag sensitivity、shadow outcome 和 rule governance，并在回测报告中嵌入晋级状态；剩余缺口是真实 forward shadow 样本和 owner/rule approval 进入 `GOV-001`；详细拆解见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#backtest-003模型晋级门槛与回测路线`|回测或模型评估报告默认声明 `ai_after_chatgpt` regime 和实际日期范围；C 级输入不得输出无条件 Sharpe/CAGR 主结论，也不得生成 promotion 通过状态；晋级状态必须引用具体 evidence、PIT 覆盖、robustness、shadow outcome 和 rule card；实现时同步更新 `docs/system_flow.md` 并补充报告文本和治理隔离测试|2026-05-05: 新增任务，原因：`BACKTEST-001/002` 已完成稳健性和数据可信度标注，但还缺一个可审计的“探索 -> 近似 PIT 回测 -> 前向 shadow -> 晋级审批 -> 生产规则”路线，防止漂亮历史回测被误读为上线证据。2026-05-05: 从 READY 改为 BASELINE_DONE，原因：新增 promotion gate 模块、Markdown/JSON 输出、回测 CLI 接入、主回测报告摘要和测试；完整 DONE 仍需真实 prediction outcome 样本和 owner approval。|
|PIT-002|特征/PIT 字段强校验|P0|BASELINE_DONE|系统实现|已完成字段级 source 检查基础版：`feature_availability` 报告可列出 `available_time` 覆盖率、缺失行、未来可见时间行和保守 fallback；`build-features`、`score-daily`、`backtest` 已对价格/利率输入写入字段级检查；剩余缺口是后续每个新增数据源继续接入专属 `available_time` 字段检查；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|新增或修改的数据源不能只凭 `event_time` 进入 A/B 级回测、日报主结论或 promotion gate；验证报告需要列出每个输入族的 `available_time` 字段覆盖率、缺失处理策略和最早可用时间；测试覆盖未来公告、未来 universe、未来宏观/证据样本不能进入 signal-time 结论|2026-05-05: 新增任务，原因：`PIT-001` 已完成 catalog 和报告基础版，但最终闭环要求数据源级可见时间强校验，避免新特征绕过 `available_time <= decision_time`。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始实现数据源级 `available_time` 字段覆盖、未来可见时间和 report/trace 摘要。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `FeatureAvailabilitySourceCheck`、字段级报告/summary、市场输入 CLI 接入和未来 `filed_date` 防护测试；完整 DONE 仍需随新增数据源继续补专属检查。|
|SHADOW-002|模型评估/challenger shadow runner|P1|BASELINE_DONE|系统实现|已新增 `aits feedback run-shadow`：读取 rule experiment ledger、production decision snapshot 和 trace，追加 challenger prediction 到 append-only ledger，强制 `production_effect=none`；剩余缺口是真实候选规则每日运行调度和长期样本积累；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|`score-daily` 或独立 shadow 命令可同时记录 production 与多个 challenger；challenger 不写正式日报动作、不改变 `scores_daily.csv` production 行、不改变 position gate 或 belief_state；prediction ledger 能按 candidate_id/model_version 区分并追溯输入；测试覆盖 production 隔离、重复 prediction_id 停止和 candidate trace 引用|2026-05-05: 新增任务，原因：`SHADOW-001` 已有 ledger 和 outcome 校准，但还缺真正每天运行 challenger 的入口，这是进入前向 shadow 的下一步。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始实现独立 shadow runner 和 append-only prediction ledger 写入。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：shadow runner、报告和 CLI 测试已完成；完整 DONE 仍需真实每日调度和 outcome 样本。|
|SHADOW-003|模型评估/forward shadow 样本成熟度|P1|BASELINE_DONE|系统实现 + 时间窗口|已新增 `aits feedback shadow-maturity`，按 candidate、horizon、market regime 和 `production_effect` 汇总 available/pending/missing、收益、胜率、回撤和 benchmark excess，并输出 `READY_FOR_SHADOW` / `READY_FOR_GOV_REVIEW`；真实 promotion 结论仍受交易日和 label horizon 限制；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|报告能显示 production vs challenger 可用样本数、胜率、平均收益、最大回撤、benchmark excess return、样本不足原因和预计可评估日期；少于最低样本或 horizon 未完成时 promotion gate 必须保持 `READY_FOR_SHADOW` 或 `MISSING`；测试覆盖样本不足不晋级、pending outcome 不算通过、missing data 降级|2026-05-05: 新增任务，原因：前向 shadow 的最大 blocker 是真实时间和 outcome 样本积累；该任务先建立监控和门槛，让样本成熟前不会误晋级。2026-05-05: 从 BLOCKED_EXTERNAL 改为 IN_PROGRESS，原因：先实现成熟度监控代码；真实 promotion 结论仍受交易日和 label horizon 限制。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：样本成熟度报告、CLI 和样本不足测试已完成；完整 DONE 需要真实 outcome 样本达到门槛。|
|GOV-003|治理/candidate promotion 与 retirement 流程|P1|BASELINE_DONE|系统实现 + 项目 owner|已新增受控 `promote-rule-card` / `retire-rule-card` 工作流：promotion 必须带 owner、批准理由、promotion report 和 outcome 引用；retirement 必须带退役原因；写出后立即复用 rule card validator；真实规则上线仍需要 owner 逐项批准；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|未通过 promotion gate 或缺少 owner approval 的 candidate 不能写入 production rule card；promotion/retirement 操作必须更新 rule card registry、生成治理报告、保留旧版本、写明 rollback condition，并被 `score-daily`/`backtest` trace 捕获；测试覆盖未批准不能 promotion、批准后 production rule version 变更可追溯、retired rule 不再参与生产|2026-05-05: 新增任务，原因：当前 rule card registry 只是 baseline governance；完整路线需要把通过 shadow 的候选规则以 owner 批准方式变成 production rule。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始实现受控 promotion/retirement CLI 和治理报告。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：规则生命周期写入、报告、校验和 promotion/retirement 测试已完成；完整 DONE 取决于真实 owner approval 和通过门槛的候选规则。|
|REPORT-005|报告/production vs challenger 复盘|P2|BASELINE_DONE|系统实现|已把 `prediction_outcomes.csv` 和 shadow maturity 摘要接入 `aits reports investment-review` 与 `aits feedback loop-review`；报告区分 production/challenger、样本成熟度和样本不足边界；剩余缺口是真实 shadow outcome 积累后再校准展示阈值；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|周报/月报显示 production vs challenger 的样本数、收益、回撤、胜率、benchmark excess、pending outcome、promotion gate 状态和 owner action；样本不足时只给观察建议，不给 production 结论；测试覆盖报告文本、样本不足边界和 `production_effect=none`|2026-05-05: 新增任务，原因：`REPORT-004` 已有投资复盘入口，但还没有读取 prediction/shadow outcome 和 promotion gate，无法完整回答候选规则是否值得晋级。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始把 prediction outcomes 和 shadow maturity 接入周/月复盘与 feedback loop review。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：周/月复盘和 feedback loop review 已读取 prediction outcomes 并展示 production vs challenger；完整 DONE 仍需真实样本验证报告解释质量。|
|REPORT-006|报告/关注股票趋势|P1|DONE|系统实现|已在 `score-daily` 日报结论区增加核心观察池逐 ticker 趋势分析；只读使用已通过市场数据质量门禁的 `features_daily` 价格/趋势特征，不改变评分、仓位闸门、execution policy 或 prediction ledger|日报显示 `config/universe.yaml` 的 AI core_watchlist 每个 ticker 的 1/5/20 日收益、20/50/100/200 日均线位置、相对均线偏离和趋势状态；缺少某 ticker 或窗口特征时显式标记数据缺口；报告声明 `production_effect=none`，trace bundle 可追溯到价格缓存和特征缓存；同步更新 `docs/system_flow.md` 并补充报告文本测试|2026-05-05: 新增并进入实现，原因：owner 要求当前输出结论增加关注股票趋势分析；该改动影响日报解释和投资阅读顺序，但不改变评分或仓位。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：新增关注股票趋势分析模块、日报章节、trace claim/evidence、系统流图和测试；`ruff check src tests` 与全量 `pytest -q` 382 项通过。|
|MACRO-001|宏观/仓位|P2|BASELINE_DONE|系统实现|已完成第一阶段独立 `macro_risk_asset_budget` 层：在 `config/portfolio.yaml` 静态预算基础上，按 VIX、10Y 利率变化和 FRED `DTWEXBGS` 广义美元指数变化下调总风险资产预算；日报、`scores_daily.csv`、decision snapshot、belief_state、回测每日明细和回测报告均输出静态预算、调整后预算、触发等级和原因；剩余缺口是参数 replay/shadow、更多宏观指标和 `GOV-001` 批准|当 VIX、利率、美元等触发流动性风险时，总风险资产预算可下调；AI 在风险资产内部的相对权重仍可单独解释；回测实际策略敞口使用总资产内 AI exposure；实现时同步更新 `docs/system_flow.md` 和测试|2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现宏观总风险资产预算层，避免宏观流动性只体现在模块分或风险资产内 AI gate。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `macro_risk_asset_budget` 配置、计算层、日报/CSV/snapshot/belief_state/回测输出和测试；完整 `DONE` 仍需阈值 replay/shadow 和规则批准。2026-05-05: 更新宏观美元输入语义，原因：DATA-006 已将 Yahoo `DX-Y.NYB` 替换为 FRED `DTWEXBGS` 广义美元指数。|
|THESIS-001|交易 thesis|P1|DONE|系统实现|已把 thesis 从结构校验/复核升级为状态机：`draft -> active -> warning -> challenged -> invalidated -> closed`，并把 `warning/challenged/invalidated` 作为日报解释和 `position_gate` 候选输入；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#thesis-001`|thesis 报告显示当前状态、上次状态、触发原因、证据来源、相关 ticker/产业链节点和人工复核要求；`draft` 不进入正式评分，`warning` 至少触发禁止主动加仓，`challenged/invalidated` 能进入仓位限制或人工复核流程；实现时同步更新 `docs/system_flow.md` 并补充 schema、复核、日报和回测相关测试|2026-05-04: 从 P2/PROPOSED 升级为 P1/READY，原因：状态机比单纯总分下降更能解释 thesis 承压、证伪、估值过高和基本面恶化的差异。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始扩展 thesis 状态机字段、状态复核规则和仓位闸门联动。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：状态机 schema、状态迁移校验、复核健康状态、日报人工复核摘要、position_gate 联动、模板和测试均已完成；`python -m pytest -q` 通过 220 项测试。|
|DATA-001|估值数据|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成低成本 baseline：新增 EODHD Earnings Trends 当前 EPS 90 日修正接入、原始 payload 审计、合并估值快照、中文报告和数据源目录/系统流图；剩余历史 point-in-time estimates/archive 采购或回填暂缓，forward-only 自建快照归档由 `DATA-003` 承接；详细拆解见 `docs/requirements/valuation_history_baseline_2026-05-04.md`|Baseline 验收：当前 `score-daily` 可从 EODHD trends 生成可审计 `eps_revision_90d_pct`，记录 provider、endpoint、请求参数、下载时间、row count 和 checksum，并标记采集日后可见；`valuation_percentile` 至少有 3 个历史点。完整 `DONE` 验收仍需要稳定 90 天真实历史/PIT 支持；历史采购/回填当前不推进，forward-only 快照按 `DATA-003` 自然积累，且不得把采集日回填伪装为 signal_date 当时可见数据|基础拉取、历史缓存和校验已完成；EODHD baseline 不替代 Intrinio/Zacks、FactSet 或等价 PIT estimates archive。2026-05-04: 从 BLOCKED_EXTERNAL 改为 IN_PROGRESS，原因：owner 批准先按低成本 baseline 接入 EODHD Earnings Trends 补当前 EPS 90 日修正，同时保留严格 PIT 完成条件。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：实现 `aits valuation fetch-eodhd-trends`、`data/raw/eodhd_earnings_trends` 原始审计、合并估值快照、数据源目录、系统流图、README/架构/backlog 文档和测试；`python -m ruff check src tests`、`python -m pytest -q` 通过。2026-05-04: owner 决策不购买或伪造历史 PIT；本任务不继续推进历史 PIT estimates/archive 采购、接入或历史回测可见性升级；forward-only 自建快照归档另由 `DATA-003` 登记。|
|AUDIT-001|回测审计|P2|VALIDATING|系统实现|用真实历史数据持续检查 `backtest_audit_*.md` 和覆盖诊断是否足够可解释|如果按月聚合不足以定位问题，拆分或补充更细粒度诊断；若足够，则保持当前格式|当前基础版已输出机器可读覆盖诊断和中文审计报告|
|RISK-006|政策/地缘可靠数据源|P1|BASELINE_DONE|项目 owner + 系统验证|已完成低成本官方来源第一版：`aits risk-events fetch-official-sources` 监控 Federal Register/BIS/OFAC/USTR/Congress.gov/GovInfo/Trade.gov CSL，写入 raw payload、download manifest、待人工复核候选 CSV 和中文报告；剩余缺口是 owner 配置 Congress.gov/GovInfo API key、每日复核声明和真实样本验证；详细边界见 `docs/requirements/risk_event_review_workflow_2026-05-04.md#risk-006`|Baseline 验收：官方来源组合、provider、endpoint、授权/LLM 权限、缓存路径、row count/checksum、跳过来源和 `pending_review` 隔离已可审计；完整 `DONE` 仍需真实样本通过 source policy、occurrence/attestation 校验，并让 `score-daily` 区分“已复核无重大事件”与“没有可靠来源输入”；不得把 `public_convenience`、未授权供应商文本或 `llm_extracted` 输出直接当作可评分输入|2026-05-05: 新增任务，原因：owner 指出当前 `policy_geopolitics` 置信度很低，后续需要可靠数据源才能正确分析；在可靠来源和复核纪律到位前，本模块保持保守降级，不用临时新闻源或模型推断绕过。2026-05-05: 从 BLOCKED_OWNER_INPUT 改为 IN_PROGRESS，原因：owner 确认低成本官方来源组合和人工复核责任，开始实现第一版官方来源抓取与审计链路。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：官方来源抓取、原始缓存、manifest 审计、候选 CSV、中文报告、数据源目录、系统流图、`.env.example`、Federal Register live smoke test 和全量测试已完成；完整生产使用仍依赖 owner API key、每日复核声明和真实样本。|
|RISK-007|风险事件/日报前 OpenAI 自动预审|P1|BASELINE_DONE|系统实现 + 项目 owner|基础版已完成：`score-daily` 默认在风险事件发生记录校验前抓取官方来源并调用 OpenAI metadata-only 风险事件预审；详细拆解见 `docs/requirements/risk_event_daily_openai_precheck_2026-05-05.md`|`score-daily` 默认在风险事件发生记录校验前抓取官方来源并调用 OpenAI Responses API，可用 `--skip-risk-event-openai-precheck` 跳过；默认最多处理 20 条官方候选，OpenAI 默认模型 `gpt-5.5`、`reasoning.effort=high`、读超时 120 秒；单个 OpenAI 请求失败时重试该请求 2 次，仍失败则整批 fail closed；输出官方抓取报告、OpenAI 预审报告和 `risk_event_prereview_queue.json`；缺 `OPENAI_API_KEY`、官方来源抓取失败、provider LLM 权限失败或 OpenAI 请求最终失败时停止日报评分；无候选或 irrelevant 结果不增加人工队列；预审输出仍为 `llm_extracted / pending_review`，不得写 occurrence、复核声明、评分、仓位闸门或 thesis 状态；完整 `DONE` 仍需 owner 人工复核纪律|2026-05-05: 新增并进入实现，原因：owner 要求先用 OpenAI key 做政策/地缘预审，并加入日报前自动流程；同时明确默认不把正常无关结果加入人工确认输入。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增官方候选批量 OpenAI 预审、`score-daily --risk-event-openai-precheck` 前置流程、README、系统流图和测试；`ruff check src tests` 通过，`pytest -q` 362 passed。2026-05-05: owner 确认默认 OpenAI 响应可能超过 60 秒，已将默认请求读超时调为 600 秒并开放 CLI 参数；单候选真实 smoke 等待约 408 秒后收到 OpenAI HTTP 502，按 fail-closed 未写队列。2026-05-05: `gpt-5.5` 单候选 smoke 可收到正常响应；同步修正 irrelevant/none 但带 risk_id 的输出不入人工队列。2026-05-05: owner 决策后续默认使用 `gpt-5.5`，不再默认使用较不稳定的 `gpt-5.5-pro`；代码常量、README、系统流图、数据源目录和示例已同步；不带 `--openai-model` 的单候选日报前预审 smoke 约 96 秒完成，预审 PASS_WITH_WARNINGS，待复核队列 0，日报 PASS_WITH_LIMITATIONS。2026-05-05: owner 决策把 OpenAI 默认配置改为 `gpt-5.5` + `reasoning.effort=high` + 120 秒读超时；`score-daily` 默认启用日报前预审，候选排序改为 AI 政策/出口管制优先，日报正文新增预审摘要。2026-05-05: 默认 20 条真实样本已执行，官方来源抓取 PASS、8 个 payload、402 条候选；OpenAI 预审上限 20 条，LLM claim 数 23，HTTP 502 错误 1、警告 13，按 fail closed 未写新队列，日报评分未继续。2026-05-05: owner 批准单个 OpenAI 请求失败时重试 2 次；第 3 次仍失败则整批 fail closed，不做断点续跑或部分成功入队。2026-05-05: 已实现并验证请求级重试和批量失败停止；`ruff check src tests`、目标测试 42 passed、完整 `pytest -q` 366 passed；默认 20 条真实样本官方抓取 PASS，OpenAI 预审在单请求 3 次尝试后仍失败，报告 FAIL、LLM claim 16、错误 1、警告 7，未写新队列，日报未继续。|
|RISK-008|风险事件/OpenAI 无人复核边界|P1|BASELINE_DONE|系统实现|已明确日报前 OpenAI 预审在 owner 暂无复核精力时采用 backlog-only 边界：候选继续保留审计和告警，但不进入 `execution_policy.manual_review_gate_ids`，不要求当天人工复审；正式评分/仓位仍只读取已确认 occurrence 或有效复核声明|日报必须说明 OpenAI 预审候选是未确认 backlog，不会写 occurrence、复核声明、评分、仓位闸门、thesis 状态，也不会单独把执行动作改成 `wait_manual_review`；若没有人工复核能力，policy/geopolitics 仍保持低置信度/数据不足，而不是把 LLM 判断当作风险事实或无风险证明|2026-05-05: 新增并进入实现，原因：owner 表示暂时没有精力复审 OpenAI 输出；需要把报告边界从“待当天人工复核”澄清为“未确认 backlog”，避免误读为必须处理的人工 SLA，同时保持 source policy 和 LLM 隔离。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：日报 OpenAI 预审章节、README、数据源目录和系统流图已明确 backlog-only 边界，测试覆盖 OpenAI backlog 不单独触发 `wait_manual_review` 文案。|

## 更新模板

新增任务时使用下面格式补充到“当前任务”表：

```text
ID:
领域:
优先级:
状态:
下一责任方:
阻塞或下一步:
验收标准:
备注:
```

状态更新时至少记录：

```text
YYYY-MM-DD: 从 <旧状态> 改为 <新状态>，原因：<触发条件、实现范围、验证结果或 blocker>。
```
