# 产物目录

本文从“看到一个文件后怎么理解它”的角度列出关键 artifact。`docs/system_flow.md` 仍是工程事实源；本文是阅读索引，帮助使用者判断 artifact 的生成者、上游输入、下游用途、production 影响和常见误解。

如果需要理解输入数据如何计算成输出数据，先读 `docs/calculation_logic.md`；字段级含义见 `docs/schema/fields.yaml`。该 YAML 先覆盖 `scores_daily.csv`、decision snapshot、trace bundle、prediction ledger 和 shadow parameter search 的核心字段。

## Production / advisory 主链路

| Artifact | 由谁生成 | 上游输入 | 关键字段或内容 | 下游使用 | 是否影响 production | 常见误解 |
|---|---|---|---|---|---|---|
| `data/raw/prices_daily.csv` | `aits download-data` | FMP 股票/ETF、Cboe VIX | `ticker`、`date`、OHLCV、`adj_close`、provider metadata | `validate-data`、features、backtest、outcome | 是，作为主价格缓存 | 不是供应商原始响应；已标准化为本地长表 |
| `data/raw/prices_marketstack_daily.csv` | `aits download-data` | Marketstack | 第二行情源 OHLCV | `validate-data` reconciliation | 是，作为质量核验输入 | 不覆盖主价格缓存 |
| `data/raw/rates_daily.csv` | `aits download-data` | FRED | `series`、`date`、`value` | macro features、quality gate、risk budget | 是 | `DTWEXBGS` 是广义美元指数代理，不是 ICE DXY |
| `data/raw/download_manifest.csv` | `aits download-data` | 本次下载请求 | provider、endpoint、request params、timestamp、row count、checksum | 数据源审计、质量报告 | 是，作为来源审计 | 不包含 API key 或 token 原文 |
| `outputs/reports/data_quality_YYYY-MM-DD.md` | `aits validate-data` | raw market/macro cache、download manifest、data quality config | schema、freshness、duplicates、suspicious values、reconciliation | features、score-daily、backtest 前置门禁 | 是，失败时下游应停止 | PASS 不等于投资结论可靠，只代表数据缓存可继续用 |
| `outputs/reports/data_quality_YYYY-MM-DD_marketstack_reconciliation.csv` | `aits validate-data` | FMP 主源、Marketstack 第二源 | ticker/date、主/二源数值、分类、severity、证据 | 数据质量复核 | 是，作为质量证据 | INFO 不代表改价，只是可解释差异 |
| `data/processed/features_daily.csv` | `aits build-features` / `aits score-daily` | 通过门禁的 raw cache、feature config | tidy features、as_of、ticker/series、value | scoring、reports、backtest | 是 | 不是所有字段都会进入评分 |
| `outputs/reports/feature_summary_YYYY-MM-DD.md` | `aits build-features` | features、quality gate | feature coverage、warnings | score-daily、人工复核 | 是，作为特征健康报告 | 不能替代 `validate-data` |
| `outputs/reports/feature_availability_YYYY-MM-DD.md` | `build-features` / `score-daily` / `backtest` | `config/feature_availability.yaml`、feature rows | PIT availability、future rows、fallback 策略 | score-daily、backtest fail-closed | 是 | 缺少 availability rule 不是小问题，会触发 fail closed |
| `data/processed/sec_fundamentals_YYYY-MM-DD.csv` | SEC/TSM fundamentals pipeline | SEC companyfacts、TSMC IR | metric、period、filed/disclosed dates、value | fundamental scoring | 是 | TSM 可由官方 IR 合并，不等同 SEC companyfacts 缺失 |
| `data/processed/sec_features_YYYY-MM-DD.csv` | `aits fundamentals build-sec-features` / `score-daily` | SEC-style metrics | margin、R&D、CapEx 等特征 | fundamental component | 是 | 单位或周期不一致时会跳过或告警，不应手工平滑 |
| `data/raw/sec_edgar/*` + `data/processed/sec_edgar/*.csv` + `outputs/reports/sec_pit_backfill/*` | `aits sec-pit backfill` / `scripts/run_sec_pit_backfill.py` | SEC submissions、SEC companyfacts、fundamental metric/feature policy | raw manifest checksum、filing timeline、fact availability、PIT intervals、daily panel、feature panel、coverage/leakage validation | B-grade reconstructed filing-time fundamentals for backtest/score loaders | 否，`production_effect=none` | 默认是 `B_RECONSTRUCTED_SEC_FILING_PIT`，不是 strict vendor archive；不得只按 fiscal period end 合并 |
| `data/external/valuation_snapshots/*.yaml` | `aits valuation fetch-fmp` 或人工导入 | FMP / 可审计估值来源 | valuation percentile、crowding、EPS revision、PIT metadata | valuation component、valuation gate | 是 | 过期或 public convenience 来源不能伪装成高可信生产输入 |
| `data/external/risk_event_occurrences/*.yaml` | 风险事件导入、LLM formal、人工复核 | 官方来源、LLM precheck、review attestation | risk id、level、evidence grade、lifecycle、action class | policy/geopolitics score、risk gate、alerts | 是 | LLM formal 不是人工复核，B 级 evidence 不能单独触发 gate |
| `data/external/trade_theses/*.yaml` | 人工维护 | thesis、验证指标、证伪条件 | status、review state、invalidators | thesis gate、日报解释 | 是，对 active_trade 纪律有效 | watch_only ticker 缺 thesis 不等于 thesis 失败 |
| `data/external/portfolio_positions/current_positions.csv` | 人工持仓快照 | 真实账户持仓 | ticker、市值、AI exposure、节点/地区/因子 | portfolio exposure、risk budget 扩展 | Advisory 输入 | 缺失时不能用观察池或模型建议仓位替代真实持仓 |
| `data/processed/scores_daily.csv` | `aits score-daily` | features、fundamentals、valuation、risk、weights、gates | component score、overall、confidence、effective weights、model/final position、gate summary | 日报、snapshot、dashboard、previous comparison | 是，作为评分输出 | 不等于交易指令，也不等于账户实际仓位 |
| `outputs/reports/daily_score_YYYY-MM-DD.md` | `aits score-daily` | scores、quality、features、manual inputs、trace context | Decision Card、Data Lineage、funnel、gate ladder、module scores、review sections | 人读主报告、dashboard 输入 | Advisory / trend judgment | 报告仓位是解释区间，不是自动买卖 |
| `data/processed/decision_snapshots/decision_snapshot_YYYY-MM-DD.json` | `aits score-daily` | 当日评分上下文、gate、quality、trace | market regime、scores、confidence、positions、position_gates、trace refs | feedback、shadow、replay、dashboard | 是，记录 production 判断 | 不是回测结果，且不应被后验 outcome 改写 |
| `outputs/reports/evidence/daily_score_YYYY-MM-DD_trace.json` | `aits score-daily` | report claims、evidence、datasets、quality、run manifest | claim/evidence/dataset/quality refs、run id、rule versions、weight calibration | dashboard、trace lookup、审计 | 不直接改变结果 | 不是日报正文，而是证据索引 |
| `data/processed/prediction_ledger.csv` | `score-daily` / challenger shadow | decision snapshot、trace | candidate_id、production_effect、signal date、score、final position、outcome status | outcome calibration、shadow maturity | production 行影响正式观察；challenger 行必须隔离解释 | 后验 outcome 不应改写 signal-time 输入 |
| `outputs/reports/evidence_dashboard_YYYY-MM-DD.html` | `aits reports dashboard` | daily report、trace、snapshot、belief state、alerts、scores | 分层展示结论、evidence、dataset、quality、gate、trend | 本地只读下钻 | 否，`production_effect=none` | dashboard 不替代 Markdown 日报和 trace 的审计责任 |

## Daily-run / replay 运行产物

| Artifact | 由谁生成 | 上游输入 | 关键字段或内容 | 下游使用 | 是否影响 production | 常见误解 |
|---|---|---|---|---|---|---|
| `outputs/reports/daily_ops_plan_YYYY-MM-DD.md` | `aits ops daily-plan` / `daily-run` | market calendar、env presence、config | planned commands、required env、expected artifacts、skip reason | 调度前检查、daily-run | 否，只读计划 | 计划报告不代表命令已经执行 |
| `outputs/reports/daily_ops_run_YYYY-MM-DD.md` | `aits ops daily-run` | plan、实际子命令结果 | step status、exit code、duration、artifact paths | 运行审计、问题定位 | 是，作为生产运行审计 | 不保存 stdout/stderr 原文或 secret |
| `outputs/reports/daily_task_dashboard_YYYY-MM-DD.html/json` | `aits ops daily-run` / `aits reports daily-tasks` | daily_ops_run metadata、本轮同日子报告、evidence dashboard JSON、最近可用 shadow parameter search bundle、最近同日 `shadow_iteration_YYYY-MM-DD.json`、可选同日 `paper_trading_summary_YYYY-MM-DD.json`、可选同日 `paper_signal_quality_YYYY-MM-DD.json`、可选同日 `shadow_parameter_impact_YYYY-MM-DD.json`、可选同日 `weight_adjustment_candidates_YYYY-MM-DD.json`、可选同日 `weight_candidate_evaluation_YYYY-MM-DD.json`、可选同日 `weight_promotion_gate_YYYY-MM-DD.json`、可选同日 `daily_weight_adjustment_summary_YYYY-MM-DD.json`、可选 shadow weight iteration current/candidate JSON、可选 shadow vs production comparison JSON、最近 7/14/30 日已存在的 paper trading summary 和 order intent candidate JSON、可选最近已有 `paper_trading_replay_START_END.json`、search window 内 production decision snapshots | `key_conclusions`、投资动作/仓位/置信度/Data Gate、数据可信度、参数治理、反馈复盘、shadow parameter 诊断领先 trial 与收益差距、Shadow Iteration Status 只读卡片、Paper Trading Summary（candidate_count、blocked_candidates、generated_intents、approved/rejected、submitted、filled/open、reconciliation_status）、Paper Trading Trend（7/14/30 日窗口、latest `replay_mode` / `portfolio_carry_forward`、summary 可用/缺失、候选/订单/成交/PnL/reconciliation、synthetic snapshot 使用比例、top `blocked_by`、top `reason_code`；最近 replay 为 `continuous_portfolio` 时额外展示 final_equity、max_drawdown、exposure_peak、final_positions count 和 expired DAY orders）、Paper Signal Quality 轻量卡片（evaluation_status、主要 `blocked_by`、synthetic_snapshot_ratio、sample_count、report link、observe-only / `production_effect=none`）、Shadow Impact 轻量卡片（impact_status、主要 blocked_by / warnings、7/14/30 日 production/shadow/unknown 样本数、production vs shadow filled_count、paper PnL、continuous replay availability/mode、report link）、Weight Adjustment Candidate 轻量卡片（candidate_count、top_candidate_id、gate_status、main blocked_by、report link、`production_effect=none`）、Weight Candidate Evaluation 轻量卡片（evaluation_status、candidate_count、evaluable_candidate_count、top_candidate_id、main blocked_by、report link、`production_effect=none`）、Weight Promotion Gate 轻量卡片（gate_status、candidate_count、ready_for_manual_review_count、blocked_count、top_candidate_id、main blocked_by、report link、`production_effect=none`）、Daily Weight Adjustment Summary 轻量卡片（candidate_count、evaluation_status、promotion_gate_status、ready_for_manual_review_count、main blocked_by、report link、`production_effect=none`、manual_review_only）、Shadow Weight Iteration 轻量卡片（current weights、latest decision、latest delta、candidate report link、`production_effect=none`、manual_review_only）、Shadow vs Production Comparison 轻量卡片（status、production decision、shadow decision、score_delta、decision_changed、main reason、report link、`production_effect=none`、manual_review_only）、production/current vs shadow candidate 结果对比表（Total return、Max drawdown、Turnover、Beat rate、样本覆盖和 return 计算口径）、分区参数对比表（Gate cap override 与权重参数，含 production 实际 gate cap 数值/区间）、运行健康、任务状态、子报告 `href` 和可点击子任务下钻入口 | 每日关键结论入口 | 否，`production_effect=none` | 任务状态是审计信息，不是页面首要结论；dashboard 不重新计算 shadow iteration、shadow impact、weight adjustment candidates、weight candidate evaluation、weight promotion gate、daily weight adjustment summary、shadow weight iteration 或 shadow vs production comparison，也不触发 paper trading、paper replay、真实 broker、参数晋级或调参应用，只读取 JSON；dashboard 的 Shadow Impact、Weight Adjustment Candidate、Weight Candidate Evaluation、Weight Promotion Gate、Daily Weight Adjustment Summary、Shadow Weight Iteration 和 Shadow vs Production Comparison 都只展示轻量摘要，不展示 JSON/Markdown 长表；shadow parameter 诊断领先、Shadow Impact、Weight Adjustment Candidate、Weight Candidate Evaluation、Weight Promotion Gate、Daily Weight Adjustment Summary、Shadow Weight Iteration 和 Shadow vs Production Comparison 均不等于可上线；Paper Trading Summary / Trend / Signal Quality / Shadow Impact / Weight Adjustment Candidate / Weight Candidate Evaluation / Weight Promotion Gate / Daily Weight Adjustment Summary / Shadow Weight Iteration / Shadow vs Production Comparison 只展示模拟执行复盘和只读质量解释，缺历史 summary、candidate JSON、continuous replay、signal quality JSON、shadow impact JSON、weight adjustment candidate JSON、weight candidate evaluation JSON、weight promotion gate JSON、daily weight adjustment summary JSON、shadow weight iteration JSON 或 shadow vs production comparison JSON 显示 `LIMITED` / `MISSING` / `INSUFFICIENT_DATA` 或 `continuous_replay_missing` warning 且不补造结论，`LIMITED` / `ERROR` 不阻断主报告；daily-independent 趋势或 replay 不是连续组合收益，continuous replay 摘要也不是实盘收益或上线依据，paper PnL、shadow current weights 和 comparison score_delta 不改变 production 仓位建议；各子任务 Markdown/HTML/JSON 仍是审计源，后续可升级为专属网页 |
| `outputs/reports/daily_decision_summary_YYYY-MM-DD.json` | `aits ops daily-run` / `aits reports daily-tasks` | daily task dashboard view model、evidence dashboard JSON、parameter governance JSON、feedback / health 子报告、run metadata、source artifact checksums | `as_of`、`run_id`、`production_effect=none`、data_gate status/blocking_reasons、investment_conclusion action_bias/confidence/position_band/major_risks、parameter_governance production_profile/shadow_candidate/promotion_status/blocking_reasons、feedback_review summary、system_health status/warnings、source_artifacts / href / checksum | 后续 OrderIntentBuilder 或 order intent candidate adapter 的只读上游输入；当前只供审计和接线准备 | 否，`production_effect=none` | 不是交易信号、不是订单意图，也不会调用 trading engine；缺子报告时只能标记 `missing` / `limited`，不能把任务成功状态补写成投资结论 |
| `outputs/reports/order_intent_candidates_YYYY-MM-DD.json` | `aits ops daily-run` / `aits reports daily-tasks` / `scripts/run_paper_trading_from_candidates.py --date YYYY-MM-DD` 默认补齐缺失上游 | `daily_decision_summary_YYYY-MM-DD.json`、decision snapshot、position/gate 结论 | `as_of`、`run_id`、`production_effect=none`、`execution_boundary`、`source_inputs`、OrderIntentCandidate schema-compatible `candidates`、`blocked_by`、source decision、score snapshot、position/gate context | 后续 order intent candidate adapter 的只读候选输入；当前只表达“如果未来允许交易，系统可能想做什么” | 否，`production_effect=none` | daily-run / default runner 生成的 candidate 必须 `blocked=true`，且至少包含 `trading_engine_not_enabled` 和 `manual_approval_required`；Data Gate 不通过或缺失时使用 `data_gate_blocked`；不是 `OrderIntent`，不调用真实 broker API、真实下单接口或账户状态；只有显式审查后 `blocked=false` 且 `mode=paper` 的 candidate 才能由 runner 转为 paper `OrderIntent` |
| `outputs/runs/daily/<executed_at>/as_of_YYYY-MM-DD__<run_id>/manifest.json` | `aits ops daily-run` | 本轮 daily-run artifacts | run id、checksum、legacy mirror、visibility cutoff；artifact 记录由 `ArtifactRef` 统一生成 | 复现、归档、审计 | 是，作为运行 bundle | `data/raw` / `data/processed` 仍是状态缓存，不会每轮完整复制；manifest 只记录产物引用和 checksum，不保存 stdout/stderr 原文 |
| `outputs/replays/YYYY-MM-DD/<run_id>/replay_run.md` | `aits ops replay-day` | cache-only input freeze | visible cutoff、excluded future inputs、replay artifacts | 历史复现、diff | 否，不改 production artifacts | replay 不是 live daily-run，也不应调用 live provider |
| `outputs/replays/YYYY-MM-DD/<run_id>/diff_vs_production.md` | `replay-day --compare-to-production` | replay bundle、production artifacts | checksum、row count、status diff | PIT 复核 | 否 | diff 说明产物不同，不自动说明哪个结论正确 |

注：Daily task dashboard 还会只读读取 latest
`data/derived/weight_iterations/promotion/proposals/shadow_promotion_proposal_YYYY-MM-DD.json`
并展示 Shadow Promotion Proposal 轻量卡片；dashboard 不运行 TRADING-018D proposal
script，也不重跑 018B/018C/018C2、scoring、broker、paper runner 或 replay runner。
同时会只读读取 latest
`data/derived/weight_iterations/promotion/preflight/shadow_promotion_apply_preflight_YYYY-MM-DD.json`
并展示 Shadow Promotion Apply Preflight 轻量卡片；dashboard 不运行 TRADING-018E1
preflight script，也不执行 apply、不写 production profile、不触发 broker 或 replay。
同时会只读读取 latest
`data/derived/weight_iterations/promotion/apply/shadow_promotion_apply_result_YYYY-MM-DD.json`
并展示 Shadow Promotion Apply Result 轻量卡片；dashboard 不运行 TRADING-018E2
apply script，也不触发 scoring、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/weight_iterations/promotion/rollback_results/shadow_promotion_rollback_result_YYYY-MM-DD.json`
并展示 Shadow Promotion Rollback Result 轻量卡片；dashboard 不运行 TRADING-018E3
rollback script，也不触发 scoring、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/weight_iterations/promotion/audit/shadow_promotion_lifecycle_audit_YYYY-MM-DD.json`
并展示 Shadow Promotion Lifecycle Audit 轻量卡片；dashboard 不运行 TRADING-018F
audit script，也不触发 018B/018C/018C2/018D/018E1/018E2/018E3、scoring、broker、
replay 或 trading execution。
同时会只读读取 latest
`data/derived/weight_iterations/governance/parameter_governance_summary_YYYY-MM-DD.json`
并展示 Parameter Governance Summary 轻量卡片；dashboard 不运行 TRADING-019 summary
script，也不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F、scoring、broker、
replay 或 trading execution。
同时会只读读取 latest
`data/derived/weight_iterations/governance/web/parameter_governance_web_view_YYYY-MM-DD.json`
并展示 Parameter Governance Web View 轻量卡片；dashboard 不运行 TRADING-020 render script、
TRADING-019 summary、018B/018C/018C2/018D/018E1/018E2/018E3/018F、scoring、broker、
replay 或 trading execution。
同时会只读读取 latest
`data/derived/weight_iterations/governance/digests/parameter_governance_daily_digest_YYYY-MM-DD.json`
并展示 Parameter Governance Daily Digest 轻量卡片；dashboard 不运行 TRADING-021 digest script、
TRADING-020 render script、TRADING-019 summary、018B/018C/018C2/018D/018E1/018E2/018E3/018F、
scoring、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/daily_trading_system_operator_brief_YYYY-MM-DD.json`
并展示 Daily Trading System Operator Brief 轻量卡片；dashboard 不运行 TRADING-022 script、
TRADING-021 digest、TRADING-023 health summary、TRADING-024 freshness summary、TRADING-025、
TRADING-020 render、TRADING-019 summary、018B/018C/018C2/018D/018E1/018E2/018E3/018F、
market pipeline、backtest pipeline、scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/scheduler_dry_run/daily_operator_brief_scheduler_dry_run_YYYY-MM-DD.json`
并展示 Daily Operator Brief Scheduler Dry Run 轻量卡片；dashboard 不运行 018B-025、
TRADING-026 script、operator brief、scheduler creation、market pipeline、backtest pipeline、
scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/scheduler_templates/daily_operator_brief_scheduler_templates_YYYY-MM-DD.json`
并展示 Scheduler Configuration Templates 轻量卡片；dashboard 不运行 018B-027、
TRADING-028 script、operator brief、scheduler creation、market pipeline、backtest pipeline、
scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/scheduler_template_validation/daily_operator_brief_scheduler_template_validation_YYYY-MM-DD.json`
并展示 Scheduler Template Validation Report 轻量卡片；dashboard 不运行 018B-028、
TRADING-029 script、template generator、operator brief、scheduler creation、market pipeline、
backtest pipeline、scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/notifications/operator_brief_notification_draft_YYYY-MM-DD.json`
并展示 Operator Brief Notification Draft 轻量卡片；dashboard 不运行 018B-029、
TRADING-030 script、operator brief、email/Gmail/Slack/Discord/mobile push、market pipeline、
backtest pipeline、scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/notifications/delivery_preflight/operator_brief_notification_delivery_preflight_YYYY-MM-DD.json`
并展示 Operator Brief Notification Delivery Preflight 轻量卡片；dashboard 不运行 018B-030、
TRADING-031 script、operator brief、notification draft generator、email/Gmail/Slack/Discord/
webhook/mobile push、market pipeline、backtest pipeline、scoring、data download、broker、
replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/notifications/dispatch_preview/operator_brief_notification_dispatch_preview_YYYY-MM-DD.json`
并展示 Operator Brief Notification Dispatch Preview 轻量卡片；dashboard 不运行 018B-031、
TRADING-032 script、delivery preflight、operator brief、notification draft generator、
email/Gmail/Slack/Telegram/Discord/webhook/mobile push、market pipeline、backtest pipeline、
scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/notifications/approval_gate/operator_brief_notification_approval_gate_YYYY-MM-DD.json`
并展示 Operator Brief Notification Approval Gate 轻量卡片；dashboard 不运行 018B-032、
TRADING-033 script、dispatch preview、delivery preflight、operator brief、notification draft
generator、email/Gmail/SMTP/Slack/Telegram/Discord/webhook/mobile push、market pipeline、
backtest pipeline、scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/notifications/draft_dispatch/latest.json`
并展示 Operator Brief Notification Draft Dispatch 轻量卡片；dashboard 不运行 TRADING-034
script、TRADING-033 approval gate、TRADING-032 dispatch preview、delivery preflight、
operator brief、notification draft generator、email/Gmail/SMTP/Slack/Telegram/Discord/
webhook/mobile push、market pipeline、backtest pipeline、scoring、data download、broker、
replay 或 trading execution。
同时会只读读取 latest
`data/derived/operator_briefs/notifications/delivery_audit/notification_delivery_audit_summary_YYYY-MM-DD.json`
并展示 Notification Delivery Audit Summary 轻量卡片；dashboard 不运行 018B-034、
TRADING-035 script、notification draft generator、delivery preflight、draft dispatch、
operator brief、email/Gmail/Slack/Discord/webhook/mobile push、market pipeline、backtest
pipeline、scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`outputs/notification_delivery_failure_classification/notification_delivery_failure_classification_YYYY-MM-DD.json`
并展示 Notification Delivery Failure Classification 轻量卡片；dashboard 不运行
TRADING-036 classifier、TRADING-035 script、任何 notification generator / preflight /
dispatch / delivery / retry、email/Gmail/webhook/mobile、market pipeline、backtest
pipeline、scoring、data download、broker、replay 或 trading execution。
同时会只读读取 latest
`outputs/retry_candidate_queue/retry_candidate_queue_YYYY-MM-DD.json`
并展示 Retry Candidate Queue 轻量卡片；dashboard 不运行 TRADING-037 queue
generator、TRADING-036 classifier、TRADING-035 script、任何 notification generator /
preflight / dispatch / delivery / retry、email/Gmail/webhook/mobile、market pipeline、
backtest pipeline、scoring、data download、broker、replay 或 trading execution，也不修改
approval state。
同时会只读读取 latest
`outputs/retry_execution_dry_run/retry_execution_dry_run_YYYY-MM-DD.json`
并展示 Retry Execution Dry Run 轻量卡片；dashboard 不运行 TRADING-038 dry-run
generator、TRADING-037 queue generator、TRADING-036 classifier、TRADING-035 script、任何
notification generator / preflight / dispatch / delivery / retry、email/Gmail/webhook/
mobile、market pipeline、backtest pipeline、scoring、data download、broker、replay 或
trading execution，也不修改 approval record、approval state 或 delivery state。
同时会只读读取 latest
`data/derived/data_freshness/data_freshness_summary_YYYY-MM-DD.json`
并展示 Data Freshness Summary 轻量卡片；dashboard 不运行 TRADING-024 script、
TRADING-023 health summary、TRADING-022 operator brief、TRADING-021 digest、018B-018F、
data download、market pipeline、backtest pipeline、scoring、broker、replay 或 trading
execution。

## Trading engine / paper trading 产物

| Artifact | 由谁生成 | 上游输入 | 关键字段或内容 | 下游使用 | 是否影响 production | 常见误解 |
|---|---|---|---|---|---|---|
| `data/trading_engine/audit/{order_intent_log,risk_check_log,order_log,execution_report_log,fill_log,portfolio_snapshot}/YYYY-MM-DD.jsonl` | `ExecutionService` / `JsonlAuditLogger` | paper-only `OrderIntent`、risk result、paper order、execution report、portfolio snapshot | 每行包含 timestamp、run_id、strategy_id、schema_version、source_object_id、payload、`intent_id` 或 `related_intent_ids`；`replay_intent_audit_trace` 可按 intent 回放完整轨迹 | paper trading 审计和测试复现 | 否，paper trading MVP；真实交易仍未启用 | `execution_report_log` 和 `fill_log` 不等价；rejected intent 不应有 broker order 或 fill；portfolio snapshot 可通过 `related_intent_ids` 关联到 intent |
| `reports/trading_daily/YYYY-MM-DD.md` | `scripts/run_paper_trading_demo.py` / `scripts/run_paper_trading_from_candidates.py` / trading report writer | paper trading audit state、orders、fills、portfolio snapshot、ReconciliationResult | OrderIntent 数量、风控通过/拒绝、paper order、成交/未成交/取消、portfolio、realized/unrealized PnL、reconciliation status、`production_effect=none` | paper trading 执行复盘 | 否，`production_effect=none` | 不是实盘交易日报，不代表真实账户、券商状态或真实成交；reconciliation `PASS` 只说明 paper portfolio 与本地 audit/fill 重建一致 |
| `outputs/reports/paper_trading_summary_YYYY-MM-DD.json` | `scripts/run_paper_trading_from_candidates.py` / trading report writer | `order_intent_candidates_YYYY-MM-DD.json`、paper trading report、audit log、ReconciliationResult、可选 historical OHLC | status、candidate_count、blocked_candidates、generated_intents、approved/rejected、submitted、filled/open/cancelled、realized/unrealized PnL、reconciliation_status、market_snapshot_source / counts、audit_log_path、trading daily report path、`production_effect=none` | daily task dashboard 的 Paper Trading Summary 区块和 paper replay 汇总 | 否，`production_effect=none` | 该 JSON 只把模拟执行复盘带回每日主入口；market snapshot 优先 historical OHLC，其次 candidate metadata，最后才 synthetic limit price；`LIMITED` / `ERROR` 不阻断主报告，不代表实盘订单、账户状态或 broker 回报，也不会改变 production 仓位建议 |
| `outputs/reports/paper_trading_replay_START_END.json/md` | `scripts/run_paper_trading_replay.py --start YYYY-MM-DD --end YYYY-MM-DD --mode daily-independent` 或 `--mode continuous-portfolio` | 每日 `order_intent_candidates_YYYY-MM-DD.json`；缺失时由 candidate runner 生成 limited `daily_decision_summary` 和 `order_intent_candidates`；逐日 paper summary / trading daily report / audit log；可选 historical OHLC | `replay_mode`、`portfolio_carry_forward`、`production_effect=none`、`order_expiration_policy`、`unsupported_order_policy`、`continuous_metrics_available`、quality_flags、market_snapshot_source 分布、窗口状态、paper-only execution boundary、candidate / blocked / intent / risk / order / fill / PnL totals、reconciliation_status 分布、按 symbol / strategy_id / reason_code / blocked_by 聚合、每日结果和 limited upstream 标记；continuous 额外输出 `expired_day_orders`、`rejected_gtc_orders`、`carried_positions_count`、`final_cash`、`final_equity`、`final_positions`、`max_position_concentration`、`max_drawdown_pct`、equity curve、exposure peak 和 final portfolio summary | paper trading daily flow validation、复盘、dashboard latest replay 摘要和 paper signal quality 可选输入 | 否，`production_effect=none` | `daily-independent` 每天重新初始化组合，不是连续组合收益；`continuous-portfolio` 是 paper-only 连续组合模拟，不是真实账户收益、真实 broker 成交、完整税费/滑点模拟或实盘上线依据；DAY order 当日未成交即过期；GTC / non-DAY candidate 直接 rejected；replay 不读取 broker API key、不调用真实 broker；缺候选只生成 limited artifacts，不补造投资结论 |
| `outputs/reports/paper_signal_quality_YYYY-MM-DD.json/md` | `python scripts/run_paper_signal_quality.py --date YYYY-MM-DD` | 最近 7/14/30 日 `paper_trading_summary_YYYY-MM-DD.json`、`order_intent_candidates_YYYY-MM-DD.json`、`config/paper_signal_quality_policy.yaml`、可选既有 `paper_trading_replay_START_END.json` | 顶层 `policy_id`、`policy_version`、thresholds snapshot、`evaluation_status`、`evaluation_gate` checks / `blocked_by` / human-readable explanation / reason explanations、`PAPER_ONLY_SIMULATION` warning、daily-independent / no carry-forward 时的 `DAILY_INDEPENDENT_ONLY` warning、sample_count、candidate_count、generated_intents、filled_count、synthetic_snapshot_ratio、historical_ohlc_coverage、reconciliation_pass_ratio、按 strategy_id / symbol / reason_code / blocked_by / confidence bucket / market_snapshot_source 聚合的 sample_count、candidate_count、generated_intents、filled_count、avg_paper_pnl、synthetic_snapshot_ratio、quality_status、observe-only 安全边界、`production_effect=none` | daily task dashboard 的 Paper Signal Quality 轻量卡片、paper signal 质量复核 | 否，`production_effect=none` | 使用 `continuous-portfolio` replay 时不再输出 `DAILY_INDEPENDENT_ONLY`，但仍必须输出 `PAPER_ONLY_SIMULATION`；`evaluation_status` / `quality_status` 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`PROMISING_BUT_LIMITED`、`LOW_DATA_QUALITY`、`UNRELIABLE`，禁止 live/trade/promotion/approval 语义；evaluation gate 只解释 paper signal quality，不影响 production 仓位建议、参数晋级、真实交易或 replay；脚本不读取 broker API key、不调用真实 broker、不触发 paper runner/replay；paper PnL 只作诊断字段，不是上线依据 |
| `outputs/reports/shadow_parameter_impact_YYYY-MM-DD.json/md` | `python scripts/run_shadow_parameter_impact.py --date YYYY-MM-DD` | 最近 7/14/30 日 `order_intent_candidates_YYYY-MM-DD.json`、`paper_trading_summary_YYYY-MM-DD.json`、`paper_signal_quality_YYYY-MM-DD.json`、`config/shadow_parameter_impact_policy.yaml`、可选既有 `paper_trading_replay_START_END.json` | `impact_status`（仅 `INSUFFICIENT_DATA` / `OBSERVE_ONLY` / `SHADOW_PROMISING_BUT_LIMITED` / `NO_CLEAR_IMPROVEMENT` / `SHADOW_UNRELIABLE` / `LOW_DATA_QUALITY`）、`policy_id`、`policy_version`、`thresholds_snapshot`、`impact_gate` checks / blocked_by / warnings / explanation / reason explanations、production / shadow / unknown source_profile 分类、candidate_count、generated_intents、approved/rejected、submitted/filled/open/cancelled、realized/unrealized paper PnL、synthetic_snapshot_ratio、historical_ohlc_coverage、reconciliation_pass_ratio、blocked_by / reason_code / confidence bucket performance、continuous replay availability、source artifact path / mode / date range、profile final_equity / max_drawdown（仅 continuous portfolio 可用时）、observe-only 安全边界、`production_effect=none` | daily task dashboard 的 Shadow Impact 轻量卡片、shadow 参数 paper/replay/signal quality 复核 | 否，`production_effect=none` | `SHADOW_PROMISING_BUT_LIMITED` 仍不是上线、实盘、交易或参数批准；短期 paper PnL 为正不能单独证明 shadow 更好；缺 shadow 样本、production baseline 或 continuous replay 时输出 `continuous_replay_missing` warning；daily-independent replay 不会被当作 continuous portfolio 结果；synthetic snapshot 或 OHLC 覆盖问题会输出 `LOW_DATA_QUALITY` 或 gate blocked；脚本不读取 broker API key、不调用真实 broker、不触发 paper runner/replay、不改变 production 参数、仓位建议、参数晋级或交易执行 |
| `outputs/reports/weight_adjustment_candidates_YYYY-MM-DD.json/md` | `python scripts/run_weight_adjustment_candidates.py --date YYYY-MM-DD` | 同日 `daily_decision_summary_YYYY-MM-DD.json`、`paper_signal_quality_YYYY-MM-DD.json`、`shadow_parameter_impact_YYYY-MM-DD.json`、可选已有 `paper_trading_replay_START_END.json`、`config/weight_adjustment_candidate_policy.yaml`、`config/parameter_governance.yaml`、`config/weights/weight_profile_current.yaml`、可选 `config/weights/shadow_weight_profiles.yaml` | 顶层 `mode=observe_only`、`production_effect=none`、`policy_id`、`policy_version`、thresholds snapshot、candidate gate、source artifacts、`candidate_count`、`top_candidate_id`；每个 candidate 包含 `candidate_id`、`generated_at`、`source_profile`、`target_profile`、`parameter_changes`、`reason_codes`、`expected_effect`、`risk_notes`、`blocked_by`、`required_validations` 和 `production_effect=none` | daily task dashboard 的 Weight Adjustment Candidate 轻量卡片、人工调权候选复核 | 否，`production_effect=none` | 第一版只允许向 existing shadow profile 小步靠近，单日最大变化受 policy 限制且总权重不失衡；candidate gate 必须阻断 insufficient sample、low data quality、synthetic snapshot ratio too high、continuous replay missing、shadow impact insufficient、paper signal quality unreliable 和 manual approval required；缺关键输入时输出 `LIMITED` / blocked candidate；脚本不修改 `weight_profile_current.yaml`、不写 approved overlay、不改变 dashboard 主结论、不触发 paper runner/replay/broker/交易，也不根据单日 paper PnL 调高权重 |
| `outputs/reports/weight_candidate_evaluation_YYYY-MM-DD.json/md` | `python scripts/run_weight_candidate_evaluation.py --date YYYY-MM-DD` | 同日 `weight_adjustment_candidates_YYYY-MM-DD.json`、`paper_signal_quality_YYYY-MM-DD.json`、`shadow_parameter_impact_YYYY-MM-DD.json`、可选已有 `paper_trading_replay_START_END.json` 或 continuous replay summary、`config/weight_candidate_evaluation_policy.yaml`、`config/parameter_governance.yaml`、`config/weights/weight_profile_current.yaml`、可选 `config/weights/shadow_weight_profiles.yaml` 和可选 `daily_decision_summary_YYYY-MM-DD.json` | 顶层 `evaluation_mode=observe_only`、`production_effect=none`、`evaluation_status`、policy id/version/thresholds snapshot、安全边界、source artifact checksums、7/14/30 日窗口（candidate_count、evaluable_candidate_count、blocked_candidate_count、insufficient_data_count、low_quality_data_count、continuous replay availability、synthetic_snapshot_ratio、historical_ohlc_coverage、reconciliation_pass_ratio、paper_signal_quality_status、shadow_impact_status、replay_mode、可用的 max_drawdown_delta / final_equity_delta / exposure_delta / concentration_delta）；每个 candidate 包含 `candidate_id`、source/target profile、parameter_changes、required_validations、evaluation_status、blocked_by、warnings、scorecard、recommendation 和 `production_effect=none` | daily task dashboard 的 Weight Candidate Evaluation 轻量卡片、manual review 前的候选权重复核 | 否，`production_effect=none` | 第一版只读取已有产物，不主动重跑历史任务；candidate 级 `evaluation_status` 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`CANDIDATE_PROMISING_BUT_LIMITED`、`NO_CLEAR_IMPROVEMENT`、`CANDIDATE_UNRELIABLE`、`LOW_DATA_QUALITY`；candidate 本身 blocked、manual approval、样本不足、低数据质量、synthetic snapshot ratio 过高、continuous replay 缺失、shadow impact insufficient、paper signal quality unreliable、reconciliation unreliable、drawdown/exposure/concentration worse 都必须 blocked；即使 candidate 看起来更好，也只能进入 manual review；脚本不修改 production profile、不触发 IBKR / PaperBroker / replay runner / 交易，不影响 dashboard 主结论 |
| `outputs/reports/weight_promotion_gate_YYYY-MM-DD.json/md` | `python scripts/run_weight_promotion_gate.py --date YYYY-MM-DD` | 同日 `weight_adjustment_candidates_YYYY-MM-DD.json`、`weight_candidate_evaluation_YYYY-MM-DD.json`、`paper_signal_quality_YYYY-MM-DD.json`、`shadow_parameter_impact_YYYY-MM-DD.json`、可选已有 `paper_trading_replay_START_END.json`、可选 `daily_decision_summary_YYYY-MM-DD.json`、可选 `config/parameter_governance.yaml` 和可选 `config/weights/weight_profile_current.yaml` | 顶层 `gate_mode=manual_review_only`、`production_effect=none`、`promotion_gate_status`、policy id/version/thresholds snapshot、安全边界、source artifact checksums、candidate_count、ready_for_manual_review_count、blocked_count、top_candidate_id、main blocked_by；每个 candidate 包含 `candidate_id`、`candidate_evaluation_status`、`promotion_gate_status`、`blocked_by`、`warnings`、`improvement_summary`、`risk_delta_summary`、`data_quality_summary`、`required_manual_review_items`、`recommendation` 和 `production_effect=none` | daily task dashboard 的 Weight Promotion Gate 轻量卡片、manual review 前最后一层候选权重 gate | 否，`production_effect=none` | 第一版只读取已有产物，不主动重跑 candidate evaluation、paper runner、replay runner 或 broker；promotion gate status 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`BLOCKED`、`NO_CLEAR_IMPROVEMENT`、`CANDIDATE_PROMISING_BUT_LIMITED`、`READY_FOR_MANUAL_REVIEW`；缺 candidate evaluation、candidate hard blocked、manual_approval_required 缺失、样本/filled 不足、数据质量差、synthetic snapshot ratio 高、OHLC coverage 低、reconciliation 低、continuous replay 缺失、drawdown/exposure/concentration 恶化、shadow/paper unreliable、data gate BLOCK 或重大 risk warning 都必须 blocked；`READY_FOR_MANUAL_REVIEW` 也不是自动晋级、不修改 production profile、不影响交易、不代表 live readiness |
| `outputs/reports/daily_weight_adjustment_summary_YYYY-MM-DD.json/md` | `python scripts/run_daily_weight_adjustment.py --date YYYY-MM-DD` | 同日 `weight_adjustment_candidates_YYYY-MM-DD.json/md`、`weight_candidate_evaluation_YYYY-MM-DD.json/md`、`weight_promotion_gate_YYYY-MM-DD.json/md` | 顶层 `mode=observe_only`、`production_effect=none`、`candidate_status`、`evaluation_status`、`promotion_gate_status`、candidate/evaluable/ready/blocked counts、`top_candidate_id`、`main_blocked_by`、warnings、source artifacts、missing artifacts、required manual review items、recommendation、安全边界和 `manual_review_only=true` | daily task dashboard 的 Daily Weight Adjustment Summary 轻量卡片、每日只读权重调节人工复核入口 | 否，`production_effect=none` | 第一版只读取已有 015/016/017 产物，不主动重跑 candidate generator、candidate evaluation、promotion gate、paper runner、replay runner、controlled fill、lifecycle、comparison 或 broker；缺任一 JSON/Markdown 时只标记 `LIMITED` / `INSUFFICIENT_DATA`，不补造 improvement，且 summary 不把 promotion gate 展示为 `READY_FOR_MANUAL_REVIEW`；即使上游 gate 可进入人工复核，也只输出 `manual_review_only`，不修改 production profile、不写 approved profile、不改变 dashboard 主投资结论、不触发交易；当前只预留未来 GitHub Actions / Windows Task Scheduler / cron 接入，不自动启用定时任务 |
| `outputs/reports/daily_weight_adjustment_scheduler_dry_run_YYYY-MM-DD.json/md` | `python scripts/run_daily_weight_adjustment_scheduler_dry_run.py --date YYYY-MM-DD` | 同日 015/016/017 artifact，以及封装调用的 `daily_weight_adjustment_summary_YYYY-MM-DD.json/md` | 顶层 `mode=dry_run`、`production_effect=none`、`manual_review_only=true`、`started_at`、`completed_at`、`duration_seconds`、`dry_run_status`、`invoked_command`、`generated_artifacts`、`missing_artifacts`、`pipeline_status`、candidate/ready/blocked counts、`promotion_gate_status`、warnings 和 safety checks | Windows Task Scheduler dry-run 审计、每日定时调用安全性验证 | 否，`production_effect=none` | dry-run 只验证 scheduler 能安全调用 summary pipeline；允许刷新 daily summary 和写自身报告，不修改 production profile、不写 approved profile、不触发 IBKR、PaperBroker、replay、controlled fill、lifecycle、comparison 或 Git 自动 commit/push；缺上游 artifact 时 `dry_run_status=LIMITED`，不补造 improvement；GitHub Actions schedule 当前不建议启用，优先本地 Windows Task Scheduler 连续观察 3～7 天 |
| `data/derived/weight_iterations/shadow/current_shadow_weights.json` + `data/derived/weight_iterations/shadow/candidates/shadow_weight_candidate_YYYY-MM-DD.json/md` + `history/` + `logs/` | `python scripts/run_daily_shadow_weight_iteration.py --date YYYY-MM-DD` | 同日 `weight_adjustment_candidates_YYYY-MM-DD.json`、`weight_candidate_evaluation_YYYY-MM-DD.json`、`weight_promotion_gate_YYYY-MM-DD.json`、`daily_weight_adjustment_summary_YYYY-MM-DD.json`、TRADING-018A scheduler dry-run safety JSON、`config/daily_shadow_weight_iteration_policy.yaml`、首次初始化时只读 `config/weights/weight_profile_current.yaml` snapshot | candidate 顶层 `mode=shadow_only`、`production_effect=none`、`manual_review_only=true`、`decision`、input artifacts、safety checks、policy id/version/file_exists/thresholds/confidence defaults、confidence、previous/proposed/new shadow weights、constraints/evidence/audit；current 记录 schema、shadow-only weights、constraints、source、audit update_count；history 记录成功 UPDATE 后快照；logs 记录 run-level 写入结果 | daily task dashboard 的 Shadow Weight Iteration 只读卡片；后续 TRADING-018C production vs shadow comparison 输入 | 否，`production_effect=none` | `current_shadow_weights.json` 只代表 shadow 系统内部迭代结果，production 不得直接读取；policy 缺失、TRADING-018A `report_type` 不匹配或缺输入输出 `INSUFFICIENT_DATA`，safety 非 PASS 输出 `SAFETY_BLOCKED`，信号不足输出 `NO_UPDATE`，均不得更新 current；只有 `UPDATE` 才写 history 并原子替换 current；脚本不写 production profile、不写 approved profile、不触发 broker/replay/交易、不实现 promotion |
| `data/derived/weight_iterations/comparison/daily_shadow_vs_production_YYYY-MM-DD.json/md` | `python scripts/run_daily_shadow_vs_production_comparison.py --date YYYY-MM-DD` | `config/weights/weight_profile_current.yaml`、`data/derived/weight_iterations/shadow/current_shadow_weights.json`、同日 `data/processed/decision_snapshots/decision_snapshot_YYYY-MM-DD.json`、TRADING-018B `shadow_weight_candidate_YYYY-MM-DD.json`、`config/scoring_rules.yaml`、可选同日 scoring/backtest/feedback Markdown/JSON | 顶层 `mode=offline_comparison`、`production_effect=none`、`manual_review_only=true`、`comparison_status`、source artifacts、data_quality、shadow_iteration、input_validation；production/shadow 分别输出 score、normalized_score、decision、score_band、final_position_band、risk_flags、top_contributors、contribution_breakdown；difference 输出 score_delta、normalized_score_delta、decision_changed、score_band_changed、risk_flags_changed、main_reason、contribution_deltas、weight_deltas 和安全边界 | daily task dashboard 的 Shadow vs Production Comparison 轻量卡片；manual review 前的 shadow learn 对照材料 | 否，`production_effect=none` | 只用同一份 decision snapshot 组件分离线重算 production/shadow weighted score；不重新抓取数据、不重新生成特征、不运行 broker/replay runner、不写 production profile、不写 approved profile、不 promotion、不改变 dashboard 主投资结论；权重 key 必须覆盖同一组 scoring components，缺失或多余组件只披露为 `INSUFFICIENT_DATA` / warning，不做静默别名映射；shadow score 更高或 decision_changed 不代表可采用 |
| `data/derived/weight_iterations/comparison/reviews/shadow_vs_production_review_YYYY-MM-DD.json/md` | `python scripts/run_shadow_vs_production_multi_day_review.py --date YYYY-MM-DD --lookback-days 7` | 最近 N 日既有 `data/derived/weight_iterations/comparison/daily_shadow_vs_production_YYYY-MM-DD.json` | 顶层 `task_id=TRADING-018C2`、`production_effect=none`、`manual_review_only=true`、`review_decision`、available/missing/insufficient/safety blocked coverage、`average_score_delta`、shadow better/worse/equal day counts、decision difference / decision change stability、risk flag delta、new shadow risk flags、dominant changed weight keys、`promotion_readiness.ready=false`、input artifacts、pipeline contract 和 audit | daily task dashboard 的 Shadow vs Production Multi-day Review 轻量卡片；TRADING-018D manual promotion gate 前的多日人工观察证据 | 否，`production_effect=none` | 只读取既有 018C comparison artifacts，不重跑 scoring 或 comparison pipeline，不写 production profile、不写 approved profile、不 promotion、不运行 broker / paper runner / replay runner；`SHADOW_LOOKS_BETTER` 也不是上线或晋级结论，只表示进入人工观察证据更积极；dashboard 只读读取 latest review artifact，缺失显示 `MISSING` |
| `data/derived/weight_iterations/promotion/proposals/shadow_promotion_proposal_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/promotion/logs/shadow_promotion_proposal_run_YYYY-MM-DD.json/md` | `python scripts/run_shadow_promotion_proposal.py --date YYYY-MM-DD --lookback-days 7` | `current_shadow_weights.json`、`config/weights/weight_profile_current.yaml`、latest TRADING-018C2 multi-day review、最近 N 日 TRADING-018C comparison artifacts、最近 TRADING-018B candidate/history、`config/shadow_promotion_proposal_policy.yaml` | 顶层 `task_id=TRADING-018D`、`mode=manual_promotion_proposal_only`、`production_effect=none`、`manual_review_only=true`、`promotion_proposed`、`promotion_executed=false`、`safe_for_production=false`、`proposal_decision`、readiness checks、input artifacts、production/shadow/proposed weights、delta、impact summary、manual approval requirement、pipeline contract 和 audit | daily task dashboard 的 Shadow Promotion Proposal 只读卡片；future explicit apply task 的人工审核输入 | 否，`production_effect=none` | 本 artifact 只是人工审核 proposal，不是 apply、不写 production profile、不写 approved profile、不触发 broker / paper runner / replay runner / scoring；即使 `promotion_proposed=true`，也必须保持 `promotion_executed=false` 且 `safe_for_production=false`；production/shadow weight keys 必须完全一致，不做静默 alias mapping；dashboard 只读 latest proposal artifact，缺失显示 `MISSING` |
| `data/derived/weight_iterations/promotion/preflight/shadow_promotion_apply_preflight_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/promotion/preflight/logs/shadow_promotion_apply_preflight_run_YYYY-MM-DD.json/md` | `python scripts/run_shadow_promotion_apply_preflight.py --date YYYY-MM-DD` | 018D `shadow_promotion_proposal_YYYY-MM-DD.json`、`data/manual_approvals/shadow_promotion_approval_YYYY-MM-DD.json`、target production profile、`current_shadow_weights.json` | 顶层 `task_id=TRADING-018E1`、`mode=approved_apply_preflight_only`、`production_effect=none`、`manual_review_only=true`、`promotion_executed=false`、`apply_executed=false`、`preflight_only=true`、`safe_for_production=false`、`preflight_decision`、input artifact sha256、approval/proposal/weight/target validation、production diff preview、rollback plan preview、safety checklist、pipeline contract 和 audit | daily task dashboard 的 Shadow Promotion Apply Preflight 只读卡片；future TRADING-018E2 explicit apply command 的预检查输入 | 否，`production_effect=none` | 该 artifact 只说明 apply 前检查结果，不是 apply approval、不写 production profile、不写 production weights、不写 rollback snapshot、不触发 broker / paper runner / replay runner / scoring；即使 `preflight_decision=PASS`，也必须保持 `apply_executed=false` 且 `safe_for_production=false`；dashboard 只读 latest preflight artifact，缺失显示 `MISSING` |
| `data/derived/weight_iterations/promotion/apply/shadow_promotion_apply_result_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/promotion/apply/logs/shadow_promotion_apply_run_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/promotion/rollback/production_profile_before_shadow_promotion_YYYY-MM-DD.json/.sha256` | `python scripts/run_shadow_promotion_apply.py --date YYYY-MM-DD --preflight-file ... --apply-approval-file ... --target-profile ... --i-understand-this-writes-production` | TRADING-018E1 PASS preflight、单独 TRADING-018E2 apply approval、018D proposal、target production profile | 顶层 `task_id=TRADING-018E2`、`mode=explicit_approved_apply`、`apply_decision`、`manual_review_only=true`、`safe_for_scheduler=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、input artifact sha256、approval/preflight/danger flag/target/profile/post-apply validation、diff applied、rollback snapshot 和 audit；成功时 `production_effect=profile_updated_only_if_apply_executed`、`promotion_executed=true`、`apply_executed=true`；未执行时 `production_effect=none`、`promotion_executed=false`、`apply_executed=false` | daily task dashboard 的 Shadow Promotion Apply Result 只读卡片；future TRADING-018E3 rollback command 的输入 | 仅在 `apply_decision=APPLIED` 时更新 target production profile weights；否则 `production_effect=none` | 这是人工显式 apply，不是 proposal、不是 preflight、不是 scheduler 任务；必须有单独 apply approval、danger flag、target hash 一致、rollback snapshot 和 post-apply validation；只允许修改 weights，不触发 scoring、broker、paper runner、replay runner 或 trading execution；dashboard 只读 latest apply result artifact，缺失显示 `MISSING` |
| `data/derived/weight_iterations/promotion/rollback_results/shadow_promotion_rollback_result_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/promotion/rollback_results/logs/shadow_promotion_rollback_run_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/promotion/rollback_current_snapshots/production_profile_before_rollback_YYYY-MM-DD.json/.sha256` | `python scripts/run_shadow_promotion_rollback.py --date YYYY-MM-DD --apply-result-file ... --rollback-approval-file ... --target-profile ... --i-understand-this-rolls-back-production` | TRADING-018E2 `APPLIED` apply result、单独 TRADING-018E3 rollback approval、018E2 rollback snapshot、当前 target production profile | 顶层 `task_id=TRADING-018E3`、`mode=explicit_approved_rollback`、`rollback_decision`、`manual_review_only=true`、`safe_for_scheduler=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、input artifact sha256、approval/apply-result/danger flag/rollback snapshot/target/post-rollback validation、rollback applied diff、rollback-current snapshot 和 audit；成功时 `production_effect=profile_rolled_back_only_if_rollback_executed`、`rollback_executed=true`；未执行时 `production_effect=none`、`rollback_executed=false` | daily task dashboard 的 Shadow Promotion Rollback Result 只读卡片；promotion apply 后问题处理的人工恢复审计输入 | 仅在 `rollback_decision=ROLLED_BACK` 且写入后校验通过时恢复 target production profile weights；否则未执行路径 `production_effect=none` | 这是人工显式 rollback，不是 apply result 自动回滚、不是 scheduler 任务；必须有单独 rollback approval、danger flag、apply result hash、rollback snapshot hash、current target hash 和 rollback-current snapshot；本阶段只允许 weights-only restore，不触发 scoring、broker、paper runner、replay runner 或 trading execution；dashboard 只读 latest rollback result artifact，缺失显示 `MISSING` |
| `data/derived/weight_iterations/promotion/audit/shadow_promotion_lifecycle_audit_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/promotion/audit/logs/shadow_promotion_lifecycle_audit_run_YYYY-MM-DD.json/md` | `python scripts/run_shadow_promotion_lifecycle_audit.py --date YYYY-MM-DD --promotion-date YYYY-MM-DD` | 018D promotion proposal、018E1 apply preflight、018E2 apply result、可选 018E3 rollback result、可选 approval artifacts、可选 rollback/current snapshot metadata | 顶层 `task_id=TRADING-018F`、`mode=promotion_lifecycle_audit_only`、`lifecycle_decision`、`lifecycle_reason`、input artifact path/sha256、artifact chain、proposal/preflight/apply/rollback summary、weight lifecycle、safety boundary audit、critical findings、warnings、notes、traceability 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`audit_only=true`、`apply_executed_by_audit=false`、`rollback_executed_by_audit=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Shadow Promotion Lifecycle Audit 只读卡片；promotion lifecycle 人工审计记录；后续只读 monitoring 可读取 | 否，`production_effect=none` | 这是 lifecycle 审计，不是 apply/rollback 执行器；缺 rollback result 可输出 `COMPLETE_APPLIED_NO_ROLLBACK` 和 warning，不视为安全异常；hash/path/date/target/snapshot 矛盾、broker/replay/trading 字段为 true 或执行状态矛盾必须输出 `SAFETY_ANOMALY`；dashboard 只读 latest audit artifact，不重跑 018F 或任何上游/交易 pipeline |
| `outputs/reports/paperbroker_fill_model_calibration_YYYY-MM-DD.json/md` | `python scripts/run_paperbroker_fill_model_calibration.py --date YYYY-MM-DD` | 最近 N 个 `paperbroker_vs_ibkr_paper_comparison_*.json`、最近 N 个 `ibkr_paper_controlled_fill_*.json` no-fill/cancelled artifact、`config/paperbroker_fill_model_calibration_policy.yaml`、可选已有 `paper_trading_replay_START_END.json` 的 `quality_flags`、可选 `paper_signal_quality_YYYY-MM-DD.json` | `calibration_status`（仅 `INSUFFICIENT_SAMPLE` / `LIFECYCLE_ALIGNED_FILL_UNTESTED` / `LOCAL_SIM_TOO_OPTIMISTIC` / `BROKER_REJECTION_GAP` / `OBSERVE_ONLY`）、`calibration_mode=diagnostic_only`、policy id/version/thresholds snapshot、comparison_count、controlled_fill_count、calibration_evidence_count、lifecycle_match_count、lifecycle/status/fill/cancel match ratios、local optimistic、broker rejection、insufficient market data、synthetic snapshot related、no-fill lifecycle only counts、`NO_FILL_LIFECYCLE_VALIDATED` classification counts、source artifact links、recommendations、observe-only 安全边界、`production_effect=none` | PaperBroker fill model calibration 诊断复核；第一版不接 daily task dashboard 大表 | 否，`production_effect=none` | lifecycle open/cancel 对齐不代表 fill model 已验证；controlled fill `fill_seen=false` 且 final cancelled 只归类为 `NO_FILL_LIFECYCLE_VALIDATED`，仍保持 `fill_tested=false` 和 `LIFECYCLE_ALIGNED_FILL_UNTESTED`；报告不修改 `PaperBroker` fill 行为、不影响 replay、paper signal quality、shadow impact、production conclusion、参数晋级或交易建议；脚本不读取 broker API key、不调用 IBKR / broker、不触发 paper runner/replay |
| `data/derived/weight_iterations/governance/parameter_governance_summary_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/governance/logs/parameter_governance_summary_run_YYYY-MM-DD.json/md` | `python scripts/run_parameter_governance_summary.py --date YYYY-MM-DD` | production profile、`current_shadow_weights.json`、latest 018C2 review、018D proposal、018E1 preflight、018E2 apply result、018E3 rollback result、018F lifecycle audit | 顶层 `task_id=TRADING-019`、`mode=parameter_governance_summary_only`、`governance_state`、`governance_reason`、`action_required`、`action_level`、`recommended_action`、input artifact path/sha256、production/shadow weights、delta、review status、promotion status、pending items、safety boundary audit、critical findings、warnings、notes 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Parameter Governance Summary 只读卡片；人工查看当前 production / shadow governance 状态和行动需求 | 否，`production_effect=none` | 这是 governance summary，不是 apply/rollback/promotion 执行器；`safe_for_scheduler=true` 只表示可定时生成 summary，不能触发上游 pipeline 或写 production/shadow；dashboard 只读 latest summary artifact，不运行 TRADING-019 或 018B-018F；`SAFETY_ANOMALY` 需要人工调查，不能通过编辑 summary 清除 |
| `data/derived/weight_iterations/governance/web/parameter_governance_web_view_YYYY-MM-DD.html/json` | `python scripts/render_parameter_governance_web_view.py --date YYYY-MM-DD` | TRADING-019 `parameter_governance_summary_YYYY-MM-DD.json` | 顶层 `task_id=TRADING-020`、`mode=parameter_governance_web_view_only`、`render_decision`（`RENDERED` / `SAFETY_BLOCKED` / `INPUT_MISSING` / `INPUT_INVALID` / `ERROR`）、input summary path/sha256、output HTML path、render summary、summary safety validation、pipeline contract 和 audit；HTML 静态展示 governance state、action、recommended action、production vs shadow weights、shadow review、promotion lifecycle timeline、pending items、safety boundary audit、findings/warnings/notes 和 artifact paths；固定 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`web_view_only=true`、`apply_executed_by_web_view=false`、`rollback_executed_by_web_view=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false` | daily task dashboard 的 Parameter Governance Web View 只读卡片；人工下钻阅读 parameter governance summary | 否，`production_effect=none` | 这是静态只读展示层，不生成新治理判断、不运行 TRADING-019 或 018B-018F、不触发 apply/rollback/broker/replay/trading；安全字段异常时输出 `SAFETY_BLOCKED` blocked report，不能当成正常治理状态 |
| `data/derived/weight_iterations/governance/digests/parameter_governance_daily_digest_YYYY-MM-DD.json/md` + `data/derived/weight_iterations/governance/digests/logs/parameter_governance_daily_digest_run_YYYY-MM-DD.json/md` | `python scripts/run_parameter_governance_daily_digest.py --date YYYY-MM-DD` | latest TRADING-019 `parameter_governance_summary_YYYY-MM-DD.json`、可选 latest TRADING-020 web view metadata、可选 latest 018C2 review、可选 latest 018F lifecycle audit | 顶层 `task_id=TRADING-021`、`mode=parameter_governance_daily_digest_only`、`digest_status`（`OK` / `WATCH` / `ACTION_REQUIRED` / `URGENT` / `INPUT_MISSING` / `INPUT_INVALID` / `SAFETY_BLOCKED` / `ERROR`）、`summary_level`、headline、input artifact path/sha256、governance snapshot、daily readout、production/shadow weight delta summary、pending items、alerts、recommended next steps、links、summary safety validation 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`digest_only=true`、`governance_only=true`、`apply_executed_by_digest=false`、`rollback_executed_by_digest=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Parameter Governance Daily Digest 只读卡片；每日快速阅读参数治理状态 | 否，`production_effect=none` | 这是每日阅读摘要，不是新的治理判断、不是 apply/rollback/promotion 执行器；`safe_for_scheduler=true` 只表示 digest 可以定时生成，不能触发上游 pipeline 或写 production/shadow；dashboard 只读 latest digest artifact，不运行 TRADING-021、020、019 或 018B-018F；`SAFETY_BLOCKED` 和 `URGENT` 需要人工处理，不能通过编辑 digest 清除 |
| `data/derived/operator_briefs/daily_trading_system_operator_brief_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/logs/daily_trading_system_operator_brief_run_YYYY-MM-DD.json/md` | `python scripts/run_daily_trading_system_operator_brief.py --date YYYY-MM-DD` | latest TRADING-021 `parameter_governance_daily_digest_YYYY-MM-DD.json`、可选但优先读取 latest TRADING-023 `pipeline_health_summary_YYYY-MM-DD.json`、可选但优先读取 latest TRADING-024 `data_freshness_summary_YYYY-MM-DD.json`、可选 market report、backtest / weight iteration artifacts | 顶层 `task_id=TRADING-022`、`mode=daily_trading_system_operator_brief_only`、`brief_status`（`OK` / `WATCH` / `ACTION_REQUIRED` / `URGENT` / `INPUT_MISSING` / `INPUT_INVALID` / `SAFETY_BLOCKED` / `ERROR`）、`summary_level`、headline、input artifact path/sha256、system snapshot、parameter governance、pipeline health（`status`、`health_status`、required/missing/stale/critical/warning pipeline counts、report path）、data freshness（`status`、`freshness_status`、required/missing/stale/critical/warning source counts、report path）、market report status、weight iteration status、pending manual actions、source-prefixed alerts、recommended next steps、links、input safety validation 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`operator_brief_only=true`、`read_only=true`、`apply_executed_by_operator_brief=false`、`rollback_executed_by_operator_brief=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Daily Trading System Operator Brief 只读卡片；每日系统级运维阅读入口 | 否，`production_effect=none` | 这是系统级只读 brief，不是 pipeline runner、不是 parameter governance 执行器、不是 market/backtest/scoring/data download 生成器；023/024 缺失只显示 `UNKNOWN`，不导致 `INPUT_MISSING`，但默认降级 `WATCH`；已加载 023/024 safety field 异常时输出 `SAFETY_BLOCKED`；dashboard 只读 latest operator brief artifact，不触发 TRADING-022/023/024/025 或任何上游/交易 pipeline |
| `data/derived/operator_briefs/notifications/operator_brief_notification_draft_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/notifications/email/operator_brief_email_draft_YYYY-MM-DD.md` + `data/derived/operator_briefs/notifications/chat/operator_brief_chat_draft_YYYY-MM-DD.md` + `data/derived/operator_briefs/notifications/mobile/operator_brief_mobile_summary_YYYY-MM-DD.md` + `data/derived/operator_briefs/notifications/logs/operator_brief_notification_draft_run_YYYY-MM-DD.json/md` | `python scripts/generate_operator_brief_notification_draft.py --date YYYY-MM-DD` | latest TRADING-022 operator brief；可选 latest TRADING-021 digest、TRADING-023 health summary、TRADING-024 freshness summary 和 TRADING-026 scheduler dry run path/checksum | 顶层 `task_id=TRADING-030`、`mode=operator_brief_notification_draft_only`、`draft_status`（`GENERATED` / `GENERATED_WITH_WARNINGS` / `INPUT_MISSING` / `INPUT_INVALID` / `SAFETY_BLOCKED` / `ERROR`）、`notification_severity`（`NORMAL` / `WATCH` / `ACTION` / `URGENT` / `BLOCKED` / `UNKNOWN`）、headline、input artifact path/sha256、source snapshot、draft output paths、email subject、content summary、redaction warnings、manual review checklist、safety validation 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`notification_draft_only=true`、`read_only=true`、`email_sent=false`、`gmail_draft_created=false`、`slack_sent=false`、`discord_sent=false`、`mobile_push_sent=false`、`operator_brief_executed_by_notification_draft=false`、`pipelines_executed_by_notification_draft=false`、`data_downloaded_by_notification_draft=false`、`apply_executed_by_notification_draft=false`、`rollback_executed_by_notification_draft=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Operator Brief Notification Draft 只读卡片；人工复制发送前的通知草稿审查入口 | 否，`production_effect=none` | 这是 notification draft generator，不是 delivery system；不会发送 email、创建 Gmail draft、调用 Slack / Discord webhook 或 mobile push；`safe_for_scheduler=true` 只表示草稿生成器可被定时生成，不表示通知可自动发送；疑似 `api_key` / `secret` / `token` / `password` / `credential` / `account_id` 值会 redacted 并记录 warning；dashboard 只读 latest TRADING-030 metadata artifact，不运行 generator 或任何通知/交易/数据 pipeline |
| `data/derived/operator_briefs/notifications/delivery_preflight/operator_brief_notification_delivery_preflight_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/notifications/delivery_preflight/logs/operator_brief_notification_delivery_preflight_run_YYYY-MM-DD.json/md` | `python scripts/run_operator_brief_notification_delivery_preflight.py --date YYYY-MM-DD` | latest TRADING-030 notification draft metadata；metadata 声明的 email/chat/mobile draft；可选 recipient config、channel config 和 approval policy config | 顶层 `task_id=TRADING-031`、`mode=operator_brief_notification_delivery_preflight_only`、`preflight_status`（`PASS` / `PASS_WITH_WARNINGS` / `BLOCKED` / `INPUT_MISSING` / `INPUT_INVALID` / `SAFETY_BLOCKED` / `ERROR`）、`delivery_readiness`（`READY_FOR_MANUAL_REVIEW` / `NEEDS_APPROVAL` / `BLOCKED` / `SAFETY_BLOCKED` / `UNKNOWN`）、`notification_severity`、input artifact path/sha256、draft validation、approval validation、channel readiness、safety validation、alerts、recommended next steps、manual review instructions 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`notification_delivery_preflight_only=true`、`read_only=true`、`email_sent=false`、`gmail_draft_created=false`、`gmail_draft_modified=false`、`slack_sent=false`、`discord_sent=false`、`webhook_called=false`、`mobile_push_sent=false`、`operator_brief_executed_by_delivery_preflight=false`、`notification_draft_executed_by_delivery_preflight=false`、`pipelines_executed_by_delivery_preflight=false`、`data_downloaded_by_delivery_preflight=false`、`apply_executed_by_delivery_preflight=false`、`rollback_executed_by_delivery_preflight=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Operator Brief Notification Delivery Preflight 只读卡片；人工发送或未来 Gmail draft / webhook dry-run 前的 preflight 审查入口 | 否，`production_effect=none` | 这是 delivery preflight，不是 delivery system；不会发送 email、创建或修改 Gmail draft、调用 Slack / Discord webhook 或 mobile push；`safe_for_scheduler=true` 只表示 preflight 本身可定期运行，不表示允许自动发送；检测到未 redacted `api_key` / `apiKey` / `secret` / `token` / `password` / `credential` / `broker_token` / `account_id` / `private_key` 会 `SAFETY_BLOCKED`；dashboard 只读 latest TRADING-031 artifact，不运行 preflight script、notification generator 或任何通知/交易/数据 pipeline |
| `data/derived/operator_briefs/notifications/dispatch_preview/operator_brief_notification_dispatch_preview_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/notifications/dispatch_preview/latest.json` + `data/derived/operator_briefs/notifications/dispatch_preview/latest.md` + `data/derived/operator_briefs/notifications/dispatch_preview/run.log` | `python scripts/run_operator_brief_notification_dispatch_preview.py --date YYYY-MM-DD` | latest TRADING-031 delivery preflight；latest TRADING-022 operator brief JSON/Markdown；TRADING-031 声明的 TRADING-030 notification draft metadata 和 email/chat/mobile draft 内容 | 顶层 `task_id=TRADING-032`、`mode=dry_run`、`metadata`、`input_refs`、`preflight_summary`、`dispatch_preview`、`safety`、`decision`、channel plan、masked target、subject/title/body excerpt、`body_markdown`、`final_status` / `dispatch_status`（`WOULD_SEND` / `NEEDS_APPROVAL` / `SAFETY_BLOCKED` / `BLOCKED` / `NOOP`）、latest copies 和 run.log；固定 `production_effect=none`、`manual_review_only=true`、`dispatch_preview_only=true`、`read_only=true`、`external_side_effects=false`、`network_access_required=false`、`secrets_required=false`、`email_sent=false`、`gmail_draft_created=false`、`gmail_draft_modified=false`、`slack_sent=false`、`telegram_sent=false`、`discord_sent=false`、`webhook_called=false`、`mobile_push_sent=false`、`operator_brief_executed_by_dispatch_preview=false`、`notification_draft_executed_by_dispatch_preview=false`、`delivery_preflight_executed_by_dispatch_preview=false`、`pipelines_executed_by_dispatch_preview=false`、`data_downloaded_by_dispatch_preview=false`、`apply_executed_by_dispatch_preview=false`、`rollback_executed_by_dispatch_preview=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false` | daily task dashboard 的 Operator Brief Notification Dispatch Preview 只读卡片；人工审核真实 delivery 前的 dry-run dispatch plan | 否，`production_effect=none` | 这是 dry-run dispatch preview，不是真实发送；不会发送 email、创建 Gmail draft、调用 SMTP、Slack、Telegram、Discord、webhook 或 mobile push；不会读取 `.env` secrets 或私密邮箱；recipient 默认脱敏；路径逃逸 repo root 或疑似 token/private key/password 输出 `SAFETY_BLOCKED`；dashboard 只读 latest TRADING-032 artifact，不运行 preview script、delivery preflight、notification generator 或任何通知/交易/数据 pipeline |
| `data/derived/operator_briefs/notifications/approval_gate/operator_brief_notification_approval_gate_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/notifications/approval_gate/latest.json` + `data/derived/operator_briefs/notifications/approval_gate/latest.md` + `data/derived/operator_briefs/notifications/approval_gate/run.log` | `python scripts/run_operator_brief_notification_approval_gate.py --date YYYY-MM-DD` | latest TRADING-032 dispatch preview；可选本地 approval marker `data/derived/operator_briefs/notifications/approval_gate/approval_marker.json` 或 `approval_gate/approvals/operator_brief_notification_approval_YYYY-MM-DD.json` | 顶层 `task_id=TRADING-033`、`mode=approval_gate`、`metadata`、`input_refs`、`dispatch_preview_summary`、`approval_marker_summary`、`hashes.dispatch_preview_hash`、`decision.approval_gate_status`（`APPROVED` / `APPROVAL_REQUIRED` / `APPROVAL_EXPIRED` / `APPROVAL_MISMATCH` / `SAFETY_BLOCKED` / `BLOCKED` / `NOOP`）、`allowed_to_enter_dispatch`、latest copies 和 run.log；固定 `production_effect=none`、`manual_review_only=true`、`approval_gate_only=true`、`read_only=true`、`external_side_effects=false`、`network_access_required=false`、`secrets_required=false`、`email_sent=false`、`gmail_draft_created=false`、`gmail_draft_modified=false`、`slack_sent=false`、`telegram_sent=false`、`discord_sent=false`、`webhook_called=false`、`mobile_push_sent=false`、`operator_brief_executed_by_approval_gate=false`、`notification_draft_executed_by_approval_gate=false`、`delivery_preflight_executed_by_approval_gate=false`、`dispatch_preview_executed_by_approval_gate=false`、`pipelines_executed_by_approval_gate=false`、`data_downloaded_by_approval_gate=false`、`apply_executed_by_approval_gate=false`、`rollback_executed_by_approval_gate=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false` | daily task dashboard 的 Operator Brief Notification Approval Gate 只读卡片；未来真实 dispatch 前唯一可信审批门控 artifact | 否，`production_effect=none` | 这是 approval gate，不是真实发送；不会发送 email、创建 Gmail draft、调用 SMTP、Slack、Telegram、Discord、webhook 或 mobile push；不会读取 `.env` secrets 或私密邮箱；approval marker 不能覆盖 `SAFETY_BLOCKED`、hash mismatch 或 expired approval；dashboard 只读 latest TRADING-033 artifact，不运行 approval gate、dispatch preview、delivery preflight、notification generator 或任何通知/交易/数据 pipeline |
| `data/derived/operator_briefs/notifications/draft_dispatch/operator_brief_notification_draft_dispatch_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/notifications/draft_dispatch/latest.json` + `data/derived/operator_briefs/notifications/draft_dispatch/latest.md` + `data/derived/operator_briefs/notifications/draft_dispatch/run.log` | `python scripts/run_operator_brief_notification_draft_dispatch.py --date YYYY-MM-DD` | latest TRADING-033 approval gate；latest TRADING-032 dispatch preview | 顶层 `task_id=TRADING-034`、`mode=draft_dispatch`、`metadata`、`input_refs`、`approval_gate_summary`、`dispatch_preview_summary`、`draft.channels`、`draft.message`、`hashes.dispatch_preview_hash`、`hashes.draft_hash`、`decision.final_status`（`DRAFT_READY` / `APPROVAL_REQUIRED` / `APPROVAL_EXPIRED` / `APPROVAL_MISMATCH` / `SAFETY_BLOCKED` / `BLOCKED` / `NOOP`）、`ready_for_actual_dispatch`、latest copies 和 run.log；固定 `production_effect=none`、`manual_review_only=true`、`draft_dispatch_only=true`、`read_only=true`、`external_side_effects=false`、`network_access_required=false`、`secrets_required=false`、`email_sent=false`、`gmail_draft_created=false`、`gmail_draft_modified=false`、`smtp_called=false`、`slack_sent=false`、`telegram_sent=false`、`discord_sent=false`、`webhook_called=false`、`mobile_push_sent=false`、`operator_brief_executed_by_draft_dispatch=false`、`notification_draft_executed_by_draft_dispatch=false`、`delivery_preflight_executed_by_draft_dispatch=false`、`dispatch_preview_executed_by_draft_dispatch=false`、`approval_gate_executed_by_draft_dispatch=false`、`pipelines_executed_by_draft_dispatch=false`、`data_downloaded_by_draft_dispatch=false`、`apply_executed_by_draft_dispatch=false`、`rollback_executed_by_draft_dispatch=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false` | daily task dashboard 的 Operator Brief Notification Draft Dispatch 只读卡片；未来 actual dispatch 的唯一输入 artifact | 否，`production_effect=none` | 这是本地最终发送草稿，不是真实发送；不会发送 email、创建 Gmail draft、调用 Gmail API、SMTP、Slack、Telegram、Discord、webhook 或 mobile push；不会读取 `.env` secrets 或私密邮箱；`DRAFT_READY` 必须来自 `APPROVED` gate 且 preview hash 对齐；safety block、未脱敏 target、hash mismatch 或未审批不能生成 ready draft；dashboard 只读 TRADING-034 `latest.json`，不运行 draft dispatch、approval gate、dispatch preview、delivery preflight、notification generator 或任何通知/交易/数据 pipeline |
| `data/derived/operator_briefs/notifications/delivery_audit/notification_delivery_audit_summary_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/notifications/delivery_audit/logs/notification_delivery_audit_summary_run_YYYY-MM-DD.json/md` | `python scripts/run_notification_delivery_audit_summary.py --date YYYY-MM-DD` | latest TRADING-030 notification draft metadata；latest TRADING-031 delivery preflight；TRADING-034 `draft_dispatch/latest.json` 和 dated draft dispatch artifact；可选 email/chat/mobile draft files | 顶层 `task_id=TRADING-035`、`mode=notification_delivery_audit_summary_only`、`audit_status`（`PASS` / `PASS_WITH_WARNINGS` / `INCOMPLETE` / `MISMATCH` / `SAFETY_BLOCKED` / `ERROR`）、`notification_lifecycle_status`（`DRAFT_ONLY` / `PREFLIGHT_READY` / `DRAFT_READY` / `BLOCKED` / `APPROVAL_MISMATCH` / `SAFETY_BLOCKED` / `INCOMPLETE` / `UNKNOWN`）、input artifact path/sha256、artifact chain、draft summary、preflight summary、dispatch summary、external side effect audit、safety validation、alerts、recommended next steps、manual review instructions 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`notification_delivery_audit_only=true`、`read_only=true`、`safe_for_scheduler=true`、`email_sent=false`、`gmail_draft_created=false`、`gmail_draft_modified=false`、`slack_sent=false`、`discord_sent=false`、`webhook_called=false`、`mobile_push_sent=false`、`notification_draft_executed_by_audit=false`、`delivery_preflight_executed_by_audit=false`、`draft_dispatch_executed_by_audit=false`、`operator_brief_executed_by_audit=false`、`pipelines_executed_by_audit=false`、`data_downloaded_by_audit=false`、`apply_executed_by_audit=false`、`rollback_executed_by_audit=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false` | daily task dashboard 的 Notification Delivery Audit Summary 只读卡片；人工处理或未来 send-related task 前的通知生命周期追溯记录 | 否，`production_effect=none` | 这是 delivery audit summary，不是真实发送、不创建 Gmail draft、不调用 webhook、不运行 TRADING-030/031/034/022 或 market/backtest/scoring/data download/broker/replay/trading；`safe_for_scheduler=true` 只表示 audit summary 自身可定时生成，不表示允许自动发送通知；dashboard 只读 TRADING-035 artifact，不运行 035 script 或任何上游/发送/交易 pipeline |
| `outputs/notification_delivery_failure_classification/notification_delivery_failure_classification_YYYY-MM-DD.json/md/log` | `python scripts/run_notification_delivery_failure_classification.py [--audit-summary ...] [--output-dir ...] [--as-of-date YYYY-MM-DD] [--fail-on-critical]` | latest 或显式 TRADING-035 notification delivery audit summary；默认 source finder 支持 `data/derived/operator_briefs/notifications/delivery_audit/` 和 `outputs/notification_delivery_audit_summary/` | 顶层 `task_id=TRADING-036`、`report_type=notification_delivery_failure_classification`、`mode=read_only`、`production_effect=none`、`manual_review_only=true`、`source_audit`、`classification_summary.overall_status`、`total_failures`、`highest_severity`、`requires_manual_review`、`safe_to_retry`、`blocks_notification_chain`、`failure_categories`（`TRANSIENT_DELIVERY_FAILURE` / `CONFIGURATION_FAILURE` / `SAFETY_BLOCKED` / `CONTENT_MISMATCH` / `MISSING_ARTIFACT` / `UNKNOWN`）、`retry_readiness`、`recommended_actions`、`safety_invariants` 和 run log；固定 `notification_delivery_failure_classification_only=true`、`read_only=true`、全部 sent/webhook/retry/state mutation/upstream execution/data/apply/rollback/broker/replay/trading flags=false | daily task dashboard 的 Notification Delivery Failure Classification 只读卡片；人工复核失败类型和未来 TRADING-037 retry candidate queue 的输入候选 | 否，`production_effect=none` | 这是 failure classification / retry readiness 报告，不是真实发送、不创建 Gmail draft、不调用 webhook、不自动 retry、不修改 delivery state、不修改 production 参数、不运行 TRADING-035 或任何 notification 上游；dashboard 只读 TRADING-036 JSON，不运行 classifier 或任何发送/重试/交易 pipeline；`safe_to_retry` 不是自动发送授权 |
| `outputs/retry_candidate_queue/retry_candidate_queue_YYYY-MM-DD.json/md/log` | `python scripts/run_retry_candidate_queue.py [--classification-report ...] [--output-dir ...] [--as-of-date YYYY-MM-DD] [--fail-on-safety-blocked]` | latest 或显式 TRADING-036 notification delivery failure classification JSON | 顶层 `task_id=TRADING-037`、`report_type=retry_candidate_queue`、`mode=read_only`、`production_effect=none`、`manual_review_only=true`、`source_classification`、`queue_summary.queue_status`（`EMPTY` / `PENDING_APPROVAL` / `BLOCKED` / `SAFETY_BLOCKED` / `SOURCE_UNAVAILABLE`）、`candidate_queue`、`blocked_items`、`approval_gate`、`recommended_actions`、`safety_invariants` 和 run log；固定 `retry_candidate_queue_only=true`、`read_only=true`、全部 sent/webhook/retry/state mutation/approval state/upstream execution/data/apply/rollback/broker/replay/trading flags=false | daily task dashboard 的 Retry Candidate Queue 只读卡片；未来 manual approval record / retry dry-run layer 的输入候选 | 否，`production_effect=none` | TRADING-037 只生成只读 retry candidate queue 和 manual approval gate report；`PENDING_APPROVAL` 不是 retry 授权，`approval_gate.retry_execution_allowed=false`；不会发送 notification、执行 retry、修改 delivery state、修改 approval state、修改 TRADING-035/036 历史 artifact 或 production 参数；dashboard 只读 TRADING-037 JSON，不运行 queue generator、classifier 或任何发送/重试/交易 pipeline |
| `inputs/manual_retry_approvals/manual_retry_approval_YYYY-MM-DD.json` | 人工创建或手工编辑；TRADING-038 仅读取 | TRADING-037 retry candidate queue 中的 candidate ids | `metadata.task_id=TRADING-038`、`approval_record_type=manual_retry_approval`、`approval_scope.source_task_id=TRADING-037`、`approved_candidates[].approval_status=APPROVED_FOR_DRY_RUN`、`rejected_candidates[].approval_status=REJECTED`、`safety_constraints.dry_run_only=true`、`real_retry_allowed=false`、`external_delivery_allowed=false`、`production_state_mutation_allowed=false` | `python scripts/run_retry_execution_dry_run.py` 的只读输入 | 否，人工输入；程序不得修改 | approval record 不是自动审批、不是外部系统记录、不是 retry 执行授权；TRADING-038 不创建、不编辑、不反写该文件 |
| `outputs/retry_execution_dry_run/retry_execution_dry_run_YYYY-MM-DD.json/md/log` | `python scripts/run_retry_execution_dry_run.py [--queue-report ...] [--approval-record ...] [--output-dir ...] [--as-of-date YYYY-MM-DD] [--fail-on-safety-blocked] [--fail-on-approval-mismatch]` | latest 或显式 TRADING-037 retry candidate queue JSON；可选人工 `inputs/manual_retry_approvals/manual_retry_approval_YYYY-MM-DD.json` 或显式 approval record | 顶层 `task_id=TRADING-038`、`report_type=retry_execution_dry_run`、`mode=dry_run_only`、`production_effect=none`、`manual_review_only=true`、`source_queue`、`approval_record`、`dry_run_summary.dry_run_status`（`SOURCE_QUEUE_UNAVAILABLE` / `NOTHING_TO_DRY_RUN` / `WAITING_FOR_MANUAL_APPROVAL` / `READY_FOR_DRY_RUN` / `APPROVAL_MISMATCH` / `SAFETY_BLOCKED`）、`simulated_retry_actions`、`blocked_items`、`recommended_actions` 和 `safety_invariants`；固定 `retry_execution_dry_run_only=true`、`dry_run_only=true`、`read_only=true`、全部 sent/webhook/retry/state mutation/approval record mutation/upstream execution/data/apply/rollback/broker/replay/trading flags=false；每个 simulated action 固定 `actual_retry_executed=false`、`external_delivery_executed=false`、`state_mutation_executed=false` | daily task dashboard 的 Retry Execution Dry Run 只读卡片；未来 disabled-by-default retry executor skeleton 的人工复核输入 | 否，`production_effect=none` | TRADING-038 只记录人工 dry-run approval 意图并模拟 would-retry action；不会发送 notification、执行 retry、修改 delivery state、修改 approval record/state、修改 TRADING-035/036/037 历史 artifact 或 production 参数；`READY_FOR_DRY_RUN` 不是真实 retry 授权，dashboard 只读 TRADING-038 JSON，不运行 dry-run generator 或任何发送/重试/交易 pipeline |
| `data/derived/operator_briefs/scheduler_dry_run/daily_operator_brief_scheduler_dry_run_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/scheduler_dry_run/logs/daily_operator_brief_scheduler_dry_run_log_YYYY-MM-DD.json/md` | `python scripts/run_daily_operator_brief_scheduler_dry_run.py --date YYYY-MM-DD` | latest TRADING-021 `parameter_governance_daily_digest_YYYY-MM-DD.json`、可选但优先读取 latest TRADING-023 `pipeline_health_summary_YYYY-MM-DD.json`、可选但优先读取 latest TRADING-024 `data_freshness_summary_YYYY-MM-DD.json`、可选 latest TRADING-022 operator brief | 顶层 `task_id=TRADING-026`、`mode=daily_operator_brief_scheduler_dry_run_only`、`dry_run_decision`（`READY` / `READY_WITH_WARNINGS` / `NOT_READY` / `SAFETY_BLOCKED` / `ERROR`）、`dry_run_status`（`OK` / `WATCH` / `ACTION_REQUIRED` / `SAFETY_BLOCKED` / `ERROR`）、`summary_level`、headline、schedule plan、input artifact path/sha256/freshness、dependency check、safety check、expected operator brief behavior、alerts、recommended next steps、scheduler contract 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`scheduler_dry_run_only=true`、`read_only=true`、`scheduler_created=false`、`operator_brief_executed_by_scheduler_dry_run=false`、`pipelines_executed_by_scheduler_dry_run=false`、`data_downloaded_by_scheduler_dry_run=false`、`apply_executed_by_scheduler_dry_run=false`、`rollback_executed_by_scheduler_dry_run=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Daily Operator Brief Scheduler Dry Run 只读卡片；未来是否适合人工安装 scheduler 的输入审查 | 否，`production_effect=none` | 这是 scheduler dry-run，不是 scheduler installer、不是 operator brief runner、不是上游 pipeline runner；`safe_for_scheduler=true` 只表示 dry-run 本身可定期执行，不表示真实 scheduler 已创建或获准；023/024 缺失或 stale 只进入 `READY_WITH_WARNINGS` / `WATCH`，021 缺失或 stale 进入 `NOT_READY`；任何输入 safety field 异常进入 `SAFETY_BLOCKED`；dashboard 只读 latest TRADING-026 artifact，不运行 018B-025、TRADING-026 script、operator brief 或任何上游/交易 pipeline |
| `docs/runbooks/daily_operator_brief_scheduler_manual_installation.md` + `docs/requirements/daily_operator_brief_scheduler_manual_installation_YYYY-MM-DD.md` | TRADING-027 documentation-only change | TRADING-021/022/023/024/026 的既有 runbook、artifact contract 和 owner 安全边界要求；不读取 runtime data | Manual scheduler installation guide、pre-installation checklist、safety checklist、Windows Task Scheduler / cron / GitHub Actions documentation-only examples、dry-run decision handling、manual validation、failure handling、disable / pause steps；固定声明 `production_effect=none`、`manual_review_only=true`、`runbook_only=true`、`scheduler_created=false`、`operator_brief_executed_by_runbook=false`、`pipelines_executed_by_runbook=false`、`data_downloaded_by_runbook=false`、`apply_executed_by_runbook=false`、`rollback_executed_by_runbook=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false` | 人工安装 Daily Operator Brief scheduler 前的审查入口；无 dashboard runtime artifact | 否，`production_effect=none` | TRADING-027 produces documentation artifacts only. No runtime data artifact is produced. It does not create Windows Task Scheduler tasks, cron jobs, GitHub Actions workflows, scheduler scripts, market/backtest/scoring/data download pipeline outputs, apply/rollback results, broker actions, replay runs, or trading execution. |
| `data/derived/operator_briefs/scheduler_templates/daily_operator_brief_scheduler_templates_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/scheduler_templates/windows/*.template` + `cron/*.template` + `github_actions/*.template` | `python scripts/generate_daily_operator_brief_scheduler_templates.py --date YYYY-MM-DD` | repo root、Python path、expected run time、timezone、operator brief script、scheduler dry-run script；不读取 runtime market/backtest/scoring/data artifacts | 顶层 `task_id=TRADING-028`、`mode=daily_operator_brief_scheduler_template_generation_only`、`template_generation_status`（`GENERATED` / `GENERATED_WITH_WARNINGS` / `SAFETY_BLOCKED` / `ERROR`）、`output_templates`、`generated_template_count`、`safety_validation`、`manual_review_required`、`template_inputs` 和 audit；固定 `production_effect=none`、`manual_review_only=true`、`scheduler_template_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`operator_brief_executed_by_template_generator=false`、`pipelines_executed_by_template_generator=false`、`data_downloaded_by_template_generator=false`、`apply_executed_by_template_generator=false`、`rollback_executed_by_template_generator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Scheduler Configuration Templates 只读卡片；TRADING-027 人工安装前的模板审查输入 | 否，`production_effect=none` | 这是 template generator，不是 scheduler installer；所有配置输出必须以 `.template` 结尾；`safe_for_scheduler=true` 只表示 generator 本身可运行，不表示生成模板已安装或启用；生成器不写 `.github/workflows/*.yml`、不写 crontab、不调用 `schtasks`、不运行 TRADING-021/022/023/024/026 或任何 market/backtest/scoring/data download/broker/replay/trading pipeline；dashboard 只读 latest TRADING-028 metadata artifact，不运行生成器 |
| `data/derived/operator_briefs/scheduler_template_validation/daily_operator_brief_scheduler_template_validation_YYYY-MM-DD.json/md` + `data/derived/operator_briefs/scheduler_template_validation/logs/daily_operator_brief_scheduler_template_validation_run_YYYY-MM-DD.json/md` | `python scripts/validate_daily_operator_brief_scheduler_templates.py --date YYYY-MM-DD` | TRADING-028 template metadata JSON 和其中声明的 Windows XML、PowerShell、batch、cron、GitHub Actions `.template` 文件；不读取 runtime market/backtest/scoring/data artifacts | 顶层 `task_id=TRADING-029`、`mode=daily_operator_brief_scheduler_template_validation_only`、`validation_status`（`PASS` / `PASS_WITH_WARNINGS` / `FAIL` / `SAFETY_BLOCKED` / `INPUT_MISSING` / `INPUT_INVALID` / `ERROR`）、`template_results`、`coverage`、`safety_validation`、`alerts`、`manual_review_required` 和 audit；固定 `production_effect=none`、`manual_review_only=true`、`scheduler_template_validation_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`templates_executed_by_validator=false`、`operator_brief_executed_by_validator=false`、`pipelines_executed_by_validator=false`、`data_downloaded_by_validator=false`、`apply_executed_by_validator=false`、`rollback_executed_by_validator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Scheduler Template Validation Report 只读卡片；TRADING-027 人工安装前的独立模板静态验证输入 | 否，`production_effect=none` | 这是 static validator，不是 scheduler installer、不是 template runner、不是 operator brief runner；`safe_for_scheduler=true` 只表示 validator 本身可运行，不表示模板已安装或启用；validator 不写 `.github/workflows/*.yml`、不写 crontab、不调用 `schtasks`、不运行 TRADING-021/022/023/024/026/028 或任何 market/backtest/scoring/data download/broker/replay/trading pipeline；dashboard 只读 latest TRADING-029 validation artifact，不运行 validator |
| `data/derived/pipeline_health/pipeline_health_summary_YYYY-MM-DD.json/md` + `data/derived/pipeline_health/logs/pipeline_health_summary_run_YYYY-MM-DD.json/md` | `python scripts/run_pipeline_health_summary.py --date YYYY-MM-DD` | 既有 TRADING-018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/020/021/022 artifacts；默认只扫描 required registry，`--include-optional-pipelines` 扫描 optional promotion/lifecycle artifacts | 顶层 `task_id=TRADING-023`、`mode=pipeline_health_summary_only`、`health_status`（`OK` / `WATCH` / `ACTION_REQUIRED` / `CRITICAL` / `INCOMPLETE` / `ERROR`）、`summary_level`、headline、coverage、pipeline_results、missing_required_pipelines、stale_pipelines、critical_pipelines、warning_pipelines、operator_brief_integration、alerts、recommended_next_steps、safety_validation 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`pipeline_health_only=true`、`read_only=true`、`pipelines_executed_by_health_check=false`、`apply_executed_by_health_check=false`、`rollback_executed_by_health_check=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Pipeline Health Summary 只读卡片；TRADING-022 operator brief 的 pipeline health 输入 | 否，`production_effect=none` | 这是 artifact health 读取层，不是 pipeline runner；只按 glob/status/freshness/safety 字段扫描已有文件，不运行 018B-022、market、backtest、scoring、broker、replay 或 trading；018E2 apply result 和 018E3 rollback result 可包含其自身执行结果，但只读任务出现 execution true 或异常 production_effect 必须标记 CRITICAL；dashboard 只读 latest TRADING-023 artifact，不运行 health summary script |
| `data/derived/data_freshness/data_freshness_summary_YYYY-MM-DD.json/md` + `data/derived/data_freshness/logs/data_freshness_summary_run_YYYY-MM-DD.json/md` | `python scripts/run_data_freshness_summary.py --date YYYY-MM-DD` | 既有 TRADING-021 digest、TRADING-023 pipeline health summary、TRADING-022 operator brief、TRADING-019 governance summary、current shadow weights、018C2 review、018F lifecycle audit、可选 market/backtest/price/news 或 signal cache artifacts；默认只扫描 required registry，`--include-optional-sources` 扫描 optional sources | 顶层 `task_id=TRADING-024`、`mode=data_freshness_summary_only`、`freshness_status`（`OK` / `WATCH` / `STALE` / `MISSING` / `CRITICAL` / `ERROR`）、`summary_level`、headline、coverage、source_results、missing_required_sources、stale_sources、critical_sources、warning_sources、operator_brief_integration、alerts、recommended_next_steps、safety_validation 和 run log；固定 `production_effect=none`、`manual_review_only=true`、`data_freshness_only=true`、`read_only=true`、`data_downloaded_by_freshness_check=false`、`pipelines_executed_by_freshness_check=false`、`apply_executed_by_freshness_check=false`、`rollback_executed_by_freshness_check=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true` | daily task dashboard 的 Data Freshness Summary 只读卡片；TRADING-022 operator brief 的 data freshness 输入 | 否，`production_effect=none` | 这是数据新鲜度读取层，不是数据下载器、不是 pipeline runner；只按 glob、JSON 日期字段、文件名日期、modified time、status field 和 safety field 扫描已有文件，不运行 018B-023、market、backtest、scoring、data download、broker、replay 或 trading；dashboard 只读 latest TRADING-024 artifact，不运行 freshness summary script |

## Backtest / feedback 产物

| Artifact | 由谁生成 | 上游输入 | 关键字段或内容 | 下游使用 | 是否影响 production | 常见误解 |
|---|---|---|---|---|---|---|
| `outputs/backtests/backtest_YYYY-MM-DD_YYYY-MM-DD.md` | `aits backtest` | validated data、PIT inputs、scoring rules | regime、date range、returns、drawdown、benchmark comparison、limitations | 投研复核、parameter replay | 否，回测不自动改规则 | 默认结论窗口应声明 `ai_after_chatgpt` |
| `outputs/backtests/backtest_daily_YYYY-MM-DD_YYYY-MM-DD.csv` | `aits backtest` | daily scoring context | signal date、score、target exposure、next return、costs、gates | robustness、gate attribution、parameter replay | 否 | 不是 production ledger |
| `outputs/backtests/backtest_input_coverage_*.csv` | `aits backtest` | PIT slices、quality reports | source coverage、missing inputs、warning status | audit、candidate veto | 否 | 高收益但覆盖不足不应直接晋级 |
| `outputs/reports/parameter_replay_YYYY-MM-DD.md/json` | `aits feedback build-parameter-replay` | robustness summary | scenario deltas、materiality、limitations | parameter candidate ledger | 否 | as-if replay 不是 approved overlay |
| `data/processed/parameter_candidates.json` | `aits feedback build-parameter-candidates` | parameter replay | candidate id、trial metrics、veto reasons、governance status | parameter governance、shadow runner | 否 | candidate ready 不等于 owner approval |
| `outputs/reports/market_feedback_optimization_YYYY-MM-DD.md` | `aits feedback optimize-market-feedback` | outcomes、causal chains、candidates、overlays | readiness、sample floors、open gaps、next actions | 复盘、治理 | 否 | readiness 不是生产配置变更 |
| `data/processed/prediction_outcomes.csv` | `aits feedback calibrate-predictions` | prediction ledger、prices | horizon、available/pending/missing、benchmark excess | shadow maturity、promotion review | 否 | outcome 是后验观察，不能改写原 prediction |

## Shadow / validation-only 参数搜索产物

| Artifact | 由谁生成 | 上游输入 | 关键字段或内容 | 下游使用 | 是否影响 production | 常见误解 |
|---|---|---|---|---|---|---|
| `config/weights/shadow_weight_profiles.yaml` | 人工维护 / 系统建议 | production weight profile | validation-only profile、weights、metadata | shadow observation | 否，`production_effect=none` | 不替换 `weight_profile_current.yaml` |
| `config/weight_adjustment_candidate_policy.yaml` | 人工维护 / 系统建议 | paper signal quality、shadow impact、continuous replay 和 production/shadow profile 复核要求 | max single-day weight delta、total weight tolerance、sample floors、synthetic snapshot cap、required validations 和 safety boundaries | `run_weight_adjustment_candidates.py` 的 candidate gate | 否，`production_effect=none` | policy 只约束 observe-only 候选生成，不批准修改 production profile |
| `config/weight_candidate_evaluation_policy.yaml` | 人工维护 / 系统建议 | weight adjustment candidates、paper signal quality、shadow impact、continuous replay 和 profile metadata 复核要求 | sample floors、synthetic snapshot cap、OHLC coverage、reconciliation PASS floor、max drawdown / exposure / concentration delta gate、required validations 和 safety boundaries | `run_weight_candidate_evaluation.py` 的 evaluation gate | 否，`production_effect=none` | policy 只约束 observe-only 候选评估，不批准 production promotion 或交易 |
| `config/weight_promotion_gate_policy.yaml` | 人工维护 / 系统建议 | weight candidate evaluation、paper signal quality、shadow impact、continuous replay、daily data gate 和 profile metadata 复核要求 | sample floors、filled floor、synthetic snapshot cap、OHLC coverage、reconciliation PASS floor、risk delta gate、stable improvement window floor、required validations 和 manual-review-only safety boundaries | `run_weight_promotion_gate.py` 的 promotion gate | 否，`production_effect=none` | policy 只决定 candidate 是否可进入人工复核，不批准生产修改、交易或 live readiness |
| `config/daily_shadow_weight_iteration_policy.yaml` | 人工维护 / 系统建议 | TRADING-015/016/017/018/018A artifacts 和 current shadow state | max abs/relative daily delta、minimum adjustment confidence、confidence defaults、conservative default weights、normalization target、min/max weight、required inputs 和 safety boundaries | `run_daily_shadow_weight_iteration.py` 的 shadow-only update gate | 否，`production_effect=none` | policy 只约束 shadow current state 的小步迭代，不批准 production promotion、production profile 修改或交易；policy 缺失时不得更新 shadow current |
| `config/weights/shadow_position_gate_profiles.yaml` | 人工维护 / 系统建议 | scoring/portfolio gate policy | validation-only gate cap overrides | shadow observation | 否 | relaxed gate 不是 production gate 放宽 |
| `config/weights/shadow_parameter_search_space.yaml` | 人工维护 | production weights/gates | weight grid、gate cap grid、bounds | parameter search | 否 | 搜索空间内最优不是无限连续空间最优 |
| `config/weights/shadow_parameter_objective.yaml` | 人工维护 | validation policy | objective、sample floor、regularization、eligibility | parameter search ranking | 否 | objective 排名不是 promotion contract |
| `outputs/parameter_search/<run_id>/manifest.json` | `aits feedback search-shadow-parameters` | snapshots、prices、search configs | input checksum、resolver version、git status、run params | reproducibility、promotion contract | 否 | manifest 不批准上线 |
| `outputs/parameter_search/<run_id>/trials.csv` | `search-shadow-parameters` | weight/gate candidates | trial id、metrics、objective、eligibility、regularization | analysis、Pareto front | 否 | trial 多不代表统计独立样本多 |
| `outputs/parameter_search/<run_id>/pareto_front.csv` | `search-shadow-parameters` | trials | non-dominated trials | review | 否 | Pareto front 不等于可上线名单 |
| `outputs/parameter_search/<run_id>/best_profiles.yaml` | `search-shadow-parameters` | eligible/diagnostic trials | candidate profile snapshot | review / forward shadow prep | 否 | 不应复制覆盖生产配置 |
| `outputs/parameter_search/<run_id>/search_report.md` | `search-shadow-parameters` | manifest、trials、attribution | eligible status、diagnostic-leading trial、factorial attribution、cap-level attribution、position changes | 人读参数搜索报告 | 否，`production_effect=none` | diagnostic-leading 只是当前样本解释，不是 production 建议 |
| `outputs/parameter_search/<run_id>/shadow_parameter_promotion_<run_id>.md/json` | `aits feedback evaluate-shadow-parameter-promotion` | search bundle、promotion contract | NOT_PROMOTABLE / READY_FOR_FORWARD_SHADOW / READY_FOR_OWNER_REVIEW、blockers | promotion review | 否，直到 owner approval 仍不改 production | `READY_FOR_FORWARD_SHADOW` 仍不是上线批准 |
| `data/processed/shadow_iteration_registry.csv` + `outputs/reports/shadow_iteration_YYYY-MM-DD.md/json` + `outputs/shadow_iterations/<run_id>/*` | `aits feedback run-shadow-iteration` / `aits feedback register-forward-shadow` | search bundle、promotion contract、既有 registry | `weight_only` / `gate_only` / `weight_gate_bundle` 分类、lifecycle status、Trial Card、blocked reasons、retirement evidence、P2 guardrails、attribution、position change 降级状态、lineage | dashboard 只读展示、forward shadow 候选管理 | 否，`production_effect=none` | iteration registry 只管理 shadow 观察状态；`register-forward-shadow` 只改 registry 状态；`gate_only` 和 bundle 不得被当作权重晋级候选 |
| `data/processed/prediction_ledger_flow_validation.csv` | `aits feedback run-parameter-shadow` | parameter candidates、production snapshot | validation-only challenger predictions | flow validation outcomes | 否 | 不应和正式 `prediction_ledger.csv` 混同 |

## production_effect 标签

| 标签 | 含义 | 典型位置 |
|---|---|---|
| `production` | 正式生产判断链路的一部分，会进入 canonical scoring / snapshot / ledger 语义 | production decision snapshot、production prediction ledger 行 |
| `advisory` | 投研辅助或趋势判断输出，不自动交易 | daily score report、execution advisory |
| `none` | 只读、诊断、shadow 或 validation-only，不改变生产评分、仓位 gate、正式 ledger 或 approved overlay | dashboard、shadow search、flow validation ledger |
| `validation-only` | 用于验证参数、规则或流程，不可当成生产候选批准 | parameter search、shadow gate / weight profiles |
| `blocked` | 被质量、治理、样本、owner approval 或 promotion contract 阻断 | promotion report、candidate governance |

运行时核心标签由 `ai_trading_system.core.production_effect.ProductionEffect` 表达。历史报告中较长的解释性 production effect 文本需要逐处确认语义后再迁移，不能自动归并。

当一个产物声明 `production_effect=none` 时，它仍然可以非常有价值，但价值是诊断、学习、复核或准备 forward shadow，而不是直接改变 production 结论。
