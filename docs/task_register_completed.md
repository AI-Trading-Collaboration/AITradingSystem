# 已完成任务归档

本文归档 `docs/task_register.md` 中已经 `DONE`、`BASELINE_DONE` 或 `DROPPED` 的任务，保留状态迁移、验收标准和历史备注。

最后更新：2026-06-07

## 使用规则

- 新任务、未完成任务、暂缓任务和需要 owner/外部条件解除的任务继续登记在 `docs/task_register.md`。
- 如果归档任务重新进入开发、验证或 owner 决策流程，先把该任务移回 `docs/task_register.md` 并更新状态、下一责任方和验收标准。
- `BASELINE_DONE` 表示基础版已完成但仍有后续缺口；如后续缺口需要推进，应新建任务或把原任务移回当前登记表。

## 已完成任务

|ID|领域|优先级|状态|下一责任方|阻塞或下一步|验收标准|备注|
|---|---|---|---|---|---|---|---|
|UNIV-001|标的池/模型参数化|P1|DONE|系统验证|详细拆解见 `docs/requirements/ai_universe_and_model_parameterization_2026-05-11.md`；expanded universe 已通过真实非交易日 daily-run、交易日 strict replay、replay-window 和最新生产 daily-run 17-ticker 链路验证；AMZN R&D 覆盖 warning 继续作为非阻断披露|新增代表 ticker 进入 `core_watchlist`、watchlist、lifecycle 和 SEC 映射；默认数据下载、特征、评分、回测和 dashboard 使用扩展后的核心观察池；配置测试、watchlist/lifecycle 校验和相关单测通过；文档列出 valuation gate、评分仓位映射、置信度折扣、宏观预算、风险预算、数据质量阈值和执行政策等后续可调参数候选；真实 daily/replay 验证不因新增 ticker 的价格、估值或基本面覆盖配置错误而阻断|2026-05-11: 新增并进入实现，原因：owner 明确系统尚未稳定进入生产，可以优先提升 AI 产业链代表性覆盖，不要求与早期 6 ticker 输出保持兼容；同时要求整理类似 valuation 40% cap 的硬阈值作为后续模型迭代重点。2026-05-11: 从 IN_PROGRESS 改为 VALIDATING，原因：核心观察池扩展到 17 个 ticker，watchlist/lifecycle/SEC 映射、风险事件映射、官方政策 ticker 匹配、AVGO/LRCX 拆股事件、系统流图和参数化清单均已更新；验证通过 CLI 校验、ruff 和全量 pytest。2026-05-11: 从 VALIDATING 改回 IN_PROGRESS，原因：expanded universe 真实非交易日 daily-run 已跑到 SEC metrics 并阻断于 ASML taxonomy 配置，SEC companyfacts 实际返回 `us-gaap + dei`，需要修正配置后复测。2026-05-11: 从 IN_PROGRESS 改为 VALIDATING，原因：ASML SEC taxonomy 修正为 `us-gaap + dei` 并补 `us-gaap/EUR` 指标映射；非交易日 `daily-run --as-of 2026-05-10` 为 PASS_WITH_SKIPS，交易日 `replay-day --as-of 2026-05-08 --full-universe` 为 PASS，`replay-window 2026-05-08..2026-05-10` 为 PASS_WITH_SKIPS。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 31/31 PASS，17 个 ticker 已进入日报、估值、SEC/TSM 基本面、feature 和 dashboard 链，特征警告 0；AMZN R&D 缺披露口径作为 warning 保留，不阻断 universe 验收。|
|UI-002|仪表盘/每日决策展示|P2|DONE|系统验证|详细拆解见 `docs/requirements/decision_dashboard_v2_2026-05-10.md`；dashboard v2 已接入自动 daily-run 并在最新生产 run 中以只读展示通过；后续 dashboard/Reader Brief 职责边界变化应新建报告任务|Dashboard v2 仍只读取已生成 artifact，不重新计算结论；HTML 首页能展示今日动作、仓位区间、置信度、Data Gate、最大限制、变化摘要、Top evidence/invalidators/next checks；可选读取既有 `market_feedback_optimization_YYYY-MM-DD.md`、`feedback_loop_review_YYYY-MM-DD.md` 和 `investment_weekly_review_YYYY-MM-DD.md`，缺失时必须标记 `NOT_CONNECTED` / `LIMITED`，不得补造复盘结论；页面、JSON 和文档均声明 `production_effect=none`、不替代 Markdown 日报和 trace bundle 审计源；休市日 skipped dashboard 不得从 legacy 路径带入 canonical run bundle|2026-05-10 至 2026-05-14: 已完成静态 dashboard、自动 daily-run 接入、历史趋势、run bundle mirror guard 和旧反馈复盘读取。2026-05-29: OPS-059 将旧 feedback/investment review daily-run 前置链移出每日执行，改由 `config/scheduled_tasks.yaml` 登记为 weekly/biweekly cadence；dashboard 保持只读可选消费，Reader Brief 上游链成为新的每日报告链。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 中 dashboard step 在 score 后、Reader Brief 链前 PASS，canonical run bundle 收录 `evidence_dashboard_2026-06-05.html/json`，dashboard JSON 声明 `production_effect=none`，Reader Brief quality `failed=0`。|
|RISK-013|风险事件/官方复核闭环|P1|DONE|系统验证 + 项目 owner|详细拆解见 `docs/requirements/confidence_lift_workplan_2026-05-10.md` 和 `docs/requirements/risk_event_llm_formal_assessment_2026-05-10.md`；日报内自动 LLM formal occurrence/attestation 写入、65% LLM formal 置信度和文档边界已通过最新 production daily-run 验证；LLM formal 非人工复核边界继续保留|报告能列出官方来源权限状态、OpenAI 预审队列、官方候选分类、已写入 occurrence/attestation 和缺口；`score-daily` 在 OpenAI 预审通过后可自动写入 LLM formal assessment，full coverage LLM formal 不再触发低置信模块；LLM formal 仍不得伪装成人工复核、不得单独触发 position gate，且报告必须披露人工复核跳过原因、行为影响、风险、验证覆盖和退出条件；真实人工复核声明仍只能由 reviewer 显式写入|2026-05-10: 新增并进入实现，原因：owner 要求补风险事件复核闭环以解除 `policy_geopolitics` 的 `insufficient_data` 和 data confidence 上限；人工复核不得由系统伪造。2026-05-10: 从 IN_PROGRESS 改为 BLOCKED_OWNER_INPUT，原因：官方 API key 可见、官方来源抓取/triage/occurrence 校验已通过，但当前只有 LLM formal attestation，没有真实人工复核结论，不能写 manual_input 声明。2026-05-12: 从 BLOCKED_OWNER_INPUT 改为 IN_PROGRESS，原因：owner 明确允许短期放宽人工复核限制，以 LLM formal assessment 作为可信政策/地缘结论，但必须保留审计边界和退出条件。2026-05-12: 从 IN_PROGRESS 改为 VALIDATING，原因：`score-daily` 自动 LLM formal 写入、配置化 LLM formal 置信度、报告/系统流图/README/数据源说明和测试已完成；`python -m pytest -q` 通过 467 项。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 中 OpenAI 预审和 LLM formal 自动写入均 PASS，`policy_geopolitics` 覆盖率 100%、置信度 65%，风险事件 gate 未触发；报告继续披露 LLM formal 未人工复核和不得单独触发仓位闸门。|
|OPS-016|运行架构/Reader Brief current-run decision summary|P0|DONE|系统验证|详细拆解见 `docs/requirements/reader_brief_current_decision_summary_2026-06-04.md`；2026-06-05 最新 production daily-run 已验证最终 Reader Brief 读取同一 run 的 `daily_decision_summary` / `daily_task_dashboard`，canonical run bundle 与 legacy mirror hash 一致|`aits ops daily-run` 最终发布的 Reader Brief 必须读取同一 run 的 `daily_decision_summary` 和 `daily_task_dashboard`；canonical run bundle 与 legacy `outputs/reports` mirror 保持一致；修复只允许重建 Reader Brief / Reader Brief quality，只读、`production_effect=none`，不得重跑 scoring/PIT/SEC/valuation/backtest/shadow/weight/docs 上游，不写 production weights、active shadow weights、broker state 或交易指令；目标测试和真实 as_of 复测通过|2026-06-04: 新增并进入 IN_PROGRESS，原因：修复 FRED 后完整 daily-run 31/31 PASS，但当前 Reader Brief HTML Core Decision 仍显示 `missing`，排查确认其读取修复前失败 run 的 stale summary。2026-06-04: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 daily-run finalization Reader Brief refresh helper、legacy mirror 覆盖 Reader Brief/quality、daily-run CLI 单测断言 current run_id、系统流图/artifact catalog/runbook 文档；验证通过目标测试和完整 `aits ops daily-run --as-of 2026-06-03`。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 31/31 PASS；Reader Brief、decision summary 和 task dashboard 均绑定同一 run_id，Core Decision 不再读取旧失败 run，Reader Brief quality failed=0，canonical 与 legacy mirror hash 一致。|
|OPS-014|运行健康/PIT production visibility cutoff|P0|DONE|系统验证|2026-06-05 最新 production daily-run 的 `ops health` / pipeline health 已通过，PIT freshness、checksum、normalized row count 均 OK，告警 0|`aits ops health --as-of <latest_completed_us_trading_day>` 对本轮 forward-only PIT 应要求 `available_time <= visibility_cutoff`，而不是要求 `available_time.date <= as_of`；历史 as-of 未进入 production cutoff 时仍按 as-of 严格阻断未来 `available_time`；pipeline health、alerts、daily-run 和 replay 语义不得混淆；相关测试、文档和真实 daily-run 通过|2026-05-13: 新增并进入 VALIDATING，原因：`daily_run:2026-05-11:20260512T164618Z` 已完成评分和 dashboard，但 `ops health` 因 PIT latest_available_time=2026-05-12 晚于 as_of=2026-05-11 失败；修复后单独 `ops health --as-of 2026-05-11` PASS，真实 `daily-run` 11/11 PASS。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 31/31 PASS，`pipeline_health_2026-06-05.md` 状态 PASS，PIT 相关 health checks OK，`pipeline_health_alerts_2026-06-05.md` 告警 0。|
|OPS-013|运行架构/daily-run direct dispatcher|P0|DONE|系统验证|2026-06-05 最新 production daily-run 已验证 direct dispatcher 的 `score-daily` 映射未再因 CLI 签名漂移阻断|`.venv\\Scripts\\python.exe -m ai_trading_system.cli_direct score-daily ...` 必须把 `--risk-event-openai-precheck/--skip-risk-event-openai-precheck` 正确映射到当前 `score_daily(risk_event_openai_precheck=...)`；daily-run 不得因 direct dispatcher 与主 CLI 签名漂移阻断；目标单测、ruff、`git diff --check` 和真实 daily-run 复测通过或报告新的真实门禁原因|2026-05-13: 新增并进入 IN_PROGRESS，原因：修复 FRED 无响应重试后，真实 `daily-run` 的 `score_daily` 子步骤失败于 `TypeError: score_daily() got an unexpected keyword argument 'skip_risk_event_openai_precheck'`，属于 daily-run 入口 wiring bug。2026-05-13: 从 IN_PROGRESS 改为 VALIDATING，原因：`cli_direct` 已改为传 `risk_event_openai_precheck=not --skip-risk-event-openai-precheck`；新增 direct dispatcher 单测；真实 `daily-run` 已越过 `score_daily` 并最终 11/11 PASS。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 中 `score_daily` step PASS，命令包含 OpenAI precheck profile 与显式 visibility cutoff，未出现 direct dispatcher signature drift。|
|OPS-012|运行架构/daily-run 子命令解释器|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_run_local_python_executor_2026-05-12.md`；2026-06-05 最新 production daily-run 已用项目 `.venv` direct dispatcher 31/31 PASS，无 Windows native crash|`daily-run` 执行 `aits ...` 子命令时若项目 `.venv` Python 存在，必须使用该解释器调用 direct dispatcher；找不到时才回退当前 `sys.executable`；子命令必须强制使用稳定 Python env、每次运行唯一 `PYTHONPYCACHEPREFIX`，并在启动前清理源码 `__pycache__`；PIT 等轻量子命令不得因无关 valuation pydantic class construction 或 Typer 全局 command tree annotation 解析崩溃；报告仍展示逻辑命令，主报告/metadata 不记录 secret 或未脱敏 stdout/stderr 原文；失败步骤必须写入脱敏 stdout/stderr 诊断 artifact 并在执行报告/metadata 暴露路径；真实 daily-run 必须完成最新交易日链路或报告新的真实门禁原因|2026-05-12: 新增并进入实现，原因：本机 PATH `aits` 指向全局 Python，daily-run 子命令继承全局解释器后在 PIT/SEC 大数据步骤出现 Windows access violation。2026-05-12: 已改用项目 `.venv`、强制稳定运行环境、唯一 pycache 前缀、清理源码 `__pycache__`，并新增 `cli_direct` 绕开 Typer 全局解析。2026-05-17: 从 IN_PROGRESS 改为 VALIDATING，原因：本地 Python 3.11 环境切换、依赖补齐、IBKR snapshot、全量 pytest、ruff 和 black check 通过。2026-05-28: 继续保持 VALIDATING，原因：补充失败步骤脱敏 stdout/stderr 诊断 artifact。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 31/31 PASS，报告披露 `.venv` direct dispatcher 与隔离 pycache，所有子命令无 stderr、无 native crash。|
|RISK-015|风险事件/OpenAI 预审可见时间|P0|DONE|系统验证|详细拆解见 `docs/requirements/risk_event_openai_prereview_visibility_cutoff_2026-05-12.md`；2026-06-05 最新 production daily-run 已在盘后 UTC 次日生成 OpenAI prereview 并通过可见性校验|最新已完成美股交易日的生产 `score-daily` / `daily-run` 可以在收盘后 UTC 次日生成 OpenAI 预审记录并通过校验；历史 as_of 未提供生产 cutoff 时仍拒绝晚于评估日的 live OpenAI 请求；报告/文档说明 request timestamp、as_of 和 visibility cutoff 的关系|2026-05-12: 新增并进入实现，原因：最新交易日 `daily-run` 已越过 PIT、SEC 和 valuation，但 `score_daily` 在 `risk_event_prereview_request_in_future` 阻塞。2026-06-07: 从 IN_PROGRESS 改为 VALIDATING，原因：新增 `score-daily --risk-event-openai-precheck-visibility-cutoff`、daily-run latest production 注入、预审报告 request/as_of/cutoff 披露和 focused tests。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 的 `score_daily` step PASS；`risk_event_prereview_openai_2026-06-05.md` 披露 request timestamp 晚于 as_of 但不晚于显式 production cutoff，错误 0；LLM formal 写入 5 条并保持人工复核/position gate 边界。|
|FUND-002|基本面/SEC companyfacts 写入稳定性|P0|DONE|系统验证|详细拆解见 `docs/requirements/sec_companyfacts_json_write_stability_2026-05-12.md`；2026-06-05 最新 production daily-run 已稳定越过 SEC companyfacts，companyfacts validation PASS|SEC companyfacts raw JSON 写入必须保留原始字段和值，但不得对大型 payload 先做完整 pretty/sorted `json.dumps` 中间字符串；manifest 继续记录 endpoint、请求参数、fact count 和 checksum；真实 daily-run 至少越过 SEC companyfacts|2026-05-12: 新增并进入实现，原因：扩展核心观察池后，17 个 ticker 的 SEC companyfacts 大型 JSON 在 `_write_json()` 的 pretty/sorted `json.dumps` 路径触发 Windows access violation，阻断每日链路。2026-06-07: 从 IN_PROGRESS 改为 VALIDATING，原因：SEC companyfacts 下载已优先保存 raw response bytes，非 raw provider 改为 `JSONEncoder.iterencode()` chunk 写入 compact JSON，manifest checksum 仍基于实际写入文件；focused tests、ruff、compileall 和 git diff check 通过。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 中 SEC companyfacts step PASS，17/17 公司缓存存在，`sec_companyfacts_validation_2026-06-05.md` PASS、错误 0、警告 0，manifest 记录下载时间、fact count 和 checksum。|
|DATA-017|数据工程/FMP forward PIT normalized_id|P0|DONE|系统验证|详细拆解见 `docs/requirements/fmp_forward_pit_id_normalization_crash_2026-05-12.md`；2026-06-05 最新 production daily-run 已越过 FMP forward PIT fetch/build/validate，normalized CSV 与 manifest validation 均 PASS|FMP forward-only PIT 标准化 ID 生成必须对非 ASCII、超长和非标供应商字段保持确定性、可读前缀和冲突保护；不得因逐字符 Unicode 分类或大批量 dataclass 字段复制触发 Windows 原生访问冲突；真实 CLI 应从 raw payload 流式写出 normalized CSV，而不是为全量行创建常驻 dataclass；raw payload、normalized CSV、PIT manifest、抓取报告和 PIT validation 报告必须正常生成；真实 `daily-run` 至少越过 PIT 阶段|2026-05-12: 新增并进入实现，原因：最新交易日 `daily-run` 默认 as-of 已正确落到 `2026-05-11`，但 `pit_snapshots` 在 `_id_token` 标准化真实 FMP payload 时 Windows access violation 退出。2026-05-12: 修复 raw JSON、scalar extraction、report attach、dataclass 复制和 streaming normalized CSV，单独 PIT direct dispatcher PASS。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 中 PIT fetch/build-manifest/validate steps PASS；FMP raw payload 17、原始/标准化行 12411、错误 0、警告 0，PIT manifest validation PASS。|
|FUND-001|基本面/TSM IR 覆盖规则|P1|DONE|系统验证|详细拆解见 `docs/requirements/confidence_lift_workplan_2026-05-10.md`；2026-06-05 最新 production daily-run 已验证 TSM IR merge 与 SEC metrics validation 链路，TSM Q1 `TSM-IR` 指标进入 processed metrics|TSM 基本面覆盖由 TSMC IR 官方季度 Management Report 或等价可审计来源满足；缺 TSM IR 时报告明确缺官方 IR 覆盖；不再因 TSM 缺 SEC companyfacts 季度指标而误判为美国 SEC 指标缺口|2026-05-10: 新增并进入实现，原因：owner 明确 TSM 不应硬等同美国 SEC companyfacts 覆盖。2026-05-10: 从 IN_PROGRESS 改为 VALIDATING，原因：`merge-tsm-ir-sec-metrics` 后 SEC metrics 72 行且 validation PASS，`build-sec-features` PASS、警告 0；日报计划和 `score-daily` 已接入 TSM IR 合并路径。2026-06-07: 从 VALIDATING 改为 DONE，原因：最新真实 `daily-run --as-of 2026-06-05 --run-id codex_20260605_20260607103901` 中 `merge-tsm-ir-sec-metrics` 和 `validate-sec-metrics` steps PASS；`sec_fundamentals_validation_2026-06-05.md` 只有 AMZN R&D 覆盖警告，不是 TSM 覆盖缺口，TSM Q1 `TSM-IR` 指标已在 processed metrics 中可见。|
|TRADING-101|ETF portfolio research/Dynamic v3 Rescue Real Evaluator Sweep Integration|P0|DONE|系统验证|详细拆解见 `docs/requirements/TRADING-101_Dynamic_v3_Rescue_Real_Evaluator_Sweep_Integration.md`；real evaluator sweep integration 已完成并通过全量验证；后续真实参数研究、evidence closure 和 promotion review 由 `TRADING-102_to_110`、`TRADING-111_to_113` 继续观察 owner/真实 evidence 结论|Evaluator mode 仅允许 `tiny_fixture_proxy` / `real_dynamic_v3_rescue`；config 或 CLI 可指定 real mode；candidate parameters 注入 dynamic-v3-rescue real evaluation；`candidate_results.jsonl` 包含 evaluator mode/version、real artifact path、data_quality 和 metrics_source；tiny reports 标记 not-for-investment 且 promotion pack 不可 `promote_candidate`；real mode metrics 来自真实 evaluation artifact，`validate-sweep` 检查 artifact path；small real sweep smoke、focused tests、ruff、compileall、git diff --check 和 `.venv\Scripts\python.exe -m pytest tests -q` 通过|2026-06-06: 新增并进入 IN_PROGRESS，原因：TRADING-093~100 platform MVP 默认 tiny fixture proxy 只能验证平台闭环，不能作为真实 investment evidence；需要把 sweep 平台接入 TRADING-091 price-driven real evaluation pipeline。2026-06-06: 实现完成并进入 VALIDATING；sample real sweep `sweep_20260606T034508Z_ae5ae1d8` 以 `real_dynamic_v3_rescue` / workers=1 跑通 2 candidates 且 `validate-sweep` PASS；focused tests 16 passed，ruff、compileall、git diff --check PASS；全量 `pytest tests -q` 首次 15 分钟超时未返回失败明细。2026-06-07: 从 VALIDATING 改为 DONE，原因：修复 `score-daily` CLI 模块化后的测试边界引用漂移后，focused regression 通过，`.venv\Scripts\python.exe -m pytest tests -q` 全量验证通过（2197 passed, 329 warnings, 2266.75s）。|
|TRADING-067|ETF satellite stock replacement/Satellite Stock Replacement Policy|P0|BASELINE_DONE|系统验证 + 后续 owner manual review|详细拆解见 `docs/requirements/TRADING-067_Satellite_Stock_Replacement_Policy.md`；TRADING-067A~K baseline complete；后续如需把个股替换候选进入更长 forward attribution 或 owner-reviewed production policy，应新建后续任务|`python -m ai_trading_system.cli etf satellite validate` PASS；TRADING-067A~K 均完成；全量 pytest、ruff、compileall、diff check 通过；所有 satellite 输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；ETF-first fallback 存在；不写 official ETF target weights、不触发 broker action、不自动 production promotion；report registry、Reader Brief、artifact catalog、system flow 和 tests 同步|2026-06-02: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：完成 satellite universe config、stock-to-ETF benchmark mapping、relative-strength features、SatelliteCandidateScore、replacement eligibility gate、ETF-first replacement plan、shadow experiment、risk constraint diagnostics、standalone JSON/Markdown report、Reader Brief Satellite Replacement section 和 final validation gate；所有输出仅为 candidate/shadow/hypothetical replacement artifacts，不改变 TRADING-062~066 official ETF allocation 安全边界。|
|TRADING-066|ETF AI confirmation/AI Confirmation Score Calibration|P0|BASELINE_DONE|系统验证 + 后续 forward attribution review|TRADING-066A~J baseline complete；后续建议新建 TRADING-071 AI Confirmation Forward Attribution Review 或 TRADING-067 Satellite Stock Replacement Policy|`python -m ai_trading_system.cli etf ai-confirmation validate` PASS；AI confirmation outputs 固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不写 official ETF target weights、不触发 broker action、不自动 promotion；feature/score timing 满足 no-lookahead；JSON/Markdown report、shadow overlay、Reader Brief、report registry、artifact catalog、system flow 和 tests 同步；全量 pytest、ruff、compileall、diff check 通过|2026-06-01: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：TRADING-066A~J 已完成并验证。新增 AI confirmation universe config、AI/semiconductor breadth features、MegaCapAIScore、AISemiconductorRelativeStrengthScore、event risk overlay、AIConfirmationScore composite、standalone report、shadow overlay experiment、Reader Brief AI Confirmation section 和 final validation gate；最终真实命令 `python -m ai_trading_system.cli etf ai-confirmation validate` 输出 PASS，`production_weights_mutated=false`，所有输出保持 observe-only / candidate-only / no broker action。|
|TRADING-063B|ETF 组合可信度/Benchmark Suite Hardening|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_credibility_validation_2026-06-01.md`；下一步推进 TRADING-063C No-Lookahead Validation Framework|`config/etf_portfolio/backtest.yaml` 提供 B001-B008 benchmark registry；B001-B004 为 buy-and-hold SPY/QQQ/SMH/SOXX；B005/B006 为 stable static portfolios；B007 为 QQQ 50/200 moving-average baseline；B008 为 risk-off cash switch baseline；backtest summary / metrics 输出 `benchmark_metrics` 和 `benchmark_comparisons` common schema，包含 strategy-vs-benchmark CAGR、drawdown、Sharpe、Calmar 和 turnover 字段；测试覆盖 registry、buy-and-hold deterministic weights、static weight sum、MA warm-up cash behavior、risk-off signal-date switch 和 schema stability；README、artifact catalog、system flow 同步|2026-06-01: 新增并直接归档为 DONE，原因：完成 TRADING-063B benchmark suite hardening；真实 smoke `aits etf backtest run --config config/etf_portfolio/backtest.yaml --fast` 生成 summary，其中 `benchmark_metrics` 包含 B001-B008 且 `benchmark_comparisons` 含 required fields；验证通过 `python -m pytest tests -q`（1633 passed, 330 warnings）、`python -m ruff check config src tests scripts docs`、`python -m compileall -q src tests scripts` 和 `git diff --check`。|
|TRADING-062|ETF 组合配置/Portfolio Allocation System|P0|BASELINE_DONE|项目 owner + 后续 provider/API 决策|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md`；P0/P1 baseline、P2 observe-only contracts、报告可见性、CLI compatibility、simulation benchmark outcomes、backtest audit artifacts、daily brief simulation summary 和 CLI option compatibility 已完成；P2 live news/LLM sentiment、完整 EDGAR 财报解释、真实 VXN/skew vendor、实时 issuer holdings API、多账户/broker route 仍需 owner/provider/API/PIT policy 决策后升级|`aits etf validate-config`、`aits etf data validate`、`aits features build --end latest`、`aits etf signals generate`、`aits etf regime generate`、`aits etf portfolio allocate`、`aits etf backtest run --config config/etf_portfolio/backtest.yaml --fast`、`aits etf simulation record/evaluate/report`、`aits etf report daily`、`aits etf run daily --dry-run`、P1/P2 observe-only commands、Reader Brief/report registry 可运行；target weights sum=1；数据质量门禁、no-lookahead、simulation null semantics、报告章节、config schema、P2 source contract、安全边界和 full pytest 通过；不修改 production 参数或真实交易动作|2026-05-31: 新增并进入 IN_PROGRESS，原因：owner 提供 ETF 组合配置系统详细开发文档并要求参考实现所有功能。2026-05-31: P0/P1/P2 observe-only baseline 与后续补齐项 TRADING-062A 至 TRADING-062M 完成并验证；从 IN_PROGRESS 改为 BASELINE_DONE，原因：原文 DoD 的本地可运行功能、验证、工程说明和安全边界已覆盖；剩余 live/provider 能力依赖 owner/API/PIT policy，不能用临时数据或未批准 broker route 替代。|
|TRADING-062A|ETF P2/EDGAR text topic audit baseline|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已在 TRADING-062 的 EDGAR text cache 之上新增只读 topic audit baseline|`p2.yaml` 治理 topic keyword set、min text length、candidate-only 和 auto-promotion 边界；`aits etf p2 edgar-topics` 只能读取已缓存 official filing text，输出 topic counts、matched keywords、limitation、policy version 和 `production_effect=none`；不得生成 sentiment、财务结论、权重、交易建议或 broker action；测试覆盖 PIT as-of、缺文本、关键词计数和安全边界|2026-05-31: 新增并进入 IN_PROGRESS，原因：TRADING-062 已具备 official filing text cache，但原 P2 文档还要求 EDGAR / 财报文本解释层；本阶段先实现受治理、observe-only 的 topic audit，不接 LLM、不做自动投资解释。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：新增 `edgar_text_analysis` policy、`aits etf p2 edgar-topics`、topic audit builder 和测试；真实 NVDA 10-Q smoke 输出 4 个 topic rows，固定 `candidate_only=true`、`auto_promotion=false`、`production_effect=none`。|
|TRADING-062B|ETF P2/ML weight optimizer baseline|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已在现有 P0 signals 和 P2 policy 上新增 candidate-only 权重优化报告|`p2.yaml` 治理 optimizer lookback、score/return/risk 权重、单标的上限、cash floor、candidate-only 和 auto-promotion 边界；`aits etf p2 weight-optimizer` 先复用 ETF price data quality gate，读取 P0 signals 和价格历史，输出候选权重、score components、data quality、config hash、limitation 和 `production_effect=none`；不得写入 `data/etf_portfolio/target_weights.csv`、不得调用 broker、不得改变日报正式建议；测试覆盖权重合计、CASH 吸收、上限和安全边界|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文 P2 明确包含“机器学习权重优化”，当前已有 `ml-ranking` 仅做排序，尚未输出候选组合权重；本阶段先实现可审计 candidate-only baseline，不宣称生产级 ML。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：新增 `weight_optimizer` policy、`aits etf p2 weight-optimizer`、candidate weight builder 和测试；真实 2026-05-29 smoke 输出 5 行候选权重，固定 `candidate_only=true`、`auto_promotion=false`、`production_effect=none`，并披露 data quality PASS。|
|TRADING-062C|ETF P2/news theme tracking baseline|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已把 canonical news/theme input 升级为 observe-only 主题事件追踪报告|`p2.yaml` 治理 tracking window、max report rows、candidate-only 和 auto-promotion 边界；`aits etf p2 news-themes` 在 canonical input 存在时输出 symbol/theme event count、weighted sentiment、avg relevance、latest published/available time、latest summary、source URL、limitation、policy version 和 `production_effect=none`；缺输入时仍输出 `MISSING_INPUT`；不得调用 LLM、不得生成交易建议、不得改变正式信号或权重；测试覆盖 normalizer 默认 sentiment/relevance、tracking aggregation 和安全边界|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文 P2 明确包含“新闻摘要和主题事件追踪”，当前已有 `normalize-news` 和 source contract，但 `news-themes` 尚未输出主题追踪摘要。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：新增 `build_news_theme_tracking_report`、`news_themes` tracking policy 和 `aits etf p2 news-themes` 追踪输出；fixture smoke 在 `artifacts/etf_portfolio/p2_news_tracking_smoke/` 输出 NVDA/SMH 主题追踪，固定 `production_effect=none`。|
|TRADING-062D|ETF P2/options IV-VXN-skew source normalizer|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已把 owner/vendor 本地审计 options CSV 接入 canonical `options_iv_skew.csv` normalizer，继续保持 observe-only|`aits etf p2 normalize-options-risk` 只接受本地审计 CSV/Parquet，不下载未批准 provider；必须规范化 symbol、as_of、available_at、iv_rank、skew_zscore、vxn_level、source_provider、source URL、downloaded_at、row checksum 和 source manifest；缺列或无有效值 fail closed；risk flag 只能使用输入列或 `p2.yaml` 中 IV rank 阈值派生；不得写正式权重、不得改变信号、不得调用 broker；测试覆盖列名别名、percent IV rank、VXN/skew numeric summary、manifest 和 source contract|2026-05-31: 新增并从 IN_PROGRESS 改为 DONE，原因：新增 `normalize_options_risk_source`、`aits etf p2 normalize-options-risk`、fixture CSV 和测试；fixture smoke 在 `artifacts/etf_portfolio/p2_options_normalize_smoke/` 输出 NORMALIZED 与 `options-risk` `PASS_WITH_LIMITATIONS`，固定 `production_effect=none`。|
|TRADING-062E|ETF reports/Reader Brief visibility|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已把 ETF brief 和关键 ETF 支撑报告纳入现有 report registry / Reader Brief navigation，不改变 ETF allocation、主 daily-run 或 broker 边界|`config/report_registry.yaml` 登记 ETF portfolio brief、ETF data quality、ETF backtest summary 和 ETF P2 walk-forward readiness；`aits reports index` 能只读扫描最新 ETF artifacts 并输出 freshness/status/owner_action；Reader Brief 在有 report_index 时能通过 report navigation/cadence calendar 下钻 ETF 报告；`docs/artifact_catalog.md` 和 `docs/system_flow.md` 同步；不重跑 ETF 上游、不写 production weights、不触发交易；测试覆盖 registry 默认加载、report index 最新 artifact 识别和 documentation contract 覆盖|2026-05-31: 新增并进入 IN_PROGRESS，原因：ETF P0/P1/P2 报告已独立生成，但尚未进入现有 Reader Brief/report registry 可见性层。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：新增 registry 条目、artifact catalog、system flow、report index compact ETF backtest run date 解析和 Reader Brief navigation 测试；真实 smoke `aits reports index --as-of 2026-05-29`、`aits reports reader-brief --date 2026-05-29`、`aits reports validate-reader-brief --date 2026-05-29` 通过，ETF 条目在 Reader Brief report navigation 可见且 `production_effect=none`。|
|TRADING-062F|ETF CLI compatibility aliases|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已补齐开发文档中更接近无前缀的 CLI 入口，同时不覆盖现有主系统根级 backtest/daily scoring 语义|`aits data ingest/validate`、`aits features build`、`aits signals generate`、`aits regime generate`、`aits portfolio allocate`、`aits simulation record/evaluate/report`、`aits report daily`、`aits run daily` 作为 ETF workflow compatibility aliases；`aits backtest` 保持现有主系统命令，ETF 回测继续为 `aits etf backtest run/report`；alias help 必须说明 ETF 兼容入口和 production/trading 边界；测试覆盖 help/command availability，smoke 覆盖至少一个 alias dry-run|2026-05-31: 新增并进入 IN_PROGRESS，原因：原始开发文档 CLI 示例使用 `data/features/signals/regime/portfolio/simulation/report/run` 顶层路径，当前只有 `aits etf ...` 隔离入口。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：已新增 root-level ETF compatibility aliases，`aits backtest --help` 保持主系统每日评分回测；`aits run daily --date latest --dry-run` 输出 2026-05-29 ETF brief 且 `data_quality_status=PASS`，目标 alias 测试通过。|
|TRADING-062G|ETF README operator documentation|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已补齐原始开发文档 Definition of Done 中 “README 说明如何运行” 的可验证交付|README 必须说明 ETF 主仓配置系统定位、默认 namespace 与 compatibility aliases、P0 daily/backtest/simulation/report 命令、P1/P2 observe-only 命令入口、主要 artifact 路径、数据质量门禁、`ai_after_chatgpt` 默认窗口、`production_effect=none` / no broker 边界和已知外部数据源限制；文档不得把 ETF brief 写成自动交易指令或主 daily score 替代品；测试至少覆盖 README 包含关键命令和安全边界文本|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文最终 DoD 要求 README 说明如何运行；当前 ETF 实现已有 `IMPLEMENTATION_NOTES.md` 和系统流图，但 README 尚未覆盖 ETF workflow、artifact 和安全边界。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：README 新增 ETF workflow、artifact、compatibility alias、`ai_after_chatgpt`、data quality gate、Reader Brief visibility 和 `production_effect=none` / no broker 边界；`tests/test_etf_readme.py` 通过。|
|TRADING-062H|ETF P1 experiment run/compare registry|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已补齐原始 P1 Experiment Registry 中 `experiments run` / `experiments compare` 的本地治理入口，保持 candidate-only / observe-only|`aits etf experiments run --config <candidate.yaml>` 必须读取候选配置、记录 candidate_config_hash、baseline config hash、参数 diff、可选 backtest summary strategy metrics、manual_review_required 和 `production_effect=none`，不得写正式配置或目标权重；`aits etf experiments compare --baseline production` 必须只读 registry，输出 candidate/shadow 与 production baseline 的 metric delta 或 `MISSING_METRICS`；不得自动 promotion；测试覆盖 registry record、diff、metrics delta、缺 metrics 状态和 CLI smoke|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文 P1 明确列出 `experiments run` / `experiments compare`，当前 ETF CLI 只有 `experiments register`。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：新增 `append_experiment_run`、`build_experiment_comparison`、`aits etf experiments run/compare` 和根级 `aits experiments run/compare/register` alias；真实 smoke `aits experiments compare` 生成 observe-only 比较报告，目标测试通过。|
|TRADING-062I|ETF simulation benchmark outcome fields|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已补齐原文 Simulation Ledger 后验字段和 simulation report 的 portfolio-vs-benchmark 摘要|`aits etf simulation evaluate` 必须在 forward window 足够时写入 `relative_return_vs_spy_20d`、`relative_return_vs_qqq_20d`、`weight_contribution_20d`，并可计算组合级 `portfolio_return_20d`、`portfolio_relative_return_vs_spy_20d`、`portfolio_relative_return_vs_qqq_20d`；未来窗口不足或 benchmark 缺失必须保持 null，不得填 0；`aits etf simulation report` 必须显示 model_version 级 20d hit rate 和 portfolio vs SPY/QQQ 摘要；测试覆盖字段、null 语义和报告小节|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文 11.3/12.2 要求 simulation ledger 包含相对 benchmark 和权重贡献字段，并要求日报/模拟舱说明 portfolio vs benchmarks；当前 evaluator 只写 forward return、drawdown 和 hit rate。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：新增 20d SPY/QQQ-relative return、weight contribution、portfolio-vs-benchmark 字段和 simulation report 小节；historical smoke 生成有效 20d outcome，latest ledger 窗口不足保持 null；目标测试、ruff、compileall、diff check 和全量 pytest 1627 passed。|
|TRADING-062J|ETF backtest artifact and benchmark completeness|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已补齐原文 ETF backtest 审计产物和 QQQ 50/200 MA 基准|`aits etf backtest run` 必须输出 `daily.csv`、`weights.csv`、`trades.csv`、`summary.json`、`metrics.json` 和 `summary.md`；daily rows 必须包含资产收益和资产贡献 JSON；weights/trades 必须含 signal_date、return_date、symbol、target/current/delta、model_version、config_hash；benchmark metrics 必须包含 `ma_50_200_qqq`，并继续包含 buy-and-hold SPY/QQQ/SMH 与 static default portfolio；summary 必须披露 data quality、AI regime、execution lag、benchmark comparison 和 correctness notes；测试覆盖文件输出、权重/交易 schema、MA baseline 和资产贡献口径|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文 10.1/10.5/10.7 要求权重历史、交易历史、资产级收益归因、结果文件和 QQQ 50/200 MA 基准；当前 ETF backtest 只写 `daily.csv`、`summary.json/md` 和 buy-and-hold/static 基准。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：新增 weights/trades/metrics artifacts、daily asset return/contribution JSON、扩展指标、benchmark relative return 和 `ma_50_200_qqq`；真实 fast backtest smoke 生成完整 run 目录；目标测试、ruff、compileall、diff check 和全量 pytest 1627 passed。|
|TRADING-062K|ETF daily brief simulation performance integration|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已补齐日报 Simulation Performance 对 simulation ledger 的真实摘要读取|`aits etf report daily` 和 `aits etf run daily` 生成的 portfolio brief 必须读取 `data/simulation/etf_ledger.csv` 或显式 ledger path，展示 model_version 级 records、20d hit rate、avg portfolio return、relative vs SPY/QQQ；缺 ledger、空 ledger、窗口不足或 benchmark 缺失必须明确输出 `n/a` / 暂无记录，不得补造表现；测试覆盖 ledger 有值和缺 ledger 两种语义；文档同步说明日报小节读取 ledger 但不触发交易动作|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文 12.2 要求 Daily Portfolio Brief 的 Simulation Performance 包含 last 20 trading days、signal hit rate 和 portfolio vs benchmarks；当前 CLI 日报没有读取 simulation ledger，只输出占位文案。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：新增 `summarize_simulation_for_brief` 并接入 `aits etf report daily` / `aits etf run daily`；真实 smoke brief 显示默认 ledger 摘要，latest forward window 不足保持 `n/a`；目标测试、ruff、compileall、diff check 和全量 pytest 1628 passed。|
|TRADING-062L|ETF simulation record --date compatibility|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已补齐 simulation record 的 latest/指定日期选择|`aits etf simulation record --date latest` 必须按 allocation 文件最新日期记录 snapshot；`--date YYYY-MM-DD` 必须只记录该日期 allocation rows；allocation 缺 date column、无有效日期或指定日期缺失必须 fail closed；重复运行仍保持 idempotent upsert；根级 `aits simulation record --date latest` 同步可用；测试覆盖 latest、指定日期、缺日期失败和 idempotent|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文 11.4 / 16.6 和 README 均给出 `simulation record --date latest`，当前 CLI 未提供 `--date`，文档命令不可运行。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：`simulation record` 新增 `--date`，默认 latest allocation date，指定日期按 allocation rows 过滤，缺日期 fail closed；root alias 同步通过；真实 smoke 和全量 pytest 1629 passed。|
|TRADING-062M|ETF features/backtest CLI option compatibility|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_allocation_system_2026-05-31.md#状态`；已补齐原文 features/backtest CLI 参数兼容|`aits etf features build --end latest` 和根级 `aits features build --end latest` 必须使用价格缓存最新日期作为 end；显式 `--end YYYY-MM-DD` 继续按日期过滤；`aits etf backtest run --config config/etf_portfolio/backtest.yaml --fast` 必须读取指定 ETF backtest config；根级 `aits backtest` 不得变成 ETF alias；测试覆盖 help/CLI smoke 和现有主系统 backtest help 不变|2026-05-31: 新增并进入 IN_PROGRESS，原因：原文 16.2 / 16.5 给出 `features build --end latest` 和 backtest `--config` 示例，当前 ETF features 对 `latest` 解析失败，ETF backtest run 不暴露 `--config`。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：`features build --end latest`、root alias smoke 和 ETF `backtest run --config` 均通过，根级 `aits backtest` 仍为主系统命令；全量 pytest 1630 passed。|
|TRADING-063A|ETF 组合可信度/Runtime Artifact Hygiene & Fixture Policy|P0|DONE|系统验证|详细拆解见 `docs/requirements/etf_portfolio_credibility_validation_2026-06-01.md`；下一步推进 TRADING-063B Benchmark Suite Hardening|`.gitignore` 忽略 `data/etf_portfolio/`、`data/simulation/` 和 `reports/`；`tests/fixtures/etf_portfolio/` 成为 ETF deterministic fixture 目录；artifact catalog 与 system flow 明确 runtime artifacts 不属于 source artifacts，daily reports 不提交为源码，test fixtures 不放在 `data/` 或 `reports/`；现有 local ETF runtime outputs 不再出现在 `git status --short`；验证通过 `git diff --check`、`python -m pytest tests -q` 和 `python -m ruff check config src tests scripts docs`|2026-06-01: 新增并直接归档为 DONE，原因：完成 TRADING-063A runtime artifact hygiene、fixture policy 和文档同步；`git check-ignore` 确认 `data/etf_portfolio/target_weights.csv`、`data/simulation/etf_ledger.csv`、`reports/etf_portfolio/2026-05-29_portfolio_brief.md` 均被忽略；`python -m pytest tests -q` 结果为 1630 passed，ruff 和 diff check 通过。|
|TRADING-061A|回测输入/Stable Weight Tuning Input Readiness Recovery|P1|BASELINE_DONE|系统验证|详细拆解见 `docs/requirements/weight_stability_input_readiness_recovery_2026-05-31.md`；已完成 stable tuning upstream freshness、recover-freshness、signal snapshot、backtest manifest 和 price coverage blocker 结构化诊断；后续若需自动执行数据恢复，应新增 TRADING-061B，不在本任务内静默改 cache|新增 `aits parameters diagnose-weight-stability-inputs --latest`、`aits parameters recover-weight-stability-inputs --latest --dry-run`、`aits parameters validate-weight-stability-readiness --latest`、`aits reports weight-stability-readiness --latest`；生成 `artifacts/weight_stability_readiness/YYYY-MM-DD/weight_stability_readiness_summary.json/md`；`tune-weights-stable` 在输入阻塞时引用 readiness artifact 并显示 `reason=input_readiness_blocked`、`candidates_backtested=0`；shadow backtest supporting artifact、Dashboard、Reader Brief 只读引用 readiness；不修改 `config/parameters/production/current.yaml`，不降低 data quality gate，不使用 mock 或 synthetic price history；专项测试、全量 pytest、ruff、compileall 和 diff check 通过|2026-05-31: 新增并进入 IN_PROGRESS，原因：owner 明确 TRADING-061 latest 为 `INSUFFICIENT_DATA` / `candidates_backtested=0`，阻塞来自 freshness / signal snapshot / manifest / price coverage readiness；需要先让 stable tuning 明确诊断输入阻塞。2026-05-31: 从 IN_PROGRESS 改为 BASELINE_DONE 并归档，原因：已完成 readiness artifact、CLI/report alias、stable tuning 阻塞引用、shadow backtest/Dashboard/Reader Brief 接入和专项/全量验证；真实 latest readiness 为 `RECOVERY_FAILED`，blocking checks 包含 `freshness`、`recover_freshness`、`signal_snapshot`、`backtest_manifest`、`price_coverage`，`tune-weights-stable` 仍 `candidates_backtested=0` 但现在明确为 input readiness blocked，不是稳定约束失败。|
|OPS-061|运行架构/Daily-run direct dispatcher daily chain coverage|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_run_direct_dispatcher_daily_chain_coverage_2026-05-31.md`；已确认 2026-05-31 的 `market_data_freshness` 阻断不是预期 freshness/data gate，而是 scheduled daily command 与 direct dispatcher coverage 漂移；修复、测试和真实 daily-run 验证已完成|`ai_trading_system.cli_direct` 覆盖 `config/scheduled_tasks.yaml` 中 daily-run 会执行的全部命令，特别是 `data freshness --latest`、`data recover-freshness --latest`、`portfolio track-candidate --latest`、`portfolio review-tracking --latest --show-window-progress` 和 `reports portfolio-tracking-review --latest`；新增测试从 scheduled daily config 逐条验证 direct dispatcher coverage；真实 `aits ops daily-run` 能越过原阻断并完成 Reader Brief / `validate-reader-brief`；不修改 production weights、active shadow weights、broker 或 trading action|2026-05-31: 先以 `OPS-060` 新增 READY，原因：`daily_ops_run:2026-05-29:20260530T223234Z` 已通过 data gate、PIT、score、dashboard、SEC PIT shadow、score attribution 和 market panel，但在 `market_data_freshness` 以 `daily-run direct dispatcher 不支持命令：data freshness --latest` 失败；未做手动补跑或临时绕行。2026-05-31: 从 READY 改为 IN_PROGRESS，原因：排查确认阻断不符合预期，属于 direct dispatcher coverage 缺口。2026-05-31: 修正任务 ID 为 `OPS-061`，原因：`OPS-060` 已被周期任务总入口 Runbook 使用。2026-05-31: 从 IN_PROGRESS 改为 DONE，原因：已补齐 dispatcher coverage 和防回归测试；验证通过 `tests/test_scheduled_tasks.py tests/test_ops_daily.py tests/test_cli_direct.py -q`（45 passed）以及真实 `daily_ops_run:2026-05-29:20260531T020123Z` 28/28 PASS。|
|TRADING-050|回测输入/Signal Snapshot Builder & Full-signal-limited Shadow Backtest|P0|DONE|系统验证|详细拆解见 `docs/requirements/signal_snapshot_builder_full_signal_limited_backtest_2026-05-30.md`；已实现 signal snapshot builder/validator/report alias、diagnostics mode 判定、backtest input manifest 集成、shadow backtest score calculation / attribution / parameter contribution summary、Dashboard/Reader Brief 展示和文档同步|`aits signals build-snapshot --latest` 生成 `artifacts/signal_snapshots/YYYY-MM-DD/signal_snapshot.json/md`；`aits signals validate-snapshot --latest` 输出 `status=LIMITED`、`real_signals>=2`、`fallback_signals>=1`、`missing_signals=0`；diagnostics 在 snapshot limited 时输出 `backtest_mode=full_signal_backtest_limited`、`can_run_shadow_backtest=true`、`can_promote_candidate=false`；shadow backtest dry-run 读取 snapshot、输出 score calculation / attribution / parameter contribution summary，promotion 最多 watch；所有输出保持 `production_effect=none`、`manual_review_required=true`、`auto_promotion=false`，不修改 `config/parameters/production/current.yaml`；目标验证通过|2026-05-30: 新增并进入 IN_PROGRESS，原因：TRADING-049 已使 price-only shadow backtest 可运行，下一阶段需要生成可审计 signal snapshots 并进入 full-signal-limited 模式。2026-05-30: 从 IN_PROGRESS 改为 DONE，原因：实现和真实 CLI 验收完成；`build-snapshot --latest` 生成 2026-05-28 snapshot，`status=LIMITED`、`real_signals=2`、`proxy_signals=1`、`fallback_signals=3`、`missing_signals=0`；`validate-snapshot --latest` 通过；`diagnose-backtest-inputs --latest` 输出 `price_data_status=OK`、`signal_snapshots_status=LIMITED`、`backtest_mode=full_signal_backtest_limited`、`can_run_shadow_backtest=true`、`can_promote_candidate=false`；`parameters shadow-backtest --latest --dry-run` 输出 `backtest_mode=full_signal_backtest_limited`、`promotion_status=rejected`，reason 明确 signal quality limited 不能 candidate；验证通过全量 `python -m pytest -q`（1420 passed, 1 warning）、全量 ruff、compileall 和 diff check。|
|BTINPUT-002|回测输入/shadow backtest diagnostics|P0|DONE|系统实现|详细拆解见 `docs/requirements/shadow_backtest_input_diagnostics_2026-05-29.md`；已补齐 shadow backtest input diagnostics、snapshot manifest、repair dry-run plan、shadow summary 引用、dashboard 和 Reader Brief 只读展示|`aits data diagnose-backtest-inputs --latest` 生成结构化 JSON/Markdown 和 backtest input manifest；`aits data repair-backtest-inputs --latest --dry-run` 输出可执行 repair plan；shadow backtest 失败时 summary/Markdown 明确引用 diagnostic report 和 blocking errors；dashboard/Reader Brief 显示最新 backtest data quality 状态；不修改 production 参数、promotion criteria 或真实交易路径；目标测试通过|2026-05-29: 完成实现和验证；当前本地 latest 诊断为 FAILED，阻断项为缺少 `GOOGL`、`BRK.B`、`SGOV` price history；actual repair 按本阶段边界返回 `NOT_IMPLEMENTED`。2026-05-30: 从当前任务表归档到 completed，原因：该行已为 DONE，需保持任务登记表只保留未完成任务。|
|TRADING-049|回测输入/Price History Repair & Price-only Shadow Baseline|P0|DONE|系统验证|详细拆解见 `docs/requirements/price_history_repair_price_only_shadow_baseline_2026-05-29.md`；已完成缺失价格历史 repair、price-only shadow backtest 模式和 promotion 禁止边界|`aits data repair-backtest-inputs --latest --price-only` 可幂等补齐主价格缓存并记录 provider/source symbol mapping；repair 后 diagnostics 显示 price/asset/date coverage OK、signal snapshots LIMITED、overall LIMITED、`can_run_shadow_backtest=true`、`can_promote_candidate=false`；`aits parameters shadow-backtest --latest --dry-run` 输出 `backtest_mode=price_only_shadow_backtest`、`production_effect=none`、`manual_review_required=true` 且 candidate promotion disabled；Dashboard/Reader Brief 显示 price-only 模式和 limited signal warning；不修改 production 参数|2026-05-29: 新增并进入 IN_PROGRESS，原因：latest backtest input diagnostics 已把 blocker 收敛为 `GOOGL`、`BRK.B`、`SGOV` price history 缺失。2026-05-29: 从 IN_PROGRESS 改为 DONE，原因：已实现 FMP/yfinance provider 复用的 price repair、`BRK.B -> BRK-B` mapping、manifest 审计、diagnostics LIMITED-but-runnable、shadow backtest price-only promotion constraints、Dashboard/Reader Brief 展示和测试；真实 `aits data repair-backtest-inputs --latest --price-only` 已补齐 `GOOGL`、`BRK.B`、`SGOV`，重新诊断为 `overall_status=LIMITED`、`price_data_status=OK`、`signal_snapshots_status=LIMITED`、`can_run_shadow_backtest=true`、`can_promote_candidate=false`；shadow dry-run 输出 `promotion_status=rejected`，reason 明确 signal snapshots LIMITED 不能 promotion。验证通过全量 `python -m pytest -q`（1412 passed, 1 warning）、全量 ruff、compileall 和 diff check。|
|CALIBRATION-020|市场反馈优化/Shadow Parameter Backtest & Promotion Framework|P1|BASELINE_DONE|系统验证|详细拆解见 `docs/requirements/shadow_parameter_backtest_promotion_framework_2026-05-29.md`；v0.1 observe-only 参数回测与晋升框架已完成，后续剩余工作是更严格 PIT 数据、长期 leaderboard 和人工 promotion workflow|新增 production 参数 schema、shadow backtest config、promotion rules config、parameter loader/diff、walk-forward 日频回测 skeleton、bounded candidate generator、promotion evaluator、JSON/Markdown report、`aits parameters shadow-backtest`、`aits parameters validate-shadow-backtest`、`aits reports shadow-parameter-backtest`、`aits reports parameter-promotion`、dashboard 只读卡片、Reader Brief 摘要、artifact catalog/system flow/runbook 更新和目标测试；所有输出固定 `production_effect=none`、`manual_review_required=true`、`auto_promotion=false`，不得修改 `config/parameters/production/current.yaml` 或任何 production weights/gates/broker/trading artifact；数据不足或 `aits validate-data` 失败必须输出 `INSUFFICIENT_DATA` / `FAILED` 并拒绝 candidate；2026-05-29 dry-run 样例因本地缓存质量门禁失败输出 `DEGRADED` / rejected，验证了保守拒绝路径|2026-05-29: 新增并进入 IN_PROGRESS，原因：owner 提供完整 Shadow Parameter Backtest & Promotion Framework 需求，需要在既有 shadow search/promotion contract 之外补齐 baseline 参数读取、walk-forward 对比、参数变化归因、保守晋升判断和只读报告闭环。2026-05-29: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：已实现 v0.1 schema/config、walk-forward backtest、bounded candidate、promotion evaluator、report/CLI/dashboard/Reader Brief 接线、治理文档和目标测试；验证通过目标 pytest、受影响 dashboard/Reader Brief/CLI dispatcher tests、ruff、black check，并完成 dry-run/validate/report alias smoke。2026-05-30: 从当前任务表归档到 completed，原因：该行已为 BASELINE_DONE，需保持任务登记表只保留仍需推进或验证的任务。|
|OPS-060|运维/周期任务总入口 Runbook|P1|DONE|系统验证|新增 `docs/operations/operations_runbook.md`，并更新 `AGENTS.md`，要求 Codex 执行任何周期任务前先读取该总入口；本次为文档-only 变更，不修改业务代码、不提交|operations runbook 覆盖 daily / weekly / biweekly / monthly / governance 任务；说明每日 scheduler trigger 是统一外部入口，较长周期任务只能通过日期和条件门控编排或人工按 runbook 执行；每日任务列出 daily PIT、`score-daily`、dashboard/latest checks、Reader Brief 和 `validate-reader-brief`；周期任务登记 weekly review、biweekly weight feedback、monthly governance、scheduler validation 和 artifact catalog consistency；AGENTS 明确执行周期任务前读取该 runbook|2026-05-29: 新增并直接归档为 DONE，原因：owner 要求补充周期任务总入口文档，范围限于 Markdown 文档和 AGENTS 指令，不改变 scheduler 代码或业务逻辑。|
|OPS-059|运维/Unified Daily Weekly Monthly Scheduled Task Orchestration Review|P1|DONE|系统验证|详细拆解见 `docs/requirements/scheduled_task_orchestration_review_2026-05-29.md`；已完成统一调度配置、daily-plan/daily-run 顺序约束、Reader Brief 上游链接入、非 daily cadence 登记、runbook、system flow/README 更新和专项测试|新增 `config/scheduled_tasks.yaml`，登记 `daily_trading_day`、`weekly`、`biweekly`、`monthly` 和 `ad_hoc_research`；`aits ops daily-plan` 必须列出 required daily commands 并按配置顺序校验；`aits ops daily-run` 在 `score-daily` 后执行 dashboard、SEC PIT shadow observe/monitor、score change attribution、market panel、report index、documentation contract、research governance summary、Reader Brief 和 Reader Brief quality；report index/documentation contract/research governance summary/SEC PIT shadow monitor 均在 Reader Brief 前；weekly/monthly/ad hoc tasks 登记但不 daily-run；closed-market mode 跳过 scoring/Reader Brief artifacts 并保留 health/PIT/SEC/valuation；reader/governance/shadow/report tasks 固定 `production_effect=none`、不写 production weights、不写 active shadow weights、不触发 broker/trading；测试覆盖命令顺序、闭市行为、非 daily 隔离和安全边界|2026-05-29: 新增并进入 IN_PROGRESS，原因：Reader Brief、SEC PIT shadow monitor、research governance、backtest、parameter/weight governance、documentation contract 和 report registry 产物已经形成，但缺少统一调度计划，存在上游链遗漏风险。2026-05-29: 从 IN_PROGRESS 改为 DONE，原因：已新增配置/loader/runbook/requirements，更新 daily-plan/daily-run/direct dispatcher/latest CLI 支持、system flow/README 和测试；验证通过 OPS-059 目标 pytest、ruff/black 和 diff check。|
|TRADING-046A|基本面/SEC PIT Shadow Monitor State Normalization|P1|DONE|系统验证|详细拆解见 `docs/requirements/sec_pit_shadow_monitor_state_normalization_2026-05-28.md`；已完成 SEC PIT shadow monitor 状态机归一化，把 monitor maturity 与 rolling metrics availability 拆开，修正 healthy coverage/sample/days 仍显示 `INSUFFICIENT_MONITORING_SAMPLE` 的误导性状态|coverage gate 通过但 minimum evidence 未达标时输出 `MONITORING_ACTIVE`；coverage gate、minimum sample、observation days 达标且无 warning/rollback 时输出 `OK_MONITORING`，rolling metrics 暂不完整只阻断 rollback，不再解释为 sample insufficiency；rollback 必须同时满足 coverage、minimum evidence、rolling metrics available 和 factor deterioration RankIC + outcome 双重确认；summary / Markdown / dashboard 展示 `monitor_maturity`、`rolling_metrics_available` 和 `state_transition_reason`；不修改 production weights、active shadow weights 或交易动作|2026-05-28: 新增并进入 IN_PROGRESS，原因：TRADING-046 latest run 已满足 coverage、样本、观察期和无 warning/rollback 条件，但状态仍为 `INSUFFICIENT_MONITORING_SAMPLE`。2026-05-28: 从 IN_PROGRESS 改为 DONE，原因：已完成状态机、summary schema 1.1、dashboard 只读展示、runbook/artifact catalog/learning path/system flow 和测试；真实 `python -m ai_trading_system.cli sec-pit shadow-monitor --latest` 输出 `OK_MONITORING`、`monitor_maturity=MINIMUM_EVIDENCE_ACHIEVED`、`rolling_metrics_available=false`、`rollback_recommended=false`、`warning_count=0`；验证通过 `python -m pytest tests/trading_engine/test_sec_pit_shadow_monitor.py -q`（9 passed）、`python -m pytest tests/test_daily_task_dashboard.py -q`（21 passed）、全量 `python -m pytest -q`（1326 passed, 1 warning）、全量 ruff 和触达 Python Black check。|
|TRADING-046|基本面/SEC PIT Shadow Observe Rolling Monitor|P1|DONE|系统验证|详细拆解见 `docs/requirements/sec_pit_shadow_monitor_2026-05-27.md`；已完成只读 rolling monitor，消费 TRADING-044/045 artifacts，为 `capex_intensity` observe-only lane 生成每日/每周滚动状态，不新增因子、不改权重、不触发交易|新增 `aits sec-pit shadow-monitor` / `--latest`；输出 summary JSON/Markdown、rolling metrics CSV、warning events CSV；TRADING-046A 后状态口径增加 `MONITORING_ACTIVE`，并把 `INSUFFICIENT_MONITORING_SAMPLE` 保留为历史兼容状态；baseline coverage gate 未通过或 factor underperformance 未经 RankIC + outcome 双重确认时不得 rollback；所有输出固定 `production_effect=none`、`manual_review_required=true`；daily task dashboard 新增只读 `SEC PIT Shadow Monitor` 卡片；专项测试覆盖 schema、Markdown、rolling metrics、warning events、insufficient/OK/rollback gating、dashboard read-only、determinism 和 no production config writes|2026-05-27: 新增并进入 IN_PROGRESS，原因：TRADING-045 已修正 baseline coverage gate 误判，当前剩余 `INSUFFICIENT_MONITORING_SAMPLE` 需要通过持续滚动监控沉淀证据，而不是继续扩展因子或调整权重。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：已完成核心 monitor、CLI、dashboard、runbook、artifact catalog、learning path、system flow 和测试；验证通过 `python -m pytest tests/trading_engine/test_sec_pit_shadow_monitor.py -q`（7 passed）、`python -m pytest tests/test_daily_task_dashboard.py -q`（21 passed）、TRADING-044/045 回归、全量 `python -m pytest -q`（1324 passed, 1 warning）、全量 ruff、触达 Python Black check 和 CLI help smoke。|
|TRADING-045B|基本面/SEC PIT Shadow Status Coverage Gate Alignment|P0|DONE|系统验证|TRADING-045 实跑后修正 shadow observe 顶层状态口径：baseline coverage gate 已通过时，少量 baseline gap 只进入 limitations，不再把 `shadow_status` 降级为 `LIMITED_BASELINE_MISSING`|当 `baseline_coverage_ratio >= min_baseline_coverage_ratio` 时，少量 missing baseline rows 不触发 `LIMITED_BASELINE_MISSING`；只有 coverage gate 未通过或 baseline 全量缺失时才输出 `LIMITED_BASELINE_MISSING`；新增专项测试覆盖 coverage gate pass with partial baseline gaps；重新跑 shadow-observe 后预期不再因 7 行 tail baseline gap 误报 baseline limited|2026-05-27: 新增并进入 IN_PROGRESS，原因：按 TRADING-045 链路实跑，baseline coverage audit `OK`、shadow summary `baseline_coverage_status=OK` 且 `monitoring_status=INSUFFICIENT_MONITORING_SAMPLE`，但 `shadow_status=LIMITED_BASELINE_MISSING`，说明顶层状态仍沿用旧 any-missing baseline 口径。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：已修正 shadow_status coverage gate 口径并新增测试；验证通过 `python -m pytest tests/trading_engine/test_sec_pit_shadow_observe.py -q`（12 passed）、全量 `python -m pytest -q`（1317 passed, 1 warning）、全量 `python -m ruff check config src tests scripts docs` 和触达 Python 文件 `python -m black --check ...`。|
|TRADING-045A|基本面/SEC PIT Research Baseline Backfill Path Safety|P0|DONE|系统验证|TRADING-045 后续轻量安全修正：`aits score-daily backfill-baseline` 默认输出到 `data/processed/research/` research-only 路径；production `data/processed/scores_daily.csv` 只有显式传 `--overwrite-production-path` 才允许作为 output path；runbook 和文档示例改为 research-only baseline path|默认 output path 不再指向 production score cache；显式 `--output-path data/processed/scores_daily.csv` 且缺少 `--overwrite-production-path` 时 fail closed；文件存在时仍需要 `--overwrite`；CLI summary 继续披露 `research_backfill=true`、`production_effect=none`；baseline backfill、baseline coverage 和 shadow-observe runbook 示例使用 `data/processed/research/scores_daily_backfill_sec_pit_2023_2026.csv`；artifact catalog 和 system flow 区分 production `scores_daily.csv` 与 research backfill CSV；新增测试覆盖 production path guard|2026-05-27: 新增并进入 IN_PROGRESS，原因：owner 指出 TRADING-045 已支持任意 output path，但 CLI 默认仍是 production `scores_daily.csv`，容易误写生产评分缓存。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：已完成 CLI path safety、文档示例和验证；验证通过 `python -m pytest tests/trading_engine/test_baseline_score_backfill.py -q`（4 passed）、全量 `python -m pytest -q`（1316 passed, 1 warning）、全量 `python -m ruff check config src tests scripts docs`、触达 Python 文件 `python -m black --check ...` 和 `score-daily backfill-baseline --help` smoke。|
|TRADING-045|基本面/SEC PIT Historical Baseline Score Backfill & Shadow Monitoring Coverage|P0|DONE|系统验证|详细拆解见 `docs/requirements/baseline_score_backfill_shadow_monitoring_2026-05-27.md`；已完成 research-only historical baseline score backfill、SEC PIT baseline coverage audit、shadow observe monitoring quality gate、dashboard 只读覆盖展示、artifact catalog、system flow、learning path 和 runbook 更新；完成后可重新跑 TRADING-044 observe-only shadow lane|新增 `aits score-daily backfill-baseline` 和 `aits sec-pit audit-baseline-coverage`；baseline backfill 先跑数据质量门禁，默认输出到 research-only 路径，输出 `baseline_score`、`baseline_rank`、`baseline_action`、score source/version、feature completeness、`research_backfill=true`、`production_effect=none`；coverage audit 输出 summary JSON/Markdown、by ticker、by date、gap CSV；shadow observe 区分 `LIMITED_BASELINE_MISSING`、`LIMITED_LABELS_MISSING`、`INSUFFICIENT_MONITORING_SAMPLE`、`ROLLBACK_TRIGGERED_BY_FACTOR`、`ROLLBACK_TRIGGERED_BY_DATA` 和 `FAILED_SAFETY_CHECK`，baseline/label/sample gates 未通过时不得标记 factor rollback；dashboard 只读读取 baseline coverage 和 shadow observe artifacts，不运行 audit/backfill/shadow-observe，不修改 production decisions、active trading behavior、production weights、active shadow weights 或 production scoring config|2026-05-27: 新增并进入 IN_PROGRESS，原因：TRADING-044 真实 observe-only run 的 `shadow_status=LIMITED_BASELINE_MISSING` 但 `monitoring_status=ROLLBACK_TRIGGERED`，需要修复 historical baseline score 覆盖和 monitoring 诊断。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：实现、文档和验证完成；验证通过 `python -m pytest tests/trading_engine/test_baseline_score_backfill.py -q`（3 passed）、`python -m pytest tests/trading_engine/test_sec_pit_baseline_coverage.py -q`（3 passed）、`python -m pytest tests/trading_engine/test_sec_pit_shadow_observe.py -q`（11 passed）、`python -m pytest tests/test_daily_task_dashboard.py -q`（21 passed）、全量 `python -m pytest -q`（1315 passed, 1 warning）、全量 `python -m ruff check config src tests scripts docs`、触达 Python 文件 `python -m black --check ...` 和新增 CLI help smoke。|
|TRADING-044|基本面/SEC PIT Observe-Only Shadow Lane|P1|DONE|系统验证|详细拆解见 `docs/requirements/sec_pit_shadow_observe_2026-05-27.md`；已完成隔离的 SEC PIT observe-only lane：`config/sec_pit_shadow_observe.yaml`、`aits sec-pit shadow-observe` / `--latest`、只读核心模块、shadow scores、rank shift、bucket comparison、monitoring plan、safety audit、summary JSON/Markdown、dashboard 只读卡片、artifact catalog、system flow、learning path 和 runbook 更新|`capex_intensity` 以 `observe_weight=-0.025`、`max_allowed_initial_weight=0.05` 进入 `sec_pit_capex_intensity_observe_only`；输出仅写 `outputs/sec_pit_shadow_observe`；所有相关输出固定 `manual_review_required=true`、`production_effect=none`；安全 audit 校验 candidate review status、diagnostics、provenance、data quality、drawdown label coverage、weight 上限、PIT grade、source lineage、baseline overlap 和输出隔离；critical safety failure 只写 safety audit 和 degraded summary；缺 baseline 或 candidate review 明确降级；dashboard 只读读取 latest summary，不运行 shadow scoring；不修改 production weights、production actions、active shadow weights 或既有非 SEC shadow lane；真实 `--latest --candidate-feature capex_intensity` 已生成 `outputs/sec_pit_shadow_observe/sec_pit_shadow_observe_summary_2026-05-26.*`，`shadow_status=LIMITED_BASELINE_MISSING`，原因是 baseline score 覆盖不完整，safety checks 全部通过，`monitoring_status=ROLLBACK_TRIGGERED` 暴露 platform bucket 弱期风险但不影响生产|2026-05-27: 新增并进入 IN_PROGRESS，原因：TRADING-043 真实 candidate review 将 `capex_intensity` 标记为 `READY_FOR_MANUAL_REVIEW`，但存在 mixed regime dependency 和 ticker-level 差异，owner 决策为 `APPROVE_OBSERVE_ONLY_SHADOW`，只允许进入隔离观察 lane，不得 promotion 或影响生产。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：实现、真实 observe-only artifacts、dashboard 和文档完成；验证通过 `python -m pytest tests/trading_engine/test_sec_pit_shadow_observe.py -q`（10 passed）、`python -m pytest tests/test_daily_task_dashboard.py -q`（21 passed）、`python -m pytest tests/trading_engine/test_sec_pit_candidate_review.py -q`（10 passed）、`python -m pytest tests/trading_engine/test_sec_pit_real_run_diagnostics.py -q`（10 passed）、全量 `python -m pytest -q`（1308 passed, 1 warning）、全量 `python -m ruff check config src tests scripts docs` 和触达 Python 文件 `python -m black --check ...`。|
|TRADING-043|基本面/SEC PIT Shadow Candidate Review & Evidence Pack|P1|DONE|系统验证|详细拆解见 `docs/requirements/sec_pit_candidate_review_2026-05-27.md`；已完成 `aits sec-pit review-candidates` / `--latest`、只读 candidate review 核心模块、summary JSON/Markdown、candidate evidence CSV、by-ticker CSV、by-period CSV、baseline overlap CSV、shadow proposal CSV、缺失 artifact / baseline fields 降级、dashboard 只读卡片、artifact catalog、system flow、learning path 和 runbook 更新|CLI 支持 `--start`、`--end`、`--evaluation-dir`、`--comparison-dir`、`--diagnostics-dir`、`--candidate-feature`、`--output-dir`、`--latest`；输出 evidence pack 用于人工判断 SEC PIT shadow candidate 是否适合 observe-only iteration；所有 evidence 固定 `manual_review_required=true`、`production_effect=none`，所有 proposal 固定 `review_required=true`、`production_effect=none`；不修改 production weights、approved overlay、score config 或 active shadow weights；baseline fields 缺失时 overlap 明确降级为 `LIMITED_BASELINE_FIELDS_MISSING`；dashboard 只读读取 latest review JSON，不运行 review 或上游 pipeline；真实 `--latest --candidate-feature capex_intensity` 已生成 `outputs/sec_pit_candidate_review/sec_pit_candidate_review_summary_2026-05-26.*`，`review_status=OK`，`capex_intensity` 为 `READY_FOR_MANUAL_REVIEW`|2026-05-27: 新增并进入 IN_PROGRESS，原因：TRADING-042A 修复并真实复跑后 `capex_intensity` 已进入 manual-review shadow candidate 状态，但下一步需要人工可复核 evidence pack，而不是自动 promotion。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：实现、真实 review artifacts、dashboard 和文档完成；验证通过 `python -m pytest tests/trading_engine/test_sec_pit_candidate_review.py -q`（10 passed）、`python -m pytest tests/test_daily_task_dashboard.py -q`（21 passed）、`python -m pytest tests/trading_engine/test_sec_pit_real_run_diagnostics.py -q`（10 passed）、全量 `python -m pytest -q`（1298 passed, 1 warning）、全量 `python -m ruff check config src tests scripts docs` 和触达 Python 文件 `python -m black --check ...`。|
|TRADING-042A|基本面/SEC PIT Provenance and Label Coverage Remediation|P0|DONE|系统验证|详细拆解见 `docs/requirements/sec_pit_provenance_label_coverage_remediation_2026-05-27.md`；已完成 TRADING-042 后续修复：重建 SEC PIT metrics/panel/evaluation/comparison/diagnostics，修复 feature panel 源头重复观测 deterministic dedupe、baseline comparison `source_lineage` 稳定 JSON 汇总、forward max drawdown label 非正数约束、diagnostics tail 20D label 缺失解释、diagnostics summary 推荐语和 ticker CLI 文档示例|真实复跑 `aits sec-pit build-metrics --to 2026-05-26`、`aits sec-pit build-panel --from 2023-01-01 --to 2026-05-26`、`aits sec-pit evaluate --start 2023-01-01 --end 2026-05-26 --tickers NVDA --tickers MSFT --tickers AMD --tickers AVGO --tickers GOOGL --tickers META --tickers AMZN`、`aits sec-pit compare-baseline --start 2023-01-01 --end 2026-05-26 --tickers NVDA --tickers MSFT --tickers AMD --tickers AVGO --tickers GOOGL --tickers META --tickers AMZN --baseline-score-path data/processed/scores_daily.csv`、`aits sec-pit diagnose-run --latest`；最新 diagnostics 为 `OK`，downstream missing provenance 0，first loss stage 空，coverage ratio > 1 feature 0，duplicates removed 0，drawdown label coverage 0.9444，tail forward/relative 20D label 缺失明确归因为未来价格窗口限制；decision impact `source_lineage` 可解析为 JSON；`max_drawdown_forward_20d` 范围为 [-0.0975, 0.0]；`capex_intensity` 只进入 manual-review shadow candidate 输出，`production_effect=none`，未修改 production/shadow weights 或 actions|2026-05-27: 新增并进入 IN_PROGRESS，原因：TRADING-042 诊断已定位 `first_loss_stage=xbrl_facts_long`、feature panel missing provenance 51,972 行、drawdown label coverage 0、coverage ratio 修正前大于 1 和 near-promotion 只受 data quality 阻断的 feature；owner 要求继续修复过程中遇到的所有问题。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：修复、真实复跑和文档完成；验证通过 `python -m pytest tests/test_sec_pit_backfill.py tests/trading_engine/test_sec_pit_evaluation.py tests/trading_engine/test_sec_pit_baseline_comparison.py tests/trading_engine/test_sec_pit_real_run_diagnostics.py -q`（37 passed）、`python -m pytest tests/test_daily_task_dashboard.py -q`（21 passed）、全量 `python -m pytest -q`（1288 passed, 1 warning）、全量 `python -m ruff check config src tests scripts docs` 和触达 Python 文件 `python -m black --check ...`。|
|TRADING-042|基本面/SEC PIT Real Run Diagnostics & Data Coverage Improvement|P0|DONE|系统验证|详细拆解见 `docs/requirements/sec_pit_real_run_diagnostics_2026-05-27.md`；已完成 `aits sec-pit diagnose-run` / `--latest`、只读 real-run diagnostics 核心模块、provenance gap / coverage / alias / label / candidate sensitivity CSV、summary JSON/Markdown、SEC provenance propagation、GOOG/GOOGL alias、baseline artifact fallback、coverage ratio 去重修正、evaluation drawdown label 传播、dashboard 只读卡片、artifact catalog、system flow、learning path、runbook、data source 和 feature availability 更新|CLI 支持 `--start`、`--end`、`--tickers`、`--feature-panel`、`--evaluation-dir`、`--comparison-dir`、`--baseline-score-path`、`--baseline-score-dir`、`--output-dir`、`--latest`；诊断产物能定位 provenance 首个明显丢失阶段、记录 baseline artifact、审计 alias、label coverage、coverage ratio 修正前后和 duplicate observations；candidate sensitivity 固定 `manual_review_required=true`、`production_effect=none`，不自动 promotion；dashboard 只读读取 latest diagnostics JSON，不运行 diagnostics；真实 `--latest` 已生成 `outputs/sec_pit_diagnostics/sec_pit_real_run_diagnostics_2026-05-26.*`，显示 `first_loss_stage=xbrl_facts_long`、feature panel missing provenance 51,972 行、baseline fallback 使用 `data/processed/scores_daily.csv`、drawdown coverage 0、修正前 coverage ratio > 1 的 feature 4 个、near-promotion 3 个；未修改 production/shadow weights 或 actions|2026-05-27: 新增并进入 IN_PROGRESS，原因：2026-05-26 真实 SEC PIT run 完成但不具备 shadow promotion 条件，暴露 provenance 缺失、GOOGL alias、baseline artifact discovery、drawdown label NaN、coverage ratio > 1 和候选敏感性诊断缺口。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：实现、真实诊断产物和文档完成；验证通过 `python -m pytest tests/trading_engine/test_sec_pit_real_run_diagnostics.py -q`（10 passed）、`python -m pytest tests/test_daily_task_dashboard.py -q`（21 passed）、`python -m pytest tests/trading_engine/test_sec_pit_evaluation.py -q`（11 passed）、`python -m pytest tests/trading_engine/test_sec_pit_baseline_comparison.py -q`（7 passed）、全量 `python -m pytest -q`（1286 passed, 1 warning）、全量 `python -m ruff check config src tests scripts docs` 和触达 Python 文件 `python -m black --check ...`。|
|TRADING-041|基本面/SEC PIT Baseline Comparison & Decision Impact Report|P1|DONE|系统验证|详细拆解见 `docs/requirements/sec_pit_baseline_comparison_2026-05-27.md`；已完成 `aits sec-pit compare-baseline`、只读 baseline comparison 核心模块、summary JSON/Markdown、decision impact CSV、rank shift CSV、incremental alpha CSV、缺失输入 LIMITED 模式、`--strict` fail-closed、dashboard 只读卡片、artifact catalog、system flow、learning path、runbook、data source 和 feature availability 更新|CLI 支持 `--start`、`--end`、`--sec-pit-evaluation-dir`、`--baseline-score-dir`、`--benchmark`、`--output-dir`、`--tickers`、`--strict`；输出 decision-level comparison artifact；缺 baseline 或 SEC PIT evaluation 时默认生成 LIMITED report；overlap 不足输出 `INSUFFICIENT_OVERLAP`；所有 decision impact 行固定 `manual_review_required=true`、`production_effect=none`；dashboard 只读读取 latest comparison summary，不运行 comparison、不读取 market data、不修改 production weights/actions；专项测试覆盖 summary/Markdown/CSV schema、missing baseline、missing evaluation、insufficient overlap、CLI、dashboard read-only 和 deterministic output|2026-05-27: 新增并进入 IN_PROGRESS，原因：TRADING-040 已能评估 SEC PIT feature-level evidence，但仍缺少与 price-only / score-daily baseline 的 decision-level 对比。2026-05-27: 从 IN_PROGRESS 改为 DONE，原因：实现和文档完成；验证通过 `python -m pytest tests/trading_engine/test_sec_pit_baseline_comparison.py -q`（7 passed）、`python -m pytest tests/test_daily_task_dashboard.py -q`（21 passed）、全量 `python -m pytest -q`（1276 passed, 1 warning）、全量 `python -m ruff check config src tests scripts docs` 和触达 Python 文件 `python -m black --check ...`。|
|TRADING-039|基本面/SEC EDGAR Reconstructed PIT Backfill|P0|DONE|系统验证|详细拆解见 `docs/requirements/sec_edgar_reconstructed_pit_backfill_2026-05-26.md`；SEC submissions/companyfacts raw fetch、filing timeline、XBRL fact long、metric mapping、PIT intervals/daily panel/feature panel、validation/report、CLI、score-daily/backtest 可选接入、配置、数据源目录、feature availability、system flow、artifact catalog、schema、learning path 和测试已完成；任务 ID 因 TRADING-035 至 TRADING-038 已占用按 owner 要求顺延|新增 `config/sec_pit_backfill.yaml`、`aits sec-pit fetch-raw/build-filing-timeline/build-facts/build-metrics/build-panel/validate/backfill`、`scripts/run_sec_pit_backfill.py`、`data/raw/sec_edgar/...` raw manifest schema、`data/processed/sec_edgar/filing_timeline.csv`、`xbrl_facts_long.csv`、`mapped_metrics_long.csv`、`fundamental_pit_intervals.csv`、`fundamental_pit_daily_panel.csv`、`sec_pit_feature_panel.csv`、`sec_pit_coverage_summary.csv`、`outputs/reports/sec_pit_backfill/*` validation/coverage/leakage/backfill reports；artifact 固定声明 `production_effect=none`、`backtest_data_grade=B_RECONSTRUCTED_SEC_FILING_PIT`、`strict_vendor_archive=false`、`external_side_effects=false`、`broker_access_required=false`、`paid_vendor_required=false`、`retroactive_strict_pit=false`、`manual_review_required_for_grade_upgrade=true`；loader 和 score/backtest 可选接入强制 `max_input_available_time_utc <= decision_date` / `available_for_signal_date <= decision_date`；validation 覆盖 checksum、B-grade accession join、future availability leakage、form whitelist、cross-currency ratio block、overlapping intervals、staleness、restatement availability 和 coverage thresholds；验证通过目标 SEC PIT 测试、SEC/FMP/PIT/backtest 回归、全量 pytest、ruff 和触达文件 black check|2026-05-26: 新增并进入 IN_PROGRESS，原因：owner 提供 TRADING-035 SEC EDGAR reconstructed PIT backfill 外部需求文档；当前 TRADING-035/036/037/038 已被通知投递审计和 retry 链路占用，本任务按顺延规则使用 TRADING-039。2026-05-26: 从 IN_PROGRESS 改为 DONE，原因：已完成 reconstructed filing-time PIT 数据层、SEC fair access/cache provider、manifest/checksum、timeline/facts/metrics/intervals/panel/report/validation、CLI、score-daily/backtest 可选接入、治理配置和文档；验证通过 `tests/test_sec_pit_backfill.py`、`tests/test_sec_companyfacts.py`、`tests/test_sec_metrics.py`、`tests/test_sec_features.py`、`tests/test_sec_filings.py`、`tests/test_fmp_forward_pit.py`、`tests/test_pit_snapshots.py`、`tests/test_external_request_cache.py`、`tests/test_backtest.py`、`tests/test_feature_availability.py`、`tests/test_data_sources.py`、全量 `python -m pytest -q`（1258 passed, 1 warning）、全量 `python -m ruff check` 和触达文件 `python -m black --check ...`。|
|TRADING-034|交易执行/Operator Brief Notification Draft Dispatch|P0|DONE|系统验证|详细拆解见 `docs/requirements/operator_brief_notification_draft_dispatch_2026-05-25.md`；只读 draft dispatch 核心模块、CLI、JSON/Markdown/latest/run.log、稳定 draft hash、TRADING-032 `body_markdown` schema 扩展、dashboard 只读卡片、runbook、system flow、artifact catalog、requirements 索引、专项测试和最终收尾验证已完成；本任务严格 draft-dispatch-only/read-only，不发送 email、不创建 Gmail draft、不调用 Gmail/SMTP/webhook、不访问网络、不读取 `.env` secrets、不修改 TRADING-032/TRADING-033 artifact 或 approval marker|新增 `scripts/run_operator_brief_notification_draft_dispatch.py` 和 `src/ai_trading_system/trading_engine/operator_brief_notification_draft_dispatch.py`；输出 `data/derived/operator_briefs/notifications/draft_dispatch/operator_brief_notification_draft_dispatch_YYYY-MM-DD.json/md`、`latest.json`、`latest.md`、`run.log`；支持 `DRAFT_READY` / `APPROVAL_REQUIRED` / `APPROVAL_EXPIRED` / `APPROVAL_MISMATCH` / `SAFETY_BLOCKED` / `BLOCKED` / `NOOP`；稳定 `draft_hash` 忽略 `generated_at` 等 volatile 字段；dashboard 只读读取 TRADING-034 `latest.json`，不重跑 draft dispatch 或任何发送链路；repo 外 smoke 覆盖正常 `DRAFT_READY`、缺少必要 artifact 的 `BLOCKED`、preview/gate hash 不匹配的 `APPROVAL_MISMATCH`、TRADING-032 safety blocked 的 `SAFETY_BLOCKED`、`latest.json` 更新、历史 dated artifact 未覆盖和 Dashboard 只读读取；全部 smoke 均确认 `production_effect=none`、`manual_review_only=true`、`draft_dispatch_only=true`、`read_only=true`，email/Gmail/Slack/Discord/webhook/mobile push/operator brief/pipeline/data/apply/rollback/broker/replay/trading flags 均为 false；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 diff check；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-25: 新增并进入 IN_PROGRESS，原因：TRADING-033 只判断 approval gate 是否允许进入下一阶段，仍缺少真实发送前唯一可审阅 draft dispatch artifact；本任务只生成本地草稿 artifact，并为未来 actual dispatch 提供唯一输入，不执行任何外部副作用。2026-05-25: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 draft dispatch builder、CLI、JSON/Markdown/latest/run.log、稳定 draft hash、TRADING-032 `body_markdown` 扩展、dashboard 只读卡片、runbook、system flow、artifact catalog、requirements 索引和专项测试；验证通过 `tests/trading_engine/test_operator_brief_notification_draft_dispatch.py`、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest、ruff 和 diff check；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-25: 从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 确认正常 draft dispatch、缺少必要 artifact、hash mismatch、safety blocked 和 latest 更新/历史 artifact 未覆盖场景；Dashboard import guard 确认 TRADING-034 卡片只读读取 latest.json，不触发 TRADING-034 script、approval gate、dispatch preview、operator brief、notification draft generator、email/Gmail/Slack/Discord/webhook/mobile push、market/backtest/scoring/data download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 diff check；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-030|交易执行/Operator Brief Notification Draft|P0|DONE|系统验证|详细拆解见 `docs/requirements/operator_brief_notification_draft_2026-05-24.md`；只读 notification draft generator、CLI、email/chat/mobile 草稿、metadata JSON、summary Markdown、run log、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和最终收尾验证已完成；本任务不发送 email、不创建 Gmail draft、不调用 Slack/Discord webhook、不推送手机通知、不运行 operator brief、scheduler、market/backtest/scoring/data download/apply/rollback/broker/replay/trading|新增 `scripts/generate_operator_brief_notification_draft.py`、`src/ai_trading_system/trading_engine/operator_brief_notification_draft.py`、`data/derived/operator_briefs/notifications/` 下 JSON/Markdown/channel draft/run log schema、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；覆盖输入缺失/非法、安全阻断、severity 映射、草稿输出、敏感字段 redaction、不发送通知、dashboard 只读和 output safety invariants；最终 repo 外 smoke 确认 NORMAL 生成 email/chat/mobile/summary/metadata、ACTION 包含 `Action Required`、URGENT 包含 `URGENT`、SAFETY_BLOCKED 明确标记 blocked、REDACTION 不泄露构造的 `api_key` / `token` / `password` / `credential` 值；所有 smoke 均确认 notification metadata 顶层安全边界为 `production_effect=none`、`manual_review_only=true`、`notification_draft_only=true`、`read_only=true`、`email_sent=false`、`gmail_draft_created=false`、`slack_sent=false`、`discord_sent=false`、`mobile_push_sent=false`、`operator_brief_executed_by_notification_draft=false`、`pipelines_executed_by_notification_draft=false`、`data_downloaded_by_notification_draft=false`、`apply_executed_by_notification_draft=false`、`rollback_executed_by_notification_draft=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard 只读读取 TRADING-030 metadata artifact，不触发 018B-029、TRADING-030 script、operator brief、email/Gmail/Slack/Discord/mobile push、market/backtest/scoring/data download/broker/replay/trading；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-022 已能生成系统级 operator brief，TRADING-026/027/028/029 已覆盖 scheduler dry-run、人工安装、模板和静态验证；下一阶段需要把 operator brief 压缩成可人工审阅和手动发送的通知草稿。本任务严格 draft-only/read-only，不发送任何通知、不创建外部系统草稿、不运行任何上游或交易流水线。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成核心 generator、CLI、notification metadata、email/chat/mobile 草稿、summary/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和测试；临时目录 smoke 覆盖 NORMAL、ACTION、URGENT、SAFETY_BLOCKED、REDACTION，且确认 sent flags 全为 false；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-24: 从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 再次确认 NORMAL、ACTION、URGENT、SAFETY_BLOCKED、REDACTION 五路径输出符合预期，metadata 安全边界全部通过，REDACTION 草稿未泄露构造的敏感值；dashboard import guard 验证 Operator Brief Notification Draft 卡片只读读取 TRADING-030 metadata artifact，不触发 018B-029、030 script、operator brief、email/Gmail/Slack/Discord/mobile push、market/backtest/scoring/data download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-029|交易执行/Scheduler Template Validation Report|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_operator_brief_scheduler_template_validation_2026-05-24.md`；只读 scheduler template validation report、CLI、validation JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和最终收尾验证已完成；本任务不安装 scheduler、不运行模板、不运行 operator brief、不运行 TRADING-021/022/023/024/026/028 或任何 market/backtest/scoring/data download/broker/replay/trading pipeline|新增 `scripts/validate_daily_operator_brief_scheduler_templates.py` 和核心 validator；读取 TRADING-028 metadata 与其声明的 Windows XML、PowerShell、batch、cron、GitHub Actions `.template` 文件；静态校验 metadata 安全边界、模板存在性、`.template` 后缀、路径安全、XML/YAML/cron 基础语法、PowerShell/batch 命令 allowlist、危险命令和危险路径；输出 JSON/Markdown/run log；所有输出固定 `production_effect=none`、`manual_review_only=true`、`scheduler_template_validation_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`templates_executed_by_validator=false`、`operator_brief_executed_by_validator=false`、`pipelines_executed_by_validator=false`、`data_downloaded_by_validator=false`、`apply_executed_by_validator=false`、`rollback_executed_by_validator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；dashboard 只读读取 TRADING-029 validation artifact，不触发 018B-028、TRADING-029 script、template generator、operator brief、scheduler creation 或任何 market/backtest/scoring/data download/broker/replay/trading；最终收尾验证确认 PASS / invalid XML FAIL / apply script SAFETY_BLOCKED / placeholder PASS_WITH_WARNINGS smoke、validation output 安全边界、dashboard import guard、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 Black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-028 已生成可审阅 scheduler `.template` 文件，但缺少独立静态验证层；本任务严格 validation-only，不安装 scheduler、不运行模板、不运行 operator brief、不运行任何上游或交易流水线。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成核心 validator、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；repo 外 smoke 确认正常模板 `PASS`、invalid XML `FAIL`、apply script `SAFETY_BLOCKED`、placeholder path `PASS_WITH_WARNINGS`；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-24: 从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 再次确认正常模板 `PASS`、invalid XML 输出 `FAIL`、模板包含 `run_shadow_promotion_apply.py` 输出 `SAFETY_BLOCKED`、placeholder path 输出 `PASS_WITH_WARNINGS`；四路径均确认 validation output 顶层安全边界为 `production_effect=none`、`manual_review_only=true`、`scheduler_template_validation_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`templates_executed_by_validator=false`、`operator_brief_executed_by_validator=false`、`pipelines_executed_by_validator=false`、`data_downloaded_by_validator=false`、`apply_executed_by_validator=false`、`rollback_executed_by_validator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard import guard 验证 Scheduler Template Validation Report 卡片只读读取 TRADING-029 artifact，不触发 018B-028、TRADING-029 script、template generator、operator brief、scheduler creation、market/backtest/scoring/data download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-028|交易执行/Scheduler Configuration Template Generator|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_operator_brief_scheduler_templates_2026-05-24.md`；template-only scheduler 配置模板生成器、CLI、metadata JSON、summary Markdown、Windows/PowerShell/batch/cron/GitHub Actions `.template` 输出、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和最终收尾验证已完成；本任务不安装或启用任何 scheduler，不运行 TRADING-021/022/023/024/026 或任何 market/backtest/scoring/data download/broker/replay/trading pipeline；后续可在 TRADING-029 增加模板语法验证，仍不得安装 scheduler|新增 `scripts/generate_daily_operator_brief_scheduler_templates.py` 和核心 builder；所有模板输出位于 `data/derived/operator_briefs/scheduler_templates/` 且以 `.template` 结尾；metadata 固定 `production_effect=none`、`manual_review_only=true`、`scheduler_template_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`operator_brief_executed_by_template_generator=false`、`pipelines_executed_by_template_generator=false`、`data_downloaded_by_template_generator=false`、`apply_executed_by_template_generator=false`、`rollback_executed_by_template_generator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；safety scan 阻断危险输出路径和危险执行命令；dashboard 只读读取 TRADING-028 metadata artifact；最终收尾验证确认 GENERATED smoke、SAFETY_BLOCKED smoke、template content checks、15 项 metadata safety invariant、全部模板文件 `.template` 后缀、无真实 Windows Task Scheduler XML 安装文件、无 crontab 文件、无 `.github/workflows/*.yml`、无 scheduler 自动安装脚本；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 Black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-027 已提供人工安装指南，但 Windows Task Scheduler、cron 和 GitHub Actions 配置仍需人工拼接，容易产生路径或命令错误；本任务只生成可审阅模板和 metadata，不创建、不安装、不启用 scheduler。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成核心 builder、CLI、模板输出、metadata/summary、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff、repo 外 GENERATED / SAFETY_BLOCKED / template content smoke；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-24: 从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 确认默认生成 `GENERATED` 且 5 个模板存在，危险 `.github/workflows` 输出为 `SAFETY_BLOCKED` 且不创建 unsafe 目录，模板内容全部包含 `TEMPLATE ONLY` 和 `Manual review required`，不包含 apply/rollback/schtasks/crontab 安装命令，active command 不包含 broker/replay/trading execution；metadata 15 项安全边界全部为预期 false/true；确认无真实 Windows Task Scheduler XML 安装文件、crontab 文件、`.github/workflows/*.yml` 或 scheduler 自动安装脚本；dashboard import guard 验证 Scheduler Configuration Templates 卡片只读读取 TRADING-028 metadata artifact，不触发 018B-027、TRADING-028 script、operator brief、scheduler creation、market/backtest/scoring/data download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-027|交易执行/Daily Operator Brief Scheduler Runbook and Manual Installation Guide|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_operator_brief_scheduler_manual_installation_2026-05-24.md`；Daily Operator Brief scheduler 人工安装 runbook、需求文档、system flow、artifact catalog 和最终收尾验证已完成；本任务只生成文档，不创建 Windows Task Scheduler、cron、GitHub Actions workflow 或 scheduler 自动安装脚本，不运行 TRADING-021/022/023/024/026、market/backtest/scoring/data download/broker/replay/trading；后续如需模板生成可另建 TRADING-028，仍不得自动安装 scheduler|新增 `docs/runbooks/daily_operator_brief_scheduler_manual_installation.md` 和需求文档；runbook 覆盖 Windows Task Scheduler 手动安装、cron 手动安装、GitHub Actions 可选示例、pre-installation checklist、safety checklist、dry-run decision 处理、disable / pause scheduler、failure handling 和人工验证；文档固定声明 `production_effect=none`、`manual_review_only=true`、`runbook_only=true`、`scheduler_created=false`、`operator_brief_executed_by_runbook=false`、`pipelines_executed_by_runbook=false`、`data_downloaded_by_runbook=false`、`apply_executed_by_runbook=false`、`rollback_executed_by_runbook=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；明确禁止 scheduler 运行 TRADING-018E2 apply、TRADING-018E3 rollback、broker execution、replay runner、trading execution、data download、market/backtest/scoring pipeline，除非未来任务明确审查；最终验证通过目标 pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-026 已能只读判断 operator brief 是否适合被调度，但真实 scheduler 启用前缺少人工安装 runbook、平台 checklist 和安全边界说明。本任务为文档 only，不新增执行逻辑或调度配置。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 manual installation runbook、需求文档、system flow 和 artifact catalog；验证通过 `tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest 和 ruff；Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-24: 从 VALIDATING 改为 DONE，原因：最终人工检查确认 runbook 包含 Windows Task Scheduler、cron、GitHub Actions 示例、pre-installation checklist、safety checklist、dry-run decision、disable / pause、failure handling 和不创建 scheduler 声明；确认本任务未新增 Windows Task Scheduler XML、cron file、GitHub Actions workflow 或 scheduler 自动安装脚本；确认文档明确禁止 scheduler 运行 apply/rollback/broker/replay/trading/data download/market/backtest/scoring pipeline；收尾验证通过目标 pytest、`tests/trading_engine`、全量 pytest 和 ruff；Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-026|交易执行/Daily Operator Brief Scheduler Dry Run|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_operator_brief_scheduler_dry_run_2026-05-24.md`；只读 scheduler dry-run 层、standalone script、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和最终收尾验证已完成；后续 TRADING-027 可只做人工 scheduler 安装指南，不自动创建真实 scheduler|新增 `scripts/run_daily_operator_brief_scheduler_dry_run.py`、`src/ai_trading_system/trading_engine/daily_operator_brief_scheduler_dry_run.py` 和 `data/derived/operator_briefs/scheduler_dry_run/daily_operator_brief_scheduler_dry_run_YYYY-MM-DD.json/md`、`scheduler_dry_run/logs/daily_operator_brief_scheduler_dry_run_log_YYYY-MM-DD.json/md`；输出 `dry_run_decision`（`READY` / `READY_WITH_WARNINGS` / `NOT_READY` / `SAFETY_BLOCKED` / `ERROR`）、`dry_run_status`（`OK` / `WATCH` / `ACTION_REQUIRED` / `SAFETY_BLOCKED` / `ERROR`）、schedule plan、input artifact checksum、dependency check、safety check、expected operator brief behavior、alerts 和 run log；所有输出固定 `production_effect=none`、`manual_review_only=true`、`scheduler_dry_run_only=true`、`read_only=true`、`scheduler_created=false`、`operator_brief_executed_by_scheduler_dry_run=false`、`pipelines_executed_by_scheduler_dry_run=false`、`data_downloaded_by_scheduler_dry_run=false`、`apply_executed_by_scheduler_dry_run=false`、`rollback_executed_by_scheduler_dry_run=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；dashboard 只读展示 latest TRADING-026 artifact，不触发 018B-025、026 script、operator brief、scheduler creation 或任何交易/数据 pipeline；最终收尾验证通过 repo 外 READY / READY_WITH_WARNINGS / NOT_READY / SAFETY_BLOCKED smoke、Markdown banner/section 检查、dashboard import guard、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-022 已消费 TRADING-021/023/024 并生成系统级每日 operator brief，owner 要求在真实定时任务前增加只读 scheduler dry-run，验证输入齐全性、安全降级语义和无 apply/rollback/broker/replay/trading 风险。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 scheduler dry-run builder、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 repo 外四路径 smoke；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-24: 从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 再次确认 READY 输出 `READY/OK` 且 Markdown 包含 Dry Run Summary / Dependency Check / Safety Check，READY_WITH_WARNINGS 顶部包含 `Scheduler Dry Run Ready With Warnings`，NOT_READY 顶部包含 `Scheduler Dry Run Not Ready`，SAFETY_BLOCKED 顶部包含 `Scheduler Dry Run Safety Blocked`；四路径均确认 dry-run 顶层 `production_effect=none`、`manual_review_only=true`、`scheduler_dry_run_only=true`、`read_only=true`、`scheduler_created=false`、`operator_brief_executed_by_scheduler_dry_run=false`、`pipelines_executed_by_scheduler_dry_run=false`、`data_downloaded_by_scheduler_dry_run=false`、`apply_executed_by_scheduler_dry_run=false`、`rollback_executed_by_scheduler_dry_run=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard import guard 验证 Daily Operator Brief Scheduler Dry Run 卡片只读读取 TRADING-026 artifact，不触发 018B-025、026 script、operator brief、scheduler creation、market/backtest/scoring/data download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-025|交易执行/Operator Brief Health Freshness 接入|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_trading_system_operator_brief_2026-05-23.md#trading-025-health--freshness-接入`；TRADING-022 已只读消费 TRADING-023 pipeline health summary 和 TRADING-024 data freshness summary，最终收尾验证已完成|Operator brief CLI 支持显式传入 pipeline health / data freshness summary JSON；默认自动查找 latest 023/024 artifact，缺失时对应状态为 `UNKNOWN` 且 brief 降级 `WATCH`；存在时校验 023/024 safety fields，异常输出 `SAFETY_BLOCKED`；`brief_status`、`summary_level`、trust/manual action、alerts、pending manual actions、Markdown 和 dashboard 卡片均消费真实 023/024 状态；所有输出安全 invariant 保持不变；Dashboard 只读读取 TRADING-022 operator brief artifact 并展示 `pipeline_health.status`、`pipeline_health.health_status`、`data_freshness.status`、`data_freshness.freshness_status`、missing/stale pipeline/source counts；收尾验证通过 repo 外 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-023/024 已生成独立只读 summary artifact，TRADING-022 仍需把 `pipeline_health` / `data_freshness` 从 legacy optional artifact 读取升级为消费真实 summary。安全边界：TRADING-025 不运行 021/023/024 或任何 market/backtest/scoring/data download/broker/replay/trading pipeline。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 operator brief builder、CLI、Markdown、alert/pending action 合并、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED repo 外 smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-24: 从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 再次确认 OK 输出真实 Pipeline Health / Data Freshness 状态，WATCH 顶部包含 `Watch: Monitoring Recommended` 且缺失 023/024 时为 `UNKNOWN` 而非 `INPUT_MISSING`，ACTION_REQUIRED / URGENT / SAFETY_BLOCKED Markdown banner 正确；五路径均确认 operator brief 输出固定 `production_effect=none`、`manual_review_only=true`、`operator_brief_only=true`、`read_only=true`、`apply_executed_by_operator_brief=false`、`rollback_executed_by_operator_brief=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；输入 023/024 artifact checksum 生成前后不变；dashboard import guard 验证 Daily Trading System Operator Brief 卡片只读读取 TRADING-022 artifact，不触发 TRADING-023/024、market/backtest/scoring/data download/broker/replay/trading，并展示 health/freshness 状态和 missing/stale counts；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|
|TRADING-024|交易执行/Data Freshness Summary|P0|DONE|系统验证|详细拆解见 `docs/requirements/data_freshness_summary_2026-05-23.md`；只读 data freshness summary artifact、CLI、dashboard 卡片、runbook、system flow、artifact catalog、测试和最终收尾验证已完成；后续 TRADING-022 如需直接消费该 artifact，应另建 TRADING-025 只读接入任务，不混入本任务|新增 `scripts/run_data_freshness_summary.py` 和 `data/derived/data_freshness/data_freshness_summary_YYYY-MM-DD.json/md`、`data_freshness/logs/data_freshness_summary_run_YYYY-MM-DD.json/md`；覆盖 required/optional source registry、JSON date_fields / filename / modified time 日期抽取、missing/stale/status/safety field 扫描、overall `freshness_status`、Markdown banners、run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和测试；所有输出固定 `production_effect=none`、`manual_review_only=true`、`data_freshness_only=true`、`read_only=true`、`data_downloaded_by_freshness_check=false`、`pipelines_executed_by_freshness_check=false`、`apply_executed_by_freshness_check=false`、`rollback_executed_by_freshness_check=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；Dashboard 只读读取 TRADING-024 artifact，不触发 018B-023、TRADING-024 script、data download、market/backtest/scoring/broker/replay/trading；最终收尾验证通过 repo 外 OK/MISSING/CRITICAL/STALE/optional-missing smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-23: 新增并进入 IN_PROGRESS，原因：TRADING-022 operator brief 的 `data_freshness.status` 仍为 `UNKNOWN`，需要先补一个独立只读 data freshness artifact；本任务只扫描已有文件和 metadata，不刷新数据、不触发任何上游 pipeline。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 data freshness summary builder、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过新增专项 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 repo 外 OK/MISSING/CRITICAL/STALE smoke；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-24: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture；OK 输出 `OK` 且 Markdown 包含 `Freshness Summary`、`Required Sources`、`Optional Sources`；缺失 required artifact 输出 `MISSING` 且 Markdown 顶部包含 `Required Data Missing`；required artifact `broker_execution=true` 输出 `CRITICAL`，Markdown 顶部包含 `CRITICAL: Data Freshness Issue Detected` 且 critical alerts 非空；required artifact stale 输出 `STALE`，Markdown 顶部包含 `Stale Required Data Detected` 且 stale required sources 非空；`include_optional_sources=true` 下 optional missing 输出 `WATCH` 而不是 `MISSING`。所有 smoke 输出均确认 `production_effect=none`、`manual_review_only=true`、`data_freshness_only=true`、`read_only=true`、`data_downloaded_by_freshness_check=false`、`pipelines_executed_by_freshness_check=false`、`apply_executed_by_freshness_check=false`、`rollback_executed_by_freshness_check=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard import guard 验证 Data Freshness Summary 卡片只读读取 TRADING-024 artifact，不触发 018B-023、TRADING-024 script、market/backtest/scoring/data download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-023|交易执行/Pipeline Health Summary|P0|DONE|系统验证|详细拆解见 `docs/requirements/pipeline_health_summary_2026-05-23.md`；只读 pipeline health summary artifact、CLI、dashboard 卡片、runbook、system flow、artifact catalog、测试和最终收尾验证已完成；后续 TRADING-022 如需直接消费该 artifact，应另建只读接入小任务，不混入本任务|新增 `scripts/run_pipeline_health_summary.py` 和 `data/derived/pipeline_health/pipeline_health_summary_YYYY-MM-DD.json/md`、`pipeline_health/logs/pipeline_health_summary_run_YYYY-MM-DD.json/md`；覆盖 TRADING-018B 到 022 的 required/optional artifact registry、missing/stale/status/safety field 扫描、overall health_status、Markdown banners、run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和测试；所有输出固定 `production_effect=none`、`manual_review_only=true`、`pipeline_health_only=true`、`read_only=true`、`pipelines_executed_by_health_check=false`、`apply_executed_by_health_check=false`、`rollback_executed_by_health_check=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；Dashboard 只读读取 TRADING-023 artifact，不触发 018B-022、TRADING-023 script、market/backtest/scoring/broker/replay/trading；最终收尾验证通过 repo 外 OK/INCOMPLETE/CRITICAL/STALE smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-23: 新增并进入 IN_PROGRESS，原因：TRADING-022 operator brief 的 `pipeline_health` 仍可能为 UNKNOWN，需要先补一个独立只读 health artifact；本任务只做 artifact health summary，不运行或补造任何 pipeline。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 pipeline health summary builder、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过新增专项 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 repo 外 OK/INCOMPLETE/CRITICAL/STALE smoke；全仓 Black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未格式化无关文件。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 四路径 smoke；OK 输出 `OK`，Markdown 包含 `Health Summary`、`Required Pipelines`、`Optional Pipelines`；缺失 required artifact 输出 `INCOMPLETE`；required artifact `broker_execution=true` 输出 `CRITICAL`，Markdown 顶部显示 `CRITICAL: Pipeline Health Issue Detected` 且 critical alerts 非空；required artifact stale 输出 `ACTION_REQUIRED`，Markdown 顶部显示 `Action Required` 且 stale required pipelines 非空；`include_optional_pipelines=true` 下 optional missing 输出 `WATCH` 而不是 `INCOMPLETE`；所有 smoke 输出和 run log 均确认 `production_effect=none`、`manual_review_only=true`、`pipeline_health_only=true`、`read_only=true`、`pipelines_executed_by_health_check=false`、`apply_executed_by_health_check=false`、`rollback_executed_by_health_check=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard import guard 验证 Pipeline Health Summary 卡片只读读取 TRADING-023 artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/020/021/022、TRADING-023 script、market/backtest/scoring/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-022|交易执行/Daily Trading System Operator Brief|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_trading_system_operator_brief_2026-05-23.md`；系统级只读 daily operator brief、standalone script、JSON/Markdown/run log、dashboard 只读卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；后续 pipeline health artifact 可在 TRADING-023 单独推进，不混入本任务|新增 `scripts/run_daily_trading_system_operator_brief.py` 和 `src/ai_trading_system/trading_engine/daily_trading_system_operator_brief.py`；输出 `data/derived/operator_briefs/daily_trading_system_operator_brief_YYYY-MM-DD.json/md` 与 `logs/daily_trading_system_operator_brief_run_YYYY-MM-DD.json/md`；只读读取 latest TRADING-021 digest，必须校验 `task_id` 与 digest 安全字段；可选读取 pipeline health、data freshness、market report、backtest / weight iteration artifacts，缺失时只使对应模块 `UNKNOWN`，不得导致 `INPUT_MISSING`；状态覆盖 OK/WATCH/ACTION_REQUIRED/URGENT/INPUT_MISSING/INPUT_INVALID/SAFETY_BLOCKED/ERROR；所有输出固定 `production_effect=none`、`manual_review_only=true`、`operator_brief_only=true`、`read_only=true`、`apply_executed_by_operator_brief=false`、`rollback_executed_by_operator_brief=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；dashboard 只读读取 operator brief artifact，不运行 018B-022、market/backtest/scoring/broker/replay/trading；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 repo 外 smoke 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-021 参数治理 daily digest 后增加系统级每日运维简报，回答整体交易系统健康、pipeline 状态、数据新鲜度、artifact 缺失、人工待处理事项以及当天是否可信任系统输出；本任务只聚合已有状态，不重新运行任何 pipeline。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 operator brief builder、CLI、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过 TRADING-022 目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 repo 外 OK/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED smoke；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 四路径 smoke；OK 输出 `OK/NORMAL` 且 Markdown 包含 `Continue observation`，ACTION_REQUIRED 顶部显示 `Action Required` 且 pending manual actions 为 true，URGENT 顶部显示 `URGENT: Manual Attention Required` 且 critical alert 非空，SAFETY_BLOCKED 顶部显示 `Operator Brief Safety Blocked`；四路径均确认 operator brief 顶层 `production_effect=none`、`manual_review_only=true`、`operator_brief_only=true`、`read_only=true`、`apply_executed_by_operator_brief=false`、`rollback_executed_by_operator_brief=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`，且 optional artifacts 缺失时显示 `UNKNOWN` 而不是失败；dashboard import guard 验证 Daily Trading System Operator Brief 卡片只读读取 operator brief artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/020/021/022/market/backtest/scoring/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-021|交易执行/Parameter Governance Daily Digest|P0|DONE|系统验证|详细拆解见 `docs/requirements/parameter_governance_daily_digest_2026-05-23.md`；只读 daily digest 生成器、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；TRADING-022 如需整合全系统 operator brief 应另建任务，不混入本任务|新增 `scripts/run_parameter_governance_daily_digest.py`、`src/ai_trading_system/trading_engine/parameter_governance_daily_digest.py` 和 `data/derived/weight_iterations/governance/digests/parameter_governance_daily_digest_YYYY-MM-DD.json/md`、`digests/logs/parameter_governance_daily_digest_run_YYYY-MM-DD.json/md`；只读读取 latest TRADING-019 summary，可选读取 TRADING-020 metadata / 018C2 review / 018F lifecycle audit，不重跑 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/020，不修改 production/shadow，不执行 promotion/apply/rollback，不触发 scoring/broker/replay/trading；输出固定 `production_effect=none`、`manual_review_only=true`、`digest_only=true`、`governance_only=true`、`apply_executed_by_digest=false`、`rollback_executed_by_digest=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；覆盖 OK/WATCH/ACTION_REQUIRED/URGENT/INPUT_MISSING/INPUT_INVALID/SAFETY_BLOCKED/ERROR、Markdown banners、weight snapshot、pending items、critical/warning/notes、dashboard 只读卡片和 repo 外 smoke；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-019 summary 和 TRADING-020 web view 后增加每日快速阅读的参数治理摘要；本任务只压缩和解释既有 summary，不生成新治理判断，不扩展到全系统 operator brief。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：digest builder、CLI、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和 repo 外 smoke 已完成；验证通过目标 pytest 21 passed、dashboard pytest 8 passed、`tests/trading_engine` 389 passed / 1 warning、全量 pytest 973 passed / 1 warning、ruff passed、触达文件 black check passed；全仓 black check 被既有 `tests/test_market_data.py` 格式 baseline 阻断，未格式化无关文件。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 四路径 smoke；OK 输出 `OK/NORMAL` 且 Markdown 包含 `Continue observation`，ACTION_REQUIRED 顶部显示 `Action Required` 且 `pending_apply=true` 渲染为 pending apply，URGENT 顶部显示 `URGENT: Manual Attention Required` 且 critical findings 渲染在 Alerts/Critical，SAFETY_BLOCKED 顶部显示 `Digest Safety Blocked`；四路径均确认 digest 顶层 `production_effect=none`、`manual_review_only=true`、`digest_only=true`、`governance_only=true`、`apply_executed_by_digest=false`、`rollback_executed_by_digest=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard import guard 验证 Parameter Governance Daily Digest 卡片只读读取 digest artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/020/021/scoring/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-020|交易执行/Parameter Governance Web View|P0|DONE|系统验证|详细拆解见 `docs/requirements/parameter_governance_web_view_2026-05-23.md`；只读 HTML web view 渲染器、render metadata、dashboard 只读 metadata 卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务|新增 `scripts/render_parameter_governance_web_view.py`、`src/ai_trading_system/trading_engine/parameter_governance_web_view.py` 和 `data/derived/weight_iterations/governance/web/parameter_governance_web_view_YYYY-MM-DD.html/json`；只读读取 `parameter_governance_summary_YYYY-MM-DD.json`，校验 `task_id=TRADING-019` 和 summary 安全边界；正常输出 `render_decision=RENDERED`，安全异常输出 `SAFETY_BLOCKED` blocked report，缺失/非法输入输出 `INPUT_MISSING` / `INPUT_INVALID`；HTML 单文件静态、内联 CSS、不访问网络、不触发 API，展示 governance_state、action_required/action_level、recommended_action、production vs shadow weights、shadow review、promotion lifecycle timeline、pending items、safety boundary audit、findings/warnings/notes 和 artifact paths；所有 TRADING-020 输出固定 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`web_view_only=true`、`apply_executed_by_web_view=false`、`rollback_executed_by_web_view=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard 只能读取 020 render metadata，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/020、scoring、broker、replay 或 trading execution；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-019 summary 后新增只读 Parameter Governance Web View，让人工能直观看到治理状态、pending actions、safety boundary、production/shadow 差异和 lifecycle；本任务不允许修改 production/shadow，不执行 promotion/apply/rollback，不重跑任何上游 pipeline。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成只读 web view renderer、CLI、HTML、metadata、dashboard 只读卡片、runbook、system flow / artifact catalog 更新和测试；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；repo 外临时 fixture smoke 验证 `RENDERED`、`SAFETY_ANOMALY` 和 `SAFETY_BLOCKED`；全仓 black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 三路径 smoke；`RENDERED` 页面包含 governance_state、action_level、production vs shadow weights、promotion lifecycle timeline、pending items 和 safety boundary audit；`SAFETY_ANOMALY` 页面顶部显示 urgent banner 且 critical findings 已 HTML escape；`SAFETY_BLOCKED` 对 summary `broker_execution=true` 明确显示 render blocked；三路径均确认 render metadata 顶层 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`web_view_only=true`、`apply_executed_by_web_view=false`、`rollback_executed_by_web_view=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard import guard 验证 Parameter Governance Web View 卡片只读读取 render metadata artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/020/scoring/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-019|交易执行/Parameter Governance Dashboard|P0|DONE|系统验证|详细拆解见 `docs/requirements/parameter_governance_summary_2026-05-23.md`；只读 summary builder、CLI、JSON/Markdown/run log、dashboard 总览卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务|新增 `scripts/run_parameter_governance_summary.py`、`src/ai_trading_system/trading_engine/parameter_governance_summary.py` 和 `data/derived/weight_iterations/governance/parameter_governance_summary_YYYY-MM-DD.json/md`、`governance/logs/parameter_governance_summary_run_YYYY-MM-DD.json/md`；只读读取 production profile、current shadow weights、latest 018C2 review、018D proposal、018E1 preflight、018E2 apply result、018E3 rollback result 和 018F lifecycle audit；输出 governance_state、action_required/action_level、pending_items、promotion status、production/shadow weights、safety boundary audit 和 findings；所有输出固定 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；dashboard 只能读取 summary artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019、scoring、broker、replay 或 trading execution|2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 shadow weight / promotion governance 闭环后新增统一只读治理视图，用于聚合 production/shadow、proposal/preflight/apply/rollback/lifecycle 状态和人工行动需求；本任务不允许修改 production/shadow、不执行 promotion/apply/rollback、不重跑任何上游 pipeline。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成只读 summary builder、CLI、JSON/Markdown/run log、dashboard 只读卡片、runbook、system flow / artifact catalog 更新和测试；repo 外临时 fixture smoke 成功验证 `ROLLBACK_COMPLETED`、`PREFLIGHT_READY` 和 `SAFETY_ANOMALY`；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 三路径 smoke；`ROLLBACK_COMPLETED` 输出 `action_required=false`、`safety_boundary_audit.status=PASS`，`PREFLIGHT_READY` 输出 `pending_apply=true`、`action_level=APPROVAL_REQUIRED`，`SAFETY_ANOMALY` 输出 `action_required=true`、`action_level=URGENT` 且 critical findings 非空；三路径均确认 summary 顶层 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard import guard 验证 Parameter Governance Summary 卡片只读读取 governance summary artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/scoring/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-018F|交易执行/Promotion Lifecycle Audit Report|P0|DONE|系统验证|详细拆解见 `docs/requirements/shadow_promotion_lifecycle_audit_2026-05-23.md`；只读 lifecycle audit builder、CLI、JSON/Markdown/run log、artifact chain 校验、安全边界审计、weight lifecycle、dashboard 只读卡片、runbook、system flow / artifact catalog、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务|新增 `scripts/run_shadow_promotion_lifecycle_audit.py`、`src/ai_trading_system/trading_engine/shadow_promotion_lifecycle_audit.py` 和 `data/derived/weight_iterations/promotion/audit/shadow_promotion_lifecycle_audit_YYYY-MM-DD.json/md`、`audit/logs/shadow_promotion_lifecycle_audit_run_YYYY-MM-DD.json/md`；报告固定 `production_effect=none`、`manual_review_only=true`、`audit_only=true`、`apply_executed_by_audit=false`、`rollback_executed_by_audit=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；只读读取已有 018D/018E1/018E2/可选 018E3 artifacts，不重跑 018B/018C/018C2/018D/018E1/018E2/018E3，不执行 apply/rollback，不写 production；dashboard 只读读取 latest audit artifact，不触发 018F 或任何上游/交易 pipeline|2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 018D/018E1/018E2/018E3 安全闭环后补齐只读 lifecycle audit report，用于回答 proposal 到 preflight/apply/rollback 的 artifact chain、weight lifecycle 和安全边界是否完整一致；本任务不允许修改 production 或触发任何上游 pipeline。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 lifecycle audit builder、CLI、JSON/Markdown/run log、dashboard 只读展示、runbook、system flow / artifact catalog 更新和测试；repo 外临时 fixture smoke 成功验证 `COMPLETE_WITH_ROLLBACK`、`COMPLETE_APPLIED_NO_ROLLBACK` 和 `SAFETY_ANOMALY`，完整生命周期 net delta 为 0，broker/replay/trading 均为 false；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 三路径 smoke；`COMPLETE_WITH_ROLLBACK` 输出 artifact_chain PASS、safety_boundary PASS、net delta 全为 0；`COMPLETE_APPLIED_NO_ROLLBACK` 输出 warning 且 after_rollback 为 null，不视为 safety anomaly；`SAFETY_ANOMALY` 对 apply artifact `broker_execution=true` 输出 critical finding；三路径均确认 audit 顶层 `production_effect=none`、`audit_only=true`、`apply_executed_by_audit=false`、`rollback_executed_by_audit=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard import guard 验证 Shadow Promotion Lifecycle Audit 卡片只读读取 audit artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/scoring/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-018E3|交易执行/Explicit Approved Shadow Promotion Rollback Command|P0|DONE|系统验证|详细拆解见 `docs/requirements/shadow_promotion_rollback_2026-05-23.md`；explicit rollback command、单独 rollback approval、danger flag、apply result / rollback snapshot / target profile hash gate、rollback-current snapshot、weights-only restore、dashboard 只读卡片、runbook、system flow、artifact catalog、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务|新增 `scripts/run_shadow_promotion_rollback.py`、`src/ai_trading_system/trading_engine/shadow_promotion_rollback.py` 和 `data/derived/weight_iterations/promotion/rollback_results/shadow_promotion_rollback_result_YYYY-MM-DD.json/md`、`rollback_results/logs/shadow_promotion_rollback_run_YYYY-MM-DD.json/md`、`promotion/rollback_current_snapshots/production_profile_before_rollback_YYYY-MM-DD.json/.sha256`；只有 018E2 apply result 为 `APPLIED`、rollback approval 明确授权、rollback snapshot sha256 匹配、当前 production profile 仍等于 apply 后 hash 且 danger flag 存在时，才原子恢复 target profile weights；失败仍生成 result artifact 且未执行时 `production_effect=none`；所有输出固定 `manual_review_only=true`、`safe_for_scheduler=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard 只读读取 rollback result，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading|2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018E2 已能创建 rollback snapshot 后补齐显式人工批准 rollback command；apply result 不能充当 rollback approval，本任务不允许 scheduler 或 dashboard 触发 rollback。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 rollback builder、CLI、JSON/Markdown/run log、rollback-current snapshot、approval/apply/snapshot/danger/target/post-rollback validation、dashboard 只读展示、runbook、system flow / artifact catalog 更新和测试；repo 外 smoke 成功验证 `ROLLED_BACK` 恢复 weights 并生成 current snapshot，缺 danger flag 输出 `DANGER_FLAG_MISSING` 且 target profile hash 不变；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 成功/失败 smoke；成功路径输出 `rollback_decision=ROLLED_BACK`、`rollback_executed=true`，production profile weights 恢复为 rollback snapshot weights，current snapshot 已生成且 snapshot sha256 等于 rollback 前 production profile sha256，`post_rollback_validation.status=PASS`，`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；失败路径缺 danger flag 输出 `rollback_decision=DANGER_FLAG_MISSING`、`rollback_executed=false`、`production_effect=none` 且 production profile sha256 不变；dashboard import guard 验证 Shadow Promotion Rollback Result 卡片只读读取 rollback result artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-018E2|交易执行/Explicit Approved Shadow Promotion Apply Command|P0|DONE|系统验证|详细拆解见 `docs/requirements/shadow_promotion_apply_2026-05-23.md`；explicit apply command、单独 apply approval、danger flag、target profile hash gate、rollback snapshot、post-apply validation、dashboard 只读卡片、runbook、system flow、artifact catalog、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务|新增 `scripts/run_shadow_promotion_apply.py`、`src/ai_trading_system/trading_engine/shadow_promotion_apply.py` 和 `data/derived/weight_iterations/promotion/apply/shadow_promotion_apply_result_YYYY-MM-DD.json/md`、`apply/logs/shadow_promotion_apply_run_YYYY-MM-DD.json/md`、`promotion/rollback/production_profile_before_shadow_promotion_YYYY-MM-DD.json/.sha256`；成功 apply 仅修改目标 production profile 的 weights，失败生成 result artifact 且 `production_effect=none`；所有输出固定 `manual_review_only=true`、`safe_for_scheduler=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard 只读展示 latest apply result artifact，不重跑 018B/018C/018C2/018D/018E1/018E2/scoring/broker/replay/trading；rollback command 留给 TRADING-018E3|2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 018E1 preflight-only 后实现首次严格人工授权下的 production profile weights apply；本任务不实现 rollback command，rollback 留给 TRADING-018E3。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 explicit apply builder、CLI、JSON/Markdown/run log、rollback snapshot、approval/preflight/danger/target/post-apply validation、dashboard 只读展示、runbook、system flow / artifact catalog 更新和测试；repo 外 smoke 成功验证 `APPLIED` 写入 weights 并生成 rollback，缺 danger flag 输出 `DANGER_FLAG_MISSING` 且 target profile hash 不变；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证通过 repo 外临时 fixture 成功/失败 smoke；成功路径输出 `apply_decision=APPLIED`，production profile sha256 发生变化、production weights 等于 expected weights、rollback snapshot 已生成、rollback snapshot sha256 等于 apply 前 production profile sha256、`only_allowed_fields_changed=true`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；失败路径缺 danger flag 输出 `apply_decision=DANGER_FLAG_MISSING`、`apply_executed=false`、`promotion_executed=false`、`production_effect=none` 且 production profile sha256 不变；dashboard import guard 验证只读读取 apply result artifact，不触发 018B/018C/018C2/018D/018E1/018E2/scoring/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-018E1|交易执行/Approved Promotion Apply Preflight Only|P0|DONE|系统验证|详细拆解见 `docs/requirements/shadow_promotion_apply_preflight_2026-05-22.md`；只读 apply preflight、standalone script、dashboard 只读卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务|新增 `scripts/run_shadow_promotion_apply_preflight.py`、`src/ai_trading_system/trading_engine/shadow_promotion_apply_preflight.py` 和 `data/derived/weight_iterations/promotion/preflight/shadow_promotion_apply_preflight_YYYY-MM-DD.json/md`、`preflight/logs/shadow_promotion_apply_preflight_run_YYYY-MM-DD.json/md`；读取 018D proposal、人工 approval artifact、target production profile 和 current shadow weights；输出 approval/proposal/weight/target validation、production diff preview、rollback plan preview、safety checklist、pipeline contract 和 audit；所有输出固定 `production_effect=none`、`manual_review_only=true`、`promotion_executed=false`、`apply_executed=false`、`preflight_only=true`、`safe_for_production=false`；dashboard 只读展示 latest preflight artifact，不重跑 018B/018C/018C2/018D/018E1/scoring/broker/replay，也不 apply production；最终收尾 smoke 使用 repo 外临时 fixture 生成 preflight JSON/Markdown/run log，production profile hash 前后一致|2026-05-22: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018D proposal 后新增 approved apply preflight only 阶段；本任务只生成 apply 前检查报告、production diff preview 和 rollback plan，不授权 production modification，不执行 promotion/apply。2026-05-22: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 apply preflight builder、CLI、JSON/Markdown/run log 输出、approval/proposal/weight/target validation、diff preview、rollback plan、dashboard Shadow Promotion Apply Preflight 只读卡片、runbook、系统流图/产物目录和测试；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仅被既有无关 `tests/test_market_data.py` 格式 baseline 阻断，本次改动文件 Black check 通过。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证通过手动 CLI smoke（临时 approval/proposal/shadow/production fixture，输出 `preflight_decision=PASS`，生成 preflight JSON/Markdown/run log、diff preview 和 rollback plan）、安全边界检查（`production_effect=none`、`manual_review_only=true`、`promotion_executed=false`、`apply_executed=false`、`preflight_only=true`、`safe_for_production=false`）、production profile hash 前后一致、dashboard 只读测试确认不导入或重跑 018B/018C/018C2/018D/018E1/scoring/broker/replay；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|
|TRADING-018D|交易执行/Manual Shadow-to-Production Promotion Proposal Gate|P0|DONE|系统验证|详细拆解见 `docs/requirements/shadow_promotion_proposal_2026-05-20.md`；只读 promotion proposal gate、policy、standalone script、dashboard 只读卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成|新增 `scripts/run_shadow_promotion_proposal.py` 和 `data/derived/weight_iterations/promotion/proposals/shadow_promotion_proposal_YYYY-MM-DD.json/md`、`logs/shadow_promotion_proposal_run_YYYY-MM-DD.json/md`；读取 `current_shadow_weights.json`、production profile snapshot、latest TRADING-018C2 review、最近 N 日 018C comparison artifacts 和 018B candidate/history；输出固定 `mode=manual_promotion_proposal_only`、`production_effect=none`、`manual_review_only=true`、`promotion_executed=false`、`safe_for_production=false`；proposal decision 只允许 `PROPOSE_FOR_MANUAL_REVIEW`、`CONTINUE_OBSERVATION`、`INSUFFICIENT_HISTORY`、`INSUFFICIENT_DATA`、`SAFETY_BLOCKED`、`REJECT_SHADOW`、`ERROR`；production/shadow weight keys 必须完全一致且 shadow sum 约等于 1.0，不做 alias mapping；dashboard 只读展示 latest proposal artifact，不重跑 018B/018C/018C2/proposal/scoring/broker/replay|2026-05-20: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018B/018C/018C2 后新增人工 promotion proposal gate；本阶段只生成人工审核 proposal，不允许修改 production profile、不写 production weights、不自动 promotion、不触发 broker/replay/交易。2026-05-20: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 policy manifest、核心 proposal builder、CLI、JSON/Markdown proposal 与 run log、dashboard Shadow Promotion Proposal 只读卡片、runbook、系统流图/产物目录和测试；验证通过目标 pytest、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest、ruff 和 black check。2026-05-20: 从 VALIDATING 改为 DONE，原因：最终收尾验证通过手动 CLI smoke（当前本地无 current shadow / latest review artifacts，按真实可用输入生成 `INSUFFICIENT_DATA` proposal JSON/Markdown）、安全边界检查（`production_effect=none`、`manual_review_only=true`、`promotion_executed=false`、`safe_for_production=false`）、dashboard 只读读取 proposal artifact 且 import guard / artifact mtime / 上游 artifact 列表确认未重跑 018B/018C/018C2/018D；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。|
|TRADING-018C2|交易执行/Multi-day Shadow vs Production Review|P0|DONE|系统验证|详细拆解见 `docs/requirements/shadow_vs_production_multi_day_review_2026-05-19.md`；只读 multi-day review builder、standalone script、dashboard 轻量卡片、系统流图/产物目录/runbook、测试和最终收尾验证已完成|新增 `scripts/run_shadow_vs_production_multi_day_review.py` 和 `data/derived/weight_iterations/comparison/reviews/shadow_vs_production_review_YYYY-MM-DD.json/md`；默认读取最近 7 日既有 `daily_shadow_vs_production_YYYY-MM-DD.json`，不重跑 scoring/comparison pipeline；输出 available/missing/insufficient/safety blocked coverage、average score delta、score better/worse/equal day counts、decision difference count、risk flag delta、dominant changed weight keys 和 promotion readiness；固定 `production_effect=none`、`manual_review_only=true`、promotion readiness false；dashboard 只读展示 latest review artifact，不触发 pipeline、broker、paper runner、replay runner 或 production profile 写入|2026-05-19: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018C 单日 comparison 后新增多日 review evidence 层；本阶段只观察 shadow vs production 多日稳定性，不允许 promotion、production profile 修改或交易触发。2026-05-19: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 multi-day review JSON/Markdown 生成器、standalone script、dashboard 只读卡片、runbook、系统流图/产物目录和测试；验证通过目标 pytest、`tests/test_daily_task_dashboard.py`、`tests/trading_engine`、全量 pytest、ruff 和 black check。2026-05-20: 从 VALIDATING 改为 DONE，原因：最终收尾验证通过手动 CLI smoke（当前本地无 comparison artifacts，按真实可用输入生成 `INSUFFICIENT_HISTORY` review JSON/Markdown）、dashboard 只读读取 smoke、`production_effect=none`、`manual_review_only=true`、promotion readiness false 和 pipeline safety contract 检查；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。|
|TRADING-018C|交易执行/Production vs Shadow Weight Comparison|P0|DONE|系统验证|详细拆解见 `docs/requirements/daily_shadow_vs_production_comparison_2026-05-19.md`；离线 production/shadow 权重对照报告、standalone script、dashboard 只读卡片、系统流图、产物目录、测试和最终收尾验证已完成|新增 `scripts/run_daily_shadow_vs_production_comparison.py` 和 `data/derived/weight_iterations/comparison/daily_shadow_vs_production_YYYY-MM-DD.json/md`；读取 production profile snapshot、`current_shadow_weights.json`、同日 `decision_snapshot` 组件分、TRADING-018B candidate/result 和当日 scoring/backtest/feedback artifacts；同一批输入信号下分别用 production/shadow weights 计算 score，比较 decision、score_delta、risk flags、top contributors 和 contribution breakdown；所有输出固定 `production_effect=none`、`manual_review_only=true`；不得接 broker runner、replay runner、production profile modification、approved profile 写入、promotion 或 dashboard 主结论变更；dashboard 只读展示 score_delta、decision_changed、main reason、report link 和安全边界|2026-05-19: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018B 后新增 production vs shadow 离线比较；当前阶段只是比较，不允许采用 shadow、promotion、production profile 修改、broker/replay runner 或交易触发。2026-05-19: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增 comparison builder、`scripts/run_daily_shadow_vs_production_comparison.py`、JSON/Markdown 输出、dashboard Shadow vs Production Comparison 只读卡片、系统流图/产物目录和测试；验证通过 018C 目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check。2026-05-19: 从 VALIDATING 改为 DONE，原因：最终收尾验证通过手动 CLI smoke（生成 comparison JSON/Markdown）、dashboard 只读读取 smoke（删除源输入后仍读取最新 comparison artifact）、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest、ruff 和 black check；安全边界保持 `production_effect=none`、`manual_review_only=true`、无 broker runner、无 replay runner、无 production profile 写入、无 promotion。|
|LLM-004|新闻/LLM 请求配置|P1|DONE|系统实现|已完成请求 profile 配置、加载器、CLI 覆盖参数、daily-run profile 传递、文档和测试；详细拆解见 `docs/requirements/llm_request_profiles_2026-05-13.md`|不同 LLM 请求入口从可审计 profile 读取模型、reasoning、timeout、HTTP client、缓存 TTL、重试、候选上限和 LLM formal 运行参数；CLI 显式参数可覆盖 profile；`daily-run` 传递 profile id 而不是把候选上限等效硬编码；provider 权限、cache_allowed、`store=false`、本地审计和 `llm_extracted / pending_review` 边界不放松；系统流图、README 和测试同步|2026-05-13: 新增并进入 IN_PROGRESS，原因：owner 要求梳理当前 LLM 请求来源，并希望系统按不同请求使用不同配置。第一版只配置请求策略和候选/LLM formal 运行参数，不开放 prompt/schema 版本。2026-05-13: 从 IN_PROGRESS 改为 DONE，原因：新增 `config/llm_request_profiles.yaml`、profile schema/loader、四个 live LLM 请求入口的 profile 默认值和 CLI 覆盖、`daily-run`/direct dispatcher profile 传递、日报输出 profile id、README/系统流图/数据源说明同步；验证通过 `pytest` 506 passed、`ruff check src tests`、`git diff --check`、CLI help smoke、`data-sources validate` 和 daily-plan smoke。|
|OBS-001|观察池/行业趋势分析语义|P1|DONE|系统实现|已实现 watch-only 核心 ticker 不要求主动交易 thesis，同时保留关注股票趋势和产业链节点分析；详细拆解见 `docs/requirements/watch_only_core_observation_2026-05-10.md`|默认核心观察池可被标记为观察阶段；观察阶段 ticker 不因缺少 `data/external/trade_theses/` 触发 thesis gate；active-trade 高风险 ticker 仍必须要求 thesis；日报继续输出核心 ticker 趋势和产业链节点热度/健康度，且明确 `production_effect=none`|2026-05-10: 新增，原因：owner 明确当前 6 个核心 ticker 均处于观察阶段，希望系统结合行业趋势给出分析，而不是把缺少主动交易 thesis 解释为仓位约束。2026-05-10: 从 IN_PROGRESS 改为 DONE，原因：新增 `decision_stage`、更新核心观察池为 `watch_only`、调整 thesis 缺失校验、同步系统流图和测试；验证通过 `ruff check src tests`、`pytest -q`、`watchlist validate`、`thesis review` 和 `watchlist validate-lifecycle`。|
|OPS-010|运行架构/replay PIT as-of 截止窗口|P0|DONE|系统实现|已实现 replay PIT effective cutoff 和从 filtered raw manifest 重建 replay-scoped FMP forward PIT normalized CSV；不调用外部服务、不复用旧 replay 输出、不改写生产缓存|当 production metadata cutoff 晚于 as-of 时，`replay-day` 的 PIT manifest/normalized 输入不包含晚于 as-of 的 `available_time`；若生产 normalized CSV 被补跑覆盖为未来 `available_time`，replay 从 filtered raw manifest 中按 ticker 选择 as-of 当天最新 raw payload 重建 normalized；2026-05-08 cache-only replay 的 `score_daily`、`ops health`、secret scan 全部 PASS；单测覆盖 cutoff 收紧和 raw manifest 重建|2026-05-10: 新增并完成，原因：真实 replay 验证显示 5/8 production metadata 来自 5/10 补跑，直接使用 visibility cutoff 会纳入未来 PIT；随后发现生产 normalized CSV 已被 5/10 补跑覆盖，需从 5/8 raw payload 重建 replay 输入。验证：`ruff check src/ai_trading_system/historical_replay.py tests/test_historical_replay.py` 通过，`pytest -q tests/test_historical_replay.py` 8 passed，真实 `replay-day --as-of 2026-05-08 --openai-replay-policy cache-only --compare-to-production` PASS。|
|OPS-009|运行架构/replay 风险事件 PIT 冻结|P0|DONE|系统实现|已让 `replay-day` 生成 `risk_event_occurrences` 的 as-of 可见隔离视图，并把 `score-daily --risk-event-occurrences-path` 指向 replay bundle|input freeze manifest 记录风险事件源路径、隔离路径、总数、纳入数、排除数和 checksum；生产 occurrence YAML 不被改写；2026-05-08 cache-only replay 不再因 2026-05-10 LLM formal occurrence/attestation 触发未来日期错误；相关单测、系统流图和需求文档已同步更新|2026-05-10: 新增并完成，原因：真实最近交易日回放验证发现 valuation 已隔离，但 risk event occurrence store 仍读取生产目录，导致历史回放被未来风险事件数据阻断。验证：`score_daily` 在真实 2026-05-08 replay 中 PASS，风险事件报告改为隔离路径且无未来日期错误。|
|OPS-008|运行架构/daily ops runbook|P1|DONE|系统实现|已新增 `docs/runbook_daily_ops.md`，并同步更新 `OPS-003`、系统流图和系统设计 review 需求文档|新协作者可按 runbook 在本地或云 VM 复现 `aits ops daily-run` 运行；runbook 明确哪些失败必须停止、哪些只能降级披露、哪些报告是正式结论、哪些是审计附录；不新增生产 GitHub Actions cron|2026-05-10: 新增并完成，原因：系统设计 review 认为当前 daily ops 命令链路已具备基础能力，但缺少可交接运行手册和调度闭环说明。|
|RUN-001|运行归档/canonical run bundle|P1|BASELINE_DONE|系统实现|`daily-run` 已默认生成 `outputs/runs/YYYY-MM-DD/<safe_run_id>/` canonical bundle，并保留 legacy mirror；回测和 replay bundle 迁移不在本任务范围|`daily-run` 支持 `--run-output-root`、`--run-id` 和 `--legacy-output-mode mirror/off`；run bundle 包含 manifest、reports、traces、metadata；manifest 记录输入/输出路径、checksum、质量状态、legacy mirror、visibility cutoff 和 run id；stdout/stderr 原文、secret 和付费内容不得进入 manifest|2026-05-10: 从 READY 改为 BASELINE_DONE，原因：daily-run canonical run bundle、manifest、legacy mirror、run id 贯穿和测试已完成；剩余是后续逐步扩大 canonical bundle 覆盖面。|
|REPORT-008|报告/Decision Card v2|P2|DONE|系统实现|已在现有“今日结论卡”补充 Data Gate、Run ID / Trace、Main Invalidator 和 Next Checks，并补充文本顺序/CLI 测试|日报顶部结论卡继续先于数据门禁、模块评分和审计明细；新增字段不改变 scoring、position gate、execution policy 或回测仓位；报告能一眼看到数据质量状态、run id、trace 入口、主要反证和下一观察项|2026-05-10: 从 READY 改为 DONE，原因：Decision Card v2 字段和 `score-daily --run-id` trace 引用已实现，正式评分和仓位语义未改变。|
|DOC-002|文档治理/新鲜度检查|P2|DONE|系统实现|已新增 `aits docs validate-freshness`、CI 门禁和单测，并补齐历史关键需求文档的 `最后更新` 元数据|关键 task/requirements 文档必须包含 `最后更新`；文档内部最新状态记录日期不得晚于 `最后更新`；CI 可在无本地数据缓存时执行；检查只验证文档元数据，不推断业务完成度|2026-05-10: 从 READY 改为 DONE，原因：文档新鲜度 CLI、报告渲染、CI 接入、通过/失败单测和当前仓库文档元数据修复已完成。|
|TREND-001|趋势判断/产品边界|P0|DONE|系统实现|已落实“当前系统仅作趋势判断，不触发交易”的结论边界；详细拆解见 `docs/requirements/trend_judgment_scope_2026-05-04.md`|日报和回测即使数据质量全绿，也不能把默认成功态写成交易触发、账户目标仓位或“可作为仓位复核依据”；报告必须明确趋势判断范围、`production_effect=none` 和投资姿态标签边界；实现时同步更新 `docs/system_flow.md` 并补充报告边界测试|2026-05-04: 新增并进入实现，原因：owner 明确当前系统只做趋势判断；该边界影响投资解释，属于 P0 误读风险控制。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `trend_only` 结论等级和 `trend_judgment` 适用范围，`score-daily` 与回测均按趋势判断范围渲染，测试覆盖日报、回测和干净通过时不升级为仓位复核。|
|PROD-001|趋势判断就绪/日报边界|P1|DONE|系统实现 + 项目 owner|总控任务已收口：日报范围已从生产交易依据改为趋势判断和投研复核辅助，阻止趋势判断可靠性的缺口已拆成独立子任务；剩余推进项见 `PROD-002`、`PROD-003`、`PROD-004`、`PROD-005`、`PROD-006`|完成标准为边界固定与任务拆分，而非所有子模块生产成熟：交易执行、券商税费和账户级买卖闭环不作为近期阻塞项；风险复核运行纪律、第二合格数据源、PIT 样本成熟度、规则 owner approval 和 forward shadow 成熟度均有独立任务、owner、阻塞条件和验收标准|2026-05-04: 新增任务，原因：2026-05-04 日报虽成功生成但结论使用等级为 `data_limited`、执行动作为 `observe_only`，需要把生产化不足登记为有序 TODO。2026-05-04: 更新口径，原因：owner 明确当前系统不需要实际触发交易，仅作趋势判断。2026-05-06: 从 IN_PROGRESS 改为 DONE，原因：owner 决策将牵涉多模块的总控任务收口，后续工作拆为可独立验收的子任务并保留在当前任务登记。|
|OPS-004|运行架构/PIT 日常抓取韧性|P1|DONE|系统实现|已修复 `aits pit-snapshots fetch-fmp-forward` 静默失败风险；详细边界见 `docs/requirements/pit_daily_run_resilience_2026-05-06.md`|FMP PIT 抓取命令遇到供应商、权限、写入或校验失败时会输出并尽量写入脱敏中文失败报告；显式 `--continue-on-failure` 可用于每日调度继续执行后续自带质量门禁的步骤；失败或未通过校验的 PIT 快照不得被标记为可用，也不得补写缺跑日期为 strict PIT|2026-05-06: 新增并进入实现，原因：owner 要求修复 PIT 命令静默失败，并避免 PIT 抓取入口层故障阻塞后续日报流程。2026-05-06: 从 IN_PROGRESS 改为 DONE，原因：新增 FMP PIT 失败报告构造、未捕获异常脱敏诊断、`--continue-on-failure` 非阻断开关、daily-plan 接入、README/系统流图说明和回归测试；真实运行 `aits pit-snapshots fetch-fmp-forward --as-of 2026-05-06 --continue-on-failure` 通过并生成 4956 行 normalized、PIT manifest PASS，随后 `aits score-daily --as-of 2026-05-06` 通过为 PASS_WITH_LIMITATIONS；`ruff check src tests` 与全量 `pytest -q` 407 passed。|
|REPORT-001|报告/审计|P0|DONE|系统实现|已按 `docs/requirements/report_traceability.md` 第一版实现日报/回测 evidence bundle、run manifest、dataset/quality refs 和 `aits trace lookup` 反查入口|最终报告的每个核心结论都有稳定 `claim`、`evidence`、`dataset`、`quality` 引用；evidence bundle 记录 signal、配置快照、ticker、日期窗口、`ai_after_chatgpt` regime、数据质量门状态和数据集 provider/row count/checksum；可从报告反查原始或标准化数据快照；实现时同步更新 `docs/system_flow.md` 并补充测试或示例报告|2026-05-04: 从 READY 改为 IN_PROGRESS，原因：创建需求拆解文档并开始实现第一版报告可追溯链路。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：日报和回测已生成 trace bundle 并注入可追溯引用章节，`aits trace lookup` 可反查 claim/evidence/dataset/quality/run id；`python -m pytest -q` 通过 203 项测试。|
|REPORT-002|报告/执行解释|P1|DONE|系统实现|设计并实现日报/最终报告的变化原因树和“什么情况会改变判断”章节；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#report-002`|报告明确展示本期仓位区间变化、分模块分数变化、thesis/风险事件状态、最终动作约束和加/减仓触发条件；能区分市场短期波动、thesis 承压、thesis 证伪、估值过高但基本面未坏、基本面恶化；实现时同步更新 `docs/system_flow.md` 并补充报告快照或文本测试|2026-05-04: 已完成首版实现，`scores_daily.csv` overall 行保存模型/最终/置信度调整仓位区间，日报新增变化原因树、分模块分数变化、最终动作约束和判断改变条件；来自 2026-05-04 报告解释优化讨论；与 `REPORT-001`、`SCORE-002`、`THESIS-001` 协同|
|REPORT-003|报告/呈现结构|P1|BASELINE_DONE|系统实现|已完成第一阶段日报“今日结论卡”和决策链入口：开头输出状态标签、市场吸引力、判断置信度、评分映射仓位、风险闸门后最终仓位、执行动作、主结论、三个核心原因、最大限制和下一步触发条件；剩余缺口是统一所有报告模板、状态标签配置化和核心原因排序策略；详细拆解见 `docs/requirements/report_decision_chain_presentation_2026-05-04.md#report-003`|日报开头输出结论卡，按“状态标签、市场吸引力、判断置信度、评分映射仓位、风险闸门后最终仓位、执行动作、一句话主结论、三个核心原因、一个最大限制、一个下一步触发条件”组织；报告章节顺序从“状态 -> 原因 -> 约束 -> 行动 -> 证据下钻”展开，评分明细后置；每个核心输出先写判断句再给数字、证据和限制；实现时同步更新 `docs/system_flow.md` 并补充日报快照或文本顺序测试|来自 2026-05-04 最终报告呈现讨论。该任务不新增评分信息，而是重排 `REPORT-001/002`、`SCORE-001/002`、`THESIS-001`、`COGNITION-001` 和 `position_gate` 已有输出，避免报告退化为模块结果堆叠。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现日报“今日结论卡”、核心原因/限制/触发条件和文本顺序测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：`score-daily` 日报开头已生成结论卡，执行动作接入 `execution_policy` 输出，测试覆盖结论卡位于变化原因树、数据门禁和模块评分之前；`production scoring`、`position_gate` 和 evidence bundle 语义未改变。|
|REPORT-004|报告/周期复盘|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成周报/月报投资复盘 CLI：读取 `scores_daily.csv`、decision snapshots、belief_state、outcomes、learning queue 和 rule experiments，输出人类投资复核模板，并显式声明 `production_effect=none`；剩余缺口是 catalyst calendar 自动接入、完整产业链节点健康度、真实 outcome/learning/rule experiment 数据积累和更严格的样本充分性校准；详细拆解见 `docs/requirements/report_decision_chain_presentation_2026-05-04.md#report-004`|周报回答本周结论/仓位是否变化、改变判断的前三个证据、产业链节点热度/健康度变化、thesis/risk 变化、系统判断是否被市场验证和下周观察事件；月报回答系统判断表现、高分信号有效性、gate 是否过严或过松、thesis warning 是否有提前量、哪些数据源/模块影响置信度和候选规则是否需要调整；报告声明 `ai_after_chatgpt` regime 和实际日期范围，保留 trace 下钻；实现时同步更新 `docs/system_flow.md` 并补充报告文本测试|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现面向投资复核者的周/月复盘报告入口，区别于 `feedback loop-review` 的系统运营视角。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits reports investment-review`、周/月 Markdown 输出、trace 下钻、outcome/learning/rule experiment 覆盖摘要和文本测试；真实本地运行周报/月报均为 `PASS_WITH_LIMITATIONS`，原因是 decision outcomes、learning queue 和 rule experiments 尚未形成有效样本。|
|SCORE-001|评分/仓位|P0|DONE|系统实现|已实现独立 `position_gate` 层，把评分仓位、风险事件、估值拥挤、thesis 状态、数据置信度和组合限制取最严格上限|`score-daily` 和 `backtest` 使用同一最终仓位约束；日报说明每个 gate 的来源、上限和是否触发；测试覆盖 L2/L3 风险压低仓位但不只依赖总分|2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现共享仓位硬闸门并接入日报、回测、文档和测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：`score-daily` 与 `backtest` 已共用最终仓位闸门；日报和回测报告输出 gate 摘要；`python -m pytest -q` 通过 203 项测试。|
|SCORE-002|评分/报告|P1|DONE|系统实现|已实现 AI 产业链评分与判断置信度拆分；日报、`scores_daily.csv`、decision snapshot、回测每日明细和回测报告均输出 confidence|`score-daily` 日报顶部同时输出 AI 产业链评分、判断置信度、模型评分仓位、置信度调整后建议仓位和限制原因；`scores_daily.csv` 或等价结构化输出可追踪整体置信度和主要扣减原因；回测每日明细和审计报告能按置信度分桶检查低置信度结论；实现时同步更新 `docs/system_flow.md` 并补充评分/报告/回测测试|来自 2026-05-04 评分解释讨论。目标是避免用户把 72/100 之类的市场分数误读为同等可靠的交易结论；低置信度不直接否定机会，但必须限制结论解释和仓位使用。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现评分/置信度拆分、日报/CSV/回测输出和系统不变量测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：已完成组件级和整体置信度、日报/CSV/快照/回测输出、文档和测试；`python -m pytest -q` 通过 220 项测试。|
|SCORE-003|仓位/风险预算|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成可审计风险预算基础版：保留总分生成基础仓位区间，再由市场压力、真实组合单票/节点/相关性簇集中度、ETF beta 覆盖和已有估值/风险/thesis gate 约束最终建议区间；剩余缺口是真实持仓、参数 replay/shadow、流动性/税费/完整相关性预算和 `GOV-001` 批准；详细拆解见 `docs/requirements/risk_budget_model_2026-05-04.md`|形成风险预算设计文档或配置方案，明确哪些指标作为硬上限、哪些作为禁止加仓条件、哪些只进入报告解释；`score-daily` 和 `backtest` 输出模型基础区间、风险预算后建议区间、总资产折算、主要限制原因和不得加仓条件；回测覆盖同分不同风险状态下最终仓位不同；实现时同步更新 `docs/system_flow.md`|来自 2026-05-04 仓位映射优化讨论。该任务应和 `SCORE-001` 的 `position_gate`、`VALUATION-001` 的估值拥挤 gate、`MACRO-001` 的动态风险资产预算协同推进；避免直接引入不可审计黑箱优化器。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：`PORTFOLIO-002` 已完成基础暴露输入，开始实现第一阶段 `risk_budget` position gate、配置 schema、日报/回测输出和测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/portfolio.yaml:risk_budget`、共享 `risk_budget` gate、日报真实持仓集中度约束、回测市场压力约束、执行纪律禁止主动加仓语义、系统流图和测试；完整 `DONE` 仍需真实持仓和长期验证。|
|COGNITION-001|认知模型/belief_state|P1|DONE|系统实现|建立只读 `belief_state` 认知状态层；详细拆解见 `docs/requirements/cognitive_model_2026-05-04.md#cognition-001`|每次 `score-daily` 通过质量门禁后生成机器可读 `belief_state`，结构化表达市场状态、产业链节点状态、估值状态、风险状态、thesis 状态、仓位边界、限制因素和多维置信度；核心判断必须引用数据质量、证据或人工复核来源；日报输出中文认知状态摘要；第一版不得改变正式评分、`position_gate`、回测仓位或交易建议；实现时同步更新 `docs/system_flow.md` 并补充 schema、报告、trace 引用和“belief_state 不直接改仓位”的测试|2026-05-04: 已完成首版实现，`score-daily` 写入 `belief_state` JSON 和历史索引，日报输出认知状态摘要，decision snapshot 写入 `belief_state_ref`，evidence bundle 增加 `belief_state` dataset/claim 引用，并测试只读状态不改变仓位；原因：将系统长期目标从评分器升级为可审计认知模型，需要显式中间状态承接 evidence、thesis、risk、valuation、industry chain、confidence、position boundary 和后续校准；与 `EVIDENCE-001`、`CHAIN-001`、`FEEDBACK-001/002`、`CAUSE-001`、`GOV-001` 协同|
|CHAIN-001|产业链/报告解释|P1|BASELINE_DONE|系统实现|已完成阶段 3 回测历史节点状态追踪：`backtest_daily_*.csv` 写入 top 节点、热度、健康等级和节点覆盖缺口，回测报告输出“产业链节点历史状态摘要”；剩余缺口是阶段 4 仓位约束评估，需先经过真实数据观察、回测和规则治理；详细拆解见 `docs/requirements/industry_chain_node_state.md`|`score-daily` 报告按产业链节点展示热度、健康度、覆盖率、集中度和解释；能区分行情是 GPU/HBM 等局部节点交易，还是完整产业链扩散；健康度只使用已通过门禁的基本面、估值、风险事件和 thesis 输入，数据不足必须显式标记；报告能按“市场价格信号 -> 产业链节点 -> thesis 状态 -> 评分模块 -> 仓位 gate -> 行动影响”解释关键结论；回测或审计能追踪历史节点状态；实现时同步更新 `docs/system_flow.md` 并补充测试|来自 2026-05-04 产业链节点热度/健康度讨论。2026-05-04: 补充最终报告呈现讨论中的产业链因果链模板；第一版定位为解释和诊断层，不直接触发交易；后续可与 `SCORE-001`/`SCORE-003` 协同进入仓位限制。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段节点热度基础版、日报章节和文本/覆盖测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `industry_node_state` 模块，`score-daily` 日报输出产业链节点热度、覆盖率、集中度和主要贡献 ticker；测试覆盖集中/扩散节点和缺失 ticker 覆盖；`production_effect=none` 防止节点热度改变 scoring、`position_gate` 或执行建议。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续实现第二阶段节点健康度基础版，要求保持只读解释层并显式标记基本面、估值、风险和 thesis 覆盖缺口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：日报已接入节点健康度基础版，明确估值拥挤和风险事件不是基本面证伪，并保持 `production_effect=none`；剩余缺口转入历史节点状态追踪和仓位约束评估。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：开始实现阶段 3 回测历史节点状态追踪和报告摘要。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：回测每日明细和报告已可按 `signal_date` 追踪产业链节点热度/健康度，测试覆盖 CSV 字段和报告摘要；`production_effect=none`，未改变评分、仓位闸门或回测仓位。|
|EVIDENCE-001|信息输入/证据账本|P1|DONE|系统实现 + 项目 owner|已实现 `market_evidence` schema、YAML/CSV 导入、校验报告、CLI 和 LLM/public_convenience 隔离|新市场信息进入系统前都有结构化证据记录、来源等级、采集时间、去重键、影响对象和人工复核状态；报告能区分已确认证据、待复核证据和不能进入自动评分的低可信证据；证据可追溯到 risk event、thesis、valuation、catalyst、industry_chain_node 或日报 claim；实现时同步更新 `docs/system_flow.md` 并补充 schema、去重、来源等级和 LLM 隔离测试|该任务补足“新信息进入闭环”的入口；与 `LLM-001` 边界明确：LLM 只能辅助分类和写入待复核证据，不得直接改评分或仓位。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现 market_evidence schema、CSV/YAML 导入、校验、CLI 和 LLM 隔离测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：证据账本、导入、校验、报告、系统流图和 LLM 隔离测试已完成；`python -m pytest -q` 通过 220 项测试。|
|CAUSE-001|决策因果链/审计|P1|DONE|系统实现|已建立 `decision_causal_chain` ledger、构建命令和查询入口，把 evidence、模块变化、gate、decision snapshot、quality、outcome 和预留 rule candidate 串起来；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#cause-001`|每个核心仓位变化或结论降级都能对应至少一个 `chain_id`；因果链能连接 evidence、module score/confidence 变化、gate、decision snapshot、outcome 和后续规则复核；不允许用未来 outcome 改写 signal_date 当时的 causal chain，只能追加后验观察字段；实现时同步更新 `docs/system_flow.md` 并补充因果链和未来函数防护测试|2026-05-04: 开始实现 `decision_causal_chain` ledger、CLI 构建/查询入口和未来 outcome 隔离测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `aits feedback build-causal-chain`、`aits feedback lookup-chain`、`data/processed/decision_causal_chains.json` schema、中文因果链报告、系统流图和测试；后续 `linked_rule_candidate` 由 `LEARNING-001/EXPERIMENT-001` 回填。|
|LEARNING-001|反馈闭环/错误归因|P1|DONE|系统实现 + 项目 owner|已建立结果归因、错误分类与学习队列；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#learning-001`|校准报告和重大偏差样本能生成 review queue，记录 `data_issue`、`rule_issue`、`thesis_issue`、`execution_issue`、`universe_or_proxy_issue`、`exogenous_unforecastable`、`sample_limited` 等归因分类、证据、owner、下一步和是否需要规则候选；成功样本也能归因；样本不足不得强行生成规则修改建议；实现时同步更新 `docs/system_flow.md` 并补充归因分类测试|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：`FEEDBACK-002` 和 `CAUSE-001` 已具备基础产物，开始实现 learning queue、归因分类、CLI 和样本不足防护测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `decision_learning_queue`、`aits feedback build-learning-queue`、`aits feedback lookup-learning`、中文报告、系统流图和测试；样本不足项不生成规则候选，规则候选仍需 `EXPERIMENT-001/GOV-001`。|
|EXPERIMENT-001|规则实验/回放验证|P1|BASELINE_DONE|系统实现|已完成第一阶段 rule experiment ledger：从 learning queue 生成 candidate-only 规则实验、replay/shadow 计划、报告和查询入口，并接入 loop-review；剩余缺口是真实 historical replay runner、前向 shadow 观测记录和 GOV-001 rule card 批准流程|每个候选规则都有触发原因、关联 causal chain、历史 replay、前向 shadow 计划、样本限制说明、风险和回滚条件；报告比较 production vs candidate 在收益、回撤、换手、置信度、触发 gate、结论等级和失败样本上的差异；候选规则未批准前不得影响正式评分、仓位或日报结论；实现时同步更新 `docs/system_flow.md` 并补充 candidate isolation 和 replay 测试|与 `GOV-001` 协同：该任务负责验证候选规则，`GOV-001` 负责规则生命周期、批准和版本治理。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 rule experiment ledger，把 learning queue 中需要规则候选的复核项接入 replay/shadow 计划和生产隔离报告。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits feedback build-rule-experiments`、`aits feedback lookup-rule-experiment`、`data/processed/rule_experiments.json` schema、中文实验台账报告、loop-review 规则候选接入和测试；`production_effect=none` 防止候选规则影响生产评分/仓位/日报；`python -m ruff check src tests`、`python -m pytest -q` 通过。|
|LOOP-001|反馈闭环/周期复核|P1|DONE|系统实现|已建立闭环复核编排与周期报告；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#loop-001`|生成闭环复核报告，覆盖 new evidence、decision snapshots、due outcomes、calibration findings、causal chains、learning queue、rule candidates、blocked owner inputs 和 task register 状态变化；报告声明市场阶段，默认以 `ai_after_chatgpt` 作为主要结论窗口；明确哪些结论可执行、哪些仅研究、哪些因数据或样本不足降级；实现时同步更新 `docs/system_flow.md` 并补充周期报告测试|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：反馈产物已具备，开始实现 `aits feedback loop-review` 周期复核报告，把证据、快照、outcome、因果链、学习队列和 task register 状态串成运营入口。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `feedback_loop_review`、`aits feedback loop-review`、中文周期复核报告、系统流图和测试；rule candidate 暂标 `NOT_CONNECTED` 等待 `EXPERIMENT-001/GOV-001`。|
|FEEDBACK-001|反馈闭环/决策记录|P1|DONE|系统实现|已实现 `decision_snapshot`，`score-daily` 通过数据质量门禁后按 signal_date 覆盖写入确定路径，并保存 trace 关联|每次评分都保存 market regime、实际请求日期、总分、模块分、置信度、模型仓位区间、最终仓位区间、触发 gate、thesis 状态、风险事件、估值状态、主要解释因子、配置版本、数据质量引用和 evidence bundle 引用；重复运行同一 signal_date 具备幂等策略；实现时同步更新 `docs/system_flow.md` 并补充快照测试|来自 2026-05-04 反馈闭环强化讨论；是 `FEEDBACK-002`、规则校准和后续执行复盘的前置条件。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现 score-daily 决策快照写入、trace 关联和幂等测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：score-daily 已写入包含评分、置信度、仓位、gate、质量、估值、风险和 trace 引用的 JSON 快照；`python -m pytest -q` 通过 220 项测试。|
|FEEDBACK-002|反馈闭环/评分校准|P1|DONE|系统实现|基于 `decision_snapshot` 生成 1D/5D/20D/60D/120D 结果观察和评分校准报告；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#feedback-002`|输出 `decision_outcomes` 和校准报告，按总分分桶、置信度分桶、gate 状态、thesis 状态、风险等级和估值状态统计未来收益、回撤、波动、胜率和相对 SPY/QQQ/SMH/SOXX 或 AI proxy 的超额收益；报告声明样本数量、市场阶段、数据质量状态和样本不足限制；实现时同步更新 `docs/system_flow.md` 并补充 outcome 计算测试|2026-05-04: 已完成首版实现，新增 `aits feedback calibrate`，复用数据质量门禁，从本地 decision snapshot 与 prices_daily 生成 outcome CSV 和中文校准报告；支持总分/置信度/gate/thesis/风险/估值分桶，并明确样本不足、窗口重叠和不得自动修改生产规则。|
|CALIBRATION-001|反馈闭环/权重校准 overlay|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段不改变 production 的权重 profile、approved overlay schema、context matching、effective weights 计算和 `aits feedback apply-calibration-overlay`；后续再接入日报 `Historical Calibration`、weight diagnostics、policy replay、promotion 和 shadow 样本成熟度；详细拆解见 `docs/requirements/feedback_weight_calibration_overlay_2026-05-06.md`|基础设施验收：非法权重、缺 approval、过期 overlay、未批准 overlay 和 `approved_soft` hard block 均会被拒绝或过滤；无 overlay 命中时 effective weights 等于 base weights；命中时输出 matched overlays、base/effective weights、confidence_delta、position_multiplier、required confirmations 和审计原因；后续完整验收需要 replay + forward shadow + owner approval 后才允许 production 使用 overlay|2026-05-06: 新增并进入实现，原因：owner 同意先搭建基础系统，再通过每日真实 PIT 样本、回测、forward shadow 和人工审批逐步让 approved overlay 影响后续判断。2026-05-06: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：第一阶段 schema、loader、matching、standalone CLI、系统流图和单元测试已完成；尚未接入 `score-daily` production scoring 或回测仓位。|
|CALIBRATION-002|调权/防过拟合验证协议|P1|BASELINE_DONE|系统设计 + 系统实现|已完成第一阶段 `aits feedback validate-calibration-protocol`：校验 protocol manifest 必填字段、占位符、AI regime 日期、nested walk-forward、purging/embargo、benchmark、参数分层、trial、多重测试折扣和 production boundary；详细拆解见 `docs/requirements/backtest_weight_tuning_validation_protocol_2026-05-06.md#calibration-002调权防过拟合验证协议`|任何权重候选不得只因历史最优收益晋级；必须固定数据、特征、成本、执行、切分和验收指标，记录 experiment_id、git_commit、yaml_hash、data_snapshot_hash、tested_parameters、number_of_trials、chosen_config 和 in/out sample 指标；OOS 聚合、参数扰动、消融、benchmark、成本压力、稳定区域和更新节奏均达标后才允许进入 shadow 或 promotion；完整 DONE 仍需真实调权 trial registry、DSR/PBO 结果、稳定区域和 forward shadow 样本|2026-05-06: 新增任务，原因：外部复核指出回测调权的主要风险是 data snooping、selection bias 和回测过拟合；现有 `BACKTEST-001` 覆盖单次稳健性，但还缺调权试验协议、trial 次数审计和多重测试折扣。2026-05-06: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现不改变 production 的 protocol manifest 校验和中文审计报告基础版。2026-05-06: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增校验模块、CLI、模板、系统流图、README 和测试；命令只输出报告，不批准 overlay 或改变生产评分/仓位。|
|BACKTEST-004|回测/Gate 与事件效果归因|P1|BASELINE_DONE|系统设计 + 系统实现|已完成第一阶段 `aits backtest-gate-attribution`：读取已生成 backtest daily 与 input coverage，输出 gate trigger、平均仓位降幅、avoided drawdown、missed upside、net effect、false alarm、late trigger 和 event label readiness；详细拆解见 `docs/requirements/backtest_weight_tuning_validation_protocol_2026-05-06.md#backtest-004gate-与事件效果归因`|报告输出 gate_trigger_count、average_position_reduction、avoided_drawdown、missed_upside、net_effect、false_alarm_rate、late_trigger_rate、event_precision、severity_accuracy、review_pass_rate、pending_to_confirmed_rate 和 confirmed_high 后 1/5/20 日最大不利波动；LLM 只能使用当时可见文本做结构化抽取，不得做未来收益判断；所有 event/gate 评估按 PIT 输入和可见时间执行；完整 DONE 仍需真实事件标签、severity/outcome 账本和更完整反事实回测|2026-05-06: 新增任务，原因：gate 和 news/LLM 模块的价值不能只用最终收益衡量；需要分别解释避免损失、错失上涨、误报、迟报和人工复核质量。2026-05-06: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现基于已有 backtest daily 与 input coverage 的只读 gate/event attribution 报告基础版。2026-05-06: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 gate/event 归因模块、CLI、系统流图、README 和测试；真实 2026-04-01 至 2026-05-05 回测产物生成报告为 PASS_WITH_LIMITATIONS，限制来自当前无事件标签。|
|SCORE-004|评分架构/Alpha-Risk 解耦|P1|BASELINE_DONE|系统设计 + 系统实现|已完成第一阶段审计输出：日报、decision snapshot 和 trace 显示 base signal、risk state、risk-adjusted score、组件 semantic role、primary_channel、horizon 和 confidence 使用边界；详细拆解见 `docs/requirements/scoring_architecture_refactor_2026-05-06.md#score-004alpha风险状态和约束通道解耦`|为每个评分模块和信号声明 semantic role、horizon、source_type、primary_channel、coverage 和 confidence 使用边界；日报、decision snapshot 和 trace 能同时展示 base signal 与 risk-adjusted context；同一 evidence、risk_id、dedup_group 或 source_id 不得在 alpha 和 gate 中静默重复惩罚；第一阶段只做审计输出，不改变 production 仓位；完整 DONE 仍需 evidence/dedup_group 级双重计分强校验|2026-05-06: 新增任务，原因：外部复核建议指出当前总分仍可能把方向信号和风险状态混在一起；现有 `position_gate` 已解决部分问题，但仍需要显式 Alpha/Risk/Execution 通道和防双重计分审计。2026-05-06: 从 PROPOSED 改为 BASELINE_DONE，原因：新增 `score_architecture` 审计、日报 `Base Signal / Risk Caps` 通道表、decision snapshot 字段和 trace claim；未改变 production scoring、position_gate 或回测仓位。|
|SCORE-005|仓位模型/软硬闸门与非线性映射|P1|BASELINE_DONE|系统设计 + 系统实现|已完成第一阶段 gate taxonomy 审计字段：`PositionGate` 输出 `gate_class`、`target_effect`、`execution_effect`，日报 Risk Caps 表展示每个 gate 的来源、触发状态和上限；详细拆解见 `docs/requirements/scoring_architecture_refactor_2026-05-06.md#score-005软硬闸门非线性仓位映射与执行平滑`|P0 硬约束仍 fail closed；soft gate 只能降低目标、速度或确认要求，不得释放硬风险；非线性映射、turnover limit、cooldown 和 rebalance band 必须先作为 challenger 经回测、forward shadow 和 rule governance 验证；报告能解释最终仓位变化来自基础分、hard cap、soft cap 还是执行平滑；完整 DONE 仍需 soft gate 与非线性映射候选通过 replay/shadow/governance|2026-05-06: 新增任务，原因：现有 `min(cap)` 结构稳健但可能过于保守并造成跳变；需要在不削弱硬闸门的前提下，把软风险和执行纪律从硬上限中拆出来。2026-05-06: 从 PROPOSED 改为 BASELINE_DONE，原因：新增 gate taxonomy 字段和报告审计表；当前仍保持最严格上限生产行为，不启用 soft gate 或非线性仓位映射。|
|RISK-009|风险事件/生命周期与去重|P1|BASELINE_DONE|系统设计 + 系统实现 + 项目 owner|已完成第一阶段风险事件 lifecycle/dedup schema 与过期防护：occurrence 支持 `lifecycle_state`、`dedup_group`、`primary_channel`、`used_in_alpha`、`used_in_gate`、`decay_half_life_days`、`expiry_time`、`resolution_reason`；详细拆解见 `docs/requirements/scoring_architecture_refactor_2026-05-06.md#risk-009风险事件生命周期衰减和去重`|风险事件支持 extracted、pending_review、confirmed_low、confirmed_medium、confirmed_high、confirmed_thesis_break、resolved、expired、rejected 等状态；事件记录包含 dedup_group、primary_channel、used_in_alpha、used_in_gate、decay_half_life、expiry_time、next_review_due 和 resolution_reason；回测按 signal_date 只读取当时未过期、已复核且来源合格的事件状态；完整 DONE 仍需按事件类型配置默认半衰期、自动归并建议和 owner 复核运行纪律|2026-05-06: 新增任务，原因：消息面风险不适合线性相加；现有 LLM 隔离和人工复核边界已经具备，但还缺事件生命周期、衰减、过期和重复新闻归并机制。2026-05-06: 从 PROPOSED 改为 BASELINE_DONE，原因：新增生命周期/去重字段、review report/snapshot 审计、`expiry_time` 过期 active/watch 记录不进入自动评分或仓位闸门；仍未实现自动衰减或自动 dedup。|
|REPORT-007|报告/Base Signal 与 Risk Caps 拆分|P1|BASELINE_DONE|系统实现|已在日报结论卡后固定展示 `Base Signal / Risk Caps`：Base Signal、Raw Position、Risk Caps、Final Position 和 Channel Audit；详细拆解见 `docs/requirements/scoring_architecture_refactor_2026-05-06.md#report-007基础信号与风险调整后结论拆分`|报告能一眼区分基础信号弱、基础信号强但风险受限、数据受限和人工复核要求；每个 cap 都有来源、触发状态、上限和证据引用；复用现有 `REPORT-001/002/003`、`SCORE-001/002/003` 和 `execution_policy` 输出，不新增独立评分逻辑；完整 DONE 仍需后续 soft gate/非线性映射稳定后统一周/月报模板|2026-05-06: 新增任务，原因：当前日报已有模型仓位和最终仓位，但还未固定为 base signal、raw position、risk caps、final position 的结构化阅读块；该改动能减少把“不看好”和“看好但不能重仓”混淆的风险。2026-05-06: 从 PROPOSED 改为 BASELINE_DONE，原因：日报新增固定结构、trace claim 和测试，且不新增独立评分逻辑。|
|UNIVERSE-001|观察池/回测正确性|P0|DONE|系统实现|已建立 point-in-time watchlist lifecycle、校验命令和回测 signal_date 过滤|每个进入评分、回测或报告的 ticker 都有 `added_at`、`removed_at`、`reason`、`active_from`、`active_until`、`competence_status`、`node_mapping_valid_from` 和 thesis 要求可见日期；回测只能使用 signal_date 当时已进入观察池且映射有效的标的；实现时同步更新 `docs/system_flow.md` 并补充防幸存者偏差测试|优先级列为 P0，原因：扩展 universe 后如果没有 point-in-time 生命周期，回测会产生幸存者偏差并误导投资解释。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现 watchlist lifecycle schema、校验、回测按 signal_date 过滤和防幸存者偏差测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：默认 lifecycle、`aits watchlist validate-lifecycle`、回测过滤、系统流图和防幸存者偏差测试已完成；`python -m pytest -q` 通过 220 项测试。|
|DATA-002|数据源/质量|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成低成本初版：基于现有数据源目录和 download manifest 建立 provider health score、cache/manifest 检查和 reconciliation 覆盖报告；趋势判断继续使用现有 FMP + Cboe VIX + Marketstack + FRED，并显式披露 `macro_rates` 单源限制；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#data-002`|初版可验收为 `BASELINE_DONE`：对价格、估值、财报和宏观数据输出 provider 新鲜度、字段漂移、row count 异常、checksum 变化、ticker alias 风险和可用来源差异；跨供应商冲突进入调查项而不是自动平滑；无合格第二来源的域必须标记 reconciliation 未覆盖；严重问题能停止下游或降级结论；完整生产级双源核验仍需要市场价格和宏观利率明确第二 qualified source、授权/成本/限制、缓存审计字段和 checksum，但该方向按 2026-05-10 owner 决策暂缓；实现时同步更新 `docs/system_flow.md` 并补充数据源健康测试|2026-05-04: 从 BLOCKED_OWNER_INPUT 改为 READY，原因：owner 确认先搭低成本框架版，后续再按需订阅 Intrinio、Sharadar、Databento 等生产级来源。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现 `aits data-sources health`、provider health score、cache/manifest 检查和 reconciliation 未覆盖声明。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增数据源健康报告、checksum mismatch 失败、cache/manifest/freshness 检查、qualified source 覆盖表和测试。2026-05-04: owner 已提供并验证低成本初版权限：SEC User-Agent、FMP、FRED、Tiingo EOD 和 EODHD Fundamentals 可访问；EODHD EOD 价格未订阅，价格核验使用 Tiingo。完整 `DONE` 仍依赖生产级第二来源、商业授权/再分发限制和长期口径策略。2026-05-04: 更新趋势判断口径，原因：owner 明确当前系统只做趋势判断，`market_prices` 与 `macro_rates` 的 reconciliation 缺口仍直接影响趋势可信度，继续作为 P1 后续推进。2026-05-10: owner 确认继续使用现有 FMP + Marketstack + FRED，暂无引入第二个更可靠 macro/price qualified source 计划；后续由 `PROD-003` 暂缓登记承接限制披露。|
|DATA-005|数据源/Marketstack 行情第二来源|P1|BASELINE_DONE|系统实现|owner 已订阅 Marketstack Basic 并配置 `MARKETSTACK_API_KEY`；低成本正式接入已完成，详细记录见 `docs/requirements/marketstack_second_price_source_2026-05-05.md`；完整生产级覆盖仍受 Marketstack Basic `^VIX` 缺失和部分 ETF adjusted close 分红复权口径限制|`aits download-data` 写入独立 `data/raw/prices_marketstack_daily.csv` 与 manifest 记录；`aits validate-data` 校验 Marketstack schema/覆盖/新鲜度/重复键/异常值，并比较主缓存与第二来源 raw `close` 价差和 adjusted close 口径差异；`data_sources health` 中 `market_prices` 在 Yahoo + Marketstack 低成本组合下显示 `COVERED_BASELINE`；报告明确 Marketstack 只做第二来源核验，不自动修正主价格；API key 不进入报告、manifest 或错误输出|2026-05-05: 新增并进入实现，原因：owner 已购买 Marketstack Basic，希望正式接入项目；Marketstack Basic 不覆盖 `^VIX` 等所有 Yahoo 宏观代理，实施中必须显式记录覆盖限制，不能伪造第二来源。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：已实现 provider、独立缓存、manifest、质量门禁、数据源目录、系统流图、README 和测试；真实烟测下载成功，`validate-data` 为 `PASS_WITH_WARNINGS`，警告来自 ETF adjusted close 分红复权口径差异，raw close 核验通过。|
|DATA-006|数据源/广义美元指数|P0|DONE|系统实现|已用 FRED `DTWEXBGS` 替换生产路径中的 Yahoo `DX-Y.NYB`；详细记录见 `docs/requirements/broad_usd_index_replacement_2026-05-05.md`|验收完成：`configured_price_tickers` 不再包含 `DX-Y.NYB`；`configured_rate_series` 包含 `DTWEXBGS`；`aits download-data` 已将 `DTWEXBGS` 写入 `rates_daily.csv` 和 manifest；`validate-data` 对 `DTWEXBGS` 使用 series 级数值范围和单日变化阈值，不再因 Yahoo `DX-Y.NYB` OHLC 异常阻断；`score-daily` 宏观流动性评分和 `macro_risk_asset_budget` 使用 `DTWEXBGS.return_20d`；报告和文档明确这是广义美元指数，不是 ICE DXY；已同步更新 `docs/system_flow.md` 和测试|2026-05-05: 新增并进入实现，原因：Yahoo `DX-Y.NYB` 在数据质量门禁中出现 OHLC 异常，ICE DXY 商务接入较慢；owner 批准使用 FRED 官方 `DTWEXBGS` 作为可审计、低成本、daily 的广义美元指数生产输入。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：配置、下载、质量门禁、特征、评分规则、宏观预算、文档和测试已切换到 `DTWEXBGS`；真实 `validate-data` 不再出现 `DX-Y.NYB`，但仍因 SPY/Yahoo OHLC、SOXX 异常和 Yahoo/Marketstack GOOG/SMH 口径差异等价格问题失败。|
|DATA-007|数据源/FMP 主价格源|P0|BASELINE_DONE|系统实现 + 项目 owner|已接入 FMP Starter 作为默认股票/ETF 日线价格主源；详细设计和烟测记录见 `docs/requirements/fmp_primary_price_source_2026-05-05.md`；`^VIX` 已由 DATA-008 Cboe 源补入，价格 alias 和二源归因由 DATA-009 继续收敛，第二源阻断策略由 DATA-011 落地|Baseline 完成：`aits download-data` 默认读取 `FMP_API_KEY` 并使用 FMP 写入股票/ETF 主价格；manifest 主价格 source_id 为 `fmp_eod_daily_prices` 且不泄露 API key；Marketstack 继续作为第二行情源；Yahoo 仅保留显式迁移选项，不再默认生产使用；FMP raw OHLC 使用 `non-split-adjusted` endpoint，`adj_close` 使用 `dividend-adjusted` endpoint；价格路径直接请求 `GOOG`，估值/PIT alias 独立维护；已同步更新 `docs/system_flow.md`、数据源目录、README 和测试。剩余限制：需持续用真实 `download-data`/`validate-data` 确认主价格源清洁度；Marketstack 第二源自检异常按 DATA-011 记录为告警，跨源 raw close 未解决冲突仍会阻断下游结论|2026-05-05: 新增并进入实现，原因：Yahoo 主源质量错误阻断日报，owner 已购买 FMP Starter 作为低成本主价格源；`^VIX` 仍需 Cboe 单独接入，FMP 主源不覆盖全系统宏观代理。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：FMP provider、CLI 默认主源、manifest、数据源目录、系统流图、README 和测试已完成；真实烟测下载成功但质量门禁仍失败，不能继续日报评分。2026-05-05: 更新剩余 blocker，原因：DATA-008 已用 Cboe official historical data 补 `^VIX`；DATA-009 已确认此前部分硬错误主要落在 Marketstack 第二源缓存，后续按来源归因继续验证。2026-05-05: 更新限制，原因：DATA-011 owner 已批准第二源自身异常降级为可审计告警，FMP 主源仍是价格权威输入。|
|DATA-008|数据源/Cboe VIX 官方源|P0|BASELINE_DONE|系统实现|已接入 Cboe VIX official historical data，作为内部 ticker `^VIX` 的专用主源；详细边界见 `docs/requirements/cboe_vix_official_price_source_2026-05-05.md`|Baseline 完成：`aits download-data` 在 FMP 股票/ETF 主价格后，如主源缺少 `^VIX`，从 Cboe CSV 补 `^VIX` 并合并写入 `prices_daily.csv`；manifest 单独记录 `cboe_vix_daily_prices` 的 provider、endpoint、请求参数、row count 和 checksum；Cboe schema/空结果/请求失败 fail closed，不允许用 Yahoo 或本地缓存补写；完整 DONE 仍需真实全量下载后确认 `validate-data` 不再出现 `prices_missing_expected_values: ^VIX`|2026-05-05: 新增并进入实现，原因：DATA-007 已接入 FMP 主价格源，但 FMP 不覆盖 `^VIX`，质量门禁仍因缺少 VIX 阻断；owner 要求提交 FMP 后继续接入 VIX。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 Cboe provider、主缓存合并、manifest 记录、数据源目录、系统流图、README 和测试；Cboe live smoke 返回 2 行 `^VIX`，102 项聚焦测试和 Ruff 通过。|
|DATA-009|数据源/价格源不一致归因|P0|BASELINE_DONE|系统实现 + 项目 owner|已拆分 FMP 价格 alias 与估值/PIT alias，并让数据质量报告明确标注问题来源；详细记录见 `docs/requirements/price_source_reconciliation_2026-05-05.md`；下一步等待真实 `download-data` / `validate-data` 观察|Baseline 完成：`FmpPriceProvider` 默认直接请求 `GOOG`，manifest 不再记录 `GOOG -> GOOGL` 的价格 alias；估值/PIT 路径仍保留独立 alias；数据质量报告问题表包含“来源”列，能区分价格主源、第二行情源 Marketstack、跨源核验、FRED 宏观序列和下载审计清单；Marketstack 自身硬错误不再被误读为主源错误；已同步更新 `docs/system_flow.md` 并补充测试。完整 DONE 需下一次真实下载确认主源和第二源质量状态|2026-05-05: 新增并进入实现，原因：owner 要求继续重点关注股价数据不一致；复核显示 `GOOG` 硬差异来自价格路径错误复用估值 alias，SPY/INTC/MSFT/SOXX 硬异常主要落在 Marketstack 第二源缓存，报告缺少来源列导致误读。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：价格路径已直接请求 `GOOG`，质量报告来源列和归因测试已完成；定向 Ruff、87 项相关 pytest 和 `aits data-sources validate` 通过。|
|DATA-010|数据源/一致性研究窗口|P0|BASELINE_DONE|系统实现|已把价格一致性、宏观变化和跨源 reconciliation 的默认检查窗口绑定到 `ai_after_chatgpt` 研究窗口；详细记录见 `docs/requirements/price_source_reconciliation_2026-05-05.md`|Baseline 完成：默认数据质量门禁的价格波动、复权比例、宏观单日变化和主源/二源 consistency 检查只统计 `2022-12-01` 以来样本；早于研究窗口的复权/跨源/宏观变化差异不阻断或提示默认日报；基础 schema、覆盖、新鲜度、重复键、主源硬错误和研究窗口内 raw close 跨源真实冲突仍 fail closed；第二源自身硬错误的默认处理由 DATA-011 降级为告警；报告声明一致性检查窗口；已同步更新 `docs/system_flow.md`、配置和测试。完整 DONE 需后续按真实日报运行观察确认窗口口径足够稳定|2026-05-05: 新增并进入实现，原因：owner 明确验证数据一致性只考虑研究时间跨度，太久之前的不一致可忽略或降低严重等级；该规则影响质量门禁和日报是否可继续，属于 P0 数据解释口径。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：配置、质量报告、价格/宏观窗口过滤和测试已完成；真实 `validate-data` warning 从 8 降到 7，2020 年 DTWEXBGS 旧窗口 warning 已移出，4 个研究窗口内 Marketstack 错误仍 fail closed；`ruff check .` 和 `pytest -q` 356 项通过。2026-05-05: 更新边界，原因：DATA-011 已将第二源自身异常从阻断项改为可审计告警。|
|DATA-011|数据源/第二源阻断策略|P0|BASELINE_DONE|系统实现|owner 已批准 Marketstack 第二行情源自身异常降级策略；详细记录见 `docs/requirements/price_source_reconciliation_2026-05-05.md`|Baseline 完成：Marketstack 第二源 self-check 的 schema/覆盖/新鲜度/非正价格/OHLC/极端 adjusted close 等异常默认记录为“第二行情源 Marketstack”警告，不覆盖、不平滑、不修正 FMP 主价格缓存，且在 FMP 主源通过时不阻断 `score-daily`；配置 `secondary_source_self_check_fail_closed` 可恢复 fail-closed；第二源文件缺失/不可读、主源错误、raw close 跨源未解决冲突和重叠覆盖不足仍按错误阻断；数据质量报告保留 row count、样例、来源和告警状态|2026-05-05: 新增任务，原因：DATA-010 已排除旧历史一致性噪音，但真实 `validate-data` 仍因 2024/2026 Marketstack 第二源自身错误 fail closed；这不是代码 bug，而是数据源治理策略选择。2026-05-05: 从 BLOCKED_OWNER_INPUT 改为 BASELINE_DONE，原因：owner 明确“记录第二源异常，以主数据源为主，不阻塞后续流程”；实现了配置开关、告警降级、报告归因、文档和测试，主源错误与 raw close 跨源冲突仍保持 fail closed。|
|DATA-012|数据源/Yahoo 诊断复查|P1|BASELINE_DONE|系统实现|已新增 `aits data-sources yahoo-price-diagnostic`：只针对 Marketstack self-check 异常 `ticker/date` 拉取 Yahoo raw OHLC/close，并输出只读诊断报告；详细记录见 `docs/requirements/price_source_reconciliation_2026-05-05.md`|实现一个只读诊断入口或报告：仅针对质量报告中 Marketstack 第二源 self-check 异常的 ticker/date 附近样本拉取 Yahoo raw OHLC/close，记录 provider、endpoint、request parameters、download timestamp、row count、checksum、FMP/Marketstack/Yahoo 对比和 `diagnostic only / production_effect=none`；不得写入 `prices_daily.csv` 或 `prices_marketstack_daily.csv`，不得进入评分、仓位闸门或回测真值；Yahoo 失败、空结果或与 FMP 不一致只进入调查报告，不阻断 `score-daily`，也不降低 FMP 主源权威性|2026-05-05: 新增任务，原因：owner 提出当 Marketstack 第二源明显有问题时，可借助 Yahoo 做简单复查；该来源必须保持 public_convenience 诊断定位，不能重新启用为生产主源或形成三源投票自动改价。2026-05-06: 从 READY 改为 IN_PROGRESS，原因：开始实现只读 Yahoo 诊断复查入口、报告和隔离测试。2026-05-06: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增诊断模块、CLI、Markdown 报告、README、系统流图和测试；命令不写价格缓存、不进入评分/仓位/回测真值。|
|DATA-013|数据源/SEC 日报指标缓存|P0|DONE|系统实现|已通过正式 CLI 生成并校验 `data/processed/sec_fundamentals_2026-05-05.csv`，`score-daily` 已越过 SEC 指标门禁|当日 `sec_fundamentals_YYYY-MM-DD.csv` 必须来自正式 `aits fundamentals extract-sec-metrics` 路径，先通过 SEC companyfacts 缓存校验，再通过 `aits fundamentals validate-sec-metrics`；覆盖缺口必须如实进入校验报告，不允许补空行或伪造指标；`score-daily` 应能越过 SEC 指标校验进入后续门禁，若后续门禁失败则单独报告|2026-05-05: 新增并进入实现，原因：当前 `score-daily` 已通过市场数据质量门禁，但因 `data/processed/sec_fundamentals_2026-05-05.csv` 不存在、覆盖 0/72 而停止。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：`validate-sec-companyfacts` PASS，`extract-sec-metrics` 生成 66 行并 `PASS_WITH_WARNINGS`，`validate-sec-metrics` 覆盖率 92% 且 0 错误，`score-daily --as-of 2026-05-05` 已完成。|
|OPS-002|数据源健康/Yahoo inactive manifest|P1|DONE|系统实现|已把 inactive/diagnostic-only manifest checksum 漂移降为调查警告，active source checksum mismatch 仍 fail closed|Inactive 或 diagnostic-only 数据源的历史 manifest checksum 与当前共享缓存不一致时，不应让 provider health 进入 FAIL；应记录为 inactive/diagnostic stale manifest warning 或忽略硬错误，同时 active source 的 checksum mismatch 仍必须 fail closed；补充测试、更新系统流图或数据源健康文档，并验证 `aits data-sources health` 不再因 inactive Yahoo 旧 manifest 阻断|2026-05-05: 新增任务，原因：FMP/Cboe 已成为主价格缓存来源后，inactive `yahoo_finance_daily_prices` 的历史 manifest 仍指向同一个 `prices_daily.csv` 并触发 checksum mismatch，造成 data-sources health FAIL；这是 health 语义问题，不应恢复 Yahoo 为主源或改写缓存。2026-05-05: 从 READY 改为 DONE，原因：新增 `inactive_manifest_checksum_mismatch` warning，`tests/test_data_sources.py` 覆盖 inactive 降级和 active 硬失败，`aits data-sources health --as-of 2026-05-05` 已转为 PASS_WITH_WARNINGS、0 错误。|
|DATA-003|数据源/PIT 快照归档|P1|DONE|系统实现 + 项目 owner|阶段 1-5 已完成：通用 raw manifest/校验、FMP forward-only PIT 抓取归档、raw payload、normalized as-of CSV、valuation/revision as-of 查询、pipeline health / alerts，以及 SEC accession-level archive 评估和后续任务登记；详细拆解见 `docs/requirements/pit_snapshot_archive_2026-05-04.md#data-003`|已完成验收：能校验 raw snapshot manifest 必填字段、payload sha256、row count、`available_time`/`ingested_at` 约束、重复快照、PIT 可信度误标和 provider 授权字段；FMP 四类 endpoint 写入 raw archive 和 normalized as-of 输出；valuation/revision 查询走 PIT as-of 索引，自建历史不足 90 天时输出降级原因；运行健康报告/告警能暴露 PIT 缺跑、断更、row count 和 checksum 异常；SEC accession-level 增强已拆为 `DATA-004`|2026-05-04: 新增任务，原因：owner 确认不应为了补齐历史 PIT estimates archive 硬买昂贵数据，改为从当前日期开始积累可审计 as-of 数据层；历史回测不得倒用采集日之后才可见的供应商视图。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：owner 确认优先开发 DATA-003，并要求先把具体任务加入 TODO 后开始逐项实现。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：阶段 1 已实现并通过本地 `aits pit-snapshots build-manifest --as-of 2026-05-05`、全量 pytest 和 Ruff 校验；后续阶段继续保留。2026-05-05: 从 BASELINE_DONE 改回 IN_PROGRESS，原因：继续推进阶段 2 的 FMP forward-only PIT 抓取归档。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：阶段 2 已实现 `fetch-fmp-forward` raw/normalized 归档、manifest 刷新、文档和测试；阶段 3-5 继续保留。2026-05-05: 从 BASELINE_DONE 改回 IN_PROGRESS，原因：继续推进阶段 3 的 valuation/revision as-of 查询接入。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：阶段 3 已实现 PIT normalized as-of loader 并接入 `eps_revision_90d_pct` 计算；阶段 4-5 继续保留。2026-05-05: 从 BASELINE_DONE 改回 IN_PROGRESS，原因：继续推进阶段 4 的 PIT health/alerts 接入。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：阶段 4 已将 PIT manifest、PIT 质量报告和当日 FMP PIT normalized CSV 接入 `aits ops health`，并把缺失、row count、新鲜度和 checksum 异常输出为 `pipeline_health_alerts_YYYY-MM-DD.md` data/system 告警；阶段 5 继续保留。2026-05-05: 从 BASELINE_DONE 改回 IN_PROGRESS，原因：继续推进阶段 5 的 SEC accession-level archive 设计评估。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：阶段 5 已完成设计结论并新增 `DATA-004`；DATA-003 的 forward-only PIT 快照归档闭环完成。|
|DATA-004|数据源/SEC accession archive|P1|BASELINE_DONE|系统实现 + 项目 owner|已实现 baseline：下载 SEC submissions JSON、从 SEC 指标 CSV 生成 accession coverage，并归档 accession directory `index.json`；剩余决策是是否进一步下载 primary document、complete submission text 或全部 exhibit；详细拆解见 `docs/requirements/sec_accession_archive_2026-05-05.md`|Baseline 验收已完成：能下载 active CIK 的 submissions JSON 并写 manifest；能从 SEC 指标 CSV 已使用的 accession 生成 accession coverage report；至少保存 accession directory `index.json` 的 raw payload 路径、sha256、bytes 和下载时间；报告明确 `accepted_time`/`filing_date` 可见性规则和剩余限制；完整 DONE 仍取决于 owner 是否要求归档 primary document、complete submission text 或全部 exhibit|2026-05-05: 从 DATA-003 阶段 5 评估拆出，原因：当前 SEC companyfacts baseline 已保留 `accession_number` 和 `filed_date`，但还不能追溯到 accession 原始 filing 目录、accepted time、primary document 和 raw payload checksum；该能力影响 SEC 基本面下载、指标抽取、验证、报告和回测审计，应作为独立 P1 实施。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始实现 baseline submissions 下载、accession coverage 和 accession directory index 归档。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：已实现 `download-sec-submissions`、`download-sec-filing-archive`、`sec-accession-coverage`、数据源目录、README、系统流图和测试；剩余 raw document/exhibit 归档范围待 owner 决策。|
|SCENARIO-001|风险/压力测试|P1|BASELINE_DONE|系统实现|已完成第一阶段 scenario library registry：覆盖首批 AI 产业链压力场景、节点/ticker/risk event/position gate 映射、观察条件、证据要求和人工复核要求；剩余缺口是日报/回测当前组合暴露、量化冲击参数和历史压力样本接入|每个场景能映射到产业链节点、ticker、thesis、风险事件和仓位 gate；报告输出当前组合在云 CapEx 下修、GPU 毛利压缩、HBM 供给、先进封装、出口管制、地缘、利率、估值压缩、电力限制和 AI 应用需求不及预期等场景下的脆弱点、观察条件和人工复核要求；实现时同步更新 `docs/system_flow.md` 并补充场景映射测试|与 `BACKTEST-001` 互补：回测回答历史表现，场景压力测试回答当前组合在指定冲击下的暴露。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 scenario library registry、节点/ticker/risk event/gate 映射校验、报告和查询入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/scenario_library.yaml`、`scenario_library` 模块、`aits scenarios validate`、`aits scenarios lookup`、中文情景库报告、系统流图和测试；情景库只做压力测试和人工复核提示，不作为概率预测，也不直接修改 production 评分、仓位闸门或回测仓位。|
|CATALYST-001|事件/报告|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段手工/审计 catalyst calendar schema、校验、未来 5/20/60 天分桶报告、`upcoming` 报告和查询入口；剩余缺口是真实事件数据源或 owner 手工维护流程、日报摘要接入和事件后复核队列联动|日报显示未来 5/20/60 天重要财报、指引、宏观、CapEx、芯片发布、监管、出口管制和行业会议等催化剂；重大事件前可触发 `pre_event_review`、禁止主动加仓或结论降级；事件后要求复核相关 thesis、risk event 或 valuation state；实现时同步更新 `docs/system_flow.md` 并补充日历和报告测试|自动化数据源需 owner 确认；第一版可从人工复核日历开始，但必须记录来源和采集时间。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段手工/审计 catalyst calendar schema、校验、未来 5/20/60 天分桶报告和查询入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/catalyst_calendar.yaml`、`catalyst_calendar` 模块、`aits catalysts validate`、`aits catalysts upcoming`、`aits catalysts lookup`、中文日历报告、系统流图和测试；默认日历不编造真实事件日期，当前只提供审计结构和空基线。|
|PORTFOLIO-002|组合风险/解释|P1|BASELINE_DONE|系统实现 + 项目 owner|已建立真实持仓文件驱动的 AI 仓位 ticker、产业链节点、地区、客户链、因子、ETF beta、相关性簇和集中度暴露分解；剩余缺口是真实账户持仓文件、FX/流动性/税费扩展，以及后续 `SCORE-003` 风险预算接入；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#portfolio-002`|日报显示当前 AI 仓位不是只显示百分比，还显示单票集中度、节点暴露、地区暴露、客户链暴露、估值/成长因子、ETF beta、相关性簇和主要拥挤风险；缺少真实持仓输入时明确 `NOT_CONNECTED`；第一版只做解释层，不直接改变仓位；实现时同步更新 `docs/system_flow.md` 并补充暴露计算测试|与 `SCORE-003` 有重叠但不重复；该任务先提供解释和输入数据，后续由 `SCORE-003` 决定是否进入风险预算约束。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：生产就绪复盘确认日报仓位还不是账户级建议，开始实现真实持仓 CSV、只读暴露分解、CLI、日报章节和测试；没有真实持仓时不得用观察池替代。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `portfolio_exposure` 模块、`aits portfolio exposure`、持仓 CSV 模板、日报组合暴露章节、数据源目录、系统流图和测试；`production_effect=none`，不会改变评分、`position_gate`、执行建议或回测仓位。|
|EXEC-001|执行纪律/报告|P1|BASELINE_DONE|系统实现|已完成第一阶段 advisory execution policy：固定动作词表、阈值/低置信度/gate 配置、校验报告、查询入口和日报执行建议章节；剩余缺口是冷却期、连续信号确认、成本、事件窗口和回测执行模拟的完整联动；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#exec-001`|日报输出执行建议章节；再平衡阈值、最小调仓幅度、冷却期、禁止追涨、事件前不新增仓位、连续信号确认和高波动期降低交易频率等规则可配置、可回测、可审计；动作词汇固定为维持、小幅加仓、禁止主动加仓、减仓到目标区间、等待人工复核、观察不形成交易结论等 advisory language；小幅变化不触发交易建议；实现时同步更新 `docs/system_flow.md` 并补充执行纪律测试|系统不是自动交易，但需要避免用户把每日小幅仓位区间变化误当成机械交易指令。2026-05-04: 补充最终报告呈现讨论中的动作语言规范，默认不输出“买入/卖出”式交易指令。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 advisory execution policy、固定动作词表、校验、日报执行建议和查询入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/execution_policy.yaml`、`execution_policy` 模块、`aits execution validate`、`aits execution lookup`、中文校验报告、日报执行建议章节、系统流图和测试；`production_effect=none` 防止执行建议改变 production scoring、`position_gate` 或回测仓位。|
|COST-001|回测/执行成本|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成交易摩擦模型基础版：回测成本拆成 commission、spread、slippage、market impact、tax、FX、financing 和 ETF delay 等显式假设，并写入明细、报告和 trace manifest；剩余缺口是真实成交样本、券商费率/税费规则、容量约束、最小交易单位和成交质量验证；详细拆解见 `docs/requirements/transaction_cost_model_2026-05-04.md`|回测支持 bid-ask spread、滑点、市场冲击、汇率、税费、融资利率、ETF 申赎或成交延迟、最小交易单位；报告显示成本敏感性，run manifest 和 evidence bundle 记录成本假设|当前回测已有基础单边成本和线性滑点；该任务有价值但不应挤占 P1 的 point-in-time、反馈闭环和数据源健康任务。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：生产就绪缺口推进到交易摩擦层，开始实现不依赖外部数据的成本假设拆分、报告和 trace 记录。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 backtest 成本假设拆分、每日成本列、中文报告摘要、trace `cost_assumptions` 和测试；完整 `DONE` 仍需真实成交和券商/税务/容量验证。|
|PROXY-001|回测/基准|P1|BASELINE_DONE|系统实现|已完成第一阶段 benchmark policy registry：登记 SPY/QQQ/SMH/SOXX 解释角色，提供 `validate-benchmark-policy` 校验、报告和 `lookup-benchmark-policy` 查询，并把回测/校准报告接入基准政策解释；剩余缺口是可复算的自定义 AI basket 历史收益、成员 lifecycle 与权重快照|报告说明为什么使用 SPY、QQQ、SMH、SOXX 或自定义 AI basket；回测和校准可区分半导体 beta、纳指 beta、AI 主题 beta 和系统动态仓位价值；自定义 basket 必须 point-in-time 维护；实现时同步更新 `docs/system_flow.md` 并补充基准治理测试|避免把 SMH 这类半导体 ETF 表现误解释成完整 AI 产业链判断；与 `UNIVERSE-001` 协同。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 benchmark policy registry、校验报告、查询入口，并把回测/校准报告接入基准政策解释。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/benchmark_policy.yaml`、`benchmark_policy` 模块、`aits feedback validate-benchmark-policy`、`aits feedback lookup-benchmark-policy`、回测/校准报告基准政策章节、系统流图和测试；当前 planned custom AI basket 只做治理要求，不生成正式 basket return；`python -m ruff check src tests` 和相关 pytest 通过。|
|GOV-001|治理/规则管理|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段 rule card registry：登记现有 production 规则 baseline，提供 `validate-rule-cards` 校验、治理报告和 `lookup-rule-card` 查询；剩余缺口是正式 owner 批准流、候选规则 promotion/retirement 操作，以及 `score-daily`、回测和 decision snapshot 写入 rule version|scoring、position gate、risk event、valuation gate、thesis 状态机等核心规则有 rule card；每次规则变更可追溯到规则版本、适用范围、上线原因、验证报告、已知失败模式、最后复核时间、批准记录和回滚方式；`score-daily`、回测和 decision snapshot 记录 rule version；实现时同步更新 `docs/system_flow.md` 并补充规则版本测试|反馈闭环可提出改进，但不能无审计地自动改生产规则。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 rule card registry，先把已存在 production 规则登记为 baseline rule cards，并提供校验、报告和查询入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `config/rule_cards.yaml`、`rule_governance` 模块、`aits feedback validate-rule-cards`、`aits feedback lookup-rule-card`、中文治理报告、系统流图和测试；当前 `baseline_recorded` 只表示已把现有 production 行为纳入审计台账，正式 promotion/retirement 和 rule version 注入仍待后续实现；`python -m ruff check src tests`、`python -m pytest -q` 通过。|
|GOV-002|治理/规则版本|P1|BASELINE_DONE|系统实现|已把当前 production rule card 版本注入 `score-daily`、回测、decision snapshot 和 evidence bundle；剩余缺口是正式 owner approval、promotion、retirement 操作和历史 rule registry replay|`score-daily` 和 `backtest` 先校验 rule card registry，失败时停止；trace run manifest 记录适用于本次命令的 production rule_id/version/type/approval/validation/source paths；decision snapshot 保存同一 rule version manifest；报告/系统流图说明这是本次运行使用的当前规则登记，不是历史时点审批证明；补充规则版本和 trace 测试|2026-05-04: 新增并进入实现，原因：生产就绪缺口 #7 已有 rule card registry，但日报/回测产物尚未写入具体 rule version，审计链仍不完整。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 rule version manifest、日报/回测 rule card 校验、trace run manifest 参数、decision snapshot 字段、系统流图和测试；真实日报/短区间回测已验证 rule_versions 写入。正式 approval/promotion/retirement 仍由 `GOV-001` 后续 owner 流程承接。|
|OPS-001|运维/可靠性|P2|BASELINE_DONE|系统实现|已完成第一阶段只读 pipeline health report：检查关键输入/输出 artifact 存在性、mtime、文件大小和排查入口；剩余缺口是结构化 run log、耗时、异常栈、API 错误和自动化调度状态采集；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#ops-001`|每次运行记录成功/失败、耗时、输入文件版本、质量门禁状态、API 错误、缺失数据和报告生成状态；失败时生成明确排查入口和相关日志引用；运行成功不等于投资结论有效|适合在日常自动化运行稳定后推进；当前优先级低于数据质量和投资解释正确性。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段只读 pipeline health 报告，检查关键输入/输出存在性、mtime、文件大小和排查入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `pipeline_health` 模块、`aits ops health` 和 `outputs/reports/pipeline_health_YYYY-MM-DD.md`；报告明确运行健康不等于投资结论有效。|
|ALERT-001|报告/告警|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成只读 data/system 与 investment/risk 告警基础版：`score-daily` 生成结构化告警记录、单独 Markdown 告警报告，并把告警摘要接入日报；剩余缺口是通知渠道、确认/解除流、持久化去重、调度状态和 owner 告警策略；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#alert-001`|告警有等级、来源、触发条件、解除条件、对应 claim/evidence 引用和去重策略；覆盖数据源断更、估值快照过期、risk event 升级、L2/L3 风险触发、thesis 状态恶化、仓位上限大幅下降和未来重大事件；日报输出告警摘要|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：报告、risk event、thesis 和 catalyst 基础触发条件已稳定，开始实现不接通知渠道的本地只读告警基线。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `alerts` 模块、`outputs/reports/alerts_YYYY-MM-DD.md`、日报“告警摘要”、告警 schema/去重/触发/解除字段和测试；真实 2026-05-04 日报告警状态为 `ACTIVE_WARNINGS`，说明数据质量、政策/地缘输入、估值拥挤和 thesis 复核仍需关注。|
|TEST-001|测试/正确性|P1|DONE|系统实现|已建立跨模块 system invariant test suite；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#test-001`|每条核心投资原则都有测试：`watch` 风险事件不直接重扣仓位、`public_convenience` 不自动评分、LLM 输出不直接影响仓位、`position_gate` 取最严格上限、回测不能使用未来估值快照、SEC 指标 `filed_date <= signal_date`、估值采集日之后才可见、数据质量未通过不能输出完整结论；新增关键规则时同步补测试或说明不适用原因|保护系统设计边界，防止后续改动破坏 source policy、point-in-time、gate 优先级、LLM 隔离和 watch/active 区分。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始补充覆盖 source policy、PIT、gate、LLM 隔离和 watch/active 边界的系统不变量测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 watch/active、public_convenience、LLM evidence、watchlist PIT、decision snapshot、confidence、valuation PIT 和 thesis 状态机测试；`python -m pytest -q` 通过 220 项测试。|
|UI-001|仪表盘/解释|P2|BASELINE_DONE|系统实现|已完成第一阶段本地静态 evidence-first dashboard：`aits reports dashboard` 读取日报、trace bundle、decision snapshot 和可选 belief_state，生成简约 HTML，把报告结论、论证链、实际输入数据、quality refs 和 trace lookup 连起来；剩余缺口是更完整交互服务、跨日报对比和可配置 reader mode；详细拆解见 `docs/requirements/ui_evidence_dashboard_2026-05-04.md`|第一阶段从日报 Markdown、evidence bundle、decision snapshot 和可选 belief_state 生成只读 HTML；可按快速读者、投资复核者、系统审计者查看结论卡、动作/限制、论证链、gate、thesis/risk/valuation 状态、数据源、原始证据和质量报告；输出声明 `production_effect=none` 且不替代 Markdown 报告的审计责任|2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：owner 明确主要使用者是自己，优先关注报告结论与实际输入数据的联系以及论证逻辑，视觉风格保持简约；第一阶段先做本地静态证据下钻页，不引入交互式服务或新投资计算。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits reports dashboard`、`evidence_dashboard` 模块、HTML 输出、系统流图和测试；页面只读展示结论到 evidence/dataset/quality 的链路，未改变评分、仓位、回测或执行建议。|
|SECURITY-001|安全/密钥|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段 secret hygiene 扫描：本地 CLI 扫描配置、文档、报告、manifest 和 trace bundle 中的疑似 secret，并输出脱敏报告；剩余缺口是供应商权限治理、日志脱敏策略、pre-commit/CI 集成和 owner 审批流程；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#security-001`|API key 不进入报告、日志、manifest、trace bundle 或错误输出；支持 secret scan 或等价检查；供应商权限、缓存限制、再分发限制和 LLM 处理限制记录在 data source catalog；新数据源接入必须通过安全与权限检查|当前已有 FMP key 从环境变量读取且不写入报告的基础约束；扩展供应商后需要系统化治理。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现第一阶段 secret hygiene 扫描报告和 CLI，检查配置、文档、报告、manifest、trace bundle 中的疑似 secret。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `secret_hygiene` 模块、`aits security scan-secrets` 和 `outputs/reports/secret_hygiene_YYYY-MM-DD.md`；报告只输出脱敏片段，不输出完整疑似密钥。|
|DOC-001|产品边界/解释|P2|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段 recommendation status taxonomy：日报和回测报告输出结论使用等级、适用范围、降级原因、解除条件、证据引用，并与日报投资姿态标签分开；剩余缺口是 taxonomy 配置化、未来事件窗口和更完整 owner review policy；详细拆解见 `docs/requirements/feedback_loop_governance_2026-05-04.md#doc-001`|每份日报和回测报告都有结论使用等级：`trend_only`、`actionable`、`review_required`、`research_only`、`data_limited`、`backtest_limited` 或等价分类；当前 `score-daily` 和回测以 `trend_judgment` 范围运行，干净通过时也只能显示趋势判断、不触发交易；同时引入独立投资姿态标签，如积极进攻、中高配、中高配但受限、中性观察、防守降仓、人工复核；低置信度、数据不足、来源不足、回测覆盖不足和事件前状态自动降级，并说明原因、解除条件和证据引用；实现时同步更新 `docs/system_flow.md` 并补充报告测试|与 `SCORE-002` 置信度拆分、`REPORT-002` 解释输出和 `REPORT-003` 决策链呈现协同；减少用户把市场状态、分数、仓位和结论可靠性混为同一个答案的风险。2026-05-04: 从 PROPOSED 改为 IN_PROGRESS，原因：开始实现日报和回测报告的结论使用等级、投资姿态标签、降级原因和解除条件。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `conclusion_boundary` taxonomy，日报与回测报告均输出“结论使用等级”，覆盖 `review_required`、`data_limited` 和 `backtest_limited` 降级；报告明确结论等级与投资姿态标签不能互相替代。2026-05-04: 补充 `trend_only` 和 `trend_judgment` 适用范围，原因：owner 明确当前系统只做趋势判断，不需要实际触发交易。|
|RISK-001|风险事件|P1|DONE|系统实现|已扩展 `risk_event_occurrence` schema，增加 `evidence_grade`、`severity`、`probability`、`scope`、`time_sensitivity`、`reversibility`、`action_class`|校验报告显示来源可信度和风险本体分层；低可信高严重事件进入人工复核但不自动大幅扣分；高可信高严重事件可触发 position gate|保持向后兼容或提供迁移模板。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始扩展 occurrence schema、导入模板、校验报告和评分资格规则。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：schema、CSV 导入、模板、校验/复核报告、评分资格规则和测试均已完成；`python -m pytest -q` 通过 220 项测试。|
|RISK-002|风险事件|P1|DONE|系统实现|已区分 `watch` 与 `active` 对评分和仓位的影响|`watch` 默认进入报告和人工复核；只有达到证据等级或确认条件后才进入自动扣分或仓位闸门；回测 point-in-time 切片保持一致|当前 `active_or_watch_l2_count` 和 `active_or_watch_l3_count` 会把两类状态合并评分。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始把 watch 发生记录改为默认不可自动评分，只进入报告和人工复核。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：policy/geopolitics 评分和 risk_events gate 只使用可评分 active occurrence，watch 只进入报告/人工复核；`python -m pytest -q` 通过 220 项测试。|
|SOURCE-001|数据源/风险|P1|DONE|系统实现|已按 owner 批准的保守 source policy 落地；详细拆解见 `docs/requirements/source_policy_2026-05-04.md`|形成 source policy：S/A/B/C/D/X 等级；`S/A` 可自动评分和触发仓位闸门，`B` 仅在确认后进入普通评分，`C/D/X` 只能 report/manual review；`public_convenience` 与 `llm_extracted` 不得直接评分；更新数据源目录、导入校验、风险事件评分资格和系统流图|2026-05-04: 从 BLOCKED_OWNER_INPUT 改为 IN_PROGRESS，原因：owner 批准先按保守方案落地 source policy。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：新增 `source_policy` helper，market evidence 和 risk event 校验报告已区分普通评分/仓位闸门资格，风险事件 position gate 只读取 `S/A` active 且 `position_gate_eligible` 的记录，测试覆盖 B 级 score-only、C/D/X 报告复核、public_convenience/LLM 隔离和 CSV 缺省 action class 保守行为；`python -m pytest -q` 通过 239 项测试，`python -m ruff check .` 通过。|
|RISK-003|风险事件数据源|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成正式流程基础版：`SOURCE-001` 固化来源分层，`RISK-004` 提供 OpenAI 结构化预审导入、live API 整理和待人工复核队列，`RISK-005` 提供每日复核声明链路，正式 occurrence 校验要求 reviewer/reviewed_at/review_decision/rationale/next_review_due；剩余缺口是真实授权来源样本、provider 级外部 LLM 授权记录、风险事件生产样本验证和 owner 每日复核运行纪律；详细流程见 `docs/requirements/risk_event_review_workflow_2026-05-04.md`|风险事件发生记录能从更可信来源或结构化人工流程进入；报告记录 provider、endpoint/来源、captured_at、row count/checksum 或人工复核人；复核流程明确 owner、频率、SLA、升级标准、双确认条件和无法确认时的保守降级规则|2026-05-04: 从 BLOCKED_OWNER_INPUT 改为 READY，原因：owner 确认采用“官方/一手来源 + OpenAI API 预审 + 结构化人工复核”的保守流程；实际实施拆解由 `RISK-004` 承接。2026-05-04: 从 READY 改为 BASELINE_DONE，原因：`SOURCE-001/RISK-004/RISK-005` 已完成来源分层、预审隔离、人工确认元数据、复核声明、日报识别、回测 PIT 切片和系统流图；完整生产使用仍依赖真实授权来源样本、API key、provider 级外部 LLM 授权记录和 owner 运行纪律。2026-05-04: 更新剩余缺口，原因：`RISK-004` 已补风险事件专用 live OpenAI 预审入口，生产级完成仍需真实授权样本和 owner 运行纪律。|
|RISK-004|风险事件/OpenAI 预审|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第二阶段 live OpenAI Responses API 风险事件预审入口：`aits risk-events precheck-openai` 复用 provider LLM 权限 fail closed、固定结构化输出和审计字段，并把风险事件候选直接写入 `risk_event_prereview_queue.json`；真实授权来源生产样本验证仍依赖 owner 批准样本|API 输出只能转换成 `llm_extracted` / `pending_review` 风险事件候选，保存 model、reasoning effort、prompt version、source URL、request id/response id、输入/输出 checksum、source permission、候选 risk_id、ticker/节点映射和人工复核问题；不得直接评分、触发仓位闸门或自动写成 L2/L3 active；人工确认后的正式 occurrence 仍必须包含 reviewer、reviewed_at、review_decision、rationale、next_review_due；同步更新 `docs/system_flow.md`、示例输入和隔离测试|来自 2026-05-04 RISK-003 流程讨论；付费新闻或供应商内容只有在授权允许外部 LLM API 处理时才可作为 OpenAI 输入。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现第一阶段结构化预审导入、待人工复核队列、报告和隔离测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits risk-events import-prereview-csv`、`data/processed/risk_event_prereview_queue.json` schema、中文导入报告、预审模板、正式 occurrence 人工复核元数据校验和测试；`python -m ruff check src tests`、`python -m pytest -q` 通过。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：owner 批准顺着 OpenAI API 整理风险事件思路继续推进，开始补风险事件专用 live API 预审入口。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits risk-events precheck-openai`、live API 到风险事件待复核队列转换、source permission 审计、中文报告、示例输入、系统流图和隔离测试；`.venv\\Scripts\\python.exe -m pytest -q` 通过 311 项测试，ruff 定向检查通过。|
|RISK-005|风险事件/每日复核声明|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第一阶段“已复核且未发现未记录重大事件”的结构化声明链路：支持 `review_attestation` YAML、`aits risk-events record-review-attestation`、校验报告、日报政策/地缘评分识别和 point-in-time 回测切片；剩余缺口是真实每日复核 owner、来源清单和运行纪律；详细流程见 `docs/requirements/risk_event_review_workflow_2026-05-04.md#risk-005`|支持人工复核声明 YAML 和 CLI 写入；校验报告显示复核声明数量、当前有效声明数量、覆盖窗口、复核人、来源范围和过期状态；只有覆盖 `as_of`、复核结论明确、未过期且来源范围合格的声明，才允许政策/地缘模块把无可评分 active 风险事件作为经复核输入，而不是 `insufficient_data`；日报和系统流图说明该声明不是自动风险消除证明；补充测试覆盖有效声明、过期声明、日报评分和 gate 隔离|2026-05-04: 新增并进入实现，原因：2026-05-04 日报显示风险事件发生记录目录缺失导致政策/地缘模块降级；该项可独立补工程链路，但真实复核内容仍需 owner 填写，不由系统伪造。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增复核声明 schema/loader/writer、CLI 写入、发生记录校验报告展示、日报政策/地缘模块接入、历史切片、数据源目录、系统流图和测试；系统没有自动生成任何真实无风险声明。|
|LLM-001|新闻/LLM|P1|BASELINE_DONE|系统实现 + 项目 owner|已把 owner 选择的 OpenAI 默认请求策略落地为 `gpt-5.5` + `reasoning.effort=high`，并把 reasoning effort 写入请求、报告和待复核队列；剩余依赖仍是真实 owner 批准样本、更多 provider 授权条款覆盖、人工复核 SLA 运行纪律和批量处理评估；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#llm-001`|LLM 输出必须是结构化证据分类结果，并保留原始来源、采集时间、输入/输出 checksum、model、reasoning effort、prompt version、request id 和 provider 授权标记；报告区分 LLM 辅助抽取、人工确认和自动评分输入；没有人工确认或明确证据等级前，LLM 输出不得直接改变仓位建议；付费新闻/供应商内容只有在 provider 级授权允许外部 LLM 处理时才可发送，授权未知时 fail closed；实现时同步更新 `docs/system_flow.md` 并补充防止 LLM 交易建议直连评分和供应商授权绕过的测试|来自 2026-05-04 新闻/LLM 接入边界讨论；明确禁止 LLM 直接输出看多/看空/加仓/减仓作为交易评分。2026-05-04: 从 PROPOSED 改为 READY，原因：owner 确认使用边界、付费/供应商内容处理原则和预审 schema 方案，剩余工作转为实现 provider 授权字段、结构化输出 schema、OpenAI 请求审计和隔离测试。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：owner 已设置 API key，并批准剩余阻塞项按保守默认处理：真实样本先用官方/一手来源和手工摘要，provider 授权未知时 fail closed，LLM 输出只进入待复核队列。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `llm_precheck` 模块、`aits llm precheck-claims`、`data/processed/llm_claim_prereview_queue.json` schema、中文预审报告、provider `llm_permission` 校验、输入模板、系统流图和隔离测试；`python -m pytest -q` 通过 301 项测试，`python -m ruff check .` 通过；尚未用真实 owner 批准样本发起生产预审。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：owner 指定 OpenAI 默认策略升级为 `gpt-5.5-pro` 和 `reasoning.effort=high`，开始补请求 payload、CLI 选项、审计输出、系统流图和测试。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：`aits llm precheck-claims` 和 `aits risk-events precheck-openai` 默认请求均使用 `gpt-5.5` + `reasoning.effort=high`，队列 schema、报告、数据源目录、示例、系统流图和测试均记录 reasoning effort；`.venv\Scripts\python.exe -m pytest -q` 通过 311 项测试，`ruff check config src tests` 通过。2026-05-05: owner 决策后续默认从 `gpt-5.5-pro` 切换为 `gpt-5.5`，原因：日报前单候选真实 smoke 中 `gpt-5.5-pro` 返回 HTTP 502，而 `gpt-5.5` 可稳定返回；代码常量、文档和示例已同步；不带 `--openai-model` 的单候选日报前预审 smoke 约 96 秒完成，预审 PASS_WITH_WARNINGS，待复核队列 0，日报 PASS_WITH_LIMITATIONS。|
|LLM-002|OpenAI 请求发送链路诊断|P1|BASELINE_DONE|系统实现|已完成生产默认链路修复：OpenAI Responses API 默认 HTTP client 改为 `requests`，保留 `--openai-http-client urllib` 做本机对照；失败/重试报告输出 sanitized diagnostics；所有 5xx（含 HTTP 520）按瞬时错误重试 2 次；剩余增强是独立 dry-run/debug 命令和 SDK/httpx 后续评估；详细拆解见 `docs/requirements/openai_transport_diagnostics_2026-05-05.md`|失败报告或 debug 输出应记录 sanitized attempt 诊断：attempt count、HTTP client、每次 attempt 的 client request id、candidate/precheck id、endpoint host、payload byte size、input checksum、exception type、errno/reason、HTTP status 和 OpenAI x-request-id（如有）；不得输出 API key、Authorization header 或未授权付费全文；单次请求失败仍按 RISK-007 重试 2 次，第 3 次失败整批 fail closed；默认链路必须能用真实 `score-daily` 跑通日报前 OpenAI 预审|2026-05-05: 新增任务，原因：默认 20 条日报前 OpenAI 预审样本出现间歇性 `URLError`；同一失败候选用相同 payload builder 单独重跑约 20 秒成功，说明候选内容和模型判断不是主要 blocker，需先补请求发送链路可观测性和客户端稳定性验证。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：已在 OpenAI 重试层记录每次 attempt 的 sanitized diagnostics，并在 LLM claim/risk event Markdown 报告输出“请求诊断”；验证 `ruff check src tests` 和目标测试 22 passed。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：默认 OpenAI HTTP client 切换为 `requests` 并补 CLI 参数、文档、数据源目录和测试；真实 USTR 单候选 dry-run 约 20 秒 PASS、record_count 0；完整默认 `score-daily --as-of 2026-05-05` 在补齐 HTTP 520 作为 5xx 重试后通过，OpenAI 预审 PASS_WITH_WARNINGS、官方候选 402、上限 20、LLM claim 24、待复核队列 7、L2/L3 候选 6、active 候选 0，日报 PASS_WITH_LIMITATIONS；验证 `ruff check src tests`、目标测试 45 passed、完整 `pytest -q` 369 passed。|
|VALUATION-001|估值/仓位|P1|DONE|系统实现|把估值拥挤从单纯评分升级为新增仓位限制或禁止加仓 gate|估值过热时日报输出“趋势仍强但新增仓位受限”等结论；回测使用同一规则；不把估值拥挤误写成基本面证伪|2026-05-04: 已完成，valuation `position_gate` 在共享 gate 层按 `EXPENSIVE_OR_CROWDED` / `EXTREME_OVERHEATED` 限制最终 AI 仓位，日报和回测共用同一最终仓位约束；补充测试验证估值拥挤 gate 压低仓位且不被误写成 thesis 证伪。|
|VALUATION-002|估值/数据可信度|P1|DONE|系统实现|已把估值输入显式分为真实 point-in-time、采集日快照和回填历史分布，并在估值复核、日报和回测审计中输出可信度等级和限制原因；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#valuation-002`|日报、估值复核和回测审计显示估值分位可信度：高/中/低及原因；严格回测不能使用采集日之后才可见的估值回填数据得出历史时点结论；`eps_revision_90d_pct` 只允许真实可审计历史 analyst 快照支持；实现时同步更新 `docs/system_flow.md` 并补充估值校验和回测切片测试|来自 2026-05-04 估值历史快照伪 point-in-time 风险讨论；与 `BTINPUT-001`、`DATA-001` 协同。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现估值 PIT 可信度字段、FMP historical 低可信标签、报告输出和切片测试。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：valuation snapshot、CSV/FMP 来源、校验、复核、日报、decision snapshot、回测输出和测试均已覆盖 PIT 可信度；`python -m pytest -q` 通过 220 项测试。|
|BTINPUT-001|回测输入覆盖|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成第二阶段 forward-only PIT 覆盖持续验证：新增 `aits backtest-pit-coverage`，按 PIT manifest 汇总自建快照覆盖、历史 C 级原因和可升级为 B/A 级样本的起始日期；历史补数继续暂缓；详细拆解见 `docs/requirements/pit_snapshot_archive_2026-05-04.md#btinput-001`|在提供合格历史估值/风险事件输入后，`backtest_audit_2022-12-01_2026-05-01.md` 中 `valuation` 和 `policy_geopolitics` 覆盖率不再长期为 0%；审计报告能区分“历史确无事件”和“事件库未建设”；估值历史不能用采集日伪造过去可见性；新增或更新导入模板、校验报告和测试|来自 2026-05-04 TSM IR 回填后的下一步建议；当前回测仍显示估值快照和风险事件发生记录全区间为空，限制投资解释力。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现第一阶段历史输入缺口报告，先把可独立完成的补数诊断和模板入口落地。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `backtest.input_gaps` 模块、`aits backtest-input-gaps`、中文缺口报告、系统流图和测试；真实短区间诊断显示 2026-04-01 至 2026-05-01 的估值和风险事件/复核声明缺口均为 21 个 signal_date。2026-05-04: owner 决策不购买或伪造历史 PIT；本任务保留缺口诊断能力，但不继续推进历史 PIT 补数、严格 PIT 估值或历史 occurrence/attestation 补齐；未来自建快照样本由 `DATA-003` 自然积累，历史回测可信度由 `BACKTEST-002` 标注。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：owner 接受不采购低成本 PIT 的策略，开始补 forward-only 覆盖持续验证和升级日期报告。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `backtest.pit_coverage`、`aits backtest-pit-coverage`、中文覆盖报告、系统流图和测试；`ruff check src tests` 和 `.venv\Scripts\python.exe -m pytest -q` 通过 372 项测试。|
|BACKTEST-001|回测/稳健性|P1|DONE|系统实现|已完成回测稳健性报告：覆盖成本压力、起点后移、固定仓位、再平衡频率、趋势信号族基线、模块权重扰动、同换手率随机策略和时间顺序样本外验证；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#backtest-001`|报告声明市场阶段、实际请求日期范围和数据质量状态；所有实验使用相同 point-in-time 输入和交易成本假设，且不引入未来函数；Markdown 和机器可读摘要都能说明动态系统相对固定仓位/买入持有基准的收益、回撤、换手和纪律差异；实现时同步更新 `docs/system_flow.md` 并补充 baseline 和摘要测试|来自 2026-05-04 回测防过拟合讨论；优先默认使用 `ai_after_chatgpt` 窗口。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：`SCORE-003` 和 `COST-001` 已具备基础输入，开始实现第一阶段 backtest robustness report。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `backtest.robustness` 模块、`--robustness-report`/`--robustness-report-path`、中文稳健性报告、系统流图和 CLI 测试；完整 `DONE` 仍需完整扰动/随机/样本外实验。2026-05-04: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：开始补第二阶段固定 60% 基线和机器可读稳健性摘要。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增固定 60% 总资产 AI exposure 基线、`--robustness-summary-path` JSON 摘要、系统流图和测试；真实短区间回测已验证摘要生成，固定仓位在短样本中优于动态策略，结论仍需更多稳健性实验。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续按回测开发优先顺序补充再平衡频率稳健性场景，复用同一 PIT 输入、收益生效规则和显式成本假设。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `rebalance_every_5d` 和 `rebalance_every_21d` 稳健性场景，JSON 剩余缺口移除 `rebalance_frequency`；`ruff check src tests` 和 `.venv\Scripts\python.exe -m pytest -q` 通过 371 项测试。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续补趋势-only 与趋势+风险情绪基线，评估复杂基本面/估值模块相对简化信号族的增量。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `trend_only_baseline` 和 `trend_plus_risk_sentiment_baseline` 稳健性场景，JSON 剩余缺口移除趋势基线；`ruff check src tests` 和 `tests/test_backtest.py` 通过。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续补模块权重扰动，使用扰动后的评分规则重新运行同一 PIT 输入，避免近似复用 gate 上限。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增每个评分模块上下扰动场景、`--robustness-weight-perturbation-pct`、权重敏感性解释和 JSON 字段，剩余缺口移除 `module_weight_perturbation`；`ruff check src tests` 和 `tests/test_backtest.py` 通过。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续补同换手率随机策略，判断信号方向是否优于相同交易频率下的随机方向。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增同换手率随机策略、`--robustness-random-seed-start`、`--robustness-random-seed-count`、随机基线解释和 JSON 字段，剩余缺口移除 `same_turnover_random_strategy`；`ruff check src tests` 和 `tests/test_backtest.py` 通过。2026-05-05: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：继续补时间顺序样本外验证，避免只看全样本和单一起点后移。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：新增 `in_sample_window` 和 `out_of_sample_holdout` 场景、`--robustness-oos-split-ratio`、样本外解释和 JSON 字段，`remaining_gaps` 为空；`ruff check src tests` 和 `.venv\Scripts\python.exe -m pytest -q` 通过 371 项测试。|
|BACKTEST-002|回测/数据可信度|P1|DONE|系统实现|已完成回测 A/B/C 数据可信度、vendor historical estimates / self-archived snapshot 声明、核心输入 PIT 覆盖摘要、输入覆盖 CSV PIT 字段和 feature/universe lag sensitivity Markdown + JSON；详细拆解见 `docs/requirements/pit_snapshot_archive_2026-05-04.md#backtest-002`|回测报告显示 `Backtest Data Quality`、`Uses Vendor Historical Estimates`、`Uses Self-Archived Snapshots`、`Minimum Feature Lag`、`Universe PIT`、`Corporate Actions Handling` 和核心输入 `point_in_time_class/backtest_use` 覆盖；支持 0/1/3/5/10/20 交易日 feature-only、universe-only、combined 滞后敏感性；C 级探索性输入明确不得输出无条件 Sharpe 结论；已同步更新 `docs/system_flow.md` 并补充报告、CLI 和审计测试|2026-05-04: 新增任务，原因：自建 PIT 只能改善未来样本，历史回测仍需要显式标注可信度和滞后敏感性，防止把供应商回填历史误读为可交易结论。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：owner 要求按回测相关任务优先顺序继续开发，先实现 A/B/C 数据可信度、核心 PIT 覆盖摘要和 lag sensitivity 报告。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：`aits backtest` 已可输出数据可信度标签、核心 PIT 覆盖和滞后敏感性报告/摘要，C 级报告明确降级 Sharpe 解释；`ruff check src tests` 和 `.venv\Scripts\python.exe -m pytest -q` 通过 371 项测试。|
|PIT-001|特征/PIT 可见时间|P0|BASELINE_DONE|系统实现|已建立 `config/feature_availability.yaml` 和 PIT feature availability 报告；`build-features`、`score-daily`、`backtest` 写入可见时间报告，并把摘要纳入 report trace bundle；剩余缺口是把单个新特征族的真实 `available_time` 字段校验继续下沉到各数据源 schema；详细拆解见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#pit-001全系统-feature-availability-policy`|所有用于评分、回测、校准和日报核心结论的特征族都有明确 availability rule；缺少 `available_time` 或只有 `event_time` 的新特征默认不得进入 A/B 级回测主结论；回测报告和 trace bundle 能显示特征可见时间规则、实际 `feature_as_of` 和数据可信度等级；实现时同步更新 `docs/system_flow.md` 并补充未来快照、未来公告、未来 universe 和未来宏观修订防护测试|2026-05-05: 新增任务，原因：PIT 讨论确认下一阶段应把 `available_time <= decision_time` 从估值/风险/SEC/观察池扩展为全系统特征规则，避免新增特征族重新引入未来函数。2026-05-05: 从 READY 改为 BASELINE_DONE，原因：新增 feature availability catalog、校验/报告模块、CLI 接入、trace 引用和测试；完整 DONE 仍需各数据源级别的可见时间字段强校验。|
|SHADOW-001|模型评估/在线 PIT prediction ledger|P1|BASELINE_DONE|系统实现|已新增 append-only `prediction_ledger.csv` schema、`score-daily` production 写入和 `aits feedback calibrate-predictions` outcome 报告；剩余缺口是把 `EXPERIMENT-001` challenger/shadow 候选自动接入同一 ledger，并让周/月复盘读取 production vs challenger 表现；详细拆解见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#shadow-001在线-pit-prediction--shadow-ledger`|每个 production/challenger prediction 都能追溯到当时可见的数据快照、特征版本、规则版本和质量状态；challenger 行 `production_effect=none`，不得改变正式评分、仓位闸门或日报动作；outcome 追加后不能改写 signal-time 字段；周报/月报能区分 production 真实判断和 challenger shadow 表现；实现时同步更新 `docs/system_flow.md` 并补充 append-only、future outcome isolation 和 challenger isolation 测试|2026-05-05: 新增任务，原因：历史 PIT 不完整时，在线真实 prediction log 是后续模型迭代和晋级最可靠的 PIT 样本；现有 `decision_snapshot` 只覆盖 production 判断，不足以比较多个 challenger。2026-05-05: 从 READY 改为 BASELINE_DONE，原因：生产日报写入 prediction ledger，prediction outcome 校准命令和报告已实现，测试覆盖 append-only、shadow isolation 和 outcome 分桶；完整 DONE 仍需 challenger 自动接入和周期复盘展示。|
|BACKTEST-003|回测/模型晋级门槛|P1|BASELINE_DONE|系统实现 + 项目 owner|已实现模型晋级门槛基础版：`aits backtest --promotion-report` 汇总数据可信度、robustness、lag sensitivity、shadow outcome 和 rule governance，并在回测报告中嵌入晋级状态；剩余缺口是真实 forward shadow 样本和 owner/rule approval 进入 `GOV-001`；详细拆解见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#backtest-003模型晋级门槛与回测路线`|回测或模型评估报告默认声明 `ai_after_chatgpt` regime 和实际日期范围；C 级输入不得输出无条件 Sharpe/CAGR 主结论，也不得生成 promotion 通过状态；晋级状态必须引用具体 evidence、PIT 覆盖、robustness、shadow outcome 和 rule card；实现时同步更新 `docs/system_flow.md` 并补充报告文本和治理隔离测试|2026-05-05: 新增任务，原因：`BACKTEST-001/002` 已完成稳健性和数据可信度标注，但还缺一个可审计的“探索 -> 近似 PIT 回测 -> 前向 shadow -> 晋级审批 -> 生产规则”路线，防止漂亮历史回测被误读为上线证据。2026-05-05: 从 READY 改为 BASELINE_DONE，原因：新增 promotion gate 模块、Markdown/JSON 输出、回测 CLI 接入、主回测报告摘要和测试；完整 DONE 仍需真实 prediction outcome 样本和 owner approval。|
|PIT-002|特征/PIT 字段强校验|P0|BASELINE_DONE|系统实现|已完成字段级 source 检查基础版：`feature_availability` 报告可列出 `available_time` 覆盖率、缺失行、未来可见时间行和保守 fallback；`build-features`、`score-daily`、`backtest` 已对价格/利率输入写入字段级检查；剩余缺口是后续每个新增数据源继续接入专属 `available_time` 字段检查；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|新增或修改的数据源不能只凭 `event_time` 进入 A/B 级回测、日报主结论或 promotion gate；验证报告需要列出每个输入族的 `available_time` 字段覆盖率、缺失处理策略和最早可用时间；测试覆盖未来公告、未来 universe、未来宏观/证据样本不能进入 signal-time 结论|2026-05-05: 新增任务，原因：`PIT-001` 已完成 catalog 和报告基础版，但最终闭环要求数据源级可见时间强校验，避免新特征绕过 `available_time <= decision_time`。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始实现数据源级 `available_time` 字段覆盖、未来可见时间和 report/trace 摘要。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `FeatureAvailabilitySourceCheck`、字段级报告/summary、市场输入 CLI 接入和未来 `filed_date` 防护测试；完整 DONE 仍需随新增数据源继续补专属检查。|
|SHADOW-002|模型评估/challenger shadow runner|P1|BASELINE_DONE|系统实现|已新增 `aits feedback run-shadow`：读取 rule experiment ledger、production decision snapshot 和 trace，追加 challenger prediction 到 append-only ledger，强制 `production_effect=none`；剩余缺口是真实候选规则每日运行调度和长期样本积累；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|`score-daily` 或独立 shadow 命令可同时记录 production 与多个 challenger；challenger 不写正式日报动作、不改变 `scores_daily.csv` production 行、不改变 position gate 或 belief_state；prediction ledger 能按 candidate_id/model_version 区分并追溯输入；测试覆盖 production 隔离、重复 prediction_id 停止和 candidate trace 引用|2026-05-05: 新增任务，原因：`SHADOW-001` 已有 ledger 和 outcome 校准，但还缺真正每天运行 challenger 的入口，这是进入前向 shadow 的下一步。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始实现独立 shadow runner 和 append-only prediction ledger 写入。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：shadow runner、报告和 CLI 测试已完成；完整 DONE 仍需真实每日调度和 outcome 样本。|
|SHADOW-003|模型评估/forward shadow 样本成熟度|P1|BASELINE_DONE|系统实现 + 时间窗口|已新增 `aits feedback shadow-maturity`，按 candidate、horizon、market regime 和 `production_effect` 汇总 available/pending/missing、收益、胜率、回撤和 benchmark excess，并输出 `READY_FOR_SHADOW` / `READY_FOR_GOV_REVIEW`；真实 promotion 结论仍受交易日和 label horizon 限制；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|报告能显示 production vs challenger 可用样本数、胜率、平均收益、最大回撤、benchmark excess return、样本不足原因和预计可评估日期；少于最低样本或 horizon 未完成时 promotion gate 必须保持 `READY_FOR_SHADOW` 或 `MISSING`；测试覆盖样本不足不晋级、pending outcome 不算通过、missing data 降级|2026-05-05: 新增任务，原因：前向 shadow 的最大 blocker 是真实时间和 outcome 样本积累；该任务先建立监控和门槛，让样本成熟前不会误晋级。2026-05-05: 从 BLOCKED_EXTERNAL 改为 IN_PROGRESS，原因：先实现成熟度监控代码；真实 promotion 结论仍受交易日和 label horizon 限制。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：样本成熟度报告、CLI 和样本不足测试已完成；完整 DONE 需要真实 outcome 样本达到门槛。|
|GOV-003|治理/candidate promotion 与 retirement 流程|P1|BASELINE_DONE|系统实现 + 项目 owner|已新增受控 `promote-rule-card` / `retire-rule-card` 工作流：promotion 必须带 owner、批准理由、promotion report 和 outcome 引用；retirement 必须带退役原因；写出后立即复用 rule card validator；真实规则上线仍需要 owner 逐项批准；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|未通过 promotion gate 或缺少 owner approval 的 candidate 不能写入 production rule card；promotion/retirement 操作必须更新 rule card registry、生成治理报告、保留旧版本、写明 rollback condition，并被 `score-daily`/`backtest` trace 捕获；测试覆盖未批准不能 promotion、批准后 production rule version 变更可追溯、retired rule 不再参与生产|2026-05-05: 新增任务，原因：当前 rule card registry 只是 baseline governance；完整路线需要把通过 shadow 的候选规则以 owner 批准方式变成 production rule。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始实现受控 promotion/retirement CLI 和治理报告。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：规则生命周期写入、报告、校验和 promotion/retirement 测试已完成；完整 DONE 取决于真实 owner approval 和通过门槛的候选规则。|
|REPORT-005|报告/production vs challenger 复盘|P2|BASELINE_DONE|系统实现|已把 `prediction_outcomes.csv` 和 shadow maturity 摘要接入 `aits reports investment-review` 与 `aits feedback loop-review`；报告区分 production/challenger、样本成熟度和样本不足边界；剩余缺口是真实 shadow outcome 积累后再校准展示阈值；详细顺序见 `docs/requirements/pit_backtest_shadow_plan_2026-05-05.md#下一批开发顺序`|周报/月报显示 production vs challenger 的样本数、收益、回撤、胜率、benchmark excess、pending outcome、promotion gate 状态和 owner action；样本不足时只给观察建议，不给 production 结论；测试覆盖报告文本、样本不足边界和 `production_effect=none`|2026-05-05: 新增任务，原因：`REPORT-004` 已有投资复盘入口，但还没有读取 prediction/shadow outcome 和 promotion gate，无法完整回答候选规则是否值得晋级。2026-05-05: 从 READY 改为 IN_PROGRESS，原因：开始把 prediction outcomes 和 shadow maturity 接入周/月复盘与 feedback loop review。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：周/月复盘和 feedback loop review 已读取 prediction outcomes 并展示 production vs challenger；完整 DONE 仍需真实样本验证报告解释质量。|
|REPORT-006|报告/关注股票趋势|P1|DONE|系统实现|已在 `score-daily` 日报结论区增加核心观察池逐 ticker 趋势分析；只读使用已通过市场数据质量门禁的 `features_daily` 价格/趋势特征，不改变评分、仓位闸门、execution policy 或 prediction ledger|日报显示 `config/universe.yaml` 的 AI core_watchlist 每个 ticker 的 1/5/20 日收益、20/50/100/200 日均线位置、相对均线偏离和趋势状态；缺少某 ticker 或窗口特征时显式标记数据缺口；报告声明 `production_effect=none`，trace bundle 可追溯到价格缓存和特征缓存；同步更新 `docs/system_flow.md` 并补充报告文本测试|2026-05-05: 新增并进入实现，原因：owner 要求当前输出结论增加关注股票趋势分析；该改动影响日报解释和投资阅读顺序，但不改变评分或仓位。2026-05-05: 从 IN_PROGRESS 改为 DONE，原因：新增关注股票趋势分析模块、日报章节、trace claim/evidence、系统流图和测试；`ruff check src tests` 与全量 `pytest -q` 382 项通过。|
|MACRO-001|宏观/仓位|P2|BASELINE_DONE|系统实现|已完成第一阶段独立 `macro_risk_asset_budget` 层：在 `config/portfolio.yaml` 静态预算基础上，按 VIX、10Y 利率变化和 FRED `DTWEXBGS` 广义美元指数变化下调总风险资产预算；日报、`scores_daily.csv`、decision snapshot、belief_state、回测每日明细和回测报告均输出静态预算、调整后预算、触发等级和原因；剩余缺口是参数 replay/shadow、更多宏观指标和 `GOV-001` 批准|当 VIX、利率、美元等触发流动性风险时，总风险资产预算可下调；AI 在风险资产内部的相对权重仍可单独解释；回测实际策略敞口使用总资产内 AI exposure；实现时同步更新 `docs/system_flow.md` 和测试|2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始实现宏观总风险资产预算层，避免宏观流动性只体现在模块分或风险资产内 AI gate。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `macro_risk_asset_budget` 配置、计算层、日报/CSV/snapshot/belief_state/回测输出和测试；完整 `DONE` 仍需阈值 replay/shadow 和规则批准。2026-05-05: 更新宏观美元输入语义，原因：DATA-006 已将 Yahoo `DX-Y.NYB` 替换为 FRED `DTWEXBGS` 广义美元指数。|
|THESIS-001|交易 thesis|P1|DONE|系统实现|已把 thesis 从结构校验/复核升级为状态机：`draft -> active -> warning -> challenged -> invalidated -> closed`，并把 `warning/challenged/invalidated` 作为日报解释和 `position_gate` 候选输入；详细拆解见 `docs/requirements/decision_support_improvements_2026-05-04.md#thesis-001`|thesis 报告显示当前状态、上次状态、触发原因、证据来源、相关 ticker/产业链节点和人工复核要求；`draft` 不进入正式评分，`warning` 至少触发禁止主动加仓，`challenged/invalidated` 能进入仓位限制或人工复核流程；实现时同步更新 `docs/system_flow.md` 并补充 schema、复核、日报和回测相关测试|2026-05-04: 从 P2/PROPOSED 升级为 P1/READY，原因：状态机比单纯总分下降更能解释 thesis 承压、证伪、估值过高和基本面恶化的差异。2026-05-04: 从 READY 改为 IN_PROGRESS，原因：开始扩展 thesis 状态机字段、状态复核规则和仓位闸门联动。2026-05-04: 从 IN_PROGRESS 改为 DONE，原因：状态机 schema、状态迁移校验、复核健康状态、日报人工复核摘要、position_gate 联动、模板和测试均已完成；`python -m pytest -q` 通过 220 项测试。|
|DATA-001|估值数据|P1|BASELINE_DONE|系统实现 + 项目 owner|已完成低成本 baseline：新增 EODHD Earnings Trends 当前 EPS 90 日修正接入、原始 payload 审计、合并估值快照、中文报告和数据源目录/系统流图；剩余历史 point-in-time estimates/archive 采购或回填暂缓，forward-only 自建快照归档由 `DATA-003` 承接；详细拆解见 `docs/requirements/valuation_history_baseline_2026-05-04.md`|Baseline 验收：当前 `score-daily` 可从 EODHD trends 生成可审计 `eps_revision_90d_pct`，记录 provider、endpoint、请求参数、下载时间、row count 和 checksum，并标记采集日后可见；`valuation_percentile` 至少有 3 个历史点。完整 `DONE` 验收仍需要稳定 90 天真实历史/PIT 支持；历史采购/回填当前不推进，forward-only 快照按 `DATA-003` 自然积累，且不得把采集日回填伪装为 signal_date 当时可见数据|基础拉取、历史缓存和校验已完成；EODHD baseline 不替代 Intrinio/Zacks、FactSet 或等价 PIT estimates archive。2026-05-04: 从 BLOCKED_EXTERNAL 改为 IN_PROGRESS，原因：owner 批准先按低成本 baseline 接入 EODHD Earnings Trends 补当前 EPS 90 日修正，同时保留严格 PIT 完成条件。2026-05-04: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：实现 `aits valuation fetch-eodhd-trends`、`data/raw/eodhd_earnings_trends` 原始审计、合并估值快照、数据源目录、系统流图、README/架构/backlog 文档和测试；`python -m ruff check src tests`、`python -m pytest -q` 通过。2026-05-04: owner 决策不购买或伪造历史 PIT；本任务不继续推进历史 PIT estimates/archive 采购、接入或历史回测可见性升级；forward-only 自建快照归档另由 `DATA-003` 登记。|
|AUDIT-001|回测审计|P2|DONE|系统实现|已用 2026-04-01 至 2026-05-05 真实本地缓存回测复核 `backtest_audit_*.md` 和覆盖诊断；当前格式足够定位主要输入问题|已确认报告按月列出数据可信度、模块覆盖率和历史输入缺口，`backtest_input_coverage_*.csv` 提供 `signal_date` 级下钻；若后续出现按月聚合无法定位的新问题，再登记新任务补细粒度 Markdown 视图|2026-05-06: 从 VALIDATING 改为 DONE，原因：运行 `aits validate-data --as-of 2026-05-05` 与 `aits backtest --from 2026-04-01 --to 2026-05-05 --quality-as-of 2026-05-05`，审计输出清楚标出 C 级数据可信度、估值/风险事件历史缺口、模块覆盖率和 2026-04/2026-05 月度问题。|
|RISK-006|政策/地缘可靠数据源|P1|BASELINE_DONE|项目 owner + 系统验证|已完成低成本官方来源第一版：`aits risk-events fetch-official-sources` 监控 Federal Register/BIS/OFAC/USTR/Congress.gov/GovInfo/Trade.gov CSL，写入 raw payload、download manifest、待人工复核候选 CSV 和中文报告；剩余缺口是 owner 配置 Congress.gov/GovInfo API key、每日复核声明和真实样本验证；详细边界见 `docs/requirements/risk_event_review_workflow_2026-05-04.md#risk-006`|Baseline 验收：官方来源组合、provider、endpoint、授权/LLM 权限、缓存路径、row count/checksum、跳过来源和 `pending_review` 隔离已可审计；完整 `DONE` 仍需真实样本通过 source policy、occurrence/attestation 校验，并让 `score-daily` 区分“已复核无重大事件”与“没有可靠来源输入”；不得把 `public_convenience`、未授权供应商文本或 `llm_extracted` 输出直接当作可评分输入|2026-05-05: 新增任务，原因：owner 指出当前 `policy_geopolitics` 置信度很低，后续需要可靠数据源才能正确分析；在可靠来源和复核纪律到位前，本模块保持保守降级，不用临时新闻源或模型推断绕过。2026-05-05: 从 BLOCKED_OWNER_INPUT 改为 IN_PROGRESS，原因：owner 确认低成本官方来源组合和人工复核责任，开始实现第一版官方来源抓取与审计链路。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：官方来源抓取、原始缓存、manifest 审计、候选 CSV、中文报告、数据源目录、系统流图、`.env.example`、Federal Register live smoke test 和全量测试已完成；完整生产使用仍依赖 owner API key、每日复核声明和真实样本。|
|RISK-007|风险事件/日报前 OpenAI 自动预审|P1|BASELINE_DONE|系统实现 + 项目 owner|基础版已完成：`score-daily` 默认在风险事件发生记录校验前抓取官方来源并调用 OpenAI metadata-only 风险事件预审；详细拆解见 `docs/requirements/risk_event_daily_openai_precheck_2026-05-05.md`|`score-daily` 默认在风险事件发生记录校验前抓取官方来源并调用 OpenAI Responses API，可用 `--skip-risk-event-openai-precheck` 跳过；默认最多处理 20 条官方候选，OpenAI 默认模型 `gpt-5.5`、`reasoning.effort=high`、读超时 120 秒；单个 OpenAI 请求失败时重试该请求 2 次，仍失败则整批 fail closed；输出官方抓取报告、OpenAI 预审报告和 `risk_event_prereview_queue.json`；缺 `OPENAI_API_KEY`、官方来源抓取失败、provider LLM 权限失败或 OpenAI 请求最终失败时停止日报评分；无候选或 irrelevant 结果不增加人工队列；预审输出仍为 `llm_extracted / pending_review`，不得写 occurrence、复核声明、评分、仓位闸门或 thesis 状态；完整 `DONE` 仍需 owner 人工复核纪律|2026-05-05: 新增并进入实现，原因：owner 要求先用 OpenAI key 做政策/地缘预审，并加入日报前自动流程；同时明确默认不把正常无关结果加入人工确认输入。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增官方候选批量 OpenAI 预审、`score-daily --risk-event-openai-precheck` 前置流程、README、系统流图和测试；`ruff check src tests` 通过，`pytest -q` 362 passed。2026-05-05: owner 确认默认 OpenAI 响应可能超过 60 秒，已将默认请求读超时调为 600 秒并开放 CLI 参数；单候选真实 smoke 等待约 408 秒后收到 OpenAI HTTP 502，按 fail-closed 未写队列。2026-05-05: `gpt-5.5` 单候选 smoke 可收到正常响应；同步修正 irrelevant/none 但带 risk_id 的输出不入人工队列。2026-05-05: owner 决策后续默认使用 `gpt-5.5`，不再默认使用较不稳定的 `gpt-5.5-pro`；代码常量、README、系统流图、数据源目录和示例已同步；不带 `--openai-model` 的单候选日报前预审 smoke 约 96 秒完成，预审 PASS_WITH_WARNINGS，待复核队列 0，日报 PASS_WITH_LIMITATIONS。2026-05-05: owner 决策把 OpenAI 默认配置改为 `gpt-5.5` + `reasoning.effort=high` + 120 秒读超时；`score-daily` 默认启用日报前预审，候选排序改为 AI 政策/出口管制优先，日报正文新增预审摘要。2026-05-05: 默认 20 条真实样本已执行，官方来源抓取 PASS、8 个 payload、402 条候选；OpenAI 预审上限 20 条，LLM claim 数 23，HTTP 502 错误 1、警告 13，按 fail closed 未写新队列，日报评分未继续。2026-05-05: owner 批准单个 OpenAI 请求失败时重试 2 次；第 3 次仍失败则整批 fail closed，不做断点续跑或部分成功入队。2026-05-05: 已实现并验证请求级重试和批量失败停止；`ruff check src tests`、目标测试 42 passed、完整 `pytest -q` 366 passed；默认 20 条真实样本官方抓取 PASS，OpenAI 预审在单请求 3 次尝试后仍失败，报告 FAIL、LLM claim 16、错误 1、警告 7，未写新队列，日报未继续。|
|RISK-008|风险事件/OpenAI 无人复核边界|P1|BASELINE_DONE|系统实现|已明确日报前 OpenAI 预审在 owner 暂无复核精力时采用 backlog-only 边界：候选继续保留审计和告警，但不进入 `execution_policy.manual_review_gate_ids`，不要求当天人工复审；正式评分/仓位仍只读取已确认 occurrence 或有效复核声明|日报必须说明 OpenAI 预审候选是未确认 backlog，不会写 occurrence、复核声明、评分、仓位闸门、thesis 状态，也不会单独把执行动作改成 `wait_manual_review`；若没有人工复核能力，policy/geopolitics 仍保持低置信度/数据不足，而不是把 LLM 判断当作风险事实或无风险证明|2026-05-05: 新增并进入实现，原因：owner 表示暂时没有精力复审 OpenAI 输出；需要把报告边界从“待当天人工复核”澄清为“未确认 backlog”，避免误读为必须处理的人工 SLA，同时保持 source policy 和 LLM 隔离。2026-05-05: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：日报 OpenAI 预审章节、README、数据源目录和系统流图已明确 backlog-only 边界，测试覆盖 OpenAI backlog 不单独触发 `wait_manual_review` 文案。|
|RISK-010|风险事件/官方候选 AI 模块 triage|P1|BASELINE_DONE|系统验证 + 项目 owner|详细拆解见 `docs/requirements/risk_event_candidate_triage_2026-05-10.md`；已实现确定性 triage 命令和报告；后续如 owner 真实复核发现 bucket 误判，应新建或重开任务调整关键词/抽样策略|提供确定性 triage 命令和报告，读取 `official_policy_source_candidates_YYYY-MM-DD.csv` 后输出 `official_policy_candidate_triage_YYYY-MM-DD.csv` 与中文报告；分类必须优先保留 AI/半导体/先进计算/出口管制/相关 ticker 的直接命中项，并把无明显 AI 模块联系的 sanctions/geopolitics 候选降为低优先级；输出强制 `production_effect=none`，不得写入正式 occurrence、评分或仓位闸门；系统流图、README 和测试同步更新|2026-05-10: 新增并进入实现，原因：owner 确认“先分类”，关注 AI 模块，无明显联系的官方政策/地缘候选降低复核优先级。2026-05-10: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits risk-events triage-official-candidates`、triage CSV/报告、系统流图、README、需求文档和测试；真实 2026-05-10 官方候选 403 条分类为 must_review=7、review_next=1、sample_review=94、auto_low_relevance=297、duplicate_or_noise=4；验证通过 `ruff check src tests`、目标测试 24 passed 和完整 `pytest -q` 436 passed。|
|RISK-011|风险事件/高优先级候选 LLM 风险等级预审|P1|BASELINE_DONE|系统验证 + 项目 owner|详细拆解见 `docs/requirements/risk_event_triaged_llm_precheck_2026-05-10.md`；已实现高优先级候选 OpenAI metadata-only 预审入口；后续采纳为正式 occurrence 或复核声明仍需 owner 复核或独立任务推进|提供命令读取 triage CSV 和官方候选 CSV，按 bucket 过滤后调用现有 OpenAI 风险预审链路；输出必须保存 model、reasoning effort、request id、input/output checksum、`status_suggestion`、`level_suggestion`、matched risk/ticker/node 和人工复核问题；结果强制 `llm_extracted / pending_review`，不得写入正式 occurrence、评分或仓位闸门；系统流图、README 和测试同步更新|2026-05-10: 新增并进入实现，原因：owner 确认高风险源可用 LLM 判断风险等级，但该等级只能作为人工复核建议。2026-05-10: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits risk-events precheck-triaged-official-candidates`、官方候选 CSV loader、triage bucket 过滤、系统流图、README、需求文档和测试；真实 2026-05-10 高优先级候选 8 条送入 OpenAI，生成 5 条 pending_review 预审记录，其中 L2/L3 候选 4 条、active 候选 0 条；验证通过 `ruff check src tests`、CLI help、目标测试 26 passed 和完整 `pytest -q` 438 passed。|
|RISK-012|风险事件/LLM 正式评估准入|P1|BASELINE_DONE|系统验证 + 项目 owner|详细拆解见 `docs/requirements/risk_event_llm_formal_assessment_2026-05-10.md`；已实现 LLM formal assessment 准入；后续如需调整 LLM watch/active 映射或人工复核替代关系，应新建或重开任务|提供命令把 `risk_event_prereview_queue.json` 转换为正式 risk occurrence YAML 与 LLM formal attestation；日报政策/地缘模块必须显示 `llm_formal_assessment` 来源类型，置信度低于人工复核；LLM formal evidence 最高按 B 级处理，默认不能单独触发 position gate；输出保留 model、request id、checksum、status/level suggestion、confidence 和“未人工复核”声明；系统流图、README、测试同步更新|2026-05-10: 新增并进入实现，原因：owner 明确要求参考 LLM 复核结果作为正式评估结果，先不通过人工复核。2026-05-10: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits risk-events apply-llm-formal-assessment`、LLM formal occurrence/attestation 写入、日报 `llm_formal_assessment` 来源类型和较低置信度、系统流图、README、需求文档和测试；真实 2026-05-10 队列写入 5 条 watch occurrence 和 1 条 LLM formal attestation，validate-occurrences PASS；验证通过 `ruff check src tests`、CLI help、完整 `pytest -q` 444 passed。|
|LLM-003|新闻/LLM/Agent 请求缓存|P1|DONE|系统实现|详细拆解见 `docs/requirements/openai_request_cache_2026-05-10.md`；已实现 provider-neutral 短 TTL 精确请求缓存和本地请求/响应审计归档，OpenAI Responses 是首个接入 adapter，避免调试和日报 rerun 在半天或一天内重复发送完全相同请求|相同 OpenAI request payload 在 TTL 内第二次运行不调用 HTTP client，报告显示 cache HIT；TTL 过期后允许重新请求；所有实际发送请求写入本地审计缓存，包含 provider/api family、sanitized request、response body、attempt diagnostics、cache key 和 checksum，不包含 API key；`cache_allowed=false` 时 fail closed；系统流图、README 和测试同步更新|2026-05-10: 新增并进入实现，原因：owner 要求短时间重复发送相同 OpenAI 请求时复用本地缓存，并完整归档已付费请求/响应详情。2026-05-10: 从 IN_PROGRESS 改为 DONE，原因：新增 `data/processed/agent_request_cache` 通用短 TTL request cache/archive，OpenAI 归档位于 `archive/openai/responses/YYYY-MM-DD/`，CLI 默认 24 小时 TTL 并可通过 `--openai-cache-ttl-hours` 调整；报告和队列输出 cache HIT/MISS/EXPIRED/DISABLED；验证通过目标测试 53 passed、完整 `pytest -q` 445 passed 和 `git diff --check`。|
|OPS-003|运行架构/云端持续运行|P1|BASELINE_DONE|系统实现 + 项目 owner|本地基础闭环已完成 `aits ops daily-plan` 与 `aits ops daily-run`；详细拆解见 `docs/requirements/cloud_operations_2026-05-06.md`。后续云 VM、通知、secret 管理和备份策略需 owner 决策时新建或重开任务|每日运行入口必须明确下载、PIT 快照、数据质量门禁、日报、pipeline health、secret hygiene 的顺序和阻断关系；缺少关键凭据或输入时不得静默跳过；云端部署文档必须覆盖 systemd/cron、持久化目录、日志、告警、备份和恢复；运行报告必须显示数据质量状态和 output artifact 路径|2026-05-06: 新增任务，原因：当前持续运行依赖开发机开机；迁移到云 VM 可行但会影响运行链路、质量门禁和 PIT 样本连续性，必须先拆解和登记。2026-05-06: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增每日运行计划命令、报告、系统流图、README 和测试；完整 DONE 仍需 run log、真实调度执行器、云 VM runbook、备份恢复和通知策略。2026-05-08: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：每日自动化真实运行暴露 `daily-plan` 只生成计划、不执行前置依赖和日报评分，owner 同意以能让每日任务正常跑完为目标补齐执行入口。2026-05-08: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits ops daily-run`、脱敏执行报告和关键 artifact 状态检查；自动化改为调用 daily-run；真实 2026-05-08 每日链路 9/9 步通过，验证 `ruff check src tests` 与 `pytest -q` 412 passed。|
|OPS-005|运行架构/日报前置数据依赖|P0|DONE|系统实现|详细拆解见 `docs/requirements/daily_score_preflight_dependencies_2026-05-07.md`；daily plan 已新增 SEC companyfacts 刷新、SEC metrics 抽取/校验和 FMP 估值快照刷新；2026-05-07 真实日报链路已验证不再因当日 SEC metrics 缺失停止；`ops health` 已显式检查 FMP PIT 抓取报告状态|默认每日计划必须在 `score-daily` 前列出并阻断执行 `aits fundamentals download-sec-companyfacts`、`aits fundamentals extract-sec-metrics --as-of YYYY-MM-DD`、`aits fundamentals validate-sec-metrics --as-of YYYY-MM-DD` 和 `aits valuation fetch-fmp --as-of YYYY-MM-DD`；缺少 `SEC_USER_AGENT` 或 `FMP_API_KEY` 必须显示 `BLOCKED_ENV`；报告、README、系统流图和测试同步更新；下一次真实日报不应再因当日 SEC metrics 文件缺失或估值快照过期而失败；PIT 抓取失败必须进入 FMP PIT 抓取报告和 pipeline health/alert|2026-05-07: 新增任务，原因：真实每日运行暴露日报依赖当日 SEC metrics，但现有每日编排没有生成该 CSV；同时审计发现估值快照刷新也不在日报前置链路中。2026-05-07: 从 IN_PROGRESS 改为 VALIDATING，原因：`aits ops daily-plan` 已补齐 SEC companyfacts、SEC metrics、SEC metrics validation 和 FMP valuation 前置步骤；验证通过 `ruff check src tests` 和完整 `pytest -q` 408 项。2026-05-07: 从 VALIDATING 改为 DONE，原因：真实日报链路通过，`score-daily` 输出 `PASS_WITH_LIMITATIONS`；同时修复运行观察发现的 PIT fetch 报告状态 health 漏报，验证通过 `ruff check src tests`、`pytest -q tests/test_pipeline_health.py`、`pytest -q tests/test_ops_daily.py tests/test_pipeline_health.py` 和 `aits ops health --as-of 2026-05-07`。|
|OPS-006|运行架构/休市日每日模式|P0|DONE|系统实现|详细拆解见 `docs/requirements/closed_market_daily_mode_2026-05-10.md`；已实现 `daily-plan/daily-run` 的显式休市日模式，避免周末或 NYSE 常规休市日硬跑当日市场下载和 `score-daily`|`aits ops daily-plan` 与 `aits ops daily-run` 必须识别非交易日并在报告中声明 market session、休市原因和上一交易日；当上一交易日价格缓存已覆盖时不得用休市日作为 `download-data --end`；休市日不得生成新的 `daily_score`、decision snapshot、执行动作或 score-derived alerts；仍可运行官方政策来源、PIT、SEC、valuation、ops health 和 secret scan，且 health 不得把缺少休市日日报当作失败；系统流图、README、测试和需求文档同步更新|2026-05-10: 新增并进入实现，原因：真实 2026-05-10 daily-run 在周日用 `download-data --end 2026-05-10` 失败，owner 决策将休市日运行策略正式记录并实现，避免把休市日缺行情误解释为评分失败或生成伪交易结论。2026-05-10: 从 IN_PROGRESS 改为 DONE，原因：新增交易日历模块、closed-market daily-plan/daily-run 步骤调整、`ops health --non-trading-day`、README/系统流图/需求文档和测试；验证 `ruff check src tests` 通过，`pytest -q` 424 passed，2026-05-10 daily-plan CLI smoke 输出 `READY_WITH_SKIPS`、`CLOSED_MARKET` 和上一交易日 `2026-05-08`。2026-05-10: 追加验证，原因：owner 要求清理后复验交易日/非交易日流程；备份并移出 2026-05-10 旧 daily ops 报告后真实 `daily-run --as-of 2026-05-10` 通过 `PASS_WITH_SKIPS`，交易日路径用干净临时目录 fake runner 验证 2026-05-08 9/9 步 PASS，并修复 PIT produced_paths 隔离路径问题；复验 `ruff check src tests` 通过、`pytest -q` 424 passed。2026-05-10: 补充归档输入回放，5/8 PIT 备份可用，但必须同时冻结 PIT manifest 可见窗口并隔离 5/9/5/10 valuation snapshots；按该边界执行缓存回放 `daily-run --as-of 2026-05-08`，`score_daily`、`ops health`、secret scan PASS，整体 `PASS_WITH_SKIPS`。|
|OPS-007|运行架构/历史交易日归档回放|P0|DONE|系统实现|详细设计见 `docs/requirements/historical_day_replay_infrastructure_2026-05-10.md`；单日 cache-only replay、批量 `replay-window`、production diff、production `daily-run` 脱敏 metadata sidecar、OpenAI 历史缓存策略、pre-run input checksum 和 production visibility cutoff 归档均已完成|必须提供 cache-only 历史交易日回放入口，冻结 as-of 可见输入窗口，隔离 replay 输出，不改写生产 artifacts，不调用 live provider；PIT/valuation/SEC/market/macro/risk/rule card 均需按 as-of 门禁 fail closed；输出 replay bundle、input freeze manifest、结构化脱敏 run log 和可选 production diff；5/8 回放应能自动排除 5/9/5/10 valuation 与未来 PIT manifest 行并通过 `score_daily`、`ops health`、secret scan；批量回放必须按交易日枚举并生成结构化窗口报告；production daily-run 必须归档 run id、git/config/rule hash、命令清单、env presence、pre-run input checksum、produced artifact checksum 和 visibility cutoff；OpenAI replay 不得调用 live API，只能 disabled 或 cache-only|2026-05-10: 新增 PROPOSED，原因：owner 确认历史交易日分析产出回放会成为模型调优重点基础设施；当前只能通过手工 backup/filter/restore 做归档回放，无法支撑高频、安全、可审计 replay。2026-05-10: 从 PROPOSED 改为 IN_PROGRESS，原因：owner 确认按设计实现；第一阶段聚焦单日 cache-only replay MVP，不覆盖批量回放和 production replay 元数据强化。2026-05-10: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `aits ops replay-day`、input freeze manifest、隔离 replay bundle、PIT/valuation 可见窗口冻结、replay-scoped `score-daily`/`ops health`/secret scan；真实 2026-05-08 cache-only replay PASS，验证 `ruff check src tests` 通过、`pytest -q` 426 passed。2026-05-10: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：owner 要求后续任务依次开发，先推进阶段 5 的批量窗口回放和 production diff，仍不进入 production daily-run 元数据强化阶段。2026-05-10: 阶段 5 完成并继续阶段 6，原因：`replay-window` 真实 inventory-only 验证 2026-05-07 到 2026-05-10 通过且跳过周末，`replay-day --compare-to-production` 真实 2026-05-08 完整回放通过并生成 diff；下一步补 production `daily-run` metadata sidecar。2026-05-10: 从 IN_PROGRESS 改为 BASELINE_DONE，原因：新增 `daily_ops_run_metadata_YYYY-MM-DD.json`，记录 run id、git/config/rule hash、命令清单、env presence、step result 和 produced artifact checksum；阶段 5/6 验证通过 `ruff check src tests`、`pytest -q` 430 passed、真实 5/8 replay diff PASS 和 5/7-5/10 window inventory PASS。2026-05-10: 从 BASELINE_DONE 改为 IN_PROGRESS，原因：owner 要求完成剩余重点任务并提交，范围收敛为 OpenAI 历史缓存策略、pre-run input checksum 和 production visibility cutoff 归档。2026-05-10: 从 IN_PROGRESS 改为 DONE，原因：新增 `--openai-replay-policy disabled|cache-only`、production metadata cutoff 推导、pre-run input artifact checksum；真实 5/8 OpenAI cache-only inventory replay PASS，验证 `ruff check src tests`、`pytest -q` 431 passed 和 CLI help 均通过。|
|DATA-014|数据质量/DTWEXBGS 新鲜度|P0|DONE|系统实现 + 项目 owner|详细拆解见 `docs/requirements/dtwexbgs_freshness_policy_2026-05-09.md`；已把 `DTWEXBGS` 从全体 FRED 宏观序列 7 日历日 freshness 改为 14 日历日 series 级阈值；后续 owner 可决定是否接入第二宏观来源或直接 Federal Reserve H.10 来源|`DTWEXBGS` 使用符合 Federal Reserve H.10 周度发布机制的 series 级 freshness 阈值，DGS2/DGS10 仍保持较严格默认阈值；`aits validate-data`、`score-daily` 和 `backtest` 继续 fail closed 于真正过期或缺失的宏观输入；数据源成本选项和接入边界记录在需求文档；系统流图、配置和测试同步更新|2026-05-09: 新增并进入实现，原因：真实每日 `daily-run` 已更新 FRED 缓存，但 FRED/Board `DTWEXBGS` 最新 observation 仍停在 2026-05-01，8 个日历日触发全局 7 天门禁；该滞后符合 H.10 周度发布时间特征，需用 series 级 freshness 表达，而不是绕过数据质量门禁。2026-05-09: 从 IN_PROGRESS 改为 DONE，原因：新增 rate series 级 `max_stale_calendar_days` override，`DTWEXBGS` 配置为 14 天，DGS2/DGS10 保持默认 7 天；`validate-data --as-of 2026-05-09` 已从 `FAIL` 变为 `PASS_WITH_WARNINGS` 且错误数 0；验证通过 `ruff check src tests`、目标测试 39 passed 和完整 pytest 415 passed。2026-05-10: 归档时将原登记 ID 从 `DATA-012` 修正为 `DATA-014`，原因：`DATA-012` 已被早期 Yahoo 诊断复查任务占用，避免 completed register 出现重复 ID。|
|DATA-015|PIT/原始快照不可变性|P0|DONE|系统实现|详细拆解见 `docs/requirements/pit_raw_payload_immutability_2026-05-09.md`；已修复 `valuation fetch-fmp` 同日多次运行覆盖 `fmp_analyst_estimates` raw JSON，导致 PIT manifest checksum mismatch 的问题|PIT manifest 指向的 raw payload 不得被后续日常步骤覆盖；同一 ticker 同一 captured date 多次运行必须写入不同 analyst-estimates raw 文件，文件名含下载时间或 checksum；`aits ops daily-run` 的 pipeline health 不再因同日 valuation rerun 产生 PIT checksum mismatch；测试覆盖同日多次写入不覆盖和 CLI raw path 变更|2026-05-09: 新增并进入实现，原因：验证 `DATA-014` 后真实 `daily-run` 已通过 score_daily，但 pipeline_health 失败，报 6 条 PIT raw payload checksum mismatch；根因是 valuation 前置步骤覆盖了 PIT manifest 之前记录的同日 analyst-estimates raw 文件。2026-05-09: 从 IN_PROGRESS 改为 DONE，原因：analyst-estimates raw 文件名新增 downloaded_at UTC token 与 checksum 前缀，loader 继续兼容旧 glob；真实 `aits ops daily-run` 9/9 步 PASS，pipeline health PASS，secret scan PASS；目标测试 53 passed，Ruff 通过。2026-05-10: 归档时将原登记 ID 从 `DATA-013` 修正为 `DATA-015`，原因：`DATA-013` 已被早期 SEC 日报指标缓存任务占用，避免 completed register 出现重复 ID。|
|SCORE-006|评分/仓位置信度约束|P0|DONE|系统实现|详细拆解见 `docs/requirements/confidence_position_gate_2026-05-08.md`；已把判断置信度上限并入 `position_gate` 最严格上限链路；默认 live `score-daily --as-of 2026-05-08` 额外验证在 10 分钟超时内未返回，已终止残留进程，未作为通过证据|`score-daily`、decision snapshot、belief_state、回测每日评分均使用一致的置信度仓位边界；置信度调整仓位基于评分模型原始仓位计算，并作为独立 gate 参与最终仓位约束；最终仓位不得高于置信度 gate 和其他风险 gate 的最严格上限；报告能说明估值、风险、thesis、数据质量和置信度谁是当前最严格限制；测试覆盖无更严格 gate 时置信度能压低最终仓位，以及估值极端过热时估值 gate 仍可比置信度更严格|2026-05-08: 新增并进入实现，原因：运行观察发现最近日报最终仓位持续 40%-40%，主因是估值 `EXTREME_OVERHEATED` gate；但报告同时显示“置信度调整后建议仓位”34%-34%、最终仓位 40%-40%，暴露置信度展示值未参与最终仓位约束，容易造成错误投资解释。2026-05-08: 从 IN_PROGRESS 改为 DONE，原因：`DailyConfidenceAssessment` 改为评分构建时固定计算，基于评分模型原始仓位生成 `confidence` position gate，并参与最终仓位最严格上限；日报文案、系统流图和测试同步更新；验证通过 `ruff check src tests`、`pytest -q tests/test_daily_scoring.py tests/test_position_model.py tests/test_alerts.py tests/test_backtest.py` 和完整 `pytest -q` 414 项。|
