# AITradingSystem Operations Runbook

最后更新：2026-06-08

本文是 AITradingSystem 周期性任务的总入口。执行任何 daily、weekly、biweekly、monthly、governance、scheduler validation 或 artifact catalog consistency 任务前，先读取本文，再进入对应的细分 runbook、配置或报告。

本文不替代 `config/scheduled_tasks.yaml`、`config/etf_portfolio/operations_schedule.yaml`、`docs/runbooks/scheduled_task_orchestration.md`、`docs/runbook_daily_ops.md`、`docs/artifact_catalog.md` 或 `docs/system_flow.md`。其中：

- `config/scheduled_tasks.yaml` 是调度 cadence 和命令登记的配置源。
- `config/etf_portfolio/operations_schedule.yaml` 是 TRADING-074 ETF portfolio operations schedule source config，用于描述 ETF research daily / weekly / biweekly / monthly / manual-review command plan、dependencies、expected outputs 和 safety boundary；它不是外部 scheduler entry。
- `docs/runbooks/scheduled_task_orchestration.md` 说明 daily chain、非 daily cadence 隔离和 scheduler safety checklist。
- `docs/runbook_daily_ops.md` 说明人工交接、失败排查和运行归档。
- `docs/artifact_catalog.md` 说明关键 artifact 的生成者、上游、下游、production effect 和常见误解。
- `docs/system_flow.md` 仍是数据输入到结论输出路径的源-of-truth 图。

## Operating Principles

- 每日 scheduler trigger 是统一外部入口。Windows Task Scheduler、cron、GitHub Actions 或云调度器默认只应调用 `aits ops daily-run`，不要把 weekly / biweekly / monthly / governance 命令直接散落成多个未审计系统任务。
- 更长周期任务可以由同一个每日 scheduler 入口根据日期和条件触发，但必须通过受控编排实现：检查交易日、cadence due 状态、上游 daily artifacts、数据质量、production safety 和人工审批条件。
- 当前 baseline 中，`aits ops daily-run` 只执行 `daily_trading_day` 链路；weekly / biweekly / monthly / ad hoc research 任务已在 `config/scheduled_tasks.yaml` 登记，但不会自动进入 daily-run。自动 due-cadence 执行实现前，非 daily 任务由 operator 按本文手动运行或由后续受控 scheduler 执行。
- `daily_trading_day` 中允许登记轻量 scheduler gate，只用于 due/skip/block 审计和只读检查。例如 TRADING-099 的 `aits etf dynamic-v3-rescue schedule observe --as-of {as_of}` 只检查 weekly due 条件、latest pointer validation、stale 状态和可选 observe-only shadow monitor，不自动运行 `run-profile`、real sweep、promotion pack、approval、baseline mutation 或 broker action。
- TRADING-074A/B/C/D/E/F/G/H/I/J/K 的 ETF operations schedule spec、daily / weekly / biweekly / monthly graph、artifact freshness checker、failure policy evaluator、owner checklist builder、scheduler dry-run、operations health report、Reader Brief operations health section 和 final validation gate 只用于规划、校验和展示 ETF portfolio research workflow；weekly / biweekly / monthly graph 会显式披露跨 cadence 上游为 external dependencies，并标记人工复核 checkpoint；monthly graph 保证 bounded historical weight search 只在 slower cadence 计划中出现；freshness checker 只读扫描既有 artifacts，required stale/missing/unknown artifacts 标记为 blocking，optional missing artifacts 标记为 warning，并传播 upstream blocking dependency；failure policy evaluator 把 issue 转成 `info` / `warning` / `error` / `critical` severity、`fail_pipeline` / `block_dependent_steps` / `manual_review_required` 等 policy effect 和稳定 summary；owner checklist builder 从 `manual_review_steps` 生成 daily / weekly / monthly / incident owner review template，并绑定 blocking、warning、manual-review events 与 signoff；`aits etf ops dry-run --cadence ... --as-of ...` 只生成 planned steps、blocking failures、warnings、expected outputs 和 safety summary，`aits etf ops report --cadence ... --as-of ...` 只把 graph/freshness/failure/checklist/dry-run 状态写成 JSON / Markdown operations health report；Reader Brief 只读 report index 指向的 latest `etf_operations_health_report_v1`，展示 status、blocking failures、warnings、stale/missing artifacts、next owner review、safety posture 和 detail link，缺失时显示 section-level `MISSING`；`aits etf ops validate --as-of ...` 生成 `etf_operations_validation_v1` JSON / Markdown，校验 A-J 组件可用性、required missing blocker、optional missing warning、Reader Brief registry integration 和固定 safety boundary。这些路径都不执行 planned commands、不写 production state、不自动 dispatch cadence、不改变 `aits ops daily-run` 的统一入口。
- TRADING-075 data quality governance 使用 `config/etf_portfolio/data_quality.yaml` 和 `aits etf data-quality report --as-of {as_of}`，只读检查 price freshness、calendar coverage/missing bars、return outliers、config/model drift、evidence completeness、validation gate freshness、report staleness 和 Reader Brief links。ETF operations daily graph 在 report index 后、Reader Brief 前规划 `data_quality_governance_report` step；Reader Brief 只读 latest `etf_data_quality_governance_report`，缺失时显示 `MISSING`，不补跑上游。`aits etf data-quality validate --as-of {as_of}` 是 TRADING-075 final gate，使用 deterministic probes 校验 workflow availability 和 safety boundary；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`。
- TRADING-076 strategy evidence dashboard 使用 `config/etf_portfolio/evidence_dashboard.yaml` 和 `aits etf evidence-dashboard report --as-of {as_of}`，只读聚合 ETF baseline、weight calibration、forward simulation、AI confirmation / attribution、satellite replacement / attribution、parameter review、weekly review、decision journal、data quality、operations health 和 validation gates。Dashboard 输出 evidence cards、candidate ranking、conflicts、data-quality overlay 和 manual review priority queue；缺失、stale、blocked 和 optional missing source 都必须显式披露。Reader Brief 只读 latest `etf_strategy_evidence_dashboard`，缺失时显示 `MISSING`，不补跑上游。`aits etf evidence-dashboard validate --as-of {as_of}` 是 TRADING-076 final gate，校验 schema、registry、aggregator、cards、ranking、conflict overlay、manual queue、report generator、Reader Brief integration、traceability 和固定 safety boundary；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`。
- TRADING-077 baseline candidate review 使用 `config/etf_portfolio/baseline_review.yaml` 和 `aits etf baseline-review eligibility/package --candidate <candidate_id>`，只读评估 ETF allocation candidate 是否可进入 owner baseline review。Eligibility gate 必须 fail closed 阻断 strategy evidence dashboard blocked、critical data quality、ops validation failed、validation gate stale、forward sample too small、missing required journal link、parameter review blocked、unsafe production effect 和 broker action；review package、owner decision、decision journal link、proposal draft 和 outcome tracker 都是 manual-review-only artifacts。Reader Brief 只读 latest `etf_baseline_review_package` / decision / proposal / outcome，缺失时显示 `MISSING`，不补跑上游。`aits etf baseline-review validate --as-of {as_of}` 是 TRADING-077 final gate，校验 A-J workflow、owner decision requirement、proposal-only behavior、Reader Brief/report registry integration 和固定 safety boundary；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`。
- TRADING-080 weight calibration cache / parallel diagnostics 使用 `config/etf_portfolio/cache_policy.yaml`、`aits etf weight-calibration diagnostics --cache ... --workers ... --resume ... --include-performance-report` 和 `aits etf weight-calibration performance-validate`，只提升 historical weight diagnostics 的 repeatability、resume 和 runtime visibility。Diagnostics 必须先通过 ETF price quality gate；cache manifest 必须校验 safety、config hash、data hash 和 engine version；runtime cache 写入 ignored `data/cache/weight_calibration/`，不得写 production weights、baseline config、shadow registry 或 broker state。
- TRADING-081 weight calibration profiling 使用 `config/etf_portfolio/profiling_policy.yaml`、`aits etf weight-calibration diagnostics --profile off/summary/detailed/cprofile --profile-output <path> --profile-top-n N` 和 `aits etf weight-calibration profiling-validate`，只测量 cold diagnostics runtime 分布并生成 manual-review optimization evidence。Profiling report 可披露 step timing、candidate hotspots、cache read/write/serialization timing、worker balance、optional cProfile top functions、vectorization audit、regime mask precomputation assessment 和 recommendations；Reader Brief 只读 latest `etf_weight_calibration_profiling_report`，缺失时显示 `MISSING`，不运行 diagnostics/cProfile 上游。所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得注册 candidate、enroll shadow、修改 production weights、修改 baseline config、触发 broker action 或引入 native/Numba/Polars rewrite。
- TRADING-082 shadow-ready candidate review 使用 `config/etf_portfolio/shadow_ready_review.yaml`、`aits etf shadow-review package --latest --top N`、`approve`、`enroll-approved` 和 `validate`，只把 TRADING-079 diagnostics 中的 `shadow_ready` stable shapes 转成 owner-reviewed forward shadow observation candidates。Package 只读 diagnostics JSON、stable shapes CSV 和 near-shadow CSV；approval 是 enrollment 前置条件；`enroll-approved` 只生成 forward tracking link，不写 production weights、baseline config、shared experiment shadow registry 或 broker state。Reader Brief 只读 latest shadow-review artifacts，缺失时显示 `MISSING`；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`。
- TRADING-083 trend signal weight calibration 使用 `config/etf_portfolio/trend_calibration.yaml`、`aits etf trend-calibration run --start YYYY-MM-DD --end YYYY-MM-DD`、`report --latest` 和 `validate`，只校准 trend-analysis information weights，不输出 ETF target weights。Run 命令必须先通过 `aits validate-data` 等价 cached market / macro data quality gate，并在 dataset / report 中披露 data quality status、market regime 和 requested / resolved date range；forward returns / drawdowns 固定 `evaluation_only=true`，只能用于 attribution/redundancy/regime stability。Reader Brief 只读 latest `etf_trend_calibration_report`，缺失时显示 `MISSING`；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`。
- TRADING-084 dynamic allocation policy 使用 `config/etf_portfolio/dynamic_allocation_policy.yaml`、`aits etf dynamic-allocation decide --date {as_of}`、`report --latest` 和 `validate`，只把 trend/regime/risk scores 映射为 candidate dynamic allocation decision records。`candidate_target_weights` 只用于人工复核和后续 TRADING-085/086 输入，不写 official `data/etf_portfolio/target_weights.csv`、不改 baseline config、不触发 broker。Reader Brief 只读 latest `etf_dynamic_allocation_report`，缺失时显示 `MISSING`；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`official_target_weights_mutated=false`。
- TRADING-085 dynamic calibration batch/cache 使用 `config/etf_portfolio/dynamic_calibration.yaml`、`aits etf dynamic-calibration run --pack ... --cache ... --workers ...`、`report --latest` 和 `validate`，只把 TRADING-083 signal configs 与 TRADING-084 allocation profiles 组合为 candidate packs、cache payloads 和 ranking report。`dynamic_backtest` cache 是 calibration proxy，固定 `full_robustness_backtest_required=true`，不替代 TRADING-086 robustness；Reader Brief 只读 latest `etf_dynamic_calibration_report`，缺失时显示 `MISSING`；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`。
- TRADING-086 dynamic robustness review 使用 `config/etf_portfolio/dynamic_robustness.yaml`、`aits etf dynamic-robustness report --candidate <candidate_id>`、`report --latest` 和 `validate`，只用真实 ETF price returns 评估 dynamic candidate 是否优于 static alternatives。`report` 必须先运行 `aits validate-data` 等价 cached market / macro data quality gate 并在输出披露 `validate_data_status` / report path；通过后比较 dynamic candidate、static base、current baseline、QQQ/SPY/SMH buy-and-hold 和 best static historical candidate，并输出 walk-forward、regime attribution、false risk-on/off、turnover sensitivity、AI/semiconductor、event risk overlay 和 overfit diagnostics。Reader Brief 只读 latest `etf_dynamic_robustness_report`，缺失时显示 `MISSING`；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`。该报告只是 TRADING-087 owner-approved dynamic shadow review 前置证据，不自动 enroll、不写 production weights、不触发 broker。
- TRADING-087 dynamic shadow workflow 使用 `config/etf_portfolio/dynamic_shadow.yaml`、`aits etf dynamic-shadow package --latest --top N`、`approve`、`enroll-approved`、`update`、`weekly-review` 和 `validate`，只把 owner-approved dynamic candidates 纳入 forward shadow observation。Package 只读 TRADING-086/085 evidence 和 validation artifacts；approval 必须记录 owner rationale 和 decision journal link；enrollment 只接受 `approved_for_dynamic_shadow` 且无 hard blockers 的 approval；`update` 必须先运行 `aits validate-data` 等价 cached market / macro data quality gate 并披露 status/report path；weekly review 只输出 active_shadow / needs_more_data / watch / reject_pending_review / rejected / archived 和 manual review actions。Reader Brief 只读 latest dynamic shadow artifacts，Strategy Evidence Dashboard 只读 latest weekly review；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`。Forward shadow observation 不代表 production approval、baseline replacement、broker order 或 automatic promotion。
- TRADING-088 dynamic failure diagnostics / rescue workflow 使用 `config/etf_portfolio/dynamic_failure_diagnostics.yaml`、`aits etf dynamic-rescue run --base-candidate <candidate_id>` 或 `--latest-failed-package`、`report --latest` 和 `validate`，只诊断 failed dynamic v0.1 并生成 bounded candidate-only v0.2 rescue evidence。`run` 必须先运行 `aits validate-data` 等价 cached market / macro data quality gate 并披露 status/report path；通过后读取 failed dynamic robustness report 和 optional dynamic shadow package，输出 failure dataset、Layer 1 / Layer 2 attribution、false signal attribution、turnover/constraint breakdown、rescue templates、candidate comparison 和 evaluation report。Reader Brief 只读 latest `etf_dynamic_rescue_evaluation_report`，缺失时显示 `MISSING`；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`。Rescue candidate found 不代表 approval、shadow enrollment、baseline replacement、broker order 或 automatic promotion。
- TRADING-090 dynamic v0.3 constraint-aware rescue workflow 使用 `config/etf_portfolio/dynamic_v3_constraint_aware_rescue.yaml`、`aits etf dynamic-v3-rescue run --latest-v2-review` 或 `--v2-review-package <path>`、`report --latest` 和 `validate`，只读取 TRADING-089 v0.4 review package 并生成 candidate-only v0.3 rescue evaluation evidence。`run` 不直接读取 cached market / macro data，不替代 TRADING-086/088 的数据质量门禁；它必须保留 source links 中的 dynamic rescue / robustness / data quality evidence，并在输出中披露 review-only / candidate-only safety boundary。Reader Brief 只读 latest `etf_dynamic_v3_rescue_evaluation_report`，缺失时显示 `MISSING`；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`。v0.3 rescue success 不代表 approval、shadow enrollment、baseline replacement、broker order 或 automatic promotion。
- TRADING-091 dynamic v0.3 rescue real evaluation workflow 使用 `config/etf_portfolio/dynamic_v3_real_evaluation.yaml`、`aits etf dynamic-v3-rescue real-evaluate`、`real-report --latest` 和 `validate-real`，是 owner-requested ad-hoc 复核，不是 daily / weekly / monthly scheduler entry。`real-evaluate` 必须先运行 `aits validate-data` 等价 cached market / macro data quality gate，随后用真实 ETF price-driven robustness path 比较 v0.3 vs baseline / v0.2 / v0.4 / static / current / QQQ / SPY / SMH，并输出 constraint hit、false risk-off、drawdown preservation、turnover、static gap、overfit 和 `promote_candidate` / `observe_only` / `reject` gate。Gate label 只表示人工复核候选资格；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`，不得 approval、enroll、mutate baseline/production 或触发 broker。
- TRADING-092 dynamic v0.3 failure attribution and v0.4 promotion review workflow 使用 `config/etf_portfolio/dynamic_v3_failure_attribution.yaml`、`aits etf dynamic-v3-rescue failure-attribution`、`failure-attribution-report --latest` 和 `validate-attribution`，是 owner-requested ad-hoc 复核，不是 daily / weekly / monthly scheduler entry。`failure-attribution` 只读 latest TRADING-091 real evaluation reject report，必须先运行 `aits validate-data` 等价 cached market / macro data quality gate，随后从 source configs 和真实 ETF price cache 重建 v0.3 / v0.4 robustness daily paths，输出 reject attribution、v0.3 vs v0.4 metric delta、constraint hit reason/regime/ticker/rebalance-window bucket、drawdown attribution、overfit `REVIEW_REQUIRED` explanation、v0.4 promotion review 和 v0.5 design recommendation。Review / recommendation label 只表示人工复核优先级；所有输出固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`、`shadow_enrollment_allowed=false`、`automatic_enrollment_allowed=false`、`owner_approval_executed=false`，不得 approval、enroll、mutate baseline/production 或触发 broker。
- TRADING-093 到 TRADING-110 dynamic v3 rescue parameter research platform 使用 `config/etf_portfolio/dynamic_v3_rescue/parameter_sweep_v1.yaml`、`config/etf_portfolio/dynamic_v3_rescue/parameter_sweep_real_smoke.yaml`、`config/etf_portfolio/dynamic_v3_rescue/parameter_sweep_profiles.yaml`、`config/etf_portfolio/dynamic_v3_rescue/parameter_governance_v1.yaml`、`aits etf dynamic-v3-rescue data-audit run/report`、`sweep-config validate/preview`、`sweep profile-list/profile-validate/run-profile`、`sweep run/status/validate/leaderboard/report`、`injection-audit run/report`、`candidate report/attribution`、`walk-forward run/report/select-run/selection-report`、`robustness run/report`、`overfit run/report`、`governance validate/report/diff`、`research index-build/query/compare/history`、`shadow register/list/report/monitor-run/monitor-report`、`artifacts latest/validate/stale`、`schedule observe`、`promotion review/pack` 和对应 validate 命令。这是 owner-requested ad-hoc / weekly manual research workflow；daily scheduler 只允许调用 lightweight `schedule observe` gate，研究链本体不是 daily scheduler entry。`schedule observe` 在 daily-run 中只写 `reports/etf_portfolio/dynamic_v3_rescue/schedule_observe/dynamic_v3_rescue_schedule_observe_YYYY-MM-DD.json/md`，记录 closed-market/not-due/no-pointer/broken-pointer/stale/shadow-monitor 状态；small_real 必须按 data audit -> run-profile -> injection audit -> leaderboard -> candidate report -> attribution -> walk-forward selection -> overfit -> shadow register -> shadow monitor -> promotion pack 顺序手动或受控运行。Evaluator mode 只允许 `tiny_fixture_proxy` 或 `real_dynamic_v3_rescue`；CI/focused tests 默认 tiny fixture，manual research run 使用 `small_real` / `medium_real` / `overnight_real` profile。Real mode 必须先运行 cached data quality gate，candidate metrics 必须来自 linked real evaluation artifact，`validate-sweep` 会检查 real artifact path；tiny mode 必须标记 `not_for_investment_decision=true` 且 promotion pack 不得进入 `promote_candidate`。所有阈值和 search-space governance 来自 YAML；large sweep 不进入 CI。Latest pointer 只指向历史 artifact，不复制或覆盖历史报告；`artifacts validate` 对空 pointer 和 broken path fail closed，retention policy 只文档化和校验，不删除文件。本 workflow 固定 `production_effect=none`、`broker_action=none`、`production_candidate_generated=false`，自动命令不得生成 `production_candidate`、approval、shadow enrollment、baseline mutation、official target weights mutation 或 broker action。
- TRADING-111 到 TRADING-113 real research evidence closure 是上述 dynamic v3 rescue research workflow 的 manual evidence gate，不是 daily scheduler entry。真实 small_real / medium_real / overnight_real 研究解释 promotion pack 前，必须使用 `aits etf dynamic-v3-rescue data-provenance inspect-price-cache` 或 data audit 内置 provenance report 确认可审计 price cache checksum / manifest coverage，必要时用 `repair-price-manifest` 明确生成 `RECONSTRUCTED_MANIFEST`；必须运行 `window-audit run` / `validate-window-audit` 确认 configured/requested/actual backtest window 没有 promotion-blocking truncation；必须用 `weight-path validate/report` 和 `candidate attribution` 检查 per-candidate daily weight path completeness。`promotion pack` 的 `evidence_summary.json` 是最终人工复核入口，任一 window、weight path、candidate attribution 或 data provenance gate 不完整时不得进入 `promote_candidate`。
- TRADING-114 到 TRADING-120 medium_real candidate discovery and regime coverage 是上述 dynamic v3 rescue research workflow 的 weekly/manual 研究包，不是 daily scheduler entry，也不自动启动 `overnight_real`。`evidence-summary`、`medium-real report`、`regime-coverage`、`candidate interpretation-pack`、`observe-pool`、`overnight-readiness` 和 `research-decision` 只读或写 ignored research artifacts；observe pool 默认不写 shadow registry，overnight readiness 只评估 runtime/disk/failure/evidence 风险，research decision 只给 owner-readable next task。Reader Brief 只读 latest `etf_dynamic_v3_evidence_summary`、`etf_dynamic_v3_observe_pool`、`etf_dynamic_v3_overnight_readiness` 和 `etf_dynamic_v3_research_decision`，缺失时显示 `MISSING`；所有输出固定 `production_effect=none`、`broker_action=none`、`manual_review_required=true`，不得 approval、enroll shadow、写 official target weights、改 baseline/production state、触发 broker 或生成 `production_candidate`。
- TRADING-121 到 TRADING-125 evidence gate diagnosis and candidate usability recovery 是 TRADING-114 到 TRADING-120 后的 weekly/manual 研究复核包，不是 daily scheduler entry，也不自动启动 `overnight_real`。`evidence-diagnosis`、`gate-impact`、`gate-policy`、`candidate-recovery`、`observe-pool rebuild` 和 `research-decision update` 只读或写 ignored research artifacts；`config/etf_portfolio/dynamic_v3_rescue/evidence_gate_policy_v1.yaml` 必须保留 owner/version/status/rationale/intended effect/review condition。该 workflow 只允许把 policy 明确允许的 soft blockers 降级为 observe-only/manual-review，true hard failures 仍 fail closed；Reader Brief 只读 latest diagnosis/impact/policy/recovery/observe-pool/decision-update artifacts，缺失时显示 `MISSING`。所有输出固定 `production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_candidate_generated=false`，不得 approval、自动 shadow enrollment、写 official target weights、改 baseline/production state、触发 broker 或把 recovered candidates 解读为 promotion/production candidates。
- TRADING-126 到 TRADING-130 shadow shortlist and position advisory readiness 是 TRADING-121 到 TRADING-125 后的 weekly/manual 研究复核包，不是 daily scheduler entry，也不自动写 shadow registry 或 current portfolio state。`shortlist`、`candidate-cluster`、`shadow-shortlist`、`position-advisory` 和 `position-review` 只读或写 ignored research artifacts；`config/etf_portfolio/dynamic_v3_rescue/position_advisory_v1.yaml` 必须保留 owner/version/status/rationale/intended effect/review condition，`current_portfolio_snapshot.example.yaml` 只是人工 snapshot 格式样例。该 workflow 只允许把 rebuilt observe pool 压缩成可人工复核的 shadow monitoring shortlist，并生成 TARGET_ONLY 或 snapshot-delta advisory；Reader Brief 只读 latest shortlist/cluster/shadow shortlist/advisory/review artifacts，缺失时显示 `MISSING`。所有输出固定 `production_effect=none`、`broker_action=none`、`broker_action_allowed=false`、`owner_approval_required=true`、`manual_review_required=true`、`production_candidate_generated=false`、`production_readiness=NOT_READY`，不得 approval、自动 shadow enrollment、写 official target weights、改 baseline/production state、触发 broker 或把 position advisory 解读为可执行交易指令。
- 所有依赖 cached market / macro data 的评分、回测、技术特征、报告或治理任务必须先通过 `aits validate-data` 或同一路径的数据质量门禁，并在下游输出中可见。
- 默认市场 regime 为 `ai_after_chatgpt`，anchor event 为 ChatGPT public launch on 2022-11-30，默认回测结论窗口从 2022-12-01 开始。任何回测、weekly review 或 monthly governance 报告都必须写明实际日期范围。
- Reader Brief、dashboard、report index、documentation contract、research governance、SEC PIT shadow monitor、weight feedback 和 scheduler validation 默认 `production_effect=none`。它们不得写 production weights、active shadow weights、broker action 或 trading action。

## Unified Daily Trigger

标准外部调度入口：

```powershell
aits ops daily-run
```

人工预检入口：

```powershell
aits ops daily-plan --fail-on-missing-env
```

指定评估日时使用：

```powershell
aits ops daily-run --as-of YYYY-MM-DD
```

`daily-run` 面向最新已完成的 U.S. equity market trading day。历史严格复现使用 `aits ops replay-day --mode cache-only --as-of YYYY-MM-DD`，不要用生产 daily scheduler 入口补造历史 PIT 视图。

## Daily Tasks

交易日 daily chain 至少覆盖以下必需任务；完整顺序以 `config/scheduled_tasks.yaml` 的 `daily_trading_day` 为准。

|类别|任务|命令或检查|阻断规则|
|---|---|---|---|
|Daily PIT|抓取 forward-only PIT snapshots|`aits pit-snapshots fetch-fmp-forward --as-of {as_of} --continue-on-failure`|失败快照不得作为可用 PIT 输入；失败必须进入报告或 `ops health`。|
|Daily PIT|刷新 PIT manifest|`aits pit-snapshots build-manifest --as-of {as_of}`|manifest 缺失、checksum 或 schema 异常时下游不得静默继续。|
|Daily PIT|验证 PIT manifest|`aits pit-snapshots validate --as-of {as_of}`|严重错误必须 fail closed。|
|Daily scoring|生成每日评分和决策 artifact|`aits score-daily --as-of {as_of}`|必须先通过 `validate-data`；latest production daily-run 会向 `score-daily` 注入风险事件 OpenAI `visibility_cutoff`，历史 as-of 或无 cutoff 的 OpenAI request timestamp 仍按 as-of 当日 UTC 末尾 fail closed；失败时停止 Reader Brief、dashboard/latest 和下游报告。|
|Dashboard/latest checks|生成 evidence dashboard|`aits reports dashboard --as-of {as_of}`|只读，不替代 `daily_score` 和 trace audit。|
|Dashboard/latest checks|刷新 report index|`aits reports index --latest`|只读扫描既有报告；`STALE` / `MISSING` 需要进入人工复核。|
|Dashboard/latest checks|生成 ETF data quality governance report|`aits etf data-quality report --as-of {as_of}`|在 Reader Brief 前检查 ETF price/artifact/gate/report/link staleness；critical required findings 阻断 dependent research interpretation，optional missing 只 warning。|
|Dashboard/latest checks|刷新 documentation contract|`aits docs report-contract --latest`|用于检查 registry 与 artifact catalog 文档覆盖。|
|Dashboard/latest checks|刷新 research governance summary|`aits reports research-governance-summary --latest`|必须在 Reader Brief 前完成；缺失时 Reader Brief 只能披露受限上下文。|
|Shadow candidate tracking|生成 market data freshness readiness|`aits data freshness --latest`|只读检查 tracking readiness；stale/missing freshness 不得静默进入 tracking review。|
|Shadow candidate tracking|执行受控 freshness recovery|`aits data recover-freshness --latest`|失败时显式阻断；不得伪造价格、manifest 或 tracking days。|
|Shadow candidate tracking|滚动 active candidate tracking|`aits portfolio track-candidate --latest`|只写 shadow tracking artifact；不启用 candidate、不修改 production。|
|Shadow candidate tracking|生成 tracking window review|`aits portfolio review-tracking --latest --show-window-progress`|`tracking_days<5` 必须保持 `needs_more_data`；这是 VALIDATING 正常状态，不是 scheduler 失败。|
|Shadow candidate tracking|生成 tracking review report alias|`aits reports portfolio-tracking-review --latest`|只读读取 review artifact，供 Dashboard / Reader Brief 展示。|
|Reader Brief|生成读者入口|`aits reports reader-brief --latest`|只读消费既有 artifact，不重跑 scoring、backtest、SEC PIT、shadow、weight 或 docs 上游。|
|Reader Brief|校验读者入口质量|`aits reports validate-reader-brief --latest`|质量失败或上下文受限必须在后续输出中可见。|

Daily chain 还包括 `download-data`、`validate-data`、SEC companyfacts / metrics、valuation fetch、SEC PIT shadow observe / monitor、score change attribution、market panel、Dynamic v3 rescue `schedule observe` gate、`ops health` 和 secret scan。`aits ops daily-run` 在 finalization 写出 current-run `daily_decision_summary` 和 `daily_task_dashboard` 后，会用同一 run 的 summary 只读刷新最终 Reader Brief / Reader Brief quality mirror；该刷新不得重跑 scoring、PIT、SEC、valuation、backtest、shadow、weight 或 docs 上游。休市日模式下，系统不得生成新的 score、decision snapshot、Reader Brief scoring artifacts、tracking review artifacts、prediction ledger 行或执行动作；Dynamic v3 rescue gate 只写 closed-market / not-due / due 审计。

## Periodic Task Register

以下任务登记为周期性运营职责。除非后续受控 scheduler 明确实现 due-cadence dispatch，否则它们不会由 `aits ops daily-run` 自动执行。

|Cadence|登记任务|默认入口|Due 条件|验收重点|
|---|---|---|---|---|
|Weekly|Weekly review|`aits etf weekly-review generate --as-of {as_of}`、`aits etf weekly-review validate`、`aits backtest --regime ai_after_chatgpt`、`aits backtest --regime ai_after_chatgpt --robustness-report`、`aits feedback build-parameter-replay --as-of {as_of}`、`aits feedback build-parameter-candidates --as-of {as_of}`、`aits feedback evaluate-parameter-governance --as-of {as_of}`、`aits reports research-governance-summary --latest`|每周最后一个已完成美股交易日之后，且 daily chain PASS 或明确披露限制。|ETF weekly review 只读 existing artifacts，并固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；报告声明 regime/date range、source artifacts、missing data 和 validation status；backtest/robustness/parameter governance 不得直接改变 production。|
|Weekly|Portfolio decision journal|`aits etf decision-journal add/update/list/remove`、`aits etf decision-journal report --as-of {as_of}`、`aits etf decision-journal analytics --as-of {as_of}`、`aits etf decision-journal propose-state-updates --as-of {as_of}`、`aits etf decision-journal validate`|TRADING-068 weekly review 已生成且 reviewer 已完成人工判断后；也可在下一次 weekly review 前刷新 report。|Journal entries 必须链接 source weekly review 和 action item；report/proposal/validation 固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；proposal 不修改 shadow registry、production weights 或 broker state。|
|Weekly|ETF parameter review evidence|`aits etf parameter-review aggregate --as-of {as_of}`、`aits etf parameter-review report --as-of {as_of}`、`aits etf parameter-review validate`|TRADING-065 forward dashboard、TRADING-068 weekly review 和 TRADING-069 decision journal report 已存在后；缺少 forward dashboard 或 forward candidate rows 时输出 `needs_more_data`。|Aggregation/report 只读 latest evidence sources，保留 source paths、missing source warnings、proposal scorecard 和 safety banner；validation gate 确认 proposal-only workflow、Reader Brief visibility、source links 和 unsafe action blockers；Reader Brief 只读最新 report 摘要；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 baseline config、production weights、shadow registry 或 broker state。|
|Weekly / Manual|AI confirmation forward attribution review|`aits etf ai-attribution build --as-of {as_of}`、`aits etf ai-attribution report --as-of {as_of}`、`aits etf ai-attribution validate`|AI confirmation reports 和 ETF price cache 已存在后；用于复核 `AIConfirmationScore` 与 component scores 是否对 forward ETF / semiconductor / satellite outcomes 具备 attribution value；样本不足时输出 `needs_more_data`。|Build/report 必须先通过 ETF price quality gate；dataset row 固定 `evaluation_only=true`，forward returns 只能用于 attribution/evaluation；report 必须披露 `ai_after_chatgpt` regime、requested date range、source report paths、sample size、data quality status、bucket/component/regime/event/redundancy analysis、evidence scorecard 和 manual review recommendation；validation gate 确认 workflow modules、Reader Brief/report registry visibility、evaluation-only separation、forbidden production/trading output keys 和 safety fields；Reader Brief 只读 latest attribution report，不运行上游；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 production weights、不得自动 promotion、不得触发 broker action。|
|Weekly / Manual|Satellite replacement forward attribution review|`aits etf satellite-attribution build --as-of {as_of}`、`aits etf satellite-attribution report --as-of {as_of}`、`aits etf satellite-attribution validate`|Satellite replacement reports、可选 AI confirmation reports 和 ETF/satellite price cache 已存在后；用于复核 eligible satellite stocks 是否跑赢 benchmark ETF、fallback_to_etf 是否保护 downside、SatelliteCandidateScore 是否有 ranking power，以及 role/group/risk/AI interaction attribution；样本不足时输出 `needs_more_data`。|Build/report 必须先通过 ETF price quality gate；dataset row 固定 `evaluation_only=true`，forward returns 只能用于 attribution/evaluation；report 必须披露 `ai_after_chatgpt` regime、requested date range、source report paths、sample size、data quality status、eligibility bucket、stock-vs-ETF、fallback、score、risk、role/group、AI interaction analysis、evidence scorecard 和 manual review recommendation；validation gate 确认 workflow modules、Reader Brief/report registry visibility、evaluation-only separation、forbidden production/trading output keys 和 safety fields；Reader Brief 只读 latest attribution report，不运行上游；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 production weights、不得自动 promotion、不得触发 broker action。|
|Weekly / Manual|ETF weight calibration dual-track setup|`aits etf weight-calibration search --search etf_initial_weight_search_v1 --preset ai_cycle_recent` 或 `--preset last_3y`、`aits etf weight-calibration export-top --latest --top N`、`aits etf weight-calibration comparison --latest --top N`、`aits etf weight-calibration regime-robustness --latest --top N`、`aits etf weight-calibration register-candidates --latest --top N`、`aits etf weight-calibration enroll-forward --latest --top N`、`aits etf weight-calibration aggregate-evidence --as-of {as_of}`、`aits etf weight-calibration overfit-diagnostics`、`aits etf weight-calibration generate-proposals`、`aits etf weight-calibration report --latest`、`aits etf weight-calibration validate`|需要重新生成 historical candidate initial weights、导出可读 Top-N shortlist、生成 candidate/benchmark comparison table、生成 regime robustness heatmap data、把已登记 safe candidates 显式放入 forward observation、比较 historical expectation vs forward evidence、生成 overfit risk bands、生成 evidence-linked proposal-only recommendations、汇总 dual-track manual review package，或确认 TRADING-071 workflow safe/complete 时。|TRADING-078A presets 位于 `config/etf_portfolio/weight_calibration_presets.yaml`，search 可用 `last_2y`、`last_3y`、`last_5y`、`post_2022_bear`、`ai_cycle_recent` 或 `full_available` 解析 historical requested date range，并在 payload 披露 resolved preset metadata；TRADING-078B `export-top` 写出 Top-N JSON / CSV / Markdown，包含 historical metrics、benchmark comparison、overfit risk、forward readiness、blockers/warnings 和 safety fields；TRADING-078C `comparison` 写出 current baseline、Buy & Hold QQQ/SPY/SMH、static references 和 Top-N candidates comparison table，缺失 metric 保留 null + reason；TRADING-078D `regime-robustness` 写出 candidate/regime heatmap matrix，缺失 regime slice 保留 `MISSING` 与 confidence warning；search 必须先通过 ETF price quality gate，并披露 `ai_after_chatgpt` regime、requested date range、baseline/benchmark comparison、robustness metrics 和 safety fields；register/enroll/aggregate/diagnostics/proposals/report/validate 只写 ignored `data/etf_portfolio/weight_calibration/` 和 `reports/etf_portfolio/weight_calibration/` runtime artifacts，blocked/rejected/needs_more_data candidates 不得 enroll；缺少 forward row 或 forward days 不足时 evidence status 必须是 `needs_more_forward_data`，不得补造结论；overfit diagnostics 只给 low/medium/high/critical risk band；proposal types 只允许 `continue_forward_observation`、`reject_weight_set`、`defer_until_more_forward_data`、`propose_extended_shadow` 和 `propose_manual_baseline_review`，不得 apply/promote/enable broker；report 只汇总 source links、proposal scorecard 和 manual review package；validation gate 必须确认 config bounded、workflow modules available、Reader Brief/report registry visibility、unsafe proposal type blocking、evidence-linked proposals 和 safety fields，失败时 fail closed；Reader Brief 只读 latest `etf_weight_dual_track_calibration_report` 摘要，不运行上游；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 `target_weights.csv`、baseline config、shared experiment shadow registry 或 broker state。|
|Manual / Governance|ETF weight calibration cache / parallel diagnostics validation|`aits etf weight-calibration diagnostics --include-robust-packs --cache read-write --workers auto --include-performance-report`、同输入 warm rerun、可选 `--resume --run-id <run_id>`、`aits etf weight-calibration performance-validate`|变更 TRADING-080 cache policy、cache key/manifest、diagnostics runner、parallel worker、resume manifest、performance report 或 CLI cache flags 后；也可在默认 ETF price cache 补齐后手动观察 production-like cold/warm 表现。|Diagnostics 运行必须先通过 ETF price quality gate 并披露 `ai_after_chatgpt` regime、requested date range、data quality status、cache mode、worker count、cache hit/miss/write count、price/returns matrix status、diagnostics aggregation status、resume status 和 performance report path；cache entry / run manifest / validation output 必须保留 safety fields、source_config_hash、data_hash 和 engine_version；fixture 验证只可在默认 price cache 缺失时使用，且必须记录原因和独立 ETF data validation artifact；`performance-validate` fail closed 校验 policy/key/manifest/price/candidate/regime/aggregation/parallel/resume/performance/safety；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`，不得把 cache hit 当作跳过 data gate、放宽 overfit/shadow gate、注册 candidate、enroll shadow、修改 production weights 或触发 broker action。|
|Manual / Governance|ETF weight calibration profiling baseline|`aits etf weight-calibration diagnostics --include-robust-packs --cache read-write --workers auto --profile summary`、需要 candidate/function 热点时用 `--profile detailed` 或 `--profile cprofile`、`aits etf weight-calibration profiling-validate`|TRADING-080 cold run 进入 20-30 minute profiling trigger zone 后；变更 profiling policy、diagnostics timing instrumentation、hotspot report、Reader Brief profiling section 或准备讨论 TRADING-082 numerical optimization 前。|Diagnostics 必须先通过 ETF price quality gate 并披露 `ai_after_chatgpt` regime、requested date range、data quality status 和 profiling mode；profiling report 必须包含 safety banner、step timing、cache timing、worker timing、candidate hotspot table（detailed/cprofile）、optional cProfile summary、vectorization audit、regime mask assessment、optimization recommendations 和 detail path；Reader Brief 只读 latest profiling report，不补跑上游；`profiling-validate` fail closed 校验 workflow 和 safety；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`，不得把 recommendation 解读为 production approval、candidate promotion、broker action 或 native rewrite approval。|
|Weekly / Manual|Shadow-ready candidate review|`aits etf shadow-review package --latest --top N`、`aits etf shadow-review approve --package <package.json> --shape <shape_id>`、`aits etf shadow-review enroll-approved --approval <approval.json> --package <package.json>`、`aits etf shadow-review validate`|TRADING-079 diagnostics 已生成 stable shapes / near-shadow artifacts，owner 需要决定哪些 shadow-ready historical weight shapes 进入 forward shadow observation 时。|Package 只读 diagnostics artifacts，不运行 upstream diagnostics、不补造 source links；ranking 不能只按 historical return，必须披露 shadow_ready appearances、cross-preset stability、rank consistency、shape similarity、regime failures、overfit distribution 和 exposure balance；`approve` 必须捕获 rationale / confidence / selected weight_set_id；`enroll-approved` 只接受 `approved_for_shadow`，non-approved decision fail closed；Reader Brief 只读 latest package/approval/enrollment；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`，不得 auto-enroll、auto-promote、写 production weights、改 baseline config 或触发 broker。|
|Weekly / Manual|Trend signal weight calibration|`aits etf trend-calibration run --start YYYY-MM-DD --end YYYY-MM-DD`、`aits etf trend-calibration report --latest`、`aits etf trend-calibration validate`|TRADING-084 dynamic allocation policy calibration 前；trend signal registry、score bands、forward attribution policy 或 AI/regime/risk signal sources 变更后；也可在 owner 需要复核 Layer 1 trend score evidence 时手动运行。|Run 必须先通过 `aits validate-data` 等价 cached market / macro data gate，失败即停止；报告必须披露 `ai_after_chatgpt` regime、requested / resolved date range、data quality status、top candidate signal configs、bucket forward attribution、redundancy risk、regime stability 和 source links；forward return / drawdown 字段固定 `evaluation_only=true`，只能支持 attribution/evaluation；Reader Brief 只读 latest report，不运行上游；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得输出 `target_weights`、production weights、baseline config mutation、automatic promotion 或 broker action。|
|Weekly / Manual|Dynamic allocation candidate policy review|`aits etf dynamic-allocation decide --date {as_of} --score-profile neutral`、`aits etf dynamic-allocation report --latest`、`aits etf dynamic-allocation validate`|TRADING-085 two-layer dynamic candidate batch/cache 前；dynamic allocation policy、regime targets、trend/event overlay、exposure/turnover constraints 或 rebalance gates 变更后；也可在 owner 需要复核 Layer 2 candidate target weights 时手动运行。|Decision/report 只生成 candidate allocation artifacts，必须披露 selected regime、candidate target weights、previous weights、rebalance decision、constraints、reason codes、source trend report 和 safety；Reader Brief 只读 latest report，不运行上游；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`；不得写 official `target_weights.csv`、baseline config、broker action、automatic promotion 或 shadow enrollment。|
|Weekly / Manual|Dynamic calibration batch/cache review|`aits etf dynamic-calibration run --pack dynamic_etf_v1 --cache read-write --workers auto`、`aits etf dynamic-calibration report --latest`、`aits etf dynamic-calibration validate`|TRADING-086 dynamic robustness review 前；trend signal config registry、dynamic allocation policy、candidate pack schema、cache key schema、ranking policy 或 calibration proxy scales 变更后；也可在 owner 需要复核 two-layer candidate shortlist 时手动运行。|Run 只生成 candidate packs、cache payloads 和 calibration proxy ranking report；必须披露 source trend report、data quality status、cache hit/miss/write、worker count、top candidate packs、ranking components、blockers 和 `full_robustness_backtest_required=true`；Reader Brief 只读 latest report，不运行上游；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`、`production_state_mutated=false`、`baseline_config_mutated=false`、`official_target_weights_mutated=false`、`automatic_candidate_promotion=false`、`auto_enrollment_without_owner_approval=false`；不得把 calibration proxy 当作 TRADING-086 robustness 结论，不得写 official target weights、baseline config、broker action、automatic promotion 或 shadow enrollment。|
|Weekly / Manual|Dynamic shadow review and observation|`aits etf dynamic-shadow package --latest --top 3`、`aits etf dynamic-shadow approve --package <package.json> --candidate <candidate_id> --rationale <text> --decision-journal-link <id>`、`aits etf dynamic-shadow enroll-approved --latest`、`aits etf dynamic-shadow update --latest`、`aits etf dynamic-shadow weekly-review --latest`、`aits etf dynamic-shadow validate`|TRADING-086 robustness report 和 validation artifacts 已存在后；owner 要决定 dynamic candidate 是否进入 forward shadow observation 时；approved candidate 进入观察后每日/每周手动或受控 cadence 刷新 tracking。|Package/approval/enrollment/update/weekly-review 均固定 observe-only/candidate-only；enrollment 必须有 owner approval，未批准或 gate 缺失时 fail closed；update 必须先通过 `aits validate-data` 等价门禁并披露 data quality status；weekly review 只输出 manual review statuses/actions；不得写 official target weights、baseline config、broker state、automatic promotion 或 auto enrollment。|
|Weekly / Manual|Dynamic failure diagnostics and rescue review|`aits etf dynamic-rescue run --latest-failed-package` 或 `aits etf dynamic-rescue run --base-candidate <candidate_id>`、`aits etf dynamic-rescue report --latest`、`aits etf dynamic-rescue validate`|Dynamic v0.1 failed robustness/shadow evidence 已存在后；owner 需要知道失败来自 Layer 1 signal、Layer 2 allocation、false signal、turnover 还是 constraint hits，并判断 bounded v0.2 rescue 是否值得进入 TRADING-089。|Run 必须先通过 `aits validate-data` 等价门禁并披露 data quality status；rescue templates 必须来自 policy config、evidence-linked、candidate-only、bounded；报告只输出 diagnostics/evaluation，不执行 approval、不 enroll shadow、不写 official target weights、不改 baseline config、不触发 broker；validate fail-closed 校验 A-K workflow 和 safety。|
|Manual|Dynamic v0.3 failure attribution and v0.4 review|`aits etf dynamic-v3-rescue failure-attribution` 或 `aits etf dynamic-v3-rescue failure-attribution --real-evaluation-report <path>`、`aits etf dynamic-v3-rescue failure-attribution-report --latest`、`aits etf dynamic-v3-rescue validate-attribution`|TRADING-091 latest real evaluation 已输出 `reject` 后；owner 需要判断 v0.3 reject 归因、v0.4 是否作为 rescue 主路径继续加 constraint guard，或是否进入 v0.5 exposure redesign。|Run 必须先通过 `aits validate-data` 等价门禁并披露 data quality status；daily paths 从 source configs 和 price cache 重建，不从截断 report 补造；报告只输出 failure attribution / promotion-review / design-recommendation evidence；不得 approval、enroll shadow、写 official target weights、改 baseline config、触发 broker 或自动 promotion。|
|Daily Gate + Weekly / Manual|Dynamic v3 rescue parameter research and promotion review|Daily gate: `aits etf dynamic-v3-rescue schedule observe --as-of YYYY-MM-DD`。Manual research: `aits etf dynamic-v3-rescue data-audit run --as-of YYYY-MM-DD --end YYYY-MM-DD`、`aits etf dynamic-v3-rescue sweep profile-validate`、`aits etf dynamic-v3-rescue sweep run-profile --profile small_real --as-of YYYY-MM-DD --end YYYY-MM-DD --workers 1`、`aits etf dynamic-v3-rescue injection-audit run --as-of YYYY-MM-DD --end YYYY-MM-DD --max-candidates N`、`aits etf dynamic-v3-rescue sweep leaderboard --sweep-id <sweep_id>`、`aits etf dynamic-v3-rescue candidate report --sweep-id <sweep_id> --candidate-id <candidate_id>`、`aits etf dynamic-v3-rescue candidate attribution --sweep-id <sweep_id> --candidate-id <candidate_id>`、`aits etf dynamic-v3-rescue walk-forward select-run --sweep-id <sweep_id> --profile small_real`、`aits etf dynamic-v3-rescue overfit run --sweep-id <sweep_id> --candidate-id <candidate_id>`、`aits etf dynamic-v3-rescue shadow register --sweep-id <sweep_id> --candidate-id <candidate_id>`、`aits etf dynamic-v3-rescue shadow monitor-run --as-of YYYY-MM-DD`、`aits etf dynamic-v3-rescue promotion pack --candidate-id <candidate_id>`、`aits etf dynamic-v3-rescue governance validate/report`、`aits etf dynamic-v3-rescue research index-build`、对应 validate 命令|Daily gate 由统一 `aits ops daily-run` 调用，只有 as-of 是周内最后一个已完成美股交易日且 latest pointer 存在时才做 pointer/stale/shadow monitor 检查；TRADING-092 已完成失败归因后，owner 可手动或通过后续受控 cadence 运行 small_real research smoke。|Daily gate 只做 due/skip/block 审计、latest pointer validation、stale 检查和可选 observe-only shadow monitor，不自动 `run-profile`、不 promotion pack、不生成 `production_candidate`。Manual research 先 data audit 和 governance gate，再 hard gate / soft ranking；real mode 先过 cached data quality gate 且 metrics 只来自 real evaluation artifact；tiny mode 只用于 artifact contract，不得进入 `promote_candidate`；candidate attribution 缺 daily path 必须显式 `INCOMPLETE`；overfit `HIGH_RISK` fail closed，PBO / DSR placeholder 只能 `REVIEW_REQUIRED`；shadow monitor observe-only，promotion pack 至少保持 manual review；`production_candidate` 只允许人工确认，不由脚本生成；Reader Brief 只读 latest sweep leaderboard / promotion pack / shadow monitor。|
|Weekly / Manual|Dynamic v3 real evidence closure|`aits etf dynamic-v3-rescue data-provenance inspect-price-cache`、`aits etf dynamic-v3-rescue data-provenance repair-price-manifest`、`aits etf dynamic-v3-rescue data-provenance validate`、`aits etf dynamic-v3-rescue window-audit run --artifact-root <path>`、`aits etf dynamic-v3-rescue window-audit report --latest`、`aits etf dynamic-v3-rescue validate-window-audit --audit-id <window_audit_id>`、`aits etf dynamic-v3-rescue weight-path validate --evaluation-id <evaluation_id>`、`aits etf dynamic-v3-rescue weight-path report --evaluation-id <evaluation_id>`、`aits etf dynamic-v3-rescue promotion pack --candidate-id <candidate_id>`|真实 real evaluator sweep / promotion pack 已生成后，owner 需要判断数据来源、回测窗口和 daily weight path 是否足够支撑候选解释和人工 promotion review。|Price cache provenance 必须披露 checksum、row count、download manifest coverage 和 reconstructed status；window audit 必须披露 configured/requested/actual range 与 promotion-blocking reasons；weight path 缺失只能标 `INCOMPLETE`，path 不完整只能标 `PARTIAL`；promotion pack 必须写 `evidence_summary.json`，并在 window、weight path、candidate attribution 或 data provenance 不完整时阻断 `promote_candidate`；Reader Brief 只读展示 evidence status，不补跑审计。|
|Weekly / Manual|Dynamic v3 medium_real candidate discovery and research decision|`aits etf dynamic-v3-rescue sweep run-profile --profile medium_real --as-of YYYY-MM-DD --end YYYY-MM-DD --workers N`、`aits etf dynamic-v3-rescue evidence-summary run --sweep-id <sweep_id>`、`aits etf dynamic-v3-rescue medium-real report --sweep-id <sweep_id>`、`aits etf dynamic-v3-rescue regime-coverage run --sweep-id <sweep_id>`、`aits etf dynamic-v3-rescue candidate interpretation-pack --sweep-id <sweep_id> --candidate-id <candidate_id>`、`aits etf dynamic-v3-rescue observe-pool build --sweep-id <sweep_id>`、`aits etf dynamic-v3-rescue overnight-readiness run --sweep-id <sweep_id>`、`aits etf dynamic-v3-rescue research-decision run --sweep-id <sweep_id>`、对应 validate/report 命令|TRADING-111 到 TRADING-113 evidence gate 可读后，owner 需要把 medium_real 结果转成候选证据矩阵、regime 覆盖结论、人工观察池和是否值得启动 overnight_real 的下一步决定。|Run-profile 必须先通过 cached data quality gate 且 metrics 来自 linked real evaluation artifact；evidence summary 缺证据 fail closed；regime coverage 必须披露 AI bull / drawdown / semiconductor stress / sideways / high-vol / recovery 缺口；observe pool 默认不写 shadow registry；overnight readiness 不启动 `overnight_real`；research decision 只输出 next task，不生成 approval、shadow enrollment、production candidate、baseline mutation、official target weight mutation 或 broker action；Reader Brief 只读 latest evidence/observe/readiness/decision。|
|Weekly / Manual|Dynamic v3 evidence gate diagnosis and candidate usability recovery|`aits etf dynamic-v3-rescue evidence-diagnosis run --sweep-id <sweep_id>`、`aits etf dynamic-v3-rescue gate-impact run --diagnosis-id <diagnosis_id>`、`aits etf dynamic-v3-rescue gate-policy validate`、`aits etf dynamic-v3-rescue gate-policy apply --sweep-id <sweep_id>`、`aits etf dynamic-v3-rescue candidate-recovery run --sweep-id <sweep_id> --policy-run-id <policy_run_id>`、`aits etf dynamic-v3-rescue observe-pool rebuild --recovery-id <recovery_id>`、`aits etf dynamic-v3-rescue research-decision update --sweep-id <sweep_id> --diagnosis-id <diagnosis_id> --impact-id <impact_id> --recovery-id <recovery_id>`、对应 validate/report 命令|A medium_real sweep has zero usable candidates because evidence gates are over-blocking, and owner needs to distinguish true hard failures from soft/manual-review blockers before deciding whether limited observe-only research may continue.|Diagnosis and impact simulation do not mutate source sweep artifacts；gate policy must be reviewed config with owner/version/rationale；only policy-allowed soft blockers may become observe-only/manual-review；hard failures remain rejected；candidate recovery and observe-pool rebuild do not write shadow registry；research decision update may output `GO_WITH_LIMITS` only with owner approval required；no approval、automatic shadow enrollment、production candidate、official target weights、baseline mutation、production state mutation、broker action or automatic `overnight_real` trigger is allowed.|
|Weekly / Manual|Dynamic v3 shadow shortlist and position advisory readiness|`aits etf dynamic-v3-rescue shortlist build --observe-pool-id <pool_id>`、`shortlist report --latest`、`validate-shortlist --shortlist-id <shortlist_id>`、`candidate-cluster run --shortlist-id <shortlist_id>`、`candidate-cluster report --latest`、`validate-candidate-cluster --cluster-id <cluster_id>`、`shadow-shortlist build --shortlist-id <shortlist_id> --cluster-id <cluster_id>`、`shadow-shortlist report --latest`、`validate-shadow-shortlist --shadow-shortlist-id <shadow_shortlist_id>`、`position-advisory run --shadow-shortlist-id <shadow_shortlist_id>`、`position-advisory report --latest`、`validate-position-advisory --advisory-id <advisory_id>`、`position-review pack --shortlist-id <shortlist_id> --cluster-id <cluster_id> --shadow-shortlist-id <shadow_shortlist_id> --advisory-id <advisory_id>`、`position-review report --latest`、`validate-position-review --review-id <review_id>`|TRADING-121 到 TRADING-125 已恢复 observe-only/manual-review candidates 后，owner 需要把 300 个候选压缩成可长期观察的 representative shortlist，并在不写生产仓位的前提下复核 candidate target weights 与当前持仓差异。|Shortlist hard failures fail closed；clustering 缺 daily weight path 只能标 `INCOMPLETE`；shadow shortlist 只写 monitoring pack，不写 shadow registry、不自动 enrollment；position advisory 使用 reviewed config，缺 portfolio snapshot 时必须 `TARGET_ONLY`，有 snapshot 时也只能输出 manual-review delta；position review 必须保持 `production_readiness=NOT_READY`、`broker_action_allowed=false`、`owner_approval_required=true`；Reader Brief 只读 latest artifacts；不得 approval、official target weights mutation、baseline mutation、production state mutation、broker action 或把 advisory 当作交易指令。|
|Daily / Manual|Dynamic v3 shadow monitoring and manual position advisory observation|`aits etf dynamic-v3-rescue shadow-monitor activate --shadow-shortlist-id <shadow_shortlist_id>`、`shadow-monitor run --shadow-shortlist-id <shadow_shortlist_id> --as-of YYYY-MM-DD`、`shadow-monitor report --latest`、`validate-shadow-monitor-run --monitor-run-id <monitor_run_id>`、`portfolio-snapshot validate/report/normalize --snapshot config/etf_portfolio/dynamic_v3_rescue/current_portfolio_snapshot.yaml`、`position-advisory daily-run --shadow-monitor-run-id <monitor_run_id> --config config/etf_portfolio/dynamic_v3_rescue/position_advisory_v1.yaml [--portfolio-snapshot <snapshot.yaml>]`、`position-advisory daily-report --latest`、`validate-position-advisory-daily --daily-advisory-id <daily_advisory_id>`、`consensus-drift run/report --shadow-monitor-run-id <monitor_run_id>`、`validate-consensus-drift --drift-id <drift_id>`、`owner-review create/list/report/record-decision/validate-owner-review`|TRADING-126 到 TRADING-130 的 shadow shortlist / position review pack 已存在后；owner 需要每日或手动观察 candidate target weights、manual snapshot delta、candidate disagreement 和 owner decision journal。|Shadow monitor 只读 shadow shortlist；manual snapshot 必须 fail closed 校验权重和值一致性、重复 symbol、负值、currency、`as_of` 和 `broker_imported=false`；缺 snapshot 时 daily advisory 必须 `TARGET_ONLY`，有 snapshot 时也只是 `SNAPSHOT_DELTA` manual-review delta；`HIGH_DISAGREEMENT` consensus drift 强制 advisory `manual_review`；owner review 只记录 decision 和 paper-only action；Reader Brief 只读 latest artifacts；固定 `production_effect=none`、`broker_action=none`、`broker_action_allowed=false`、`broker_action_taken=false`、`owner_approval_required=true`、`manual_review_required=true`，不得 approval、official target weights mutation、baseline/production state mutation、broker action 或自动实盘执行。|
|Weekly / Manual|ETF weight calibration overfit explanation|`aits etf weight-calibration overfit-explain --latest --top N` 或 `--run-id <run_id>`|TRADING-078B Top-N shortlist 和可选 TRADING-071G overfit diagnostics 已存在后；用于把 candidate risk band 转成可审阅的 reasons、supporting metrics、blocking metrics 和 manual review note。|TRADING-078E 只写 `reports/etf_portfolio/weight_calibration/overfit_explanations/` runtime JSON / Markdown；不登记 candidate、不 enroll shadow、不应用权重、不修改 baseline config、不触发 broker action；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`。|
|Weekly / Manual|ETF weight candidate shadow enrollment|`aits etf weight-calibration enroll-top --latest --top N` 或 `aits etf weight-calibration enroll --latest --weight-set <weight_set_id>`|TRADING-078B Top-N shortlist、TRADING-078C comparison table 和可选 recommendation/source report 已存在后；用于把已人工选择且 `shadow_ready` 的 candidate 放入 forward observation ledger。|TRADING-078F 只写 ignored `data/etf_portfolio/weight_calibration/forward_enrollments.json`；只允许 `forward_readiness_status=shadow_ready` 且无 blockers 的 candidates，保留 source links、warnings、`enrollment_id` 和 `shadow_candidate_id`；blocked / high-overfit / data-quality-blocked candidates fail closed；不得写 production weights、baseline config、shared experiment shadow registry 或 broker state。|
|Weekly / Manual|ETF initial weight recommendation report|`aits etf weight-calibration recommendation --latest --top N` 或 `--run-id <run_id>`|TRADING-078B Top-N shortlist、TRADING-078C comparison、TRADING-078D regime robustness 和 TRADING-078E overfit explanation 已存在后，或需要生成人工 review package 前。|TRADING-078G 只写 `reports/etf_portfolio/weight_calibration/recommendations/` JSON / Markdown；报告可在内存补齐缺失 section，但不运行 enrollment、不应用 weights、不修改 baseline config、不触发 broker action；输出必须披露 safety banner、data range/preset、bounded constraints、source artifacts 和 shadow enrollment recommendation。|
|Reader Brief|ETF initial weight candidate summary|`aits reports reader-brief --latest`|latest `etf_initial_weight_recommendation_report` 已经由 report index 发现后；用于在每日读者入口展示 historical candidate shortlist 的人工复核状态。|TRADING-078H 只读 latest recommendation report，展示 latest preset、top candidate、suggested shadow action、overfit risk、best robustness、blocked candidate count、safety 和 detail link；缺 report 时显示 `MISSING`，不运行 weight-calibration recommendation/search/comparison/robustness/overfit/enrollment 上游，不写 production weights、baseline config 或 broker state。|
|Governance|ETF historical calibration usability validation|`aits etf weight-calibration usability-validate`|完成或变更 TRADING-078A-H presets/search/export/comparison/robustness/overfit/enrollment/recommendation/Reader Brief workflow 后；标记 TRADING-078 baseline status 前必须运行。|TRADING-078I 生成 JSON / Markdown validation artifact，fail-closed 校验 presets、bounded search、A-H sample workflow、Reader Brief registry visibility、benchmark/overfit coverage 和固定 safety boundary；unsafe production_effect、broker_action、manual_review_required、unbounded search 或 enrollment production mutation 均阻断。|
|Monthly / Manual|Strategy evidence dashboard|`aits etf evidence-dashboard aggregate --as-of {as_of}`、`aits etf evidence-dashboard report --as-of {as_of}`、`aits etf evidence-dashboard validate --as-of {as_of}`|月度 governance、重要 source artifact 更新后，或 owner 需要统一查看策略组件证据强弱、stale/blocked sources 和人工复核优先级时。|Dashboard 只读 report index 和既有 ETF artifacts；每个 evidence card 必须带 source link、freshness、data quality、validation status 和 sample count；conflicts 与 manual review priorities 只能产生人工复核动作；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`；不得写 production weights、不得自动 promotion、不得触发 broker action。|
|Monthly / Manual|Baseline candidate review|`aits etf baseline-review eligibility --candidate <candidate_id> --as-of {as_of}`、`aits etf baseline-review package --candidate <candidate_id> --as-of {as_of}`、`aits etf baseline-review capture-decision --review-package-path <package.json> --owner-decision <decision>`、`aits etf baseline-review proposal-draft --review-package-path <package.json> --decision-path <decision.json>`、`aits etf baseline-review outcome --candidate <candidate_id> --decision-path <decision.json>`、`aits etf baseline-review validate --as-of {as_of}`|TRADING-076 evidence dashboard、TRADING-071 weight calibration report、forward evidence、parameter review、decision journal、data quality、ops health 和 validation gates 已存在后；owner 需要判断 candidate 是否可进入 formal baseline-change proposal review 时。|Baseline review 只读既有 evidence，不运行上游、不补造 source；evidence dashboard blocked、critical data quality、ops validation failed、validation gate stale、forward sample too small、missing required journal link、parameter review blocked、unsafe production effect 或 broker action 必须 fail closed；owner decision 是 proposal draft 前置条件；proposal draft 不写 baseline config、不写 `target_weights.csv`、不触发 broker；Reader Brief 只读 latest package/decision/proposal/outcome；固定 `observe_only=true`、`candidate_only=true`、`production_effect=none`、`broker_action=none`、`manual_review_required=true`。|
|Weekly|Shadow parameter backtest review|`aits parameters shadow-backtest --latest`、`aits parameters validate-shadow-backtest --latest`、`aits reports shadow-parameter-backtest --latest`、`aits reports parameter-promotion --latest`|手动参数复核前，或每周最后一个已完成美股交易日之后，且 `aits validate-data` 可通过或报告明确 `INSUFFICIENT_DATA` / `FAILED`。|固定 `production_effect=none`、`manual_review_required=true`、`auto_promotion=false`；不得修改 `config/parameters/production/current.yaml`、production weights/gates、broker 或交易动作；报告声明 `ai_after_chatgpt` regime、实际 date range 和 data quality status。|
|Biweekly|Biweekly weight feedback|`python scripts/run_weight_candidate_evaluation.py --date {as_of}`、`python scripts/run_weight_promotion_gate.py --date {as_of}`、`aits feedback loop-review --as-of {as_of}`、`python scripts/run_shadow_vs_production_multi_day_review.py --date {as_of}`|每两周，且已有足够新 daily / weekly artifacts；样本不足时保持 `INSUFFICIENT_DATA` 或 manual review。|只读或 manual-review-only；不得写 production profile、approved overlay、broker order 或 trading action。|
|Monthly|Monthly governance|`aits docs report-contract --as-of {as_of}`、`aits reports index --as-of {as_of}`、`aits data-sources validate --as-of {as_of}`、`aits backtest-pit-coverage --as-of {as_of}`、`aits backtest --regime ai_after_chatgpt --to {as_of}`|每月最后一个已完成美股交易日之后，且 latest daily artifacts 可追溯。|检查文档、registry、数据源覆盖、PIT coverage 和长窗口回测解释是否仍可审计。|
|Governance|Scheduler validation|`aits ops daily-plan --as-of {as_of} --fail-on-missing-env`、`aits etf ops validate --as-of {as_of}`、`aits etf data-quality validate --as-of {as_of}`、`aits etf evidence-dashboard validate --as-of {as_of}`、`aits etf baseline-review validate --as-of {as_of}`、`aits etf shadow-review validate`、`aits etf trend-calibration validate`、`aits etf dynamic-allocation validate`、`aits etf dynamic-calibration validate`、`aits etf dynamic-robustness validate`、`aits etf dynamic-shadow validate`、`aits etf dynamic-rescue validate`、`aits etf weight-calibration usability-validate`、`aits etf weight-calibration performance-validate`、`aits etf weight-calibration profiling-validate`；调度配置或代码变更时运行 `python -m pytest tests/test_scheduled_tasks.py tests/test_ops_daily.py tests/test_cli_direct.py -q`|scheduler 配置、模板、daily chain、closed-market 行为、ETF operations cadence graph、data-quality governance gate、strategy evidence dashboard gate、baseline candidate review gate、shadow-ready review gate、trend signal calibration gate、dynamic allocation gate、dynamic calibration gate、dynamic robustness gate、dynamic shadow gate、dynamic rescue gate、historical calibration usability gate、weight diagnostics cache/parallel validation gate、weight diagnostics profiling validation gate 或 cadence 登记变更时；也可每周人工抽检。|daily order 与 `config/scheduled_tasks.yaml` 一致；非 daily cadence 不泄漏进 daily plan；TRADING-074 schedule/graph/freshness/failure/checklist/dry-run/report/Reader Brief integration、TRADING-075 policy/checkers/report/Reader Brief integration、TRADING-076 schema/registry/aggregation/report/Reader Brief integration、TRADING-077 eligibility/package/decision/proposal/outcome/validation/Reader Brief integration、TRADING-078A-I usability workflow safety invariants、TRADING-080 cache / parallel / resume safety invariants、TRADING-081 profiling / hotspot / Reader Brief / validation safety invariants、TRADING-082 package / approval / approved-only enrollment / Reader Brief / validation safety invariants、TRADING-083 signal registry / dataset / bounded search / attribution / Reader Brief / forbidden target weight safety invariants、TRADING-084 dynamic policy / decision record / constraint / Reader Brief / official target weight write safety invariants、TRADING-085 candidate pack / cache / ranking / Reader Brief / calibration proxy safety invariants、TRADING-086 price-driven robustness / no auto enrollment safety invariants、TRADING-087 owner-approved-only dynamic shadow / forward tracking / weekly review safety invariants 和 TRADING-088 diagnostics / rescue / no approval / no enrollment safety invariants 保持。|
|Governance|Artifact catalog consistency|`aits docs report-contract --latest`，并人工核对 `docs/artifact_catalog.md`、`config/report_registry.yaml` 和 latest report outputs|新增、重命名或删除 report/artifact 后；至少 monthly governance 时复核。|artifact 生成者、上游、下游、production effect、常见误解和 Reader Brief/report registry 可见性一致。|

## Date And Condition Dispatch

未来若把 weekly / biweekly / monthly / governance 任务接入每日 scheduler trigger，必须遵守以下 dispatch 条件：

1. 每日 trigger 仍只暴露一个外部入口，例如 `aits ops daily-run` 或后续明确命名的统一 scheduler entry。
2. due 判断必须基于明确日期规则、交易日历和已完成 daily artifacts，而不是系统启动时间的隐式副作用。
3. 非 daily 任务运行前必须确认当日 daily data gate 和必需上游 artifact 状态；缺失时输出 `SKIPPED`、`LIMITED`、`INSUFFICIENT_DATA` 或 fail-closed 诊断，不补造结论。
4. 任务输出必须写明 cadence、as_of、date range、source artifacts、data quality status 和 `production_effect`。
5. 任一会影响投资解释的阈值、promotion gate、readiness rule 或样本 floor 必须来自配置/政策 manifest，或按 AGENTS 规则登记为可审计 pilot baseline。

## Operator Checklist

执行任何周期任务前：

1. 读取本文和对应细分 runbook。
2. 确认任务 cadence、`as_of`、date range、market regime 和是否交易日。
3. 确认 `aits validate-data` 或同一路径门禁已通过，并记录对应质量报告。
4. 确认任务的 `production_effect`、weight write、broker action 和 trading action 边界。
5. 如果任务会新增或修改 CLI、配置、artifact、report schema、数据流、评分、回测或治理解释，同步更新 `docs/system_flow.md`、`docs/artifact_catalog.md`、任务登记和相关 requirements/runbook。
6. 如果遇到 blocker，不要静默绕过；按 AGENTS 的 no-silent-workaround 流程记录原因、影响、风险、验证覆盖和退出条件。
