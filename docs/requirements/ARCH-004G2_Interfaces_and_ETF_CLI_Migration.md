# ARCH-004G2 Interfaces 与 ETF CLI 迁移

最后更新：2026-07-14

## 任务信息

- task id：`ARCH-004G2_INTERFACES_AND_ETF_CLI_MIGRATION`
- parent：`ARCH-004G_DOMAIN_MIGRATION_AND_SUBTRACTION`
- priority：`P0`
- status：`IN_PROGRESS`（G2.3 `COMPLETE`，G2.4 `IN_PROGRESS`）
- owner：interface platform / architecture coordinator
- dependency：ARCH-004G1 `COMPLETE`
- production effect：`none`

## 为什么先冻结 contract

`src/ai_trading_system/cli_commands/etf_portfolio.py` 当前包含41个一级command group、291个group、993个叶子命令。它同时承担import composition、Typer app构造、group注册、参数声明和command callback，任何直接切文件操作都可能静默改变command path、option alias、required/default、help、exit code或重复注册状态。

G2因此先把当前Click/Typer解析后的真实command tree冻结为可复算contract，而不是只数decorator。Baseline必须忽略callback模块位置，使函数迁移不会天然造成漂移；但必须覆盖用户可见的path、参数、类型、默认值、flag语义、help、group行为和duplicate状态。

## 分阶段计划

- 2026-07-14：G2.4CP Smoothed Evidence Completeness / Promotion Readiness `COMPLETE_G2_4_CONTINUES`，不触发phase-level ARCH-005 handoff。TRADING-256～260共15 callback已迁`dynamic_v3_system_target_smoothed_readiness.py` canonical interface/domain；五类bounded v2 snapshots、pre-output live validation/cutoff、common Review/Comparison/Backfill lineage、reviewed readiness policy、explicit delta/turnover identity、unique finite method/date、missing/null、Confirmation-only candidate authority和全部views byte rebuild闭合。当前fixture candidate=null，Gap direct backfill不可解决且仍需forward observation，Scorecard=`CONTINUE_OBSERVATION/INSUFFICIENT_EVIDENCE`，Owner action=`request_additional_evidence`、promotion=false；旧固定3d/5d/missing-to-zero/no-candidate promotion结论失效。Legacy CLI/domain=`17,328/19,856`行，functions=`468/760`，decorators=429；generated=`917 modules/1,119 tests/858 writers/0 violations`；focused integration/hardening/CLI=`1/7/116 passed`，architecture-fitness=`277 passed`（`architecture-fitness_20260714T084835Z`），contract-validation=`203 passed`（`contract-validation_20260714T085007Z`），CLI tree hash不变。下游TRADING-261～265仍存在candidate-null回填固定3d/5d的legacy debt，已进入下一slice contract freeze，不以workaround掩盖。固定not-PIT/manual research、`production_effect=none`；G2.4继续，不进入G2.5。
- 2026-07-14：G2.4CO Smoothed Evidence `COMPLETE_G2_4_CONTINUES`。15 callbacks 已迁 canonical interface/domain；legacy root减至17739行/483 functions/444 decorators，System Target legacy domain为21897行/805 functions。五类 v2 snapshot、pre-output live replay、exact Review→Comparison→Smoothed/Baseline lineage、reviewed policy、per-method/null-preserving metrics、candidate-driven zero/4 targets 和 all-view byte rebuild 已闭合。Fixture 3d/5d benefit=`MODERATE/MODERATE`、tradeoff=`FAVORABLE/FAVORABLE`、sideways=`MIXED/IMPROVED`、recovery=`LOW/LOW`，但 Review无 eligible candidate，Confirmation=`INSUFFICIENT_EVIDENCE/0 targets`、Watch=`NOT_REGISTERED`。Focused/slice+CLI/downstream/architecture/contract=`12/120/1/276/203 passed`；generated=915 modules/1118 tests/858 writers/0 violations，CLI tree/hash不变。Task转`BASELINE_DONE`等待独立forward/PIT/DQ/cost/holdout与owner review；整个G2.4继续，不进入G2.5，不触发ARCH-005 handoff。
- 2026-07-14：G2.4CO 实现完成并进入 `VALIDATING`。15 个 Smoothed Evidence callbacks 已迁 canonical interface/domain，legacy callback/decorator 与旧 public domain 实现清零；五类 v2 snapshots、live upstream replay、Review→Comparison→Smoothed/Baseline exact lineage、reviewed policy、null-preserving metrics、candidate-driven zero/4 targets 与全 views byte rebuild 已落地。当前 fixture 仍为 Review=`CONTINUE_OBSERVATION/LOW`、candidate=null、Confirmation 0 targets/`INSUFFICIENT_EVIDENCE`；CLI tree 41/291/993/hash 不变，current slice + CLI contract 120 tests 与 downstream compatibility PASS。正在执行 architecture / contract closeout；不进入 G2.5，G2.4 继续且不触发 ARCH-005 handoff。
- 2026-07-14：G2.4CO Smoothed Evidence Drilldown / Forward Confirmation contract freeze并进入`IN_PROGRESS`，整个G2.4仍继续且不触发phase-level ARCH-005 handoff。范围固定为TRADING-251～255的Attribution、Benefit/Lag、Regime、Confirmation、Watch共15 callback，迁独立canonical `dynamic_v3_system_target_smoothed_evidence.py` interface/domain。审计确认旧producer只读取materialized JSON、不调用CN upstream validator、不冻结source/config/policy bytes或generated cutoff；Attribution可组合不同Review/Comparison/Backfill，Benefit/Lag可跨Backfill，Confirmation可组合不同Review/Regime，Watch可拼接五条无共同lineage的artifact；validators只检查文件/枚举/安全字段，不能重放来源、公式或Markdown。旧helper/report还固定3d为primary，硬编码forward event/sideways/recovery阈值，并把zero-sample return/drawdown经0解释，直接违背CN当前recommended/secondary=null结论。Acceptance：五类bounded v2 snapshots、timezone-aware cutoff、正式写件前live upstream PASS、policy binding与严格Review→Comparison→Smoothed/Baseline Backfill lineage；Benefit/Lag和Regime保留missing/null并用reviewed policy管理sample floors/status boundaries；Confirmation只为Review实际evidence-backed recommended method登记targets，若为空则输出`INSUFFICIENT_EVIDENCE`且0 target，不以3d id补造；Watch要求Attribution/Benefit/Regime/Confirmation同一Review/Comparison/Backfill链并允许candidate_method=null；所有validator重验live source/policy并逐byte重建全部JSON/Markdown/Reader Brief。Invalid/missing/ambiguous/future/cross-lineage/duplicate/non-finite/tamper fail closed；CLI tree/help parity、focused/architecture/contract/Ruff/compileall/manifests/deprecation/flow/runbook/registry/catalog/研究执行链同步。固定not-PIT research/manual-only、no official/no auto/no order/no broker、`production_effect=none`；不进入G2.5。

- 2026-07-14：G2.4CN Smoothed Limited Adjustment `COMPLETE_G2_4_CONTINUES`，整个G2.4仍继续且不触发phase-level ARCH-005 handoff。15 callback已迁canonical smoothed-method interface/domain；Config/Target/Backfill/Comparison/Review五类bounded v2 snapshots、pre-output live source/config/cache/DQ/cutoff、canonical Paper Backfill显式编排、Smoothed/Baseline/Risk同Baseline exact lineage、日期/chronology、unique method/date、missing/null、reviewed evaluation policy、evidence-driven selection和全部views逐byte重建均闭合。Fixture=`2022-12-01..2024-02-29`、63 rebalances、326 common return observations；3d/5d相对limited return/drawdown/turnover/jump delta分别为`-0.0012239743/-0.0000650563/-0.0962240287/+1`与`-0.0017790195/-0.0000172701/-0.1151699146/-1`，decision=`CONTINUE_OBSERVATION/LOW`且recommended/secondary均为null，旧固定3d/5d角色已撤销。Focused/current-slice+CLI/architecture/contract=`45/152/275/203 passed`，generated=`913 modules/1,117 tests/858 writers/0 violations`，CLI仍993 leaf/0 duplicate/tree hash不变。Task转`BASELINE_DONE`等待独立forward/PIT/DQ/cost/holdout证据；next owner=architecture coordinator，继续剩余G2.4 matrix，不进入G2.5，`production_effect=none`。

- 2026-07-14：G2.4CN Smoothed Limited Adjustment contract freeze并进入`IN_PROGRESS`，整个G2.4仍继续且不触发phase-level ARCH-005 handoff。范围固定为TRADING-246～250的config/target/backfill/comparison/review共15 callback，迁独立canonical `dynamic_v3_system_target_smoothed_method.py` interface/domain。现状审计确认旧链仅检查文件与部分shape：producer未在写件前验证Model Target/Backfill/Comparison来源，不冻结source/config/policy bytes，Comparison可混用不同Backfill lineage且zero-sample会经`_float(None)`伪装成0，Review固定推荐3d并依赖未治理的hardcoded阈值，validator不能重放来源、lineage、公式或Markdown。Acceptance：五类bounded v2 snapshot、timezone-aware cutoff与pre-output live validation；Target冻结validated Model Target、smoothed/model configs并逐byte重算；Backfill显式编排并绑定validated canonical Paper Backfill；Comparison要求Smoothed/Baseline/Risk三者同一Baseline lineage、日期区间和chronology，去除重复method rows，zero-sample metrics=null/`INSUFFICIENT_DATA`；reviewed evaluation policy管理return/drawdown/turnover/jump/lag/sample floors，recommended/secondary只能从完整可用证据选择，不能因method id硬编码；Review与Comparison exact lineage；所有validator重验live source/config并逐byte重建全部views/report。Invalid/missing/ambiguous/future/cross-lineage/non-finite/tamper fail closed；CLI tree/help parity、focused/architecture/contract/Ruff/compileall/manifests/deprecation/flow/runbook/registry/catalog同步。固定not-PIT research/paper-shadow/manual-only、no official/no auto/no order/no broker、`production_effect=none`；不进入G2.5。

- 2026-07-14：G2.4CM Weight Optimization Experiment Factory `COMPLETE_G2_4_CONTINUES`，不触发 phase-level ARCH-005 handoff。TRADING-239～245 共 21 callback 迁独立 canonical experiment-factory interface/domain；legacy CLI root 19,197→18,568 行、534→513 函数、495→474 decorators，legacy system-target domain 24,598→23,375 行/803 函数。七类 bounded v2 snapshots、producer pre-output live source/config PASS/cutoff、Matrix→Backfill→Batch→Triage→Interpretation→Plan exact lineage、common finite duplicate-free price dates、首日/缺失收益不补 0、candidate selection 真执行、zero-sample regime=null、zero-effect transform=DEFER、reviewed complete triage policy、expected/observed evidence 分离及 live-source/policy/全 views byte rebuild 已闭合。Fixture requested=`2022-12-01..2024-02-29`、actual returns=`2022-12-02..2024-02-29`、DQ=`PASS_WITH_WARNINGS`、15 variants，结果=`0 PROMOTE / 3 KEEP / 7 REJECT / 5 DEFER`，top=`sideways_choppy_signal_persistence_3d/KEEP/0.656744`，return/drawdown/turnover delta=`-0.0194958444/+0.0019514235/-0.1531475311`，Promotion Plan=`DEFER`且methods为空；旧3 promotions/2 proposed smoothing baseline不再是可信当前结论。Full gate 同步修复 direct scheduler canonical dispatch、hardened outcome snapshot fixture 与唯一 Model Target fixture reuse，未放宽 fail-closed gate。Focused/slice+CLI/architecture/contract/full=`12/118/274/203/5,977 passed`；runtime artifacts=`architecture-fitness_20260714T012508Z`、`contract-validation_20260714T012705Z`、`full_20260714T024455Z`；generated=`911/1,116/858/0`，CLI tree保持41/291/993/0 duplicate/hash不变。固定 experiment-only/research-screening/manual-only、not formal method/no official/no auto/no order/no broker、`production_effect=none`。

- 2026-07-13：G2.4CL Dynamic-v3 Risk-Capped Limited Adjustment `COMPLETE_G2_4_CONTINUES`，不触发 phase-level ARCH-005 handoff。TRADING-229～233 共 15 callback 迁至 canonical `dynamic_v3_system_target_risk_capped.py` interface/domain；legacy CLI root 19,586→19,197 行、549→534 函数、510→495 decorators，legacy system-target domain 25,367→24,598 行/802 函数。五类 bounded v2 snapshots、pre-output live validation/time cutoff、canonical Backfill 显式编排、Risk/Baseline exact Backfill lineage、reviewed evaluation policy、DQ/cache commitments、去除重复 risk-method observations、missing regime metrics=null 及 live-source/policy/逐 byte 重建均已闭合。Fixture 2022-12-01～2024-02-29 为 63 rebalances/130 cap events，return delta=-0.0171641318、max-drawdown delta=+0.0017484558、average semiconductor exposure delta=-0.0238778863、turnover delta=+0.3211926199，rolling/stability=`MIXED`，pilot decision=`PROMOTE_TO_RECOMMENDED_RESEARCH/LOW`；该结果仅证明在 reviewed pilot threshold 下可继续研究，不是投资、promotion 或 production 结论。Focused/slice+CLI/architecture/contract=`11/116/273/203 passed`；generated=`909/1,115/858/0`，CLI tree 保持 41/291/993/0 duplicate/hash 不变；固定 not-PIT research/paper-shadow/manual-only、no official/no auto/no order/no broker、`production_effect=none`。

### G2.1 Command registry 与 golden contract

输入：

- runtime `etf_app`；
- Click/Typer解析后的command tree；
- 当前`etf_portfolio.py` source hash与deprecation inventory；
- 两组待选真实CLI fixture及其现有测试。

计算：

1. 递归遍历Click group，生成稳定command path；
2. 对每个group/leaf规范化help、short help、epilog、invoke/no-args行为；
3. 对每个parameter规范化argument/option类型、name、opts、required、nargs、multiple、default、type、flag/count/hidden/help；
4. 对每条command contract计算canonical JSON SHA-256，再计算全树SHA-256；
5. 同级command name重复、path重复、非确定default或baseline drift一律fail closed。

输出：

- canonical CLI contract scanner；
- `inputs/architecture/arch_004g2_etf_cli_contract.yaml`；
- command/group/count/hash drift tests；
- 两组真实CLI fixture选择与迁移前结果。

退出：`41 root / 291 group / 993 leaf / 0 duplicate`可复算；tracked baseline hash一致；architecture-fitness纳入该test。

### G2.2 Registration shell 与 shared parameter contract

- 建立`interfaces.cli.etf_portfolio` package与明确group ownership；
- root module先只保留app composition和兼容export；
- 共享callback helper/parameter alias只在至少两个真实caller且语义一致时抽取；
- 禁止在拆分中修改threshold、date/window、DQ/PIT或artifact语义。

退出：root registration与旧command tree golden parity；无duplicate；fixture 1 PASS。

### G2.3 Data、operations 与 reporting groups

- 先迁data/features/data-quality、ops/run、report/evidence-dashboard等较低投资解释风险group；
- callback连同其专属imports移动，禁止保留双实现；
- root只注册迁移后的Typer app。

退出：选定group旧callback reachability=0；golden tree不变；fixture 1/2 PASS。

### G2.4 Research、shadow 与 portfolio groups

- 分别迁research/experiments/calibration、shadow/paper-shadow、portfolio/allocation/backtest；
- 每个slice单独证明DQ/PIT、窗口、阈值、artifact和exit parity；
- dynamic-v3大组继续按内部语义边界拆分，不形成另一个god module。

退出：全部993 leaf由明确模块owner承载；root无计算和artifact write；两组真实fixture及相关domain tests PASS。

### G2.5 Freeze、deprecation 与 closeout

- 将旧root置为thin registration façade或按gate删除；
- 更新deprecation inventory、compatibility baseline、system flow和task register；
- 通过focused、CLI contract、architecture、integration、reproducibility和full parallel。

退出：command registry全覆盖、0 duplicate、root thin、无永久dual track；G3才可开始。

## 不变量

- command name/path/options/default/help/exit不变；
- AI regime与research window不混用；
- required DQ gate不得旁路；
- 不在结构迁移中调策略、阈值、权重或promotion；
- 不产生paper-shadow、production或broker effect；
- baseline drift不得用更新snapshot直接掩盖，必须先解释差异来源。

## 当前状态

- 2026-07-14：G2.4CQ=`COMPLETE_G2_4_CONTINUES`，TRADING-261～265转`BASELINE_DONE`。16 callbacks与旧业务实现已迁独立smoothed-promotion interface/domain，legacy root对应callbacks/decorators清零，旧domain只保留11个lazy compatibility wrappers；五类v2 snapshots、live source/policy replay、Confirmation-only candidate、exact lineage/chronology、content-derived evidence/criteria/targets/switch/journal、null/0-target、Owner record原子重建与all-view byte rebuild闭合。Bounded source binding只冻结消费的business views/commitments，不递归嵌入上游input snapshot；完整validator与tamper fail-closed保持。当前candidate=null、Gate=`CONTINUE_OBSERVATION`、Binding=`targets=[]/NOT_REGISTERED`、Switch proposed=null、Owner=`request_more_forward_data`。Focused hardening/five-stage=`7/5 passed`、architecture/contract=`278/203 passed`、generated=`919/1,120/858/0`；研究证据依赖保留。G2.4继续，不进入G2.5，`production_effect=none`。
- 2026-07-14：G2.4CQ TRADING-261～265 Promotion/Gate/Binding/Switch/Owner Journal contract freeze并进入`IN_PROGRESS`。范围为16 callbacks迁独立canonical smoothed-promotion interface/domain；旧consumer在candidate=null时回填固定3d/5d、写静态supporting evidence/criteria、补造3个confirmation targets、生成nonnull switch plan且validator只接受PROMOTE。Exit固定五类bounded v2 snapshots、pre-output upstream/live policy validation、Confirmation-only candidate与全链exact match、0-target/null preservation、content-derived evidence/criteria/switch/journal、Owner record原子重建、全部views byte rebuild；合法continue-observation workflow必须validation PASS。固定not-PIT/manual paper-shadow research、`production_effect=none`；G2.4继续，不进入G2.5。
- 2026-07-13：G2.4CK Dynamic-v3 Weakness Diagnosis / Method Refinement `COMPLETE_G2_4_CONTINUES`，不触发phase-level ARCH-005 handoff。TRADING-224～228共15 callback迁canonical `dynamic_v3_system_target_refinement.py` interface/domain；legacy CLI root 20,017→19,586行、564→549函数、525→510 decorators，legacy system-target domain 26,087→25,367行/801函数。五类bounded v2 snapshots、pre-output upstream PASS/time cutoff、same-Backfill/Selection exact lineage/chronology、reviewed `method_refinement_v1`、Risk DQ/cache commitments、common finite prices、missing/null/UNKNOWN与live-source/policy/cache/DQ/逐byte重建闭合；首日/缺失收益不再补0，conceptual metrics保持null。Fixture为49 unstable windows、48 drawdown-worse、risk source=`mixed`、268个重叠20日risk events、Warning repair=`UNKNOWN`、Proposal=`IMPLEMENT_RISK_CAPPED_RESEARCH_METHOD/LOW`，只证明可复算而非策略有效。Focused/slice+CLI/architecture/contract=`7/110/272/203 passed`，runtime artifacts=`architecture-fitness_20260713T225921Z`、`contract-validation_20260713T230116Z`；generated=`907/1,114/858/0`，CLI tree仍41/291/993/0 duplicate/hash不变。固定not-PIT research/manual-only、no official/no auto/no order/no broker、`production_effect=none`。
- 2026-07-13：G2.4CJ Dynamic-v3 Selection Drilldown / Research Method Hardening `COMPLETE_G2_4_CONTINUES`，不触发phase-level ARCH-005 handoff。TRADING-219～223共15 callback迁canonical `dynamic_v3_system_target_hardening.py` interface/domain；legacy CLI root 20,437→20,017行、579→564函数、540→525 decorators，legacy system-target domain 27,034→26,087行/806→801函数。五类bounded v2 snapshots、pre-output validated live sources、zero implicit upstream run、null/reference-only/UNKNOWN preservation、Consistency exact same-Backfill lineage、Hardening exact Backfill/Selection lineage与chronology、reviewed policy及逐byte validator全部闭合。Fixture Long Risk=`RETURN_IMPROVES_RISK_WORSENS`、Rolling=`MIXED`、Regime=`WEAK_IN_PRESSURE`、Warning=`UNKNOWN/REVIEW_REQUIRED`、Hardening=`REVIEW_REQUIRED/LOW`；workflow PASS不构成投资结论。Focused core/downstream=`7/5`、slice+CLI=`110`、architecture=`271`、contract=`203` passed；generated=`905/1,114/858/0`，CLI tree hash不变。固定not-PIT research/manual-only、no official/no auto/no order/no broker、`production_effect=none`。
- 2026-07-13：G2.4CI Dynamic-v3 Paper Shadow Historical Evaluation `COMPLETE_G2_4_CONTINUES`，不触发phase-level ARCH-005 handoff。TRADING-214～218共16 callback迁canonical `dynamic_v3_system_target_history.py` interface，领域实现进入同名bounded domain；legacy CLI root 20,841→20,437行、595→579函数、556→540 decorators，legacy system-target domain 27,838→27,034行并删除17个旧public实现，仅保留lazy compatibility wrappers。Backfill只接受cutoff内唯一validated v2 Model Target，不从raw mutable latest或baseline补造；pre-output timezone/cutoff、reviewed config、DQ与source/config/cache commitments、common finite duplicate-free dates、versioned costs及`not_pit_safe/current_definition_replayed_historically`闭合。五阶段bounded v2 snapshots、same-Backfill exact lineage/chronology、missing/insufficient=null且不排名、reviewed symbol/threshold regime、policy rank stability、reference-only不可推荐、missing component不填0及live-source/逐byte validator全部通过。Source-backed fixture=10 methods、`2022-12-01..2024-02-29`、63 rebalances、3,260 states、49 rolling windows/490 metrics、DQ=`PASS_WITH_WARNINGS`、Selection=`limited_adjustment/REVIEW_REQUIRED`且`consensus_target`仅reference-only；不构成策略优越结论。Focused=`33`、core=`10`、slice+CLI=`112`、architecture-fitness=`270`（`architecture-fitness_20260713T204112Z`）、contract-validation=`203`（`contract-validation_20260713T204319Z`）；generated=`903 modules / 1,114 tests / 858 writers / 0 violations`，CLI仍41/291/993/0 duplicate/tree hash不变。固定research/manual-only、no official/no auto/no order/no broker、`production_effect=none`。
- 2026-07-13：G2.4CH Dynamic-v3 System Target Portfolio and Paper Shadow Core slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。TRADING-209～213共17 callback已迁`dynamic_v3_system_target_portfolio.py` canonical interface；legacy CLI root 21,286→20,841行、612→595函数、573→556 decorators，legacy domain 28,805→27,838行，删除19个旧public实现并仅保留lazy compatibility wrappers。五类`*.v2` bounded snapshot、pre-output source/config/cache/DQ validation、timezone cutoff、semantic unique selection、no-fabricated Target、Paper source-state immutable + append-only post-state、common finite-date/null-preserving Performance及Target→Paper→Performance exact lineage均闭合；validator重验live commitments并逐byte重建全部输出。当前source-backed fixture为10 methods、Rebalance turnover=`0.4961400001`、Performance 3个共同return observations且DQ=`PASS_WITH_WARNINGS`；return/risk-adjusted winner=`consensus_target`，但reviewed observation priority仍选择`limited_adjustment`并明确`performance_winner_claimed=false`。Focused=`225 passed`、core positive/negative=`11 passed`、slice+CLI contract=`112 passed`、architecture-fitness=`269 passed`（`outputs/validation_runtime/architecture-fitness_20260713T194015Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T194221Z/test_runtime_summary.json`），generated=`901 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍41 root/291 group/993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。该fixture只证明链路可复算，不证明策略优越；固定research/paper-shadow/manual-only、no official/no auto/no order/no broker、`production_effect=none`。
- 2026-07-13：G2.4CH Dynamic-v3 System Target Portfolio and Paper Shadow Core contract freeze并进入`IN_PROGRESS`。范围固定为TRADING-209～213的Model Target 4、Paper Shadow Account 4、Model Rebalance 3、Paper Shadow Performance 3、System Target Review 3，共17 callback迁至`dynamic_v3_system_target_portfolio.py` canonical interface；Backfill/Rolling/Regime/Stability/Selection保持后续独立slice。审计确认旧Model Target按目录latest读取Daily/Monitor/Shortlist/Drift，不调用upstream validator、无timezone/cutoff/snapshot，并在candidate/consensus缺失时回退baseline造出consensus/top/equal；Paper Init吞掉missing Model Target并默认initial equity/weights；Rebalance直接读取并原地覆盖mutable paper state，无Paper/Target验证、chronology、duplicate target gate或pre/post state commitment，可用旧target回滚state；Performance虽执行DQ，但不冻结Paper/config/cache/DQ evidence，空样本与missing baseline经0参与relative/ranking，regime阈值未绑定本链reviewed policy；Review不验证三源或exact lineage/chronology，可组合无关Target/Paper/Performance，并把review preference直接写成recommended method。CH要求所有producer正式写件前完成timezone-aware cutoff和适用content-derived source validation，语义选择zero-or-one source并冻结config/source/cache/DQ、selection/calculation views到五类bounded `*.v2` snapshot。Model Target只用真实validated source cohort，无candidate/consensus时显式INSUFFICIENT，不补造研究方法权重；Paper Init冻结validated Target与account policy，默认值/方法集合/initial state全部source-exact；Rebalance要求Target日期不早于Paper state、方法/constraints唯一且exact，冻结preimage/postimage并拒绝同Paper+Target重复，materialized state只允许atomic forward transition；Performance按state生效日后的共同可用交易日、finite price和versioned cost/policy计算，missing/null不填0，DQ必须在正式output前PASS并冻结cache bytes；Review要求Target→Paper→Performance exact lineage与chronology，明确`REVIEWED_OBSERVATION_PRIORITY`不等于performance winner/official recommendation。五类validator重验live sources/config/cache并逐byte重建全部JSON/JSONL/Markdown/Reader Brief；invalid/missing/ambiguous/future/duplicate/rollback/cross-lineage/non-finite/tamper fail closed。固定research/paper-shadow/manual-only、no official target/no auto/no order/no broker、`production_effect=none`，不触发phase handoff。
- 2026-07-13：G2.4CG Dynamic-v3 Forward Pressure Evidence Automation slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。TRADING-194～198的15 callback已迁`dynamic_v3_forward_pressure.py` canonical interface；legacy root 21,696→21,286行、627→612函数、588→573 decorators。五类`*.v2` bounded snapshot与content-derived validator已闭合reviewed policy、timezone cutoff、Trigger正式写件前同源DQ、config/price/rates commitments与live recompute、Capture的Trigger验证和Tag→Backfill→Compare exact lineage、Ledger/Weekly的semantic cutoff selection及`source_mode+source_event_id` distinct-event计数；invalid/ambiguous/future/cross-lineage/duplicate/non-finite/tamper全部fail closed，显式DQ skip仅测试夹具并披露。累计focused=`606 passed`、当前slice+CLI=`117 passed`、architecture-fitness=`268 passed`（`outputs/validation_runtime/architecture-fitness_20260713T183548Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T183809Z/test_runtime_summary.json`），generated=`899 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。固定research-only/no-auto/no-policy/no-broker、`production_effect=none`。
- 2026-07-13：G2.4CF Dynamic-v3 Defensive Research Synthesis slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。TRADING-189～193的Deep Dive、Label Review、Failure Study、Research Note、Owner Pack共15 callback迁至`dynamic_v3_defensive_research.py` canonical interface；legacy root 22,069→21,696行、642→627函数、603→588 decorators，外部CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。五类producer的pre-output upstream content validation、timezone cutoff、same-lineage/chronology、bounded source/policy snapshot与五类validator的live-source/live-policy/逐byte重算闭合；missing/empty保持null，case按return/drawdown delta分supporting/contradicting/mixed，severity/label/owner options全部由reviewed `defensive_research_synthesis_v1`治理。当前fixture为3 simulation distinct events、0 forward/PIT events：2 supporting、1 contradicting、0 mixed，label=`POTENTIALLY_MISLEADING`，Research Note=`RESEARCH_ONLY/forward_support=NONE`。新增focused 11与CLI组合110并行通过，累计focused=`601 passed`；下游Forward Pressure链兼容=`5 passed`；architecture-fitness=`267 passed`（`outputs/validation_runtime/architecture-fitness_20260713T173934Z/test_runtime_summary.json`）；contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T174151Z/test_runtime_summary.json`）；generated=`897 modules / 1,114 tests / 858 direct writers / 0 violations`。固定`can_support_rule_approval=false`、`auto_apply=false`、`policy_change_allowed=false`、`production_effect=none`；TRADING-194～198保持后续独立slice。
- 2026-07-13：G2.4CE Dynamic-v3 Pressure Validation and Defensive Decision Update slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。15 callback迁canonical interface，legacy root 22,538→22,069行、657→642函数、618→603 decorators；累计focused=`590 passed`，CE及下游Defensive Evidence兼容回归=`20 passed`，architecture-fitness首次因实现后manifest freshness正确报警为265/266、刷新后同16-worker tier=`266 passed`（`outputs/validation_runtime/architecture-fitness_20260713T163727Z/test_runtime_summary.json`），contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T163450Z/test_runtime_summary.json`）；generated=`895 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。五类producer的pre-output validation、timezone/cutoff、semantic selection、bounded source/policy snapshot、live commitment及逐byte重算闭合；Backfill只纳入AVAILABLE/finite/unique outcome并同时计算distinct events，Compare要求全部configured regimes达到reviewed floor，Weekly按distinct forward events且严格校验Backfill→Compare→Review lineage。当前fixture仅2 simulation/0 forward/0 historical events，总体`NOT_PROVEN_DEFENSIVE`与`continue_tracking`；旧116/87 row artifact无CE snapshot，仅作legacy观察。Pressure Capture下游改为显式传递三类source roots，测试不再篡改Backfill产物。`rule_approval_allowed=false`、`auto_apply=false`、`production_effect=none`。
- 2026-07-13：G2.4CE Dynamic-v3 Pressure Validation and Defensive Decision Update contract freeze并进入`IN_PROGRESS`。范围固定为Pressure Tag Diagnosis、Pressure Outcome Backfill、Defensive Pressure Compare、Defensive Rule Review、Weekly Ops Decision Update各`run/report/validate`，共15 callback迁至`dynamic_v3_pressure_validation.py` canonical interface，后续Defensive Evidence链保持独立。审计发现旧链读取mutable latest且不校验timezone/cutoff或调用适用upstream content validator，无versioned input snapshot；Diagnosis可把未验证historical/simulation inventory计入mapping；Backfill会扫描未验证/future/duplicate outcome，并把非AVAILABLE或缺variant的数据计作defensive relevant；Compare用`_float/_avg`把missing/non-finite/空cohort填0，单event即可经隐式边界标`PROVEN_DEFENSIVE`，sample floor/return/drawdown/win-rate要求未绑定reviewed policy；Rule Review不校验Compare且把硬编码要求写成owner evidence；Weekly Update只读三个summary，不校验Weekly→Backfill→Compare→Review lineage/chronology，还用广义异常把invalid Pressure source降为0。CE要求任何正式output前完成timezone-aware cutoff、semantic source selection、适用的content-derived consumer validation与bounded commitments；五阶段各自保存versioned input snapshot，validator重验live source/policy并逐byte重算全部views/report。只纳入`AVAILABLE`、finite、identity唯一、同event/window成对的variant evidence；missing/null不转0；reviewed policy治理regimes/windows/distinct-event floor及return/drawdown/win-rate边界，simulation/historical只作research evidence，永不自动批准rule。Weekly Update要求same Backfill与Compare→Review lineage、生成时间递增，invalid optional source fail closed。默认manual/research-only，`production_effect=none`，不改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4CD Dynamic-v3 Confirmation Operations slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。16 callback迁canonical interface module，legacy root 22,981→22,538行、673→657函数、634→618 decorators；累计focused=`580 passed`，architecture-fitness=`265 passed`（`outputs/validation_runtime/architecture-fitness_20260713T152448Z/test_runtime_summary.json`），contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T152711Z/test_runtime_summary.json`），generated=`894 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Plan/Weekly/Pressure/Dashboard/Queue的timezone-aware cutoff、semantic selection、bounded source commitments、live-source与逐byte view重算闭合；Weekly严格区分optional absence与invalid source，Pressure的DQ临时preflight失败不留正式目录且PENDING outcome不计防御证据，Dashboard不以Pressure覆盖Progress readiness，Queue不接受跨Cycle同target decision。深链并行回归实际为Weekly 1 passed/1,184.39s、其余6 passed/763.60s，耗时来自重复上游承诺哈希，已列入“按source hash+validator version复用验证证据”的优化空间，不削弱现有gate。默认dry-run，`production_effect=none`。
- 2026-07-13：G2.4CD Dynamic-v3 Confirmation Operations contract freeze并进入`IN_PROGRESS`。范围固定为Confirmation Cycle 6个、Pressure Regime Tag 4个、Confirmation Dashboard 3个、Rule Review Queue 3个，共16个callback迁至`dynamic_v3_confirmation_operations.py`，后续slice保持独立。审计发现旧Weekly仅校验schedule config且浅validator不重验各step artifact；Dashboard用广义`except Exception`把invalid/missing source都静默降为空、读取mutable latest且以Pressure latest覆盖Progress的defensive available count；Queue直接读取mutable owner journal，只按target id匹配而不约束同Cycle，可把其他Cycle decision误记为reviewed；Pressure先创建output再做DQ、扫描未验证Advisory Outcome且无timezone/cutoff/source snapshot。CD要求所有producer在任何正式output前校验timezone-aware cutoff及适用source content-derived PASS，latest按semantic generated time/id与cutoff确定，invalid relevant source fail closed；以versioned bounded input snapshot冻结config/source commitments、validation和lineage。Weekly冻结并重验step artifact chain且不把可选forward decision异常一概吞掉；Dashboard只投影validated Progress/Evaluation/Rule Review/Queue/Pressure，不覆盖Progress readiness，missing明确为absent而invalid为FAIL；Queue只接受同source Cycle、合法event chain与cutoff内final owner decision；Pressure用临时preflight完成DQ，PASS后才原子落正式DQ evidence，并冻结config/price/rates及validated outcome commitments。Plan及四类operations validator须重验live sources/config并逐字节重算全部views/report；legacy unsnapshotted artifact只读warning，不参与新结论。仅生成operations evidence，默认dry-run，不改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4CC Dynamic-v3 Rule Owner Decision Integration slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。5 callback已迁`dynamic_v3_rule_owner_decision.py`，legacy root 23,136→22,981行、678→673函数、639→634 decorators；focused累计=`578 passed`，architecture-fitness=`264 passed`（`outputs/validation_runtime/architecture-fitness_20260713T133137Z/test_runtime_summary.json`），contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T133559Z/test_runtime_summary.json`），generated=`893 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Rule Review pre-output PASS/cutoff、`rule_owner_decision_source_snapshot.v2` bounded commitments、exact scope/eligibility、每Cycle唯一decision、append-only create/record event SHA-256 chain、pending单次final/严格chronology、legacy只读及全部materialized views/live Cycle byte重算闭合。当前Cycle为`CONTINUE_TRACKING`且0 owner action，scope显式为全部targets/`ALL_CYCLE_TARGETS_NO_READY_OWNER_ACTION`，不可记录`approve_manual_policy_review`。普通hash链不等同身份签名，后续可增加owner identity/signature/trusted timestamp与显式legacy migration。首次architecture run因CB历史hash未声明本slice更新的Reader Brief回归而1 fail/263 pass；明确superseded path后同16-worker tier重跑264 pass，未以serial替代。仅manual owner record，不改policy/config/portfolio/production/order/broker；Confirmation Operations仍是下一独立slice。
- 2026-07-13：G2.4CC Dynamic-v3 Rule Owner Decision Integration contract freeze并进入`IN_PROGRESS`。范围固定为`rule-owner-decision create/list/record/report`与`validate-rule-owner-decision`共5 callback，迁至`dynamic_v3_rule_owner_decision.py`；Confirmation Operations保持后续独立slice。旧producer不调用Rule Review content-derived validator，无timezone/cutoff、immutable source snapshot或duplicate-cycle gate；0 owner-action时隐式fallback全部targets。Record原地重写journal状态行，可重复覆盖final decision/notes且无严格chronology；journal无event identity、previous/event hash chain或source-cycle commitment。Report用全局mutable Markdown且忽略传入decision id，validator仅查row/enum/safety，不重验Cycle、不校验target scope、decision eligibility、duplicate/chronology/chain，也不重算record/manifest/Markdown bytes。本slice要求任何output前Rule Review PASS/time cutoff，并以`rule_owner_decision_source_snapshot.v2`冻结bounded Cycle file commitments、validation、target exact scope与allowed decisions；同cycle只允许一个decision。Journal使用create/record append-only semantic events与SHA-256 chain，PENDING只允许一次final transition且事件时间严格递增；仅Cycle含`READY_FOR_OWNER_REVIEW` target时允许`approve_manual_policy_review`，该值也只批准开启人工review，不批准policy change。Materialized record/manifest/per-decision report/global report完全从snapshot+events重算，validator重验live Cycle、snapshot/chain/eligibility/chronology及全部bytes。允许owner显式记录，不自动运行review/change，不改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4CB Dynamic-v3 Rule Review Cycle slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁`dynamic_v3_rule_review_cycle.py`；legacy root 23,246→23,136行、681→678函数、642→639 decorators；累计focused=`570 passed`，architecture-fitness=`263 passed`（`outputs/validation_runtime/architecture-fitness_20260713T082125Z/test_runtime_summary.json`），contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T082335Z/test_runtime_summary.json`），generated=`892 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Registry/Progress/Evaluation三源pre-output validation/cutoff、严格lineage/chronology/exact target coverage、`rule_review_cycle_input_snapshot.v2` bounded commitments、source-action-preserving generic owner review、progress/evaluation分离统计及live sources/五类output byte validator闭合。bounded修复后四层snapshot约18KB/28KB/14KB/37KB；首次0.89GB/2.37GB/6.50GB递归膨胀失败证据保留，修复后同16-worker parallel通过，未以serial替代。仅manual Rule Review evidence，不创建Owner Decision、不改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4CB实现审计发现BY/BZ/CA的parsed+raw full-bundle snapshot存在递归指数膨胀：当前fixture Registry/Progress/Evaluation input snapshot约0.89GB/2.37GB/6.50GB，CB首次16-worker parallel focused在写新snapshot时`MemoryError`。该失败不是phase blocker，也不以串行或删验证绕过；CB扩展为统一bounded full-byte commitment bundle：每个canonical source file冻结path/size/SHA-256，计算必需views独立解析，live validator逐文件重算commitment并维持content-derived PASS/cutoff/lineage。目标是保留全字节漂移检测且将snapshot增长由递归指数改为线性；首次失败证据保留，修复后仍用16-worker parallel配置重跑。
- 2026-07-13：G2.4CB Dynamic-v3 Rule Review Cycle contract freeze并进入`IN_PROGRESS`。范围固定为`rule-review-cycle run/report`与`validate-rule-review-cycle`共3 callback，迁至`dynamic_v3_rule_review_cycle.py`；Owner Decision Journal及Confirmation Operations保持后续独立slice。旧producer直接读取Registry/Progress/Evaluation mutable views，不调用三个content-derived validator，无timezone/cutoff、Registry→Progress→Evaluation lineage/chronology gate或input snapshot，缺失target会被空mapping/default降为CONTINUE_TRACKING；它还按`defensive_limited_adjustment_drawdown` target id硬编码RENAME，否则FAILURE固定TIGHTEN，未直接保留source failure actions。旧report把Evaluation SUCCESS数量误标为ready-for-evaluation；浅validator只查四文件、decision enum与安全字段，不重验上游、不重算matrix/manifest/Markdown/Reader Brief。本slice要求任何output前Registry/Progress/Evaluation均content-derived PASS、timezone-aware cutoff、严格id/target exact coverage与Registry→Progress→Evaluation lineage/生成时间顺序，并以`rule_review_cycle_input_snapshot.v2`冻结三个full bundles/validations。Decision仅由frozen target status、progress/evaluation状态推导：watch-only=`KEEP_REFERENCE_ONLY`；active NOT_READY=`CONTINUE_TRACKING`；MIXED/REVIEW_REQUIRED=`DEFER`；SUCCESS或FAILURE均只解锁generic `READY_FOR_OWNER_REVIEW`，同时逐值携带criteria/failure condition/action，不以target id或新threshold替代source action。Validator重验live sources并逐字节重算snapshot、matrix、manifest、report与Reader Brief；报告分别披露progress-ready、success、failure、review-required样本。仅生成manual rule-review evidence，`policy_change_allowed=false`，不创建/记录owner decision、不改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4CA Dynamic-v3 Success/Failure Evaluator slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁`dynamic_v3_confirmation_evaluation.py`；legacy root 23,336→23,246行、684→681函数、645→642 decorators；CA focused/downstream=`13 passed`（Evaluation 5、Progress/Rule Review 6、Dashboard/Queue 2），architecture-fitness=`262 passed`（`outputs/validation_runtime/architecture-fitness_20260713T064729Z/test_runtime_summary.json`），contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T064937Z/test_runtime_summary.json`），generated=`891 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Progress pre-output PASS/time cutoff、`confirmation_evaluation_input_snapshot.v2` full bundle/validation、NOT_READY零partial evaluation、READY source-exact finite criteria、all-pass/no-failure SUCCESS、source-criterion-bound failure及全view/live-source byte validator闭合；旧evaluation/failure双轨helpers已删除。当前fixture为1个limited target、1 NOT_READY、0 success/failure。首次architecture与重型focused并发导致parallel worker `MemoryError`（`architecture-fitness_20260713T062654Z`）；保留失败证据后按同16-worker parallel配置资源隔离重跑通过，未用serial替代。仅manual evaluation evidence，不运行Rule Review或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4CA Dynamic-v3 Success/Failure Evaluator contract freeze并进入`IN_PROGRESS`。范围固定为`confirmation-evaluate run/report`与`validate-confirmation-evaluate`共3 callback，迁至`dynamic_v3_confirmation_evaluation.py`；Rule Review及后续链保持独立slice。旧producer直接读取mutable Progress，不调用Progress content-derived validator，无timezone/cutoff或immutable snapshot，且先创建output；它在Progress未READY时仍逐指标计算PASS/FAIL，可能把partial evidence泄漏为正面结论。Registry failure conditions在BY后是source-exact list，旧`_mapping`读取会静默变空；旧failure helper又以隐式0判断underperformance/drawdown，未绑定source criteria。浅validator只查四文件、status/not-ready/安全字段，不重验Progress、不重算criteria/failures/summary/manifest/Markdown。本slice要求任何output前Progress validator PASS、timezone-aware cutoff，并以`confirmation_evaluation_input_snapshot.v2`冻结Progress full bundle/validation/lineage。Progress非`READY_FOR_EVALUATION`时criteria全部输出`NOT_EVALUATED`、actual=null且failure触发为空；只有READY时才逐值使用Progress冻结的success criteria和finite metrics。Criteria必须非空、唯一且仅接受`_min/_max` operator；unknown/missing/非finite保持`INSUFFICIENT_DATA/REVIEW_REQUIRED`，全部PASS且无failure才SUCCESS，任一FAIL或failure trigger才FAILURE，单指标PASS不得SUCCESS。Failure list必须完整保留condition/action；condition-to-criterion adapter只使用同target source criteria的required边界，不另造0/threshold，unknown condition fail closed。Validator重验live Progress并逐字节重算snapshot、JSONL、summary、manifest和Markdown。仅生成manual evaluation evidence，不运行Rule Review、不改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4BZ Dynamic-v3 Confirmation Progress Tracker slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁`dynamic_v3_confirmation_progress.py`；legacy root 23,439→23,336行、687→684函数、648→645 decorators；focused confirmation/downstream=`10 passed`、final Progress hardening=`4 passed`、architecture-fitness=`261 passed`（`outputs/validation_runtime/architecture-fitness_20260713T032814Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T033023Z/test_runtime_summary.json`），generated=`890 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Registry/evidence pre-output validation、semantic cutoff selection、`confirmation_progress_input_snapshot.v2` full bundles/validations、source-exact windows/events、cross-window conservative unique count、missing-null/pressure-tag insufficiency和全view/live-source byte validator闭合；旧mutable-latest/default/80% near-ready双轨helpers已删除。当前source-backed fixture只有1个limited target且0 available event，正确输出`INSUFFICIENT_EVENTS`/0 ready。仅manual progress observation，不运行Evaluation或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BZ Dynamic-v3 Confirmation Progress Tracker contract freeze并进入`IN_PROGRESS`。范围固定为`confirmation-progress update/report`与`validate-confirmation-progress`共3 callback，迁至`dynamic_v3_confirmation_progress.py`；Evaluation及后续链保持独立slice。旧producer直接读取mutable Registry和limited/consensus latest，不调用三个content-derived validators，无timezone/cutoff、deterministic source selection或immutable snapshot；以target id硬编码source分支和10/5事件fallback，跨window求和可重复计数，defensive可用数固定0，unknown target误走consensus，missing指标可经默认值参与状态。浅validator只查文件、status、event floor与summary count，不重验live sources或重算rows/summary/manifest/Markdown。本slice要求任何output前Registry PASS/time cutoff；只为Registry真实targets选择其明确支持的zero-or-one validated evidence source，按semantic generated time/id而非mtime选择，并冻结Registry、selected evidence full bundles/validations及selection inventory到`confirmation_progress_input_snapshot.v2`。Events/windows/criteria逐值继承Registry，unknown target/source mapping、duplicate identity、future/invalid source fail closed；available count必须是同target/window的unique AVAILABLE finite event，不跨window重复累计，missing/pressure tags不足保持null/`INSUFFICIENT_EVENTS`，不得补造0或READY。Validator重验live Registry/evidence并逐字节重算snapshot、JSONL、summary、manifest和Markdown。仅生成manual progress observation，不运行Evaluation、不改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4BY Dynamic-v3 Confirmation Targets Registry slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。4 callback迁`dynamic_v3_confirmation_targets.py`；legacy root 23,584→23,439行、691→687函数、652→648 decorators；累计focused=`540 passed`、architecture-fitness=`260 passed`（`outputs/validation_runtime/architecture-fitness_20260713T015300Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T015529Z/test_runtime_summary.json`），generated=`889 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Register要求Plan validated/AVAILABLE/cutoff，以`confirmation_registry_input_snapshot.v2`冻结full Plan bundle/validation、registry preimage/postimage。只注册Plan真实target并保留source target/criteria/failure，unknown status、duplicate target/Plan fail closed；artifact与materialized registry使用atomic writer。Validator重验live Plan并逐字节重算JSON/YAML/report，当前materialized registry drift阻断。Fixture只注册1个limited target。仅显式manual registry mutation，不运行Progress/Evaluation或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BY Dynamic-v3 Confirmation Targets Registry contract freeze并进入`IN_PROGRESS`。范围固定为`confirmation-targets register/list/report`与`validate-confirmation-targets`共4 callback，迁至`dynamic_v3_confirmation_targets.py`；Progress及后续链保持独立slice。旧register直接读取手工/mutable Plan三文件，不调用Plan validator，无timezone/cutoff/full snapshot；固定要求三个历史target，以target id硬编码active/watch状态，并用`_text/_int`默认为MEDIUM/0。它在内容验证前创建artifact并无条件覆盖全局registry YAML；同一Plan可重复注册。浅validator只查三文件、固定三target/id/safety，不重验Plan、不重算registry/manifest/report或检测source/materialized drift。本slice要求任何写件前Plan content-derived PASS且status=`AVAILABLE`、timezone-aware cutoff；冻结full Plan bundle/validation、lineage及registry preimage到versioned snapshot。只逐值注册Plan真实targets，status由source tracking status严格映射，events/windows/criteria/failure完整保留，missing/duplicate/unknown status或同Plan重复注册fail closed，不补造target/default。Artifact views全部预计算；materialized registry使用canonical atomic writer且记录registry id。Validator重验live Plan并逐字节重算snapshot/registry/manifest/report；当materialized id仍指向本artifact时必须byte-equal，已被新registry显式supersede时只验证冻结postimage。仅显式注册manual tracking state，不运行Progress/Evaluation或改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4BX Dynamic-v3 Forward Confirmation Plan slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁`dynamic_v3_forward_confirmation_plan.py`；legacy root 23,688→23,584行、694→691函数、655→652 decorators；累计focused=`529 passed`、architecture-fitness=`259 passed`（`outputs/validation_runtime/architecture-fitness_20260713T005502Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T005710Z/test_runtime_summary.json`），generated=`888 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Producer要求Proposal Review/Forward Bridge validated/cutoff/same-Calibration，以`forward_confirmation_plan_input_snapshot.v2`冻结full bundles/validations+reviewed semantic policy。Plan只投影真实proposal解锁的Bridge target，events/windows/criteria逐值继承；空/无匹配为INSUFFICIENT_DATA，fixture只输出1个limited target，不补造defensive/consensus或threshold。Validator重验live sources/policy并逐字节重算全部JSON/Markdown。只生成non-PIT manual tracking plan，不注册target、不运行Forward或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BX Dynamic-v3 Forward Confirmation Plan contract freeze并进入`IN_PROGRESS`。范围固定为`forward-confirmation-plan run/report`与`validate-forward-confirmation-plan`共3 callback，迁至`dynamic_v3_forward_confirmation_plan.py`；Confirmation Targets注册及后续链保持独立slice。旧run直接读取Proposal Review与Forward Bridge mutable views，不调用两个validator，无timezone/cutoff、same-Calibration lineage或input snapshot；会补造Bridge不存在的`consensus_target_risk`，并以10/5 events、0.55/0.50/0阈值及固定windows/actions作为fallback。浅validator只查六文件、三类固定target与safety，不重验sources/policy、不重算targets/triggers/failures/manifest/Markdown。本slice要求任何output前两个source content-derived PASS、timezone-aware cutoff与same-Calibration lineage，以`forward_confirmation_plan_input_snapshot.v2`冻结full bundles/validations及reviewed semantic policy。Plan只能投影Bridge真实target，并由真实Proposal Review proposal映射解锁；events/windows/numeric criteria逐值继承Bridge冻结policy，空proposal/无匹配target为`INSUFFICIENT_DATA`，不得补造target、confidence或数值。Failure/action mapping来自versioned policy且必须完整覆盖被选target。Validator重验live sources/policy并逐字节重算全部JSON/Markdown/Reader Brief；仅写non-PIT manual tracking plan，不注册target、不运行Forward、不改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4BW Dynamic-v3 Advisory Proposal Review slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁`dynamic_v3_advisory_proposal_review.py`；legacy root 23,815→23,688行、697→694函数、658→655 decorators；累计focused=`514 passed`、architecture-fitness=`258 passed`（`outputs/validation_runtime/architecture-fitness_20260713T000825Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260713T001033Z/test_runtime_summary.json`），generated=`887 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Producer要求四source validated/cutoff/same-Outcome、reviewed policy与`advisory_proposal_review_input_snapshot.v2` full bundles/validations。Matrix只映射真实Calibration proposals；空proposal为INSUFFICIENT_DATA，不补造confidence，decision/conditions只来自policy。Validator重验live sources/policy并逐字节重算全部JSON/Markdown。只生成non-PIT manual review，不运行Forward或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BW Dynamic-v3 Advisory Proposal Review contract freeze并进入`IN_PROGRESS`。范围固定为`advisory-proposal-review run/report`与`validate-advisory-proposal-review`共3 callback，迁至`dynamic_v3_advisory_proposal_review.py`；Forward Confirmation及后续链保持独立slice。旧run直接读取Interpretation/Risk Return/Defensive/Calibration的mutable views，不调用四个validator，无timezone/cutoff、same-Outcome lineage、full bundle snapshot或review policy；Calibration无proposal时会伪造`require_forward_confirmation`，decision/conditions含硬编码映射和0.55/0阈值，missing confidence默认MEDIUM。浅validator只查五文件、decision/safety，不重验sources/policy、不重算matrix/checklist/report/Reader Brief/manifest。本slice要求pre-output四source content-derived PASS/time/same-Outcome lineage，冻结四个full bundles/validations与reviewed proposal policy；不得伪造proposal或confidence，空proposal显式INSUFFICIENT_DATA。Decision与conditions只来自versioned policy，所有结果仍为manual observation/owner review。Validator重验live sources/policy并逐字节重算snapshot及全部views；仅non-PIT review，不运行Forward Confirmation或改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4BV Dynamic-v3 Simulation Defensive Validation slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁`dynamic_v3_sim_defensive_validation.py`；legacy root 23,911→23,815行、700→697函数、661→658 decorators；累计focused=`500 passed`、architecture-fitness=`257 passed`（`outputs/validation_runtime/architecture-fitness_20260712T233711Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T233915Z/test_runtime_summary.json`），generated=`886 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Producer要求validated/cutoff Outcome和reviewed policy，以`sim_defensive_validation_input_snapshot.v2`冻结full bundle/validation/lineage/policy。Matrix只用same-regime/event/window AVAILABLE finite defensive/no_trade pairs并披露paired units；missing为null/INSUFFICIENT，best variant只在共同cohort排名；policy治理regimes/windows/sample floor/boundaries。Validator重验live Outcome/policy并逐字节重算全views。只生成non-PIT diagnostic，不运行Proposal或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BV Dynamic-v3 Simulation Defensive Validation contract freeze并进入`IN_PROGRESS`。范围固定为`sim-defensive-validation run/report`与`validate-sim-defensive-validation`共3 callback，迁至`dynamic_v3_sim_defensive_validation.py`；Proposal Review及后续链保持独立slice。旧run直接读取mutable Outcome manifest/windows，不调用Outcome validator，无timezone/cutoff或input snapshot；regime matrix把missing/non-finite return、relative和drawdown经`_float/_avg`填0，defensive与no_trade的event/window cohort不严格对齐，sample_count按仅有defensive row的distinct event计算，best variant也跨不完整cohort；pressure sample floor=5和边界未进入governed policy。浅validator只查五文件、regime/status/safety，不重验source/policy、不重算matrix/failure/summary/manifest/Markdown。本slice要求任何output前Outcome content-derived PASS/time cutoff，冻结full Outcome bundle/validation及reviewed defensive policy；只使用同regime+event+window的AVAILABLE finite defensive/no_trade pairs，严格披露paired event/window counts，missing保持null/INSUFFICIENT。Pressure regimes、tracked windows、sample floor及return/drawdown boundaries来自versioned policy。Validator重验live Outcome/policy并逐字节重算snapshot、matrix、failure cases、summary、manifest和Markdown；仅non-PIT diagnostic，不运行Proposal Review或改policy/config/portfolio/production/order/broker，不触发phase handoff。
- 2026-07-13：G2.4BU Dynamic-v3 Simulation Risk-Return slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁`dynamic_v3_sim_risk_return.py`；legacy root 23,994→23,911行、703→700函数、664→661 decorators；累计focused=`486 passed`、architecture-fitness=`256 passed`（`outputs/validation_runtime/architecture-fitness_20260712T231001Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T231207Z/test_runtime_summary.json`），generated=`885 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Producer要求pre-output Outcome content-derived PASS/time cutoff，以`sim_risk_return_input_snapshot.v2`冻结full bundle/validation/lineage。Tradeoff仅用same-event 20d AVAILABLE finite active/no_trade pairs并披露paired units；missing和不可定义ratio为null/INSUFFICIENT。Validator重验live Outcome并逐字节重算snapshot/CSV/summary/manifest/Markdown。只生成non-PIT diagnostic，不运行Defensive或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BU Dynamic-v3 Simulation Risk-Return contract freeze并进入`IN_PROGRESS`。范围固定为`sim-risk-return run/report`与`validate-sim-risk-return`共3 callback，迁至`dynamic_v3_sim_risk_return.py`；Defensive Validation及后续链保持独立slice。旧run不调用Outcome validator，无timezone/source cutoff或input snapshot；tradeoff从各variant非同cohort aggregate summary相减，missing metrics经`_float`/0 default与`_empty_risk_return_row`变成全零，zero denominator ratio也会被误写0。这会把“无样本”误解为“无收益/无回撤/无周转代价”。浅validator只查四文件、active variants/status/字段与safety，不重验source、不重算CSV/summary/manifest/Markdown或检测tamper。本slice要求任何output前Outcome content-derived PASS、timezone-aware generated cutoff，并以versioned snapshot冻结full Outcome bundle/validation。Tradeoff只在同`sim_event_id+20d window`的AVAILABLE finite variant/no_trade pair上计算return/drawdown/turnover delta，披露paired distinct-event/window counts；missing与不可定义ratio保持null/`INSUFFICIENT_DATA`，不得填0或跨cohort。Validator重验live Outcome并逐字节重算snapshot、CSV、summary、manifest与Markdown，source/snapshot/output tamper均FAIL。仅生成non-PIT risk-return diagnostic，不运行Defensive或改policy/config/portfolio/production/order/broker，不触发phase-level ARCH-005 handoff。
- 2026-07-13：G2.4BT Dynamic-v3 Simulation Interpretation slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 24,096→23,994行、706→703函数、667→664 decorators；累计focused=`475 passed`、architecture-fitness=`255 passed`（`outputs/validation_runtime/architecture-fitness_20260712T224036Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T224249Z/test_runtime_summary.json`），generated=`884 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Producer要求pre-output Outcome/Calibration/Bridge content-derived PASS/time/same-Outcome lineage，以`sim_interpretation_input_snapshot.v2`冻结full bundles/validations。Matrix只用same-event/window AVAILABLE finite pairs，missing为null/INSUFFICIENT；findings confidence从真实证据推导，tracking plan不写成forward success。Validator重验live三source并逐字节重算全JSON/Markdown。只生成non-PIT interpretation，不运行Risk Return或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BT Dynamic-v3 Simulation Interpretation contract freeze并进入`IN_PROGRESS`。范围固定为`sim-interpretation run/report`与`validate-sim-interpretation`共3 callback，迁至`dynamic_v3_sim_interpretation.py`；Risk Return及后续链保持独立slice。旧run直接读Outcome/Calibration/Forward Bridge的mutable views，不调用三个validator，无timezone/source cutoff、Outcome→Calibration→Bridge lineage或input snapshot。Matrix/findings把missing metrics经`_float`/0 default变成“与no_trade相似”，empty evidence的drawdown/consensus/defensive/forward finding仍可得MEDIUM/HIGH confidence；特别是BS的`TRACKING_PLAN_ONLY`可被误解为forward confirmation已成立。浅validator只查四文件、variant字段、report label与safety，不重验source、不重算matrix/findings/manifest/Markdown或检测tamper。本slice要求任何output前Outcome/Calibration/Bridge三source content-derived PASS、timezone-aware generated cutoff和严格同Outcome lineage，并以versioned snapshot冻结三个full bundles/validations。Return/drawdown/turnover比较只使用AVAILABLE finite same-cohort metrics，missing保持null/`INSUFFICIENT_DATA`；confidence必须由可用证据推导，不得硬编码冒充完整性。Forward target只能解释为`TRACKING_REQUIRED`，不声明forward success/defensive proof/production candidate。Validator重验live三source并逐字节重算snapshot/matrix/findings/manifest/Markdown，source/snapshot/output tamper均FAIL。仅生成non-PIT interpretation，不运行后续链、不改policy/config/portfolio/production/order/broker，不触发phase-level ARCH-005 handoff。
- 2026-07-13：G2.4BS Dynamic-v3 Backtest Simulation Forward Bridge slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 24,180→24,096行、709→706函数、670→667 decorators；累计focused=`464 passed`、architecture-fitness=`254 passed`（`outputs/validation_runtime/architecture-fitness_20260712T221202Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T221408Z/test_runtime_summary.json`），generated=`883 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Producer要求pre-output Calibration content-derived PASS/time cutoff，以`backtest_sim_forward_bridge_input_snapshot.v2`冻结full bundle/validation/lineage及reviewed policy/governance。Events/windows/criteria strict no-fallback，target仅`TRACKING_REQUIRED`/`TRACKING_PLAN_ONLY`；validator重验live Calibration并逐字节重算全部JSON/Markdown/Reader Brief。只生成non-PIT manual observation plan，不运行后续链、不改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BS Dynamic-v3 Backtest Simulation Forward Bridge contract freeze并进入`IN_PROGRESS`。范围固定为`backtest-sim forward-bridge/forward-bridge-report`与`validate-backtest-sim-forward-bridge`共3 callback，迁至`dynamic_v3_backtest_sim_forward_bridge.py`；Calibration后续链保持独立slice。旧producer不调用Calibration validator、不校验timezone/generated cutoff，无input snapshot；它从mutable evidence/proposal直接生成targets，policy缺失时默认为10 events/0.55/0/0，windows固定为`[1,5,10,20]`，容易把未就绪Calibration误解为已确认计划。浅validator只查五文件、target非空和safety，不重验source、不重算snapshot/targets/questions/manifest/Markdown/Reader Brief。本slice要求任何output前timezone-aware cutoff与Calibration content-derived PASS，以`backtest_sim_forward_bridge_input_snapshot.v2`冻结Calibration full bundle/validation/lineage及reviewed forward policy。Required events、tracked windows、win-rate/return/drawdown criteria必须来自frozen policy且完整、唯一、finite；missing/invalid直接fail closed，禁止数值fallback。Targets只标记`TRACKING_REQUIRED`并披露source calibration readiness/metric availability，不声明forward success或production candidate。Validator重验live Calibration并逐字节重算snapshot、targets、questions、manifest、Markdown与Reader Brief，source/snapshot/policy/output tamper均FAIL。仅写non-PIT forward observation/review plan，不运行下游、不修改policy/config/portfolio/production/order/broker，不触发phase-level ARCH-005 handoff。
- 2026-07-13：G2.4BR Dynamic-v3 Backtest Simulation Calibration Pack slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 24,296→24,180行、712→709函数、673→670 decorators；累计focused=`451 passed`、architecture-fitness=`253 passed`（`outputs/validation_runtime/architecture-fitness_20260712T213807Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T214009Z/test_runtime_summary.json`），generated=`882 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`d4744f3e...bc6f3`。Producer要求pre-output四source content-derived PASS/cutoff，以`backtest_sim_calibration_input_snapshot.v2`冻结full bundles/validations及cross-lineage。Evidence missing为null；positive proposal仅LOW_RISK+finite/positive，其他状态只保留manual/forward confirmation。Validator重验四个live source并逐字节重算全部JSON/Markdown/Reader Brief，tamper FAIL。只生成non-PIT calibration review，不自动运行bridge，不改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BR Dynamic-v3 Backtest Simulation Calibration Pack contract freeze并进入`IN_PROGRESS`。范围固定为`backtest-sim calibration-pack/calibration-report`与`validate-backtest-sim-calibration`共3 callback，迁至`dynamic_v3_backtest_sim_calibration.py`；Forward Bridge及后续链保持独立slice。旧run直接读取Outcome/Paper/Regime/Sensitivity各一两个mutable view，不调用四个validator，无timezone/source chronology与cross-lineage gate，也无input snapshot；missing evidence/paper metrics写0，`REVIEW_REQUIRED`仍可能生成positive keep-rule proposal。浅validator只查文件/auto-apply/safety，不重验sources、不重算evidence/proposals/limitations/manifest/Markdown/Reader Brief或检测tamper。本slice要求pre-output timezone与四source content-derived PASS/cutoff；四份full bundle/validation冻结到`backtest_sim_calibration_input_snapshot.v2`，并校验同一Outcome/variant/event lineage。Evidence只保留finite source metrics，missing为null并披露source readiness；positive rule proposal仅在Sensitivity=`LOW_RISK`且对应metric finite/positive时允许，其他状态只保留forward-confirmation/manual-review，不自动apply。Validator重验四个live source并逐字节重算snapshot、evidence、proposals、limitations、manifest、Markdown与Reader Brief，source/snapshot/output tamper FAIL。只生成non-PIT calibration review pack，不运行bridge或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BQ Dynamic-v3 Backtest Simulation Sensitivity slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 24,394→24,296行、715→712函数、676→673 decorators；累计focused=`437 passed`、architecture-fitness=`252 passed`（`outputs/validation_runtime/architecture-fitness_20260712T211201Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T211402Z/test_runtime_summary.json`），generated=`881 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`17b74072...90070`。Producer要求pre-output Outcome content-derived PASS/source chronology并以`backtest_sim_sensitivity_input_snapshot.v2`冻结full Outcome bundle/validation和唯一variant/event lineage。四类grid exact/unique，只纳入AVAILABLE finite；missing avg/spread为null，event/window/result/excluded单位分开，threshold缺dispersion显式排除；strong calibration仅LOW_RISK允许且不自动apply。Validator重验live Outcome并逐字节重算全部JSON/Markdown，tamper FAIL。只生成non-PIT sensitivity diagnostics，不自动运行calibration/bridge，不改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BQ Dynamic-v3 Backtest Simulation Sensitivity contract freeze并进入`IN_PROGRESS`。范围固定为`backtest-sim sensitivity-run/sensitivity-report`与`validate-backtest-sim-sensitivity`共3 callback，迁至`dynamic_v3_backtest_sim_sensitivity.py`；Calibration及后续链保持独立slice。旧run不验证/freeze Outcome source，无timezone/source chronology gate；另外从event目录与live price cache独立取数，形成与Outcome evidence可能分叉的mutable lineage。四类grid在空样本时用0表示avg/spread，threshold缺`consensus_max_symbol_dispersion`时会静默当作通过；sample row/window/event单位未分离，`REVIEW_REQUIRED`仍允许strong calibration。浅validator只查文件/status/safety，不重验source、不重算frequency/adjustment/shortlist/threshold/warnings/manifest/Markdown或检测tamper。本slice要求pre-output timezone、Outcome content-derived PASS/source cutoff，冻结full outcome bundle/validation；event/config/price/DQ只能从该冻结lineage解析并由live Outcome validator复核。四类policy grid必须exact/unique，只有AVAILABLE且required metrics finite的rows参与；missing样本均值/spread保持null并披露missing/excluded counts，缺dispersion不得静默通过。Event/window/result row单位分开；strong calibration仅在`LOW_RISK`允许，且仍不得自动apply。Validator逐字节重算snapshot、四类diagnostics、warnings、manifest和Markdown，source/snapshot/output tamper FAIL。只生成non-PIT sensitivity diagnostics，不运行calibration/bridge或改policy/config/portfolio/production/order/broker。
- 2026-07-13：G2.4BP Dynamic-v3 Backtest Simulation Regime Review slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 24,483→24,394行、718→715函数、679→676 decorators；累计focused=`423 passed`、architecture-fitness=`251 passed`（`outputs/validation_runtime/architecture-fitness_20260712T203937Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T204138Z/test_runtime_summary.json`），generated=`880 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`41822fdf...e22dc`。Producer要求pre-output Outcome content-derived PASS/source chronology并以`backtest_sim_regime_input_snapshot.v2`冻结full outcome/validation bundle；regime×variant只聚合AVAILABLE finite windows，event/window count分开，missing metrics保持null且不参与ranking。Validator重验live Outcome并逐字节重算snapshot/inventory/metrics/summary/manifest/Markdown，tamper FAIL。只生成non-PIT regime diagnostics，不自动运行sensitivity/calibration，不改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BP Dynamic-v3 Backtest Simulation Regime Review contract freeze并进入`IN_PROGRESS`。范围固定为`backtest-sim regime-review/regime-report`与`validate-backtest-sim-regime`共3 callback，迁至`dynamic_v3_backtest_sim_regime.py`；Sensitivity及后续链保持独立slice。旧run不验证/freeze Outcome source，无timezone/source chronology gate，先建output；直接读取mutable outcome windows且无input snapshot。旧metrics/summary在缺样本时大量写0，可能把missing误作真实return/risk；浅validator不重验source、不重算inventory/metrics/summary/manifest/Markdown或检测tamper。本slice要求pre-output timezone、Outcome content-derived PASS/source cutoff，冻结full outcome bundle/validation到immutable regime snapshot。Regime×variant metrics只纳入AVAILABLE且finite windows，missing return/drawdown/relative/win-rate保持null；event/window/count单位明确，known regime exact coverage与unique identity闭合。Validator重验live outcome并逐字节重算全部views，source/snapshot/output tamper FAIL。只生成non-PIT regime diagnostics，不运行sensitivity/calibration或改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BO Dynamic-v3 Backtest Simulation Paper slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 24,581→24,483行、721→718函数、682→679 decorators；累计focused=`411 passed`、architecture-fitness=`250 passed`（`outputs/validation_runtime/architecture-fitness_20260712T201424Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T200957Z/test_runtime_summary.json`），generated=`879 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`e1375d61...1454f`。Producer要求pre-output Variant content-derived PASS/source/requested-end cutoff/DQ并以`backtest_sim_paper_input_snapshot.v2`冻结full variant/event/config/validation/cache-cutoff/DQ；selected/no_trade state/ledger/performance可复算，无READY/不可计算metrics为null/INSUFFICIENT_DATA。Return明确gross-before-costs/no-cost-model，不能冒充net。Validator重验live variant/cache/DQ并逐字节重算所有views，tamper FAIL。只生成non-PIT paper evidence，不自动运行regime/sensitivity/calibration，不改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BO Dynamic-v3 Backtest Simulation Paper contract freeze并进入`IN_PROGRESS`。范围固定为`backtest-sim paper-run/paper-report`与`validate-backtest-sim-paper`共3 callback，迁至`dynamic_v3_backtest_sim_paper.py`；Regime及后续链保持独立slice。旧run不验证/freeze Variant/Event source，无timezone/source chronology/requested-end/DQ gate，直接读取mutable cache且无input snapshot；paper history对无READY输入也可能标PASS，空/未知performance和relative被写0；gross return未明确披露未配置cost model。浅validator不重验source/cache/DQ、不重算state/ledger/summary/manifest/Markdown或检测tamper。本slice要求pre-output timezone、Variant content-derived PASS/source cutoff/requested-end与required DQ；冻结full variant/event/config/validation/cache-cutoff/DQ snapshot。Selected variant与no_trade baseline必须存在且state/date/weights/ledger/turnover/value/drawdown/return finite一致；无READY/不可计算metrics显式INSUFFICIENT_DATA/null；gross-before-costs与cost-model-not-configured必须披露且不能冒充net performance。Validator重验live source/cache/DQ并逐字节重算全部views，source/snapshot/output tamper FAIL。只生成non-PIT paper simulation evidence，不运行regime/sensitivity/calibration或改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BN Dynamic-v3 Backtest Simulation Outcome Windows slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 24,676→24,581行、724→721函数、685→682 decorators；累计focused=`398 passed`、architecture-fitness=`249 passed`（`outputs/validation_runtime/architecture-fitness_20260712T195028Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T195028Z/test_runtime_summary.json`），generated=`878 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`e1375d61...1454f`。Producer要求pre-output Variant content-derived PASS/source cutoff/DQ并以`backtest_sim_outcome_input_snapshot.v2`冻结full variant/event/config/validation/cache-cutoff/DQ；AVAILABLE metrics finite，PENDING/INSUFFICIENT unknown metrics为null且不参与summary/ranking。Validator重验live variant/cache/DQ并逐字节重算所有views，source/snapshot/output tamper FAIL。只生成non-PIT outcome evidence，不自动运行paper/regime/sensitivity，不改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BN Dynamic-v3 Backtest Simulation Outcome Windows contract freeze并进入`IN_PROGRESS`。范围固定为`backtest-sim outcome-run/outcome-report`与`validate-backtest-sim-outcome`共3 callback，迁至`dynamic_v3_backtest_sim_outcome.py`；Paper及后续链保持独立slice。旧run不验证/freeze Variant/Event source，无timezone/source chronology gate，先建output再DQ且FAIL留partial；直接读取mutable config/cache，未冻结cutoff price/rate/DQ evidence。PENDING/INSUFFICIENT return/relative/risk被写0，summary缺window也写0并可能参与best ranking。浅validator不重验source/DQ、不重算windows/summary/manifest/Markdown或检测tamper。本slice要求pre-output timezone、Variant content-derived PASS、source cutoff与required DQ；冻结full variant/event/config/validation/cache-cutoff/DQ snapshot。每个event×variant×window唯一，只有AVAILABLE metrics可为finite number；PENDING/INSUFFICIENT未知metrics保持null且不参与summary/ranking。Validator重验live source/cache/DQ并逐字节重算全部views，source/snapshot/output tamper FAIL。只生成non-PIT outcome research evidence，不运行paper/regime/sensitivity或改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BM Dynamic-v3 Backtest Simulation Variants slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 24,761→24,676行、727→724函数、688→685 decorators；累计focused=`387 passed`、architecture-fitness=`248 passed`（`outputs/validation_runtime/architecture-fitness_20260712T191521Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T191722Z/test_runtime_summary.json`），generated=`877 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`e1375d61...1454f`。Producer要求event content-derived PASS与source cutoff，`backtest_sim_variant_input_snapshot.v2`冻结full event/config/validation，exact enabled variant/state/weight/delta/turnover/limit invariants与逐字节全view/live-source validator闭合。只生成non-PIT research variants，不自动运行outcome/paper，不改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BM Dynamic-v3 Backtest Simulation Variants contract freeze并进入`IN_PROGRESS`。范围固定为`backtest-sim variants-generate/variants-report`与`validate-backtest-sim-variants`共3 callback，迁至`dynamic_v3_backtest_sim_variants.py`；Outcome及后续simulation链保持独立slice。审计发现旧producer不验证event artifact/content、无timezone/source chronology gate，不冻结event/config/validation bundle，且在计算前创建output；mutable event/config drift可改变variants。旧validator不读取ledger/report内容，不重算rows/ledger/manifest/Markdown，不校验source lineage、enabled variant exact coverage、event×variant unique identity、weights simplex/finite、state transition、turnover/limits或source/output tamper。本slice要求timezone-aware generated、pre-output upstream event content-derived PASS与generated cutoff；冻结完整event bundle/validation/config/enabled variants到immutable variant snapshot，从snapshot确定性重算rows/ledger/manifest/report。Validated event的exact enabled variants、unique event×variant、before→after state continuity、finite normalized weights、delta/turnover/limit与safety必须闭合；合法empty event set显式`INSUFFICIENT_DATA`。Validator重验live event source并重算全部views，source/snapshot/output tamper FAIL。只生成`BACKTEST_SIMULATION_NOT_PIT` variant research artifacts，不运行outcome/paper或改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BJ Dynamic-v3 Evidence Trend slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 25,020→24,948行、737→734函数、698→695 decorators；累计focused=`357 passed`、architecture-fitness=`245 passed`（`outputs/validation_runtime/architecture-fitness_20260712T165653Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T165850Z/test_runtime_summary.json`），generated=`874 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate，tree hash=`44a9b870...055ae`。Run仅选择unique validated COMMITTED refresh；ROLLED_BACK/legacy/future以full bundle和可重验reason排除，PREPARED/invalid COMMITTED阻断。Timeseries读取post-Dashboard full state，missing保持null；history/confidence/growth/return/risk/action与conflict precedence由reviewed policy治理。Validator重验selected/excluded live source与policy并重算全部views，source/snapshot/policy/output tamper FAIL；不运行上游、不改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BK Dynamic-v3 Forward Outcome Decision slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 24,948→24,861行、734→731函数、695→692 decorators；累计focused=`365 passed`、architecture-fitness=`246 passed`（`outputs/validation_runtime/architecture-fitness_20260712T174124Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T174326Z/test_runtime_summary.json`），generated=`875 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate，因新增显式source ids/roots/policy options，tree hash更新为`e1375d61...1454f`。Run建立generated/week cutoff，支持显式id或semantic-latest zero-or-one选择并要求selected Update/Refresh/Trend full PASS、full bundle/validation、Update→Refresh→Trend lineage；missing与invalid分离，full-Dashboard metrics保持null。Readiness、trend/risk manual routing、recommendation precedence和cadence由reviewed policy治理；validator重验live selection/source/policy并重算全部views，tamper FAIL。只生成owner decision support，不运行上游、不改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BL Dynamic-v3 Backtest Simulation Event Foundation slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。4 callback迁canonical；legacy root 24,861→24,761行、731→727函数、692→688 decorators；累计focused=`377 passed`、architecture-fitness=`247 passed`（`outputs/validation_runtime/architecture-fitness_20260712T184327Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T184533Z/test_runtime_summary.json`），generated=`876 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate/tree hash=`e1375d61...1454f`。Strict governed config、pre-output timezone/range/DQ、`backtest_sim_event_input_snapshot.v2`完整config/shortlist/position-policy/candidate/cache-cutoff/DQ绑定、candidate identity与legal empty schedule语义、全view/live-source重算validator均闭合。只生成`BACKTEST_SIMULATION_NOT_PIT` events，不自动运行variants/outcomes，不改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BL Dynamic-v3 Backtest Simulation Event Foundation contract freeze并进入`IN_PROGRESS`。范围固定为`backtest-sim config-validate/event-generate/event-report`与`validate-backtest-sim-events`共4 callback，迁至`dynamic_v3_backtest_sim_events.py`；Variants及后续模拟链保持独立slice。审计发现旧config validator只检查部分字段/文件exists，空path可解析为project root而误PASS，未校验schedule/variant/window唯一性、完整threshold/limit/cost语义或source schema。Event producer在DQ前先创建output，失败遗留partial artifact；generated/end无timezone/cutoff防护，rates empty-path fallback不可达；直接读取mutable config/shortlist candidate weights/price/rate caches，snapshot只存path和浅price summary，无完整config/source/DQ/checksum/cutoff rows，candidate source不验证identity/duplicate/time。浅artifact validator只查四文件、rows存在/status/safety，不能重验config/DQ/live source、重算event dates/events/counts/manifest/Markdown或检测tamper。本slice必须先做strict config schema/unique/domain/finite/threshold governance/source-path validation，再在任何output前执行timezone/range与required cached DQ；冻结config checksum、qualified candidate source bundles、DQ evidence、cutoff-bound price/rate rows和event schedule到immutable snapshot。Candidate duplicate/invalid/missing source和future end fail closed；合法empty schedule必须显式INSUFFICIENT而不是validator要求events非空。Producer只从snapshot重算events/counts/manifest/report，异常不得留下partial artifact；validator重验live config/source/DQ并重算全部views，source/snapshot/output tamper FAIL。该链明确`BACKTEST_SIMULATION_NOT_PIT`，不得伪装PIT/forward evidence，不自动运行variants/outcomes、修改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BK Dynamic-v3 Forward Outcome Decision contract freeze并进入`IN_PROGRESS`。范围固定为`forward-outcome-decision run/report`与`validate-forward-outcome-decision`共3 callback，迁至`dynamic_v3_forward_outcome_decision.py`；后续Backtest Simulation保持独立slice。审计发现旧run通过mutable latest pointers读取Outcome Update、Rolling Refresh和Evidence Trend，不调用三个content-derived validator、不校验generated/week/source chronology或update→refresh→trend lineage、不冻结source/policy；source缺失与invalid均可能被降格为`NO_DUE_WINDOWS/INSUFFICIENT_HISTORY`。Readiness仅由硬编码`confidence==HIGH`与consensus PASS决定，实际trend deterioration未参与gate；missing count转0，READY时next actions仍固定`continue_tracking/do_not_change_policy`，risk manual review routing与7日due cadence均未治理。浅validator只查五文件、安全字段和一条NOT_READY关系，不能重验live source、重算matrix/actions/manifest/Markdown/Reader Brief或检测tamper。本slice必须以timezone-aware generated与week window建立cutoff，按显式id或cutoff内semantic-latest选择zero-or-one validated source；任何selected source必须full PASS、identity/time一致并冻结完整bundle/validation，refresh必须绑定update、trend必须包含selected refresh，invalid/duplicate/cross-lineage阻断，真正无source才显式MISSING而非NO_DUE。Forward availability、confidence、risk、trend与readiness保留null/MISSING语义；minimum sample、eligible confidence/risk/trend、manual-review routing、recommended action precedence和next due cadence全部来自带owner/version/rationale/review condition的policy。Validator重验live sources/policy并从snapshot重算matrix/actions/manifest/Markdown/Reader Brief，source/snapshot/policy/output tamper FAIL；只生成owner decision support，不运行update/refresh/trend、不修改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BJ Dynamic-v3 Evidence Trend contract freeze并进入`IN_PROGRESS`。范围固定为`evidence-trend run/report`与`validate-evidence-trend`共3 callback，迁至`dynamic_v3_evidence_trend.py`；Forward Outcome Decision保持后续独立slice。审计发现旧run无timezone/cutoff/source validation/snapshot/policy，遍历全部refresh目录并只要有manifest就纳入，可能混入legacy unsnapshotted、PREPARED/ROLLED_BACK、tampered或future refresh；不拒绝duplicate refresh/update identity。Timeseries直接比较BI selected-cohort `forward_available`，不同update cohort不可作为累计历史；missing limited metrics经`_float`写0。Minimum 2 history、confidence order、available growth、0-return positive boundary、trend precedence、risk signal与manual-review routing均为未治理投资解释常量。浅validator只查四文件、refresh id和single-row status，不能重验live refresh、重算timeseries/summary/manifest/Markdown或检测policy/source/output drift。本slice必须在任何output前按timezone-aware generated cutoff扫描refresh：明确排除ROLLED_BACK与legacy unsnapshotted、阻断PREPARED/invalid COMMITTED，要求selected refresh content-derived PASS、transaction COMMITTED、unique refresh/outcome-update identity并冻结full bundles/validation evidence。Timeseries必须从每个refresh snapshot内冻结的post-Dashboard full availability matrix取累计state，不比较selected cohort；limited/consensus字段保持null，且source generated/as-of chronology显式。所有history floor、confidence ordering、growth/return boundary、trend/signal/action precedence由带owner/version/rationale/review condition的policy治理。Validator重验live refresh/policy并从snapshot重算rows/summary/manifest/Markdown，source/snapshot/policy/output tamper FAIL；只生成manual trend observation，不自动运行refresh、改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BI Dynamic-v3 Rolling Evidence Refresh slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 25,111→25,020行、740→737函数、701→698 decorators；累计focused=`349 passed`、architecture-fitness=`244 passed`（`outputs/validation_runtime/architecture-fitness_20260712T162345Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T162345Z/test_runtime_summary.json`），generated=`873 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI保持993 leaf/0 duplicate/tree hash=`77c2a9ce...3753`。Run现要求validated/COMMITTED/time-ordered/live/single-use explicit update，以PREPARED/COMMITTED/ROLLED_BACK transaction刷新六下游；失败删除新增artifacts并恢复latest pointers，成功要求全部实际下游content-derived PASS。Snapshot冻结update/validated baseline/post bundles/validation evidence，forward delta限selected cohort；post-update Dashboard显式排除已消费Due，Reader Brief只生成section。8个领域测试覆盖正常commit、invalid update、single-use、partial rollback与view/snapshot/transaction/live-post tamper；无policy/config/portfolio/production/broker effect。
- 2026-07-12：G2.4BI Dynamic-v3 Rolling Evidence Refresh contract freeze并进入`IN_PROGRESS`。范围固定为`rolling-evidence-refresh run/report`与`validate-rolling-evidence-refresh`共3 callback，迁至`dynamic_v3_rolling_evidence_refresh.py`；Evidence Trend保持后续独立slice。审计发现旧run不验证显式Outcome Update、不校验update→refresh time/identity/single-use，顺序运行Dashboard/Limited-vs-NoTrade/Consensus Risk/Owner Attribution/Shadow Aging/Weekly Review却无事务边界；任一中途失败会留下partial artifacts/latest pointers。Before读取不相关mutable latest，after未要求六个content-derived validator PASS，`reader_brief_updated=true`与`status=PASS`均为常量声明。旧artifact不冻结update/downstream full bundles或validation evidence，浅validator只查五文件、几个ID/delta非空和broker flags，不能重算delta/manifest/Markdown/Reader Brief或检测source/output drift。本slice必须在任何下游运行前要求explicit Outcome Update content-derived PASS、id/time/live-source一致且single-use；以PREPARED/COMMITTED/ROLLED_BACK transaction捕获全部output roots和latest pointers，异常删除本次新增artifacts并恢复全部pointer state。Success必须逐一要求所有实际刷新下游validator为content-derived PASS（无shadow shortlist则显式SKIPPED），冻结完整update/pre-refresh baseline/post-refresh bundles及validation evidence；before只能来自本次update snapshot的selected cohort与同一transaction捕获的validated baseline，after只能来自本次生成bundle，delta/reader brief/status由冻结内容推导。Validator重验live update和全部committed downstream bundles并重算refreshed inventory/delta/manifest/Markdown/Reader Brief/transaction；single-use/source/snapshot/output/transaction tamper FAIL。该层只编排证据刷新，不改policy/config/weights/portfolio/production/order/broker。
- 2026-07-12：G2.4BH Dynamic-v3 Outcome Update slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁canonical；legacy root 25,221→25,111行、743→740函数、704→701 decorators；累计focused=`340 passed`、architecture-fitness=`243 passed`（`outputs/validation_runtime/architecture-fitness_20260712T152631Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T152837Z/test_runtime_summary.json`），generated=`872 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI保持993 leaf/0 duplicate，tree hash=`77c2a9ce...3753`。Run现要求validated explicit review/time/identity/live pre-state/single-use，full batch isolated preflight后以PREPARED/COMMITTED/ROLLED_BACK transaction执行，异常全selected rollback，post validator全PASS才commit；冻结review/pre/post bundles并只算selected cohort delta。8个领域测试覆盖正常commit、CASH、invalid review、single-use、forced post-mutation rollback及snapshot/report/live-source tamper。Validator重算全部views并核对live post source；无automatic refresh/policy/portfolio/production/broker effect。
- 2026-07-12：G2.4BH Dynamic-v3 Outcome Update contract freeze并进入`IN_PROGRESS`。范围固定为`outcome-update run/report`与`validate-outcome-update`共3 callback，迁至`dynamic_v3_outcome_update.py`；rolling refresh保持后续独立slice。审计发现旧run不调用Outcome Update Review validator、不校验review/generated/as-of ordering、row identity/live pre-state或single-use，直接逐outcome append mutation；多outcome后项失败可留下partial commit，且source mutation后才创建update artifact。Run不冻结review/pre/post outcome bundles，不要求每个post outcome validator PASS，before/after status delta还扫描整个mutable root而非selected cohort；future-data flag为常量。浅validator只查五文件、row枚举、delta count和broker flags，不能重算review rows、selected cohort、pre→post event append、updated/skipped rows、delta、transaction status、manifest或Markdown。本slice必须在任何source mutation前要求explicit review content-derived PASS、timezone-aware generated不早于review且as-of不晚于generated date，拒绝duplicate/empty identity与live pre-state drift；同一review只允许一个COMMITTED update。全selected cohort先在isolated copy完成DQ/price/update/post-validation预演，再写PREPARED transaction evidence与rollback backups；实际批次任一异常必须恢复全部selected outcomes并记录ROLLED_BACK，只有全部post validators PASS才COMMITTED。Snapshot冻结完整review bundle、pre/post outcome bundles、validation evidence和selected identities；status delta只计算selected frozen cohort，no-future-data由日期边界证明。Validator从snapshot重算review、pre→post单一append event、updated/skipped/delta/manifest/Markdown并核对live committed outcomes；source/snapshot/output/transaction tamper FAIL。该层只追加forward outcome evidence，不自动refresh下游、不改policy/weights/portfolio/production/order/broker；合法evidence mutation仍为`production_effect=none`。
- 2026-07-12：G2.4BG Dynamic-v3 Outcome Update Review slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 25,301→25,221行、746→743函数、707→704 decorators；累计focused=`333 passed`、architecture-fitness=`242 passed`、contract-validation=`203 passed`，generated=`871 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI保持993 leaf/0 duplicate，canonical owner变化使tree hash更新为`055c644c...f75e`。Run强制显式Outcome Due content-derived validation、id/cutoff/unique outcome×window和full-bundle snapshot；READY仅由DUE+PENDING+can_update+cutoff-visible price推导，future-data safety由日期证明。Validator重验live source并重算matrix/safety/impact/status/manifest/Markdown；empty为INSUFFICIENT_DATA，duplicate/tamper FAIL。同步修复Dynamic-v3 family validation只扫描legacy/base owner而漏报已迁canonical命令的问题；无outcome update/data refresh/policy/portfolio/production/broker effect。
- 2026-07-12：G2.4BG Dynamic-v3 Outcome Update Review contract freeze并进入`IN_PROGRESS`。范围固定为`outcome-update-review run/report`与`validate-outcome-update-review`共3 callback，迁至`dynamic_v3_outcome_update_review.py`；实际outcome update保持后续独立slice。审计发现旧run直接读取explicit due目录，不调用Outcome Due validator、不校验due id/manifest generated cutoff/as-of/row identity，也不冻结source；row transformation信任`can_update/latest_price_date/current_status`且无duplicate outcome×window gate。`future_data_used_in_decision=false`为常量声明而非由date boundaries证明；浅validator只查五文件、status和ready row三个字段，不能重算source/review matrix/safety/impact/status/manifest/report，且empty合法INSUFFICIENT artifact会因`bool(rows)`失败。本slice必须在任何output前要求explicit Outcome Due content-derived validator PASS、manifest due id一致、generated/as-of/latest price均不晚于review cutoff；冻结full due bundle与validation evidence。Rows必须有唯一outcome×window identity、合法due/current status；READY只能由DUE+PENDING+can_update+cutoff-visible price推导，禁止信任或覆盖source语义。Validator重验live due source并从snapshot重算review rows/safety/impact/status/manifest/Markdown；source/snapshot/output tamper FAIL，empty artifact可验证为INSUFFICIENT_DATA，legacy unsnapshotted只读warning。只生成owner review input，不执行outcome update、不刷新数据、不改policy/portfolio/production/order/broker。
- 2026-07-12：G2.4BF Dynamic-v3 Consensus Risk slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 25,399→25,301行、749→746函数、710→707 decorators；累计focused=`330 passed`、architecture-fitness=`241 passed`（`outputs/validation_runtime/architecture-fitness_20260712T134252Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T134512Z/test_runtime_summary.json`），generated=`870 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate，tree hash=`c46f54c3...4070`。Run强制validated/cutoff-bound unique Daily、unique latest Replay和same-lineage Repair/Backfill，冻结full source/policy snapshot；Daily只接受明确consensus weights，同日Daily/Replay一致才合并，drawdown严格paired，turnover按distinct decision date计一次，missing aggregate为null。所有sample floors/required windows/risk thresholds由reviewed policy治理。Empty fixture为exposure/pairs/turnover=0、INSUFFICIENT_DATA；positive frozen fixture仅验证计算契约，不代表真实策略结论。Validator重验live source/policy并重算全部views，report tamper FAIL；无default execution/policy/portfolio/production/broker effect。
- 2026-07-12：G2.4BF Dynamic-v3 Consensus Risk contract freeze并进入`IN_PROGRESS`。范围固定为`consensus-risk run/report`与`validate-consensus-risk`共3 callback，迁至`dynamic_v3_consensus_risk.py`。审计发现旧run直接扫描全部mutable Daily Advisory、Historical Replay与Repair/Backfill目录，不调用source validator、不校验generated cutoff/semantic latest/replay lineage/duplicate identity，也不冻结source/policy。Daily缺consensus weights时会回退到candidate target，改变`consensus_target`语义；historical exposure和outcome turnover跨window/跨source重复计数。Exposure把missing CASH当0并推导100% risk asset；drawdown/turnover缺样本写0。5个样本、50% semiconductor、95% risk asset、-2% drawdown delta、50% turnover等投资解释门槛均为code constants。Outcome grouping以dict覆盖duplicate variant；浅validator只查五文件、sample floor和no execution，不能重算source selection/exposure/drawdown/turnover/status/manifest/report。本slice必须在任何output前要求selected Daily、唯一latest Historical Replay与唯一latest Repair或Backfill content-derived validators PASS，按timezone-aware cutoff冻结full bundles和reviewed risk policy；outcome replay lineage必须匹配，duplicate id/as-of/event×window×variant/conflict fail closed。Exposure以distinct decision date为样本，Daily只接受明确consensus weights，不得借candidate target；Daily与Replay同日重复必须相同后去重，否则阻断。Turnover按distinct exposure event计一次，不跨window重复。AVAILABLE drawdown pair必须finite，missing/empty aggregate保持null。Sample floors、exposure/drawdown/turnover thresholds、required windows和overall status precedence全部由带owner/version/rationale/validation-plan/review-condition的policy治理。Validator重验live source/policy并从snapshot重算exposure samples/coverage、exposure summary、paired drawdown、turnover、manifest与Markdown；source/snapshot/policy/output tamper FAIL，legacy unsnapshotted只读warning。只生成manual observation/risk review，不推荐default execution、不apply policy、不改portfolio/production/order/broker。
- 2026-07-12：G2.4BE Dynamic-v3 Limited-vs-NoTrade slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 25,492→25,399行、752→749函数、713→710 decorators；累计focused=`326 passed`、architecture-fitness=`240 passed`、contract-validation=`203 passed`，generated=`869 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate，tree hash=`707e8028...ed15`。Run强制validated/cutoff-bound unique latest sources、strict event×window pairing、immutable source/policy snapshot；missing/empty metrics保持null，无真实regime label时UNAVAILABLE。Overall recommendation threshold、confidence floors、win threshold和aggregation均由reviewed policy治理；未知variant/status/regime fail closed。Validator重验live source/policy并重算全部views，output tamper FAIL。当前empty-source fixture为paired=0、available=0、recommendation=insufficient_data，不伪造方向结论；无policy/portfolio/production/broker effect。
- 2026-07-12：G2.4BE Dynamic-v3 Limited-vs-NoTrade contract freeze并进入`IN_PROGRESS`。范围固定为`limited-vs-notrade run/report`与`validate-limited-vs-notrade`共3 callback，迁至`dynamic_v3_limited_vs_notrade.py`。审计发现旧run直接遍历全部mutable Advisory Outcome与Repair/Backfill目录，不调用source validator、不校验generated cutoff/latest lineage/duplicate event×window×variant，也不冻结source/policy。Historical grouping以dict覆盖duplicate variant；缺paired variant被静默跳过。Forward把paper portfolio return直接命名为limited adjustment，未证明action/variant语义；所有missing metrics经`_float`写0，empty window的avg/median/win/drawdown/turnover也写0。5/20 confidence floors与跨window recommendation为code constants，虽有pilot注释但未绑定owner/version/review条件。Regime breakdown完全不读取regime，固定六桶0与INSUFFICIENT/PARTIAL，可被误读为完成regime分析。旧validator只查五文件、status枚举、empty confidence和no-auto-policy，不能重算samples/metrics/regime/manifest/report。本slice必须在任何output前要求selected Advisory Outcome和唯一latest Repair或Backfill content-derived validators PASS，按timezone-aware cutoff冻结full source bundles与reviewed comparison policy；event×window必须有唯一limited/no_trade pair，duplicate/conflict fail closed，unpaired显式进入coverage而不静默消失。Forward只有明确limited-adjustment decision lineage才可进入paired comparison；否则标记UNSUPPORTED而非借paper return冒充。AVAILABLE paired metrics必须finite；PENDING/INSUFFICIENT与empty aggregates保持null，不写0。Confidence floors、window aggregation、win threshold、recommendation precedence和regime taxonomy/mapping均由policy治理；没有真实regime label时breakdown明确UNAVAILABLE且各metric null。Validator重验live sources/policy并从snapshot重算sample inventory、coverage、window metrics、regime breakdown、manifest、Markdown，source/snapshot/policy/output tamper FAIL；legacy unsnapshotted只读warning。只生成manual research evaluation，不自动apply policy、不改config/portfolio/production/order/broker。
- 2026-07-12：G2.4BD Dynamic-v3 Outcome Dashboard slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 25,589→25,492行、755→752函数、716→713 decorators；focused=`170 passed`、architecture-fitness=`239 passed`（`outputs/validation_runtime/architecture-fitness_20260712T121630Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T121849Z/test_runtime_summary.json`），generated=`868 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate，因新增显式sim/diagnosis/due/policy source options，tree hash更新为`4abbbdca...1422`。Build强制selected source full validation/semantic latest/cutoff/duplicate-lineage gate，冻结full bundles与pending action policy；按三mode明确sample unit重算matrix/mode/pending/manifest/Markdown/Reader Brief。当前fixture仅4个forward PENDING windows，historical/simulation=0、available=0、reason=future_window_not_reached，不伪造完整性。Validator重验live source/policy且tamper FAIL；无upstream/policy/portfolio/production/broker effect。
- 2026-07-12：G2.4BD Dynamic-v3 Outcome Dashboard contract freeze并进入`IN_PROGRESS`。范围固定为`outcome-dashboard build/report`与`validate-outcome-dashboard`共3 callback，迁至`dynamic_v3_outcome_dashboard.py`。审计发现旧build直接遍历全部mutable Advisory Outcome、Repair/Backfill和Historical Paper Sim目录，不调用任何source validator、不校验generated cutoff、source lineage或duplicate sample identity，也不冻结source。只要repair目录非空就完全遮蔽backfill，无semantic latest或ambiguity gate；simulation仅看manifest/summary status字符串即可计为AVAILABLE。Pending reasons又读取全体Outcome Due与目录顺序latest Diagnosis，可能跨run重复计数和混合未来/旧链。旧validator只查六文件、三mode key和no-broker，不能重算mode rows、availability matrix、mode summary、pending reasons、manifest、Markdown或Reader Brief。本slice必须在任何output前要求selected Advisory Outcome、Historical Repair/Backfill、Paper Sim、Diagnosis和Outcome Due content-derived validators PASS，按timezone-aware generated cutoff选择唯一latest链并拒绝duplicate/cross-lineage/ambiguous sample；缺source保持显式0/MISSING而不伪造AVAILABLE。冻结所有selected full bundles、validation evidence和带owner/version/rationale/review-condition的pending-reason action policy；forward/historical/simulation分别使用window/event/sim sample unit，不把artifact count当outcome count。Pending reason只能来自冻结selected diagnosis/due与frozen PENDING rows，禁止全目录重复聚合。Validator重验live source/policy并从snapshot重算matrix/mode summary/pending dashboard/manifest/Markdown/Reader Brief，source/snapshot/policy/output tamper FAIL；legacy unsnapshotted只读warning。只聚合已有证据，不运行upstream、不改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BC Dynamic-v3 Replay Sample Expansion slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 25,692→25,589行、758→755函数、719→716 decorators；focused=`168 passed`、architecture-fitness=`238 passed`（`outputs/validation_runtime/architecture-fitness_20260712T115117Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T115326Z/test_runtime_summary.json`），generated=`867 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍993 leaf/0 duplicate，因新增显式owner/rates/policy options，tree hash治理性更新为`3032d165...e516`。Run强制range/time/DQ、Daily/append-only Owner/latest Replay Inventory full validation与duplicate/conflict gate，冻结完整source/policy/cache/date snapshot；PIT safety与price evaluability分离，unique sample key阻断双计数。Validator重验live source/DQ/policy并重算全部views，tamper FAIL；当前validated TARGET_ONLY fixture因缺current weights正确为PIT_UNSAFE/INELIGIBLE，不伪造合格样本。不运行replay/backfill，无policy/portfolio/production/broker effect。
- 2026-07-12：G2.4BC Dynamic-v3 Replay Sample Expansion contract freeze并进入`IN_PROGRESS`。范围固定为`replay-sample-expansion run/report`与`validate-replay-sample-expansion`共3 callback，迁至`dynamic_v3_replay_sample_expansion.py`。审计发现旧run直接扫描未验证Daily Advisory、mutable owner journal和全部Replay Inventory；不调用daily/inventory validators、不校验source generated cutoff、range/identity uniqueness或跨源冲突、不执行cached price DQ，也不冻结source/policy/price inputs。Shadow Monitor与Consensus Drift只被计数且`discovered_count=0`，却进入candidate source inventory；daily与replay同一真实事件可双重计数，重复expanded id由first-win静默吞掉。旧PIT判断只把缺target/current视为unsafe，owner缺失/generated-after-as-of只是warning，price-after-as-of不影响eligibility；validator仅查五文件、枚举和PIT_UNSAFE exclusion，不能重算inventory/events/summary/manifest/report或检测source drift。本slice必须在任何output前校验`start<=end<=generated.date`，执行cached price/rate DQ；只接受content-derived PASS的Daily Advisory与Replay Inventory，按timezone-aware cutoff选择且拒绝duplicate daily id/as-of、duplicate inventory event和冲突的跨源event identity。Owner evidence必须来自validated append-only events并受cutoff约束；未实际参与样本计算的Shadow/Drift不得伪装成discovered source。冻结完整source bundles、DQ evidence、price checksum/cutoff availability和带owner/version/rationale/review-condition的PIT eligibility policy；expanded event保持source lineage与唯一sample key，PIT_UNSAFE固定INELIGIBLE，missing/ambiguous inputs不写成可用证据。Validator重验live source/DQ/policy并从snapshot重算source inventory、events、summary、manifest和Markdown，source/snapshot/input/output tamper FAIL；legacy unsnapshotted只读warning。只写replay candidate evidence/latest pointer，不运行historical replay/backfill，不修改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4BB Dynamic-v3 Outcome Due slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。4 callback迁至canonical owner；legacy root 25,823→25,692行、762→758函数、723→719 decorators；focused=`165 passed`、architecture-fitness=`237 passed`（`outputs/validation_runtime/architecture-fitness_20260712T111430Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T111624Z/test_runtime_summary.json`），generated=`866 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Scan在写出前强制timezone/as-of、cached DQ和所有Advisory Outcome content-derived validation，冻结完整source/DQ/price-rate checksum/cutoff trading-date snapshot；validator重验live inputs并重算全部views，tamper FAIL。Update-ready要求validated且fresh due artifact、single-use execution和显式allowed windows，更新后outcome validator PASS；不更新NOT_DUE/PRICE_MISSING，不自动运行后续链，无policy/portfolio/production/broker effect。
- 2026-07-12：G2.4BB Dynamic-v3 Outcome Due contract freeze并进入`IN_PROGRESS`。范围固定为`outcome-due scan/report/update-ready`与`validate-outcome-due`共4 callback，迁至`dynamic_v3_outcome_due.py`。审计发现旧scan先创建output再执行DQ，随后遍历mutable Advisory Outcome目录但不调用outcome validator、不校验generated cutoff或daily/outcome/window唯一性、不冻结source、价格日历或DQ inputs；invalid/missing manifest被静默跳过。Due validator只查五文件、status枚举和safety，不能重算inventory/summary/ready/manifest/report。`update-ready`直接信任mutable due JSON，不先运行due validator，不校验due generated time/live source freshness，按outcome去重后调用全window update而未传`allowed_window_days`，可重复执行且不验证更新后的outcome chain。本slice必须在任何due output前完成timezone/as-of、cached DQ和所有selected Advisory Outcome content-derived validation；future/invalid/duplicate/cross-key source fail closed。冻结完整outcome bundles、DQ evidence、price/rate checksums及只用于到期判断的cutoff price-date availability到immutable snapshot；due计算只按frozen trading dates和event/window/source rows，missing保持INSUFFICIENT_DATA/PRICE_MISSING而不写0。Validator从snapshot重算inventory/summary/ready/manifest/Markdown并检测source/snapshot/DQ/input/output drift。`update-ready`必须先要求due validator PASS、time ordered、live source与snapshot相符、无既有execution；按每个outcome的ready window集合显式传`allowed_window_days`，只允许DUE/can_update rows，更新后outcome validator必须PASS并写可审计execution。不得更新NOT_DUE/PRICE_MISSING、不得自动调度后续链、不改policy/weights/production/order/broker；合法update是受限forward evidence mutation，不是production effect。
- 2026-07-12：G2.4BA Dynamic-v3 Replay-to-Forward Bridge slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 25,921→25,823行、765→762函数、726→723 decorators；focused=`148 passed`、architecture-fitness=`236 passed`（`outputs/validation_runtime/architecture-fitness_20260712T104017Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T104234Z/test_runtime_summary.json`），generated=`865 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Run强制三源content-derived validation/lineage/time并冻结完整source/policy；不足证据继承`require_more_forward_data` non-policy action，无reason不造unknown，10-event/windows/focus/questions由reviewed policy治理。Validator重验live source/policy并重算全部views，tamper FAIL；不运行上游，无policy/config/portfolio/production/broker effect。
- 2026-07-12：G2.4BA Dynamic-v3 Replay-to-Forward Bridge contract freeze并进入`IN_PROGRESS`。范围固定为`replay-forward-bridge run/report`与`validate-replay-forward-bridge`共3 callback，迁至`dynamic_v3_replay_forward_bridge.py`。审计发现旧run直接读取mutable Diagnosis/Comparison/Calibration文件，不调用三个source validator、不校验diagnosis→comparison→calibration lineage/generated time、不冻结source或reviewed bridge policy；固定10个future events和focus/weekly questions为未治理常量。它只读取calibration proposals，不读取AZ已拆出的`evidence_collection_actions.json`，因此证据不足时会把`require_more_forward_data`丢失并回落`continue_forward_tracking`；无pending reason时还注入`unknown`。Manifest的best/confidence从comparison manifest默认值读取，validator只查五文件/focus非空/safety，不能重算focus/weekly updates/manifest/report/Reader Brief。本slice必须在任何output前要求Diagnosis、Variant Comparison、Rule Calibration三个content-derived validators PASS，校验comparison backfill/replay与diagnosis一致、calibration comparison与显式comparison一致，且bridge time不早于三源。冻结三类完整source bundles和带owner/version/rationale/review-condition的bridge policy；required future events、tracked windows/focus与weekly questions均来自policy。Evidence action与policy proposal保持分离：不足证据时next action必须继承`require_more_forward_data` evidence action，显式`policy_change_allowed=false`；没有pending reason保持none，不制造unknown blocker。Status/best/confidence/next action必须由冻结source重算，missing保持null/MISSING。Validator重验live sources/policy并重算focus/weekly updates/manifest/Markdown/Reader Brief，source/snapshot/policy/output tamper FAIL；legacy只读warning。只写forward observation/review guidance，不运行upstream、不修改policy/config/weights/portfolio/production/order/broker。
- 2026-07-12：owner批准ARCH-004→ARCH-005 bootstrap停止条件：必须完成整个G2.4 callback/migration matrix与phase exit criteria、required focused/architecture/contract/full validation、manifest/inventory/source-hash freshness和clean attribution后，才可产出`arch_005_bootstrap_handoff.v1` PASS。ARCH-005需求保持READY，但S0实现等待该handoff；handoff必须固定`next_slice_unblocked=false`，ARCH-004停在G2.5之前且不自动进入G2.5/G3/G4/G5。ARCH-005 S0/S1完成后，只能由新的显式指令恢复ARCH-004。
- 2026-07-12：G2.4AZ Dynamic-v3 Rule Calibration slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 26,007→25,921行、768→765函数、729→726 decorators；focused=`141 passed`、architecture-fitness=`235 passed`（`outputs/validation_runtime/architecture-fitness_20260712T101619Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T101831Z/test_runtime_summary.json`），generated=`864 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Run强制validated Comparison/time与完整source/calibration-policy/target-policy snapshot；只有PILOT_ELIGIBLE可产生manual proposal，当前fixture正确INSUFFICIENT_DATA/proposal_count=0并输出non-policy evidence action。Validator重验live source/policies并重算全部views，tamper FAIL；不自动应用proposal，无policy/config/portfolio/production/broker effect。
- 2026-07-12：G2.4AZ Dynamic-v3 Rule Calibration contract freeze并进入`IN_PROGRESS`。仅迁移`rule-calibration run/report`及`validate-rule-calibration`三callback。Run必须pre-output要求Comparison validator PASS与time ordering，冻结comparison bundle/policy、目标advisory policy及reviewed calibration eligibility policy。只有source-derived `PILOT_ELIGIBLE` comparison允许directional policy-change proposal；证据不足时status=INSUFFICIENT_DATA、calibration proposal count=0，require-more-data仅是`policy_change_allowed=false` evidence action，missing不得写0或推导方向结论。Validator重验live sources/policies并重算diagnostics/actions/proposals/safety/manifest/Markdown，tamper FAIL；legacy只读warning。所有输出manual-only、owner approval required、auto_apply=false，不修改policy/config/portfolio/production/order/broker。
- 2026-07-12：G2.4AY Dynamic-v3 Variant Comparison slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 26,099→26,007行、771→768函数、732→729 decorators；focused=`117 passed`、architecture-fitness=`234 passed`（`outputs/validation_runtime/architecture-fitness_20260712T094044Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T094258Z/test_runtime_summary.json`），generated=`863 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Run强制validated source/lineage/time和reviewed policy snapshot，duplicate阻断、missing为null、pairwise按paired distinct event、ranking只用5d same cohort与3 event/10 window/3 pair门槛；当前fixture正确MISSING/INSUFFICIENT_DATA。Validator重验live source/policy并重算全部views，tamper FAIL；不运行calibration，无policy/portfolio/production/broker effect。
- 2026-07-12：G2.4AX Dynamic-v3 Backfill Repair slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 26,200→26,099行、774→771函数、735→732 decorators；focused=`115 passed`、architecture-fitness=`233 passed`（`outputs/validation_runtime/architecture-fitness_20260712T091535Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T091743Z/test_runtime_summary.json`），generated=`862 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Run在output前强制backfill/diagnosis/replay full validation、lineage/time/DQ gate，冻结三源bundle、cost和cutoff price rows；原AVAILABLE immutable，非AVAILABLE按AT fixed-share/cost/null语义重算，delta单位为`event_variant_window`。Validator重验live source/DQ并重算全部views，tamper FAIL；不覆写原backfill、不运行comparison/calibration，无policy/portfolio/production/broker effect。
- 2026-07-12：G2.4AW Dynamic-v3 Replay Diagnosis slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 26,322→26,200行、777→774函数、738→735 decorators；focused=`83 passed`、architecture-fitness=`232 passed`（`outputs/validation_runtime/architecture-fitness_20260712T084612Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T084809Z/test_runtime_summary.json`），generated=`861 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。五source validation/lineage/time/snapshot与coverage/reasons/health/report全部可重算；comparison readiness不再由单window解锁；同步修复repair cost_rate回归。无source/config/policy/portfolio/production/order/broker effect。
- 2026-07-12：G2.4AW Dynamic-v3 Replay Diagnosis contract freeze并进入`IN_PROGRESS`。仅迁移`replay-diagnosis run/report`及`validate-replay-diagnosis`三callback。Run必须pre-output验证Inventory/Replay/Backfill/Sim/Review五源、完整lineage与generated time，冻结全部source bundles。Coverage/reasons/health分开event/window/state/chain单位；无reason不造blocking unknown；comparison readiness继承AV reviewed evidence gate。Validator重验live source并重算coverage/reasons/health/manifest/Markdown，tamper FAIL；legacy只读warning。本层不运行repair/comparison/calibration，不改source/config/policy/portfolio/production/order/broker。
- 2026-07-12：G2.4AV Dynamic-v3 Replay Performance Review slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 26,414→26,322行、780→777函数、741→738 decorators；focused=`91 passed`、architecture-fitness=`231 passed`（`outputs/validation_runtime/architecture-fitness_20260712T081047Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T081250Z/test_runtime_summary.json`），generated=`860 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Run/validator强制full-PASS same-replay sources、time/policy/snapshot与全部view重算；null与unsupported classification semantics正确，sample floors未通过只允许continue tracking。当前fixture仅1个可比distinct event，`directional_evidence_ready=false`。本层不修改source/config/policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AV Dynamic-v3 Replay Performance Review contract freeze并进入`IN_PROGRESS`。仅迁移`replay-performance-review run/report`及`validate-replay-performance-review`三callback。Run必须在任何output前要求Backfilled Outcome与Historical Paper Simulation full PASS、共同replay lineage和timezone-aware review cutoff；冻结两者全部计算source与独立reviewed policy。Relative return只能支持positive/nonpositive rate，false alarm/missed opportunity无独立label时必须null，missing metrics不得写0。Directional manual proposal只有通过pilot distinct-event/window与simulation门槛才可产生，否则continue forward tracking；所有建议均manual-only。Validator重验live source/policy并重算effectiveness/recommendations/manifest/Markdown/Reader Brief，source/snapshot/policy/output tamper FAIL；legacy只读warning。本层不修改source/config/policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AU Dynamic-v3 Historical Paper Simulation slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 26,504→26,414行、783→780函数、744→741 decorators；focused=`95 passed`、architecture-fitness=`230 passed`（`outputs/validation_runtime/architecture-fitness_20260712T073441Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T073654Z/test_runtime_summary.json`），generated=`859 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Run强制full-PASS replay/time/DQ preflight并冻结replay/config/cost/price evidence；event interval用与total return一致的fixed-share value path，缺价/少样本不再写0；event target reset的one-way L1成本进入net equity，no_trade独立重放。Validator重验live replay/DQ/source并重算history/ledger/summary/manifest/Markdown，snapshot/history/report/source tamper均FAIL。AU同步修正AT gross risk path口径。本层不修改replay/config/policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AU Dynamic-v3 Historical Paper Simulation contract freeze并进入`IN_PROGRESS`。仅迁移`historical-paper-sim run/report`及`validate-historical-paper-sim`三callback。旧run不验证source replay/time，不走统一DQ，不冻结replay/config/price，先创建output；缺区间价格时把return静默写0，交易成本未应用，event-period return被命名为daily return并直接年化。进一步复核发现共享`_portfolio_return_and_path`的total return是fixed-share口径，但risk daily path用constant-weight daily rebalance口径，AT报告中的fixed-share risk语义需在本slice同步校正。AU必须在任何output前要求full-PASS replay、timezone/cutoff、真实cached price/rate DQ，冻结source replay、reviewed cost policy和cutoff price rows；逐event interval用同一fixed-share value path计算gross return/daily path，缺required price不得置0，必须显式INSUFFICIENT并停止后续物化。每次target reset按simulated before→variant target的one-way L1 turnover计成本并进入net equity，state/trade/equity/sample units与comparator lineage显式。Validator从immutable snapshot重算history、ledger、summary、manifest、Markdown及source/DQ binding，tamper FAIL；legacy只读warning。本层不修改replay/config/policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AT Dynamic-v3 Backfilled Outcome slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 26,602→26,504行、786→783函数、747→744 decorators；focused=`87 passed`、architecture-fitness=`229 passed`（`outputs/validation_runtime/architecture-fitness_20260712T070207Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T070408Z/test_runtime_summary.json`），generated=`858 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Run强制full-PASS replay、time ordering与真实DQ preflight；snapshot冻结replay/config/window/session/cost/price rows及checksums。AVAILABLE按fixed-share gross path并仅扣一次initial one-way L1 turnover cost；非AVAILABLE的gross/net/relative/risk/cost保持null+reason。Validator重验live replay/DQ/source并重算rows、summary、manifest、Markdown，snapshot/rows/report/source tamper均FAIL。研究链/system flow/runbook/registry/catalog已同步why、输入输出、公式和优化空间。本层不修改replay/config/policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AT Dynamic-v3 Backfilled Outcome contract freeze并进入`IN_PROGRESS`。仅迁移`backfill-outcome run/report`及`validate-backfill-outcome`三callback到`interfaces/cli/etf_portfolio/dynamic_v3_backfilled_outcome.py`。Run在任何backfill output前要求source Historical Replay full validator PASS，拒绝legacy/source drift并要求timezone-aware generated time不早于replay time；冻结replay snapshot、reviewed config/windows、DQ evidence与仅本次事件/窗口所需的price rows/checksums。真实运行必须先走统一cached market/rate DQ gate，测试skip必须显式标记且不得成为投资证据。Window按可用exchange-session序列计算；只有required symbols在start/end与路径上完整、finite、positive才为AVAILABLE。PENDING与INSUFFICIENT所有return/relative/drawdown/volatility字段必须null+reason，不能以0伪装；AVAILABLE relative metric只在自身及comparator均AVAILABLE时计算。Fixed-share path、cash handling、turnover与versioned cost role必须披露，sample units区分event×variant×window。Validator从immutable snapshot重算rows、summary、manifest、Markdown与DQ/source binding，tamper FAIL；legacy只读warning。本层不修改replay/config/policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AS Dynamic-v3 Historical Replay slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback迁至canonical owner；legacy root 26,686→26,602行、789→786函数、750→747 decorators；focused=`83 passed`、architecture-fitness=`228 passed`（`outputs/validation_runtime/architecture-fitness_20260712T062532Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T062735Z/test_runtime_summary.json`），generated=`857 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Run强制full-PASS snapshotted inventory与cutoff ordering，严格执行PIT/eligibility矩阵；五variant校验weight simplex，Owner/Paper fallback显式，turnover convention固定。Replay source snapshot绑定inventory manifest/snapshot/rows；validator重算events、decision inputs、summary、manifest、Markdown并检测snapshot/event/report/source tamper。研究执行链详文同步why、输入输出、计算与优化顺序。本层不读取outcome price、不运行backfill，无policy/portfolio/official/production/order/broker effect。
- 2026-07-12：G2.4AS Dynamic-v3 Historical Replay contract freeze并进入`IN_PROGRESS`。仅迁移`historical-replay run/report`及`validate-historical-replay`三callback到`interfaces/cli/etf_portfolio/dynamic_v3_historical_replay.py`，不提前合并backfill/simulation/review。Run必须在任何output前要求source Replay Inventory full validator PASS，拒绝legacy-unsnapshotted、source drift、id/path/checksum不一致；timezone-aware replay generated time不得早于inventory evidence cutoff。默认只纳入`PIT_SAFE/ELIGIBLE`，显式`include_pit_warning`仅可增加`PIT_WARNING/PARTIAL`，hard limitation或`PIT_UNSAFE/INELIGIBLE`永远排除。冻结validated inventory snapshot/checksum、selected/skipped rows与variant inputs到immutable replay source snapshot；五个variant必须从frozen decision inputs确定性生成，权重finite/nonnegative/sum-to-one，turnover=`Σ|w_variant-w_current|`并明确one-way convention，owner/paper缺失时的no-trade fallback必须披露source status而非伪装成真实action。Manifest/report披露range/regime/inventory cutoff、sample units、skip reasons、variant lineage、no-future-price/no-execution；validator从snapshot重算events、decision inputs、action summary、manifest与Markdown，tamper/source drift FAIL，旧unsnapshotted replay仅只读warning。本层不读取outcome price、不计算return、不运行backfill、不修改policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AR Dynamic-v3 Historical Replay Inventory slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback已迁至canonical owner；legacy root 26,802→26,686行、792→789函数、753→750 decorators；focused=`77 passed`、architecture-fitness=`227 passed`（`outputs/validation_runtime/architecture-fitness_20260712T055538Z/test_runtime_summary.json`）、contract-validation=`203 passed`（`outputs/validation_runtime/contract-validation_20260712T055738Z/test_runtime_summary.json`），generated=`856 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI保持993 leaf/0 duplicate/tree hash不变。Builder现要求valid range/timezone-aware cutoff，拒绝daily id/as-of与cutoff binding歧义，排除future-generated Daily；Owner/Paper/Drift使用cutoff-visible语义时间，Drift不再按mtime。Immutable snapshot冻结selected Daily、decision bindings、source size/checksum和canonical rows，future price明确只作outcome availability；validator重算rows/PIT audit/coverage/manifest/Markdown，snapshot/report/source drift失败，missing artifact不再冒充legacy。研究执行链详文已记录why、inputs/outputs、公式、当前证据与优化顺序。本层未运行historical replay/backfill，无policy/portfolio/official/production/order/broker effect。
- 2026-07-12：G2.4AR Dynamic-v3 Historical Replay Inventory contract freeze并进入`IN_PROGRESS`。仅迁移`replay-inventory build/report`及`validate-replay-inventory`三callback到`interfaces/cli/etf_portfolio/dynamic_v3_replay_inventory.py`，不提前合并historical replay/backfill。Builder要求valid range与generated cutoff，拒绝daily id/as-of歧义，排除并披露future-generated artifacts；冻结selected Daily、config、price availability及cutoff-visible Owner/Paper/Drift inputs/checksums。Historical invalid/missing evidence应归类`PIT_UNSAFE/INELIGIBLE`，但source ambiguity或无法重放必须fail closed。Current-policy/baseline/paper approximation保留warning，future price只作outcome-availability evidence，不是decision input。Manifest/report披露range/regime/source role/no-execution，validator从snapshot重算rows、PIT audit、coverage、manifest和Markdown；legacy unsnapshotted只读warning。本层只产出inventory，不运行historical replay/backfill、不刷新数据、不改policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AQ Dynamic-v3 Weekly Advisory Review slice `COMPLETE`，G2.4继续且不触发phase-level ARCH-005 handoff。3 callback已迁至canonical owner；legacy root 26,911→26,802行、795→792函数、756→753 decorators；focused=`116 passed`、architecture-fitness=`226 passed`、contract-validation=`203 passed`，generated=`855 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI仍为993 leaf/0 duplicate且tree hash不变。Run现在以calendar week/generated cutoff选择validated Daily→Monitor链，对Owner/Paper/Outcome重放cutoff event prefix，对Aging做semantic latest选择；duplicate daily/outcome、多paper lineage、invalid/future source和aging时间歧义在output前fail closed。Reviewed `weekly_review_v2` policy治理coverage、downgrade与recommendation precedence；无AVAILABLE outcome均值为null/reason，周后update不回填。Snapshot/source validation可重算全部9个产物，snapshot/report/Reader Brief篡改失败；legacy unsnapshotted只读warning。相邻Owner Attribution同时改为先分组检测daily duplicate outcome、再验证唯一source，错误语义不再受目录排序影响。权威研究执行链已记录why、逐环节inputs/outputs/formula、当前结果和优化顺序。本层不新增独立scheduler，无policy/official/paper/real/production/order/broker effect。
- 2026-07-12：G2.4AQ Dynamic-v3 Weekly Advisory Review contract freeze并进入`IN_PROGRESS`。迁移边界为`weekly-advisory-review run/report`及`validate-weekly-advisory-review`三callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_weekly_advisory_review.py`。Run以七日calendar week和周末UTC日末为evidence cutoff，要求week内Monitor/Daily/Owner及所选Paper/Outcome/Aging source通过各自validator，拒绝duplicate as-of/daily、未来generated/event/update及cross-lineage；append-only owner/paper/outcome按cutoff prefix物化，禁止全局latest或mtime把后续证据倒灌历史周，多paper portfolio歧义显式阻断。Immutable weekly source snapshot冻结reviewed `weekly_review_v2` policy、source content/prefix/inventory/validation；各类sample unit分开披露，AVAILABLE为空时relative metric为null。Coverage、downgrade和recommendation precedence从带owner/version/rationale/review condition的pilot policy读取；缺证据输出`INSUFFICIENT_EVIDENCE/manual_review_required`。Summary/manifest/Markdown/Reader Brief仅从snapshot物化，披露cadence、`ai_after_chatgpt`、实际周范围、DQ inheritance与no-execution；validator从snapshot重算。Legacy unsnapshotted只读warning。本层不新增scheduler、不运行上游、不刷新数据，不修改policy/portfolio/official/production/order/broker。
- 2026-07-12：G2.4AP Dynamic-v3 Shadow Aging slice `COMPLETE`，G2.4继续。3 callback已迁至canonical owner；legacy root 27,004→26,911行、798→795函数、759→756 decorators；focused=`108 passed`、architecture-fitness=`225 passed`，generated=`854 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI仍为993 leaf/0 duplicate且tree hash不变。Validated/cutoff/duplicate source gates、immutable policy/shortlist/monitor/drift/outcome snapshot、四类observation sample unit、真实weight-change rebalance、candidate-specific frozen-target/price/cost outcome replay、AVAILABLE-only equal-window aggregation、null missing evidence、reviewed promotion-clock thresholds及content-derived full-view validator均已实现；invalid/future/duplicate/rehash tamper fail closed，legacy unsnapshotted只读warning。Outcome selective update已改为event创建前合并允许窗口，恢复append-only event与materialized view一致性。研究执行链详文记录why、inputs/outputs、公式、current result、证据限制与优化顺序。`eligible_for_review`仅为manual queue，本层无portfolio/official/production/order/broker effect。
- 2026-07-12：G2.4AP Dynamic-v3 Shadow Aging contract freeze并进入`IN_PROGRESS`。迁移边界为`shadow-aging run/report`及`validate-shadow-aging`三callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_shadow_aging.py`。Run必须先验证唯一Shadow Shortlist、cutoff内每个selected Shadow Monitor、same-lineage Consensus Drift和相关Advisory Outcome，拒绝duplicate as-of/monitor/daily绑定并冻结reviewed config与全部source snapshots。Candidate observation必须区分calendar span、unique days、monitor runs和相邻权重真实变化次数；drift/high-disagreement不得因重复artifact被加权。Candidate outcome只可从validated outcome冻结的daily candidate targets与immutable price snapshot重算fixed-share、cost-adjusted relative-to-no-trade窗口，AVAILABLE window以config声明的equal-window口径聚合，缺失为null并阻断资格而非转0；downgrade floor、minimum score、warning/coverage/days/rebalance门槛全部从带owner/version/rationale/review condition的pilot policy读取。`eligible_for_review`仅是manual review queue，不是promotion。Summary/manifest/report仅从snapshot物化，validator从snapshot重算并检查checksum/lineage/cutoff/null/policy/no-execution；legacy unsnapshotted只读warning。相邻outcome accumulation不得在append event后旁路改写windows；review允许的window集合必须在event创建前合并到`outcome_windows`，使event、manifest、report和validator保持一致。本层只写aging/outcome evaluation evidence，无upstream/config/official/paper/real/production/order/broker effect。
- 2026-07-12：G2.4AO Dynamic-v3 Owner Attribution slice `COMPLETE`，G2.4继续。3 callback已迁至canonical owner；legacy root 27,086→27,004行、801→798函数、762→759 decorators；focused=`107 passed`、architecture-fitness=`224 passed`，generated=`853 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI仍为993 leaf/0 duplicate且tree hash不变。Owner event replay/逐review validation、outcome strict validation/one-daily-one-outcome/as-of lineage、generated cutoff、immutable owner/outcome/source-validation snapshots、四类sample unit、null missing horizons、content-derived full-view validator与legacy warning均已实现；invalid/future/duplicate/rehash tamper fail closed。研究执行链详文记录设计原因、输入输出、计算与非因果解释边界。该层只写观察性attribution evidence，无upstream/portfolio/official/policy/production/order/broker effect。
- 2026-07-12：G2.4AO Dynamic-v3 Owner Attribution contract freeze并进入`IN_PROGRESS`。迁移边界为`owner-attribution run/report`及`validate-owner-attribution`三callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_owner_attribution.py`。Run必须先验证Owner Review event chain/selected records与每个相关Advisory Outcome，按generated cutoff阻断未来owner/outcome events，并强制one daily→zero-or-one outcome及review/outcome id/as-of一致。Immutable owner/outcome snapshots保存source paths/checksums/validator evidence和latest windows；summary/matrix/comparison/report仅从snapshots物化。Comparison显式区分review/outcome/window样本单位，缺5d/20d或非AVAILABLE证据保持null/reason，不能转0。Validator从snapshots重算全部views并检查bindings/checksums/no-execution；legacy unsnapshotted只读warning。只写attribution evidence/latest pointer，不修改任何upstream、portfolio、official/policy/production/broker或生成order。
- 2026-07-12：G2.4AN Dynamic-v3 Advisory Outcome slice `COMPLETE`，G2.4继续。4 callback已迁至canonical owner；legacy root 27,212→27,086行、805→801函数、766→762 decorators；focused=`100 passed`、architecture-fitness=`223 passed`，generated=`852 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI仍为993 leaf/0 duplicate且tree hash不变。Track prerequisite/frozen source/duplicate gate、immutable advisory event、single-portfolio as-of/known-time action cutoff、next-complete-date effective timing、append-only update checksum chain、immutable DQ/required-symbol snapshots、fixed-share/piecewise paths、turnover costs、null non-available metrics、PARTIAL rollup及content-derived replay validator均已实现。未来action、as-of reversal、missing path、rehashed event与view/source tamper均fail closed；legacy unchained只读warning且不可update。研究执行链详文记录why、inputs/outputs、全部公式、current results与optimization backlog。该层只写outcome evidence，无paper/real/official/policy/production/order/broker effect。
- 2026-07-12：G2.4AN Dynamic-v3 Advisory Outcome contract freeze并进入`IN_PROGRESS`。迁移边界为`advisory-outcome track/update/report`及`validate-advisory-outcome`四callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_advisory_outcome.py`。Track必须验证并冻结Daily Advisory、reviewed outcome config、ETF baseline files及selected Paper Portfolio lineage，拒绝duplicate tracker；immutable advisory event只保存decision-time weights/sources。Update采用append-only checksum event chain和每次immutable DQ/required-symbol price snapshot，as-of单调；paper action只能来自track绑定portfolio且不得晚于updated-as-of，在action后首个complete trading date生效，禁止look-ahead。Window必须用required-symbol complete dates；fixed-share/static与piecewise paper path、turnover cost、drawdown/volatility均可重算，pending/insufficient metric为null并带reason，rollup支持PARTIAL。Materialized windows/manifest/report由latest update event产生；validator从frozen sources/snapshots逐event重算。Legacy unchained只读warning，update fail closed。该层不修改paper/real/official/production/broker，不生成order。
- 2026-07-12：G2.4AM Dynamic-v3 Paper Portfolio slice `COMPLETE`，G2.4继续。5 callback已迁至canonical owner；legacy root 27,373→27,212行、810→805函数、771→766 decorators；focused=`136 passed`、architecture-fitness=`222 passed`，generated=`851 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI仍为993 leaf/0 duplicate且tree hash不变。Init source validation/checksum、append-only `paper_action_ledger.v2`、one review→one action、Owner Review/Daily Advisory/config binding、manual finite zero-sum、policy-limit formula replay以及state/history/manifest/report materialization均已实现；source drift、duplicate/as-of reversal、legacy mutation、rehashed forged event和view/report tamper均fail closed。研究执行链详文已记录why、inputs/outputs、gross-L1/symbol/sell-limit公式、current results与optimization backlog。该层只更新隔离paper state，无real/official/production/order/broker effect；后续增强不得把paper validator PASS解释为execution authorization。
- 2026-07-12：G2.4AM Dynamic-v3 Paper Portfolio contract freeze并进入`IN_PROGRESS`。迁移边界为`paper-portfolio init/apply-review/state/report`及`validate-paper-portfolio`五callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_paper_portfolio.py`。Canonical state由validated initial snapshot/config与append-only、portfolio-local sequence/previous checksum/event checksum action ledger重放得到；state/history/manifest/report只能作为materialized views。Init必须验证policy metadata/safety/finite thresholds及manual snapshot owner-reviewed、unique/finite/nonnegative/sum-one，并冻结paths/checksums。Apply必须先要求current paper validator、Owner Review validator及其frozen Daily Advisory source全部PASS，拒绝caller root替换、source drift、duplicate review、pending/invalid decision；paper adjustment从daily consensus重算，manual adjustment必须explicit/non-empty/finite/zero-sum，再按versioned policy确定性限幅。Validator逐event重算proposal/limit/after并核对source binding、checksum chain、views/report与no-execution。Legacy unchained只读并明确warning，mutation fail closed。该层仅更新paper simulation evidence，不修改real/official/production/broker，不生成order。
- 2026-07-12：G2.4AL closeout validation=`focused 120 passed / architecture-fitness 221 passed`；architecture runtime artifact=`outputs/validation_runtime/architecture-fitness_20260712T003441Z/test_runtime_summary.json`。
- 2026-07-12：G2.4AL Dynamic-v3 Owner Review Journal slice `COMPLETE`，G2.4继续。5 callback已迁至canonical owner；legacy root 27,497→27,373行、815→810函数、776→771 decorators；focused=`120 passed`，generated=`850 modules / 1,114 tests / 858 direct writers / 0 violations`，CLI仍为993 leaf/0 duplicate且tree hash不变。Owner review现在以append-only、sequence与checksum chain事件为source of truth；producer在任何write前验证并冻结daily advisory七类source，强制one daily→one review、pending→one final decision和shared privacy gate。兼容journal/latest/report从事件确定性物化，paper action绑定decision event和daily deltas并由validator重算；source/event/view/report/paper tamper、二次决定、重复daily及敏感notes均fail closed。Legacy unchained只读并显式warning，等待独立migration。研究执行链详文已记录why、inputs、outputs、calculations、current results、fail-closed和optimization backlog。本层无portfolio/official/production/order/broker effect；后续可增加identity/signature/trusted time、supersede/revoke、transactional append及retention/encryption，但不得把review decision解释为execution authorization。
- 2026-07-12：G2.4AL Dynamic-v3 Owner Review Journal contract freeze并进入`IN_PROGRESS`。迁移边界为`owner-review create/list/report/record-decision`与`validate-owner-review`五callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_owner_review_journal.py`。Create必须先验证显式daily advisory并冻结七类source files；新`owner_review_events.jsonl`使用append-only sequence、previous checksum和event checksum，兼容`owner_review_journal.jsonl`/latest/report只能从事件重放物化。每个daily advisory最多一个review，每个review只允许pending→一个final decision；二次覆盖、source drift、event chain异常和敏感notes均在写前阻断。Paper adjustment只追加一条可从daily deltas重算的paper-only action。Validator重放event/source并核对journal/latest/summary/report/paper log。Legacy无event chain只读时必须显式`PASS_WITH_WARNINGS/LEGACY_UNCHAINED`且禁止mutation，等待独立migration。该层不修改daily/portfolio/official/production/broker，不生成order或执行approval。
- 2026-07-12：G2.4AK Dynamic-v3 Consensus Drift slice `COMPLETE`，G2.4继续。3 callback迁至`dynamic_v3_consensus_drift.py`，legacy root 27,580→27,497行、818→815函数、779→776 decorators；focused=`95 passed`、architecture-fitness=`220 passed`，generated=`848 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Current monitor prerequisite与target invariants、semantic cutoff-bound previous selection、correct previous id、all-symbol-union delta、policy-tolerance agreement、all-non-consensus manual review、source/checksum/inventory/output binding和content-derived selection/CSV/summary/manifest/Markdown validation均已实现；invalid/future/current/prior/source/output tamper、mtime reversal、cutoff stability和invalid policy threshold测试通过。权威研究链详文已记录全部输入输出、计算公式、状态优先级、fail-closed条件与优化空间。该层仅写consensus review evidence/latest pointer，无monitor/config/advisory/portfolio/official/production/order/broker effect。
- 2026-07-12：G2.4AK Dynamic-v3 Consensus Drift contract freeze并进入`IN_PROGRESS`。迁移边界为`consensus-drift run/report`与`validate-consensus-drift`三callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_consensus_drift.py`。Producer必须在output前要求current monitor validator PASS并校验id/as-of、candidate唯一性和weights有限/范围/守恒；policy继续来自reviewed `position_advisory_v1.yaml`。Previous monitor只按同shortlist、prior as-of、generated cutoff和semantic `(as_of, generated_at, id)`选择，latest relevant invalid必须fail closed；previous change记录真实source id、candidate-set状态并比较全symbol union。Manifest冻结current/prior monitor六类files、config、previous inventory及outputs checksums；validator从source重算pairwise distance、policy-tolerance symbol agreement/dispersion、risk/cash/defensive exposure dispersion、disagreement、previous change、manifest derived fields和Markdown。所有non-consensus均强制`manual_review_required`。仅允许写consensus drift evidence/latest pointer，不修改monitor/config/advisory/portfolio/official/production/broker state，不生成order。
- 2026-07-12：G2.4AJ Dynamic-v3 Daily Position Advisory slice `COMPLETE`，G2.4继续。3 callback迁至`dynamic_v3_position_advisory_daily.py`，legacy root 27,682→27,580行、821→818函数、782→779 decorators；focused=`86 passed`、architecture-fitness=`219 passed`，generated=`847 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Monitor prerequisite/id/as-of/target-weight invariants、policy-tolerance agreement、all non-consensus manual-review gate、semantic same-chain drift selection、future snapshot block、source/checksum/inventory/output binding和content-derived target/consensus/delta/action/manifest/report/Reader Brief validation均已实现；monitor/drift/output/source tamper、future snapshot和mtime reversal领域测试通过。权威研究链详文已记录全部输入输出、计算逻辑、状态优先级、fail-closed条件与优化空间。该层仅写daily manual-review evidence/latest pointer，无official/portfolio/production/order/broker effect。
- 2026-07-12：G2.4AJ Dynamic-v3 Daily Position Advisory contract freeze并进入`IN_PROGRESS`。迁移边界为`position-advisory daily-run/daily-report`与`validate-position-advisory-daily`三callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_position_advisory_daily.py`；consensus drift CLI留在后续切片。Run先要求指定monitor validator PASS，校验monitor id/as-of、candidate id唯一、target weights有限且守恒；snapshot as-of不得晚于monitor。Daily consensus的agreement tolerance必须是reviewed `max_symbol_dispersion`而非observed dispersion本身，非`CONSENSUS`或drift evidence insufficient均强制manual review。Optional consensus drift仅可选monitor/config checksum/as-of一致、validator PASS、summary status与recomputed status一致的artifact；按`generated_at/id`在daily generated cutoff前选择，不使用mtime。Run冻结monitor/config/snapshot/drift source paths/checksums和drift inventory，validator重算targets/consensus/deltas/actions/manifest/report/Reader Brief并校验outputs。仅允许写daily advisory evidence/latest pointer，不修改monitor/drift/config/snapshot/portfolio/official/production/broker state，不生成order。
- 2026-07-12：G2.4AI Dynamic-v3 Position Advisory slice `COMPLETE`，G2.4继续。3 callback迁至`dynamic_v3_position_advisory.py`，legacy root 27,770→27,682行、824→821函数、785→782 decorators；focused=`77 passed`、architecture-fitness=`218 passed`，generated=`846 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。上游validator prerequisite、全candidate complete weight-path gate、shortlist/config/snapshot/weight source checksums、output checksums与content-derived target/consensus/delta/action/manifest/report validation均已实现；上游失效、source drift和output tampering测试通过。权威研究链详文已补齐设计原因、输入输出、计算公式、状态优先级和优化空间。该层仅写manual-review evidence/latest pointer，无official/portfolio/production/order/broker effect。
- 2026-07-12：G2.4AI Dynamic-v3 Position Advisory contract freeze并进入`IN_PROGRESS`。迁移边界为`position-advisory run/report`与`validate-position-advisory`三callback，canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_position_advisory.py`；daily advisory留在后续切片。Run只消费显式shadow shortlist、reviewed policy config和可选manual portfolio snapshot，必须先通过shadow-shortlist validation，冻结shortlist manifest/candidates、每个candidate weight path、config和snapshot的path/checksum。Validator重读source重算candidate latest targets、median/dispersion/agreement consensus、snapshot delta/limits、action/status、manifest derived fields和Markdown，source drift、产物篡改或绕过upstream validation必须FAIL。无snapshot=`TARGET_ONLY/monitor`；有snapshot始终仍为manual-review advisory，`small_adjustment/no_trade/manual_review`只是review suggestion，不是official weights、approval或execution authorization。仅允许写advisory evidence/latest pointer，不修改shortlist/weight/config/snapshot/portfolio/official/production/broker state，不生成order。
- 2026-07-12：G2.4AH Dynamic-v3 Weekly Real Snapshot Review slice `COMPLETE`，G2.4继续。3 callback迁至`dynamic_v3_weekly_real_snapshot_review.py`，legacy root 27,855→27,770行、827→824函数、788→785 decorators；focused=`107 passed`、architecture-fitness=`217 passed`，generated=`845 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Week-ending cutoff、owner-chain anchor、same-chain paper selection、owner inventory/source checksum和content-derived summary/render validation均已实现；cross-chain、future-as-of、source/output drift测试通过。Reader Brief改读weekly frozen sources，并同步F3/deprecation ratchet为29,050行/367函数。该层仅写weekly evidence，无upstream/portfolio/official/production/order/broker effect。
- 2026-07-12：G2.4AH Dynamic-v3 Weekly Real Snapshot Review contract freeze并进入`IN_PROGRESS`。迁移边界为weekly run/report/validate三callback；canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_weekly_real_snapshot_review.py`。Baseline independent-latest选择存在cross-chain与future-as-of风险；新契约按`week_ending`过滤source timestamps，以最新owner链为锚点并只接受同owner/dry的paper action，无owner才选择最新dry。Run冻结source paths/checksums和owner decision inventory并输出显式chain status；validator重做选择、五类source validation、counts、summary、Markdown/Reader Brief。只写weekly evidence，不运行上游或修改任何source/portfolio/official/production/broker state，不生成order。
- 2026-07-12：G2.4AG Dynamic-v3 Real Snapshot Paper Action slice `COMPLETE`，G2.4继续。3 callback迁至`dynamic_v3_real_snapshot_paper_action.py`，legacy root 27,937→27,855行、830→827函数、791→788 decorators；focused=`96 passed`、architecture-fitness=`216 passed`，generated=`844 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Pending fail-before-write、五类source validation、九类path/checksum binding、lineage和content-derived action/weight重算由领域测试覆盖，source drift与output tampering均fail closed；paper-only/no-action均不修改real snapshot、existing paper portfolio、official weights、production/broker或生成order。
- 2026-07-12：G2.4AG Dynamic-v3 Real Snapshot Paper Action contract freeze并进入`IN_PROGRESS`。迁移边界为paper action apply/report/validate三callback；canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_real_snapshot_paper_action.py`。Apply必须先验证显式owner review为PASS且decision非pending，再验证dry-run/manual snapshot/drift/guardrail sources；`paper_adjustment_review_only`只投影未blocked capped deltas，其他reviewed decisions只记录no-action。当前validator缺source binding与content-derived重算，本slice同步增加source paths/checksums/id lineage、before/proposed/applied/after/action type重算，source drift或artifact tampering fail closed。允许写isolated paper evidence，不修改real snapshot、既有paper portfolio、official weights、production/broker，不生成order。
- 2026-07-12：G2.4AF Dynamic-v3 Real Execution Owner Decision Journal slice `COMPLETE`，G2.4继续。4 callback迁至`dynamic_v3_real_execution_owner_review.py`，legacy root 28,032→27,937行、834→830函数、795→791 decorators；focused=`89 passed`、architecture-fitness=`215 passed`，generated=`843 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Create/record/report/validate契约与领域privacy测试通过：敏感notes在任何写入前fail closed，validator同时扫描manifest/decision以发现tampering；owner decision record不运行dry-run、不自动paper action、不生成order或修改official weights/portfolio/production/broker。单份decision覆盖写仍是后续append-only audit优化项。
- 2026-07-12：G2.4AF Dynamic-v3 Real Execution Owner Decision Journal contract freeze并进入`IN_PROGRESS`。迁移边界为owner review create/record/report/validate四callback；canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_real_execution_owner_review.py`。Create建立pending审计记录，record只接受reviewed非pending枚举，report只读，validator fail closed。同步修复notes safety契约与实现不一致：写入前和validator均必须拒绝长数字及account/order/tax-lot/SSN/passport/national-id/personal-identifier/statement-path敏感token。允许记录人工decision，不允许自动paper action、order、official weights、portfolio/production/broker effect。
- 2026-07-12：G2.4AE Dynamic-v3 Real Snapshot Dry-run Orchestration slice `COMPLETE`，G2.4继续。3 callback迁至`dynamic_v3_real_snapshot_dry_run.py`，legacy root 28,115→28,032行、837→834函数、798→795 decorators；focused=`86 passed`、architecture-fitness=`214 passed`，generated=`842 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。领域测试证明intake/manual snapshot/exposure/drift/guardrail/manual review六类links与status完整且validator PASS；run只生成research evidence，不创建owner/paper/order/portfolio/production/broker effect。
- 2026-07-12：G2.4AE Dynamic-v3 Real Snapshot Dry-run Orchestration contract freeze并进入`IN_PROGRESS`。迁移边界为real snapshot dry-run run/report/validate三callback；canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_real_snapshot_dry_run.py`。显式run允许串联并写exposure/drift/guardrail/manual-review/dry-run research artifacts，但前提是指定snapshot intake PASS且有manual snapshot link，并指定shadow shortlist；所有links/status必须写入summary。该允许副作用不扩展到intake、owner decision、paper action、order、official weights、real portfolio、production或broker。Report只读、validator fail closed；`recommended_action`只供人工复核。
- 2026-07-12：G2.4AD Dynamic-v3 Real Snapshot Intake slice `COMPLETE`，G2.4继续。5 callback迁至`dynamic_v3_real_snapshot_intake.py`，legacy root 28,226→28,115行、842→837函数、803→798 decorators；focused=`84 passed`、architecture-fitness=`213 passed`，generated=`841 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Template/redaction/sensitive-field fail-closed、manual snapshot validation和tmp-output latest-pointer isolation由领域测试覆盖；manual snapshot link仅为research lineage，不运行dry-run/owner/paper loop，不产生broker/order/portfolio/production effect。
- 2026-07-12：G2.4AD Dynamic-v3 Real Snapshot Intake contract freeze并进入`IN_PROGRESS`。迁移边界为real snapshot template/lint/intake/report/validate五callback；canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_real_snapshot_intake.py`，dry-run及后续owner/paper loop不并入。Template/lint冻结redaction-safe contract；intake必须先阻断broker/account/order/tax-lot/PII/statement path字段，再校验manual portfolio并写脱敏artifact。关联manual snapshot不是broker sync或real portfolio mutation。Report只读、validator fail closed；不得补值、运行下游、生成order、改official weights/portfolio/production或触发broker。
- 2026-07-12：G2.4AC Dynamic-v3 Manual Execution Review slice `COMPLETE`，G2.4继续。3 callback迁至`dynamic_v3_manual_execution_review.py`，legacy root 28,335→28,226行、845→842函数、806→803 decorators；focused=`79 passed`、architecture-fitness=`212 passed`，generated=`840 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Review pack generation/validation与Reader Brief领域测试通过；只聚合显式source artifacts，`manual_execution_decision`保持待owner复核语义，不记录approval、不生成order、不触发broker或修改weights/portfolio/production。
- 2026-07-12：G2.4AC Dynamic-v3 Manual Execution Review contract freeze并进入`IN_PROGRESS`。迁移边界为manual execution review pack/report/validate三callback；canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_manual_execution_review.py`。Pack只聚合显式snapshot/exposure/drift/guardrail artifacts并生成待owner复核的recommendation/checklist；`manual_execution_decision`不是owner decision record，PASS也不是approval。Report只读，validator fail closed；不得运行上游、修改source/policy/weights/portfolio、记录owner approval、生成order ticket或触发production/broker。固定`order_ticket_generated=false`、`broker_action_allowed=false`、`owner_approval_required=true`。
- 2026-07-12：G2.4AB Dynamic-v3 Portfolio Risk Controls slice `COMPLETE`，G2.4继续。9 callback迁至`dynamic_v3_portfolio_risk_controls.py`，legacy root 28,604→28,335行、854→845函数、815→806 decorators；focused=`76 passed`、architecture-fitness=`211 passed`，generated=`839 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Exposure decomposition、candidate/consensus drift/agreement及guardrail cap/block行为由领域测试覆盖；模块只消费显式已有artifact与reviewed config，不运行intake/manual review，不修改source、policy、threshold、weights、portfolio、production或broker。所有结果保持diagnostic/manual-review-only。
- 2026-07-12：G2.4AB Dynamic-v3 Portfolio Risk Controls contract freeze并进入`IN_PROGRESS`。迁移边界为portfolio exposure、position drift、execution guardrails各run/check/validate、report、artifact validate共9 callback；canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_portfolio_risk_controls.py`，manual execution review不并入本slice。输入必须是显式已有且归属一致的snapshot/shortlist/exposure/drift artifact及reviewed policy config；计算保持exposure decomposition、candidate/consensus delta/agreement、cap/block/stepwise review语义；report只读已有artifact，validator fail closed。该层不运行intake或manual review，不改source/config/threshold/official weights/real portfolio/production/broker；任何PASS、cap、block或`recommended_action`都只供owner复核，不是order ticket或execution authorization。
- 2026-07-12：G2.4AA Dynamic-v3 Portfolio Intake slice `COMPLETE`，G2.4继续。7 callback迁至`dynamic_v3_portfolio_intake.py`，legacy root 28,802→28,604行、861→854函数、822→815 decorators；focused=`69 passed`、architecture-fitness=`210 passed`，generated=`838 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。Portfolio/manual snapshot的显式source、schema/weight/path/id、normalized summary和fail-closed行为保持；intake不读取broker、不补造position、不串行运行exposure/drift/guardrail。PASS仅代表输入artifact可消费，不代表portfolio approval、position advisory、official target weights、production mutation或broker authorization。
- 2026-07-12：G2.4AA Dynamic-v3 Portfolio Intake contract freeze并进入`IN_PROGRESS`。迁移边界为portfolio snapshot validate/report/normalize、manual portfolio validate/normalize/report及rescue validate-manual-portfolio共7 callback；canonical owner为`interfaces/cli/etf_portfolio/dynamic_v3_portfolio_intake.py`。输入只接受显式snapshot YAML、reviewed schema config或既有snapshot id；输出只允许validation、normalized portfolio/positions和report artifacts。必须保持schema/weight/path/id/fail-closed及CLI parity；normalized/PASS不等于portfolio approval、position advisory、official target weights、production state或broker authorization，不自动读取broker账户、不补造持仓、不串行触发exposure/execution后续链。
- 2026-07-11：G2.4N Data Provenance slice `COMPLETE`，G2.4继续。最终provenance/contract组合=`58 passed`、architecture-fitness=`197 passed`，generated=`825 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback及85行；inspect/validate只读、explicit reconstruction、强制RECONSTRUCTED/limitation与no-fabrication边界通过。
- 2026-07-11：G2.4N Data Provenance contract freeze。迁`data-provenance inspect-price-cache/repair-price-manifest/validate`。Inspect只读cache/manifest并写provenance report/latest pointer；validate只读且要求price/rates cache、manifest和对应checksums。Repair是唯一受控写路径，仅接受显式`reconstruct-from-cache`，要求所有cache文件存在，以当前文件checksum替换同output_path的manifest rows并写`source=cache_rebuild_from_existing_file`/`provenance_status=RECONSTRUCTED_MANIFEST`/`limitations=original_download_event_not_available`；不得声称恢复provider、endpoint、download timestamp或原始事件，不得把reconstructed提升为primary-download provenance。禁止candidate/backtest/promotion/production/broker。
- 2026-07-11：G2.4O Window Audit contract freeze。迁`window-audit run/report/inspect-artifact`与`validate-window-audit`四个callback。`run --as-of`语义冻结为本次`requested_start`而非报告观察时点，必须与`requested_end`、policy解析出的`configured_backtest_start`、artifact披露的`actual_evaluation_start/end`分字段输出；`ai_after_chatgpt=2022-12-01`仍是项目默认AI周期主结论起点，早于该日起点的2021/2020数据只能按已登记research-window policy解释为primary strategy evidence、warm-up、stress、sensitivity或regime comparison，不得由window audit静默改写market-regime默认值。当前domain只对missing/invalid range、actual start晚于configured start及actual end早于requested end实施fail-closed/promotion blocker；迁移不得把“actual start早于2022-12-01”误报为违规，也不得声称现有审计已验证每个pre-regime artifact的evidence role。`report`与`inspect-artifact`只读，`validate`要求完整artifact inventory字段并fail closed；禁止candidate generation、backtest execution、promotion、production和broker。同步维护`docs/research/current_research_strategy_execution_chain.md`，把每环节why/input/calculation/output/blocker/optimization trigger作为后续研究链路权威说明。
- 2026-07-11：G2.4O Window Audit slice `COMPLETE`，G2.4继续。四个callback迁canonical，root 31,379→31,277行、956→952函数、917→913 decorators；focused=`48 passed`、architecture-fitness=`198 passed`，generated=`826 modules / 1,114 tests / 858 direct writers / 0 violations`。配置/请求/实际/AI-regime窗口分层、pre-regime evidence role边界、missing/late/short fail-closed、只读report/inspect与no candidate/backtest/promotion/production通过；研究执行链路文档同步补齐12环节I/O、计算、阻断与优化入口。
- 2026-07-11：G2.4P Injection Audit contract freeze与正确性修复。迁`injection-audit run/report`及`validate-injection-audit`三callback。审计确认旧`_parameter_effect_summary`把全体候选的config/metric/weight hash差异重复归因给每个参数，且`max_candidates*20`的grid前缀不能保证覆盖前部参数轴，存在把其他参数效应误标为当前参数`EFFECTIVE`的混杂风险。本slice必须改为deterministic one-factor-at-a-time candidate set：先生成共同base，再为每个required parameter生成仅该轴变化的matched pair，剩余额度才由普通grid补齐；参数结论只允许使用matched pairs，实际effective policy hash未变化=`NOT_CONSUMED`，policy hash变化但metric/weight均未变化=`NO_OBSERVED_EFFECT`，两者均变化才=`EFFECTIVE`，无matched pair=`INSUFFICIENT_MATCHED_PAIR_EVIDENCE`。必须独立写`parameter_effect_summary.json`并让report只读消费；默认20候选应覆盖全部required parameters，显式过小budget只能生成`INCOMPLETE`且validation fail closed。Real run仍先走cached DQ/PIT context；禁止把config declaration当消费证据、禁止candidate promotion、production和broker。
- 2026-07-11：G2.4P Injection Audit slice `COMPLETE`，G2.4继续。3 callback迁canonical并完成OFAT attribution correctness fix；root 31,277→31,183行、952→949函数、913→910 decorators。Focused=`53 passed`、reporting/policy组合=`70 passed`、architecture-fitness=`199 passed`，generated=`827 modules / 1,114 tests / 858 direct writers / 0 violations`。Base+7逐轴pair、matched-pair-only taxonomy、独立summary artifact、legacy artifact降级`INCOMPLETE`、小budget fail-closed、report registry/catalog/runbook/research-chain更新及no promotion/production通过。
- 2026-07-11：G2.4Q Weight Path contract freeze与validator hardening。迁`weight-path validate/report`两callback。审计确认exporter当前只生成minimal path并诚实声明`PARTIAL`，但旧validator仅检查文件存在、daily CSV列、metadata声明与正数count，无法阻止伪造`COMPLETE`、metadata count/start/end/symbol与CSV不一致、duplicate date-symbol、非数值/越界/不归一权重、candidate/evaluation id错配、event/turnover schema或candidate id错配；旧report也直接把metadata declaration当observed status。本slice必须由同一只读evidence inspection path重新计算：唯一evaluation目录、daily row/schema/date/id/duplicate/weight bounds与per-date sum、metadata parity、event JSON schema/id、turnover schema/id/nonnegative。Core path无效=`INCOMPLETE`且validate FAIL；core有效但detail level minimal或missing_fields非空=`PARTIAL`且artifact validation可PASS；只有core有效、detail complete、required detail columns逐行可解析、missing_fields空、所有flags与files一致才=`COMPLETE`。Report必须同时披露declared/observed completeness和limitations，不补造path、不改source artifact；禁止promotion/production/broker。
- 2026-07-11：G2.4Q Weight Path slice `COMPLETE`，G2.4继续。2 callback迁canonical，root 31,183→31,144行、949→947函数、910→908 decorators；focused=`59 passed`、architecture-fitness=`200 passed`，generated=`828 modules / 1,114 tests / 858 direct writers / 0 violations`。Shared content inspection、declared/observed分离、唯一目录、daily权重守恒与metadata/event/turnover parity、伪造COMPLETE/detail-level及内容失真fail-closed、valid minimal `PARTIAL`、report registry/catalog/system-flow/research-chain修正与no source mutation/promotion/production通过。TRADING-112仍为`VALIDATING`，因为真实exporter尚缺pre-constraint/post-rescue/constraint-limit字段，且真实20-candidate evidence重跑尚未完成。
- 2026-07-12：G2.4R Candidate Evidence contract freeze并进入正确性修复。迁`candidate report/attribution`与`validate-candidate-attribution`三callback。审计确认旧attribution会以`candidate_report_payload(write=True)`隐式重写上游candidate report，直接信任weight metadata声明；`_candidate_weight_delta_rows`从不含`latest_weights`的top-level `best_candidate`取candidate weights，导致现有真实`weight_path_delta.csv`只有`status`表头却仍可标`PARTIAL`；旧validator只查目录/文件和status枚举，不能阻止source sweep/candidate/real artifact串线、伪造completeness、空/错误delta或sub-artifact内容错配。本slice冻结为显式阶段链：candidate report必须先存在，attribution只读消费且不得补建/改写；real artifact必须位于source sweep的`real_evaluation/<candidate_id>/`且report id一致；weight completeness必须来自G2.4Q shared inspection的observed status；latest candidate weights从validated daily path读取，并与同一real report中实际提供完整daily path的`static_base_candidate` reference比较，delta逐行重算。当前real artifact没有导出dynamic_v0_4 daily weights，因此不得虚构v0.4 weight delta；该比较留作后续exporter优化。Manifest记录source paths/checksums、declared/observed status、limitations和component status；validator从source和artifact内容重算，诚实`INCOMPLETE/PARTIAL` package可结构PASS，但伪造/串线/算术错配必须FAIL。禁止运行sweep/real evaluation、自动promotion、source mutation、production或broker。
- 2026-07-12：G2.4R Candidate Evidence slice `COMPLETE`，G2.4继续。3 callback迁canonical，root 31,144→31,059行、947→944函数、908→905 decorators；focused=`62 passed`、architecture-fitness=`201 passed`，generated=`829 modules / 1,114 tests / 858 direct writers / 0 violations`。显式candidate-report prerequisite、source candidate-results/report checksums、sweep/candidate/real-report path ownership、G2.4Q observed weight status、candidate-vs-static-base daily delta重算、component/source-analysis status、output checksum与content-derived validator通过；伪造status、stale source和delta篡改均fail closed。当前`path_and_aggregate_v2`诚实封顶`PARTIAL`，dynamic_v0_4 daily path与逐event完整attribution仍是TRADING-105后续优化，不promotion、不改source、不产生production/broker effect。
- 2026-07-12：G2.4S Walk-forward / Overfit Evidence contract freeze与正确性修复启动。范围固定为`walk-forward select-run/selection-report`、`validate-walk-forward-selection`、`overfit run/report`、`validate-overfit`六callback。审计确认旧walk-forward所谓train/test实际复用全周期candidate score/metrics并加入stable-hash jitter/drift，profile/config不约束source sweep，validator只查文件；旧overfit把全局rank percentile、synthetic neighborhood和aggregate extreme-day proxy包装成风险组件，validator允许仅改status为`LOW_RISK`。本slice要求real evaluator按source real artifact daily path和冻结window切片重算train/test evidence；tiny fixture继续只做contract proxy且不得形成投资解释PASS；manifest锁定sweep config/results/real paths/checksums、evidence method/completeness和output checksums；validator从source重算window、ranking、selection、test、component/status/checksum。任何proxy/缺失component使overfit最高`REVIEW_REQUIRED`，不能靠改status升级；旧`51fb80c576c29d2e PASS`证据降级为superseded，需当前实现真实重跑。迁移不得运行新sweep、自动promotion/enrollment、production或broker。
- 2026-07-12：G2.4S Walk-forward / Overfit Evidence slice `COMPLETE`，G2.4继续。6 callback迁canonical，root 31,059→30,896行、944→938函数、905→899 decorators；focused=`65 passed`、architecture-fitness=`202 passed`，generated=`830 modules / 1,114 tests / 858 direct writers / 0 violations`。全周期score/metrics与hash jitter/drift已移除，global gate不再预删train candidate，unsupported score contribution归零，全体train reject时不选占位candidate；source/profile/config/real/output checksums和content-derived validators通过。真实`sweep_20260607T102300Z_ae5ae1d8`重算300/300 path usable，但2/2 windows均`NO_ELIGIBLE_TRAIN_CANDIDATE`，新artifact `1ded73491a9da880`诚实`INCOMPLETE`且validation PASS；错误中间artifact `4aa0f92422101d7b`validation FAIL；独立overfit v2 smoke `02b5d9d041395362`为`REVIEW_REQUIRED`且validation PASS。TRADING-106/107保持`VALIDATING`，等待window-specific full gate、逐fold evaluator/neighbor/rank和multiple-testing证据。
- 2026-07-12：G2.4T Legacy Walk-forward / Robustness Evidence contract freeze并进入正确性修复。范围固定为`walk-forward run/report`、`validate-walk-forward`、`robustness run/report`与`validate-robustness`六个callback。TRADING-096不得再把full-period metrics复制到window并添加stable-hash drift；real evaluator必须对source sweep中top-N candidate的同一dynamic/static daily path按train/test日期切片重算，tiny fixture仅保留contract proxy且总体最高`INCOMPLETE`。TRADING-097必须区分dedicated path-derived evidence、aggregate policy check和missing evidence；观察到regime不等于regime stability `PASS`，aggregate drawdown/overfit analysis不等于dedicated stress bucket `PASS`。Manifest必须锁定sweep manifest/config/leaderboard/results、candidate及real artifact path/report-id ownership、source checksums、component method/completeness/limitations与output checksums；validator从source重算windows/results/summaries/sensitivity/stress/regime/diagnostics/Markdown并拒绝源数据漂移或artifact篡改。本slice不运行新sweep，不自动shadow enrollment/promotion，不写production或broker state；旧`c49f65c76e2b9b73 PASS`与`6df822e705e15a42`只作历史workflow记录，不再作为当前证据充足性结论。
- 2026-07-12：G2.4T Legacy Walk-forward / Robustness Evidence slice `COMPLETE`，G2.4继续。6 callback迁至`dynamic_v3_legacy_validation.py`，旧root 30,896→30,762行、938→932函数、899→893 decorators；focused=`66 passed`、architecture-fitness=`203 passed`，generated=`831 modules / 1,114 tests / 858 direct writers / 0 violations`；CLI仍为993 leaf/0 duplicate且tree hash不变。TRADING-096删除hash-window helper并复用G2.4S path slicing，TRADING-097锁定source/neighbor identity并把aggregate stress/regime observation降级，两个validator均重算source/content/checksum/Markdown，shadow basis拒绝tiny及旧manifest。真实artifact：walk-forward `7b6db671cbd67468 PATH_DERIVED_PARTIAL/REVIEW_REQUIRED`、robustness `87b0fc81d6681368 PATH_AND_AGGREGATE_PARTIAL/REVIEW_REQUIRED`，validation均PASS；旧artifact validation均FAIL。TRADING-096/097保持`VALIDATING`；现有shadow registry因旧basis出现4项usable-evidence失败，TRADING-098转`VALIDATING`并要求owner显式重登记，未自动修改registry。No new sweep、promotion、production或broker effect。
- 2026-07-12：G2.4Z Dynamic-v3 Candidate Observation slice `COMPLETE`，G2.4继续。Shortlist、candidate cluster、shadow shortlist及shadow monitor activation/run/report/validate共13 callback迁至`dynamic_v3_candidate_observation.py`；root降至28,802行/861函数/822 decorators。CLI=993 leaf/0 duplicate/tree hash不变；focused=72、architecture-fitness=209，generated=837 modules/1,114 tests/858 writers/0 violations。所有输出保持observe-only/manual review，不自动enrollment、position advisory、production或broker。
- 2026-07-12：G2.4Y Dynamic-v3 Evidence Governance slice `COMPLETE`，G2.4继续。Research decision、evidence diagnosis、gate impact/policy、candidate recovery、observe-pool rebuild与decision update共19 callback迁至`dynamic_v3_evidence_governance.py`；root降至29,165行/874函数/835 decorators。CLI=993 leaf/0 duplicate/tree hash不变；focused=71、architecture-fitness=208，generated=836 modules/1,114 tests/858 writers/0 violations。Simulation/policy/recovery不改写hard blockers、旧shadow registry、promotion、production或broker。
- 2026-07-12：G2.4X Dynamic-v3 Evidence Readiness slice `COMPLETE`，G2.4继续。Evidence summary、medium-real、regime coverage、candidate interpretation、observe pool和overnight readiness共17 callback迁至`dynamic_v3_evidence_readiness.py`；root降至29,703行/893函数/854 decorators。CLI=993 leaf/0 duplicate/tree hash不变；focused=70、architecture-fitness=207，generated=835 modules/1,114 tests/858 writers/0 violations。Readiness/coverage/observe-only不等于promotion/production，不写shadow registry、不运行overnight sweep或broker。
- 2026-07-12：G2.4W Dynamic-v3 Observation Lifecycle slice `COMPLETE`，G2.4继续。Shadow monitor run/report/validate、scheduled observe、promotion review/pack/validate共7 callback迁至`dynamic_v3_observation_lifecycle.py`；root降至30,163行/910函数/871 decorators。CLI=993 leaf/0 duplicate/tree hash不变；focused=69、architecture-fitness=206，generated=834 modules/1,114 tests/858 writers/0 violations。Observe gate保持lightweight，promotion pack保持manual-review-only，不自动promotion/enrollment/production/broker。
- 2026-07-12：G2.4V Dynamic-v3 Research Control slice `COMPLETE`，G2.4继续。Governance validate/report/diff、research index-build/query/compare/history、artifacts latest/validate/repair-latest/stale共11 callback迁至`dynamic_v3_research_control.py`；root降至30,391行/917函数/878 decorators。CLI=993 leaf/0 duplicate/tree hash不变；focused=68、architecture-fitness=205，generated=833 modules/1,114 tests/858 writers/0 violations。`repair-latest`保持canonical-root-only pointer重建边界，不执行sweep/candidate/promotion/production/broker。
- 2026-07-12：G2.4U Shadow Registry Contract slice `COMPLETE`，G2.4继续。4 callback迁`dynamic_v3_shadow_registry.py`，root降至30,628行/928函数/889 decorators；register使用成对显式artifact id、两份source-recomputing validator和candidate/sweep ownership形成basis，删除mtime selection与promotion latest fallback；validator重算source/basis且不自动修复。CLI=993 leaf/0 duplicate，新增两个显式id option并更新冻结tree hash；focused=67、docs/report/policy=45、architecture-fitness=204，generated=832 modules/1,114 tests/858 writers/0 violations。临时真实registry登记/validation PASS；runtime registry保持未写入并FAIL 12项，TRADING-098继续`VALIDATING`等待owner迁移。
- 2026-07-11：G2.4M Research Data Audit slice `COMPLETE`，G2.4继续。最终audit/contract组合=`57 passed`、architecture-fitness=`196 passed`，generated=`824 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback及84行；DQ-failure evidence、checksum/provenance、PIT/data-gap、latest/validation与no-cache-mutation边界通过。
- 2026-07-11：G2.4M Research Data Audit contract freeze。迁`data-audit run/report`与`dynamic-v3-rescue validate-data-audit`三callback到同一owner。Run显式要求as-of/end，调用同一validate-data路径并始终写可审计PASS/FAIL package；DQ FAIL不得伪装PASS，但data-audit作为质量诊断本身允许落盘失败证据。Package必须包含provider/cache paths、quality report、checksum/provenance、PIT coverage、data gap、issues、latest pointer与safety。Report latest/explicit只读；validate要求全部required artifacts且DQ非FAIL。禁止修改cache/manifest、生成candidate、运行backtest/PIT evaluation、promotion、production或broker。
- 2026-07-11：G2.4L Parameter Sweep Runtime slice `COMPLETE`，G2.4继续。最终runtime/contract组合=`56 passed`、architecture-fitness=`195 passed`，generated=`823 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减8 callback、1 helper及285行；profile/evaluator/DQ-PIT/resume/derived-artifact/no-production边界通过。
- 2026-07-11：G2.4L Parameter Sweep Runtime contract freeze。迁`profile-list/profile-validate/run-profile/run/status/validate/leaderboard/report`八callback及独占latest-id resolver。Profile list/validate只读；run-profile/run可写research sweep/checkpoint/leaderboard/report artifacts。`real_dynamic_v3_rescue`必须执行其内建DQ/PIT路径并保留source lineage；`tiny_fixture_proxy`必须`not_for_investment_decision=true`。Resume不得改变既有evaluator mode，允许显式worker override且记录manifest。Status/validate/report只读或按原语义补建derived leaderboard/report；所有结果candidate-only，禁止production candidate、promotion、approval、enrollment、official target或broker。
- 2026-07-11：G2.4K Parameter Sweep Config slice `COMPLETE`，G2.4继续。最终config/contract组合=`55 passed`、architecture-fitness=`194 passed`，generated=`822 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减2 callback及43行；config-only、stable-id、preview-limit和no-runtime/no-production边界通过。
- 2026-07-11：G2.4K Parameter Sweep Config contract freeze。审计决定与Sweep Runtime拆分：本slice只迁`sweep-config validate/preview`两callback；它们仅读取reviewed parameter-sweep policy、校验schema/安全字段并稳定枚举candidate_id，不运行evaluator/backtest/PIT/DQ、不写sweep runtime artifact，也不生成production candidate。Preview limit只限制展示行数，不改变candidate universe/count；禁止promotion、approval、enrollment、production或broker。Runtime `profile-list/.../report`留待G2.4L独立slice。
- 2026-07-11：G2.4J Dynamic v0.3 Failure Attribution slice `COMPLETE`，G2.4继续。最终attribution/contract组合=`40 passed`、architecture-fitness=`193 passed`，generated=`821 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback及290行；source lineage→DQ→price→PIT、range/regime分离、canonical validation与no-promotion/production边界通过。
- 2026-07-11：G2.4J Dynamic v0.3 Failure Attribution contract freeze。迁`failure-attribution/failure-attribution-report/validate-attribution`；先解析explicit/latest real-evaluation lineage与其requested range，再以explicit as-of/end或source end确定DQ日期，随后严格cached DQ→standard price→PIT attribution并fail closed。Start/end override不得改写source artifact，range/regime继续分开披露。v0.4 promotion review和v0.5 design recommendation仅为治理建议，禁止实际promotion、approval、enrollment、official target、production或broker；latest report只读，validation改查canonical owner。
- 2026-07-11：G2.4I Dynamic v0.3 Real Evaluation slice `COMPLETE`，G2.4继续。最终real/contract组合=`40 passed`、architecture-fitness=`192 passed`，generated=`820 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback及223行；DQ→price→PIT、range/regime分离、canonical validation与no-promotion/production边界通过。
- 2026-07-11：G2.4I Dynamic v0.3 Real Evaluation contract freeze。迁`real-evaluate/real-report/validate-real`；生成路径严格保持cached DQ gate→standard ETF price validation→PIT/no-lookahead real evaluation，任一失败fail closed。Requested start/end与AI regime分开披露，未指定start使用reviewed policy default，不得暗改为更早历史结论窗；pre-regime仅可作warm-up/stress/comparison。Promotion gate仅输出治理decision，不执行promotion、approval、enrollment、official target、production或broker；latest report只读，validation改查canonical owner。
- 2026-07-11：G2.4H Dynamic v0.3 Rescue base slice `COMPLETE`，G2.4继续。最终base/contract组合=`40 passed`、architecture-fitness=`191 passed`，generated=`819 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback及157行；v0.4只读、base-policy match、latest只读、dual-owner validation与no-approval/enrollment/promotion/production边界通过。
- 2026-07-11：G2.4H Dynamic v0.3 Rescue base contract freeze。`dynamic_v3_rescue_app`拥有大量跨阶段命令，本slice只迁TRADING-090 `run/report/validate`，不得整体搬成新god module；real evaluation、failure attribution及后续命令保持原owner待独立slice。输入只读latest/explicit v0.4 review package，base candidate必须匹配reviewed policy；输出仅constraint-aware candidate evaluation/validation治理artifact，latest report只读。禁止approval、enrollment、auto promotion、official target、production和broker；validation同时检查canonical base owner与legacy remaining-command owner。
- 2026-07-11：G2.4G Dynamic v0.2 Review slice `COMPLETE`，G2.4继续。最终review/contract组合=`39 passed`、architecture-fitness=`190 passed`，generated=`818 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback及167行；mandatory/optional source语义、latest只读、canonical CLI visibility和no-backtest/enrollment/approval/promotion/production边界通过。
- 2026-07-11：G2.4G Dynamic v0.2 Review contract freeze。迁`package/report/validate`三callback；package只读existing rescue、candidate robustness和optional shadow artifacts，不重跑market backtest。Rescue/robustness缺失fail closed，optional shadow缺失仅显式warning；保留source DQ/range/lineage、AI-regime policy、eligibility blockers和latest只读。只允许review package/validation治理artifact，禁止approval、shadow enrollment、auto promotion、official target、production和broker；CLI visibility必须切到canonical registration/owner。
- 2026-07-11：G2.4F Dynamic Rescue slice `COMPLETE`，G2.4继续。最终rescue/contract组合=`37 passed`、architecture-fitness=`189 passed`，generated=`817 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback及266行；DQ/price/rescue顺序、fail-closed、canonical CLI visibility和no-enrollment/approval/promotion/production边界通过。
- 2026-07-11：G2.4F Dynamic Rescue实现完成并进入`VALIDATING`。三callback迁canonical，旧root callback为0；validation从旧root文本探针改查canonical registration/owner。Root=`32,979 -> 32,713 lines / 988 -> 985 functions / 948 -> 945 decorators`，direct writer=858、CLI tree不变。Cached DQ→standard-price validation→bounded rescue顺序、fail-closed、source lineage与no-enrollment/approval/promotion/official-target/broker边界通过focused=25。
- 2026-07-11：G2.4F Dynamic Rescue contract freeze。迁`run/report/validate`三callback；保持cached DQ→standard-price validation→bounded rescue comparison，失败fail closed。Failure dataset/rescue candidates仅为治理artifact；禁止auto enrollment、owner approval execution、promotion、official target和broker；保留source robustness/shadow package lineage及CLI parity。
- 2026-07-11：G2.4E Dynamic Robustness slice `COMPLETE`，G2.4继续。最终robustness/contract=`24 passed`、architecture-fitness=`188 passed`，generated=`816 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减2 callback及220行；DQ/price/robustness顺序、fail-closed和no-enrollment/production边界通过。
- 2026-07-11：G2.4E Dynamic Robustness实现完成并进入`VALIDATING`。两callback迁canonical，旧root callback为0；Root=`33,199 -> 32,979 lines / 990 -> 988 functions / 950 -> 948 decorators`，direct writer=858、tree不变。Cached DQ→standard-price validation→robustness顺序、fail-closed、latest只读、range/DQ lineage和no-enrollment/production边界通过focused=24。
- 2026-07-11：G2.4E Dynamic Robustness contract freeze。迁`report/validate`两callback；生成路径必须先完整cached DQ gate、再ETF standard-price validation、最后robustness计算，任一失败fail closed；latest路径保持只读。保留requested start/end、DQ report lineage、candidate-only/no-shadow-enrollment/no-production/no-broker和CLI tree parity。
- 2026-07-11：G2.4D Dynamic Calibration slice `COMPLETE`，G2.4继续。最终calibration/contract=`24 passed`、architecture-fitness=`187 passed`、Ruff/contract PASS；generated=`815 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback及206行；canonical CLI visibility、research cache与no-auto-promotion/enrollment/production边界均通过。
- 2026-07-11：G2.4D Dynamic Calibration实现完成并进入`VALIDATING`。三callback迁canonical，旧root callback为0；validation已从旧root文本探针改查canonical registration/owner。Root=`33,405 -> 33,199 lines / 993 -> 990 functions / 953 -> 950 decorators`，direct writer=858、CLI tree不变。Research cache/candidate artifacts允许，auto promotion/enrollment、official target、production/broker禁止；focused=24。
- 2026-07-11：G2.4D Dynamic Calibration进入实现前contract freeze。迁`run/report/validate`三callback及专属imports，复用canonical mapping/optional-JSON helper。允许read-write研究cache和candidate pack/report artifacts；cache仅含trend score、allocation path和dynamic backtest proxy，不等于production state。禁止automatic promotion、无owner approval enrollment、official target write和broker；validation的CLI visibility同步改查canonical registration+command owner，禁止用旧root伪引用维持PASS。退出要求cache mode/hit/write、ranking、trend/DQ lineage、validation、path/schema/bytes/exit/tree parity。
- 2026-07-11：G2.4C Dynamic Allocation slice `COMPLETE`，G2.4继续。最终dynamic-allocation/contract=`21 passed`、architecture-fitness=`186 passed`、Ruff/standalone contract PASS；generated=`814 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减3 callback、2 helper及251行，CLI tree不变；candidate registry/weights与runtime/official/production边界保持。
- 2026-07-11：G2.4C Dynamic Allocation实现完成并进入`VALIDATING`。三callback、独占JSON parser和shared mapping helper迁canonical，旧root callback/helper为0；仅保留dynamic-calibration所需policy imports。Root=`33,656 -> 33,405 lines / 998 -> 993 functions / 956 -> 953 decorators`，direct writer仍858、CLI tree不变。Candidate decision/report/policy-registry artifact允许，但runtime registry、official target、production rebalance和broker禁止。
- 2026-07-11：G2.4C Dynamic Allocation进入实现前contract freeze。迁`decide/report/validate`三callback与专属domain imports，迁独占JSON float option parser，`mapping_obj`进入common供legacy callers alias复用；保留dynamic-calibration仍需的policy config/loader imports。Decision/report/policy-registry允许写candidate governance artifacts，但registry不是runtime registry，candidate weights不是official target；禁止production rebalance、official target write和broker。退出要求AI-regime date、trend/DQ lineage、constraints、hold/rebalance-candidate、path/schema/bytes、validation exit和CLI tree parity。
- 2026-07-11：G2.4B Shadow Review slice `COMPLETE`，G2.4继续。最终shadow/contract=`21 passed`、architecture-fitness=`185 passed`、Ruff/standalone contract PASS；generated=`813 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减4 callback及294行，CLI 993 leaf与tree hash不变；治理artifact与approved-only enrollment语义保留，journal/runtime registry/paper-shadow execution/production/broker均未扩大。
- 2026-07-11：G2.4B Shadow Review实现完成并进入`VALIDATING`。四个callback与专属domain imports迁至canonical `shadow_review.py`，复用common artifact/JSON helper，旧root对应callback/import为0。Root=`33,950 -> 33,656 lines / 1,002 -> 998 functions / 960 -> 956 decorators`，direct writer仍858，CLI tree/hash不变；package/approval/enrollment治理artifact写入保留，但不写decision journal、不自动执行paper shadow、不改runtime registry/production/broker。
- 2026-07-11：G2.4B Shadow Review进入实现前contract freeze。范围固定为`package/approve/enroll-approved/validate`四个callback及专属`shadow_ready_review` imports，迁至canonical `shadow_review.py`并复用common `artifact_stem/load_optional_json_payload`。语义边界：package、owner approval和approved enrollment均可写candidate governance artifact；approval只保存decision-journal link字符串，不写journal；enrollment只建立forward-tracking记录，不自动调度/执行paper shadow，不写runtime registry、production weights或broker。退出要求diagnostics FAIL exit、unsafe decision拒绝、approved-only enrollment、validation fail-closed、artifact path/bytes、安全字段和CLI tree parity。
- 2026-07-11：G2.4A Baseline Review slice `COMPLETE`，G2.4继续。最终baseline/journal/contract=`36 passed`、architecture-fitness=`184 passed`、Ruff/standalone contract PASS；generated=`812 modules / 1,114 tests / 858 direct writers / 0 violations`。Legacy root净减7 callback、1 shared helper及490行，CLI 993 leaf与tree hash不变；治理journal写入、draft-only及no-production/no-broker边界均保持。
- 2026-07-11：G2.4A Baseline Review实现完成并进入`VALIDATING`。七个callback与专属domain imports迁至canonical `baseline_review.py`，`artifact_stem`迁common并由legacy shadow-review callers直接alias复用，旧root对应callback/helper/import均为0；eligibility/matrix改用atomic writer，direct writer=`860 -> 858`。Root=`34,440 -> 33,950 lines / 1,010 -> 1,002 functions / 967 -> 960 decorators`，CLI仍为41 root/291 group/993 leaf/0 duplicate且tree hash不变。显式journal linkage仍允许写governance decision journal；production runtime/config/weights/broker禁止，proposal保持draft-only。
- 2026-07-11：G2.4A Baseline Review进入实现前contract freeze。七个callback连同专属domain imports迁canonical `baseline_review.py`；`artifact_stem`迁common供legacy callers alias复用；eligibility/matrix直接写入改用canonical atomic writer且保持bytes。语义边界修正为：`capture-decision --link-journal`允许显式写governance decision journal，但不得写production runtime/config/weights/broker；proposal draft不等于adoption。退出要求eligibility blocked exit、evidence matrix、package、decision+journal optional linkage、proposal preconditions/draft-only、outcome、validation及CLI tree parity。
- 2026-07-11：G2.3 closeout正式通过：selected-group test=`15 passed`、contract/policy=`27 passed`、architecture-fitness=`183 passed`、standalone contract/Ruff PASS；G2.4保持`IN_PROGRESS`，Baseline Review实现尚未开始。G2.3的DQ/regime/strategy/threshold/production/broker不变量全部保留。
- 2026-07-11：G2.3正式`COMPLETE`，G2.4 research/shadow/portfolio进入`IN_PROGRESS`。退出审计固定8 slices / 9 canonical modules / 26 callbacks / 13 helpers；旧root selected callback/helper/domain imports均为0。G2.3累计root=`36,045 -> 34,440 lines / 1,049 -> 1,010 functions / 993 -> 967 decorators`，direct writer=`861 -> 860`；closeout focused=`15 passed`，CLI仍为41 root/291 group/993 leaf/0 duplicate。G2.4首个slice选择7个Baseline Review callback，本closeout未开始实现。
- 2026-07-11：G2.3H trend-calibration slice `COMPLETE`，G2.3继续。最终trend/DQ/direct/contract=`54 passed`、contract/policy=`26 passed`、architecture-fitness=`182 passed`、Ruff/standalone contract PASS；generated=`811 modules / 1,114 tests / 860 direct writers / 0 violations`。Legacy root净减3 callback、4 DQ helper及254行，DQ-before-feature fail closed，strategy/regime/threshold不变，runtime 993 leaf不变。
- 2026-07-11：G2.3H trend-calibration slice实现完成并进入`VALIDATING`。三个callback迁canonical trend module，四个cached-DQ helper迁canonical data-quality并供legacy callers alias复用；旧root定义/import=0。Root=`34,694 -> 34,440 lines / 1,017 -> 1,010 functions / 970 -> 967 decorators`；tree不变，trend/DQ/direct/contract=`54 passed`，DQ-before-feature fail-closed fixture PASS，等待compatibility/generated/architecture门禁。
- 2026-07-11：G2.3H选择`trend-calibration run/report/validate`三个DQ/feature/research-sensitive callback，并把download-manifest、Marketstack path/requirement与完整cached-DQ gate四个helper迁入canonical data-quality module。旧root callers直接alias复用，trend module禁止反向依赖legacy root。退出要求DQ先于feature/search且失败exit 1、standard-price failure保持BadParameter、policy market regime与evaluation-only/candidate-only、dataset/report/registry path/schema、report read-only、validation fail-closed及CLI tree parity。
- 2026-07-11：G2.3G satellite-attribution slice `COMPLETE`，G2.3继续。最终satellite/AI/shared-helper/contract=`78 passed`、contract/policy=`25 passed`、architecture-fitness=`181 passed`、Ruff/standalone contract PASS；generated=`810 modules / 1,114 tests / 860 direct writers / 0 violations`。Legacy root净减3 callback、2 shared helper及243行，invalid-price fail closed，AI regime default保持2022-12-01，runtime 993 leaf不变。
- 2026-07-11：G2.3G satellite-attribution slice实现完成并进入`VALIDATING`。三个callback与prepare helper进入canonical module，optional-JSON/quality-metadata进入common并由legacy 60/15 caller复用；旧root定义/import=0。Root=`34,937 -> 34,694 lines / 1,022 -> 1,017 functions / 973 -> 970 decorators`；tree不变，satellite/AI/shared-helper/contract=`78 passed`，invalid-price fail closed，等待compatibility/generated/architecture门禁。
- 2026-07-11：G2.3G选择`satellite-attribution build/report/validate`三个DQ/regime-sensitive callback，并把`quality_metadata`与`load_optional_json_payload`两个高复用helper迁入canonical common。旧root 15/60个caller通过直接alias复用，禁止wrapper/复制；satellite module复用canonical parse/resolve-date。退出要求价格质量FAIL仍exit 1、default start仍为AI regime `2022-12-01`、requested range/market regime/DQ report lineage、build/report dataset parity、validation fail-closed、artifact path/bytes与CLI tree不变。
- 2026-07-11：G2.3F parameter-review slice `COMPLETE`，G2.3继续。最终parameter/weekly/contract=`65 passed`、contract/policy=`24 passed`、architecture-fitness=`180 passed`、Ruff/standalone contract PASS；generated=`809 modules / 1,114 tests / 860 direct writers / 0 violations`。Legacy root净减4 callback、1 helper及188行，runtime 993 leaf不变。
- 2026-07-11：G2.3F parameter-review slice实现完成并进入`VALIDATING`。四个callback与共享report helper迁canonical `parameter_review.py`，直接复用weekly-review date helper，旧root无callback/helper/domain imports。Root=`35,125 -> 34,937 lines / 1,027 -> 1,022 functions / 977 -> 973 decorators`；tree不变，parameter/weekly/contract=`65 passed`，等待compatibility/generated/architecture门禁。
- 2026-07-11：G2.3F选择`parameter-review aggregate/report/run/validate`四个只读reporting callback及共享report helper。Canonical `parameter_review.py`复用G2.3E `weekly_review_date`，禁止日期helper复制；旧root不得保留callback/helper/domain imports。退出要求report/run同源、validation fail-closed、artifact path/bytes、observe-only/candidate-only/no-production与CLI tree parity。Satellite Attribution因跨价格DQ与AI-regime日期解释留待独立高风险切片。
- 2026-07-11：G2.3E weekly-review slice `COMPLETE`，G2.3继续。最终weekly/downstream/contract=`84 passed`、contract/policy=`23 passed`、architecture-fitness=`179 passed`、Ruff/standalone contract PASS；generated=`808 modules / 1,114 tests / 860 direct writers / 0 violations`。Legacy root净减4 callback、2 helper及239行，runtime 993 leaf不变。
- 2026-07-11：G2.3E weekly-review slice实现完成并进入`VALIDATING`。四个callback、generate helper与shared date helper迁至canonical `weekly_review.py`，legacy callers直接复用date helper，旧root无callback/helper/domain import wrapper。Root=`35,364 -> 35,125 lines / 1,033 -> 1,027 functions / 981 -> 977 decorators`；tree不变，weekly/downstream/contract=`84 passed`，等待compatibility/generated/architecture门禁。
- 2026-07-11：G2.3E选择`weekly-review aggregate/generate/run/validate`四个只读reporting callback及专属generate helper。Canonical `weekly_review.py`承载命令和计算编排，shared weekly-review date parser移入该模块并由尚未迁移的legacy callers直接复用；禁止wrapper/复制。退出要求旧root四callback、generate helper和weekly domain imports=0，aggregation FAIL、validation exit、generate/run alias、artifact bytes/path、observe-only/no-production与CLI tree parity。
- 2026-07-11：G2.3D evidence-dashboard slice `COMPLETE`，G2.3继续。最终domain/direct/contract=`44 passed`、contract/policy=`22 passed`、architecture-fitness=`178 passed`、Ruff/standalone contract PASS；generated=`807 modules / 1,114 tests / 860 direct writers / 0 violations`。Legacy root净减3 callback和190行，aggregation direct write同时迁canonical atomic writer且bytes parity保持，runtime 993 leaf不变。
- 2026-07-11：G2.3D evidence-dashboard slice实现完成并进入`VALIDATING`。三个callback及专属strategy-evidence imports迁至canonical `reporting.py`，date parser/report registry复用既有contract，`cli_direct`直接转canonical，旧root无wrapper。Root=`35,554 -> 35,364 lines / 1,036 -> 1,033 functions / 984 -> 981 decorators`；tree不变，focused=`44 passed`，等待compatibility/generated/architecture门禁。
- 2026-07-11：G2.3D选择`evidence-dashboard aggregate/report/validate`三个reporting callback及其专属strategy-evidence imports。Canonical `reporting.py`直接复用common date parser与report registry contract，`cli_direct`同步转canonical，禁止legacy wrapper。退出要求旧root三callback及专属imports=0，aggregation/dashboard/validation schema、PASS/exit、artifact path、observe-only/no-production与完整CLI tree parity。
- 2026-07-11：G2.3C operations slice `COMPLETE`，G2.3继续。最终operations/direct/contract=`111 passed`、contract/policy=`21 passed`、architecture-fitness=`177 passed`、Ruff/standalone contract PASS；generated=`806 modules / 1,114 tests / 861 direct writers / 0 violations`。Legacy root净减3 callback、1 parser、3目录常量及209行，runtime 993 leaf不变。
- 2026-07-11：G2.3C operations slice实现完成并进入`VALIDATING`。三个callback、一个cadence parser和三项目录常量已迁至canonical `operations.py`，`cli_direct`直接调用canonical实现，legacy root无callback/parser/constant wrapper。Root=`35,763 -> 35,554 lines / 1,040 -> 1,036 functions / 987 -> 984 decorators`；完整tree hash不变，operations/direct/contract=`111 passed`，等待compatibility、generated inventory与architecture门禁。
- 2026-07-11：G2.3C选择`ops dry-run/report/validate`三个callback、cadence parser与三项输出目录常量。Canonical operations module成为常量/CLI唯一实现；dynamic-shadow legacy caller直接导入validation目录常量，`cli_direct`全部转canonical。退出要求旧root callback/parser/constant定义=0，调度图、observe-only/no-production、validation exit、artifact path和tree parity。
- 2026-07-11：G2.3B data-quality slice `COMPLETE`，G2.3继续。最终domain/direct=`44 passed`、contract/policy=`20 passed`、architecture-fitness=`176 passed`、Ruff/standalone contract PASS；generated=`805 modules / 1,114 tests / 861 direct writers / 0 violations`。Legacy root再净减3 callback与176行，runtime 993 leaf不变。
- 2026-07-11：G2.3B data-quality slice实现完成并进入`VALIDATING`。三个callback迁到canonical `data_quality.py`，未新增helper；`cli_direct` report/validate已直接调用canonical callback，旧root无wrapper。Root=`35,939 -> 35,763 lines / 1,043 -> 1,040 functions / 990 -> 987 decorators`；完整tree/node hash不变，DQ/direct-dispatch/contract=`44 passed`、Ruff/standalone contract PASS，等待compatibility与architecture门禁。
- 2026-07-11：G2.3B选择`data-quality price-freshness/report/validate`三个callback。三者只依赖canonical `parse_date/resolve_date`，无需新增helper；`cli_direct`中的report/validate调用必须同步转向canonical callback，禁止旧root wrapper。退出要求旧root三callback/decorator=0、DQ BLOCKED/FAIL仍exit 1、tree/report path/schema/help及相关tests parity。
- 2026-07-11：G2.3A data/features slice `COMPLETE`，G2.3继续。最终domain focused=`72 passed`、contract/policy=`19 passed`、architecture-fitness=`175 passed`、Ruff/standalone contract PASS；generated=`804 modules / 1,114 tests / 861 direct writers / 0 violations`。Legacy root已净减3 callback、3 helper及106行，完整runtime leaf仍为993。下一slice继续从operations/reporting边界选择可独立迁移group。
- 2026-07-11：G2.3首个data/features callback slice实现完成并进入`VALIDATING`。三个callback迁到canonical `data.py`，三个共享helper迁到`common.py`并由legacy root别名复用；旧root对应定义均为0，compatibility alias已直接改用canonical callback而非保留wrapper。Root=`36,045 -> 35,939 lines / 1,049 -> 1,043 functions / 993 -> 990 decorators`；tree/node hash不变，data/feature/alias相关=`72 passed`、Ruff/standalone contract PASS，DQ与exit语义未改。
- 2026-07-11：G2.3首个callback slice选择`data`与`features`。范围固定为`data ingest/validate`、`features build`三个callback，以及它们共用的date/latest/satellite解析helper；helper将进入canonical `common.py`并由legacy root别名复用，禁止复制。迁移目标是legacy root中这三个callback/decorator和三个helper定义reachability=0，同时保持DQ调用、path/default/help/exit、tree hash与ETF data/domain tests。
- 2026-07-11：G2.2 `COMPLETE`，G2.3 data/operations/reporting group迁移进入`IN_PROGRESS`。最终ETF consumer=`341 passed`、contract/policy focused=`18 passed`、architecture-fitness=`174 passed`、Ruff与standalone contract PASS；generated=`802 modules / 1,114 tests / 861 direct writers / 0 violations`。Registration shell未移动callback或改变tree；下一slice从低投资解释风险group开始，每个callback slice均须旧root reachability=0且tree/help/domain parity。
- 2026-07-11：G2.2 registration shell实现完成并进入`VALIDATING`。291个Typer app定义与290条`add_typer`关系完整迁到`interfaces.cli.etf_portfolio.registration`；legacy root中两类语句均为0，所有1,049个callback与993个decorator原地保留。Root=`37,604 -> 36,045 lines / 1,579,334 -> 1,520,029 bytes`；tree/counts/1,284 node contracts逐项相同，tree hash保持`afa0760c...1cec2`。`data --help`与`portfolio --help`固定宽度输出hash已冻结；25个现有ETF CLI consumer文件=`341 passed`、contract characterization=`6 passed`、Ruff/standalone baseline PASS，等待compatibility与architecture正式门禁。
- 2026-07-11：G2.1 `COMPLETE`，G2.2 registration shell进入`IN_PROGRESS`。最终focused=`15 passed`（contract characterization自身3项）、architecture-fitness=`171 passed`、Ruff与standalone baseline validate PASS；generated inventory=`798 modules / 1,114 tests / 861 direct writers / 0 violations`。Golden contract冻结`41 root / 291 group / 993 leaf / 1,284 nodes / 0 duplicate`，ETF CLI runtime与source bytes未改。G2.2先分离app composition/registration，不迁策略计算。
- 2026-07-11：G2.1实现完成并进入`VALIDATING`。Canonical scanner按解析后的Click tree冻结1284个node（291 group + 993 leaf），同时核对Typer raw registration count=993，duplicate=0；每个node hash覆盖path、group行为、help与parameter contract，但有意排除callback module/location以允许纯结构迁移。Tracked tree hash=`afa0760c...1cec2`；duplicate、option/default/help drift characterization与baseline parity=`3 passed`，Ruff PASS。等待generated manifests、compatibility和architecture正式门禁。
- 2026-07-11：G2.1进入`IN_PROGRESS`。只读runtime审计得到`41 root groups / 291 groups / 993 leaf commands / 0 duplicate / 993 unique paths`；下一步实现deterministic contract scanner与tracked golden baseline，尚未改动ETF CLI runtime。
