# 产业链节点热度与健康度需求

状态：BASELINE_DONE

最后更新：2026-05-04

关联任务：`CHAIN-001`

## 背景

当前系统已经有 `config/industry_chain.yaml` 产业链因果图和 `config/watchlist.yaml` 标的到节点的映射，但日报评分仍主要输出总体模块分数和 AI 仓位区间。这个结构无法清楚说明行情是在交易完整 AI 产业链扩散，还是集中在 GPU/HBM 或少数龙头。

目标是在产业链层增加节点状态输出：

- `node_heat`：市场是否正在交易该节点。
- `node_health`：基本面和可审计输入是否支持该节点。
- `node_coverage`：该节点可用数据覆盖率。
- `node_concentration`：节点信号是否集中在少数 ticker。

第一版定位为解释和诊断层，不直接触发买卖或仓位动作。待规则稳定后，再评估是否作为 `position_gate` 或风险预算约束输入。

## 设计原则

- 热度和健康度必须分开，避免把价格上涨误读成基本面确认。
- 节点健康度只能使用已通过门禁的基本面、估值、风险事件和 thesis 输入。
- 数据不足必须显式显示为覆盖缺口，不能静默填成中性。
- 节点输出应服务投资解释：说明本期总分来自哪些产业链环节、是否扩散、是否拥挤、是否由单一龙头驱动。
- 节点解释应接入最终报告决策链，优先回答“这个价格或基本面信号如何传导到 thesis、评分、gate 和行动”，而不是只展示节点表格。
- 默认结论窗口仍应优先服务 `ai_after_chatgpt` regime，历史回测需要保持 point-in-time。

## 因果链解释模板

日报中的产业链解释应尽量使用固定链路：

```text
市场价格信号
  -> 产业链节点
  -> thesis 状态
  -> 评分模块
  -> 仓位 gate
  -> 行动影响
```

示例：

```text
SMH 相对 QQQ 仍强
  -> 半导体链条仍是市场交易重点
  -> 支持 GPU/ASIC 与 HBM 节点热度
  -> 支持“算力需求仍强”的核心 thesis
  -> 趋势模块加分
  -> 但估值拥挤 gate 限制新增仓位
  -> 最终结论为中高配但受限
```

第一版只作为解释和诊断层，不直接改变仓位。若后续要让节点集中度或节点健康度进入仓位约束，必须先与 `SCORE-001`、`SCORE-003` 和 `GOV-001` 协同验证。

## 分步开发

### 阶段 1：节点热度基础版

输入：

- `config/industry_chain.yaml` 的 `related_tickers`。
- `config/watchlist.yaml` 的 `ai_chain_nodes`。
- 市场特征和价格缓存。

输出：

- 每个节点的趋势广度、相对强弱、热度等级和主要贡献 ticker。
- 日报中新增产业链节点热度表。

验收标准：

- 节点热度可区分 GPU/ASIC、HBM、先进封装、云 CapEx 等节点。
- 相关 ticker 缺少价格数据时显示覆盖不足。
- 测试覆盖同一总分下节点热度集中和扩散两种情况。

当前实现状态：

- 2026-05-04：进入基础实现；第一阶段先基于 `config/industry_chain.yaml`、`config/watchlist.yaml` 和已通过门禁的市场特征生成节点热度、覆盖率、集中度和主要贡献 ticker。
- 第一阶段只作为解释和诊断层进入 `score-daily` 日报，不改变 scoring、`position_gate`、execution policy、回测仓位或交易建议。
- 完整 `DONE` 仍需要仓位约束评估；节点健康度和回测历史节点状态追踪已在后续阶段形成基础闭环，当前完成态仍为 `BASELINE_DONE`。
- 2026-05-04 基础版已完成：新增 `industry_node_state` 模块，`score-daily` 日报输出“产业链节点热度”章节，按节点展示热度等级、覆盖率、集中度、主要贡献 ticker 和只读解释边界。
- 第一阶段完成态为 `BASELINE_DONE`：节点热度、覆盖率和集中度基础闭环已具备；后续阶段已补节点健康度和回测每日节点状态追踪，完整 `DONE` 仍需要仓位约束评估。
- 2026-05-04：第二阶段进入 `IN_PROGRESS`，开始接入 SEC/TSM 基本面特征、估值快照、风险事件发生记录和 thesis 复核，目标是只读节点健康度基础版；本阶段不改变 scoring、`position_gate`、execution policy、回测仓位或交易建议。
- 2026-05-04：第二阶段基础版完成：日报输出“产业链节点热度与健康度”，按节点展示市场覆盖、健康覆盖、支持项、风险/限制和数据缺口；健康度仍是解释层，估值拥挤和风险事件只作为限制说明，不写成基本面证伪。

### 阶段 2：节点健康度基础版

输入：

- SEC/TSM IR 基本面特征。
- 估值与拥挤度快照。
- 风险事件发生记录。
- thesis 复核摘要。

输出：

- 每个节点的健康度等级、数据覆盖率、主要支持项和主要风险项。

验收标准：

- 健康度只读取已通过校验且 point-in-time 可见的输入。
- 对未覆盖的节点，例如当前缺少完整基本面数据的设备、HBM、软件应用节点，报告明确显示覆盖不足。
- 估值拥挤和风险事件不能被写成基本面证伪，只能作为健康度风险或限制说明。

### 阶段 3：日报和回测审计集成

输出：

- `score-daily` 顶部或独立章节输出节点热度/健康度矩阵。
- 回测每日明细或审计报告可追踪历史节点状态。
- 报告能生成中文解释，例如“当前市场主要交易 GPU/HBM，完整产业链扩散不足”。

验收标准：

- 日报能说明本期加分集中在哪些节点，以及基本面确认是否同步。
- 回测保持无未来函数，节点状态按 signal_date 重建。
- `docs/system_flow.md` 同步更新。

### 阶段 4：仓位约束评估

前置条件：

- 节点热度和健康度规则经过真实数据和回测观察。
- 集中度、估值拥挤和风险事件 gate 已稳定。

候选规则：

- 热度高、健康度低、集中度高：限制新增仓位。
- 热度中、健康度高、覆盖充分：进入观察或增强解释，不自动加仓。
- 多节点热度和健康度同时改善：提升“健康扩散”解释置信度。

验收标准：

- 若进入仓位层，必须和 `SCORE-001`、`SCORE-003` 协同，作为可审计 gate 或风险预算输入。
- 不引入不可解释黑箱优化器。

## 开放问题

- 节点热度的等级阈值应使用固定规则，还是按历史分位自适应。
- `related_tickers` 中尚未纳入核心数据下载或 SEC 基本面覆盖的标的，应作为完整覆盖要求还是只作为扩展观察池。
- 节点健康度是否需要单独的配置文件，例如 `config/industry_node_rules.yaml`。
- 是否需要在 `backtest_daily_*.csv` 之外再写独立节点状态 CSV，例如 `data/processed/industry_node_state_daily.csv`。

## 状态记录

- 2026-05-04：创建需求文档，登记 `CHAIN-001`。当前仅完成需求评估和任务拆解，尚未实现。
- 2026-05-04：根据最终报告呈现形式讨论，补充产业链因果链解释模板，明确节点状态应服务“市场信号 -> 节点 -> thesis -> 评分 -> gate -> 行动”的报告主线；本次仅更新需求，不改变现有报告输出。
- 2026-05-04：`CHAIN-001` 达到 `BASELINE_DONE`：节点热度基础版、日报章节、系统流图和测试完成；健康度、回测追踪和仓位约束评估仍待后续阶段。
- 2026-05-04：`CHAIN-001` 从 `BASELINE_DONE` 回到 `IN_PROGRESS`：推进阶段 2 节点健康度基础版；验收边界是日报显示健康度、覆盖率、支持项、风险/限制和数据缺口，同时明确估值拥挤/风险事件不是基本面证伪。
- 2026-05-04：`CHAIN-001` 回到 `BASELINE_DONE`：阶段 2 节点健康度基础版已接入 `score-daily` 日报；完整 `DONE` 仍需要阶段 3 的回测每日节点状态追踪、历史审计输出，以及阶段 4 的仓位约束评估。
- 2026-05-04：`CHAIN-001` 从 `BASELINE_DONE` 回到 `IN_PROGRESS`：推进阶段 3 回测历史节点状态追踪；验收边界是回测每日明细或报告可按 `signal_date` 追踪节点热度/健康度，不改变评分、`position_gate`、执行建议或回测仓位。
- 2026-05-04：`CHAIN-001` 回到 `BASELINE_DONE`：阶段 3 已把节点热度/健康度按 `signal_date` 写入 `backtest_daily_*.csv`，并在回测报告输出“产业链节点历史状态摘要”；该追踪仍为只读审计层，未改变评分、`position_gate`、执行建议或回测仓位。完整 `DONE` 仍需要阶段 4 仓位约束评估与规则治理。
