# High-Intensity Cluster Actual Path Outcomes

Cluster-level matrix 以 `primary_event_id / cluster_start_date` 为起点，连续 trigger days 不重复计入主样本。这是 TRADING-2339 forward outcome review 的主要分析单位。

- cluster_actual_path_outcome_count: `240`
- primary_analysis_level: `cluster`
