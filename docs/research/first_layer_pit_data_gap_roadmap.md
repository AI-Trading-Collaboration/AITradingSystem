# First-Layer PIT Data Gap Roadmap

状态：`FIRST_LAYER_PIT_DATA_GAP_ROADMAP_READY`

高优先级 gap 是 `breadth_participation` 与 `event_risk`。这些 family 不能在 PIT-approved source 和 coverage audit 通过前进入模型。

- `breadth_participation`: `PIT_BLOCKED`，blocked_until=`PIT-approved local source and coverage audit pass`
- `event_risk`: `PIT_BLOCKED`，blocked_until=`event timestamp availability and PIT audit pass`
- `sector_participation`: `PIT_BLOCKED`，blocked_until=`PIT source qualification and primary-window coverage pass`
- `macro_shock`: `PIT_BLOCKED`，blocked_until=`PIT release timestamp and schema validation pass`
- `earnings_revision`: `PIT_BLOCKED`，blocked_until=`PIT history and issuer coverage audit pass`
