schema_version: arch_005_task_shadow_fragment.v1
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
task_record:
  schema_version: task_record.v1
  task_id: OPS-067_READER_BRIEF_QUALITY_FAIL_CLOSED_FINALIZATION
  title: null
  domain: Operations / canonical Reader Brief and report quality fail-closed finalization
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: architecture coordinator 修复并验证 Windows atomic replace contention，完成
    Full/post-Full 与 push；随后 operations owner 执行 canonical daily acceptance
  requirement_refs:
  - docs/requirements/OPS-067_Reader_Brief_Quality_Fail_Closed_Finalization.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详见 `docs/requirements/OPS-067_Reader_Brief_Quality_Fail_Closed_Finalization.md`。实现与
    `ada1f7bc` exact-tree pre-Full gates 已通过；`00b41d5c` 首次 Full 的唯一失败暴露 canonical
    writer 在 Windows 短暂 reader handle 下 `os.replace` 的 `WinError 5` contention。先完成
    bounded transient retry 与定向重复验证，再运行第二次 Full；通过、push 且无并发 writer 后，才只用 `aits ops
    daily-run` 做 provider-ready 运营验收。DATA-GOV D0C power-loss durability 仍独立跟踪。
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: Report quality `FAIL` 与 Reader Brief quality `FAILED` CLI/step/finalization
      全部 fail closed；`LIMITED_READER_CONTEXT` 保持可接受但显式披露；最终 Reader Brief bytes 由同次
      finalization quality evidence 覆盖；manifest 经`FINALIZING`两阶段 closure 后才可 PASS；canonical
      writer 只对 Windows transient replace contention bounded retry，非 transient/耗尽仍失败并清
      temp；focused/architecture/contract/integration/reproducibility/Full PASS；旧 2026-07-22
      state/ledger byte-identical；`production_effect=none`。
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  legacy_source:
    path: docs/task_register.md
    source_partition: active
    line_number: 1988
    row_sha256: adc1687edb7794bc3a4552cc5ce089a052fcbfbdb5ef44eedacc5bb5e0a46696
    cell_count: 8
    all_cells:
    - OPS-067_READER_BRIEF_QUALITY_FAIL_CLOSED_FINALIZATION
    - Operations / canonical Reader Brief and report quality fail-closed finalization
    - P0
    - IN_PROGRESS
    - architecture coordinator 修复并验证 Windows atomic replace contention，完成 Full/post-Full
      与 push；随后 operations owner 执行 canonical daily acceptance
    - 详见 `docs/requirements/OPS-067_Reader_Brief_Quality_Fail_Closed_Finalization.md`。实现与
      `ada1f7bc` exact-tree pre-Full gates 已通过；`00b41d5c` 首次 Full 的唯一失败暴露 canonical
      writer 在 Windows 短暂 reader handle 下 `os.replace` 的 `WinError 5` contention。先完成
      bounded transient retry 与定向重复验证，再运行第二次 Full；通过、push 且无并发 writer 后，才只用 `aits
      ops daily-run` 做 provider-ready 运营验收。DATA-GOV D0C power-loss durability 仍独立跟踪。
    - Report quality `FAIL` 与 Reader Brief quality `FAILED` CLI/step/finalization
      全部 fail closed；`LIMITED_READER_CONTEXT` 保持可接受但显式披露；最终 Reader Brief bytes 由同次
      finalization quality evidence 覆盖；manifest 经`FINALIZING`两阶段 closure 后才可 PASS；canonical
      writer 只对 Windows transient replace contention bounded retry，非 transient/耗尽仍失败并清
      temp；focused/architecture/contract/integration/reproducibility/Full PASS；旧 2026-07-22
      state/ledger byte-identical；`production_effect=none`。
    - 2026-07-24：pre-Full architecture 569、contract 268、integration 993、reproducibility
      23 PASS。首次 Full=`7053 passed / 1 failed / 4 skipped / 1091.87s`，唯一失败为同 key 跨进程
      cache publication 的 `ATOMIC_ARTIFACT_WRITE_FAILED`；focused 单跑随后 PASS 但不作为绕过。已按
      no-silent-workaround 登记实现层修复，不写 production/active-shadow weights，不触发 broker/order/trading。
    raw_line: '|OPS-067_READER_BRIEF_QUALITY_FAIL_CLOSED_FINALIZATION|Operations /
      canonical Reader Brief and report quality fail-closed finalization|P0|IN_PROGRESS|architecture
      coordinator 修复并验证 Windows atomic replace contention，完成 Full/post-Full 与 push；随后
      operations owner 执行 canonical daily acceptance|详见 `docs/requirements/OPS-067_Reader_Brief_Quality_Fail_Closed_Finalization.md`。实现与
      `ada1f7bc` exact-tree pre-Full gates 已通过；`00b41d5c` 首次 Full 的唯一失败暴露 canonical
      writer 在 Windows 短暂 reader handle 下 `os.replace` 的 `WinError 5` contention。先完成
      bounded transient retry 与定向重复验证，再运行第二次 Full；通过、push 且无并发 writer 后，才只用 `aits
      ops daily-run` 做 provider-ready 运营验收。DATA-GOV D0C power-loss durability 仍独立跟踪。|Report
      quality `FAIL` 与 Reader Brief quality `FAILED` CLI/step/finalization 全部 fail
      closed；`LIMITED_READER_CONTEXT` 保持可接受但显式披露；最终 Reader Brief bytes 由同次 finalization
      quality evidence 覆盖；manifest 经`FINALIZING`两阶段 closure 后才可 PASS；canonical writer
      只对 Windows transient replace contention bounded retry，非 transient/耗尽仍失败并清 temp；focused/architecture/contract/integration/reproducibility/Full
      PASS；旧 2026-07-22 state/ledger byte-identical；`production_effect=none`。|2026-07-24：pre-Full
      architecture 569、contract 268、integration 993、reproducibility 23 PASS。首次 Full=`7053
      passed / 1 failed / 4 skipped / 1091.87s`，唯一失败为同 key 跨进程 cache publication 的
      `ATOMIC_ARTIFACT_WRITE_FAILED`；focused 单跑随后 PASS 但不作为绕过。已按 no-silent-workaround
      登记实现层修复，不写 production/active-shadow weights，不触发 broker/order/trading。|'
    history_completeness: LEGACY_HISTORY_PARTIAL
    ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-adc1687edb7794bc3a4552cc
  task_id: OPS-067_READER_BRIEF_QUALITY_FAIL_CLOSED_FINALIZATION
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: arch_005_shadow_importer
  change_id: ARCH-005-S1-SHADOW-IMPORT
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: IN_PROGRESS
  payload:
    priority: P0
    next_owner: architecture coordinator 修复并验证 Windows atomic replace contention，完成
      Full/post-Full 与 push；随后 operations owner 执行 canonical daily acceptance
    notes: 2026-07-24：pre-Full architecture 569、contract 268、integration 993、reproducibility
      23 PASS。首次 Full=`7053 passed / 1 failed / 4 skipped / 1091.87s`，唯一失败为同 key 跨进程
      cache publication 的 `ATOMIC_ARTIFACT_WRITE_FAILED`；focused 单跑随后 PASS 但不作为绕过。已按
      no-silent-workaround 登记实现层修复，不写 production/active-shadow weights，不触发 broker/order/trading。
  rationale: Lossless legacy row import; historical event time is unavailable.
  evidence_refs:
  - sha256:adc1687edb7794bc3a4552cc5ce089a052fcbfbdb5ef44eedacc5bb5e0a46696
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - OPS-067_READER_BRIEF_QUALITY_FAIL_CLOSED_FINALIZATION
  - Operations / canonical Reader Brief and report quality fail-closed finalization
  - P0
  - IN_PROGRESS
  - architecture coordinator 修复并验证 Windows atomic replace contention，完成 Full/post-Full
    与 push；随后 operations owner 执行 canonical daily acceptance
  - 详见 `docs/requirements/OPS-067_Reader_Brief_Quality_Fail_Closed_Finalization.md`。实现与
    `ada1f7bc` exact-tree pre-Full gates 已通过；`00b41d5c` 首次 Full 的唯一失败暴露 canonical
    writer 在 Windows 短暂 reader handle 下 `os.replace` 的 `WinError 5` contention。先完成
    bounded transient retry 与定向重复验证，再运行第二次 Full；通过、push 且无并发 writer 后，才只用 `aits ops
    daily-run` 做 provider-ready 运营验收。DATA-GOV D0C power-loss durability 仍独立跟踪。
  - Report quality `FAIL` 与 Reader Brief quality `FAILED` CLI/step/finalization 全部
    fail closed；`LIMITED_READER_CONTEXT` 保持可接受但显式披露；最终 Reader Brief bytes 由同次 finalization
    quality evidence 覆盖；manifest 经`FINALIZING`两阶段 closure 后才可 PASS；canonical writer
    只对 Windows transient replace contention bounded retry，非 transient/耗尽仍失败并清 temp；focused/architecture/contract/integration/reproducibility/Full
    PASS；旧 2026-07-22 state/ledger byte-identical；`production_effect=none`。
  - 2026-07-24：pre-Full architecture 569、contract 268、integration 993、reproducibility
    23 PASS。首次 Full=`7053 passed / 1 failed / 4 skipped / 1091.87s`，唯一失败为同 key 跨进程
    cache publication 的 `ATOMIC_ARTIFACT_WRITE_FAILED`；focused 单跑随后 PASS 但不作为绕过。已按
    no-silent-workaround 登记实现层修复，不写 production/active-shadow weights，不触发 broker/order/trading。
  docs_links:
  - docs/requirements/OPS-067_Reader_Brief_Quality_Fail_Closed_Finalization.md
  terminal: false
  raw_row_sha256: adc1687edb7794bc3a4552cc5ce089a052fcbfbdb5ef44eedacc5bb5e0a46696
producer_version: arch_005_shadow_registry_compiler.v1
fragment_checksum: 951970c6987ce5468e0f1158e5afe4cfc4d4e97a8a6246030e68e508518143f4
