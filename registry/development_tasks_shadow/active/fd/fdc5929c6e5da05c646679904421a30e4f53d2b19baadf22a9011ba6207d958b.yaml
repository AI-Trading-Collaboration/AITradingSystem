schema_version: arch_005_task_shadow_fragment.v1
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
task_record:
  schema_version: task_record.v1
  task_id: TRADING-111_to_113
  title: null
  domain: ETF portfolio research/Dynamic v3 Rescue Real Research Evidence Closure
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: 系统验证 + 项目 owner 人工复核
  requirement_refs:
  - docs/requirements/TRADING-111_to_113_Real_Research_Evidence_Closure.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详细拆解见 `docs/requirements/TRADING-111_to_113_Real_Research_Evidence_Closure.md`；data
    provenance repair/validate、window latest fail-closed、canonical latest pointer
    validation、promotion pack latest window audit consumption 和 `artifacts repair-latest`
    已完成；当前本机 provenance 为 `RECONSTRUCTED_MANIFEST` 的 `PASS_WITH_WARNINGS`，window audit
    latest 可读但 audit status 为 `FAIL` / promotion_blocking_count=170；重新生成 promotion
    pack 后 `evidence_summary` 正确披露 `backtest_window_status=FAIL`、`window_audit_id=2eface90ec36f7bf`
    和 `BACKTEST_WINDOW_INCOMPLETE`，剩余限制是 owner/系统决定是否重新跑完整窗口 real sweep / weight tuning
    来补齐真实窗口证据
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: 新增 `window-audit`、`weight-path`、`data-provenance` CLI；real sweep / candidate
      / attribution / promotion artifacts 显示 requested 与 actual date range、daily weight
      path、data provenance 状态；缺 `actual_evaluation_start/end`、daily weight path、checksum
      provenance 或 attribution source 时不得 PASS；promotion pack 必须消费 latest window audit、weight
      path、candidate attribution、data provenance 和 overfit evidence；`BACKTEST_WINDOW_INCOMPLETE`、`MISSING_DAILY_WEIGHT_PATH`、`DATA_PROVENANCE_INCOMPLETE`、`ATTRIBUTION_INCOMPLETE`、`OVERFIT_REVIEW_REQUIRED`
      阻断 `promote_candidate` / `production_candidate`；`data-provenance validate` 和
      `window-audit report --latest` 必须对当前本机 latest/cache 状态给出可审计 PASS / PASS_WITH_WARNINGS
      / FAIL，不得 traceback 或 silent pass；`artifacts validate` 必须拒绝 canonical 根外 pointer，`repair-latest`
      只能从 canonical root 重建指针
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  legacy_source:
    path: docs/task_register.md
    source_partition: active
    line_number: 1468
    row_sha256: 641a27cb3793713ff76f390a7a7bb65f63fd8c55112c1ee30911cc68373f67cf
    cell_count: 8
    all_cells:
    - TRADING-111_to_113
    - ETF portfolio research/Dynamic v3 Rescue Real Research Evidence Closure
    - P0
    - VALIDATING
    - 系统验证 + 项目 owner 人工复核
    - 详细拆解见 `docs/requirements/TRADING-111_to_113_Real_Research_Evidence_Closure.md`；data
      provenance repair/validate、window latest fail-closed、canonical latest pointer
      validation、promotion pack latest window audit consumption 和 `artifacts repair-latest`
      已完成；当前本机 provenance 为 `RECONSTRUCTED_MANIFEST` 的 `PASS_WITH_WARNINGS`，window
      audit latest 可读但 audit status 为 `FAIL` / promotion_blocking_count=170；重新生成 promotion
      pack 后 `evidence_summary` 正确披露 `backtest_window_status=FAIL`、`window_audit_id=2eface90ec36f7bf`
      和 `BACKTEST_WINDOW_INCOMPLETE`，剩余限制是 owner/系统决定是否重新跑完整窗口 real sweep / weight
      tuning 来补齐真实窗口证据
    - 新增 `window-audit`、`weight-path`、`data-provenance` CLI；real sweep / candidate
      / attribution / promotion artifacts 显示 requested 与 actual date range、daily weight
      path、data provenance 状态；缺 `actual_evaluation_start/end`、daily weight path、checksum
      provenance 或 attribution source 时不得 PASS；promotion pack 必须消费 latest window audit、weight
      path、candidate attribution、data provenance 和 overfit evidence；`BACKTEST_WINDOW_INCOMPLETE`、`MISSING_DAILY_WEIGHT_PATH`、`DATA_PROVENANCE_INCOMPLETE`、`ATTRIBUTION_INCOMPLETE`、`OVERFIT_REVIEW_REQUIRED`
      阻断 `promote_candidate` / `production_candidate`；`data-provenance validate` 和
      `window-audit report --latest` 必须对当前本机 latest/cache 状态给出可审计 PASS / PASS_WITH_WARNINGS
      / FAIL，不得 traceback 或 silent pass；`artifacts validate` 必须拒绝 canonical 根外 pointer，`repair-latest`
      只能从 canonical root 重建指针
    - '2026-06-06: 从 IN_PROGRESS 进入 VALIDATING，原因：已完成 data provenance、window audit、weight
      path export、candidate attribution PARTIAL/COMPLETE、promotion pack evidence gate、Reader
      Brief/report registry/artifact catalog/system flow/runbook/README integration；验证通过
      ruff、compileall、git diff check、dynamic-v3 focused tests、Reader Brief focused
      tests 和全量 pytest。2026-06-07: 从 VALIDATING 改回 IN_PROGRESS，原因：复验发现 `data-provenance
      validate` 输出 FAIL（`prices_checksum_in_manifest=false`），`data-provenance inspect-price-cache`
      显示 manifest 可用但 checksum 未匹配，且 `window-audit report --latest` 指向缺失 `window_audit_manifest.json`
      并抛 traceback；本轮需修复 current evidence closure，而不是把缺口留给 owner 复核。2026-06-07: 修复完成并转回
      VALIDATING；`repair-price-manifest` 后 `data-provenance validate` 为 `PASS_WITH_WARNINGS`、failed_check_count=0、`prices_checksum_in_manifest=true`、`provenance_status=RECONSTRUCTED_MANIFEST`；新增
      latest pointer canonical-root hardening 和 `artifacts repair-latest`，当前重建 15
      个 canonical pointers，`artifacts validate` PASS，forced `schedule observe` PASS；`window-audit
      report --latest` 不再 traceback，canonical audit `2eface90ec36f7bf` 可读且 `validate-window-audit`
      PASS，但 audit status=FAIL / promotion_blocking_count=170，继续阻断 promotion；验证通过
      dynamic-v3 focused tests（16 passed）、TRADING-099 scheduler/daily/direct/dynamic-v3
      tests（72 passed）、ruff、black、compileall、docs freshness、docs report contract 和
      git diff check。2026-06-07: 再次转入 IN_PROGRESS，原因：current promotion pack regeneration
      后 `validate-promotion-pack` 结构 PASS，但 `evidence_summary.backtest_window_status=MISSING`，没有把
      latest window audit 的 `FAIL` / 170 blockers 纳入 promotion evidence gate。2026-06-07:
      修复完成并转回 VALIDATING；`promotion pack` 新增 `--window-audit-dir` 并默认读取 canonical
      `latest_window_audit`，当前重新生成 pack `4cb5deda6f6a9853` 后 `validate-promotion-pack`
      PASS，`evidence_summary` 显示 `backtest_window_status=FAIL`、`window_audit_id=2eface90ec36f7bf`、`promotion_blocking_count=170`，blocking
      flags 包含 `BACKTEST_WINDOW_INCOMPLETE`；验证通过 Dynamic v3 focused tests（16 passed）、scheduler/daily/direct/dynamic-v3
      tests（72 passed）、ruff、black、compileall、docs freshness、docs report contract 和
      git diff check。'
    raw_line: '|TRADING-111_to_113|ETF portfolio research/Dynamic v3 Rescue Real Research
      Evidence Closure|P0|VALIDATING|系统验证 + 项目 owner 人工复核|详细拆解见 `docs/requirements/TRADING-111_to_113_Real_Research_Evidence_Closure.md`；data
      provenance repair/validate、window latest fail-closed、canonical latest pointer
      validation、promotion pack latest window audit consumption 和 `artifacts repair-latest`
      已完成；当前本机 provenance 为 `RECONSTRUCTED_MANIFEST` 的 `PASS_WITH_WARNINGS`，window
      audit latest 可读但 audit status 为 `FAIL` / promotion_blocking_count=170；重新生成 promotion
      pack 后 `evidence_summary` 正确披露 `backtest_window_status=FAIL`、`window_audit_id=2eface90ec36f7bf`
      和 `BACKTEST_WINDOW_INCOMPLETE`，剩余限制是 owner/系统决定是否重新跑完整窗口 real sweep / weight
      tuning 来补齐真实窗口证据|新增 `window-audit`、`weight-path`、`data-provenance` CLI；real
      sweep / candidate / attribution / promotion artifacts 显示 requested 与 actual
      date range、daily weight path、data provenance 状态；缺 `actual_evaluation_start/end`、daily
      weight path、checksum provenance 或 attribution source 时不得 PASS；promotion pack
      必须消费 latest window audit、weight path、candidate attribution、data provenance 和
      overfit evidence；`BACKTEST_WINDOW_INCOMPLETE`、`MISSING_DAILY_WEIGHT_PATH`、`DATA_PROVENANCE_INCOMPLETE`、`ATTRIBUTION_INCOMPLETE`、`OVERFIT_REVIEW_REQUIRED`
      阻断 `promote_candidate` / `production_candidate`；`data-provenance validate` 和
      `window-audit report --latest` 必须对当前本机 latest/cache 状态给出可审计 PASS / PASS_WITH_WARNINGS
      / FAIL，不得 traceback 或 silent pass；`artifacts validate` 必须拒绝 canonical 根外 pointer，`repair-latest`
      只能从 canonical root 重建指针|2026-06-06: 从 IN_PROGRESS 进入 VALIDATING，原因：已完成 data
      provenance、window audit、weight path export、candidate attribution PARTIAL/COMPLETE、promotion
      pack evidence gate、Reader Brief/report registry/artifact catalog/system flow/runbook/README
      integration；验证通过 ruff、compileall、git diff check、dynamic-v3 focused tests、Reader
      Brief focused tests 和全量 pytest。2026-06-07: 从 VALIDATING 改回 IN_PROGRESS，原因：复验发现
      `data-provenance validate` 输出 FAIL（`prices_checksum_in_manifest=false`），`data-provenance
      inspect-price-cache` 显示 manifest 可用但 checksum 未匹配，且 `window-audit report --latest`
      指向缺失 `window_audit_manifest.json` 并抛 traceback；本轮需修复 current evidence closure，而不是把缺口留给
      owner 复核。2026-06-07: 修复完成并转回 VALIDATING；`repair-price-manifest` 后 `data-provenance
      validate` 为 `PASS_WITH_WARNINGS`、failed_check_count=0、`prices_checksum_in_manifest=true`、`provenance_status=RECONSTRUCTED_MANIFEST`；新增
      latest pointer canonical-root hardening 和 `artifacts repair-latest`，当前重建 15
      个 canonical pointers，`artifacts validate` PASS，forced `schedule observe` PASS；`window-audit
      report --latest` 不再 traceback，canonical audit `2eface90ec36f7bf` 可读且 `validate-window-audit`
      PASS，但 audit status=FAIL / promotion_blocking_count=170，继续阻断 promotion；验证通过
      dynamic-v3 focused tests（16 passed）、TRADING-099 scheduler/daily/direct/dynamic-v3
      tests（72 passed）、ruff、black、compileall、docs freshness、docs report contract 和
      git diff check。2026-06-07: 再次转入 IN_PROGRESS，原因：current promotion pack regeneration
      后 `validate-promotion-pack` 结构 PASS，但 `evidence_summary.backtest_window_status=MISSING`，没有把
      latest window audit 的 `FAIL` / 170 blockers 纳入 promotion evidence gate。2026-06-07:
      修复完成并转回 VALIDATING；`promotion pack` 新增 `--window-audit-dir` 并默认读取 canonical
      `latest_window_audit`，当前重新生成 pack `4cb5deda6f6a9853` 后 `validate-promotion-pack`
      PASS，`evidence_summary` 显示 `backtest_window_status=FAIL`、`window_audit_id=2eface90ec36f7bf`、`promotion_blocking_count=170`，blocking
      flags 包含 `BACKTEST_WINDOW_INCOMPLETE`；验证通过 Dynamic v3 focused tests（16 passed）、scheduler/daily/direct/dynamic-v3
      tests（72 passed）、ruff、black、compileall、docs freshness、docs report contract 和
      git diff check。|'
    history_completeness: LEGACY_HISTORY_PARTIAL
    ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-641a27cb3793713ff76f390a
  task_id: TRADING-111_to_113
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: arch_005_shadow_importer
  change_id: ARCH-005-S1-SHADOW-IMPORT
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: VALIDATING
  payload:
    priority: P0
    next_owner: 系统验证 + 项目 owner 人工复核
    notes: '2026-06-06: 从 IN_PROGRESS 进入 VALIDATING，原因：已完成 data provenance、window
      audit、weight path export、candidate attribution PARTIAL/COMPLETE、promotion pack
      evidence gate、Reader Brief/report registry/artifact catalog/system flow/runbook/README
      integration；验证通过 ruff、compileall、git diff check、dynamic-v3 focused tests、Reader
      Brief focused tests 和全量 pytest。2026-06-07: 从 VALIDATING 改回 IN_PROGRESS，原因：复验发现
      `data-provenance validate` 输出 FAIL（`prices_checksum_in_manifest=false`），`data-provenance
      inspect-price-cache` 显示 manifest 可用但 checksum 未匹配，且 `window-audit report --latest`
      指向缺失 `window_audit_manifest.json` 并抛 traceback；本轮需修复 current evidence closure，而不是把缺口留给
      owner 复核。2026-06-07: 修复完成并转回 VALIDATING；`repair-price-manifest` 后 `data-provenance
      validate` 为 `PASS_WITH_WARNINGS`、failed_check_count=0、`prices_checksum_in_manifest=true`、`provenance_status=RECONSTRUCTED_MANIFEST`；新增
      latest pointer canonical-root hardening 和 `artifacts repair-latest`，当前重建 15
      个 canonical pointers，`artifacts validate` PASS，forced `schedule observe` PASS；`window-audit
      report --latest` 不再 traceback，canonical audit `2eface90ec36f7bf` 可读且 `validate-window-audit`
      PASS，但 audit status=FAIL / promotion_blocking_count=170，继续阻断 promotion；验证通过
      dynamic-v3 focused tests（16 passed）、TRADING-099 scheduler/daily/direct/dynamic-v3
      tests（72 passed）、ruff、black、compileall、docs freshness、docs report contract 和
      git diff check。2026-06-07: 再次转入 IN_PROGRESS，原因：current promotion pack regeneration
      后 `validate-promotion-pack` 结构 PASS，但 `evidence_summary.backtest_window_status=MISSING`，没有把
      latest window audit 的 `FAIL` / 170 blockers 纳入 promotion evidence gate。2026-06-07:
      修复完成并转回 VALIDATING；`promotion pack` 新增 `--window-audit-dir` 并默认读取 canonical
      `latest_window_audit`，当前重新生成 pack `4cb5deda6f6a9853` 后 `validate-promotion-pack`
      PASS，`evidence_summary` 显示 `backtest_window_status=FAIL`、`window_audit_id=2eface90ec36f7bf`、`promotion_blocking_count=170`，blocking
      flags 包含 `BACKTEST_WINDOW_INCOMPLETE`；验证通过 Dynamic v3 focused tests（16 passed）、scheduler/daily/direct/dynamic-v3
      tests（72 passed）、ruff、black、compileall、docs freshness、docs report contract 和
      git diff check。'
  rationale: Lossless legacy row import; historical event time is unavailable.
  evidence_refs:
  - sha256:641a27cb3793713ff76f390a7a7bb65f63fd8c55112c1ee30911cc68373f67cf
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - TRADING-111_to_113
  - ETF portfolio research/Dynamic v3 Rescue Real Research Evidence Closure
  - P0
  - VALIDATING
  - 系统验证 + 项目 owner 人工复核
  - 详细拆解见 `docs/requirements/TRADING-111_to_113_Real_Research_Evidence_Closure.md`；data
    provenance repair/validate、window latest fail-closed、canonical latest pointer
    validation、promotion pack latest window audit consumption 和 `artifacts repair-latest`
    已完成；当前本机 provenance 为 `RECONSTRUCTED_MANIFEST` 的 `PASS_WITH_WARNINGS`，window audit
    latest 可读但 audit status 为 `FAIL` / promotion_blocking_count=170；重新生成 promotion
    pack 后 `evidence_summary` 正确披露 `backtest_window_status=FAIL`、`window_audit_id=2eface90ec36f7bf`
    和 `BACKTEST_WINDOW_INCOMPLETE`，剩余限制是 owner/系统决定是否重新跑完整窗口 real sweep / weight tuning
    来补齐真实窗口证据
  - 新增 `window-audit`、`weight-path`、`data-provenance` CLI；real sweep / candidate /
    attribution / promotion artifacts 显示 requested 与 actual date range、daily weight
    path、data provenance 状态；缺 `actual_evaluation_start/end`、daily weight path、checksum
    provenance 或 attribution source 时不得 PASS；promotion pack 必须消费 latest window audit、weight
    path、candidate attribution、data provenance 和 overfit evidence；`BACKTEST_WINDOW_INCOMPLETE`、`MISSING_DAILY_WEIGHT_PATH`、`DATA_PROVENANCE_INCOMPLETE`、`ATTRIBUTION_INCOMPLETE`、`OVERFIT_REVIEW_REQUIRED`
    阻断 `promote_candidate` / `production_candidate`；`data-provenance validate` 和 `window-audit
    report --latest` 必须对当前本机 latest/cache 状态给出可审计 PASS / PASS_WITH_WARNINGS / FAIL，不得
    traceback 或 silent pass；`artifacts validate` 必须拒绝 canonical 根外 pointer，`repair-latest`
    只能从 canonical root 重建指针
  - '2026-06-06: 从 IN_PROGRESS 进入 VALIDATING，原因：已完成 data provenance、window audit、weight
    path export、candidate attribution PARTIAL/COMPLETE、promotion pack evidence gate、Reader
    Brief/report registry/artifact catalog/system flow/runbook/README integration；验证通过
    ruff、compileall、git diff check、dynamic-v3 focused tests、Reader Brief focused tests
    和全量 pytest。2026-06-07: 从 VALIDATING 改回 IN_PROGRESS，原因：复验发现 `data-provenance validate`
    输出 FAIL（`prices_checksum_in_manifest=false`），`data-provenance inspect-price-cache`
    显示 manifest 可用但 checksum 未匹配，且 `window-audit report --latest` 指向缺失 `window_audit_manifest.json`
    并抛 traceback；本轮需修复 current evidence closure，而不是把缺口留给 owner 复核。2026-06-07: 修复完成并转回
    VALIDATING；`repair-price-manifest` 后 `data-provenance validate` 为 `PASS_WITH_WARNINGS`、failed_check_count=0、`prices_checksum_in_manifest=true`、`provenance_status=RECONSTRUCTED_MANIFEST`；新增
    latest pointer canonical-root hardening 和 `artifacts repair-latest`，当前重建 15 个
    canonical pointers，`artifacts validate` PASS，forced `schedule observe` PASS；`window-audit
    report --latest` 不再 traceback，canonical audit `2eface90ec36f7bf` 可读且 `validate-window-audit`
    PASS，但 audit status=FAIL / promotion_blocking_count=170，继续阻断 promotion；验证通过 dynamic-v3
    focused tests（16 passed）、TRADING-099 scheduler/daily/direct/dynamic-v3 tests（72
    passed）、ruff、black、compileall、docs freshness、docs report contract 和 git diff check。2026-06-07:
    再次转入 IN_PROGRESS，原因：current promotion pack regeneration 后 `validate-promotion-pack`
    结构 PASS，但 `evidence_summary.backtest_window_status=MISSING`，没有把 latest window
    audit 的 `FAIL` / 170 blockers 纳入 promotion evidence gate。2026-06-07: 修复完成并转回 VALIDATING；`promotion
    pack` 新增 `--window-audit-dir` 并默认读取 canonical `latest_window_audit`，当前重新生成 pack
    `4cb5deda6f6a9853` 后 `validate-promotion-pack` PASS，`evidence_summary` 显示 `backtest_window_status=FAIL`、`window_audit_id=2eface90ec36f7bf`、`promotion_blocking_count=170`，blocking
    flags 包含 `BACKTEST_WINDOW_INCOMPLETE`；验证通过 Dynamic v3 focused tests（16 passed）、scheduler/daily/direct/dynamic-v3
    tests（72 passed）、ruff、black、compileall、docs freshness、docs report contract 和 git
    diff check。'
  docs_links:
  - docs/requirements/TRADING-111_to_113_Real_Research_Evidence_Closure.md
  terminal: false
  raw_row_sha256: 641a27cb3793713ff76f390a7a7bb65f63fd8c55112c1ee30911cc68373f67cf
producer_version: arch_005_shadow_registry_compiler.v1
fragment_checksum: 1114e4c3208e8f6c89c40b301d42e193c9b80001ab4d04af5745532cd33dc46d
