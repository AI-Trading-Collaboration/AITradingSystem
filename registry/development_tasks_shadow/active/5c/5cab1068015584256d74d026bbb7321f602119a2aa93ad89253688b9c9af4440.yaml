schema_version: arch_005_task_shadow_fragment.v1
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
task_record:
  schema_version: task_record.v1
  task_id: TRADING-347_FULL_TEST_RUNTIME_STABILIZATION
  title: null
  domain: Developer productivity/full pytest runtime stabilization
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: 系统实现 + 项目 owner 后续复核
  requirement_refs:
  - docs/requirements/TRADING-347_Full_Test_Runtime_Stabilization.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详细拆解见 `docs/requirements/TRADING-347_Full_Test_Runtime_Stabilization.md`；承接
    TRADING-336～346 后 full pytest 604 秒超时记录，把未来 promotion 证据从单一 full/focused 二分改成可审计
    suite taxonomy；不得改变 strategy logic、production state、broker action、cached market
    data 或投资解释路径
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: 新增正式验证 suite：fast unit、contract validation、report validation、integration、slow
      research regression；保留旧 tier alias；pytest marker/命令示例/CI-friendly 用法/slow suite
      隔离/ promotion-blocking 规则文档化；`scripts/run_validation_tier.py` 输出 `test_runtime_summary.json`
      和 `test_runtime_reader_brief.md` runtime artifact；operations runbook、artifact
      catalog、report registry、system flow、README、requirements、task register 和 runner
      tests 同步；验证通过 fast suite、contract validation suite、runner tests、ruff、compileall、git
      diff check 和 documentation/report contract；slow suite 可不作为本轮 blocking 但必须显式分离
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  legacy_source:
    path: docs/task_register.md
    source_partition: active
    line_number: 1493
    row_sha256: 1f5b1fd8ec59e6126b90c083ac53f1b8e482cf8ff525df0f9e1d4139c3bca2ac
    cell_count: 8
    all_cells:
    - TRADING-347_FULL_TEST_RUNTIME_STABILIZATION
    - Developer productivity/full pytest runtime stabilization
    - P0
    - VALIDATING
    - 系统实现 + 项目 owner 后续复核
    - 详细拆解见 `docs/requirements/TRADING-347_Full_Test_Runtime_Stabilization.md`；承接
      TRADING-336～346 后 full pytest 604 秒超时记录，把未来 promotion 证据从单一 full/focused 二分改成可审计
      suite taxonomy；不得改变 strategy logic、production state、broker action、cached market
      data 或投资解释路径
    - 新增正式验证 suite：fast unit、contract validation、report validation、integration、slow
      research regression；保留旧 tier alias；pytest marker/命令示例/CI-friendly 用法/slow suite
      隔离/ promotion-blocking 规则文档化；`scripts/run_validation_tier.py` 输出 `test_runtime_summary.json`
      和 `test_runtime_reader_brief.md` runtime artifact；operations runbook、artifact
      catalog、report registry、system flow、README、requirements、task register 和 runner
      tests 同步；验证通过 fast suite、contract validation suite、runner tests、ruff、compileall、git
      diff check 和 documentation/report contract；slow suite 可不作为本轮 blocking 但必须显式分离
    - '2026-06-15: 新增并进入 IN_PROGRESS，原因：owner 要求完成附件中的 TRADING-347；P0 聚焦让 promotion
      证据不再依赖 full pytest 是否在本地 timeout 预算内完成，先建立可审计 runtime report 和 promotion-blocking
      suite 边界。2026-06-15: 实现完成并转入 VALIDATING；新增正式 suite taxonomy、legacy alias、pytest
      markers、runtime artifacts 和 report registry/catalog/system flow/runbook/README
      文档；首次默认并行运行发现本地 `.venv` 缺 `pytest-xdist`，已安装项目声明的 dev dependency 后重跑，未降级为串行
      PASS；`fast-unit` 43 passed / 31.93s，`contract-validation` 19 passed / 22.86s，runner
      tests 6 passed，documentation contract PASS，report index `PASS_WITH_WARNINGS`
      仅保留既有 missing/stale visibility；产物写入 `outputs/validation_runtime/trading-347-fast-unit/`
      和 `outputs/validation_runtime/trading-347-contract-validation/`；保持 no strategy
      logic change / no cached data mutation / no broker / no production。2026-06-20:
      继续 runtime optimization 诊断；原始串行 `python -m pytest -q` 为 2961 passed / 1805.89s，既有
      runner 并行 full `--dist loadfile` 为 2961 passed / 253.37s，`--dist load` 为 2961
      passed / 281.59s。已优化 Campaign validation artifact 重复构建：pack builder 复用 writer
      已生成的 component payload，最慢用例约 137～161s 降至约 60～73s，优化后并行 full 为 2961 passed /
      195.72s（runner elapsed 210.84s）。2026-06-20: owner 决定后续所有需要 pytest 验证的任务默认使用并行模式；首选
      validation tier runner，focused one-off pytest 显式使用 `python -m pytest -n 16 --dist
      loadfile ...`，串行 pytest 仅用于复现或 parallelism-related failure 诊断，且不能把并行失败静默改写成串行
      PASS。2026-06-20: 追加瓶颈定位；16 worker loadfile、16 worker worksteal 和 24 worker loadfile
      均约 241～248s，主瓶颈确认为少数 Campaign/research artifact 集成测试的单测粒度和串行 compute builder，而非
      worker 数不足。2026-06-20: 完成 B2 validation-run scoped compute payload cache 优化；只在
      Campaign validation artifact builder / focused tests 中复用 targeted、control-window
      和 full diagnostic payload，每次仍写入当前 output directory，默认 CLI/production Campaign
      runtime 不启用该 cache。focused validation-pack call 降至 19.30s，`tests/test_research_campaign.py`
      从 127.68s 降至 44.44s，全量并行 full tier 2961 passed / 643 warnings / pytest 173.30s（runner
      elapsed 174.02s），runtime artifact 写入 `outputs/validation_runtime/trading-347-bottleneck-after-b2-cache/`。2026-06-20:
      完成 Dynamic v3 validation sample cache 优化；failure attribution focused 52.52s
      -> 29.05s，dynamic rescue focused 58.70s -> 20.08s，parameter research focused
      56.10s -> 40.85s，三组 focused 合并 24 passed / 39.79s；全量并行 full tier 2961 passed
      / 643 warnings / pytest 167.65s（runner elapsed 168.27s），runtime artifact 写入
      `outputs/validation_runtime/trading-347-dynamic-v3-cache-full/`。当前剩余瓶颈分散在多个
      25～32s 级独立研究回归单测。保持 no strategy logic change / no cached data mutation / no
      broker / no production。'
    raw_line: '|TRADING-347_FULL_TEST_RUNTIME_STABILIZATION|Developer productivity/full
      pytest runtime stabilization|P0|VALIDATING|系统实现 + 项目 owner 后续复核|详细拆解见 `docs/requirements/TRADING-347_Full_Test_Runtime_Stabilization.md`；承接
      TRADING-336～346 后 full pytest 604 秒超时记录，把未来 promotion 证据从单一 full/focused 二分改成可审计
      suite taxonomy；不得改变 strategy logic、production state、broker action、cached market
      data 或投资解释路径|新增正式验证 suite：fast unit、contract validation、report validation、integration、slow
      research regression；保留旧 tier alias；pytest marker/命令示例/CI-friendly 用法/slow suite
      隔离/ promotion-blocking 规则文档化；`scripts/run_validation_tier.py` 输出 `test_runtime_summary.json`
      和 `test_runtime_reader_brief.md` runtime artifact；operations runbook、artifact
      catalog、report registry、system flow、README、requirements、task register 和 runner
      tests 同步；验证通过 fast suite、contract validation suite、runner tests、ruff、compileall、git
      diff check 和 documentation/report contract；slow suite 可不作为本轮 blocking 但必须显式分离|2026-06-15:
      新增并进入 IN_PROGRESS，原因：owner 要求完成附件中的 TRADING-347；P0 聚焦让 promotion 证据不再依赖 full
      pytest 是否在本地 timeout 预算内完成，先建立可审计 runtime report 和 promotion-blocking suite
      边界。2026-06-15: 实现完成并转入 VALIDATING；新增正式 suite taxonomy、legacy alias、pytest markers、runtime
      artifacts 和 report registry/catalog/system flow/runbook/README 文档；首次默认并行运行发现本地
      `.venv` 缺 `pytest-xdist`，已安装项目声明的 dev dependency 后重跑，未降级为串行 PASS；`fast-unit`
      43 passed / 31.93s，`contract-validation` 19 passed / 22.86s，runner tests 6 passed，documentation
      contract PASS，report index `PASS_WITH_WARNINGS` 仅保留既有 missing/stale visibility；产物写入
      `outputs/validation_runtime/trading-347-fast-unit/` 和 `outputs/validation_runtime/trading-347-contract-validation/`；保持
      no strategy logic change / no cached data mutation / no broker / no production。2026-06-20:
      继续 runtime optimization 诊断；原始串行 `python -m pytest -q` 为 2961 passed / 1805.89s，既有
      runner 并行 full `--dist loadfile` 为 2961 passed / 253.37s，`--dist load` 为 2961
      passed / 281.59s。已优化 Campaign validation artifact 重复构建：pack builder 复用 writer
      已生成的 component payload，最慢用例约 137～161s 降至约 60～73s，优化后并行 full 为 2961 passed /
      195.72s（runner elapsed 210.84s）。2026-06-20: owner 决定后续所有需要 pytest 验证的任务默认使用并行模式；首选
      validation tier runner，focused one-off pytest 显式使用 `python -m pytest -n 16 --dist
      loadfile ...`，串行 pytest 仅用于复现或 parallelism-related failure 诊断，且不能把并行失败静默改写成串行
      PASS。2026-06-20: 追加瓶颈定位；16 worker loadfile、16 worker worksteal 和 24 worker loadfile
      均约 241～248s，主瓶颈确认为少数 Campaign/research artifact 集成测试的单测粒度和串行 compute builder，而非
      worker 数不足。2026-06-20: 完成 B2 validation-run scoped compute payload cache 优化；只在
      Campaign validation artifact builder / focused tests 中复用 targeted、control-window
      和 full diagnostic payload，每次仍写入当前 output directory，默认 CLI/production Campaign
      runtime 不启用该 cache。focused validation-pack call 降至 19.30s，`tests/test_research_campaign.py`
      从 127.68s 降至 44.44s，全量并行 full tier 2961 passed / 643 warnings / pytest 173.30s（runner
      elapsed 174.02s），runtime artifact 写入 `outputs/validation_runtime/trading-347-bottleneck-after-b2-cache/`。2026-06-20:
      完成 Dynamic v3 validation sample cache 优化；failure attribution focused 52.52s
      -> 29.05s，dynamic rescue focused 58.70s -> 20.08s，parameter research focused
      56.10s -> 40.85s，三组 focused 合并 24 passed / 39.79s；全量并行 full tier 2961 passed
      / 643 warnings / pytest 167.65s（runner elapsed 168.27s），runtime artifact 写入
      `outputs/validation_runtime/trading-347-dynamic-v3-cache-full/`。当前剩余瓶颈分散在多个
      25～32s 级独立研究回归单测。保持 no strategy logic change / no cached data mutation / no
      broker / no production。|'
    history_completeness: LEGACY_HISTORY_PARTIAL
    ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-1f5b1fd8ec59e6126b90c083
  task_id: TRADING-347_FULL_TEST_RUNTIME_STABILIZATION
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: arch_005_shadow_importer
  change_id: ARCH-005-S1-SHADOW-IMPORT
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: VALIDATING
  payload:
    priority: P0
    next_owner: 系统实现 + 项目 owner 后续复核
    notes: '2026-06-15: 新增并进入 IN_PROGRESS，原因：owner 要求完成附件中的 TRADING-347；P0 聚焦让 promotion
      证据不再依赖 full pytest 是否在本地 timeout 预算内完成，先建立可审计 runtime report 和 promotion-blocking
      suite 边界。2026-06-15: 实现完成并转入 VALIDATING；新增正式 suite taxonomy、legacy alias、pytest
      markers、runtime artifacts 和 report registry/catalog/system flow/runbook/README
      文档；首次默认并行运行发现本地 `.venv` 缺 `pytest-xdist`，已安装项目声明的 dev dependency 后重跑，未降级为串行
      PASS；`fast-unit` 43 passed / 31.93s，`contract-validation` 19 passed / 22.86s，runner
      tests 6 passed，documentation contract PASS，report index `PASS_WITH_WARNINGS`
      仅保留既有 missing/stale visibility；产物写入 `outputs/validation_runtime/trading-347-fast-unit/`
      和 `outputs/validation_runtime/trading-347-contract-validation/`；保持 no strategy
      logic change / no cached data mutation / no broker / no production。2026-06-20:
      继续 runtime optimization 诊断；原始串行 `python -m pytest -q` 为 2961 passed / 1805.89s，既有
      runner 并行 full `--dist loadfile` 为 2961 passed / 253.37s，`--dist load` 为 2961
      passed / 281.59s。已优化 Campaign validation artifact 重复构建：pack builder 复用 writer
      已生成的 component payload，最慢用例约 137～161s 降至约 60～73s，优化后并行 full 为 2961 passed /
      195.72s（runner elapsed 210.84s）。2026-06-20: owner 决定后续所有需要 pytest 验证的任务默认使用并行模式；首选
      validation tier runner，focused one-off pytest 显式使用 `python -m pytest -n 16 --dist
      loadfile ...`，串行 pytest 仅用于复现或 parallelism-related failure 诊断，且不能把并行失败静默改写成串行
      PASS。2026-06-20: 追加瓶颈定位；16 worker loadfile、16 worker worksteal 和 24 worker loadfile
      均约 241～248s，主瓶颈确认为少数 Campaign/research artifact 集成测试的单测粒度和串行 compute builder，而非
      worker 数不足。2026-06-20: 完成 B2 validation-run scoped compute payload cache 优化；只在
      Campaign validation artifact builder / focused tests 中复用 targeted、control-window
      和 full diagnostic payload，每次仍写入当前 output directory，默认 CLI/production Campaign
      runtime 不启用该 cache。focused validation-pack call 降至 19.30s，`tests/test_research_campaign.py`
      从 127.68s 降至 44.44s，全量并行 full tier 2961 passed / 643 warnings / pytest 173.30s（runner
      elapsed 174.02s），runtime artifact 写入 `outputs/validation_runtime/trading-347-bottleneck-after-b2-cache/`。2026-06-20:
      完成 Dynamic v3 validation sample cache 优化；failure attribution focused 52.52s
      -> 29.05s，dynamic rescue focused 58.70s -> 20.08s，parameter research focused
      56.10s -> 40.85s，三组 focused 合并 24 passed / 39.79s；全量并行 full tier 2961 passed
      / 643 warnings / pytest 167.65s（runner elapsed 168.27s），runtime artifact 写入
      `outputs/validation_runtime/trading-347-dynamic-v3-cache-full/`。当前剩余瓶颈分散在多个
      25～32s 级独立研究回归单测。保持 no strategy logic change / no cached data mutation / no
      broker / no production。'
  rationale: Lossless legacy row import; historical event time is unavailable.
  evidence_refs:
  - sha256:1f5b1fd8ec59e6126b90c083ac53f1b8e482cf8ff525df0f9e1d4139c3bca2ac
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - TRADING-347_FULL_TEST_RUNTIME_STABILIZATION
  - Developer productivity/full pytest runtime stabilization
  - P0
  - VALIDATING
  - 系统实现 + 项目 owner 后续复核
  - 详细拆解见 `docs/requirements/TRADING-347_Full_Test_Runtime_Stabilization.md`；承接 TRADING-336～346
    后 full pytest 604 秒超时记录，把未来 promotion 证据从单一 full/focused 二分改成可审计 suite taxonomy；不得改变
    strategy logic、production state、broker action、cached market data 或投资解释路径
  - 新增正式验证 suite：fast unit、contract validation、report validation、integration、slow
    research regression；保留旧 tier alias；pytest marker/命令示例/CI-friendly 用法/slow suite
    隔离/ promotion-blocking 规则文档化；`scripts/run_validation_tier.py` 输出 `test_runtime_summary.json`
    和 `test_runtime_reader_brief.md` runtime artifact；operations runbook、artifact
    catalog、report registry、system flow、README、requirements、task register 和 runner
    tests 同步；验证通过 fast suite、contract validation suite、runner tests、ruff、compileall、git
    diff check 和 documentation/report contract；slow suite 可不作为本轮 blocking 但必须显式分离
  - '2026-06-15: 新增并进入 IN_PROGRESS，原因：owner 要求完成附件中的 TRADING-347；P0 聚焦让 promotion
    证据不再依赖 full pytest 是否在本地 timeout 预算内完成，先建立可审计 runtime report 和 promotion-blocking
    suite 边界。2026-06-15: 实现完成并转入 VALIDATING；新增正式 suite taxonomy、legacy alias、pytest
    markers、runtime artifacts 和 report registry/catalog/system flow/runbook/README
    文档；首次默认并行运行发现本地 `.venv` 缺 `pytest-xdist`，已安装项目声明的 dev dependency 后重跑，未降级为串行 PASS；`fast-unit`
    43 passed / 31.93s，`contract-validation` 19 passed / 22.86s，runner tests 6 passed，documentation
    contract PASS，report index `PASS_WITH_WARNINGS` 仅保留既有 missing/stale visibility；产物写入
    `outputs/validation_runtime/trading-347-fast-unit/` 和 `outputs/validation_runtime/trading-347-contract-validation/`；保持
    no strategy logic change / no cached data mutation / no broker / no production。2026-06-20:
    继续 runtime optimization 诊断；原始串行 `python -m pytest -q` 为 2961 passed / 1805.89s，既有
    runner 并行 full `--dist loadfile` 为 2961 passed / 253.37s，`--dist load` 为 2961
    passed / 281.59s。已优化 Campaign validation artifact 重复构建：pack builder 复用 writer
    已生成的 component payload，最慢用例约 137～161s 降至约 60～73s，优化后并行 full 为 2961 passed / 195.72s（runner
    elapsed 210.84s）。2026-06-20: owner 决定后续所有需要 pytest 验证的任务默认使用并行模式；首选 validation
    tier runner，focused one-off pytest 显式使用 `python -m pytest -n 16 --dist loadfile
    ...`，串行 pytest 仅用于复现或 parallelism-related failure 诊断，且不能把并行失败静默改写成串行 PASS。2026-06-20:
    追加瓶颈定位；16 worker loadfile、16 worker worksteal 和 24 worker loadfile 均约 241～248s，主瓶颈确认为少数
    Campaign/research artifact 集成测试的单测粒度和串行 compute builder，而非 worker 数不足。2026-06-20:
    完成 B2 validation-run scoped compute payload cache 优化；只在 Campaign validation artifact
    builder / focused tests 中复用 targeted、control-window 和 full diagnostic payload，每次仍写入当前
    output directory，默认 CLI/production Campaign runtime 不启用该 cache。focused validation-pack
    call 降至 19.30s，`tests/test_research_campaign.py` 从 127.68s 降至 44.44s，全量并行 full
    tier 2961 passed / 643 warnings / pytest 173.30s（runner elapsed 174.02s），runtime
    artifact 写入 `outputs/validation_runtime/trading-347-bottleneck-after-b2-cache/`。2026-06-20:
    完成 Dynamic v3 validation sample cache 优化；failure attribution focused 52.52s ->
    29.05s，dynamic rescue focused 58.70s -> 20.08s，parameter research focused 56.10s
    -> 40.85s，三组 focused 合并 24 passed / 39.79s；全量并行 full tier 2961 passed / 643 warnings
    / pytest 167.65s（runner elapsed 168.27s），runtime artifact 写入 `outputs/validation_runtime/trading-347-dynamic-v3-cache-full/`。当前剩余瓶颈分散在多个
    25～32s 级独立研究回归单测。保持 no strategy logic change / no cached data mutation / no broker
    / no production。'
  docs_links:
  - docs/requirements/TRADING-347_Full_Test_Runtime_Stabilization.md
  terminal: false
  raw_row_sha256: 1f5b1fd8ec59e6126b90c083ac53f1b8e482cf8ff525df0f9e1d4139c3bca2ac
producer_version: arch_005_shadow_registry_compiler.v1
fragment_checksum: 2e56b7f59131c777fdbb419a1623d98c74d0f604faa0194ac6c788848326d9e3
