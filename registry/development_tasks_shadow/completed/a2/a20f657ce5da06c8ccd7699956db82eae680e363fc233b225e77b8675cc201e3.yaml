schema_version: arch_005_task_shadow_fragment.v1
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
task_record:
  schema_version: task_record.v1
  task_id: TRADING-025
  title: null
  domain: 交易执行/Operator Brief Health Freshness 接入
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: 系统验证
  requirement_refs:
  - docs/requirements/daily_trading_system_operator_brief_2026-05-23.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详细拆解见 `docs/requirements/daily_trading_system_operator_brief_2026-05-23.md#trading-025-health--freshness-接入`；TRADING-022
    已只读消费 TRADING-023 pipeline health summary 和 TRADING-024 data freshness summary，最终收尾验证已完成
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: Operator brief CLI 支持显式传入 pipeline health / data freshness summary JSON；默认自动查找
      latest 023/024 artifact，缺失时对应状态为 `UNKNOWN` 且 brief 降级 `WATCH`；存在时校验 023/024
      safety fields，异常输出 `SAFETY_BLOCKED`；`brief_status`、`summary_level`、trust/manual
      action、alerts、pending manual actions、Markdown 和 dashboard 卡片均消费真实 023/024 状态；所有输出安全
      invariant 保持不变；Dashboard 只读读取 TRADING-022 operator brief artifact 并展示 `pipeline_health.status`、`pipeline_health.health_status`、`data_freshness.status`、`data_freshness.freshness_status`、missing/stale
      pipeline/source counts；收尾验证通过 repo 外 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED
      smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓
      black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  legacy_source:
    path: docs/task_register_completed.md
    source_partition: completed
    line_number: 405
    row_sha256: 88183ad71a90a5e292c6eb3bd9b2beaec3cc450ddbd5293061a5f5f0b8777c04
    cell_count: 8
    all_cells:
    - TRADING-025
    - 交易执行/Operator Brief Health Freshness 接入
    - P0
    - DONE
    - 系统验证
    - 详细拆解见 `docs/requirements/daily_trading_system_operator_brief_2026-05-23.md#trading-025-health--freshness-接入`；TRADING-022
      已只读消费 TRADING-023 pipeline health summary 和 TRADING-024 data freshness summary，最终收尾验证已完成
    - Operator brief CLI 支持显式传入 pipeline health / data freshness summary JSON；默认自动查找
      latest 023/024 artifact，缺失时对应状态为 `UNKNOWN` 且 brief 降级 `WATCH`；存在时校验 023/024
      safety fields，异常输出 `SAFETY_BLOCKED`；`brief_status`、`summary_level`、trust/manual
      action、alerts、pending manual actions、Markdown 和 dashboard 卡片均消费真实 023/024 状态；所有输出安全
      invariant 保持不变；Dashboard 只读读取 TRADING-022 operator brief artifact 并展示 `pipeline_health.status`、`pipeline_health.health_status`、`data_freshness.status`、`data_freshness.freshness_status`、missing/stale
      pipeline/source counts；收尾验证通过 repo 外 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED
      smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓
      black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff
    - '2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-023/024 已生成独立只读 summary artifact，TRADING-022
      仍需把 `pipeline_health` / `data_freshness` 从 legacy optional artifact 读取升级为消费真实
      summary。安全边界：TRADING-025 不运行 021/023/024 或任何 market/backtest/scoring/data download/broker/replay/trading
      pipeline。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 operator brief builder、CLI、Markdown、alert/pending
      action 合并、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED
      repo 外 smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓
      black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-24:
      从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 再次确认 OK 输出真实 Pipeline Health / Data
      Freshness 状态，WATCH 顶部包含 `Watch: Monitoring Recommended` 且缺失 023/024 时为 `UNKNOWN`
      而非 `INPUT_MISSING`，ACTION_REQUIRED / URGENT / SAFETY_BLOCKED Markdown banner
      正确；五路径均确认 operator brief 输出固定 `production_effect=none`、`manual_review_only=true`、`operator_brief_only=true`、`read_only=true`、`apply_executed_by_operator_brief=false`、`rollback_executed_by_operator_brief=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；输入
      023/024 artifact checksum 生成前后不变；dashboard import guard 验证 Daily Trading System
      Operator Brief 卡片只读读取 TRADING-022 artifact，不触发 TRADING-023/024、market/backtest/scoring/data
      download/broker/replay/trading，并展示 health/freshness 状态和 missing/stale counts；收尾验证通过目标
      pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
      仍只被既有无关 `tests/test_market_data.py` baseline 阻断。'
    raw_line: '|TRADING-025|交易执行/Operator Brief Health Freshness 接入|P0|DONE|系统验证|详细拆解见
      `docs/requirements/daily_trading_system_operator_brief_2026-05-23.md#trading-025-health--freshness-接入`；TRADING-022
      已只读消费 TRADING-023 pipeline health summary 和 TRADING-024 data freshness summary，最终收尾验证已完成|Operator
      brief CLI 支持显式传入 pipeline health / data freshness summary JSON；默认自动查找 latest
      023/024 artifact，缺失时对应状态为 `UNKNOWN` 且 brief 降级 `WATCH`；存在时校验 023/024 safety
      fields，异常输出 `SAFETY_BLOCKED`；`brief_status`、`summary_level`、trust/manual action、alerts、pending
      manual actions、Markdown 和 dashboard 卡片均消费真实 023/024 状态；所有输出安全 invariant 保持不变；Dashboard
      只读读取 TRADING-022 operator brief artifact 并展示 `pipeline_health.status`、`pipeline_health.health_status`、`data_freshness.status`、`data_freshness.freshness_status`、missing/stale
      pipeline/source counts；收尾验证通过 repo 外 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED
      smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓
      black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff|2026-05-24:
      新增并进入 IN_PROGRESS，原因：TRADING-023/024 已生成独立只读 summary artifact，TRADING-022 仍需把
      `pipeline_health` / `data_freshness` 从 legacy optional artifact 读取升级为消费真实 summary。安全边界：TRADING-025
      不运行 021/023/024 或任何 market/backtest/scoring/data download/broker/replay/trading
      pipeline。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 operator brief builder、CLI、Markdown、alert/pending
      action 合并、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED
      repo 外 smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓
      black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-24:
      从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 再次确认 OK 输出真实 Pipeline Health / Data
      Freshness 状态，WATCH 顶部包含 `Watch: Monitoring Recommended` 且缺失 023/024 时为 `UNKNOWN`
      而非 `INPUT_MISSING`，ACTION_REQUIRED / URGENT / SAFETY_BLOCKED Markdown banner
      正确；五路径均确认 operator brief 输出固定 `production_effect=none`、`manual_review_only=true`、`operator_brief_only=true`、`read_only=true`、`apply_executed_by_operator_brief=false`、`rollback_executed_by_operator_brief=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；输入
      023/024 artifact checksum 生成前后不变；dashboard import guard 验证 Daily Trading System
      Operator Brief 卡片只读读取 TRADING-022 artifact，不触发 TRADING-023/024、market/backtest/scoring/data
      download/broker/replay/trading，并展示 health/freshness 状态和 missing/stale counts；收尾验证通过目标
      pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
      仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|'
    history_completeness: LEGACY_HISTORY_PARTIAL
    ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-88183ad71a90a5e292c6eb3b
  task_id: TRADING-025
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: arch_005_shadow_importer
  change_id: ARCH-005-S1-SHADOW-IMPORT
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: DONE
  payload:
    priority: P0
    next_owner: 系统验证
    notes: '2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-023/024 已生成独立只读 summary artifact，TRADING-022
      仍需把 `pipeline_health` / `data_freshness` 从 legacy optional artifact 读取升级为消费真实
      summary。安全边界：TRADING-025 不运行 021/023/024 或任何 market/backtest/scoring/data download/broker/replay/trading
      pipeline。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 operator brief builder、CLI、Markdown、alert/pending
      action 合并、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED
      repo 外 smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓
      black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-24:
      从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 再次确认 OK 输出真实 Pipeline Health / Data
      Freshness 状态，WATCH 顶部包含 `Watch: Monitoring Recommended` 且缺失 023/024 时为 `UNKNOWN`
      而非 `INPUT_MISSING`，ACTION_REQUIRED / URGENT / SAFETY_BLOCKED Markdown banner
      正确；五路径均确认 operator brief 输出固定 `production_effect=none`、`manual_review_only=true`、`operator_brief_only=true`、`read_only=true`、`apply_executed_by_operator_brief=false`、`rollback_executed_by_operator_brief=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；输入
      023/024 artifact checksum 生成前后不变；dashboard import guard 验证 Daily Trading System
      Operator Brief 卡片只读读取 TRADING-022 artifact，不触发 TRADING-023/024、market/backtest/scoring/data
      download/broker/replay/trading，并展示 health/freshness 状态和 missing/stale counts；收尾验证通过目标
      pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
      仍只被既有无关 `tests/test_market_data.py` baseline 阻断。'
  rationale: Lossless legacy row import; historical event time is unavailable.
  evidence_refs:
  - sha256:88183ad71a90a5e292c6eb3bd9b2beaec3cc450ddbd5293061a5f5f0b8777c04
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - TRADING-025
  - 交易执行/Operator Brief Health Freshness 接入
  - P0
  - DONE
  - 系统验证
  - 详细拆解见 `docs/requirements/daily_trading_system_operator_brief_2026-05-23.md#trading-025-health--freshness-接入`；TRADING-022
    已只读消费 TRADING-023 pipeline health summary 和 TRADING-024 data freshness summary，最终收尾验证已完成
  - Operator brief CLI 支持显式传入 pipeline health / data freshness summary JSON；默认自动查找
    latest 023/024 artifact，缺失时对应状态为 `UNKNOWN` 且 brief 降级 `WATCH`；存在时校验 023/024 safety
    fields，异常输出 `SAFETY_BLOCKED`；`brief_status`、`summary_level`、trust/manual action、alerts、pending
    manual actions、Markdown 和 dashboard 卡片均消费真实 023/024 状态；所有输出安全 invariant 保持不变；Dashboard
    只读读取 TRADING-022 operator brief artifact 并展示 `pipeline_health.status`、`pipeline_health.health_status`、`data_freshness.status`、`data_freshness.freshness_status`、missing/stale
    pipeline/source counts；收尾验证通过 repo 外 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED
    smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 black
    check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff
  - '2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-023/024 已生成独立只读 summary artifact，TRADING-022
    仍需把 `pipeline_health` / `data_freshness` 从 legacy optional artifact 读取升级为消费真实
    summary。安全边界：TRADING-025 不运行 021/023/024 或任何 market/backtest/scoring/data download/broker/replay/trading
    pipeline。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 operator brief builder、CLI、Markdown、alert/pending
    action 合并、dashboard 只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过 OK/WATCH/ACTION_REQUIRED/URGENT/SAFETY_BLOCKED
    repo 外 smoke、目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓
    black check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。2026-05-24:
    从 VALIDATING 改为 DONE，原因：最终 repo 外 smoke 再次确认 OK 输出真实 Pipeline Health / Data Freshness
    状态，WATCH 顶部包含 `Watch: Monitoring Recommended` 且缺失 023/024 时为 `UNKNOWN` 而非 `INPUT_MISSING`，ACTION_REQUIRED
    / URGENT / SAFETY_BLOCKED Markdown banner 正确；五路径均确认 operator brief 输出固定 `production_effect=none`、`manual_review_only=true`、`operator_brief_only=true`、`read_only=true`、`apply_executed_by_operator_brief=false`、`rollback_executed_by_operator_brief=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；输入
    023/024 artifact checksum 生成前后不变；dashboard import guard 验证 Daily Trading System
    Operator Brief 卡片只读读取 TRADING-022 artifact，不触发 TRADING-023/024、market/backtest/scoring/data
    download/broker/replay/trading，并展示 health/freshness 状态和 missing/stale counts；收尾验证通过目标
    pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
    仍只被既有无关 `tests/test_market_data.py` baseline 阻断。'
  docs_links:
  - docs/requirements/daily_trading_system_operator_brief_2026-05-23.md
  terminal: true
  raw_row_sha256: 88183ad71a90a5e292c6eb3bd9b2beaec3cc450ddbd5293061a5f5f0b8777c04
producer_version: arch_005_shadow_registry_compiler.v1
fragment_checksum: 0abbb3ef669723dd0a19b609f5e26939a8d1de17a6f12788fc44d846f14f1699
