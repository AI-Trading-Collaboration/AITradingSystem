schema_version: arch_005_task_shadow_fragment.v1
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
task_record:
  schema_version: task_record.v1
  task_id: TRADING-028
  title: null
  domain: 交易执行/Scheduler Configuration Template Generator
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: 系统验证
  requirement_refs:
  - docs/requirements/daily_operator_brief_scheduler_templates_2026-05-24.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详细拆解见 `docs/requirements/daily_operator_brief_scheduler_templates_2026-05-24.md`；template-only
    scheduler 配置模板生成器、CLI、metadata JSON、summary Markdown、Windows/PowerShell/batch/cron/GitHub
    Actions `.template` 输出、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和最终收尾验证已完成；本任务不安装或启用任何
    scheduler，不运行 TRADING-021/022/023/024/026 或任何 market/backtest/scoring/data download/broker/replay/trading
    pipeline；后续可在 TRADING-029 增加模板语法验证，仍不得安装 scheduler
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: 新增 `scripts/generate_daily_operator_brief_scheduler_templates.py` 和核心 builder；所有模板输出位于
      `data/derived/operator_briefs/scheduler_templates/` 且以 `.template` 结尾；metadata
      固定 `production_effect=none`、`manual_review_only=true`、`scheduler_template_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`operator_brief_executed_by_template_generator=false`、`pipelines_executed_by_template_generator=false`、`data_downloaded_by_template_generator=false`、`apply_executed_by_template_generator=false`、`rollback_executed_by_template_generator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；safety
      scan 阻断危险输出路径和危险执行命令；dashboard 只读读取 TRADING-028 metadata artifact；最终收尾验证确认 GENERATED
      smoke、SAFETY_BLOCKED smoke、template content checks、15 项 metadata safety invariant、全部模板文件
      `.template` 后缀、无真实 Windows Task Scheduler XML 安装文件、无 crontab 文件、无 `.github/workflows/*.yml`、无
      scheduler 自动安装脚本；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest
      和 ruff 通过；全仓 Black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化
      diff
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  legacy_source:
    path: docs/task_register_completed.md
    source_partition: completed
    line_number: 398
    row_sha256: 014f5c4ff6f967640ef2257728d075803d50a65f9c4182d60292b7dfb7abd145
    cell_count: 8
    all_cells:
    - TRADING-028
    - 交易执行/Scheduler Configuration Template Generator
    - P0
    - DONE
    - 系统验证
    - 详细拆解见 `docs/requirements/daily_operator_brief_scheduler_templates_2026-05-24.md`；template-only
      scheduler 配置模板生成器、CLI、metadata JSON、summary Markdown、Windows/PowerShell/batch/cron/GitHub
      Actions `.template` 输出、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和最终收尾验证已完成；本任务不安装或启用任何
      scheduler，不运行 TRADING-021/022/023/024/026 或任何 market/backtest/scoring/data download/broker/replay/trading
      pipeline；后续可在 TRADING-029 增加模板语法验证，仍不得安装 scheduler
    - 新增 `scripts/generate_daily_operator_brief_scheduler_templates.py` 和核心 builder；所有模板输出位于
      `data/derived/operator_briefs/scheduler_templates/` 且以 `.template` 结尾；metadata
      固定 `production_effect=none`、`manual_review_only=true`、`scheduler_template_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`operator_brief_executed_by_template_generator=false`、`pipelines_executed_by_template_generator=false`、`data_downloaded_by_template_generator=false`、`apply_executed_by_template_generator=false`、`rollback_executed_by_template_generator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；safety
      scan 阻断危险输出路径和危险执行命令；dashboard 只读读取 TRADING-028 metadata artifact；最终收尾验证确认 GENERATED
      smoke、SAFETY_BLOCKED smoke、template content checks、15 项 metadata safety invariant、全部模板文件
      `.template` 后缀、无真实 Windows Task Scheduler XML 安装文件、无 crontab 文件、无 `.github/workflows/*.yml`、无
      scheduler 自动安装脚本；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest
      和 ruff 通过；全仓 Black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化
      diff
    - '2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-027 已提供人工安装指南，但 Windows Task Scheduler、cron
      和 GitHub Actions 配置仍需人工拼接，容易产生路径或命令错误；本任务只生成可审阅模板和 metadata，不创建、不安装、不启用 scheduler。2026-05-24:
      从 IN_PROGRESS 改为 VALIDATING，原因：已完成核心 builder、CLI、模板输出、metadata/summary、dashboard
      只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量
      pytest、ruff、repo 外 GENERATED / SAFETY_BLOCKED / template content smoke；全仓 Black
      check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-24: 从 VALIDATING
      改为 DONE，原因：最终 repo 外 smoke 确认默认生成 `GENERATED` 且 5 个模板存在，危险 `.github/workflows`
      输出为 `SAFETY_BLOCKED` 且不创建 unsafe 目录，模板内容全部包含 `TEMPLATE ONLY` 和 `Manual review
      required`，不包含 apply/rollback/schtasks/crontab 安装命令，active command 不包含 broker/replay/trading
      execution；metadata 15 项安全边界全部为预期 false/true；确认无真实 Windows Task Scheduler XML
      安装文件、crontab 文件、`.github/workflows/*.yml` 或 scheduler 自动安装脚本；dashboard import
      guard 验证 Scheduler Configuration Templates 卡片只读读取 TRADING-028 metadata artifact，不触发
      018B-027、TRADING-028 script、operator brief、scheduler creation、market/backtest/scoring/data
      download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量
      pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。'
    raw_line: '|TRADING-028|交易执行/Scheduler Configuration Template Generator|P0|DONE|系统验证|详细拆解见
      `docs/requirements/daily_operator_brief_scheduler_templates_2026-05-24.md`；template-only
      scheduler 配置模板生成器、CLI、metadata JSON、summary Markdown、Windows/PowerShell/batch/cron/GitHub
      Actions `.template` 输出、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和最终收尾验证已完成；本任务不安装或启用任何
      scheduler，不运行 TRADING-021/022/023/024/026 或任何 market/backtest/scoring/data download/broker/replay/trading
      pipeline；后续可在 TRADING-029 增加模板语法验证，仍不得安装 scheduler|新增 `scripts/generate_daily_operator_brief_scheduler_templates.py`
      和核心 builder；所有模板输出位于 `data/derived/operator_briefs/scheduler_templates/` 且以
      `.template` 结尾；metadata 固定 `production_effect=none`、`manual_review_only=true`、`scheduler_template_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`operator_brief_executed_by_template_generator=false`、`pipelines_executed_by_template_generator=false`、`data_downloaded_by_template_generator=false`、`apply_executed_by_template_generator=false`、`rollback_executed_by_template_generator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；safety
      scan 阻断危险输出路径和危险执行命令；dashboard 只读读取 TRADING-028 metadata artifact；最终收尾验证确认 GENERATED
      smoke、SAFETY_BLOCKED smoke、template content checks、15 项 metadata safety invariant、全部模板文件
      `.template` 后缀、无真实 Windows Task Scheduler XML 安装文件、无 crontab 文件、无 `.github/workflows/*.yml`、无
      scheduler 自动安装脚本；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest
      和 ruff 通过；全仓 Black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化
      diff|2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-027 已提供人工安装指南，但 Windows Task Scheduler、cron
      和 GitHub Actions 配置仍需人工拼接，容易产生路径或命令错误；本任务只生成可审阅模板和 metadata，不创建、不安装、不启用 scheduler。2026-05-24:
      从 IN_PROGRESS 改为 VALIDATING，原因：已完成核心 builder、CLI、模板输出、metadata/summary、dashboard
      只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量
      pytest、ruff、repo 外 GENERATED / SAFETY_BLOCKED / template content smoke；全仓 Black
      check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-24: 从 VALIDATING
      改为 DONE，原因：最终 repo 外 smoke 确认默认生成 `GENERATED` 且 5 个模板存在，危险 `.github/workflows`
      输出为 `SAFETY_BLOCKED` 且不创建 unsafe 目录，模板内容全部包含 `TEMPLATE ONLY` 和 `Manual review
      required`，不包含 apply/rollback/schtasks/crontab 安装命令，active command 不包含 broker/replay/trading
      execution；metadata 15 项安全边界全部为预期 false/true；确认无真实 Windows Task Scheduler XML
      安装文件、crontab 文件、`.github/workflows/*.yml` 或 scheduler 自动安装脚本；dashboard import
      guard 验证 Scheduler Configuration Templates 卡片只读读取 TRADING-028 metadata artifact，不触发
      018B-027、TRADING-028 script、operator brief、scheduler creation、market/backtest/scoring/data
      download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量
      pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。|'
    history_completeness: LEGACY_HISTORY_PARTIAL
    ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-014f5c4ff6f967640ef22577
  task_id: TRADING-028
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: arch_005_shadow_importer
  change_id: ARCH-005-S1-SHADOW-IMPORT
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: DONE
  payload:
    priority: P0
    next_owner: 系统验证
    notes: '2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-027 已提供人工安装指南，但 Windows Task
      Scheduler、cron 和 GitHub Actions 配置仍需人工拼接，容易产生路径或命令错误；本任务只生成可审阅模板和 metadata，不创建、不安装、不启用
      scheduler。2026-05-24: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成核心 builder、CLI、模板输出、metadata/summary、dashboard
      只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量
      pytest、ruff、repo 外 GENERATED / SAFETY_BLOCKED / template content smoke；全仓 Black
      check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-24: 从 VALIDATING
      改为 DONE，原因：最终 repo 外 smoke 确认默认生成 `GENERATED` 且 5 个模板存在，危险 `.github/workflows`
      输出为 `SAFETY_BLOCKED` 且不创建 unsafe 目录，模板内容全部包含 `TEMPLATE ONLY` 和 `Manual review
      required`，不包含 apply/rollback/schtasks/crontab 安装命令，active command 不包含 broker/replay/trading
      execution；metadata 15 项安全边界全部为预期 false/true；确认无真实 Windows Task Scheduler XML
      安装文件、crontab 文件、`.github/workflows/*.yml` 或 scheduler 自动安装脚本；dashboard import
      guard 验证 Scheduler Configuration Templates 卡片只读读取 TRADING-028 metadata artifact，不触发
      018B-027、TRADING-028 script、operator brief、scheduler creation、market/backtest/scoring/data
      download/broker/replay/trading；收尾验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量
      pytest 和 ruff；全仓 Black check 仍只被既有无关 `tests/test_market_data.py` baseline 阻断。'
  rationale: Lossless legacy row import; historical event time is unavailable.
  evidence_refs:
  - sha256:014f5c4ff6f967640ef2257728d075803d50a65f9c4182d60292b7dfb7abd145
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - TRADING-028
  - 交易执行/Scheduler Configuration Template Generator
  - P0
  - DONE
  - 系统验证
  - 详细拆解见 `docs/requirements/daily_operator_brief_scheduler_templates_2026-05-24.md`；template-only
    scheduler 配置模板生成器、CLI、metadata JSON、summary Markdown、Windows/PowerShell/batch/cron/GitHub
    Actions `.template` 输出、dashboard 只读卡片、runbook、system flow、artifact catalog、专项测试和最终收尾验证已完成；本任务不安装或启用任何
    scheduler，不运行 TRADING-021/022/023/024/026 或任何 market/backtest/scoring/data download/broker/replay/trading
    pipeline；后续可在 TRADING-029 增加模板语法验证，仍不得安装 scheduler
  - 新增 `scripts/generate_daily_operator_brief_scheduler_templates.py` 和核心 builder；所有模板输出位于
    `data/derived/operator_briefs/scheduler_templates/` 且以 `.template` 结尾；metadata
    固定 `production_effect=none`、`manual_review_only=true`、`scheduler_template_only=true`、`read_only=true`、`scheduler_created=false`、`scheduler_installed=false`、`scheduler_enabled=false`、`operator_brief_executed_by_template_generator=false`、`pipelines_executed_by_template_generator=false`、`data_downloaded_by_template_generator=false`、`apply_executed_by_template_generator=false`、`rollback_executed_by_template_generator=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；safety
    scan 阻断危险输出路径和危险执行命令；dashboard 只读读取 TRADING-028 metadata artifact；最终收尾验证确认 GENERATED
    smoke、SAFETY_BLOCKED smoke、template content checks、15 项 metadata safety invariant、全部模板文件
    `.template` 后缀、无真实 Windows Task Scheduler XML 安装文件、无 crontab 文件、无 `.github/workflows/*.yml`、无
    scheduler 自动安装脚本；目标 pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和
    ruff 通过；全仓 Black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化
    diff
  - '2026-05-24: 新增并进入 IN_PROGRESS，原因：TRADING-027 已提供人工安装指南，但 Windows Task Scheduler、cron
    和 GitHub Actions 配置仍需人工拼接，容易产生路径或命令错误；本任务只生成可审阅模板和 metadata，不创建、不安装、不启用 scheduler。2026-05-24:
    从 IN_PROGRESS 改为 VALIDATING，原因：已完成核心 builder、CLI、模板输出、metadata/summary、dashboard
    只读卡片、runbook、system flow、artifact catalog 和专项测试；验证通过目标 pytest、dashboard pytest、`tests/trading_engine`、全量
    pytest、ruff、repo 外 GENERATED / SAFETY_BLOCKED / template content smoke；全仓 Black
    check 仍仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-24: 从 VALIDATING
    改为 DONE，原因：最终 repo 外 smoke 确认默认生成 `GENERATED` 且 5 个模板存在，危险 `.github/workflows`
    输出为 `SAFETY_BLOCKED` 且不创建 unsafe 目录，模板内容全部包含 `TEMPLATE ONLY` 和 `Manual review
    required`，不包含 apply/rollback/schtasks/crontab 安装命令，active command 不包含 broker/replay/trading
    execution；metadata 15 项安全边界全部为预期 false/true；确认无真实 Windows Task Scheduler XML 安装文件、crontab
    文件、`.github/workflows/*.yml` 或 scheduler 自动安装脚本；dashboard import guard 验证 Scheduler
    Configuration Templates 卡片只读读取 TRADING-028 metadata artifact，不触发 018B-027、TRADING-028
    script、operator brief、scheduler creation、market/backtest/scoring/data download/broker/replay/trading；收尾验证通过目标
    pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
    仍只被既有无关 `tests/test_market_data.py` baseline 阻断。'
  docs_links:
  - docs/requirements/daily_operator_brief_scheduler_templates_2026-05-24.md
  terminal: true
  raw_row_sha256: 014f5c4ff6f967640ef2257728d075803d50a65f9c4182d60292b7dfb7abd145
producer_version: arch_005_shadow_registry_compiler.v1
fragment_checksum: 1edfe39fb8ff3416a84ce5ea74d01408c321f6387c62df8db28b11cca2419801
