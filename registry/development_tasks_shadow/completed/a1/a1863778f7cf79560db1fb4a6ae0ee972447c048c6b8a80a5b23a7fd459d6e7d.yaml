schema_version: arch_005_task_shadow_fragment.v1
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
task_record:
  schema_version: task_record.v1
  task_id: TRADING-146
  title: null
  domain: ETF portfolio research/Historical Replay PIT Safety Hardening
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: 系统验证
  requirement_refs:
  - docs/requirements/TRADING-146_Historical_Replay_PIT_Safety_Hardening.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详细拆解见 `docs/requirements/TRADING-146_Historical_Replay_PIT_Safety_Hardening.md`；复验
    TRADING-141_to_145 latest artifacts 时发现 inventory 将 `ADVISORY_GENERATED_AFTER_AS_OF_DATE`
    和 `MISSING_PRICE_DATA` 归为 `PIT_WARNING`，且 latest replay 通过显式 `--include-pit-warning`
    纳入了 2 个全为 warning 的事件；本任务收紧 PIT safety 分类和 validation，避免生成时间晚于 replay `as_of`
    或缺少 outcome 价格覆盖的事件进入 historical replay；系统验证已完成，hard PIT limitations 不再能通过 warning
    opt-in 进入 historical replay
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: '`ADVISORY_GENERATED_AFTER_AS_OF_DATE` 和 `MISSING_PRICE_DATA` 默认归入 `PIT_UNSAFE`
      / `INELIGIBLE`，`validate-replay-inventory` 校验这些 hard limitations 不得出现在 `PIT_WARNING`
      或 eligible rows；`historical-replay run --include-pit-warning` 仍不能纳入 `PIT_UNSAFE`；reports
      明确披露 hard limitation count、PIT_UNSAFE exclusion 和 `include_pit_warning` 语义；focused
      tests、TRADING-141_to_145 validations、ruff、compileall、system flow、artifact catalog、README
      和 requirements 更新完成'
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  legacy_source:
    path: docs/task_register_completed.md
    source_partition: completed
    line_number: 333
    row_sha256: 4c0aab641a3c28659633ac850d517fbe1fd6ecf5b356ef42119ac726bd458a7a
    cell_count: 8
    all_cells:
    - TRADING-146
    - ETF portfolio research/Historical Replay PIT Safety Hardening
    - P0
    - DONE
    - 系统验证
    - 详细拆解见 `docs/requirements/TRADING-146_Historical_Replay_PIT_Safety_Hardening.md`；复验
      TRADING-141_to_145 latest artifacts 时发现 inventory 将 `ADVISORY_GENERATED_AFTER_AS_OF_DATE`
      和 `MISSING_PRICE_DATA` 归为 `PIT_WARNING`，且 latest replay 通过显式 `--include-pit-warning`
      纳入了 2 个全为 warning 的事件；本任务收紧 PIT safety 分类和 validation，避免生成时间晚于 replay `as_of`
      或缺少 outcome 价格覆盖的事件进入 historical replay；系统验证已完成，hard PIT limitations 不再能通过 warning
      opt-in 进入 historical replay
    - '`ADVISORY_GENERATED_AFTER_AS_OF_DATE` 和 `MISSING_PRICE_DATA` 默认归入 `PIT_UNSAFE`
      / `INELIGIBLE`，`validate-replay-inventory` 校验这些 hard limitations 不得出现在 `PIT_WARNING`
      或 eligible rows；`historical-replay run --include-pit-warning` 仍不能纳入 `PIT_UNSAFE`；reports
      明确披露 hard limitation count、PIT_UNSAFE exclusion 和 `include_pit_warning` 语义；focused
      tests、TRADING-141_to_145 validations、ruff、compileall、system flow、artifact catalog、README
      和 requirements 更新完成'
    - '2026-06-09: 新增并进入 IN_PROGRESS，原因：当前 latest replay inventory 有 2 个 `PIT_WARNING`
      事件，top limitations 包含 `ADVISORY_GENERATED_AFTER_AS_OF_DATE` 与 `MISSING_PRICE_DATA`。按
      TRADING-141_to_145 requirement，不能证明 decision input 在 replay `as_of` 已存在或缺少价格覆盖时不应进入
      default/opt-in historical replay；本任务不改变 production、target weights、portfolio
      或 broker，只收紧审计语义。2026-06-09: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 hard PIT limitation
      分类、inventory consistency validation、report disclosure 和 empty-replay paper sim
      validation；重建 latest artifacts 为 replay inventory `358d353576f86a47`（2 个 `PIT_UNSAFE`）、historical
      replay `86b56e67308cdc50`（0 events / 2 skipped）、backfill outcome `38f3b24513fc8229`（`INSUFFICIENT_DATA`）、historical
      paper sim `278aaea770c9d106`（`INSUFFICIENT_DATA`）、replay performance review
      `f5a0d6edae45994f`（`continue_forward_tracking`）；focused pytest、五个 artifact validate、ruff、compileall
      和 documentation tests 均通过。2026-06-09: 从 VALIDATING 改为 DONE，原因：复跑 `tests/test_dynamic_v3_historical_replay.py`
      为 2 passed，五个 latest artifact validate 均 PASS，`validate-replay-performance-review`
      仍披露 `production_effect=none`，各 validate 均保持 `broker_action_taken=false`；相关 ruff、compileall、documentation
      contract 和 docs freshness 通过。'
    raw_line: '|TRADING-146|ETF portfolio research/Historical Replay PIT Safety Hardening|P0|DONE|系统验证|详细拆解见
      `docs/requirements/TRADING-146_Historical_Replay_PIT_Safety_Hardening.md`；复验
      TRADING-141_to_145 latest artifacts 时发现 inventory 将 `ADVISORY_GENERATED_AFTER_AS_OF_DATE`
      和 `MISSING_PRICE_DATA` 归为 `PIT_WARNING`，且 latest replay 通过显式 `--include-pit-warning`
      纳入了 2 个全为 warning 的事件；本任务收紧 PIT safety 分类和 validation，避免生成时间晚于 replay `as_of`
      或缺少 outcome 价格覆盖的事件进入 historical replay；系统验证已完成，hard PIT limitations 不再能通过 warning
      opt-in 进入 historical replay|`ADVISORY_GENERATED_AFTER_AS_OF_DATE` 和 `MISSING_PRICE_DATA`
      默认归入 `PIT_UNSAFE` / `INELIGIBLE`，`validate-replay-inventory` 校验这些 hard limitations
      不得出现在 `PIT_WARNING` 或 eligible rows；`historical-replay run --include-pit-warning`
      仍不能纳入 `PIT_UNSAFE`；reports 明确披露 hard limitation count、PIT_UNSAFE exclusion 和
      `include_pit_warning` 语义；focused tests、TRADING-141_to_145 validations、ruff、compileall、system
      flow、artifact catalog、README 和 requirements 更新完成|2026-06-09: 新增并进入 IN_PROGRESS，原因：当前
      latest replay inventory 有 2 个 `PIT_WARNING` 事件，top limitations 包含 `ADVISORY_GENERATED_AFTER_AS_OF_DATE`
      与 `MISSING_PRICE_DATA`。按 TRADING-141_to_145 requirement，不能证明 decision input
      在 replay `as_of` 已存在或缺少价格覆盖时不应进入 default/opt-in historical replay；本任务不改变 production、target
      weights、portfolio 或 broker，只收紧审计语义。2026-06-09: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现
      hard PIT limitation 分类、inventory consistency validation、report disclosure 和
      empty-replay paper sim validation；重建 latest artifacts 为 replay inventory `358d353576f86a47`（2
      个 `PIT_UNSAFE`）、historical replay `86b56e67308cdc50`（0 events / 2 skipped）、backfill
      outcome `38f3b24513fc8229`（`INSUFFICIENT_DATA`）、historical paper sim `278aaea770c9d106`（`INSUFFICIENT_DATA`）、replay
      performance review `f5a0d6edae45994f`（`continue_forward_tracking`）；focused pytest、五个
      artifact validate、ruff、compileall 和 documentation tests 均通过。2026-06-09: 从 VALIDATING
      改为 DONE，原因：复跑 `tests/test_dynamic_v3_historical_replay.py` 为 2 passed，五个 latest
      artifact validate 均 PASS，`validate-replay-performance-review` 仍披露 `production_effect=none`，各
      validate 均保持 `broker_action_taken=false`；相关 ruff、compileall、documentation contract
      和 docs freshness 通过。|'
    history_completeness: LEGACY_HISTORY_PARTIAL
    ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-4c0aab641a3c28659633ac85
  task_id: TRADING-146
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: arch_005_shadow_importer
  change_id: ARCH-005-S1-SHADOW-IMPORT
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: DONE
  payload:
    priority: P0
    next_owner: 系统验证
    notes: '2026-06-09: 新增并进入 IN_PROGRESS，原因：当前 latest replay inventory 有 2 个 `PIT_WARNING`
      事件，top limitations 包含 `ADVISORY_GENERATED_AFTER_AS_OF_DATE` 与 `MISSING_PRICE_DATA`。按
      TRADING-141_to_145 requirement，不能证明 decision input 在 replay `as_of` 已存在或缺少价格覆盖时不应进入
      default/opt-in historical replay；本任务不改变 production、target weights、portfolio
      或 broker，只收紧审计语义。2026-06-09: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 hard PIT limitation
      分类、inventory consistency validation、report disclosure 和 empty-replay paper sim
      validation；重建 latest artifacts 为 replay inventory `358d353576f86a47`（2 个 `PIT_UNSAFE`）、historical
      replay `86b56e67308cdc50`（0 events / 2 skipped）、backfill outcome `38f3b24513fc8229`（`INSUFFICIENT_DATA`）、historical
      paper sim `278aaea770c9d106`（`INSUFFICIENT_DATA`）、replay performance review
      `f5a0d6edae45994f`（`continue_forward_tracking`）；focused pytest、五个 artifact validate、ruff、compileall
      和 documentation tests 均通过。2026-06-09: 从 VALIDATING 改为 DONE，原因：复跑 `tests/test_dynamic_v3_historical_replay.py`
      为 2 passed，五个 latest artifact validate 均 PASS，`validate-replay-performance-review`
      仍披露 `production_effect=none`，各 validate 均保持 `broker_action_taken=false`；相关 ruff、compileall、documentation
      contract 和 docs freshness 通过。'
  rationale: Lossless legacy row import; historical event time is unavailable.
  evidence_refs:
  - sha256:4c0aab641a3c28659633ac850d517fbe1fd6ecf5b356ef42119ac726bd458a7a
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - TRADING-146
  - ETF portfolio research/Historical Replay PIT Safety Hardening
  - P0
  - DONE
  - 系统验证
  - 详细拆解见 `docs/requirements/TRADING-146_Historical_Replay_PIT_Safety_Hardening.md`；复验
    TRADING-141_to_145 latest artifacts 时发现 inventory 将 `ADVISORY_GENERATED_AFTER_AS_OF_DATE`
    和 `MISSING_PRICE_DATA` 归为 `PIT_WARNING`，且 latest replay 通过显式 `--include-pit-warning`
    纳入了 2 个全为 warning 的事件；本任务收紧 PIT safety 分类和 validation，避免生成时间晚于 replay `as_of`
    或缺少 outcome 价格覆盖的事件进入 historical replay；系统验证已完成，hard PIT limitations 不再能通过 warning
    opt-in 进入 historical replay
  - '`ADVISORY_GENERATED_AFTER_AS_OF_DATE` 和 `MISSING_PRICE_DATA` 默认归入 `PIT_UNSAFE`
    / `INELIGIBLE`，`validate-replay-inventory` 校验这些 hard limitations 不得出现在 `PIT_WARNING`
    或 eligible rows；`historical-replay run --include-pit-warning` 仍不能纳入 `PIT_UNSAFE`；reports
    明确披露 hard limitation count、PIT_UNSAFE exclusion 和 `include_pit_warning` 语义；focused
    tests、TRADING-141_to_145 validations、ruff、compileall、system flow、artifact catalog、README
    和 requirements 更新完成'
  - '2026-06-09: 新增并进入 IN_PROGRESS，原因：当前 latest replay inventory 有 2 个 `PIT_WARNING`
    事件，top limitations 包含 `ADVISORY_GENERATED_AFTER_AS_OF_DATE` 与 `MISSING_PRICE_DATA`。按
    TRADING-141_to_145 requirement，不能证明 decision input 在 replay `as_of` 已存在或缺少价格覆盖时不应进入
    default/opt-in historical replay；本任务不改变 production、target weights、portfolio 或
    broker，只收紧审计语义。2026-06-09: 从 IN_PROGRESS 改为 VALIDATING，原因：已实现 hard PIT limitation
    分类、inventory consistency validation、report disclosure 和 empty-replay paper sim
    validation；重建 latest artifacts 为 replay inventory `358d353576f86a47`（2 个 `PIT_UNSAFE`）、historical
    replay `86b56e67308cdc50`（0 events / 2 skipped）、backfill outcome `38f3b24513fc8229`（`INSUFFICIENT_DATA`）、historical
    paper sim `278aaea770c9d106`（`INSUFFICIENT_DATA`）、replay performance review `f5a0d6edae45994f`（`continue_forward_tracking`）；focused
    pytest、五个 artifact validate、ruff、compileall 和 documentation tests 均通过。2026-06-09:
    从 VALIDATING 改为 DONE，原因：复跑 `tests/test_dynamic_v3_historical_replay.py` 为 2 passed，五个
    latest artifact validate 均 PASS，`validate-replay-performance-review` 仍披露 `production_effect=none`，各
    validate 均保持 `broker_action_taken=false`；相关 ruff、compileall、documentation contract
    和 docs freshness 通过。'
  docs_links:
  - docs/requirements/TRADING-146_Historical_Replay_PIT_Safety_Hardening.md
  terminal: true
  raw_row_sha256: 4c0aab641a3c28659633ac850d517fbe1fd6ecf5b356ef42119ac726bd458a7a
producer_version: arch_005_shadow_registry_compiler.v1
fragment_checksum: 8c1cd82a41b9ae6c9681a4f3175945ebf3eeabde733e265d03f10a6b1574fcc9
