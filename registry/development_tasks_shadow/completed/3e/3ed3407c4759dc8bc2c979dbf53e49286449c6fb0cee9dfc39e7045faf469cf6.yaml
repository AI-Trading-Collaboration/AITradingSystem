schema_version: arch_005_task_shadow_fragment.v1
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
task_record:
  schema_version: task_record.v1
  task_id: TRADING-019
  title: null
  domain: 交易执行/Parameter Governance Dashboard
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: 系统验证
  requirement_refs:
  - docs/requirements/parameter_governance_summary_2026-05-23.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详细拆解见 `docs/requirements/parameter_governance_summary_2026-05-23.md`；只读
    summary builder、CLI、JSON/Markdown/run log、dashboard 总览卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；repo
    既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: 新增 `scripts/run_parameter_governance_summary.py`、`src/ai_trading_system/trading_engine/parameter_governance_summary.py`
      和 `data/derived/weight_iterations/governance/parameter_governance_summary_YYYY-MM-DD.json/md`、`governance/logs/parameter_governance_summary_run_YYYY-MM-DD.json/md`；只读读取
      production profile、current shadow weights、latest 018C2 review、018D proposal、018E1
      preflight、018E2 apply result、018E3 rollback result 和 018F lifecycle audit；输出
      governance_state、action_required/action_level、pending_items、promotion status、production/shadow
      weights、safety boundary audit 和 findings；所有输出固定 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；dashboard
      只能读取 summary artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019、scoring、broker、replay
      或 trading execution
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  legacy_source:
    path: docs/task_register_completed.md
    source_partition: completed
    line_number: 407
    row_sha256: 4c8100416356d0f87817bc05bf743c597107f97be535b8460cdf2712789b5d1f
    cell_count: 8
    all_cells:
    - TRADING-019
    - 交易执行/Parameter Governance Dashboard
    - P0
    - DONE
    - 系统验证
    - 详细拆解见 `docs/requirements/parameter_governance_summary_2026-05-23.md`；只读 summary
      builder、CLI、JSON/Markdown/run log、dashboard 总览卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；repo
      既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务
    - 新增 `scripts/run_parameter_governance_summary.py`、`src/ai_trading_system/trading_engine/parameter_governance_summary.py`
      和 `data/derived/weight_iterations/governance/parameter_governance_summary_YYYY-MM-DD.json/md`、`governance/logs/parameter_governance_summary_run_YYYY-MM-DD.json/md`；只读读取
      production profile、current shadow weights、latest 018C2 review、018D proposal、018E1
      preflight、018E2 apply result、018E3 rollback result 和 018F lifecycle audit；输出
      governance_state、action_required/action_level、pending_items、promotion status、production/shadow
      weights、safety boundary audit 和 findings；所有输出固定 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；dashboard
      只能读取 summary artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019、scoring、broker、replay
      或 trading execution
    - '2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 shadow weight / promotion governance
      闭环后新增统一只读治理视图，用于聚合 production/shadow、proposal/preflight/apply/rollback/lifecycle
      状态和人工行动需求；本任务不允许修改 production/shadow、不执行 promotion/apply/rollback、不重跑任何上游 pipeline。2026-05-23:
      从 IN_PROGRESS 改为 VALIDATING，原因：已完成只读 summary builder、CLI、JSON/Markdown/run log、dashboard
      只读卡片、runbook、system flow / artifact catalog 更新和测试；repo 外临时 fixture smoke 成功验证
      `ROLLBACK_COMPLETED`、`PREFLIGHT_READY` 和 `SAFETY_ANOMALY`；目标 pytest、dashboard
      pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 Black check 仍只被既有无关 `tests/test_market_data.py`
      baseline 阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture
      三路径 smoke；`ROLLBACK_COMPLETED` 输出 `action_required=false`、`safety_boundary_audit.status=PASS`，`PREFLIGHT_READY`
      输出 `pending_apply=true`、`action_level=APPROVAL_REQUIRED`，`SAFETY_ANOMALY` 输出
      `action_required=true`、`action_level=URGENT` 且 critical findings 非空；三路径均确认 summary
      顶层 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard
      import guard 验证 Parameter Governance Summary 卡片只读读取 governance summary artifact，不触发
      018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/scoring/broker/replay/trading；收尾验证通过目标
      pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
      仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。'
    raw_line: '|TRADING-019|交易执行/Parameter Governance Dashboard|P0|DONE|系统验证|详细拆解见
      `docs/requirements/parameter_governance_summary_2026-05-23.md`；只读 summary builder、CLI、JSON/Markdown/run
      log、dashboard 总览卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py`
      Black baseline 仍需后续单独处理，不混入本任务|新增 `scripts/run_parameter_governance_summary.py`、`src/ai_trading_system/trading_engine/parameter_governance_summary.py`
      和 `data/derived/weight_iterations/governance/parameter_governance_summary_YYYY-MM-DD.json/md`、`governance/logs/parameter_governance_summary_run_YYYY-MM-DD.json/md`；只读读取
      production profile、current shadow weights、latest 018C2 review、018D proposal、018E1
      preflight、018E2 apply result、018E3 rollback result 和 018F lifecycle audit；输出
      governance_state、action_required/action_level、pending_items、promotion status、production/shadow
      weights、safety boundary audit 和 findings；所有输出固定 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；dashboard
      只能读取 summary artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019、scoring、broker、replay
      或 trading execution|2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 shadow weight
      / promotion governance 闭环后新增统一只读治理视图，用于聚合 production/shadow、proposal/preflight/apply/rollback/lifecycle
      状态和人工行动需求；本任务不允许修改 production/shadow、不执行 promotion/apply/rollback、不重跑任何上游 pipeline。2026-05-23:
      从 IN_PROGRESS 改为 VALIDATING，原因：已完成只读 summary builder、CLI、JSON/Markdown/run log、dashboard
      只读卡片、runbook、system flow / artifact catalog 更新和测试；repo 外临时 fixture smoke 成功验证
      `ROLLBACK_COMPLETED`、`PREFLIGHT_READY` 和 `SAFETY_ANOMALY`；目标 pytest、dashboard
      pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 Black check 仍只被既有无关 `tests/test_market_data.py`
      baseline 阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture
      三路径 smoke；`ROLLBACK_COMPLETED` 输出 `action_required=false`、`safety_boundary_audit.status=PASS`，`PREFLIGHT_READY`
      输出 `pending_apply=true`、`action_level=APPROVAL_REQUIRED`，`SAFETY_ANOMALY` 输出
      `action_required=true`、`action_level=URGENT` 且 critical findings 非空；三路径均确认 summary
      顶层 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard
      import guard 验证 Parameter Governance Summary 卡片只读读取 governance summary artifact，不触发
      018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/scoring/broker/replay/trading；收尾验证通过目标
      pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
      仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|'
    history_completeness: LEGACY_HISTORY_PARTIAL
    ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-4c8100416356d0f87817bc05
  task_id: TRADING-019
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: arch_005_shadow_importer
  change_id: ARCH-005-S1-SHADOW-IMPORT
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: DONE
  payload:
    priority: P0
    next_owner: 系统验证
    notes: '2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 shadow weight / promotion governance
      闭环后新增统一只读治理视图，用于聚合 production/shadow、proposal/preflight/apply/rollback/lifecycle
      状态和人工行动需求；本任务不允许修改 production/shadow、不执行 promotion/apply/rollback、不重跑任何上游 pipeline。2026-05-23:
      从 IN_PROGRESS 改为 VALIDATING，原因：已完成只读 summary builder、CLI、JSON/Markdown/run log、dashboard
      只读卡片、runbook、system flow / artifact catalog 更新和测试；repo 外临时 fixture smoke 成功验证
      `ROLLBACK_COMPLETED`、`PREFLIGHT_READY` 和 `SAFETY_ANOMALY`；目标 pytest、dashboard
      pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 Black check 仍只被既有无关 `tests/test_market_data.py`
      baseline 阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture
      三路径 smoke；`ROLLBACK_COMPLETED` 输出 `action_required=false`、`safety_boundary_audit.status=PASS`，`PREFLIGHT_READY`
      输出 `pending_apply=true`、`action_level=APPROVAL_REQUIRED`，`SAFETY_ANOMALY` 输出
      `action_required=true`、`action_level=URGENT` 且 critical findings 非空；三路径均确认 summary
      顶层 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard
      import guard 验证 Parameter Governance Summary 卡片只读读取 governance summary artifact，不触发
      018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/scoring/broker/replay/trading；收尾验证通过目标
      pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
      仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。'
  rationale: Lossless legacy row import; historical event time is unavailable.
  evidence_refs:
  - sha256:4c8100416356d0f87817bc05bf743c597107f97be535b8460cdf2712789b5d1f
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - TRADING-019
  - 交易执行/Parameter Governance Dashboard
  - P0
  - DONE
  - 系统验证
  - 详细拆解见 `docs/requirements/parameter_governance_summary_2026-05-23.md`；只读 summary
    builder、CLI、JSON/Markdown/run log、dashboard 总览卡片、runbook、系统流图、产物目录、测试和最终收尾验证已完成；repo
    既有无关 `tests/test_market_data.py` Black baseline 仍需后续单独处理，不混入本任务
  - 新增 `scripts/run_parameter_governance_summary.py`、`src/ai_trading_system/trading_engine/parameter_governance_summary.py`
    和 `data/derived/weight_iterations/governance/parameter_governance_summary_YYYY-MM-DD.json/md`、`governance/logs/parameter_governance_summary_run_YYYY-MM-DD.json/md`；只读读取
    production profile、current shadow weights、latest 018C2 review、018D proposal、018E1
    preflight、018E2 apply result、018E3 rollback result 和 018F lifecycle audit；输出 governance_state、action_required/action_level、pending_items、promotion
    status、production/shadow weights、safety boundary audit 和 findings；所有输出固定 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`、`safe_for_scheduler=true`；dashboard
    只能读取 summary artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/018F/019、scoring、broker、replay
    或 trading execution
  - '2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 shadow weight / promotion governance
    闭环后新增统一只读治理视图，用于聚合 production/shadow、proposal/preflight/apply/rollback/lifecycle
    状态和人工行动需求；本任务不允许修改 production/shadow、不执行 promotion/apply/rollback、不重跑任何上游 pipeline。2026-05-23:
    从 IN_PROGRESS 改为 VALIDATING，原因：已完成只读 summary builder、CLI、JSON/Markdown/run log、dashboard
    只读卡片、runbook、system flow / artifact catalog 更新和测试；repo 外临时 fixture smoke 成功验证
    `ROLLBACK_COMPLETED`、`PREFLIGHT_READY` 和 `SAFETY_ANOMALY`；目标 pytest、dashboard
    pytest、`tests/trading_engine`、全量 pytest 和 ruff 通过；全仓 Black check 仍只被既有无关 `tests/test_market_data.py`
    baseline 阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 三路径
    smoke；`ROLLBACK_COMPLETED` 输出 `action_required=false`、`safety_boundary_audit.status=PASS`，`PREFLIGHT_READY`
    输出 `pending_apply=true`、`action_level=APPROVAL_REQUIRED`，`SAFETY_ANOMALY` 输出 `action_required=true`、`action_level=URGENT`
    且 critical findings 非空；三路径均确认 summary 顶层 `production_effect=none`、`manual_review_only=true`、`governance_only=true`、`apply_executed_by_governance=false`、`rollback_executed_by_governance=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard
    import guard 验证 Parameter Governance Summary 卡片只读读取 governance summary artifact，不触发
    018B/018C/018C2/018D/018E1/018E2/018E3/018F/019/scoring/broker/replay/trading；收尾验证通过目标
    pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
    仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。'
  docs_links:
  - docs/requirements/parameter_governance_summary_2026-05-23.md
  terminal: true
  raw_row_sha256: 4c8100416356d0f87817bc05bf743c597107f97be535b8460cdf2712789b5d1f
producer_version: arch_005_shadow_registry_compiler.v1
fragment_checksum: e833963a7cd6e28f8af34e474360a7417a96bc650abe67fc529d1ed8b1db86b6
