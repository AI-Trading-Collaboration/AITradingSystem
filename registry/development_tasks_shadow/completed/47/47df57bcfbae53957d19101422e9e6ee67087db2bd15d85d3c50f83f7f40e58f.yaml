schema_version: arch_005_task_shadow_fragment.v1
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
task_record:
  schema_version: task_record.v1
  task_id: TRADING-017
  title: null
  domain: 交易执行/Weight Promotion Gate
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: 系统验证
  requirement_refs:
  - docs/requirements/weight_promotion_gate_2026-05-18.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详细拆解见 `docs/requirements/weight_promotion_gate_2026-05-18.md`；manual-review-only
    promotion gate、policy、standalone script、dashboard 轻量卡片、系统流图、产物目录、测试和当前复验证据已完成；后续真实
    scheduler 干跑和 shadow weight iteration 观察由 `TRADING-018A`、`TRADING-018B` 承接
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: 新增 `outputs/reports/weight_promotion_gate_YYYY-MM-DD.json/md`；读取同日 weight
      adjustment candidates、weight candidate evaluation、shadow parameter impact、paper
      signal quality、可选 replay、daily summary 和 governance/profile metadata；固定 `production_effect=none`、`gate_mode=manual_review_only`；candidate
      级 status 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`BLOCKED`、`NO_CLEAR_IMPROVEMENT`、`CANDIDATE_PROMISING_BUT_LIMITED`、`READY_FOR_MANUAL_REVIEW`；必须阻断缺
      candidate evaluation、candidate blocked、manual approval 缺失、样本/filled 不足、数据质量差、synthetic
      ratio 高、OHLC coverage 低、reconciliation 低、continuous replay 缺失、drawdown/exposure/concentration
      恶化、shadow/paper unreliable、data gate BLOCK 和重大 risk warning；dashboard 只读展示 gate_status、candidate_count、ready_for_manual_review_count、blocked_count、top_candidate_id、main
      blocked_by、report link 和 `production_effect=none`，不触发 gate 重跑；数据质量 gate、目标 pytest、dashboard
      pytest、`tests/trading_engine`、docs freshness、scoped Ruff/Black 和 GitHub Actions
      CI 通过；实现期验收记录保留全量 pytest、ruff 和 black check 结果
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  legacy_source:
    path: docs/task_register_completed.md
    source_partition: completed
    line_number: 301
    row_sha256: a7c90922e08284baf2c4295ec3153d3ec881b27a732d5b58b9eb4af828b2b71b
    cell_count: 8
    all_cells:
    - TRADING-017
    - 交易执行/Weight Promotion Gate
    - P0
    - DONE
    - 系统验证
    - 详细拆解见 `docs/requirements/weight_promotion_gate_2026-05-18.md`；manual-review-only
      promotion gate、policy、standalone script、dashboard 轻量卡片、系统流图、产物目录、测试和当前复验证据已完成；后续真实
      scheduler 干跑和 shadow weight iteration 观察由 `TRADING-018A`、`TRADING-018B` 承接
    - 新增 `outputs/reports/weight_promotion_gate_YYYY-MM-DD.json/md`；读取同日 weight adjustment
      candidates、weight candidate evaluation、shadow parameter impact、paper signal
      quality、可选 replay、daily summary 和 governance/profile metadata；固定 `production_effect=none`、`gate_mode=manual_review_only`；candidate
      级 status 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`BLOCKED`、`NO_CLEAR_IMPROVEMENT`、`CANDIDATE_PROMISING_BUT_LIMITED`、`READY_FOR_MANUAL_REVIEW`；必须阻断缺
      candidate evaluation、candidate blocked、manual approval 缺失、样本/filled 不足、数据质量差、synthetic
      ratio 高、OHLC coverage 低、reconciliation 低、continuous replay 缺失、drawdown/exposure/concentration
      恶化、shadow/paper unreliable、data gate BLOCK 和重大 risk warning；dashboard 只读展示 gate_status、candidate_count、ready_for_manual_review_count、blocked_count、top_candidate_id、main
      blocked_by、report link 和 `production_effect=none`，不触发 gate 重跑；数据质量 gate、目标 pytest、dashboard
      pytest、`tests/trading_engine`、docs freshness、scoped Ruff/Black 和 GitHub Actions
      CI 通过；实现期验收记录保留全量 pytest、ruff 和 black check 结果
    - '2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-015/016 后新增候选权重 promotion
      gate；当前阶段只允许进入人工复核判断，不允许自动晋级、production profile 修改、approved profile 写入、IBKR
      / PaperBroker / replay runner 或交易触发。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增
      `config/weight_promotion_gate_policy.yaml`、`scripts/run_weight_promotion_gate.py`、`weight_promotion_gate_YYYY-MM-DD.json/md`
      生成器、manual-review-only gate、dashboard Weight Promotion Gate 只读卡片、系统流图/产物目录和测试；验证通过目标
      pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。2026-05-18:
      GitHub Actions CI run `25997552867` 对 commit `a86fde5a207a6fffa3471dde46dcc664def184ce`
      完成且通过。2026-06-09: 从 VALIDATING 改为 DONE，原因：本轮 `aits validate-data` 为 `PASS_WITH_WARNINGS`
      / 错误数 0；默认缺输入 smoke 输出 `INSUFFICIENT_DATA`、candidate_count 0、ready_for_manual_review_count
      0、blocked_count 0、`main_blocked_by=missing_weight_candidate_evaluation`、`production_effect=none`；字段级复核确认
      `market_regime=ai_after_chatgpt`、`gate_mode=manual_review_only`、`production_effect=none`，且
      safety boundary 不写 production / reviewed profile、不调用 IBKR / PaperBroker / real
      broker、不跑 replay runner、不触发交易；目标 + dashboard pytest 32 passed，`tests/trading_engine`
      939 passed，scoped safety scan 未命中敏感输出或 forbidden production promotion 语义，归档前代码基线
      GitHub Actions run `27179645657` success。'
    raw_line: '|TRADING-017|交易执行/Weight Promotion Gate|P0|DONE|系统验证|详细拆解见 `docs/requirements/weight_promotion_gate_2026-05-18.md`；manual-review-only
      promotion gate、policy、standalone script、dashboard 轻量卡片、系统流图、产物目录、测试和当前复验证据已完成；后续真实
      scheduler 干跑和 shadow weight iteration 观察由 `TRADING-018A`、`TRADING-018B` 承接|新增
      `outputs/reports/weight_promotion_gate_YYYY-MM-DD.json/md`；读取同日 weight adjustment
      candidates、weight candidate evaluation、shadow parameter impact、paper signal
      quality、可选 replay、daily summary 和 governance/profile metadata；固定 `production_effect=none`、`gate_mode=manual_review_only`；candidate
      级 status 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`BLOCKED`、`NO_CLEAR_IMPROVEMENT`、`CANDIDATE_PROMISING_BUT_LIMITED`、`READY_FOR_MANUAL_REVIEW`；必须阻断缺
      candidate evaluation、candidate blocked、manual approval 缺失、样本/filled 不足、数据质量差、synthetic
      ratio 高、OHLC coverage 低、reconciliation 低、continuous replay 缺失、drawdown/exposure/concentration
      恶化、shadow/paper unreliable、data gate BLOCK 和重大 risk warning；dashboard 只读展示 gate_status、candidate_count、ready_for_manual_review_count、blocked_count、top_candidate_id、main
      blocked_by、report link 和 `production_effect=none`，不触发 gate 重跑；数据质量 gate、目标 pytest、dashboard
      pytest、`tests/trading_engine`、docs freshness、scoped Ruff/Black 和 GitHub Actions
      CI 通过；实现期验收记录保留全量 pytest、ruff 和 black check 结果|2026-05-18: 新增并进入 IN_PROGRESS，原因：owner
      要求在 TRADING-015/016 后新增候选权重 promotion gate；当前阶段只允许进入人工复核判断，不允许自动晋级、production
      profile 修改、approved profile 写入、IBKR / PaperBroker / replay runner 或交易触发。2026-05-18:
      从 IN_PROGRESS 改为 VALIDATING，原因：已新增 `config/weight_promotion_gate_policy.yaml`、`scripts/run_weight_promotion_gate.py`、`weight_promotion_gate_YYYY-MM-DD.json/md`
      生成器、manual-review-only gate、dashboard Weight Promotion Gate 只读卡片、系统流图/产物目录和测试；验证通过目标
      pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。2026-05-18:
      GitHub Actions CI run `25997552867` 对 commit `a86fde5a207a6fffa3471dde46dcc664def184ce`
      完成且通过。2026-06-09: 从 VALIDATING 改为 DONE，原因：本轮 `aits validate-data` 为 `PASS_WITH_WARNINGS`
      / 错误数 0；默认缺输入 smoke 输出 `INSUFFICIENT_DATA`、candidate_count 0、ready_for_manual_review_count
      0、blocked_count 0、`main_blocked_by=missing_weight_candidate_evaluation`、`production_effect=none`；字段级复核确认
      `market_regime=ai_after_chatgpt`、`gate_mode=manual_review_only`、`production_effect=none`，且
      safety boundary 不写 production / reviewed profile、不调用 IBKR / PaperBroker / real
      broker、不跑 replay runner、不触发交易；目标 + dashboard pytest 32 passed，`tests/trading_engine`
      939 passed，scoped safety scan 未命中敏感输出或 forbidden production promotion 语义，归档前代码基线
      GitHub Actions run `27179645657` success。|'
    history_completeness: LEGACY_HISTORY_PARTIAL
    ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-a7c90922e08284baf2c4295e
  task_id: TRADING-017
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: arch_005_shadow_importer
  change_id: ARCH-005-S1-SHADOW-IMPORT
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: DONE
  payload:
    priority: P0
    next_owner: 系统验证
    notes: '2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-015/016 后新增候选权重 promotion
      gate；当前阶段只允许进入人工复核判断，不允许自动晋级、production profile 修改、approved profile 写入、IBKR
      / PaperBroker / replay runner 或交易触发。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增
      `config/weight_promotion_gate_policy.yaml`、`scripts/run_weight_promotion_gate.py`、`weight_promotion_gate_YYYY-MM-DD.json/md`
      生成器、manual-review-only gate、dashboard Weight Promotion Gate 只读卡片、系统流图/产物目录和测试；验证通过目标
      pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。2026-05-18:
      GitHub Actions CI run `25997552867` 对 commit `a86fde5a207a6fffa3471dde46dcc664def184ce`
      完成且通过。2026-06-09: 从 VALIDATING 改为 DONE，原因：本轮 `aits validate-data` 为 `PASS_WITH_WARNINGS`
      / 错误数 0；默认缺输入 smoke 输出 `INSUFFICIENT_DATA`、candidate_count 0、ready_for_manual_review_count
      0、blocked_count 0、`main_blocked_by=missing_weight_candidate_evaluation`、`production_effect=none`；字段级复核确认
      `market_regime=ai_after_chatgpt`、`gate_mode=manual_review_only`、`production_effect=none`，且
      safety boundary 不写 production / reviewed profile、不调用 IBKR / PaperBroker / real
      broker、不跑 replay runner、不触发交易；目标 + dashboard pytest 32 passed，`tests/trading_engine`
      939 passed，scoped safety scan 未命中敏感输出或 forbidden production promotion 语义，归档前代码基线
      GitHub Actions run `27179645657` success。'
  rationale: Lossless legacy row import; historical event time is unavailable.
  evidence_refs:
  - sha256:a7c90922e08284baf2c4295ec3153d3ec881b27a732d5b58b9eb4af828b2b71b
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - TRADING-017
  - 交易执行/Weight Promotion Gate
  - P0
  - DONE
  - 系统验证
  - 详细拆解见 `docs/requirements/weight_promotion_gate_2026-05-18.md`；manual-review-only
    promotion gate、policy、standalone script、dashboard 轻量卡片、系统流图、产物目录、测试和当前复验证据已完成；后续真实
    scheduler 干跑和 shadow weight iteration 观察由 `TRADING-018A`、`TRADING-018B` 承接
  - 新增 `outputs/reports/weight_promotion_gate_YYYY-MM-DD.json/md`；读取同日 weight adjustment
    candidates、weight candidate evaluation、shadow parameter impact、paper signal quality、可选
    replay、daily summary 和 governance/profile metadata；固定 `production_effect=none`、`gate_mode=manual_review_only`；candidate
    级 status 只允许 `INSUFFICIENT_DATA`、`OBSERVE_ONLY`、`BLOCKED`、`NO_CLEAR_IMPROVEMENT`、`CANDIDATE_PROMISING_BUT_LIMITED`、`READY_FOR_MANUAL_REVIEW`；必须阻断缺
    candidate evaluation、candidate blocked、manual approval 缺失、样本/filled 不足、数据质量差、synthetic
    ratio 高、OHLC coverage 低、reconciliation 低、continuous replay 缺失、drawdown/exposure/concentration
    恶化、shadow/paper unreliable、data gate BLOCK 和重大 risk warning；dashboard 只读展示 gate_status、candidate_count、ready_for_manual_review_count、blocked_count、top_candidate_id、main
    blocked_by、report link 和 `production_effect=none`，不触发 gate 重跑；数据质量 gate、目标 pytest、dashboard
    pytest、`tests/trading_engine`、docs freshness、scoped Ruff/Black 和 GitHub Actions
    CI 通过；实现期验收记录保留全量 pytest、ruff 和 black check 结果
  - '2026-05-18: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-015/016 后新增候选权重 promotion
    gate；当前阶段只允许进入人工复核判断，不允许自动晋级、production profile 修改、approved profile 写入、IBKR /
    PaperBroker / replay runner 或交易触发。2026-05-18: 从 IN_PROGRESS 改为 VALIDATING，原因：已新增
    `config/weight_promotion_gate_policy.yaml`、`scripts/run_weight_promotion_gate.py`、`weight_promotion_gate_YYYY-MM-DD.json/md`
    生成器、manual-review-only gate、dashboard Weight Promotion Gate 只读卡片、系统流图/产物目录和测试；验证通过目标
    pytest、`tests/trading_engine`、dashboard 测试、全量 pytest、ruff 和 black check。2026-05-18:
    GitHub Actions CI run `25997552867` 对 commit `a86fde5a207a6fffa3471dde46dcc664def184ce`
    完成且通过。2026-06-09: 从 VALIDATING 改为 DONE，原因：本轮 `aits validate-data` 为 `PASS_WITH_WARNINGS`
    / 错误数 0；默认缺输入 smoke 输出 `INSUFFICIENT_DATA`、candidate_count 0、ready_for_manual_review_count
    0、blocked_count 0、`main_blocked_by=missing_weight_candidate_evaluation`、`production_effect=none`；字段级复核确认
    `market_regime=ai_after_chatgpt`、`gate_mode=manual_review_only`、`production_effect=none`，且
    safety boundary 不写 production / reviewed profile、不调用 IBKR / PaperBroker / real
    broker、不跑 replay runner、不触发交易；目标 + dashboard pytest 32 passed，`tests/trading_engine`
    939 passed，scoped safety scan 未命中敏感输出或 forbidden production promotion 语义，归档前代码基线
    GitHub Actions run `27179645657` success。'
  docs_links:
  - docs/requirements/weight_promotion_gate_2026-05-18.md
  terminal: true
  raw_row_sha256: a7c90922e08284baf2c4295ec3153d3ec881b27a732d5b58b9eb4af828b2b71b
producer_version: arch_005_shadow_registry_compiler.v1
fragment_checksum: 1ccd0efe40e71f2278906893e462d1f8fc5da6c84fd2561255804a7378a507a2
