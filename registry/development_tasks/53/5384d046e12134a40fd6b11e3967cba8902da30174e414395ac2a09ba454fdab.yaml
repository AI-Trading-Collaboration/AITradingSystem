schema_version: arch_005_task_registry_fragment.v1
source_of_truth: ARCH_005_TASK_REGISTRY
stable_task_identity:
  task_id: OPS-070_OBJECTIVE_BLOCKER_AND_CONSUMER_DEPENDENCY_DAG
  task_id_sha256: 5384d046e12134a40fd6b11e3967cba8902da30174e414395ac2a09ba454fdab
task_record:
  task_id: OPS-070_OBJECTIVE_BLOCKER_AND_CONSUMER_DEPENDENCY_DAG
  title: null
  domain: Operations / objective blocker taxonomy and consumer dependency DAG
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: Codex：完成 post-switch runtime self-containment 修复、新 candidate 六类 formal
    gates、promotion、temporary lane cleanup 与唯一 automation 原位切换；operations owner：复核
    deployment acceptance 与下一合法 scheduler run
  requirement_refs:
  - docs/requirements/OPS-070_Objective_Blocker_and_Consumer_Dependency_DAG.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详见 `docs/requirements/OPS-070_Objective_Blocker_and_Consumer_Dependency_DAG.md`。S1-S4
    数据/consumer 隔离、独立 runtime、152,402 files SHA migration、runtime-only exact Git exclude、cross-release
    coordinator policy 与递归 failure-event audit 已实现；candidate `a994a9d03...` 六类 PASS
    且 local/remote main 一致，真实 promotion 已到 `PROMOTED_NOT_ACTIVATED`。激活前发现 runtime
    probe 会继承 coordinator `PYTHONPATH`，且 post-switch live audit 仍依赖 temporary development
    policy；两者均保持 fail-closed 并进入最小 serial contract 修复。
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: 单源失败不阻断无关 source/consumer；缺依赖 consumer 显式 `BLOCKED`；跨 release pre-switch
      dirty inventory 必须由已验证 coordinator policy 解释并审计 exact runtime target；post-switch
      provenance 必须由 exact runtime release 自身 policy 自足审计，临时 development root 仅证明
      Git common-dir 独立且不得成为 scheduler 生命周期依赖；runtime probe 必须移除 `PYTHONPATH`/`PYTHONHOME`
      污染；失败事件 inventory 必须递归；独立 runtime 必须有 runtime-local pinned executable/environment
      fingerprint、owner-reviewed exact release/validation receipt、reviewed exact runtime
      Git exclude、唯一 scheduler entry、最小权限 credential attestation、promotion lock/active-run
      exclusion、失败回滚与 previous release retention；marker/receipt/exclude 缺失或漂移 fail
      closed；dev dirty semantics不放宽；下一合法 run 才验收 PIT/score/dashboard/Reader Brief；无
      weights/broker/trading。
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
  schema_version: task_record.v1
legacy_import_evidence:
  row_sha256: fcc98bfdbbe56f130f4c4db5ecb943a7de5c6558735594529d8205566fb6a2e9
  cell_count: 8
  all_cells:
  - OPS-070_OBJECTIVE_BLOCKER_AND_CONSUMER_DEPENDENCY_DAG
  - Operations / objective blocker taxonomy and consumer dependency DAG
  - P0
  - VALIDATING
  - Codex：完成 post-switch runtime self-containment 修复、新 candidate 六类 formal gates、promotion、temporary
    lane cleanup 与唯一 automation 原位切换；operations owner：复核 deployment acceptance 与下一合法
    scheduler run
  - 详见 `docs/requirements/OPS-070_Objective_Blocker_and_Consumer_Dependency_DAG.md`。S1-S4
    数据/consumer 隔离、独立 runtime、152,402 files SHA migration、runtime-only exact Git exclude、cross-release
    coordinator policy 与递归 failure-event audit 已实现；candidate `a994a9d03...` 六类 PASS
    且 local/remote main 一致，真实 promotion 已到 `PROMOTED_NOT_ACTIVATED`。激活前发现 runtime
    probe 会继承 coordinator `PYTHONPATH`，且 post-switch live audit 仍依赖 temporary development
    policy；两者均保持 fail-closed 并进入最小 serial contract 修复。
  - 单源失败不阻断无关 source/consumer；缺依赖 consumer 显式 `BLOCKED`；跨 release pre-switch dirty
    inventory 必须由已验证 coordinator policy 解释并审计 exact runtime target；post-switch provenance
    必须由 exact runtime release 自身 policy 自足审计，临时 development root 仅证明 Git common-dir
    独立且不得成为 scheduler 生命周期依赖；runtime probe 必须移除 `PYTHONPATH`/`PYTHONHOME` 污染；失败事件
    inventory 必须递归；独立 runtime 必须有 runtime-local pinned executable/environment fingerprint、owner-reviewed
    exact release/validation receipt、reviewed exact runtime Git exclude、唯一 scheduler
    entry、最小权限 credential attestation、promotion lock/active-run exclusion、失败回滚与 previous
    release retention；marker/receipt/exclude 缺失或漂移 fail closed；dev dirty semantics不放宽；下一合法
    run 才验收 PIT/score/dashboard/Reader Brief；无 weights/broker/trading。
  - 2026-07-27：`a994a9d03...` 已 Fast=`340`、Architecture=`747`、Contract=`275`、Integration=`995`、Reproducibility=`23`、Full=`7555
    passed / 5 skipped` 并推送；promotion 首次因 inherited `PYTHONPATH` 回滚，使用无污染调用后到 `PROMOTED_NOT_ACTIVATED`，runtime
    self-audit clean。当前在直接修复 probe environment 与 post-switch policy self-containment；修复须形成新的
    exact-commit 六档证据再 promotion/acceptance，scheduler 仍未激活，`production_effect=none`。
  raw_line: '|OPS-070_OBJECTIVE_BLOCKER_AND_CONSUMER_DEPENDENCY_DAG|Operations / objective
    blocker taxonomy and consumer dependency DAG|P0|VALIDATING|Codex：完成 post-switch
    runtime self-containment 修复、新 candidate 六类 formal gates、promotion、temporary lane
    cleanup 与唯一 automation 原位切换；operations owner：复核 deployment acceptance 与下一合法 scheduler
    run|详见 `docs/requirements/OPS-070_Objective_Blocker_and_Consumer_Dependency_DAG.md`。S1-S4
    数据/consumer 隔离、独立 runtime、152,402 files SHA migration、runtime-only exact Git exclude、cross-release
    coordinator policy 与递归 failure-event audit 已实现；candidate `a994a9d03...` 六类 PASS
    且 local/remote main 一致，真实 promotion 已到 `PROMOTED_NOT_ACTIVATED`。激活前发现 runtime
    probe 会继承 coordinator `PYTHONPATH`，且 post-switch live audit 仍依赖 temporary development
    policy；两者均保持 fail-closed 并进入最小 serial contract 修复。|单源失败不阻断无关 source/consumer；缺依赖
    consumer 显式 `BLOCKED`；跨 release pre-switch dirty inventory 必须由已验证 coordinator
    policy 解释并审计 exact runtime target；post-switch provenance 必须由 exact runtime release
    自身 policy 自足审计，临时 development root 仅证明 Git common-dir 独立且不得成为 scheduler 生命周期依赖；runtime
    probe 必须移除 `PYTHONPATH`/`PYTHONHOME` 污染；失败事件 inventory 必须递归；独立 runtime 必须有 runtime-local
    pinned executable/environment fingerprint、owner-reviewed exact release/validation
    receipt、reviewed exact runtime Git exclude、唯一 scheduler entry、最小权限 credential
    attestation、promotion lock/active-run exclusion、失败回滚与 previous release retention；marker/receipt/exclude
    缺失或漂移 fail closed；dev dirty semantics不放宽；下一合法 run 才验收 PIT/score/dashboard/Reader
    Brief；无 weights/broker/trading。|2026-07-27：`a994a9d03...` 已 Fast=`340`、Architecture=`747`、Contract=`275`、Integration=`995`、Reproducibility=`23`、Full=`7555
    passed / 5 skipped` 并推送；promotion 首次因 inherited `PYTHONPATH` 回滚，使用无污染调用后到 `PROMOTED_NOT_ACTIVATED`，runtime
    self-audit clean。当前在直接修复 probe environment 与 post-switch policy self-containment；修复须形成新的
    exact-commit 六档证据再 promotion/acceptance，scheduler 仍未激活，`production_effect=none`。|'
  history_completeness: LEGACY_HISTORY_PARTIAL
  ambiguous_unescaped_pipe_boundaries: false
events:
- schema_version: task_event.v1
  event_id: legacy-import-fcc98bfdbbe56f130f4c4db5
  task_id: OPS-070_OBJECTIVE_BLOCKER_AND_CONSUMER_DEPENDENCY_DAG
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: devx_006_shadow_v2_importer
  change_id: DEVX-006-TASK-SHADOW-V2
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: VALIDATING
  payload:
    priority: P0
    next_owner: Codex：完成 post-switch runtime self-containment 修复、新 candidate 六类 formal
      gates、promotion、temporary lane cleanup 与唯一 automation 原位切换；operations owner：复核
      deployment acceptance 与下一合法 scheduler run
    notes: 2026-07-27：`a994a9d03...` 已 Fast=`340`、Architecture=`747`、Contract=`275`、Integration=`995`、Reproducibility=`23`、Full=`7555
      passed / 5 skipped` 并推送；promotion 首次因 inherited `PYTHONPATH` 回滚，使用无污染调用后到 `PROMOTED_NOT_ACTIVATED`，runtime
      self-audit clean。当前在直接修复 probe environment 与 post-switch policy self-containment；修复须形成新的
      exact-commit 六档证据再 promotion/acceptance，scheduler 仍未激活，`production_effect=none`。
  rationale: Lossless legacy row import with mutable source location held only in
    the v2 index.
  evidence_refs:
  - sha256:fcc98bfdbbe56f130f4c4db5ecb943a7de5c6558735594529d8205566fb6a2e9
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - OPS-070_OBJECTIVE_BLOCKER_AND_CONSUMER_DEPENDENCY_DAG
  - Operations / objective blocker taxonomy and consumer dependency DAG
  - P0
  - VALIDATING
  - Codex：完成 post-switch runtime self-containment 修复、新 candidate 六类 formal gates、promotion、temporary
    lane cleanup 与唯一 automation 原位切换；operations owner：复核 deployment acceptance 与下一合法
    scheduler run
  - 详见 `docs/requirements/OPS-070_Objective_Blocker_and_Consumer_Dependency_DAG.md`。S1-S4
    数据/consumer 隔离、独立 runtime、152,402 files SHA migration、runtime-only exact Git exclude、cross-release
    coordinator policy 与递归 failure-event audit 已实现；candidate `a994a9d03...` 六类 PASS
    且 local/remote main 一致，真实 promotion 已到 `PROMOTED_NOT_ACTIVATED`。激活前发现 runtime
    probe 会继承 coordinator `PYTHONPATH`，且 post-switch live audit 仍依赖 temporary development
    policy；两者均保持 fail-closed 并进入最小 serial contract 修复。
  - 单源失败不阻断无关 source/consumer；缺依赖 consumer 显式 `BLOCKED`；跨 release pre-switch dirty
    inventory 必须由已验证 coordinator policy 解释并审计 exact runtime target；post-switch provenance
    必须由 exact runtime release 自身 policy 自足审计，临时 development root 仅证明 Git common-dir
    独立且不得成为 scheduler 生命周期依赖；runtime probe 必须移除 `PYTHONPATH`/`PYTHONHOME` 污染；失败事件
    inventory 必须递归；独立 runtime 必须有 runtime-local pinned executable/environment fingerprint、owner-reviewed
    exact release/validation receipt、reviewed exact runtime Git exclude、唯一 scheduler
    entry、最小权限 credential attestation、promotion lock/active-run exclusion、失败回滚与 previous
    release retention；marker/receipt/exclude 缺失或漂移 fail closed；dev dirty semantics不放宽；下一合法
    run 才验收 PIT/score/dashboard/Reader Brief；无 weights/broker/trading。
  - 2026-07-27：`a994a9d03...` 已 Fast=`340`、Architecture=`747`、Contract=`275`、Integration=`995`、Reproducibility=`23`、Full=`7555
    passed / 5 skipped` 并推送；promotion 首次因 inherited `PYTHONPATH` 回滚，使用无污染调用后到 `PROMOTED_NOT_ACTIVATED`，runtime
    self-audit clean。当前在直接修复 probe environment 与 post-switch policy self-containment；修复须形成新的
    exact-commit 六档证据再 promotion/acceptance，scheduler 仍未激活，`production_effect=none`。
  docs_links:
  - docs/requirements/OPS-070_Objective_Blocker_and_Consumer_Dependency_DAG.md
  terminal: false
  canonical_row_sha256: fcc98bfdbbe56f130f4c4db5ecb943a7de5c6558735594529d8205566fb6a2e9
last_event_id: legacy-import-fcc98bfdbbe56f130f4c4db5
producer_version: arch_005_s5_canonical_registry.v1
fragment_checksum: 0d0500842ecd6106c63f334053c4719adbc01bba494c7d6854bf0920b6db7b19
