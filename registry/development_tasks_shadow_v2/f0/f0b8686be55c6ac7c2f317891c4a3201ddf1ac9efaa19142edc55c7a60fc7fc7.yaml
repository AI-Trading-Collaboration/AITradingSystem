schema_version: arch_005_task_shadow_fragment.v2
shadow_only: true
source_of_truth: LEGACY_MARKDOWN
stable_task_identity:
  task_id: TRADING-018E3
  task_id_sha256: f0b8686be55c6ac7c2f317891c4a3201ddf1ac9efaa19142edc55c7a60fc7fc7
task_record:
  task_id: TRADING-018E3
  title: null
  domain: 交易执行/Explicit Approved Shadow Promotion Rollback Command
  parent_task_id: null
  created_at: null
  created_by: null
  priority: P0
  accountable_owner: null
  next_owner: 系统验证
  requirement_refs:
  - docs/requirements/shadow_promotion_rollback_2026-05-23.md
  module_ids: []
  contract_versions: []
  dependencies: []
  unstructured_legacy_blocker_or_next_step: 详细拆解见 `docs/requirements/shadow_promotion_rollback_2026-05-23.md`；explicit
    rollback command、单独 rollback approval、danger flag、apply result / rollback snapshot
    / target profile hash gate、rollback-current snapshot、weights-only restore、dashboard
    只读卡片、runbook、system flow、artifact catalog、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py`
    Black baseline 仍需后续单独处理，不混入本任务
  acceptance_criteria:
  - criterion_id: legacy_markdown
    text: 新增 `scripts/run_shadow_promotion_rollback.py`、`src/ai_trading_system/trading_engine/shadow_promotion_rollback.py`
      和 `data/derived/weight_iterations/promotion/rollback_results/shadow_promotion_rollback_result_YYYY-MM-DD.json/md`、`rollback_results/logs/shadow_promotion_rollback_run_YYYY-MM-DD.json/md`、`promotion/rollback_current_snapshots/production_profile_before_rollback_YYYY-MM-DD.json/.sha256`；只有
      018E2 apply result 为 `APPLIED`、rollback approval 明确授权、rollback snapshot sha256
      匹配、当前 production profile 仍等于 apply 后 hash 且 danger flag 存在时，才原子恢复 target profile
      weights；失败仍生成 result artifact 且未执行时 `production_effect=none`；所有输出固定 `manual_review_only=true`、`safe_for_scheduler=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard
      只读读取 rollback result，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading
  production_effect: UNKNOWN_LEGACY
  broker_action: UNKNOWN_LEGACY
legacy_row_evidence:
  row_sha256: ad449204e6551becc9a0ddd3b83eddaeeca55fde531c6cef542312c2889f681f
  cell_count: 8
  all_cells:
  - TRADING-018E3
  - 交易执行/Explicit Approved Shadow Promotion Rollback Command
  - P0
  - DONE
  - 系统验证
  - 详细拆解见 `docs/requirements/shadow_promotion_rollback_2026-05-23.md`；explicit rollback
    command、单独 rollback approval、danger flag、apply result / rollback snapshot / target
    profile hash gate、rollback-current snapshot、weights-only restore、dashboard 只读卡片、runbook、system
    flow、artifact catalog、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black
    baseline 仍需后续单独处理，不混入本任务
  - 新增 `scripts/run_shadow_promotion_rollback.py`、`src/ai_trading_system/trading_engine/shadow_promotion_rollback.py`
    和 `data/derived/weight_iterations/promotion/rollback_results/shadow_promotion_rollback_result_YYYY-MM-DD.json/md`、`rollback_results/logs/shadow_promotion_rollback_run_YYYY-MM-DD.json/md`、`promotion/rollback_current_snapshots/production_profile_before_rollback_YYYY-MM-DD.json/.sha256`；只有
    018E2 apply result 为 `APPLIED`、rollback approval 明确授权、rollback snapshot sha256
    匹配、当前 production profile 仍等于 apply 后 hash 且 danger flag 存在时，才原子恢复 target profile
    weights；失败仍生成 result artifact 且未执行时 `production_effect=none`；所有输出固定 `manual_review_only=true`、`safe_for_scheduler=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard
    只读读取 rollback result，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading
  - '2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018E2 已能创建 rollback snapshot
    后补齐显式人工批准 rollback command；apply result 不能充当 rollback approval，本任务不允许 scheduler
    或 dashboard 触发 rollback。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 rollback
    builder、CLI、JSON/Markdown/run log、rollback-current snapshot、approval/apply/snapshot/danger/target/post-rollback
    validation、dashboard 只读展示、runbook、system flow / artifact catalog 更新和测试；repo 外
    smoke 成功验证 `ROLLED_BACK` 恢复 weights 并生成 current snapshot，缺 danger flag 输出 `DANGER_FLAG_MISSING`
    且 target profile hash 不变；目标 pytest、dashboard pytest、`tests/trading_engine`、全量
    pytest 和 ruff 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-23:
    从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 成功/失败 smoke；成功路径输出 `rollback_decision=ROLLED_BACK`、`rollback_executed=true`，production
    profile weights 恢复为 rollback snapshot weights，current snapshot 已生成且 snapshot sha256
    等于 rollback 前 production profile sha256，`post_rollback_validation.status=PASS`，`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；失败路径缺
    danger flag 输出 `rollback_decision=DANGER_FLAG_MISSING`、`rollback_executed=false`、`production_effect=none`
    且 production profile sha256 不变；dashboard import guard 验证 Shadow Promotion Rollback
    Result 卡片只读读取 rollback result artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading；收尾验证通过目标
    pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
    仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。'
  raw_line: '|TRADING-018E3|交易执行/Explicit Approved Shadow Promotion Rollback Command|P0|DONE|系统验证|详细拆解见
    `docs/requirements/shadow_promotion_rollback_2026-05-23.md`；explicit rollback
    command、单独 rollback approval、danger flag、apply result / rollback snapshot / target
    profile hash gate、rollback-current snapshot、weights-only restore、dashboard 只读卡片、runbook、system
    flow、artifact catalog、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black
    baseline 仍需后续单独处理，不混入本任务|新增 `scripts/run_shadow_promotion_rollback.py`、`src/ai_trading_system/trading_engine/shadow_promotion_rollback.py`
    和 `data/derived/weight_iterations/promotion/rollback_results/shadow_promotion_rollback_result_YYYY-MM-DD.json/md`、`rollback_results/logs/shadow_promotion_rollback_run_YYYY-MM-DD.json/md`、`promotion/rollback_current_snapshots/production_profile_before_rollback_YYYY-MM-DD.json/.sha256`；只有
    018E2 apply result 为 `APPLIED`、rollback approval 明确授权、rollback snapshot sha256
    匹配、当前 production profile 仍等于 apply 后 hash 且 danger flag 存在时，才原子恢复 target profile
    weights；失败仍生成 result artifact 且未执行时 `production_effect=none`；所有输出固定 `manual_review_only=true`、`safe_for_scheduler=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard
    只读读取 rollback result，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading|2026-05-23:
    新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018E2 已能创建 rollback snapshot 后补齐显式人工批准
    rollback command；apply result 不能充当 rollback approval，本任务不允许 scheduler 或 dashboard
    触发 rollback。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 rollback builder、CLI、JSON/Markdown/run
    log、rollback-current snapshot、approval/apply/snapshot/danger/target/post-rollback
    validation、dashboard 只读展示、runbook、system flow / artifact catalog 更新和测试；repo 外
    smoke 成功验证 `ROLLED_BACK` 恢复 weights 并生成 current snapshot，缺 danger flag 输出 `DANGER_FLAG_MISSING`
    且 target profile hash 不变；目标 pytest、dashboard pytest、`tests/trading_engine`、全量
    pytest 和 ruff 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-23:
    从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 成功/失败 smoke；成功路径输出 `rollback_decision=ROLLED_BACK`、`rollback_executed=true`，production
    profile weights 恢复为 rollback snapshot weights，current snapshot 已生成且 snapshot sha256
    等于 rollback 前 production profile sha256，`post_rollback_validation.status=PASS`，`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；失败路径缺
    danger flag 输出 `rollback_decision=DANGER_FLAG_MISSING`、`rollback_executed=false`、`production_effect=none`
    且 production profile sha256 不变；dashboard import guard 验证 Shadow Promotion Rollback
    Result 卡片只读读取 rollback result artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading；收尾验证通过目标
    pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
    仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。|'
  history_completeness: LEGACY_HISTORY_PARTIAL
  ambiguous_unescaped_pipe_boundaries: false
initial_event:
  schema_version: task_event.v1
  event_id: legacy-import-ad449204e6551becc9a0ddd3
  task_id: TRADING-018E3
  event_type: LEGACY_IMPORT
  occurred_at: null
  actor: devx_006_shadow_v2_importer
  change_id: DEVX-006-TASK-SHADOW-V2
  lane_id: null
  base_commit: 152f2d332b174018ad9fca2ecb568469eaeaf0f8
  previous_state_event_id: null
  from_status: null
  to_status: DONE
  payload:
    priority: P0
    next_owner: 系统验证
    notes: '2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018E2 已能创建 rollback
      snapshot 后补齐显式人工批准 rollback command；apply result 不能充当 rollback approval，本任务不允许
      scheduler 或 dashboard 触发 rollback。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成
      rollback builder、CLI、JSON/Markdown/run log、rollback-current snapshot、approval/apply/snapshot/danger/target/post-rollback
      validation、dashboard 只读展示、runbook、system flow / artifact catalog 更新和测试；repo
      外 smoke 成功验证 `ROLLED_BACK` 恢复 weights 并生成 current snapshot，缺 danger flag 输出
      `DANGER_FLAG_MISSING` 且 target profile hash 不变；目标 pytest、dashboard pytest、`tests/trading_engine`、全量
      pytest 和 ruff 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline
      阻断。2026-05-23: 从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 成功/失败 smoke；成功路径输出
      `rollback_decision=ROLLED_BACK`、`rollback_executed=true`，production profile
      weights 恢复为 rollback snapshot weights，current snapshot 已生成且 snapshot sha256
      等于 rollback 前 production profile sha256，`post_rollback_validation.status=PASS`，`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；失败路径缺
      danger flag 输出 `rollback_decision=DANGER_FLAG_MISSING`、`rollback_executed=false`、`production_effect=none`
      且 production profile sha256 不变；dashboard import guard 验证 Shadow Promotion Rollback
      Result 卡片只读读取 rollback result artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading；收尾验证通过目标
      pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
      仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。'
  rationale: Lossless legacy row import with mutable source location held only in
    the v2 index.
  evidence_refs:
  - sha256:ad449204e6551becc9a0ddd3b83eddaeeca55fde531c6cef542312c2889f681f
  history_completeness: LEGACY_HISTORY_PARTIAL
projection:
  legacy_first_eight_cells:
  - TRADING-018E3
  - 交易执行/Explicit Approved Shadow Promotion Rollback Command
  - P0
  - DONE
  - 系统验证
  - 详细拆解见 `docs/requirements/shadow_promotion_rollback_2026-05-23.md`；explicit rollback
    command、单独 rollback approval、danger flag、apply result / rollback snapshot / target
    profile hash gate、rollback-current snapshot、weights-only restore、dashboard 只读卡片、runbook、system
    flow、artifact catalog、测试和最终收尾验证已完成；repo 既有无关 `tests/test_market_data.py` Black
    baseline 仍需后续单独处理，不混入本任务
  - 新增 `scripts/run_shadow_promotion_rollback.py`、`src/ai_trading_system/trading_engine/shadow_promotion_rollback.py`
    和 `data/derived/weight_iterations/promotion/rollback_results/shadow_promotion_rollback_result_YYYY-MM-DD.json/md`、`rollback_results/logs/shadow_promotion_rollback_run_YYYY-MM-DD.json/md`、`promotion/rollback_current_snapshots/production_profile_before_rollback_YYYY-MM-DD.json/.sha256`；只有
    018E2 apply result 为 `APPLIED`、rollback approval 明确授权、rollback snapshot sha256
    匹配、当前 production profile 仍等于 apply 后 hash 且 danger flag 存在时，才原子恢复 target profile
    weights；失败仍生成 result artifact 且未执行时 `production_effect=none`；所有输出固定 `manual_review_only=true`、`safe_for_scheduler=false`、`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；dashboard
    只读读取 rollback result，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading
  - '2026-05-23: 新增并进入 IN_PROGRESS，原因：owner 要求在 TRADING-018E2 已能创建 rollback snapshot
    后补齐显式人工批准 rollback command；apply result 不能充当 rollback approval，本任务不允许 scheduler
    或 dashboard 触发 rollback。2026-05-23: 从 IN_PROGRESS 改为 VALIDATING，原因：已完成 rollback
    builder、CLI、JSON/Markdown/run log、rollback-current snapshot、approval/apply/snapshot/danger/target/post-rollback
    validation、dashboard 只读展示、runbook、system flow / artifact catalog 更新和测试；repo 外
    smoke 成功验证 `ROLLED_BACK` 恢复 weights 并生成 current snapshot，缺 danger flag 输出 `DANGER_FLAG_MISSING`
    且 target profile hash 不变；目标 pytest、dashboard pytest、`tests/trading_engine`、全量
    pytest 和 ruff 通过；全仓 black check 仅被既有无关 `tests/test_market_data.py` baseline 阻断。2026-05-23:
    从 VALIDATING 改为 DONE，原因：最终收尾验证再次通过 repo 外临时 fixture 成功/失败 smoke；成功路径输出 `rollback_decision=ROLLED_BACK`、`rollback_executed=true`，production
    profile weights 恢复为 rollback snapshot weights，current snapshot 已生成且 snapshot sha256
    等于 rollback 前 production profile sha256，`post_rollback_validation.status=PASS`，`broker_execution=false`、`replay_execution=false`、`trading_execution=false`；失败路径缺
    danger flag 输出 `rollback_decision=DANGER_FLAG_MISSING`、`rollback_executed=false`、`production_effect=none`
    且 production profile sha256 不变；dashboard import guard 验证 Shadow Promotion Rollback
    Result 卡片只读读取 rollback result artifact，不触发 018B/018C/018C2/018D/018E1/018E2/018E3/scoring/broker/replay/trading；收尾验证通过目标
    pytest、dashboard pytest、`tests/trading_engine`、全量 pytest 和 ruff；全仓 Black check
    仍只被既有无关 `tests/test_market_data.py` baseline 阻断，未混入无关格式化 diff。'
  docs_links:
  - docs/requirements/shadow_promotion_rollback_2026-05-23.md
  terminal: true
  raw_row_sha256: ad449204e6551becc9a0ddd3b83eddaeeca55fde531c6cef542312c2889f681f
producer_version: arch_005_shadow_registry_compiler.v2
fragment_checksum: cf43abc8131229651da076e201dc9b4db72344cb6e70c0548cc402db5b86ad66
